//定义此宏，将SA_RIBBON_EXPORT定义为空
#ifndef SA_RIBBON_BAR_NO_EXPORT
#define SA_RIBBON_BAR_NO_EXPORT
#endif
//定义此宏，将SA_COLOR_WIDGETS_API定义为空
#ifndef SA_COLOR_WIDGETS_NO_DLL
#define SA_COLOR_WIDGETS_NO_DLL
#endif


/*** Start of inlined file: SARibbonAmalgamTemplateHeaderGlue.h ***/
// This file provides an extra level of indirection for the @remap in the template
#include "SARibbon.h"

/*** End of inlined file: SARibbonAmalgamTemplateHeaderGlue.h ***/

// disable warnings about unsafe standard library calls
#ifdef _MSC_VER
#pragma push_macro ("_CRT_SECURE_NO_WARNINGS")
#ifndef _CRT_SECURE_NO_WARNINGS
#define _CRT_SECURE_NO_WARNINGS
#endif
#pragma warning (push)
#pragma warning (disable: 4996) // deprecated POSIX names
#endif

// clang-format off

/*** Start of inlined file: qrc_SARibbonResource_Datas.cpp ***/
static const unsigned char qt_resource_data[] = {
  // C:/src/Qt/SARibbon/src/SARibbonBar/resource/ArrowUp.png
  0x0,0x0,0x0,0xfb,
  0x89,
  0x50,0x4e,0x47,0xd,0xa,0x1a,0xa,0x0,0x0,0x0,0xd,0x49,0x48,0x44,0x52,0x0,
  0x0,0x0,0xb,0x0,0x0,0x0,0xa,0x8,0x4,0x0,0x0,0x0,0xc8,0xf9,0x6c,0x8,
  0x0,0x0,0x0,0x4,0x67,0x41,0x4d,0x41,0x0,0x0,0xb1,0x8f,0xb,0xfc,0x61,0x5,
  0x0,0x0,0x0,0x2,0x62,0x4b,0x47,0x44,0x0,0xff,0x87,0x8f,0xcc,0xbf,0x0,0x0,
  0x0,0x7,0x74,0x49,0x4d,0x45,0x7,0xe4,0x7,0x1,0x7,0x1f,0x2d,0xc6,0x57,0xc,
  0x2f,0x0,0x0,0x0,0x2f,0x49,0x44,0x41,0x54,0x8,0xd7,0x63,0xfc,0xcf,0x80,0xd,
  0x30,0x31,0x90,0x22,0xcc,0xc2,0xc0,0xc0,0xc0,0xc0,0x8,0x61,0xff,0x87,0x30,0xfe,
  0xa3,0xa8,0xfe,0xf,0xc5,0x28,0x86,0xfc,0x47,0xa5,0x99,0x50,0x4,0xe1,0x6c,0x46,
  0x6a,0x38,0x10,0x0,0xbd,0x94,0xa,0xd,0xc6,0x7b,0xd7,0x10,0x0,0x0,0x0,0x25,
  0x74,0x45,0x58,0x74,0x64,0x61,0x74,0x65,0x3a,0x63,0x72,0x65,0x61,0x74,0x65,0x0,
  0x32,0x30,0x31,0x37,0x2d,0x31,0x32,0x2d,0x30,0x39,0x54,0x31,0x34,0x3a,0x33,0x36,
  0x3a,0x31,0x36,0x2b,0x30,0x38,0x3a,0x30,0x30,0x70,0x8c,0xd2,0xb,0x0,0x0,0x0,
  0x25,0x74,0x45,0x58,0x74,0x64,0x61,0x74,0x65,0x3a,0x6d,0x6f,0x64,0x69,0x66,0x79,
  0x0,0x32,0x30,0x32,0x30,0x2d,0x30,0x36,0x2d,0x33,0x30,0x54,0x32,0x33,0x3a,0x33,
  0x31,0x3a,0x34,0x35,0x2b,0x30,0x38,0x3a,0x30,0x30,0x8a,0xc5,0x22,0x37,0x0,0x0,
  0x0,0x0,0x49,0x45,0x4e,0x44,0xae,0x42,0x60,0x82,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/Titlebar_Max.svg
  0x0,0x0,0x1,0xd7,
  0x3c,
  0x3f,0x78,0x6d,0x6c,0x20,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x3d,0x22,0x31,0x2e,
  0x30,0x22,0x20,0x73,0x74,0x61,0x6e,0x64,0x61,0x6c,0x6f,0x6e,0x65,0x3d,0x22,0x6e,
  0x6f,0x22,0x3f,0x3e,0xd,0xa,0x3c,0x21,0x44,0x4f,0x43,0x54,0x59,0x50,0x45,0x20,
  0x73,0x76,0x67,0x20,0x50,0x55,0x42,0x4c,0x49,0x43,0x20,0x22,0x2d,0x2f,0x2f,0x57,
  0x33,0x43,0x2f,0x2f,0x44,0x54,0x44,0x20,0x53,0x56,0x47,0x20,0x31,0x2e,0x31,0x2f,
  0x2f,0x45,0x4e,0x22,0x20,0x22,0x68,0x74,0x74,0x70,0x3a,0x2f,0x2f,0x77,0x77,0x77,
  0x2e,0x77,0x33,0x2e,0x6f,0x72,0x67,0x2f,0x47,0x72,0x61,0x70,0x68,0x69,0x63,0x73,
  0x2f,0x53,0x56,0x47,0x2f,0x31,0x2e,0x31,0x2f,0x44,0x54,0x44,0x2f,0x73,0x76,0x67,
  0x31,0x31,0x2e,0x64,0x74,0x64,0x22,0x3e,0xd,0xa,,0xd,
  0xa,,0xd,0xa,
  0x3c,0x2f,0x73,0x76,0x67,0x3e,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/Titlebar_Min.svg
  0x0,0x0,0x1,0xcc,
  0x3c,
  0x3f,0x78,0x6d,0x6c,0x20,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x3d,0x22,0x31,0x2e,
  0x30,0x22,0x20,0x73,0x74,0x61,0x6e,0x64,0x61,0x6c,0x6f,0x6e,0x65,0x3d,0x22,0x6e,
  0x6f,0x22,0x3f,0x3e,0xd,0xa,0x3c,0x21,0x44,0x4f,0x43,0x54,0x59,0x50,0x45,0x20,
  0x73,0x76,0x67,0x20,0x50,0x55,0x42,0x4c,0x49,0x43,0x20,0x22,0x2d,0x2f,0x2f,0x57,
  0x33,0x43,0x2f,0x2f,0x44,0x54,0x44,0x20,0x53,0x56,0x47,0x20,0x31,0x2e,0x31,0x2f,
  0x2f,0x45,0x4e,0x22,0x20,0x22,0x68,0x74,0x74,0x70,0x3a,0x2f,0x2f,0x77,0x77,0x77,
  0x2e,0x77,0x33,0x2e,0x6f,0x72,0x67,0x2f,0x47,0x72,0x61,0x70,0x68,0x69,0x63,0x73,
  0x2f,0x53,0x56,0x47,0x2f,0x31,0x2e,0x31,0x2f,0x44,0x54,0x44,0x2f,0x73,0x76,0x67,
  0x31,0x31,0x2e,0x64,0x74,0x64,0x22,0x3e,0xd,0xa,,0xd,
  0xa,,0xd,0xa,0x3c,0x2f,0x73,0x76,0x67,0x3e,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/ArrowMore.png
  0x0,0x0,0x1,0x1,
  0x89,
  0x50,0x4e,0x47,0xd,0xa,0x1a,0xa,0x0,0x0,0x0,0xd,0x49,0x48,0x44,0x52,0x0,
  0x0,0x0,0xb,0x0,0x0,0x0,0xa,0x8,0x4,0x0,0x0,0x0,0xc8,0xf9,0x6c,0x8,
  0x0,0x0,0x0,0x4,0x67,0x41,0x4d,0x41,0x0,0x0,0xb1,0x8f,0xb,0xfc,0x61,0x5,
  0x0,0x0,0x0,0x2,0x62,0x4b,0x47,0x44,0x0,0xff,0x87,0x8f,0xcc,0xbf,0x0,0x0,
  0x0,0x7,0x74,0x49,0x4d,0x45,0x7,0xe4,0x7,0x1,0x7,0x1f,0x2d,0xc6,0x57,0xc,
  0x2f,0x0,0x0,0x0,0x35,0x49,0x44,0x41,0x54,0x8,0xd7,0x63,0xfc,0xcf,0x80,0xd,
  0x30,0x61,0x15,0x65,0x60,0x61,0x60,0x60,0x60,0x60,0x44,0xd1,0xf2,0x9f,0x11,0xaa,
  0xfa,0x3f,0x23,0xaa,0x20,0xdc,0x10,0x98,0x4,0x8c,0x66,0x42,0x56,0x85,0xd0,0xc5,
  0x88,0xdd,0x25,0x38,0xac,0x64,0x24,0xc9,0xdd,0x0,0x39,0x39,0xd,0x13,0xc6,0xeb,
  0xc,0x98,0x0,0x0,0x0,0x25,0x74,0x45,0x58,0x74,0x64,0x61,0x74,0x65,0x3a,0x63,
  0x72,0x65,0x61,0x74,0x65,0x0,0x32,0x30,0x31,0x37,0x2d,0x31,0x32,0x2d,0x30,0x39,
  0x54,0x31,0x34,0x3a,0x33,0x36,0x3a,0x31,0x36,0x2b,0x30,0x38,0x3a,0x30,0x30,0x70,
  0x8c,0xd2,0xb,0x0,0x0,0x0,0x25,0x74,0x45,0x58,0x74,0x64,0x61,0x74,0x65,0x3a,
  0x6d,0x6f,0x64,0x69,0x66,0x79,0x0,0x32,0x30,0x32,0x30,0x2d,0x30,0x36,0x2d,0x33,
  0x30,0x54,0x32,0x33,0x3a,0x33,0x31,0x3a,0x34,0x35,0x2b,0x30,0x38,0x3a,0x30,0x30,
  0x8a,0xc5,0x22,0x37,0x0,0x0,0x0,0x0,0x49,0x45,0x4e,0x44,0xae,0x42,0x60,0x82,

	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/Titlebar_Close_Hover.svg
  0x0,0x0,0x2,0x5f,
  0x3c,
  0x3f,0x78,0x6d,0x6c,0x20,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x3d,0x22,0x31,0x2e,
  0x30,0x22,0x20,0x73,0x74,0x61,0x6e,0x64,0x61,0x6c,0x6f,0x6e,0x65,0x3d,0x22,0x6e,
  0x6f,0x22,0x3f,0x3e,0xd,0xa,0x3c,0x21,0x44,0x4f,0x43,0x54,0x59,0x50,0x45,0x20,
  0x73,0x76,0x67,0x20,0x50,0x55,0x42,0x4c,0x49,0x43,0x20,0x22,0x2d,0x2f,0x2f,0x57,
  0x33,0x43,0x2f,0x2f,0x44,0x54,0x44,0x20,0x53,0x56,0x47,0x20,0x31,0x2e,0x31,0x2f,
  0x2f,0x45,0x4e,0x22,0x20,0x22,0x68,0x74,0x74,0x70,0x3a,0x2f,0x2f,0x77,0x77,0x77,
  0x2e,0x77,0x33,0x2e,0x6f,0x72,0x67,0x2f,0x47,0x72,0x61,0x70,0x68,0x69,0x63,0x73,
  0x2f,0x53,0x56,0x47,0x2f,0x31,0x2e,0x31,0x2f,0x44,0x54,0x44,0x2f,0x73,0x76,0x67,
  0x31,0x31,0x2e,0x64,0x74,0x64,0x22,0x3e,0xd,0xa,,0xd,
  0xa,,0xd,0xa,0x3c,0x2f,0x73,0x76,0x67,0x3e,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/Titlebar_Close.svg
  0x0,0x0,0x2,0x50,
  0x3c,
  0x3f,0x78,0x6d,0x6c,0x20,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x3d,0x22,0x31,0x2e,
  0x30,0x22,0x20,0x73,0x74,0x61,0x6e,0x64,0x61,0x6c,0x6f,0x6e,0x65,0x3d,0x22,0x6e,
  0x6f,0x22,0x3f,0x3e,0xd,0xa,0x3c,0x21,0x44,0x4f,0x43,0x54,0x59,0x50,0x45,0x20,
  0x73,0x76,0x67,0x20,0x50,0x55,0x42,0x4c,0x49,0x43,0x20,0x22,0x2d,0x2f,0x2f,0x57,
  0x33,0x43,0x2f,0x2f,0x44,0x54,0x44,0x20,0x53,0x56,0x47,0x20,0x31,0x2e,0x31,0x2f,
  0x2f,0x45,0x4e,0x22,0x20,0x22,0x68,0x74,0x74,0x70,0x3a,0x2f,0x2f,0x77,0x77,0x77,
  0x2e,0x77,0x33,0x2e,0x6f,0x72,0x67,0x2f,0x47,0x72,0x61,0x70,0x68,0x69,0x63,0x73,
  0x2f,0x53,0x56,0x47,0x2f,0x31,0x2e,0x31,0x2f,0x44,0x54,0x44,0x2f,0x73,0x76,0x67,
  0x31,0x31,0x2e,0x64,0x74,0x64,0x22,0x3e,0xd,0xa,,0xd,
  0xa,,0xd,0xa,0x3c,0x2f,0x73,0x76,0x67,0x3e,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/Titlebar_Min_Hover.svg
  0x0,0x0,0x1,0xcc,
  0x3c,
  0x3f,0x78,0x6d,0x6c,0x20,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x3d,0x22,0x31,0x2e,
  0x30,0x22,0x20,0x73,0x74,0x61,0x6e,0x64,0x61,0x6c,0x6f,0x6e,0x65,0x3d,0x22,0x6e,
  0x6f,0x22,0x3f,0x3e,0xd,0xa,0x3c,0x21,0x44,0x4f,0x43,0x54,0x59,0x50,0x45,0x20,
  0x73,0x76,0x67,0x20,0x50,0x55,0x42,0x4c,0x49,0x43,0x20,0x22,0x2d,0x2f,0x2f,0x57,
  0x33,0x43,0x2f,0x2f,0x44,0x54,0x44,0x20,0x53,0x56,0x47,0x20,0x31,0x2e,0x31,0x2f,
  0x2f,0x45,0x4e,0x22,0x20,0x22,0x68,0x74,0x74,0x70,0x3a,0x2f,0x2f,0x77,0x77,0x77,
  0x2e,0x77,0x33,0x2e,0x6f,0x72,0x67,0x2f,0x47,0x72,0x61,0x70,0x68,0x69,0x63,0x73,
  0x2f,0x53,0x56,0x47,0x2f,0x31,0x2e,0x31,0x2f,0x44,0x54,0x44,0x2f,0x73,0x76,0x67,
  0x31,0x31,0x2e,0x64,0x74,0x64,0x22,0x3e,0xd,0xa,,0xd,
  0xa,,0xd,0xa,0x3c,0x2f,0x73,0x76,0x67,0x3e,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/Titlebar_Normal_Hover.svg
  0x0,0x0,0x2,0xb7,
  0x3c,
  0x3f,0x78,0x6d,0x6c,0x20,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x3d,0x22,0x31,0x2e,
  0x30,0x22,0x20,0x73,0x74,0x61,0x6e,0x64,0x61,0x6c,0x6f,0x6e,0x65,0x3d,0x22,0x6e,
  0x6f,0x22,0x3f,0x3e,0xd,0xa,0x3c,0x21,0x44,0x4f,0x43,0x54,0x59,0x50,0x45,0x20,
  0x73,0x76,0x67,0x20,0x50,0x55,0x42,0x4c,0x49,0x43,0x20,0x22,0x2d,0x2f,0x2f,0x57,
  0x33,0x43,0x2f,0x2f,0x44,0x54,0x44,0x20,0x53,0x56,0x47,0x20,0x31,0x2e,0x31,0x2f,
  0x2f,0x45,0x4e,0x22,0x20,0x22,0x68,0x74,0x74,0x70,0x3a,0x2f,0x2f,0x77,0x77,0x77,
  0x2e,0x77,0x33,0x2e,0x6f,0x72,0x67,0x2f,0x47,0x72,0x61,0x70,0x68,0x69,0x63,0x73,
  0x2f,0x53,0x56,0x47,0x2f,0x31,0x2e,0x31,0x2f,0x44,0x54,0x44,0x2f,0x73,0x76,0x67,
  0x31,0x31,0x2e,0x64,0x74,0x64,0x22,0x3e,0xd,0xa,,0xd,
  0xa,,0xd,0xa,
  0x3c,0x2f,0x73,0x76,0x67,0x3e,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/Titlebar_Unshade.png
  0x0,0x0,0x1,0x85,
  0x89,
  0x50,0x4e,0x47,0xd,0xa,0x1a,0xa,0x0,0x0,0x0,0xd,0x49,0x48,0x44,0x52,0x0,
  0x0,0x0,0xa,0x0,0x0,0x0,0xa,0x8,0x4,0x0,0x0,0x0,0x27,0x3b,0x7,0x36,
  0x0,0x0,0x0,0x4,0x67,0x41,0x4d,0x41,0x0,0x0,0xb1,0x8f,0xb,0xfc,0x61,0x5,
  0x0,0x0,0x0,0x20,0x63,0x48,0x52,0x4d,0x0,0x0,0x7a,0x25,0x0,0x0,0x80,0x83,
  0x0,0x0,0xf9,0xff,0x0,0x0,0x80,0xe9,0x0,0x0,0x75,0x30,0x0,0x0,0xea,0x60,
  0x0,0x0,0x3a,0x98,0x0,0x0,0x17,0x6f,0x92,0x5f,0xc5,0x46,0x0,0x0,0x0,0x2,
  0x62,0x4b,0x47,0x44,0x0,0xff,0x87,0x8f,0xcc,0xbf,0x0,0x0,0x0,0x9,0x70,0x48,
  0x59,0x73,0x0,0x0,0xb,0x13,0x0,0x0,0xb,0x13,0x1,0x0,0x9a,0x9c,0x18,0x0,
  0x0,0x0,0x7,0x74,0x49,0x4d,0x45,0x7,0xe5,0x8,0x5,0x5,0x1f,0x31,0x17,0xbc,
  0x57,0x2d,0x0,0x0,0x0,0x78,0x49,0x44,0x41,0x54,0x8,0xd7,0x8d,0xce,0x31,0xa,
  0xc2,0x40,0x14,0x6,0xe1,0x79,0xef,0x16,0x56,0x6,0xb,0x49,0x13,0xb0,0x9,0x1e,
  0xc4,0x6b,0x26,0xe4,0x1c,0x76,0xda,0x65,0x85,0xec,0xc6,0xca,0x7a,0xf,0xb0,0xfb,
  0x5b,0x8,0x22,0x68,0xe1,0x94,0x5f,0x35,0x26,0xbe,0x73,0x7e,0x63,0xee,0x3f,0x21,
  0xf7,0x40,0xee,0x26,0xc5,0x50,0x4c,0x88,0x62,0x31,0x4c,0xca,0x1d,0x22,0x5d,0x7,
  0x2d,0xa9,0x58,0xb1,0x25,0xd,0x8a,0x41,0x98,0xa8,0x76,0x9f,0x2f,0xfb,0xb6,0xc2,
  0xec,0x87,0xdb,0xb6,0x75,0x99,0x80,0x6a,0x6b,0x3c,0x37,0x70,0x5c,0x9b,0x9d,0xb,
  0xec,0xb5,0x54,0xed,0x31,0xc2,0xe6,0xe4,0x82,0x37,0xfe,0xf1,0xf9,0x4,0x38,0xee,
  0x34,0x82,0x8d,0xf,0x22,0x2c,0x0,0x0,0x0,0x25,0x74,0x45,0x58,0x74,0x64,0x61,
  0x74,0x65,0x3a,0x63,0x72,0x65,0x61,0x74,0x65,0x0,0x32,0x30,0x32,0x31,0x2d,0x30,
  0x38,0x2d,0x30,0x32,0x54,0x32,0x32,0x3a,0x30,0x38,0x3a,0x31,0x32,0x2b,0x30,0x38,
  0x3a,0x30,0x30,0x99,0xd1,0x32,0x9e,0x0,0x0,0x0,0x25,0x74,0x45,0x58,0x74,0x64,
  0x61,0x74,0x65,0x3a,0x6d,0x6f,0x64,0x69,0x66,0x79,0x0,0x32,0x30,0x32,0x31,0x2d,
  0x30,0x38,0x2d,0x30,0x34,0x54,0x32,0x31,0x3a,0x33,0x31,0x3a,0x34,0x39,0x2b,0x30,
  0x38,0x3a,0x30,0x30,0x29,0xc7,0x98,0x5d,0x0,0x0,0x0,0x0,0x49,0x45,0x4e,0x44,
  0xae,0x42,0x60,0x82,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/define-color.svg
  0x0,0x0,0x8,,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/Titlebar_Max_Hover.svg
  0x0,0x0,0x1,0xd7,
  0x3c,
  0x3f,0x78,0x6d,0x6c,0x20,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x3d,0x22,0x31,0x2e,
  0x30,0x22,0x20,0x73,0x74,0x61,0x6e,0x64,0x61,0x6c,0x6f,0x6e,0x65,0x3d,0x22,0x6e,
  0x6f,0x22,0x3f,0x3e,0xd,0xa,0x3c,0x21,0x44,0x4f,0x43,0x54,0x59,0x50,0x45,0x20,
  0x73,0x76,0x67,0x20,0x50,0x55,0x42,0x4c,0x49,0x43,0x20,0x22,0x2d,0x2f,0x2f,0x57,
  0x33,0x43,0x2f,0x2f,0x44,0x54,0x44,0x20,0x53,0x56,0x47,0x20,0x31,0x2e,0x31,0x2f,
  0x2f,0x45,0x4e,0x22,0x20,0x22,0x68,0x74,0x74,0x70,0x3a,0x2f,0x2f,0x77,0x77,0x77,
  0x2e,0x77,0x33,0x2e,0x6f,0x72,0x67,0x2f,0x47,0x72,0x61,0x70,0x68,0x69,0x63,0x73,
  0x2f,0x53,0x56,0x47,0x2f,0x31,0x2e,0x31,0x2f,0x44,0x54,0x44,0x2f,0x73,0x76,0x67,
  0x31,0x31,0x2e,0x64,0x74,0x64,0x22,0x3e,0xd,0xa,,0xd,
  0xa,,0xd,0xa,
  0x3c,0x2f,0x73,0x76,0x67,0x3e,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/Titlebar_Shade.png
  0x0,0x0,0x1,0x87,
  0x89,
  0x50,0x4e,0x47,0xd,0xa,0x1a,0xa,0x0,0x0,0x0,0xd,0x49,0x48,0x44,0x52,0x0,
  0x0,0x0,0xa,0x0,0x0,0x0,0xa,0x8,0x4,0x0,0x0,0x0,0x27,0x3b,0x7,0x36,
  0x0,0x0,0x0,0x4,0x67,0x41,0x4d,0x41,0x0,0x0,0xb1,0x8f,0xb,0xfc,0x61,0x5,
  0x0,0x0,0x0,0x20,0x63,0x48,0x52,0x4d,0x0,0x0,0x7a,0x25,0x0,0x0,0x80,0x83,
  0x0,0x0,0xf9,0xff,0x0,0x0,0x80,0xe9,0x0,0x0,0x75,0x30,0x0,0x0,0xea,0x60,
  0x0,0x0,0x3a,0x98,0x0,0x0,0x17,0x6f,0x92,0x5f,0xc5,0x46,0x0,0x0,0x0,0x2,
  0x62,0x4b,0x47,0x44,0x0,0xff,0x87,0x8f,0xcc,0xbf,0x0,0x0,0x0,0x9,0x70,0x48,
  0x59,0x73,0x0,0x0,0xb,0x13,0x0,0x0,0xb,0x13,0x1,0x0,0x9a,0x9c,0x18,0x0,
  0x0,0x0,0x7,0x74,0x49,0x4d,0x45,0x7,0xe5,0x8,0x5,0x5,0x1f,0x31,0x17,0xbc,
  0x57,0x2d,0x0,0x0,0x0,0x7a,0x49,0x44,0x41,0x54,0x8,0xd7,0x8d,0x8e,0x21,0xe,
  0x83,0x40,0x14,0x44,0xdf,0xdf,0x70,0x89,0x2a,0x36,0x55,0x35,0x24,0x35,0x84,0x83,
  0x70,0x4d,0x1a,0xce,0x51,0xb9,0xe,0x4,0xbb,0x54,0xd5,0xc2,0x1,0x76,0xa7,0x2,
  0x51,0x51,0x44,0x67,0xc4,0x4b,0xc6,0xbc,0x31,0xf1,0x1b,0x77,0xb2,0x51,0x1d,0x28,
  0xf6,0x7e,0xc0,0xa5,0x77,0x2,0x30,0x1,0xc5,0xd6,0xf8,0xf4,0xd0,0xad,0xfe,0xea,
  0x4,0x15,0x14,0x7b,0x4d,0xc1,0xdf,0xb,0x4,0x6f,0x53,0x7d,0x73,0x42,0xa4,0x30,
  0x68,0x49,0xd9,0xb2,0x2d,0x69,0x50,0xa,0x82,0xbd,0x19,0x15,0xe7,0x6c,0x42,0x64,
  0x8b,0xf3,0xa8,0xbd,0x41,0x6c,0xad,0xf8,0x76,0x6b,0x75,0x88,0xfe,0xfa,0xf9,0x1,
  0xe6,0xd3,0x3a,0x60,0x85,0x9d,0x6f,0x4f,0x0,0x0,0x0,0x25,0x74,0x45,0x58,0x74,
  0x64,0x61,0x74,0x65,0x3a,0x63,0x72,0x65,0x61,0x74,0x65,0x0,0x32,0x30,0x32,0x31,
  0x2d,0x30,0x38,0x2d,0x30,0x32,0x54,0x32,0x32,0x3a,0x30,0x38,0x3a,0x31,0x32,0x2b,
  0x30,0x38,0x3a,0x30,0x30,0x99,0xd1,0x32,0x9e,0x0,0x0,0x0,0x25,0x74,0x45,0x58,
  0x74,0x64,0x61,0x74,0x65,0x3a,0x6d,0x6f,0x64,0x69,0x66,0x79,0x0,0x32,0x30,0x32,
  0x31,0x2d,0x30,0x38,0x2d,0x30,0x34,0x54,0x32,0x31,0x3a,0x33,0x31,0x3a,0x34,0x39,
  0x2b,0x30,0x38,0x3a,0x30,0x30,0x29,0xc7,0x98,0x5d,0x0,0x0,0x0,0x0,0x49,0x45,
  0x4e,0x44,0xae,0x42,0x60,0x82,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/Titlebar_Normal.svg
  0x0,0x0,0x2,0xb7,
  0x3c,
  0x3f,0x78,0x6d,0x6c,0x20,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x3d,0x22,0x31,0x2e,
  0x30,0x22,0x20,0x73,0x74,0x61,0x6e,0x64,0x61,0x6c,0x6f,0x6e,0x65,0x3d,0x22,0x6e,
  0x6f,0x22,0x3f,0x3e,0xd,0xa,0x3c,0x21,0x44,0x4f,0x43,0x54,0x59,0x50,0x45,0x20,
  0x73,0x76,0x67,0x20,0x50,0x55,0x42,0x4c,0x49,0x43,0x20,0x22,0x2d,0x2f,0x2f,0x57,
  0x33,0x43,0x2f,0x2f,0x44,0x54,0x44,0x20,0x53,0x56,0x47,0x20,0x31,0x2e,0x31,0x2f,
  0x2f,0x45,0x4e,0x22,0x20,0x22,0x68,0x74,0x74,0x70,0x3a,0x2f,0x2f,0x77,0x77,0x77,
  0x2e,0x77,0x33,0x2e,0x6f,0x72,0x67,0x2f,0x47,0x72,0x61,0x70,0x68,0x69,0x63,0x73,
  0x2f,0x53,0x56,0x47,0x2f,0x31,0x2e,0x31,0x2f,0x44,0x54,0x44,0x2f,0x73,0x76,0x67,
  0x31,0x31,0x2e,0x64,0x74,0x64,0x22,0x3e,0xd,0xa,,0xd,
  0xa,,0xd,0xa,
  0x3c,0x2f,0x73,0x76,0x67,0x3e,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/ArrowDown.png
  0x0,0x0,0x0,0xfc,
  0x89,
  0x50,0x4e,0x47,0xd,0xa,0x1a,0xa,0x0,0x0,0x0,0xd,0x49,0x48,0x44,0x52,0x0,
  0x0,0x0,0xb,0x0,0x0,0x0,0xa,0x8,0x4,0x0,0x0,0x0,0xc8,0xf9,0x6c,0x8,
  0x0,0x0,0x0,0x4,0x67,0x41,0x4d,0x41,0x0,0x0,0xb1,0x8f,0xb,0xfc,0x61,0x5,
  0x0,0x0,0x0,0x2,0x62,0x4b,0x47,0x44,0x0,0xff,0x87,0x8f,0xcc,0xbf,0x0,0x0,
  0x0,0x7,0x74,0x49,0x4d,0x45,0x7,0xe4,0x7,0x1,0x7,0x1f,0x2d,0xc6,0x57,0xc,
  0x2f,0x0,0x0,0x0,0x30,0x49,0x44,0x41,0x54,0x8,0xd7,0x63,0xfc,0xcf,0x80,0xd,
  0x30,0x31,0x90,0x22,0xcc,0xc2,0xc0,0xc0,0xc0,0xc0,0x88,0x62,0xd2,0x7f,0x46,0xa8,
  0xea,0xff,0x8c,0xa8,0x82,0x70,0x43,0x60,0x12,0x30,0x9a,0x9,0x59,0x15,0x42,0x17,
  0x23,0x35,0x1c,0x8,0x0,0xcb,0xec,0xa,0x13,0xe2,0xe,0x8b,0x76,0x0,0x0,0x0,
  0x25,0x74,0x45,0x58,0x74,0x64,0x61,0x74,0x65,0x3a,0x63,0x72,0x65,0x61,0x74,0x65,
  0x0,0x32,0x30,0x31,0x37,0x2d,0x31,0x32,0x2d,0x30,0x39,0x54,0x31,0x34,0x3a,0x33,
  0x36,0x3a,0x31,0x36,0x2b,0x30,0x38,0x3a,0x30,0x30,0x70,0x8c,0xd2,0xb,0x0,0x0,
  0x0,0x25,0x74,0x45,0x58,0x74,0x64,0x61,0x74,0x65,0x3a,0x6d,0x6f,0x64,0x69,0x66,
  0x79,0x0,0x32,0x30,0x32,0x30,0x2d,0x30,0x36,0x2d,0x33,0x30,0x54,0x32,0x33,0x3a,
  0x33,0x31,0x3a,0x34,0x35,0x2b,0x30,0x38,0x3a,0x30,0x30,0x8a,0xc5,0x22,0x37,0x0,
  0x0,0x0,0x0,0x49,0x45,0x4e,0x44,0xae,0x42,0x60,0x82,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/ribbonPanelOptionButton.png
  0x0,0x0,0x1,0x4b,
  0x89,
  0x50,0x4e,0x47,0xd,0xa,0x1a,0xa,0x0,0x0,0x0,0xd,0x49,0x48,0x44,0x52,0x0,
  0x0,0x0,0x9,0x0,0x0,0x0,0x9,0x4,0x3,0x0,0x0,0x0,0x12,0xbf,0x1b,0x23,
  0x0,0x0,0x0,0x4,0x67,0x41,0x4d,0x41,0x0,0x0,0xb1,0x8f,0xb,0xfc,0x61,0x5,
  0x0,0x0,0x0,0x20,0x63,0x48,0x52,0x4d,0x0,0x0,0x7a,0x26,0x0,0x0,0x80,0x84,
  0x0,0x0,0xfa,0x0,0x0,0x0,0x80,0xe8,0x0,0x0,0x75,0x30,0x0,0x0,0xea,0x60,
  0x0,0x0,0x3a,0x98,0x0,0x0,0x17,0x70,0x9c,0xba,0x51,0x3c,0x0,0x0,0x0,0x12,
  0x50,0x4c,0x54,0x45,0xff,0xff,0xff,0x22,0x22,0x23,0x2d,0x2d,0x2e,0x27,0x27,0x28,
  0x22,0x22,0x23,0xff,0xff,0xff,0x56,0x4e,0xf2,0x3b,0x0,0x0,0x0,0x4,0x74,0x52,
  0x4e,0x53,0x0,0x0,0xe6,0xf3,0x9f,0x3,0x91,0x56,0x0,0x0,0x0,0x1,0x62,0x4b,
  0x47,0x44,0x5,0xf8,0x6f,0xe9,0xc7,0x0,0x0,0x0,0x7,0x74,0x49,0x4d,0x45,0x7,
  0xe6,0x4,0xf,0x6,0x1,0x19,0xd8,0xd7,0x1,0x9a,0x0,0x0,0x0,0x26,0x49,0x44,
  0x41,0x54,0x8,0xd7,0x63,0x60,0x0,0x3,0x26,0x25,0x25,0x5,0x20,0x29,0x28,0x28,
  0x80,0x20,0xd,0x85,0x40,0xa4,0xb0,0x12,0x88,0x14,0x72,0x2,0xa9,0x52,0x52,0x82,
  0xa8,0x66,0x0,0x0,0x37,0xe2,0x2,0x18,0x83,0x28,0x69,0xeb,0x0,0x0,0x0,0x25,
  0x74,0x45,0x58,0x74,0x64,0x61,0x74,0x65,0x3a,0x63,0x72,0x65,0x61,0x74,0x65,0x0,
  0x32,0x30,0x31,0x37,0x2d,0x31,0x32,0x2d,0x30,0x39,0x54,0x31,0x34,0x3a,0x33,0x36,
  0x3a,0x31,0x36,0x2b,0x30,0x38,0x3a,0x30,0x30,0x70,0x8c,0xd2,0xb,0x0,0x0,0x0,
  0x25,0x74,0x45,0x58,0x74,0x64,0x61,0x74,0x65,0x3a,0x6d,0x6f,0x64,0x69,0x66,0x79,
  0x0,0x32,0x30,0x32,0x32,0x2d,0x30,0x34,0x2d,0x31,0x34,0x54,0x32,0x32,0x3a,0x30,
  0x31,0x3a,0x32,0x35,0x2b,0x30,0x38,0x3a,0x30,0x30,0x52,0x37,0x87,0x5,0x0,0x0,
  0x0,0x0,0x49,0x45,0x4e,0x44,0xae,0x42,0x60,0x82,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/theme-base.qss
  0x0,0x0,0x9,0x43,
  0x2f,
  0x2a,0xd,0xa,0x20,0x2a,0x20,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x20,0x54,
  0x68,0x65,0x6d,0x65,0x20,0x42,0x61,0x73,0x65,0x20,0x51,0x53,0x53,0xd,0xa,0x20,
  0x2a,0xd,0xa,0x20,0x2a,0x20,0x43,0x6f,0x6d,0x6d,0x6f,0x6e,0x20,0x73,0x65,0x6c,
  0x65,0x63,0x74,0x6f,0x72,0x73,0x20,0x73,0x68,0x61,0x72,0x65,0x64,0x20,0x61,0x63,
  0x72,0x6f,0x73,0x73,0x20,0x61,0x6c,0x6c,0x20,0x36,0x20,0x74,0x68,0x65,0x6d,0x65,
  0x73,0x20,0x28,0x77,0x69,0x6e,0x37,0x2c,0x20,0x6f,0x66,0x66,0x69,0x63,0x65,0x32,
  0x30,0x31,0x33,0x2c,0x20,0x6f,0x66,0x66,0x69,0x63,0x65,0x32,0x30,0x31,0x36,0x2d,
  0x62,0x6c,0x75,0x65,0x2c,0xd,0xa,0x20,0x2a,0x20,0x6f,0x66,0x66,0x69,0x63,0x65,
  0x32,0x30,0x32,0x31,0x2d,0x62,0x6c,0x75,0x65,0x2c,0x20,0x64,0x61,0x72,0x6b,0x2c,
  0x20,0x64,0x61,0x72,0x6b,0x32,0x29,0x2e,0x20,0x43,0x6f,0x6e,0x74,0x61,0x69,0x6e,
  0x73,0x20,0x6f,0x6e,0x6c,0x79,0x20,0x6e,0x6f,0x6e,0x2d,0x74,0x68,0x65,0x6d,0x65,
  0x2d,0x73,0x70,0x65,0x63,0x69,0x66,0x69,0x63,0x20,0x76,0x61,0x6c,0x75,0x65,0x73,
  0xd,0xa,0x20,0x2a,0x20,0x28,0x74,0x72,0x61,0x6e,0x73,0x70,0x61,0x72,0x65,0x6e,
  0x74,0x2c,0x20,0x6e,0x6f,0x6e,0x65,0x2c,0x20,0x67,0x65,0x6e,0x65,0x72,0x69,0x63,
  0x20,0x73,0x69,0x7a,0x65,0x73,0x29,0x2e,0x20,0x54,0x68,0x65,0x6d,0x65,0x2d,0x73,
  0x70,0x65,0x63,0x69,0x66,0x69,0x63,0x20,0x51,0x53,0x53,0x20,0x66,0x69,0x6c,0x65,
  0x73,0x20,0x6f,0x76,0x65,0x72,0x72,0x69,0x64,0x65,0xd,0xa,0x20,0x2a,0x20,0x63,
  0x6f,0x6c,0x6f,0x72,0x73,0x20,0x61,0x6e,0x64,0x20,0x74,0x68,0x65,0x6d,0x65,0x2d,
  0x64,0x65,0x70,0x65,0x6e,0x64,0x65,0x6e,0x74,0x20,0x70,0x72,0x6f,0x70,0x65,0x72,
  0x74,0x69,0x65,0x73,0x20,0x77,0x68,0x65,0x6e,0x20,0x63,0x6f,0x6e,0x63,0x61,0x74,
  0x65,0x6e,0x61,0x74,0x65,0x64,0x20,0x61,0x66,0x74,0x65,0x72,0x20,0x74,0x68,0x69,
  0x73,0x20,0x62,0x61,0x73,0x65,0x2e,0xd,0xa,0x20,0x2a,0xd,0xa,0x20,0x2a,0x20,
  0x55,0x73,0x61,0x67,0x65,0x3a,0x20,0x4c,0x6f,0x61,0x64,0x20,0x74,0x68,0x69,0x73,
  0x20,0x66,0x69,0x6c,0x65,0x20,0x66,0x69,0x72,0x73,0x74,0x2c,0x20,0x74,0x68,0x65,
  0x6e,0x20,0x6c,0x6f,0x61,0x64,0x20,0x74,0x68,0x65,0x20,0x74,0x68,0x65,0x6d,0x65,
  0x2d,0x73,0x70,0x65,0x63,0x69,0x66,0x69,0x63,0x20,0x51,0x53,0x53,0x20,0x66,0x69,
  0x6c,0x65,0x2e,0xd,0xa,0x20,0x2a,0x2f,0xd,0xa,0xd,0xa,0x2f,0x2a,0x20,0x3d,
  0x3d,0x20,0x46,0x6f,0x63,0x75,0x73,0x20,0x4f,0x75,0x74,0x6c,0x69,0x6e,0x65,0x73,
  0x20,0x3d,0x3d,0xd,0xa,0x20,0x2a,0x20,0x52,0x65,0x6d,0x6f,0x76,0x65,0x20,0x64,
  0x65,0x66,0x61,0x75,0x6c,0x74,0x20,0x66,0x6f,0x63,0x75,0x73,0x20,0x6f,0x75,0x74,
  0x6c,0x69,0x6e,0x65,0x20,0x66,0x72,0x6f,0x6d,0x20,0x61,0x6c,0x6c,0x20,0x77,0x69,
  0x64,0x67,0x65,0x74,0x73,0x20,0xe2,0x80,0x94,0x20,0x74,0x68,0x65,0x6d,0x65,0x73,
  0x20,0x61,0x64,0x64,0x20,0x74,0x68,0x65,0x69,0x72,0x20,0x6f,0x77,0x6e,0xd,0xa,
  0x20,0x2a,0x20,0x66,0x6f,0x63,0x75,0x73,0x20,0x73,0x74,0x79,0x6c,0x65,0x73,0x20,
  0x28,0x65,0x2e,0x67,0x2e,0x20,0x63,0x6f,0x6c,0x6f,0x72,0x65,0x64,0x20,0x62,0x6f,
  0x72,0x64,0x65,0x72,0x73,0x29,0x20,0x77,0x68,0x65,0x72,0x65,0x20,0x6e,0x65,0x65,
  0x64,0x65,0x64,0x2e,0xd,0xa,0x20,0x2a,0x2f,0xd,0xa,0x2a,0x3a,0x66,0x6f,0x63,
  0x75,0x73,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x6f,0x75,0x74,0x6c,0x69,0x6e,
  0x65,0x3a,0x20,0x6e,0x6f,0x6e,0x65,0x3b,0xd,0xa,0x7d,0xd,0xa,0xd,0xa,0x2f,
  0x2a,0x20,0x3d,0x3d,0x20,0x54,0x72,0x61,0x6e,0x73,0x70,0x61,0x72,0x65,0x6e,0x74,
  0x20,0x42,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x20,0x43,0x6f,0x6e,0x74,
  0x61,0x69,0x6e,0x65,0x72,0x73,0x20,0x3d,0x3d,0x20,0x2a,0x2f,0xd,0xa,0x53,0x41,
  0x52,0x69,0x62,0x62,0x6f,0x6e,0x43,0x74,0x72,0x6c,0x43,0x6f,0x6e,0x74,0x61,0x69,
  0x6e,0x65,0x72,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x62,0x61,0x63,0x6b,0x67,
  0x72,0x6f,0x75,0x6e,0x64,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x3a,0x20,0x74,0x72,0x61,
  0x6e,0x73,0x70,0x61,0x72,0x65,0x6e,0x74,0x3b,0xd,0xa,0x7d,0xd,0xa,0xd,0xa,
  0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x42,0x75,0x74,0x74,0x6f,0x6e,0x47,0x72,
  0x6f,0x75,0x70,0x57,0x69,0x64,0x67,0x65,0x74,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,
  0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x2d,0x63,0x6f,0x6c,0x6f,
  0x72,0x3a,0x20,0x74,0x72,0x61,0x6e,0x73,0x70,0x61,0x72,0x65,0x6e,0x74,0x3b,0xd,
  0xa,0x7d,0xd,0xa,0xd,0xa,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x54,0x61,
  0x62,0x42,0x61,0x72,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x62,0x61,0x63,0x6b,
  0x67,0x72,0x6f,0x75,0x6e,0x64,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x3a,0x20,0x74,0x72,
  0x61,0x6e,0x73,0x70,0x61,0x72,0x65,0x6e,0x74,0x3b,0xd,0xa,0x7d,0xd,0xa,0xd,
  0xa,0x2f,0x2a,0x20,0x3d,0x3d,0x20,0x53,0x79,0x73,0x74,0x65,0x6d,0x20,0x54,0x6f,
  0x6f,0x6c,0x20,0x42,0x75,0x74,0x74,0x6f,0x6e,0x73,0x20,0x3d,0x3d,0x20,0x2a,0x2f,
  0xd,0xa,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x53,0x79,0x73,0x74,0x65,0x6d,
  0x54,0x6f,0x6f,0x6c,0x42,0x75,0x74,0x74,0x6f,0x6e,0x20,0x7b,0xd,0xa,0x20,0x20,
  0x20,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x2d,0x63,0x6f,0x6c,
  0x6f,0x72,0x3a,0x20,0x74,0x72,0x61,0x6e,0x73,0x70,0x61,0x72,0x65,0x6e,0x74,0x3b,
  0xd,0xa,0x20,0x20,0x20,0x20,0x62,0x6f,0x72,0x64,0x65,0x72,0x3a,0x20,0x6e,0x6f,
  0x6e,0x65,0x3b,0xd,0xa,0x7d,0xd,0xa,0xd,0xa,0x2f,0x2a,0x20,0x3d,0x3d,0x20,
  0x53,0x65,0x70,0x61,0x72,0x61,0x74,0x6f,0x72,0x20,0x57,0x69,0x64,0x67,0x65,0x74,
  0x20,0x3d,0x3d,0xd,0xa,0x20,0x2a,0x20,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,
  0x53,0x65,0x70,0x61,0x72,0x61,0x74,0x6f,0x72,0x57,0x69,0x64,0x67,0x65,0x74,0x20,
  0x75,0x73,0x65,0x73,0x20,0x74,0x68,0x65,0x20,0x27,0x63,0x6f,0x6c,0x6f,0x72,0x27,
  0x20,0x70,0x72,0x6f,0x70,0x65,0x72,0x74,0x79,0x20,0x61,0x73,0x20,0x69,0x74,0x73,
  0x20,0x76,0x69,0x73,0x75,0x61,0x6c,0x20,0x6c,0x69,0x6e,0x65,0x20,0x63,0x6f,0x6c,
  0x6f,0x72,0x2e,0xd,0xa,0x20,0x2a,0x20,0x54,0x68,0x65,0x20,0x76,0x61,0x6c,0x75,
  0x65,0x20,0x69,0x73,0x20,0x74,0x68,0x65,0x6d,0x65,0x2d,0x73,0x70,0x65,0x63,0x69,
  0x66,0x69,0x63,0x20,0xe2,0x80,0x94,0x20,0x6f,0x76,0x65,0x72,0x72,0x69,0x64,0x65,
  0x20,0x69,0x6e,0x20,0x74,0x68,0x65,0x6d,0x65,0x20,0x51,0x53,0x53,0x2e,0xd,0xa,
  0x20,0x2a,0x2f,0xd,0xa,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x53,0x65,0x70,
  0x61,0x72,0x61,0x74,0x6f,0x72,0x57,0x69,0x64,0x67,0x65,0x74,0x20,0x7b,0xd,0xa,
  0x20,0x20,0x20,0x20,0x2f,0x2a,0x20,0x63,0x6f,0x6c,0x6f,0x72,0x3a,0x20,0x3c,0x74,
  0x68,0x65,0x6d,0x65,0x2d,0x73,0x70,0x65,0x63,0x69,0x66,0x69,0x63,0x3e,0x3b,0x20,
  0x2a,0x2f,0xd,0xa,0x7d,0xd,0xa,0xd,0xa,0x2f,0x2a,0x20,0x3d,0x3d,0x20,0x4d,
  0x65,0x6e,0x75,0x20,0x43,0x6f,0x6d,0x6d,0x6f,0x6e,0x20,0x53,0x74,0x79,0x6c,0x65,
  0x73,0x20,0x3d,0x3d,0x20,0x2a,0x2f,0xd,0xa,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,
  0x6e,0x4d,0x65,0x6e,0x75,0x3a,0x3a,0x69,0x74,0x65,0x6d,0x20,0x7b,0xd,0xa,0x20,
  0x20,0x20,0x20,0x70,0x61,0x64,0x64,0x69,0x6e,0x67,0x3a,0x20,0x35,0x70,0x78,0x3b,
  0xd,0xa,0x20,0x20,0x20,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,
  0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x3a,0x20,0x74,0x72,0x61,0x6e,0x73,0x70,0x61,0x72,
  0x65,0x6e,0x74,0x3b,0xd,0xa,0x7d,0xd,0xa,0xd,0xa,0x53,0x41,0x52,0x69,0x62,
  0x62,0x6f,0x6e,0x4d,0x65,0x6e,0x75,0x3a,0x3a,0x69,0x63,0x6f,0x6e,0x20,0x7b,0xd,
  0xa,0x20,0x20,0x20,0x20,0x6d,0x61,0x72,0x67,0x69,0x6e,0x2d,0x6c,0x65,0x66,0x74,
  0x3a,0x20,0x31,0x70,0x78,0x3b,0xd,0xa,0x7d,0xd,0xa,0xd,0xa,0x2f,0x2a,0x20,
  0x3d,0x3d,0x20,0x47,0x61,0x6c,0x6c,0x65,0x72,0x79,0x20,0x43,0x6f,0x6d,0x6d,0x6f,
  0x6e,0x20,0x53,0x74,0x79,0x6c,0x65,0x73,0x20,0x3d,0x3d,0x20,0x2a,0x2f,0xd,0xa,
  0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x47,0x61,0x6c,0x6c,0x65,0x72,0x79,0x47,
  0x72,0x6f,0x75,0x70,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x73,0x68,0x6f,0x77,
  0x2d,0x64,0x65,0x63,0x6f,0x72,0x61,0x74,0x69,0x6f,0x6e,0x2d,0x73,0x65,0x6c,0x65,
  0x63,0x74,0x65,0x64,0x3a,0x20,0x31,0x3b,0xd,0xa,0x20,0x20,0x20,0x20,0x62,0x61,
  0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x3a,0x20,
  0x74,0x72,0x61,0x6e,0x73,0x70,0x61,0x72,0x65,0x6e,0x74,0x3b,0xd,0xa,0x7d,0xd,
  0xa,0xd,0xa,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x47,0x61,0x6c,0x6c,0x65,
  0x72,0x79,0x47,0x72,0x6f,0x75,0x70,0x3a,0x3a,0x69,0x74,0x65,0x6d,0x3a,0x73,0x65,
  0x6c,0x65,0x63,0x74,0x65,0x64,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x2f,0x2a,
  0x20,0x63,0x6f,0x6c,0x6f,0x72,0x3a,0x20,0x74,0x68,0x65,0x6d,0x65,0x2d,0x73,0x70,
  0x65,0x63,0x69,0x66,0x69,0x63,0x3b,0x20,0x6f,0x76,0x65,0x72,0x72,0x69,0x64,0x65,
  0x20,0x69,0x6e,0x20,0x74,0x68,0x65,0x6d,0x65,0x20,0x51,0x53,0x53,0x20,0x2a,0x2f,
  0xd,0xa,0x7d,0xd,0xa,0xd,0xa,0x2f,0x2a,0x20,0x3d,0x3d,0x20,0x41,0x70,0x70,
  0x6c,0x69,0x63,0x61,0x74,0x69,0x6f,0x6e,0x20,0x42,0x75,0x74,0x74,0x6f,0x6e,0x20,
  0x43,0x6f,0x6d,0x6d,0x6f,0x6e,0x20,0x3d,0x3d,0x20,0x2a,0x2f,0xd,0xa,0x53,0x41,
  0x52,0x69,0x62,0x62,0x6f,0x6e,0x41,0x70,0x70,0x6c,0x69,0x63,0x61,0x74,0x69,0x6f,
  0x6e,0x42,0x75,0x74,0x74,0x6f,0x6e,0x3a,0x3a,0x6d,0x65,0x6e,0x75,0x2d,0x69,0x6e,
  0x64,0x69,0x63,0x61,0x74,0x6f,0x72,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x77,
  0x69,0x64,0x74,0x68,0x3a,0x20,0x30,0x70,0x78,0x3b,0xd,0xa,0x7d,0xd,0xa,0xd,
  0xa,0x2f,0x2a,0x20,0x3d,0x3d,0x20,0x42,0x75,0x74,0x74,0x6f,0x6e,0x47,0x72,0x6f,
  0x75,0x70,0x57,0x69,0x64,0x67,0x65,0x74,0x20,0x54,0x6f,0x6f,0x6c,0x42,0x75,0x74,
  0x74,0x6f,0x6e,0x20,0x43,0x6f,0x6d,0x6d,0x6f,0x6e,0x20,0x3d,0x3d,0x20,0x2a,0x2f,
  0xd,0xa,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x42,0x75,0x74,0x74,0x6f,0x6e,
  0x47,0x72,0x6f,0x75,0x70,0x57,0x69,0x64,0x67,0x65,0x74,0x20,0x3e,0x20,0x51,0x54,
  0x6f,0x6f,0x6c,0x42,0x75,0x74,0x74,0x6f,0x6e,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,
  0x20,0x62,0x6f,0x72,0x64,0x65,0x72,0x3a,0x20,0x6e,0x6f,0x6e,0x65,0x3b,0xd,0xa,
  0x20,0x20,0x20,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x2d,0x63,
  0x6f,0x6c,0x6f,0x72,0x3a,0x20,0x74,0x72,0x61,0x6e,0x73,0x70,0x61,0x72,0x65,0x6e,
  0x74,0x3b,0xd,0xa,0x20,0x20,0x20,0x20,0x70,0x61,0x64,0x64,0x69,0x6e,0x67,0x3a,
  0x20,0x30,0x20,0x32,0x70,0x78,0x3b,0xd,0xa,0x7d,0xd,0xa,0xd,0xa,0x53,0x41,
  0x52,0x69,0x62,0x62,0x6f,0x6e,0x42,0x75,0x74,0x74,0x6f,0x6e,0x47,0x72,0x6f,0x75,
  0x70,0x57,0x69,0x64,0x67,0x65,0x74,0x20,0x3e,0x20,0x51,0x54,0x6f,0x6f,0x6c,0x42,
  0x75,0x74,0x74,0x6f,0x6e,0x5b,0x70,0x6f,0x70,0x75,0x70,0x4d,0x6f,0x64,0x65,0x3d,
  0x22,0x31,0x22,0x5d,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x70,0x61,0x64,0x64,
  0x69,0x6e,0x67,0x2d,0x72,0x69,0x67,0x68,0x74,0x3a,0x20,0x31,0x32,0x70,0x78,0x3b,
  0xd,0xa,0x7d,0xd,0xa,0xd,0xa,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x42,
  0x75,0x74,0x74,0x6f,0x6e,0x47,0x72,0x6f,0x75,0x70,0x57,0x69,0x64,0x67,0x65,0x74,
  0x20,0x3e,0x20,0x51,0x54,0x6f,0x6f,0x6c,0x42,0x75,0x74,0x74,0x6f,0x6e,0x5b,0x70,
  0x6f,0x70,0x75,0x70,0x4d,0x6f,0x64,0x65,0x3d,0x22,0x32,0x22,0x5d,0x20,0x7b,0xd,
  0xa,0x20,0x20,0x20,0x20,0x70,0x61,0x64,0x64,0x69,0x6e,0x67,0x2d,0x72,0x69,0x67,
  0x68,0x74,0x3a,0x20,0x38,0x70,0x78,0x3b,0xd,0xa,0x7d,0xd,0xa,0xd,0xa,0x53,
  0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x42,0x75,0x74,0x74,0x6f,0x6e,0x47,0x72,0x6f,
  0x75,0x70,0x57,0x69,0x64,0x67,0x65,0x74,0x20,0x3e,0x20,0x51,0x54,0x6f,0x6f,0x6c,
  0x42,0x75,0x74,0x74,0x6f,0x6e,0x5b,0x70,0x6f,0x70,0x75,0x70,0x4d,0x6f,0x64,0x65,
  0x3d,0x22,0x30,0x22,0x5d,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x70,0x61,0x64,
  0x64,0x69,0x6e,0x67,0x3a,0x20,0x30,0x20,0x32,0x70,0x78,0x3b,0xd,0xa,0x7d,0xd,
  0xa,0xd,0xa,0x2f,0x2a,0x20,0x3d,0x3d,0x20,0x43,0x61,0x74,0x65,0x67,0x6f,0x72,
  0x79,0x20,0x53,0x63,0x72,0x6f,0x6c,0x6c,0x20,0x42,0x75,0x74,0x74,0x6f,0x6e,0x20,
  0x41,0x72,0x72,0x6f,0x77,0x20,0x54,0x79,0x70,0x65,0x73,0x20,0x3d,0x3d,0x20,0x2a,
  0x2f,0xd,0xa,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x43,0x61,0x74,0x65,0x67,
  0x6f,0x72,0x79,0x53,0x63,0x72,0x6f,0x6c,0x6c,0x42,0x75,0x74,0x74,0x6f,0x6e,0x5b,
  0x61,0x72,0x72,0x6f,0x77,0x54,0x79,0x70,0x65,0x3d,0x22,0x33,0x22,0x5d,0x20,0x7b,
  0xd,0xa,0x20,0x20,0x20,0x20,0x62,0x6f,0x72,0x64,0x65,0x72,0x2d,0x72,0x69,0x67,
  0x68,0x74,0x2d,0x77,0x69,0x64,0x74,0x68,0x3a,0x20,0x31,0x70,0x78,0x3b,0xd,0xa,
  0x7d,0xd,0xa,0xd,0xa,0x53,0x41,0x52,0x69,0x62,0x62,0x6f,0x6e,0x43,0x61,0x74,
  0x65,0x67,0x6f,0x72,0x79,0x53,0x63,0x72,0x6f,0x6c,0x6c,0x42,0x75,0x74,0x74,0x6f,
  0x6e,0x5b,0x61,0x72,0x72,0x6f,0x77,0x54,0x79,0x70,0x65,0x3d,0x22,0x34,0x22,0x5d,
  0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x62,0x6f,0x72,0x64,0x65,0x72,0x2d,0x6c,
  0x65,0x66,0x74,0x2d,0x77,0x69,0x64,0x74,0x68,0x3a,0x20,0x31,0x70,0x78,0x3b,0xd,
  0xa,0x7d,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/templates/office2013.qss
  0x0,0x0,0x5,0x3f,
  0x0,
  0x0,0x1f,0x93,0x78,0x9c,0xe5,0x59,0xcd,0x6e,0xdc,0x36,0x10,0xbe,0xef,0x53,0xa8,
  0xe9,0xa5,0x35,0xa0,0xac,0x9d,0x34,0x17,0x5,0xd,0xb0,0x76,0x8b,0x5c,0xe2,0x36,
  0xce,0x1a,0xcd,0xa1,0x28,0xa,0x4a,0xa2,0xb5,0x44,0x24,0x51,0xa0,0xa8,0x78,0xb7,
  0xc6,0x2,0x2d,0x90,0x4b,0x7e,0x4e,0x39,0xf4,0x90,0x87,0xf0,0xb1,0x48,0xf,0xed,
  0xd3,0xa4,0xd9,0xf6,0x2d,0x4a,0x4a,0x94,0x96,0x14,0xa9,0xdf,0xdd,0xfa,0x52,0xdf,
  0x4c,0x8e,0x66,0xbe,0xf9,0xe6,0x87,0x43,0xee,0xf4,0x60,0x82,0x2f,0x2e,0x90,0x7,
  0xad,0x3b,0x87,0x47,0x77,0xad,0x94,0xae,0x42,0x68,0xd1,0x5,0x8c,0xe0,0xe4,0x60,
  0x3a,0x99,0x4c,0xf,0xe6,0xb3,0x27,0xc8,0x75,0x71,0x3c,0x4b,0x92,0x10,0x79,0x80,
  0x22,0x1c,0x3f,0x45,0x7e,0x0,0x29,0xdb,0x6e,0xdc,0xb3,0xae,0x26,0x16,0xfb,0x73,
  0x81,0xf7,0x2c,0x20,0x38,0x8b,0x7d,0xdb,0xc3,0x21,0x26,0x8e,0x75,0x75,0x75,0xb9,
  0x40,0x14,0xae,0xd7,0xf7,0xb,0x1,0x4c,0x7c,0xc8,0x96,0x53,0x1c,0x22,0xbf,0xbe,
  0x59,0x7d,0x92,0x66,0x2e,0x45,0x34,0xcc,0x37,0xd6,0x32,0xa6,0x63,0x40,0x24,0x14,
  0xec,0xbf,0xb1,0x76,0x5d,0x40,0xec,0x62,0xad,0xbf,0xf1,0x39,0x65,0x56,0xa0,0xaf,
  0x91,0xa1,0xac,0x97,0x80,0x84,0xbd,0xa3,0x84,0x6e,0x6d,0xe6,0x6b,0x5,0x40,0x15,
  0x97,0x4d,0x71,0x62,0x5f,0x22,0x9f,0x2e,0x1c,0xeb,0x30,0x59,0xd6,0xed,0xc2,0x4,
  0x10,0x40,0x31,0xd1,0x2d,0xab,0x3b,0xc2,0xf6,0xd6,0x93,0x72,0x5b,0x73,0xe5,0x4,
  0x50,0x18,0x60,0xb2,0x92,0x74,0x95,0x4b,0x7d,0x18,0x35,0xea,0xfa,0xf0,0xfe,0xf5,
  0xe6,0xdd,0x8b,0x6e,0xd0,0x95,0xa1,0x7,0xd6,0x18,0x3f,0xf8,0x4e,0x4,0x48,0x80,
  0x62,0xce,0x9a,0x63,0xdd,0xe5,0x7c,0x49,0x8b,0x2e,0xa6,0x14,0x47,0x62,0x5d,0x1,
  0xfa,0x18,0xc4,0x30,0x94,0x80,0xe4,0xff,0xf,0x4d,0xa0,0x18,0xc7,0xb0,0x2b,0x61,
  0x2a,0xbf,0x8e,0x33,0x86,0x25,0x7e,0xc8,0xf4,0x26,0x85,0x67,0x8c,0xa3,0x8f,0x6f,
  0x5e,0xfe,0xf3,0xf6,0xfa,0xef,0xeb,0x3f,0x37,0x7f,0x5c,0xcb,0xb9,0x5c,0x17,0x65,
  0xf4,0x9c,0x9d,0x63,0x1c,0x16,0x1b,0x75,0x4a,0x28,0x5c,0xd2,0x6d,0x26,0xad,0xb9,
  0xd1,0x5,0x7e,0xe,0x89,0xb5,0x79,0xf5,0xdb,0xc7,0x9f,0x7f,0xb1,0xfa,0x6b,0x76,
  0x8a,0xef,0x9a,0x69,0xf0,0x70,0x4c,0x61,0x4c,0xed,0x5c,0xce,0x76,0x83,0xc2,0x60,
  0x5f,0xed,0x9,0x81,0x69,0xa,0x7d,0xad,0x2c,0x96,0x55,0x59,0x8,0xbd,0x4a,0x31,
  0xb6,0xe1,0x10,0x1a,0x7,0x23,0xf1,0x16,0x90,0x97,0x69,0xf,0x4f,0x85,0x64,0x65,
  0x61,0x7a,0x70,0xa,0xe3,0xac,0x50,0xf3,0x18,0x27,0x59,0xd2,0x9f,0xdd,0xef,0x13,
  0x2e,0x7f,0x8a,0x7d,0xf8,0xe5,0xad,0xa3,0x5b,0x3f,0x38,0x4e,0xc4,0x34,0xd9,0xee,
  0x30,0xe6,0x47,0x7a,0xdc,0x6a,0xbb,0x16,0x97,0x1,0xd6,0xa5,0x8a,0x3a,0xcb,0x90,
  0xf7,0x6c,0xe6,0x79,0x4c,0x40,0xed,0xcc,0xea,0x46,0xa3,0x11,0x4a,0x40,0x9c,0xb2,
  0xda,0x66,0x66,0x4c,0xb5,0xfa,0x8,0xb8,0x7a,0xc1,0xe6,0x8b,0xfd,0x34,0xb6,0x55,
  0xa9,0xda,0x4,0x1e,0x34,0x97,0x6c,0x4b,0xe6,0xd6,0x1b,0xba,0xee,0xc1,0xb7,0x9,
  0x3f,0x22,0xb,0x9d,0x75,0x47,0xe4,0xbd,0x11,0xfe,0xd4,0x3b,0x40,0xa3,0xee,0x51,
  0x5,0x2e,0xbb,0xac,0x9f,0x48,0xd2,0xf1,0xaf,0xf9,0xa6,0xed,0x29,0x4,0xda,0x4,
  0xf8,0x28,0x4b,0x1d,0xeb,0x4e,0xd9,0xb6,0xdb,0xda,0x6d,0x7e,0x2e,0x86,0xf0,0x82,
  0xea,0x9f,0x49,0x2,0x4,0x5,0xb,0x93,0x84,0xc1,0x55,0xc0,0x52,0x32,0xa6,0x2a,
  0x61,0x1a,0xe0,0x4e,0xc2,0xa,0x2d,0x5,0x5f,0x5d,0xba,0xba,0xeb,0x4c,0x68,0x13,
  0x82,0x5a,0x1e,0x9d,0x3,0x57,0xad,0xad,0x62,0xc1,0x71,0x28,0x70,0xb5,0x53,0x1,
  0xb8,0x36,0xcf,0x8b,0x7a,0x4,0xb7,0x49,0xab,0xe5,0xd3,0x16,0x91,0x21,0xd9,0xe4,
  0x73,0xf6,0xb0,0x76,0xce,0xe6,0xac,0xeb,0xcb,0x3c,0x5a,0x8e,0x75,0xaf,0xe1,0x50,
  0xde,0x4a,0xb3,0x45,0x31,0xf0,0xdc,0x13,0xf9,0x65,0xf0,0xcf,0x49,0x61,0x8,0x3d,
  0x5a,0xd1,0xd7,0xec,0xa8,0xe4,0x45,0x63,0x26,0xb5,0x17,0xaf,0x9c,0xa3,0x85,0x6b,
  0x3d,0xa5,0xb,0x8f,0x7b,0xa,0x97,0x44,0xc8,0xe2,0xd2,0x58,0x65,0xe2,0x20,0x4f,
  0x33,0xe7,0x93,0xff,0x1b,0x15,0x72,0x9,0x54,0x47,0x9a,0x5c,0x6,0xf5,0xd9,0x48,
  0x9f,0xd0,0x6a,0x2d,0xe7,0xb,0xad,0xe5,0xa8,0x3d,0xb4,0xc7,0xd8,0xab,0x1b,0x77,
  0x2e,0xb0,0x97,0xa5,0x2d,0x87,0x44,0xbe,0x5f,0x1f,0x6f,0x6e,0x2,0x98,0x69,0xf2,
  0x6a,0x31,0xe9,0x86,0xcc,0x42,0x73,0xe3,0x28,0x1d,0x59,0xb0,0x84,0x8,0x79,0x52,
  0xec,0x32,0xaa,0x35,0x4f,0x65,0x23,0x88,0xe9,0xcb,0xf9,0x80,0x81,0xcf,0x80,0x53,
  0x39,0x13,0x76,0xe6,0x72,0xe8,0xcc,0x6b,0x9e,0xbd,0x75,0x1a,0xf7,0xc,0x73,0x2c,
  0x8f,0x2a,0xdc,0xa,0xef,0x43,0x10,0x86,0x90,0xac,0x8c,0x55,0x2b,0x9b,0xed,0xa,
  0x8b,0xa2,0xc7,0xe0,0xf2,0x3e,0xb9,0x96,0xda,0x10,0xbf,0x4,0x48,0xd,0x88,0xff,
  0xdb,0x7e,0x2d,0x6b,0xb2,0x54,0xcc,0xe1,0xda,0x9c,0x25,0x83,0x2e,0x44,0x2a,0xcc,
  0x6b,0xc5,0xaa,0xe3,0xb0,0xba,0x8f,0xea,0x87,0xe3,0x98,0x4,0x97,0xb4,0xc9,0x34,
  0x36,0x64,0xc8,0x88,0xd9,0xb1,0x2d,0x10,0xa,0xb9,0x22,0xa6,0x12,0xbf,0x62,0xa5,
  0x25,0xb2,0x35,0x92,0xcc,0x18,0xfb,0x4f,0xd0,0x3a,0x9a,0xfc,0x1a,0xa0,0x43,0xca,
  0x97,0x77,0x1e,0xd9,0x7,0x87,0x5e,0xb6,0x3e,0x3c,0x5,0xd4,0x5e,0x6c,0x8e,0x72,
  0xab,0xad,0x1b,0xab,0x33,0x61,0xfb,0x3b,0x4,0x2f,0x13,0x4c,0xa8,0xce,0x7f,0xb9,
  0xd3,0xfb,0xa9,0x2a,0xbf,0x13,0x7d,0x78,0xff,0xfa,0xec,0x11,0x8a,0xe1,0xd7,0x3e,
  0xa2,0x9b,0x77,0x2f,0x36,0x2f,0x7f,0xff,0xeb,0xcd,0xaf,0xda,0x73,0x10,0xbb,0x41,
  0x97,0x42,0x2d,0xbe,0xa2,0x38,0xc9,0x68,0xa3,0xaf,0xda,0xe0,0x55,0x84,0x89,0x5d,
  0xa,0x6c,0x13,0x56,0x69,0x37,0xd0,0xbf,0x68,0x8b,0x92,0x6,0xb9,0x33,0x48,0x86,
  0x43,0x5c,0x65,0xe8,0x84,0x77,0x8a,0x63,0xbc,0x6c,0x65,0xa8,0x14,0x1a,0xd3,0xfe,
  0xf4,0x3b,0x7f,0x65,0xfb,0x9,0x3b,0xa9,0x70,0xd1,0xd5,0x5b,0xcd,0x4b,0x72,0x7b,
  0x46,0x70,0x82,0x23,0x17,0x77,0x7a,0x2f,0x84,0x76,0xed,0x4c,0xc6,0x1,0x4e,0xb3,
  0x32,0x26,0xa4,0x5d,0xd5,0xad,0x1b,0x81,0x2c,0x79,0x80,0x1b,0xc2,0x9e,0x7,0xc0,
  0xcd,0x67,0x78,0x85,0xd4,0xf1,0x9,0xbb,0xf3,0xfb,0xf8,0xb2,0x8c,0x7d,0x9a,0xb9,
  0xbc,0xa5,0x10,0x1c,0xda,0x98,0x5d,0x57,0x50,0xec,0x58,0x9,0xf0,0x7d,0x14,0x7,
  0xf7,0xeb,0xfb,0x9,0x4e,0x11,0x37,0xc9,0x72,0x0,0x27,0x56,0x7e,0xb7,0x29,0x64,
  0xc4,0x55,0xf4,0x8,0x46,0xa6,0xd7,0xde,0x7e,0x70,0x3a,0x3,0xd5,0x78,0x56,0x8e,
  0x98,0xa0,0x4d,0x40,0x18,0x6,0x1b,0x10,0x82,0x2f,0x5,0x6,0x14,0x81,0x0,0x3a,
  0x56,0x46,0xc2,0xcf,0x9c,0x69,0xf9,0xe1,0x34,0x5f,0x9d,0x32,0xb5,0x38,0x23,0x1e,
  0x9c,0xce,0xf8,0x7,0x5f,0xb1,0x4f,0x6f,0x27,0x71,0xf0,0xf9,0x8e,0xbf,0x3c,0x74,
  0x75,0xe2,0xfa,0x4f,0x0,0x73,0x8f,0x45,0xc5,0x74,0x8b,0x6b,0x7a,0xef,0xe9,0xbe,
  0x5c,0xee,0x7e,0x85,0x32,0x81,0xdb,0xe3,0x74,0x94,0x13,0x7c,0x8a,0xe4,0xab,0xec,
  0x7c,0x95,0xb2,0x3,0x76,0x3b,0xcd,0x7f,0x3a,0x9f,0x31,0x1,0x14,0xa1,0x9f,0xe0,
  0x53,0x14,0xb3,0xb8,0x2a,0xc,0xf5,0xa,0xeb,0x39,0x7f,0xea,0x74,0x1,0xf9,0x91,
  0x29,0xba,0x9d,0x3e,0xe7,0x91,0xe5,0xdf,0x8a,0xc2,0x10,0x97,0x81,0xf5,0x50,0x4,
  0x9d,0x2f,0x63,0xe9,0x2a,0x15,0xf,0xcc,0xa6,0xd7,0xb1,0xbe,0x56,0xba,0xdf,0xcc,
  0x24,0x3b,0xca,0xbb,0x19,0x63,0x16,0x2c,0x3b,0x98,0x5,0xcb,0x3d,0x31,0xb,0x96,
  0x23,0x99,0x35,0x20,0xf8,0xf,0x98,0x35,0x59,0x51,0x6f,0xdb,0xc3,0xfc,0xfd,0x6,
  0x93,0x8,0x84,0xc2,0x65,0x4e,0xf5,0x49,0x88,0x53,0xd8,0x4e,0x76,0x2e,0xb2,0x33,
  0xd3,0xb9,0x96,0x71,0x5c,0x6b,0x0,0xba,0x5f,0xc3,0xf9,0x17,0x5a,0xbf,0xed,0xa5,
  0xba,0xc7,0x6f,0x2a,0x42,0xb9,0x9a,0xb5,0xff,0x2,0xf0,0xa2,0xa,0x82,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/templates/office2021.qss
  0x0,0x0,0x8,0x87,
  0x0,
  0x0,0x2a,0x37,0x78,0x9c,0xcd,0x5a,0x6d,0x6f,0xdc,0x44,0x1e,0x7f,0xbf,0xd2,0x7e,
  0x7,0xc3,0xbd,0x39,0x22,0xdc,0x4d,0x42,0x22,0x21,0x57,0x20,0x85,0x80,0x78,0x43,
  0xf,0x68,0x2a,0x78,0x81,0x4e,0x27,0xaf,0x77,0xba,0xb1,0xea,0xf5,0x18,0xdb,0x4b,
  0x92,0x8b,0x22,0xa5,0xa4,0x90,0xa6,0x5d,0x9a,0x52,0x1d,0x25,0x85,0xf4,0xae,0xed,
  0x41,0xc9,0x1d,0x6a,0x8b,0x28,0xea,0x35,0xf,0x6d,0xbf,0xcc,0xda,0xbb,0xfb,0xea,
  0xbe,0xc2,0xcd,0x78,0xc6,0xde,0x19,0x7b,0x66,0x6d,0x2f,0xa1,0x1c,0x12,0x52,0x3a,
  0x4f,0xff,0xdf,0xff,0xf9,0xc1,0x5b,0x9b,0xa8,0x56,0x3c,0x5f,0x77,0x7d,0xc5,0x35,
  0xeb,0x75,0x68,0x2b,0x1e,0xf0,0xab,0x15,0x78,0xf6,0xac,0x69,0x0,0x65,0x7a,0x72,
  0x7a,0x4a,0xf1,0xfc,0x15,0xb,0x28,0xfe,0x22,0x68,0x81,0x6a,0x65,0xa2,0x56,0xad,
  0x54,0x2b,0xb5,0x89,0x85,0xb9,0xd3,0xd1,0xf1,0x39,0xc7,0xb1,0x4c,0x43,0xf7,0x4d,
  0x68,0x7f,0x68,0x36,0x9a,0xc0,0xc7,0x7,0xa4,0x9b,0xca,0x6a,0xb5,0xa2,0xa0,0xff,
  0xea,0xba,0x71,0xae,0xe9,0xc2,0xb6,0xdd,0x50,0xd,0x68,0x41,0x57,0x53,0x56,0x57,
  0x75,0xc3,0x0,0xb6,0xbf,0xb6,0x76,0x92,0x1e,0x81,0x6e,0x3,0xa0,0xd,0xf,0x5a,
  0x66,0x23,0xbb,0x9d,0x5c,0xf3,0xc1,0xb2,0x4f,0x1e,0x89,0xf6,0xd6,0x78,0x78,0x6f,
  0xe8,0x2e,0xb,0x8,0xfd,0x93,0x40,0xc8,0x5,0x30,0x92,0x7c,0x51,0xe2,0x6d,0xdf,
  0x87,0xf6,0xdb,0x88,0x8c,0x43,0xd8,0x7f,0x39,0xde,0x59,0x58,0xf1,0x7c,0xd0,0x22,
  0xfb,0x8,0x53,0x70,0xad,0x73,0xda,0x6c,0x2e,0xfa,0xcc,0x85,0xc1,0xc6,0x93,0x70,
  0xe7,0x41,0xf7,0xc9,0xb3,0xde,0xdf,0xf6,0xc2,0x7b,0xdf,0x85,0x57,0x7e,0xe8,0x1e,
  0x3e,0xfa,0xef,0x51,0xc7,0xd1,0x6d,0x1b,0x58,0x83,0xcd,0xce,0xe0,0xe6,0x9d,0xde,
  0x37,0x17,0xc2,0xce,0xd6,0xe0,0xda,0xfd,0xde,0xe1,0x85,0xee,0xfe,0x3f,0xb2,0xe7,
  0x39,0xd6,0xd3,0x60,0x64,0x82,0xf0,0x5d,0xdd,0xf6,0x1c,0xdd,0x45,0xec,0xe,0x59,
  0x52,0xa4,0xcf,0x74,0x1f,0x5f,0x26,0x20,0xfa,0xf7,0x9f,0xf6,0x9e,0xdc,0x1f,0x4d,
  0xf2,0x75,0xe5,0xfd,0x33,0x10,0x5a,0x64,0x23,0x31,0x6,0x2a,0x6a,0x1b,0xda,0x80,
  0x53,0xbe,0xea,0xea,0xd,0xb3,0xed,0x69,0xca,0x8c,0xb3,0x9c,0xa3,0x76,0xb1,0x51,
  0xf1,0xac,0xe0,0x33,0x8e,0xde,0x68,0x98,0x76,0x53,0x53,0x26,0x95,0xe9,0xe8,0xd1,
  0xb5,0x88,0xbb,0x45,0xf8,0x9,0x70,0x95,0xde,0xa5,0x47,0xe1,0xfa,0x79,0xa5,0x4,
  0xb,0x1a,0xb9,0xb8,0x9a,0x83,0x5a,0x64,0x6d,0x6,0xb4,0x7d,0x84,0x4b,0x8d,0x5e,
  0x50,0xeb,0xcd,0xd8,0x80,0x8a,0x52,0x76,0x5c,0xe0,0x79,0xa0,0x91,0x16,0xe2,0x94,
  0xb3,0x9c,0xd8,0xac,0x90,0xc4,0xd8,0x40,0x29,0xc1,0x31,0xa0,0x1a,0x8b,0xc0,0x38,
  0x37,0x1a,0xaa,0x48,0x9d,0xc7,0x3,0x32,0xd2,0x6f,0xb8,0xbb,0xd5,0xdf,0xde,0xd,
  0xbe,0xf8,0x2a,0x71,0x99,0x48,0xcb,0xb5,0x89,0x53,0xc0,0x6e,0x13,0x94,0xef,0x41,
  0xa7,0xed,0x94,0x50,0xfd,0x47,0xe,0xbe,0x70,0xa,0x36,0xc0,0x6b,0x2f,0x4e,0xbd,
  0xf8,0xe7,0x98,0x39,0x6a,0x60,0xaa,0x8b,0xfd,0x19,0xf1,0x28,0xb5,0x32,0x55,0x49,
  0xd1,0x56,0xc2,0xbd,0xdb,0xc1,0xd1,0x36,0x42,0x48,0xa0,0x12,0x9c,0x83,0x8d,0xbd,
  0xe0,0xe2,0xe7,0x65,0x6c,0x32,0x5,0x4c,0xd3,0x5a,0x88,0x8e,0x5a,0x17,0xd9,0x6b,
  0x29,0x41,0x22,0x6,0x62,0x9b,0xfb,0x5d,0x59,0x48,0x1b,0x7e,0x59,0x26,0x7a,0xdf,
  0xfe,0x12,0x5e,0xf9,0x3e,0x38,0xda,0xf,0x36,0xf,0x28,0xcc,0x8,0x35,0xa,0xae,
  0xc1,0xf9,0xed,0xde,0xf,0x87,0xaf,0x3a,0xcb,0xe8,0xef,0xfe,0xfd,0x7f,0xe1,0xd0,
  0x76,0x79,0x8b,0x46,0xb7,0x8d,0x27,0xbd,0xdd,0xcb,0xc1,0xc5,0x9f,0xc6,0xe5,0x63,
  0x5a,0x6a,0x23,0xaf,0x32,0x26,0x12,0x7e,0x7d,0x2b,0x65,0xa8,0xdd,0x67,0x37,0xc3,
  0xce,0xf9,0xe0,0xa7,0xed,0xfe,0xd3,0xfd,0xfe,0x7f,0x6e,0x8e,0x4b,0x7e,0x32,0x43,
  0x9e,0x8b,0x81,0x7c,0xe2,0x9a,0xf7,0x5d,0x6b,0x1e,0xc9,0x50,0x37,0x6d,0xc0,0xe5,
  0x4f,0x6e,0xa3,0x54,0x2,0x19,0xbe,0xb1,0x0,0xd0,0x9e,0xee,0x43,0x97,0x40,0xae,
  0x56,0xc2,0x87,0x7b,0xe1,0x5,0x2c,0x7e,0xc9,0x9,0x6c,0x4e,0x1b,0x9d,0xf0,0xc6,
  0x83,0xc1,0x9d,0xdd,0xfe,0xd6,0xcf,0x49,0x9a,0x8b,0xc8,0x55,0x2b,0xc1,0xdd,0x4f,
  0xc3,0xbf,0xef,0x86,0x1b,0xf,0x7b,0x87,0x37,0xba,0x8f,0xbf,0x8,0xae,0x76,0xd0,
  0x5,0x64,0x73,0x28,0xff,0x21,0x5,0xa2,0xbf,0x91,0xf1,0x5,0x5b,0x3f,0xf7,0xe,
  0x9e,0x91,0xc,0x45,0xce,0xc,0xfe,0x79,0x25,0xbc,0x75,0x84,0x75,0xbe,0xfd,0xa0,
  0x7b,0xf8,0x3d,0x79,0x11,0x3d,0x95,0x70,0xaa,0xfb,0xa0,0x9,0xdd,0x15,0x24,0x51,
  0x39,0xac,0xf0,0xfa,0x3e,0x35,0x9c,0xed,0x4f,0xfb,0xeb,0x1b,0x51,0x89,0xa4,0x36,
  0x74,0xf7,0xdc,0xf4,0x89,0x8f,0x3d,0x8f,0x14,0x4b,0x92,0xdb,0x44,0x78,0x89,0xc5,
  0x7a,0xf1,0xae,0xa8,0x8e,0x88,0xb1,0x70,0x9a,0xa0,0x6b,0xda,0x59,0x68,0xb4,0x3d,
  0xf2,0x1a,0x6c,0xfb,0x16,0x52,0x4c,0x92,0x49,0xa3,0x67,0x32,0xc,0x49,0xeb,0x9f,
  0xd8,0x6b,0xf8,0xd8,0x99,0x7d,0x81,0x48,0xf5,0xb7,0x52,0xa7,0x88,0x49,0xb9,0x12,
  0x46,0x8a,0x51,0x51,0x5a,0xba,0xdb,0x34,0x6d,0xd5,0x87,0x8e,0xf6,0xa,0xcd,0x1f,
  0x74,0xa9,0xe,0x91,0x87,0xb4,0xe8,0x6a,0x4a,0xde,0xef,0xe9,0xa8,0xc4,0x22,0x85,
  0x2e,0xb7,0x54,0x58,0x74,0x82,0x9a,0x66,0x8,0xb1,0x5d,0xf7,0x4d,0xdf,0x2,0x43,
  0x19,0xf3,0x34,0x5e,0x97,0x97,0x5a,0x94,0xbe,0x20,0x7d,0xd2,0x5c,0x29,0xad,0x44,
  0xa3,0xa7,0xdf,0xd1,0xeb,0xe8,0xfd,0xe8,0x4f,0x1c,0xd9,0x6e,0x6d,0xe,0xee,0xec,
  0x84,0xb7,0xb6,0x5,0x8c,0x92,0x93,0xd2,0x0,0x9b,0x2d,0xab,0x46,0x70,0x97,0x2,
  0xf1,0xae,0x83,0xbb,0x1,0xc2,0x9a,0x80,0x32,0xbb,0x5d,0x2c,0xbe,0xe4,0x54,0xe3,
  0xd2,0xd7,0xd9,0x54,0x58,0xb8,0x3e,0x1b,0x4a,0x7f,0x92,0x8d,0x9c,0x62,0x77,0x5d,
  0x30,0x5c,0x68,0x59,0x22,0x56,0x45,0x27,0x58,0xe5,0xc6,0x45,0xcf,0x34,0xb5,0xd9,
  0x12,0xc5,0x5d,0x51,0xc7,0x1e,0x5,0xe5,0x23,0xdd,0x75,0xe1,0xd2,0x99,0x15,0x7,
  0xe5,0x8d,0x57,0xe2,0xbc,0x11,0x23,0xc3,0x39,0x4b,0x5d,0x32,0x1b,0xfe,0x62,0x84,
  0xa7,0xec,0x7b,0x33,0xa9,0xf7,0x2c,0x70,0xb6,0xec,0x73,0x63,0x69,0x2e,0x93,0xe3,
  0x16,0x7c,0x1d,0x17,0xa5,0xd9,0xa6,0x95,0xdb,0x48,0xbb,0x9c,0x9f,0x91,0x7f,0xca,
  0x32,0x70,0xac,0x89,0xf9,0x99,0xcc,0xf2,0xc3,0x3d,0x9e,0x1b,0xbb,0x85,0x6d,0xf6,
  0xd0,0xa0,0xa4,0x9b,0x5,0x6c,0x29,0x1b,0x98,0x32,0x25,0x78,0xbe,0xe7,0xc5,0x85,
  0xc,0x56,0x62,0xcc,0x6e,0xa6,0xbc,0x49,0x2f,0xe3,0x60,0xac,0xa8,0xb3,0xa9,0x55,
  0x1a,0x8f,0xe3,0xd,0x4e,0x68,0x19,0xfe,0x58,0xb,0x40,0x45,0x53,0xf7,0xf1,0x25,
  0x9c,0x93,0xee,0x6d,0xd,0x7e,0xdc,0x9,0xaf,0x6f,0x6,0xf7,0xbe,0x46,0x39,0x1e,
  0x15,0x72,0xc1,0xa5,0xbd,0xa8,0x5c,0x62,0x75,0xa3,0xcd,0x26,0x3e,0x94,0xe6,0x86,
  0x1e,0x8a,0x91,0xcc,0x32,0xce,0x46,0xba,0x7f,0x62,0x51,0xbc,0xba,0x9,0x93,0x89,
  0x34,0x19,0xab,0xd6,0x44,0x59,0x38,0xcb,0xa,0x57,0xce,0x3e,0x47,0x66,0x8e,0x99,
  0x8d,0xa2,0x65,0x48,0xf6,0x26,0x29,0xee,0x4d,0xbb,0x81,0x97,0x61,0xa2,0x55,0x94,
  0x49,0xb0,0x8b,0x21,0xb7,0x57,0x1d,0xe8,0x99,0xf8,0x82,0xa6,0x44,0x30,0x4f,0x12,
  0x31,0x10,0x27,0x9b,0x14,0x25,0xf0,0x33,0x7a,0x3d,0x35,0xf8,0x21,0x2b,0x25,0x2a,
  0xd6,0xf4,0x55,0x4d,0xf3,0xf5,0x7a,0xaa,0xda,0xc8,0x7a,0x4c,0x3a,0xeb,0xf,0x49,
  0x65,0x9d,0x87,0xa9,0x4d,0x12,0x1f,0xa1,0x6b,0xd4,0x73,0x66,0xf9,0x55,0xe2,0x65,
  0xb3,0xc2,0x3a,0x66,0xf8,0x2,0x5a,0xa3,0xe1,0x67,0x36,0xed,0x79,0x63,0xb8,0xe9,
  0x8c,0xcc,0x4d,0x67,0xb2,0x6e,0xca,0x8,0x4a,0xf3,0x80,0x5,0xc,0x3f,0x36,0xeb,
  0xa1,0xc4,0xf4,0xba,0xca,0x99,0x5f,0x6d,0x62,0x70,0xfb,0x11,0x2a,0xd6,0xfb,0x77,
  0xcf,0x47,0xe5,0xfb,0x7a,0xf7,0xf1,0xbf,0x7,0xeb,0xdb,0xe1,0xce,0x15,0x54,0x26,
  0x12,0x71,0x22,0x3f,0x8,0xe,0xee,0xe2,0x22,0xfb,0xea,0xdd,0xe0,0xe2,0xd,0xea,
  0x13,0x47,0xdf,0x4,0x57,0xbf,0x44,0xbe,0x42,0x5a,0xb6,0x8c,0x63,0xcc,0x14,0x75,
  0x8c,0x19,0x76,0x8,0xc1,0xa3,0x93,0x32,0x17,0x85,0x1,0xed,0x5,0x9e,0xc7,0xff,
  0x53,0x4e,0xb8,0x98,0x35,0xb2,0x52,0xe2,0x1c,0x28,0xe9,0x21,0xc9,0x98,0x44,0x61,
  0x4,0x91,0x6c,0xa5,0xa,0xf9,0xe0,0xd9,0x67,0x83,0xdb,0x87,0xc1,0x35,0x3c,0xa2,
  0x4,0x56,0x78,0xf1,0x7a,0x7f,0xfd,0x33,0x83,0xa6,0x6f,0xdc,0x91,0x45,0x87,0x90,
  0x5c,0xfa,0x9b,0xbf,0x54,0x2b,0x28,0xa6,0xa1,0x8e,0x3a,0x6e,0xc8,0xe,0x18,0xd6,
  0x12,0xf1,0x4,0xbb,0x7b,0x73,0x6,0x76,0x7b,0x3c,0x64,0x20,0x2d,0x3a,0xa,0x8c,
  0x68,0x9b,0x44,0xc8,0xa0,0x73,0x80,0x1a,0xbb,0xa4,0x4d,0x46,0x8d,0x73,0xf8,0xf0,
  0xab,0x70,0xe7,0x69,0xef,0xbb,0x3,0x85,0xef,0x22,0xd2,0x13,0x47,0x3e,0x2f,0xce,
  0x14,0xa8,0xb1,0x92,0x1c,0x5f,0x26,0x59,0x8e,0xae,0xbb,0x64,0x93,0xbc,0xe3,0x0,
  0x57,0xb7,0x10,0x98,0x2,0xb8,0x44,0xc3,0x32,0x1,0x3e,0xb6,0xdc,0x3a,0x6,0x74,
  0x12,0xea,0x65,0x6b,0x39,0x1,0x50,0x6e,0xce,0x58,0x68,0xca,0x28,0xe6,0x67,0x3c,
  0x2d,0x17,0x94,0x26,0x5,0x99,0x95,0xea,0xef,0xc,0x75,0x8,0xf6,0x6d,0xdd,0xb2,
  0x80,0xbb,0x92,0x75,0x99,0x1c,0xd,0x17,0x73,0x1,0x21,0x95,0x3c,0x69,0x8c,0xd1,
  0xec,0x64,0x2d,0x66,0x18,0xde,0x28,0x6d,0xbe,0x29,0xa3,0x8b,0xc7,0x0,0xa2,0x54,
  0x83,0x9a,0x41,0x15,0x35,0xfc,0x42,0x68,0xd1,0xe,0xc1,0xe7,0x2d,0xc2,0x25,0xb5,
  0x1,0xc,0xe8,0x46,0xa5,0x94,0x1a,0xe7,0x22,0x84,0xf8,0x57,0x82,0x2a,0xc1,0xfb,
  0x9a,0xc,0xa3,0xa6,0x99,0x3e,0x68,0xa5,0x8a,0x80,0x12,0x4d,0xb6,0x20,0x92,0xe5,
  0xd1,0x7a,0x1e,0x6,0x24,0x27,0x3f,0xba,0x30,0xa4,0x7a,0xe6,0x2e,0x7f,0x60,0x82,
  0x25,0x7,0xba,0xbe,0x50,0xd1,0xf1,0xa6,0x5c,0x70,0x4b,0x8b,0x88,0xac,0xc8,0x88,
  0x70,0xb2,0xe4,0xdf,0xc4,0x2b,0xb9,0x85,0x6b,0x99,0x99,0x16,0x2b,0x57,0x32,0x98,
  0x8f,0x36,0xb2,0x72,0xc2,0x94,0x59,0xf9,0x24,0x83,0x67,0xdc,0x8d,0x30,0xff,0x17,
  0xb2,0x58,0xc9,0xcb,0xe3,0x1a,0x99,0xec,0x39,0xc6,0x8e,0x4a,0xe7,0x53,0xc9,0xa8,
  0xa8,0x9c,0x1f,0x51,0x2c,0x46,0x1c,0x7a,0xb9,0xe2,0x7f,0x8a,0x9f,0xd7,0xc7,0xb3,
  0xbc,0xf7,0xdf,0x41,0xfd,0xd6,0x5b,0xd,0x13,0xcf,0x56,0x7b,0x5b,0xfb,0x41,0xe7,
  0x3a,0x5b,0xff,0xc4,0x73,0xc5,0xe4,0x94,0xdc,0x4d,0x4c,0xdb,0x69,0xfb,0xac,0x3e,
  0xf9,0x16,0x86,0xb5,0x3b,0x14,0x83,0x22,0xc9,0xe3,0xe8,0x23,0x12,0xa,0xb3,0xdb,
  0xcc,0x5c,0x10,0x3b,0xb8,0x14,0x30,0xd1,0xca,0xaa,0xfc,0xf3,0x21,0xc1,0x1d,0xf5,
  0xa0,0xac,0x53,0x24,0xf2,0x99,0xc7,0x49,0xf7,0xd,0xb8,0x3c,0x5a,0x3e,0xf1,0xa9,
  0x55,0x76,0xa8,0x59,0xec,0x53,0xb3,0xf0,0x9b,0x47,0x42,0xfe,0x34,0x4a,0xda,0x30,
  0x29,0x9a,0x47,0x20,0x60,0xe,0x1e,0x3b,0x88,0x79,0xd8,0xaa,0xc3,0x7c,0x19,0xd0,
  0x53,0x63,0xda,0x88,0x24,0x42,0x49,0xc9,0xe4,0xc5,0xed,0xb4,0x62,0x65,0xb9,0x41,
  0x4e,0x0,0x20,0xfb,0xd1,0xeb,0x16,0x28,0xe4,0xd3,0xcf,0xdf,0xc4,0x13,0x9c,0x5a,
  0xc3,0x85,0x8e,0xda,0x80,0x4b,0xd4,0xef,0x99,0xf9,0x7,0x44,0x2d,0xba,0x69,0x6b,
  0x71,0xfc,0x3c,0x99,0xda,0x1e,0x8e,0x47,0x50,0xbf,0x48,0x47,0x24,0xc3,0x1,0x89,
  0x32,0x5,0x5a,0xa2,0xe1,0x44,0x41,0x30,0x79,0xa,0x12,0x4d,0xc6,0x4,0xa2,0x12,
  0x38,0xe8,0x28,0xea,0x88,0xb0,0x1a,0xd,0x8d,0x9,0x61,0xb3,0xa5,0x37,0x81,0xa6,
  0xb4,0x5d,0xeb,0x8f,0x5a,0x2d,0xbe,0x57,0x8b,0x56,0x6b,0xa8,0x94,0x85,0x6d,0xd7,
  0x0,0xb5,0x39,0x7c,0xfe,0x4d,0x74,0xf3,0x84,0x63,0x37,0x5f,0x3a,0x49,0xbf,0xa8,
  0x4a,0xbe,0x1b,0xf1,0x29,0x52,0xf8,0x51,0xa9,0x36,0x31,0x3a,0x25,0xd1,0x4e,0xbd,
  0x4c,0x72,0x26,0x3f,0xfb,0xe1,0x9b,0x6b,0x6,0x45,0x6a,0xb7,0xe8,0xf7,0xf,0x7e,
  0xa2,0xbb,0x36,0xea,0xc9,0x9c,0x31,0x5d,0x6d,0xe2,0x94,0xc9,0x4d,0x96,0xd3,0xf7,
  0xff,0xb0,0x30,0x87,0x4e,0x98,0x2d,0xf3,0xaf,0xe0,0x43,0xd3,0x46,0x6a,0x62,0x91,
  0x16,0xd2,0xd2,0x19,0xfc,0x75,0xa8,0xae,0xbb,0x7f,0x41,0xef,0x9c,0xf0,0x3e,0x89,
  0x14,0xc5,0x54,0x6,0xc9,0x58,0xa9,0x24,0x2,0x26,0x43,0x8,0x3d,0x75,0xc5,0xa3,
  0x3f,0x1f,0x60,0x2c,0x75,0xc,0x2a,0xb4,0x71,0x2a,0x42,0x87,0x1e,0x8d,0x28,0xc5,
  0xc2,0xd5,0x97,0xf3,0x84,0xab,0x2f,0x4b,0x84,0x5b,0x5a,0xbc,0xfa,0xf2,0x50,0xbc,
  0x65,0x5,0x2c,0x40,0x91,0xff,0xcb,0x91,0x71,0x64,0x2c,0x22,0x94,0xfa,0xb5,0x50,
  0x39,0xb6,0xff,0x4,0xdd,0x96,0x6e,0xc5,0x9c,0x27,0x85,0xd2,0xbc,0x5,0x3d,0x90,
  0x23,0xfb,0xe8,0xcc,0xaf,0x17,0x7c,0xf4,0xcc,0xd8,0xa2,0xcf,0x80,0xc8,0xfd,0xd8,
  0x85,0x2f,0x8,0xda,0xeb,0x42,0x6f,0x17,0xf9,0x29,0xd,0x7d,0x9f,0xb3,0x67,0x44,
  0xe7,0x7f,0x82,0x5d,0x50,0xae,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/templates/office2016.qss
  0x0,0x0,0x8,0x7a,
  0x0,
  0x0,0x28,0x51,0x78,0x9c,0xcd,0x5a,0x5b,0x6f,0xdb,0x46,0x16,0x7e,0x17,0xa0,0xff,
  0xc0,0x46,0x2f,0x5b,0xa3,0x8a,0x64,0xc5,0xb9,0x31,0x68,0x0,0x47,0x8d,0xb3,0xf,
  0xcd,0x6e,0x13,0x7,0xdb,0x87,0xa2,0x28,0x78,0x19,0xc9,0x44,0x28,0xe,0x4b,0x52,
  0xb5,0x5d,0xc3,0x40,0x52,0xa7,0x4d,0xdc,0xa8,0x71,0x11,0x6c,0xb3,0xc9,0xae,0xb3,
  0x4d,0xd3,0x9b,0xfb,0xd0,0xa4,0xdb,0x14,0x6d,0x6c,0xe7,0xf2,0x67,0x44,0x4a,0x7a,
  0xea,0x5f,0xd8,0x19,0x92,0x43,0xce,0x90,0x43,0x51,0x74,0x2e,0xad,0x8d,0x0,0xce,
  0x5c,0xce,0xf9,0xe6,0xdc,0xcf,0x91,0x6a,0x53,0xe5,0x92,0xed,0x48,0x96,0x23,0x58,
  0x9a,0x2c,0x43,0x43,0xb0,0x81,0x53,0x2e,0xc1,0x56,0x4b,0x53,0x80,0xd0,0xa8,0x4f,
  0x1f,0x12,0x6c,0x67,0x59,0x7,0x82,0xb3,0x0,0x3a,0xa0,0x5c,0x9a,0xaa,0x95,0x4b,
  0xe5,0x52,0x6d,0x6a,0x7e,0xf6,0xac,0x7f,0x7c,0xd6,0x34,0x75,0x4d,0x91,0x1c,0xd,
  0x1a,0x6f,0x6b,0x6a,0x1b,0x38,0xf8,0x40,0xe6,0xa6,0xb0,0x52,0x2e,0x9,0xe8,0x47,
  0x96,0x94,0xf3,0x6d,0xb,0x76,0xd,0xb5,0xaa,0x40,0x1d,0x5a,0xa2,0xb0,0xb2,0x22,
  0x29,0xa,0x30,0x9c,0xd5,0xd5,0x63,0xe1,0x11,0x68,0xa9,0x0,0x6d,0xd8,0x50,0xd7,
  0xd4,0xf4,0x76,0x78,0xad,0x22,0x37,0xf0,0x2f,0x5a,0x5c,0x65,0x60,0x9d,0x90,0x2c,
  0x1a,0x8,0xfa,0x6f,0xc0,0x3a,0x97,0x31,0xcb,0xb6,0x72,0xb8,0x8e,0x7f,0xfd,0x9d,
  0xe0,0x3c,0xc5,0x90,0x15,0xc4,0x89,0xae,0xe3,0x40,0xe3,0x14,0xa2,0x6d,0x6,0x6f,
  0x7d,0x8d,0xec,0xcc,0x2f,0xdb,0xe,0xe8,0x4,0xfb,0x8,0x88,0x7b,0xbd,0x77,0x56,
  0x6b,0x2f,0x38,0xd4,0x85,0xd1,0xda,0x63,0xef,0xe6,0xfd,0xfe,0xe3,0xa7,0x83,0x7f,
  0x6e,0x79,0x3f,0x7e,0xe3,0x5d,0xfb,0xbe,0xbf,0xfb,0xeb,0xef,0x8f,0x7a,0xa6,0x64,
  0x18,0x40,0x1f,0x5d,0xee,0x8d,0x6e,0xdf,0x1d,0xfc,0xfb,0x92,0xd7,0x5b,0x1f,0x5d,
  0xbf,0x37,0xd8,0xbd,0xd4,0xdf,0xfe,0x32,0x7d,0x9e,0x79,0x6f,0x12,0x4c,0xd6,0xeb,
  0x1d,0x4b,0x32,0x6c,0x53,0xb2,0x90,0x0,0xe2,0x27,0x9,0x99,0x64,0xfa,0xf,0xaf,
  0x6,0x20,0x86,0xf7,0x9e,0xc,0x1e,0xdf,0x1b,0xcf,0xf2,0xb8,0x70,0xe6,0x1c,0x84,
  0x7a,0xb0,0x11,0x69,0x3e,0x94,0xaf,0x1,0xd,0xc0,0xaa,0x92,0x28,0xa2,0xea,0x80,
  0x25,0xca,0xc,0x72,0x20,0xe3,0x33,0xa6,0xa4,0xaa,0x9a,0xd1,0x16,0x85,0xba,0xd0,
  0x30,0x97,0x82,0x77,0xa0,0x57,0x2c,0xc0,0xf,0x80,0x25,0xc,0x3e,0xfd,0xd5,0xbb,
  0x70,0x51,0x28,0x0,0x55,0xc,0x2e,0xe6,0x9a,0x6a,0xd5,0x3f,0xe7,0x23,0x5d,0x9d,
  0x9c,0xb8,0x69,0x1,0xdb,0x6,0x6a,0x42,0x1c,0xd3,0xe6,0x52,0xc2,0xd2,0xf3,0xc4,
  0x10,0x9d,0xb,0x9,0x16,0xc5,0xa1,0x2c,0x0,0xe5,0x3c,0x50,0x27,0x78,0x66,0x82,
  0x41,0x68,0x23,0xde,0xe6,0xfa,0x70,0x63,0xd3,0xfd,0xec,0x8b,0xc8,0x30,0x7d,0x19,
  0xd7,0xa6,0x4e,0x3,0xa3,0x1b,0xf0,0x78,0xb,0x9a,0x5d,0xb3,0x80,0xe0,0xdf,0x31,
  0xf1,0x85,0xd3,0x50,0x5,0xaf,0xef,0x9b,0xde,0xf7,0xee,0xa,0xa3,0xdd,0xaa,0x85,
  0x9d,0x6,0x49,0x2a,0x53,0xc5,0x55,0x21,0xc1,0x5a,0xf0,0xb6,0xbe,0x72,0x1f,0x6d,
  0x20,0x80,0x1,0xd2,0x0,0xe6,0x68,0x6d,0xcb,0xbd,0xf2,0x49,0x11,0x83,0x48,0xe0,
  0x12,0xc5,0xe,0xe2,0x53,0x95,0x8b,0x19,0x4b,0x42,0x8a,0x8,0x7c,0xb8,0xf2,0xc7,
  0xc2,0x27,0x20,0xf6,0xf2,0x80,0xc1,0x7f,0x7e,0xf1,0xae,0x7d,0xeb,0x3e,0xda,0x76,
  0x2f,0xef,0x84,0x10,0x7d,0xc4,0x28,0x72,0xb9,0x17,0x37,0x6,0xdf,0xef,0x1e,0x31,
  0x97,0xd0,0xdf,0xc3,0x7b,0x3f,0xe0,0xb8,0x71,0x75,0x3d,0xc,0x1d,0x6b,0x8f,0x7,
  0x9b,0x57,0xdd,0x2b,0x3f,0xed,0xf5,0xd,0x8d,0x7d,0xef,0xa,0x7c,0xdb,0x38,0x42,
  0x99,0x86,0xf7,0xaf,0x3b,0x9,0xfb,0xec,0x3f,0xbd,0xed,0xf5,0x2e,0xba,0x3f,0x6d,
  0xc,0x9f,0x6c,0xf,0x7f,0xbb,0xbd,0x57,0xf6,0xf5,0xa4,0x65,0x32,0x71,0x87,0x4e,
  0x9,0x67,0xba,0x9a,0x72,0x7e,0x16,0x49,0xcf,0xb6,0x13,0xf9,0x88,0xdd,0x29,0x14,
  0x9c,0x9,0x89,0xa6,0x63,0xe9,0x4d,0x68,0x38,0x92,0x66,0x0,0x86,0x36,0xb3,0x51,
  0x80,0x34,0x26,0x1e,0x53,0x99,0x7,0x68,0x57,0x72,0xa0,0x15,0x88,0xa3,0x5c,0xf2,
  0x1e,0x6c,0x79,0x97,0xb0,0x6a,0x33,0x4e,0x60,0x33,0x5d,0xeb,0x79,0xb7,0xee,0x8f,
  0xee,0x6e,0xe,0xd7,0x7f,0x8e,0xf2,0x93,0xcf,0xb0,0x5c,0x72,0xbf,0xfb,0xc8,0xfb,
  0xef,0xa6,0xb7,0xf6,0x60,0xb0,0x7b,0xab,0xff,0xf0,0x33,0xf7,0xf3,0x1e,0xba,0x80,
  0x6c,0x19,0x25,0x2e,0x64,0x1c,0xe8,0x6f,0x64,0xd4,0xee,0xfa,0xcf,0x83,0x9d,0xa7,
  0x41,0x6a,0x9,0xce,0x8c,0xbe,0xbe,0xe6,0xdd,0x79,0x84,0xed,0x69,0xe3,0x7e,0x7f,
  0xf7,0xdb,0x80,0x22,0x22,0x15,0xbd,0x55,0x72,0x40,0x1b,0x5a,0xcb,0x48,0x5b,0xd9,
  0xb0,0xbc,0x1b,0xdb,0xa1,0x51,0x6e,0x7c,0x34,0xbc,0xb0,0xe6,0x17,0x32,0x55,0x55,
  0xb2,0xce,0x37,0xf6,0xbf,0x6f,0xdb,0x41,0x49,0x93,0x71,0x3b,0x10,0x5f,0x54,0x6b,
  0x0,0xa5,0xae,0x70,0x52,0x3f,0x41,0x11,0xd4,0x46,0x29,0x6c,0x99,0x85,0x87,0x82,
  0xb4,0x84,0x3d,0x4b,0x6e,0x27,0x83,0x37,0xb9,0x2b,0xb6,0xa0,0xd2,0xb5,0x3,0xa,
  0xb0,0xeb,0xe8,0x48,0xa5,0x51,0xea,0x5c,0x4d,0xaa,0x8c,0x5c,0xa,0xe4,0xf9,0xa2,
  0x14,0xc9,0x98,0x5a,0xae,0xf8,0x33,0x4,0x28,0x8,0x1d,0xc9,0x6a,0x6b,0x46,0xd5,
  0x81,0xa6,0x78,0xc0,0xf7,0x9d,0x68,0x49,0x86,0xc8,0xe3,0x3a,0xe1,0x6a,0x42,0xd2,
  0x6f,0x49,0xa8,0x1e,0x62,0xc5,0xec,0x2f,0x4d,0x2e,0xe3,0x34,0xb9,0x37,0x25,0x19,
  0x11,0xf0,0xff,0xc4,0x61,0xea,0xce,0xe5,0xd1,0xdd,0x9b,0xde,0x9d,0xd,0xe,0x97,
  0xe0,0x64,0x66,0xa4,0x4c,0x17,0x26,0xe4,0xdd,0x87,0xfc,0x9f,0xd8,0xd1,0xd2,0xf8,
  0x8f,0x67,0xd7,0x5c,0xe1,0xdb,0x38,0x85,0x2,0x79,0x9b,0x9f,0x82,0x28,0x2b,0x62,
  0xcd,0x82,0xbc,0xec,0x25,0xd9,0x44,0xf2,0x3d,0x5,0x3c,0x2a,0xa5,0x9a,0xbf,0x9b,
  0xb8,0x75,0x8,0x24,0xc2,0xd1,0x7,0xbd,0x3d,0x59,0xa0,0xa3,0x8a,0x4d,0x5c,0x5e,
  0x5,0x87,0x62,0xb1,0x65,0x52,0xa7,0xb3,0xfc,0x38,0x2b,0x63,0x34,0x11,0x2b,0xad,
  0xce,0xb3,0x65,0xe2,0x3c,0xf3,0x8a,0x5,0x75,0x9d,0xf7,0x48,0xde,0x9,0xda,0x1a,
  0xaa,0x96,0xa4,0x6a,0x5d,0x5b,0x6c,0x84,0xe,0x34,0xb1,0x8d,0x4c,0x1a,0x8e,0xc6,
  0x43,0x79,0x47,0xb2,0x2c,0xb8,0x78,0x6e,0xd9,0x44,0x49,0xf1,0x0,0xc9,0xc9,0x4,
  0x19,0x4e,0xc8,0xd5,0x45,0x4d,0x75,0x16,0x7c,0x3c,0x45,0xe9,0xcd,0x24,0xe8,0xe9,
  0xa0,0x55,0x94,0xdc,0x9e,0x74,0x96,0xb2,0xc3,0x79,0x47,0xc2,0x65,0x72,0xba,0xb7,
  0x65,0x36,0x92,0x3e,0xea,0xa4,0xe4,0x9f,0xb0,0x9,0x1c,0xf8,0xc8,0x7b,0xea,0xe9,
  0xf7,0x30,0xc4,0x27,0x48,0x2,0x9c,0x86,0x3b,0x36,0xa8,0xcc,0xcd,0x71,0xb6,0x14,
  0x8a,0x69,0x71,0x41,0x73,0x0,0x6d,0x5c,0x51,0xdf,0x96,0xd3,0x49,0x33,0xcf,0x49,
  0x71,0xce,0xd3,0xd,0xaf,0xc1,0x1a,0x47,0x8e,0xa9,0x62,0x9f,0xd,0x59,0x9e,0xb0,
  0xb3,0x6f,0x6,0x75,0xb5,0x66,0xa8,0x78,0x19,0x86,0xaf,0xab,0x4d,0xd9,0x5d,0x19,
  0x1b,0x1,0x32,0xcc,0xaa,0x9,0x6d,0xd,0x5f,0x10,0x5,0xdf,0x3f,0x8e,0x61,0xed,
  0x8,0x42,0x60,0x6,0xdc,0x18,0x71,0x4e,0x92,0x13,0x95,0x63,0xb0,0x52,0xa8,0xac,
  0x63,0xaf,0x8a,0xa2,0x23,0xc9,0x4c,0x14,0xe6,0x75,0x9c,0xa1,0xb6,0xe3,0x48,0x92,
  0x8c,0xa3,0x31,0xef,0x74,0x8c,0xa5,0x72,0x7b,0x68,0xdc,0xd1,0x5a,0x58,0xa8,0x27,
  0x56,0xb1,0x77,0x8b,0xc2,0x41,0x6e,0x1d,0x10,0x9f,0x45,0x6b,0xa1,0xc7,0x1c,0x24,
  0x6b,0xa4,0xfe,0x67,0x9,0x24,0xba,0x82,0x83,0x69,0xff,0xa2,0x44,0x21,0xda,0x40,
  0x7,0x8a,0x43,0x8c,0x27,0x63,0x2a,0x44,0xbd,0x36,0xd3,0xa7,0x45,0x5e,0xdc,0xe5,
  0x5,0x53,0x9a,0xbb,0x6f,0xe2,0xe2,0x2b,0x2c,0x8,0xe,0xbd,0x94,0x47,0x10,0xa8,
  0x5c,0xed,0xc5,0x70,0xb3,0x7c,0x89,0xb1,0xb2,0xa8,0xcf,0x9,0x3a,0x78,0x81,0x82,
  0x1a,0x6d,0x25,0x4a,0x0,0xf7,0xe9,0xc7,0xa3,0xaf,0x76,0xdd,0xeb,0x78,0x46,0x5,
  0x74,0xef,0xca,0x8d,0xe1,0x85,0x8f,0x95,0x30,0xa,0xe3,0xca,0xde,0x3f,0xd4,0x7f,
  0x78,0x61,0x78,0xf9,0x97,0x72,0x9,0x15,0xf3,0xa8,0xeb,0x23,0x85,0xfd,0xe,0x65,
  0x2f,0xb8,0x2a,0xff,0xfc,0x3b,0xf7,0xca,0x2d,0x77,0x73,0x6b,0x56,0xc1,0xbe,0x81,
  0x9b,0xe0,0xa0,0x8d,0x44,0x35,0xb,0xda,0xee,0x3f,0xfc,0x14,0xfd,0xe1,0xf6,0x76,
  0x50,0x83,0x10,0xb5,0x72,0xa8,0xb9,0xf3,0x1e,0x7c,0xe1,0xdd,0x7c,0x32,0xf8,0x66,
  0x47,0x60,0xeb,0x8f,0xe4,0xc8,0x29,0x35,0x70,0x62,0xe3,0xdd,0xc,0x1b,0xef,0x52,
  0x75,0x41,0xf1,0x54,0x99,0x9e,0xf5,0xf0,0xe2,0x6c,0x82,0x6f,0xa5,0x5e,0xaf,0x3f,
  0xcf,0x1c,0x9e,0x4e,0x6a,0x1c,0x78,0xcc,0x8,0xa8,0xb0,0x58,0x5e,0x22,0x50,0x3a,
  0x5b,0xfc,0x59,0xa5,0x38,0x31,0xc8,0x3f,0x58,0x96,0x54,0x7e,0x1b,0x33,0x7c,0xa4,
  0xe3,0xcc,0x8b,0x72,0x98,0x18,0xde,0x29,0x49,0xd7,0x81,0xb5,0xcc,0x73,0xdb,0x31,
  0x31,0xb5,0x68,0x53,0xcd,0x70,0x49,0xab,0xeb,0x5,0x88,0x9f,0xd,0xb3,0x21,0x7f,
  0xb6,0xc6,0xf,0x17,0x9f,0x3,0x90,0x42,0x9d,0x4e,0xa,0x95,0xdf,0x70,0x72,0xa1,
  0xf9,0x3b,0x1,0x3e,0x7b,0x1,0x2e,0x56,0x55,0xa0,0x40,0xcb,0xaf,0x7b,0xaa,0x24,
  0x6b,0x21,0xc4,0xcf,0x8,0xaa,0x68,0x83,0xcb,0xc3,0x28,0x8a,0xa8,0x56,0xed,0x24,
  0xf2,0x79,0x81,0x6e,0x2d,0xdc,0x97,0x75,0x74,0x65,0x2,0x3e,0x2f,0xc3,0x80,0xb2,
  0xd9,0x27,0xaa,0x15,0xbe,0x8e,0x99,0xcb,0xff,0xd0,0xc0,0xa2,0x9,0x2d,0x87,0xab,
  0x64,0xb2,0x99,0x25,0xb4,0x4a,0xcb,0xff,0x49,0x9b,0xf,0x4e,0xd7,0x2c,0x45,0xbc,
  0x32,0x1e,0xdd,0x44,0x8e,0xcb,0x93,0x6a,0x65,0xae,0x89,0x7f,0x93,0xd2,0xc1,0x1c,
  0x69,0xa9,0x44,0x73,0x59,0x54,0xf9,0xd1,0xff,0x26,0xb2,0xd1,0xc,0xca,0x7b,0x35,
  0xab,0x2c,0x72,0x94,0xf5,0xa4,0x92,0x57,0xf1,0x1,0x43,0x31,0xa7,0x9,0x61,0x28,
  0x24,0xd6,0x32,0x55,0x38,0xd5,0x5d,0x63,0x3d,0x93,0xe9,0xd1,0x99,0x37,0x51,0x27,
  0x74,0x52,0xd5,0xf0,0x28,0x68,0xb0,0xbe,0xed,0xf6,0x6e,0xf0,0xc6,0x70,0xc7,0x85,
  0xe8,0x5c,0x96,0x67,0x54,0x9a,0xf5,0x66,0xa3,0x39,0x93,0x2a,0xab,0x2b,0x73,0x73,
  0x73,0xfe,0x62,0x20,0x68,0x1c,0x5e,0x78,0x82,0xa0,0x76,0x89,0x10,0xe2,0x25,0x56,
  0x92,0xab,0x63,0xd0,0x5,0xf2,0x5f,0xc9,0xfa,0x10,0xae,0xd2,0x38,0x74,0x54,0x6e,
  0xcd,0xc4,0xe6,0x1e,0x89,0xa1,0x89,0xb3,0xfd,0x9,0xb8,0xc4,0x88,0x21,0xc5,0x86,
  0x9c,0x5a,0xa1,0xe7,0x80,0x1c,0x3f,0xe0,0x28,0x9b,0x3b,0xf4,0x8f,0xd8,0x9f,0x45,
  0x49,0x18,0x46,0x5,0xf9,0x18,0x4,0xd4,0xc1,0xe7,0xe,0xa2,0x9,0x3b,0x32,0xcc,
  0x97,0x41,0x78,0x2a,0xd3,0x10,0x94,0x86,0x5a,0x57,0x5b,0x19,0x26,0x4f,0x6,0xf,
  0x3c,0x25,0x12,0xca,0xe3,0x43,0x70,0xac,0xc3,0xa4,0x8b,0x8d,0xa5,0x9,0x90,0x75,
  0x48,0xb2,0xe,0xf2,0x7c,0x93,0x9e,0x8d,0xbc,0x50,0x93,0x8d,0x90,0x89,0xaa,0x5,
  0xcd,0xaa,0xa,0x17,0x43,0xb7,0xa5,0x6,0xb,0x10,0xb5,0xba,0x9a,0x21,0x92,0xc8,
  0x77,0x2c,0xb1,0x1d,0xcf,0x1d,0x50,0x4f,0x1e,0xce,0x1e,0xe2,0xc9,0x83,0x30,0xd,
  0x3a,0x4c,0x34,0x89,0xc7,0x1d,0x93,0x80,0xc9,0xd1,0xc2,0xdc,0x1b,0x27,0x4f,0x9e,
  0x38,0x90,0xa1,0xe5,0xca,0x51,0xa0,0x36,0x5a,0x47,0xf3,0x99,0x21,0x3e,0x55,0x7f,
  0x3c,0x18,0xf0,0xd1,0x3a,0x52,0x1b,0x88,0x42,0xd7,0xd2,0xff,0x22,0xd6,0xc8,0xbd,
  0x9a,0xbf,0x5a,0x43,0x3d,0x17,0xec,0x5a,0xa,0xa8,0xcd,0xe2,0xf3,0x6f,0xa0,0x9b,
  0xfb,0x4d,0xa3,0xfd,0x6a,0x3a,0x75,0x25,0x26,0xd3,0x6c,0x3c,0xe3,0x8e,0xad,0x6b,
  0x53,0xe3,0xb3,0x47,0x30,0xcf,0x19,0x6b,0xcc,0xc,0x0,0xff,0x8b,0x21,0x6c,0xf7,
  0x4d,0x21,0x48,0xec,0x4e,0x3a,0xe1,0x66,0x7,0x75,0xab,0xe3,0x48,0xe6,0xc,0xbb,
  0xf0,0xa7,0xa8,0xbf,0xfd,0xf,0xcf,0xfb,0xc9,0x7,0xd1,0x83,0x7,0xbb,0x83,0xdd,
  0x2f,0xe9,0xaf,0x80,0x90,0xf,0xfd,0x35,0x66,0xd4,0x98,0x64,0x54,0x99,0x9f,0x45,
  0x27,0xb4,0x8e,0xf6,0x21,0x78,0x5b,0x33,0x90,0x36,0xe9,0x27,0x4d,0xa4,0xcc,0x73,
  0x9a,0xa3,0x3,0x59,0xb2,0xde,0x43,0x74,0xde,0xfb,0x2b,0xb6,0xb8,0xfd,0xf6,0x7,
  0xbe,0x56,0xa9,0x7c,0x3f,0x43,0x72,0x57,0x41,0x1c,0x54,0x36,0xe0,0xd8,0xa8,0x7c,
  0x4,0xff,0xee,0x8d,0x30,0xfd,0x6d,0xf,0xe,0x69,0x45,0x42,0xbf,0x32,0x91,0xf6,
  0x69,0x69,0x29,0x4f,0x88,0xd2,0x52,0x86,0x10,0xb,0x8b,0x51,0x5a,0x4a,0x8a,0xb1,
  0xa8,0x20,0x39,0x58,0xf2,0xbe,0xd,0x31,0xb1,0x2c,0x79,0xb4,0x13,0xdf,0x58,0x29,
  0xf6,0xde,0xbf,0x41,0xab,0x23,0xe9,0xec,0x93,0xa3,0x22,0xa7,0xa9,0x43,0x1b,0xe4,
  0x88,0xde,0x3f,0xf3,0xec,0x72,0xf7,0xc9,0x3c,0xa3,0xe4,0x53,0x50,0x72,0x3f,0xea,
  0xc0,0x17,0x38,0x1d,0xf1,0x44,0xb4,0x73,0xbf,0x21,0x52,0x69,0x4d,0x1f,0xae,0x1f,
  0x96,0x88,0x40,0xff,0xf,0xf1,0xed,0x5f,0x33,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/templates/win7.qss
  0x0,0x0,0x5,0x76,
  0x0,
  0x0,0x1f,0x91,0x78,0x9c,0xbd,0x59,0x4f,0x6f,0x1c,0x35,0x14,0xbf,0xef,0xa7,0x30,
  0xe5,0x2,0x11,0xd3,0x6d,0xa0,0x11,0xd2,0x44,0x20,0xa5,0x1,0xf5,0xd2,0x96,0xa6,
  0x89,0xe8,0x1,0x21,0xe4,0x99,0x71,0x67,0xad,0xcc,0x8e,0x47,0x1e,0x6f,0x77,0x43,
  0x14,0x89,0x4a,0xbd,0x14,0x2a,0x81,0x38,0x80,0xd4,0xf,0x91,0x23,0x82,0x3,0x7c,
  0x1a,0x68,0xda,0x6f,0x81,0xed,0xf9,0xb3,0x1e,0xff,0x99,0xf5,0x4c,0x92,0xe6,0x16,
  0xfb,0xf9,0xfd,0xde,0xfb,0xbd,0xe7,0xf7,0xde,0x78,0xa7,0x5b,0x93,0x92,0x41,0xca,
  0x0,0xc5,0x51,0x44,0x72,0x50,0x22,0x36,0x59,0xe2,0xfc,0x53,0x50,0xb2,0x93,0xc,
  0x1,0x36,0x43,0x73,0x34,0xd9,0x9a,0x4e,0x26,0xd3,0xad,0xc3,0xbd,0x47,0x52,0x66,
  0xaf,0x28,0x32,0x1c,0x43,0x86,0x49,0xfe,0x18,0x27,0x29,0x62,0x7c,0xdb,0xb9,0x7,
  0x4e,0x27,0x80,0xff,0x45,0x30,0x3e,0x4e,0x29,0x59,0xe4,0x49,0x10,0x93,0x8c,0xd0,
  0x10,0x9c,0x9e,0xc2,0x38,0x46,0x39,0x3b,0x3b,0xdb,0xad,0x24,0x8,0x4d,0x10,0x5f,
  0x2f,0x49,0x86,0x13,0x63,0xb7,0x3d,0xc4,0xd0,0x8a,0x55,0x2a,0xc4,0xd6,0x99,0x6a,
  0xd7,0x1d,0x48,0x15,0x4b,0xf8,0x7f,0x57,0x80,0x2d,0x1,0x40,0xab,0x73,0xc1,0x18,
  0xc9,0xef,0x72,0x5d,0x45,0xe5,0xdd,0xbf,0x7f,0xfe,0xf4,0xfa,0xe5,0x8b,0xb7,0xbf,
  0x9e,0xbf,0x39,0xff,0xe7,0xe2,0xef,0x73,0x15,0x5e,0x17,0x5,0x9f,0x83,0x83,0x23,
  0x42,0xb2,0x6a,0xa3,0x36,0xcd,0xe9,0x16,0x7,0x9d,0x91,0xa7,0x88,0x82,0x8b,0x1f,
  0xff,0x78,0xfd,0xc3,0x33,0xe0,0xaf,0x39,0xac,0xce,0xb9,0x5d,0x97,0xfb,0x41,0x94,
  0x56,0x40,0xbe,0x5a,0xb,0x8a,0xca,0x12,0x25,0x9e,0x7a,0x55,0x52,0xb7,0x8b,0x55,
  0x4b,0x6c,0x2d,0x23,0x77,0x86,0xe1,0xc7,0x33,0x14,0x1f,0x5f,0x3,0xfe,0x74,0xeb,
  0x3e,0xca,0x17,0x15,0xc8,0x43,0x52,0x2c,0xa,0x7f,0xa6,0xbf,0x29,0x84,0xfc,0x7d,
  0x92,0xa0,0xcf,0x6e,0x6c,0xdf,0xf8,0x36,0xc,0xe7,0x5c,0x53,0x10,0xf9,0x45,0xa1,
  0x92,0x65,0x79,0x20,0x5,0x7,0x71,0xd1,0x8b,0xeb,0x1d,0xa7,0xe,0x7,0xe0,0x9,
  0x89,0x17,0xe5,0x88,0x64,0xab,0xce,0x9d,0x7a,0x13,0xee,0x97,0x94,0xdc,0xa0,0x3a,
  0xde,0xe1,0xd8,0x5b,0xd0,0x3d,0x7f,0xf5,0x59,0x3,0xb4,0xb4,0x69,0xc,0x1e,0x6e,
  0x6a,0x6f,0x38,0xaf,0x2f,0xed,0x81,0x84,0xc1,0x79,0x22,0xca,0x35,0xa1,0xe0,0xed,
  0xab,0x5f,0xde,0xfc,0xfe,0xf3,0x10,0x8a,0x43,0x4d,0x43,0x65,0xe3,0x12,0x27,0x6c,
  0x16,0x82,0x5b,0xc5,0x4a,0xaf,0x9e,0xf,0x61,0x8e,0x32,0x5e,0x31,0x2f,0x5e,0x3d,
  0x77,0x43,0x28,0xf8,0x52,0x9e,0x63,0xba,0x85,0xdd,0x79,0x57,0xad,0x39,0x1a,0xc5,
  0x21,0x2a,0x20,0x15,0x26,0x1b,0xed,0x4b,0xdb,0xd1,0xab,0x74,0xd9,0x6c,0x1b,0x2a,
  0xf7,0x21,0x43,0x29,0xa1,0x27,0x8a,0xae,0x66,0xa9,0x27,0x76,0x31,0xc9,0x19,0x6f,
  0x34,0x6d,0xde,0x5b,0x14,0x76,0xf9,0x72,0x5b,0xde,0xa2,0xad,0xf9,0x1a,0xe2,0x8c,
  0xd8,0x99,0x43,0x9a,0xe2,0x3c,0x60,0xa4,0x8,0xc1,0x27,0x22,0x7c,0xca,0x62,0x44,
  0x38,0xf7,0xf3,0x7a,0xbd,0x63,0xa8,0x8c,0x92,0x11,0x35,0x97,0xcf,0x8c,0xc2,0xbc,
  0xe4,0xb8,0xdc,0xeb,0x6e,0xba,0xe6,0x24,0x47,0xfe,0xad,0x5e,0x82,0xdc,0x83,0x91,
  0x89,0x2c,0x17,0xfd,0xe1,0xd7,0x6c,0x2c,0x22,0x86,0x59,0x86,0xec,0x50,0x5f,0x15,
  0x62,0xa2,0xa9,0x12,0x50,0x47,0x54,0xf7,0xb4,0x8c,0x5c,0x3b,0x35,0xc4,0x14,0xdd,
  0x6f,0x27,0xda,0xc0,0x56,0xaf,0x38,0x75,0xb0,0xc0,0xf1,0xf1,0x1e,0x9f,0x72,0xca,
  0xb2,0x3b,0x35,0x75,0x37,0xfc,0x78,0xec,0xde,0x2d,0x6,0x45,0xc1,0x32,0x6f,0x96,
  0xba,0x6e,0x5c,0x5c,0xe6,0xbc,0xb8,0x6b,0x39,0x91,0x98,0x41,0xb7,0xba,0x58,0x95,
  0x77,0x7a,0x12,0x59,0xb0,0xc,0xe7,0xa8,0x9,0xc5,0x99,0x63,0x8e,0x35,0x22,0x6b,
  0xec,0x75,0x4c,0xe,0x28,0x4c,0xf0,0xa2,0xc,0xc1,0xc7,0xcd,0x35,0x69,0x29,0x5f,
  0xce,0x30,0x43,0x7d,0xb5,0xd8,0x36,0x79,0x4a,0xd7,0x32,0xf4,0x84,0xb5,0x8a,0x6f,
  0x37,0x8a,0x15,0x1,0x8a,0xd3,0x99,0x4d,0xc2,0x36,0xde,0x16,0x85,0x1c,0x2e,0xf4,
  0x29,0xcf,0x70,0x6b,0x63,0xa,0x35,0x9a,0xec,0x53,0xa3,0xa9,0x6f,0xf3,0xfc,0xd1,
  0x68,0xac,0x25,0x6d,0xe9,0x79,0x4,0xa3,0x6e,0x5a,0x56,0xb,0x61,0xc8,0x60,0xd4,
  0x3f,0x39,0xdb,0x59,0x37,0xab,0x4e,0x6b,0x97,0xe5,0x22,0xaa,0x75,0xf0,0x96,0x56,
  0x7,0x65,0xc,0xcc,0x65,0x11,0xbb,0x10,0xec,0x38,0x8a,0xe6,0x5a,0x9a,0x2f,0xd6,
  0x19,0xbc,0xd3,0x2e,0x16,0x30,0x49,0x70,0x9e,0xea,0x3a,0x9a,0xe5,0x1a,0x71,0xa7,
  0x9b,0xf1,0xa,0x21,0x61,0x89,0x32,0x14,0x33,0x94,0x7c,0x4,0x6c,0xbb,0x9d,0x0,
  0x5f,0x2e,0xdb,0xfa,0xe1,0xf5,0xc0,0x44,0x19,0x27,0xb9,0xef,0x26,0xf0,0xc3,0x41,
  0x73,0xd8,0x39,0x22,0x3a,0xee,0x54,0xcd,0x6d,0x60,0xde,0x3b,0xbb,0x91,0x92,0x85,
  0xf0,0x3d,0xcd,0x56,0x97,0x55,0xb6,0xa9,0xd5,0xf4,0x4b,0xa6,0x6c,0x33,0xd4,0x1c,
  0xdc,0xe3,0x65,0xe6,0xcb,0x4,0x33,0xde,0xad,0x2f,0x5e,0xfc,0xf5,0xdf,0xcb,0xdf,
  0x2c,0x93,0x4c,0x2b,0x34,0x26,0x89,0x1d,0xb5,0xd1,0xcd,0x54,0xe5,0x2c,0xbf,0x9a,
  0x81,0xed,0x1a,0x2a,0xbb,0xa9,0x79,0xc2,0xe6,0xad,0xd3,0x1b,0x4b,0x8e,0x59,0xe7,
  0xcf,0xd4,0x64,0x6d,0x5f,0xcc,0xb7,0x77,0xc8,0xaa,0x97,0xb5,0x46,0xc8,0x83,0x35,
  0x8f,0x4e,0xd5,0x62,0x3f,0xe2,0xb9,0x4d,0xaa,0xb2,0xd5,0xb,0xaf,0xc8,0x5d,0xb1,
  0x5,0xfb,0x64,0x1e,0x91,0x8d,0xde,0xd7,0x42,0x3,0xa6,0x5d,0x57,0xe9,0xb5,0x5c,
  0x12,0x3,0x66,0x50,0x30,0xfd,0xe7,0x96,0xe,0x4,0xe2,0x49,0x3,0xa3,0xc,0xd,
  0xa1,0xf3,0xdd,0xa7,0x77,0x6b,0x6e,0x98,0x50,0x5e,0x10,0x13,0xb2,0x6c,0xe2,0xcf,
  0x87,0x45,0x31,0xba,0x53,0x92,0x5,0x84,0xd7,0x49,0x9c,0x87,0x4d,0xb9,0xde,0xd5,
  0xf7,0xb,0x52,0x62,0x1,0xc9,0xf3,0x80,0x14,0x40,0x16,0xd5,0x5d,0xf5,0x3b,0x69,
  0x1b,0xcd,0x6d,0x83,0xb0,0x9f,0x39,0x23,0xdf,0x7a,0x6c,0x2a,0xb9,0xb6,0x0,0x52,
  0x4a,0x96,0xb5,0x36,0x3c,0x87,0x29,0x1f,0x9b,0x16,0x34,0xfb,0x20,0x9c,0x36,0x7,
  0xa7,0x72,0x75,0xca,0x1b,0x37,0x59,0xd0,0x18,0x4d,0xf7,0xc4,0x81,0x2f,0xf8,0xd1,
  0x9b,0x45,0x9e,0x7e,0xa8,0x37,0xf1,0xf6,0x4b,0x51,0x6d,0xe4,0xfa,0xb,0x98,0x65,
  0x54,0xee,0x8e,0x58,0xb7,0x8d,0x11,0xcb,0xe7,0xd2,0xd9,0x1b,0x82,0xff,0xbb,0x85,
  0xdc,0xd7,0x9b,0xd2,0x26,0xc3,0xd4,0x6f,0x8,0x97,0x59,0xb5,0xe2,0xd4,0x69,0x99,
  0x36,0x40,0x5d,0xd,0x19,0xf6,0x3c,0x70,0x3f,0xaf,0xbd,0x23,0x54,0xcb,0x6c,0xe2,
  0xc6,0xec,0xce,0x13,0x9b,0xbf,0x76,0x1a,0xbc,0xbb,0x30,0xcb,0x10,0x3d,0xb1,0x66,
  0x9d,0xf7,0x93,0x81,0x78,0xed,0x51,0xd2,0x58,0xfc,0x3b,0xa6,0x17,0x34,0xef,0x7e,
  0xbd,0xaf,0x35,0x95,0x88,0xe5,0x8d,0x54,0xa0,0x86,0x21,0x4f,0xeb,0xb9,0x3e,0x74,
  0xf9,0xb3,0xaf,0xe8,0x50,0xd9,0x1f,0xc7,0xb2,0x77,0xbf,0xd7,0x62,0xa1,0x30,0x59,
  0xaf,0x8c,0xf8,0x62,0xef,0x79,0x1e,0xa8,0x95,0xca,0xa7,0x22,0x13,0x4b,0x2e,0x5f,
  0x1a,0xd0,0x3f,0x8d,0x6c,0xf0,0xfe,0x61,0x4c,0xab,0x53,0xca,0xa4,0xec,0xdf,0x79,
  0x2d,0x88,0x83,0x47,0x35,0x4d,0xd7,0xd7,0x18,0x2d,0xb,0x42,0x99,0x49,0x6b,0xb3,
  0xd3,0xe3,0x8b,0x52,0x93,0x8d,0x87,0xab,0xc3,0x98,0xb7,0x4b,0x5b,0x6f,0x30,0x3f,
  0xb6,0x6d,0x76,0x97,0xf2,0xb8,0x6b,0x66,0x1f,0xdd,0x32,0x6c,0xd6,0x8d,0xbc,0x38,
  0xe5,0x49,0x59,0xbf,0xea,0xaa,0x6f,0xfe,0xe2,0x7,0x8,0xac,0x76,0xc8,0xc3,0x93,
  0x92,0xc7,0x69,0x5d,0x24,0xdf,0x3f,0xdc,0xe3,0x2,0x78,0x8e,0xbf,0x47,0x8f,0x71,
  0xce,0xfb,0x74,0x87,0x22,0xaf,0x36,0x7d,0x24,0x9a,0x52,0x4,0xe9,0x77,0x5c,0xd1,
  0xcd,0xf2,0xa9,0xe8,0xd4,0xe2,0x6c,0x3d,0xb2,0x18,0x1f,0x77,0x9e,0x16,0x6c,0x1c,
  0x3e,0xec,0xe,0xf,0x45,0xd9,0xfc,0xa0,0xa0,0xe0,0xd4,0xc2,0xed,0x4f,0x3b,0x70,
  0xb5,0x81,0x59,0xb8,0xba,0x22,0x66,0xe1,0x6a,0x24,0xb3,0x16,0xb,0xae,0x81,0x59,
  0x1b,0x4a,0xb7,0xe7,0xf,0xf3,0xf7,0x1,0xa1,0x73,0x98,0xd5,0x2e,0xb,0xaa,0xf7,
  0x33,0x52,0xa2,0x7e,0xb2,0xa5,0xc8,0xa5,0x99,0x96,0x5a,0xc6,0x71,0x6d,0x18,0xb0,
  0x91,0xe8,0x58,0x9c,0x30,0x7a,0xa8,0x97,0xea,0xcd,0x79,0xdb,0x28,0xef,0x66,0xed,
  0xff,0x0,0xea,0x9,0xf0,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/templates/dark.qss
  0x0,0x0,0x8,0x8,
  0x0,
  0x0,0x2a,0x5a,0x78,0x9c,0xc5,0x5a,0x4b,0x6f,0xd4,0x46,0x1c,0xbf,0x47,0xca,0x77,
  0x18,0xd2,0x4b,0x1b,0x75,0x59,0x42,0x89,0x84,0x8c,0x40,0xa,0x69,0x45,0xf,0xd0,
  0x2,0x89,0xca,0x1,0x21,0xe4,0x5d,0xf,0x1b,0x2b,0x5e,0x8f,0x65,0x7b,0x49,0xd2,
  0x28,0x12,0x28,0x14,0x8,0xa4,0x4,0x21,0xda,0xf2,0x8,0x22,0xa0,0x52,0xd2,0x3,
  0x81,0x16,0x4,0x9,0x49,0xe8,0x97,0xd9,0xf5,0x66,0x4f,0xfd,0xa,0x1d,0x7b,0x3c,
  0xde,0x99,0xf1,0xd8,0x6b,0xef,0x6,0x9a,0x53,0x76,0x1e,0xff,0xf7,0xe3,0xe7,0xbf,
  0x5d,0x1c,0x4,0xfd,0x7d,0x8e,0xab,0xda,0x2e,0xb0,0xf5,0x52,0x9,0x99,0xc0,0x81,
  0x6e,0x7f,0x9f,0xa6,0xda,0x93,0xc0,0x71,0x67,0xc,0x8,0xdc,0x9,0x58,0x85,0xfd,
  0x7d,0x83,0xc5,0xfe,0xbe,0xfe,0xbe,0xe2,0xe0,0xd8,0xc8,0xe9,0xe0,0xdc,0x88,0x65,
  0x19,0x7a,0x59,0x75,0x75,0x64,0x9e,0xd1,0xb5,0xa,0x74,0xfd,0x3,0x89,0x9b,0xb3,
  0xfd,0x7d,0x0,0xff,0x95,0xd4,0xf2,0x64,0xc5,0x46,0x35,0x53,0x2b,0x94,0x91,0x81,
  0x6c,0x5,0xcc,0xce,0xaa,0xe5,0x32,0x34,0xdd,0xb9,0xb9,0x43,0xe1,0x11,0x64,0x6b,
  0x10,0x6f,0x38,0xc8,0xd0,0xb5,0xf8,0x36,0xb9,0x36,0x3b,0x4b,0x84,0x2d,0xb8,0x70,
  0x9a,0xec,0xcd,0xf1,0xd2,0x1d,0x55,0x6d,0x56,0x1e,0xfc,0x33,0x90,0xa0,0x23,0xff,
  0x54,0xee,0x94,0xb7,0xcf,0x94,0xdc,0x6f,0xb3,0xe6,0x98,0xd7,0x5c,0x17,0x99,0xc7,
  0x30,0x1f,0x8b,0x68,0xff,0x25,0xdd,0x19,0x9b,0x71,0x5c,0x58,0x25,0xfb,0x58,0xa6,
  0xc6,0x9d,0xc5,0xd3,0x7a,0x65,0xc2,0x65,0x2e,0xb4,0xe6,0xb7,0xbd,0x7b,0x2f,0xeb,
  0xdb,0xff,0x34,0xef,0xae,0x7a,0x2f,0x7e,0xf7,0x6e,0x3d,0xaf,0x6f,0xbe,0xfd,0x77,
  0x6b,0xd1,0x52,0x4d,0x13,0x1a,0xad,0x6b,0x8b,0xad,0x47,0x4f,0x9b,0xf,0xae,0x78,
  0x8b,0xb,0xad,0x3b,0x6b,0xcd,0xcd,0x2b,0xf5,0x8d,0xc7,0xf1,0xf3,0x9c,0xea,0xa2,
  0x30,0x9,0x86,0x70,0x6d,0xd5,0x74,0x2c,0xd5,0xc6,0xea,0xb6,0xcd,0x9,0x12,0xa9,
  0xd4,0xd7,0x6f,0x12,0x19,0x76,0xd6,0x3e,0x34,0xb7,0xd7,0x52,0x39,0x82,0x23,0xe0,
  0xd4,0x38,0x42,0x6,0xd9,0x0,0xb3,0xbc,0xa3,0x4d,0x64,0x42,0xce,0xb9,0x20,0x66,
  0x61,0x79,0xe8,0xf0,0x12,0xfb,0x67,0x2c,0x55,0xd3,0x74,0xb3,0xa2,0x80,0x7d,0x60,
  0xbf,0x35,0x4d,0xd4,0xe8,0xa0,0x44,0x5b,0x2e,0xa2,0xe,0x98,0x40,0x17,0xa1,0xbd,
  0xd7,0xb2,0xa1,0xe3,0x40,0x6d,0x6f,0x79,0x2,0x96,0x27,0xa1,0x6,0x9a,0x37,0xde,
  0x7a,0x97,0x2e,0x83,0x1c,0x6a,0x2a,0x1,0x21,0xd0,0x31,0xf0,0xb,0xc1,0x39,0x1a,
  0x48,0x59,0x89,0x87,0xf2,0x75,0xa6,0x1e,0x1e,0xcc,0x4b,0x9f,0xea,0x9d,0xc2,0xa0,
  0x8c,0x4c,0x97,0xe1,0x50,0x28,0x55,0x28,0x93,0xbc,0x26,0xdf,0x59,0x5a,0x6e,0xfc,
  0xfc,0xb,0x63,0xe2,0xe2,0xe0,0x9,0x68,0xd6,0xc8,0x99,0x93,0xc8,0xaa,0x59,0x39,
  0xec,0x7e,0xd6,0xf2,0x2f,0x9c,0x40,0x1a,0x3c,0x3c,0x30,0x34,0x70,0x6e,0x96,0x8b,
  0x8c,0x82,0xed,0xa7,0x9b,0x2,0x86,0xd8,0xf0,0x20,0x8e,0xa,0xd9,0x17,0x80,0xc0,
  0x1a,0x78,0xab,0x4f,0x1a,0x5b,0x4b,0x38,0xe7,0x88,0x98,0x44,0xe6,0xd6,0xfc,0x6a,
  0xe3,0xfa,0xd5,0x3c,0xf1,0x20,0xc8,0xa5,0x28,0x55,0xcc,0xa7,0x50,0xca,0x17,0x2b,
  0x6d,0x6f,0x2,0x2c,0xb8,0xf7,0xf2,0x6e,0x7d,0x7d,0x93,0x8,0x14,0x50,0x20,0x3a,
  0x78,0xf,0xdf,0xd4,0xdf,0xaf,0x61,0x81,0x5b,0x4f,0x97,0x77,0x16,0xfe,0xe,0x84,
  0x24,0x9a,0x86,0xd7,0xff,0x5f,0x5d,0xa9,0x10,0xb9,0xb4,0xa5,0x1a,0x34,0x1f,0xbe,
  0xf1,0x6e,0x3d,0x6b,0x6c,0x6d,0x34,0xae,0xbd,0xf,0x65,0xc,0x44,0xc6,0x15,0xb2,
  0x71,0x79,0xa9,0xf9,0x7c,0xf3,0xa0,0x35,0x8d,0xff,0xdf,0x59,0xfb,0xd3,0x2f,0x50,
  0x37,0x17,0xc2,0x1a,0x35,0xbf,0xdd,0x5c,0xbe,0xd9,0xb8,0xfe,0xaa,0x5b,0x25,0xf6,
  0xf,0x9c,0x3,0xf2,0x48,0x3a,0xc8,0x4,0x92,0xf7,0xdb,0x4a,0x18,0xca,0xb4,0x40,
  0xd7,0xff,0x79,0xe4,0x2d,0x5e,0x6e,0xbc,0x5a,0xda,0xf9,0xb0,0xb1,0xf3,0xee,0x51,
  0xb7,0xec,0xf7,0x89,0x71,0xcc,0x55,0x38,0xb6,0xf5,0x9c,0xaa,0xe9,0xe5,0xc9,0x11,
  0x6c,0x3e,0xc7,0x11,0x5a,0x20,0xbf,0x3,0xf2,0x74,0x1,0x4a,0x62,0xd4,0xb5,0x8d,
  0x51,0x9c,0xf5,0xaa,0x6e,0x42,0x8e,0x36,0xb7,0x91,0x8b,0x74,0x9b,0xc6,0x18,0xc4,
  0x7b,0xaa,0x8b,0x6c,0x62,0x8c,0xfe,0x3e,0xef,0xf5,0xaa,0x77,0xc5,0x77,0x6c,0xc2,
  0x9,0x3f,0x4a,0xe7,0x17,0xbd,0xfb,0x2f,0x49,0x98,0x47,0x5d,0x30,0x60,0x47,0xf0,
  0x4a,0xc2,0x4d,0x22,0x61,0x14,0x6d,0xe,0xdd,0x95,0x1,0x89,0x51,0xd5,0x85,0x15,
  0x64,0xcf,0x70,0xea,0x86,0x6b,0x49,0x9a,0xb6,0x6b,0x63,0xbb,0x26,0x8a,0x77,0x95,
  0xb,0xa8,0x5c,0x73,0x8,0x5,0x54,0x73,0xd,0x6c,0xb9,0xa8,0x15,0xc6,0x6c,0x43,
  0x2f,0xe1,0x98,0xc6,0x5a,0x7f,0xa,0x8b,0x45,0x2a,0x1e,0x1,0x59,0x8c,0x98,0x1a,
  0x36,0x27,0x55,0x8c,0x5c,0x8,0x7c,0xe4,0x96,0xb2,0x5,0x8a,0x4,0x28,0x44,0x66,
  0x96,0x82,0x40,0x9e,0xc7,0x91,0xe4,0x56,0x14,0xf2,0xf,0xa9,0xf,0x59,0xd3,0x11,
  0xea,0x23,0x6b,0x22,0xc4,0x63,0x3d,0x12,0x10,0xff,0x74,0xee,0x10,0x75,0xe9,0x21,
  0xa0,0x3,0x52,0xc7,0xd5,0x12,0xa6,0x47,0x95,0xf0,0x56,0xae,0xb5,0x9e,0xde,0xf3,
  0x56,0x96,0x24,0x4e,0x22,0x27,0x13,0x6b,0x75,0x1c,0x83,0x25,0x61,0x38,0x99,0x18,
  0xdf,0x5b,0xfe,0x13,0x2,0x71,0x4c,0x4c,0x5d,0x76,0x93,0x73,0x55,0x14,0x7,0x9d,
  0xc5,0x49,0x43,0xec,0x89,0xcc,0xd8,0x6e,0x1c,0x63,0x21,0x87,0x6d,0xe2,0x13,0xc0,
  0x98,0xab,0xfa,0xf0,0x29,0xfe,0x60,0xc4,0x6d,0x8,0xf1,0xe7,0x46,0xf1,0x27,0x94,
  0xf,0x7a,0xa8,0xe0,0x22,0xab,0x30,0xa5,0x6b,0xee,0x4,0xae,0xff,0xb4,0xfa,0x4b,
  0x29,0x93,0xea,0x22,0x2d,0x2e,0x61,0xb3,0x92,0x3c,0xa9,0xc5,0xfd,0x10,0xdb,0x64,
  0x24,0x2e,0xd8,0xaa,0xa6,0xd7,0x1c,0x85,0xb4,0xa1,0x44,0x53,0x73,0xb2,0x1b,0xf0,
  0x82,0x4b,0xef,0x81,0x3,0xe1,0x45,0x66,0x3f,0xe8,0xa9,0xf1,0x3,0x59,0x5a,0x49,
  0xa2,0xc8,0xc4,0x9b,0x1d,0x9e,0xfe,0x44,0x6f,0x26,0x53,0x63,0x81,0x77,0x76,0xd8,
  0x9d,0x46,0x51,0xd3,0x1d,0xb5,0x64,0x84,0x24,0xf9,0xec,0xa1,0x5b,0xd4,0x92,0xbd,
  0x19,0x22,0x39,0x26,0x3a,0x5c,0x24,0xf0,0x4d,0x37,0x35,0x7f,0x19,0x85,0x99,0x51,
  0x1c,0x74,0x6a,0x25,0x3f,0x50,0x6d,0x64,0x14,0x2c,0xe4,0xe8,0xfe,0x5,0x5,0x4,
  0x2e,0x3c,0xe4,0x7,0x11,0x0,0x24,0x54,0xf7,0xc9,0x70,0xca,0xb8,0x5a,0x12,0xf0,
  0x9,0x59,0xc9,0x58,0x68,0x38,0x81,0xc9,0x4d,0x45,0x71,0xd5,0x92,0x50,0x8,0x43,
  0x67,0x44,0xfd,0x21,0xca,0xb6,0x76,0xb1,0x67,0xe9,0x16,0x7,0x1b,0x37,0x56,0xea,
  0xeb,0x37,0x30,0x58,0xf3,0x9e,0x5c,0xf5,0x21,0xe5,0xed,0x3f,0x1a,0xd7,0xef,0xb7,
  0x2e,0x2d,0xd4,0xd7,0x5f,0x34,0x6e,0xdf,0xc2,0xbb,0x8d,0x9f,0x9e,0x91,0x5d,0xfc,
  0xb3,0xbe,0xf5,0xc0,0x5b,0x5e,0xc0,0xa4,0x70,0x21,0x27,0xe0,0x93,0xa8,0xdd,0x16,
  0x3e,0x5e,0x90,0xaa,0xaa,0x5d,0xd1,0x4d,0x3f,0xd8,0x69,0xa,0x47,0x6b,0x21,0xa0,
  0x14,0x56,0xfd,0x94,0x51,0xc0,0x30,0xbf,0x58,0x42,0xd8,0x2f,0x55,0xe6,0x2c,0x5e,
  0xb,0xeb,0xc2,0x30,0x5d,0xa3,0x38,0x95,0x27,0x20,0xa0,0xd7,0xe1,0x64,0xdf,0x4,
  0xf6,0x54,0x1c,0x68,0xc0,0xb2,0x8b,0xd1,0x7a,0xdc,0x55,0xc2,0x1,0x49,0xf8,0x72,
  0x55,0xa0,0x33,0x4a,0xca,0xd3,0x8b,0x65,0xa2,0x4,0x39,0xac,0xec,0xe1,0x25,0x8a,
  0x39,0x5c,0x42,0x91,0x91,0x7b,0x6a,0x42,0x77,0x61,0x3a,0x9b,0x3d,0x69,0x2a,0x8b,
  0x19,0x9b,0x16,0x6e,0xe9,0xb1,0x32,0x27,0xe7,0x4e,0x19,0x7c,0x74,0xe6,0x42,0x4c,
  0x44,0xcf,0x25,0x3c,0x46,0x68,0xaf,0xf3,0x53,0x1d,0x66,0xa8,0xc3,0xb7,0xb,0x5a,
  0xd4,0x53,0x1a,0x86,0x24,0x58,0x4,0xe9,0x80,0x5c,0x4,0x16,0x53,0xcb,0x42,0x49,
  0x52,0x9b,0x93,0xc4,0xcb,0x13,0xc8,0xc9,0x7e,0x4b,0x9c,0xd7,0xec,0x82,0x49,0x3a,
  0x8e,0x78,0xe2,0xd3,0x9c,0xdd,0xe5,0x9e,0x3c,0xff,0x49,0x96,0x80,0x69,0xc6,0xbb,
  0x6d,0x5,0xe9,0x18,0x2d,0x71,0x1c,0xf7,0x49,0x59,0xf3,0x39,0x9b,0x91,0xbb,0x90,
  0xcc,0x19,0x7b,0x62,0x9b,0xff,0x31,0xd5,0x30,0xa0,0x3d,0xc3,0xe2,0xb6,0xf4,0xac,
  0x92,0xe5,0x8c,0x1c,0x4f,0x4b,0xc1,0xbd,0x94,0x6f,0x1a,0xf8,0x92,0x98,0x2f,0x63,
  0x7,0xe8,0x42,0xe,0xe,0x64,0x65,0x7f,0xe4,0xcc,0x6a,0x9,0xce,0x59,0xb1,0xe2,
  0xe9,0x4f,0xd9,0xd8,0xfe,0xe9,0xff,0x66,0xeb,0x77,0x77,0xed,0x92,0xd3,0xd6,0x27,
  0xa9,0x28,0xb8,0x77,0x55,0x9,0xe1,0x68,0x4e,0x34,0x9c,0x1d,0x44,0xc7,0x69,0x9,
  0xdd,0xbd,0xbb,0x2a,0xc0,0x90,0xe3,0x72,0x30,0x2d,0xd7,0x32,0xe2,0x0,0x9,0xb,
  0x31,0xd7,0x52,0x3b,0x64,0x37,0x66,0x29,0x87,0xd9,0xc4,0xe1,0xb3,0x21,0x19,0x8e,
  0xa,0x23,0x90,0xf5,0x7c,0xb8,0x94,0x79,0x70,0x94,0xda,0x88,0x72,0xc,0x2e,0x4,
  0xfe,0xc1,0x10,0x44,0x22,0x57,0xb0,0x4e,0x84,0x73,0x26,0xd0,0x54,0x41,0x83,0x65,
  0x64,0x7,0x4f,0x2,0x5,0x1a,0x9,0x98,0x5f,0xae,0x67,0xef,0x5e,0x24,0x97,0x49,
  0x27,0xd,0xcc,0xee,0x60,0x67,0x72,0xc8,0x4a,0x18,0xb2,0x33,0x81,0x9e,0xc5,0x4f,
  0x97,0x3a,0xf4,0x19,0x77,0xf9,0x7,0x1d,0x4e,0x59,0xc8,0x76,0x79,0xf4,0x25,0x6c,
  0xe6,0x99,0x47,0xfa,0x1c,0xe8,0xfc,0xe7,0xd4,0x71,0xfc,0x30,0xf8,0x8d,0xa6,0xfb,
  0x13,0xa9,0xe6,0xc2,0x46,0x63,0xf1,0x57,0xd9,0xec,0x29,0x3a,0x95,0x7,0x58,0xf7,
  0x18,0xe3,0xc4,0xcd,0x7e,0xfc,0xc9,0x8,0x31,0xbb,0x95,0xd8,0x5,0x39,0xa4,0x4f,
  0x54,0x4a,0x82,0x4c,0x78,0xf,0xeb,0xa6,0x55,0x73,0xb,0x1,0xc2,0x6c,0xbf,0xe7,
  0x8a,0x2c,0x38,0xea,0xe3,0x9b,0xa3,0x68,0x3a,0xdd,0x82,0xf4,0x54,0x86,0xa,0xd8,
  0x1,0x1,0xb,0xe,0x3c,0x8d,0xa1,0x4,0x22,0xad,0x2e,0x5d,0x2,0xe6,0xe0,0xae,
  0xb,0x31,0x8a,0xaa,0x25,0xd4,0xd9,0x6,0xe1,0xa9,0x78,0x2e,0xf5,0x18,0x47,0x32,
  0xef,0x52,0x66,0x2c,0xe,0xc9,0xe0,0xdb,0xd0,0x2a,0x20,0x1d,0x61,0xc4,0xb9,0x40,
  0x1c,0x48,0x7e,0x7f,0x61,0xf2,0x5b,0x46,0x23,0x63,0x5a,0x7c,0xfc,0xe0,0x8f,0x4,
  0x57,0x34,0x1b,0x59,0x5,0xd,0x4d,0x85,0x93,0x56,0x66,0xb4,0x83,0x6c,0x1d,0xf7,
  0x39,0x85,0x22,0x8a,0x43,0xc2,0x76,0x7b,0xf2,0xe3,0x22,0x2b,0x9c,0xfe,0xb4,0x67,
  0x3f,0x60,0x8,0x56,0xb9,0x92,0xdf,0x1e,0x38,0x65,0x11,0x46,0x56,0x75,0x53,0xa6,
  0xbf,0xe9,0x80,0x5c,0xc6,0x8,0xf3,0x28,0xa8,0xb6,0x8d,0xa6,0x8,0xf,0xbd,0xaa,
  0x56,0xa0,0x2,0x6a,0xb6,0xf1,0xb9,0x52,0xa4,0xf7,0x8a,0xc1,0x6a,0x11,0x37,0xb,
  0x54,0xb3,0xcb,0xb0,0x38,0xe2,0x9f,0xff,0x1a,0xdf,0xdc,0x6b,0x99,0x95,0x2f,0xe2,
  0x3d,0x56,0x98,0xcd,0xf3,0x15,0x5b,0x3a,0xb8,0xef,0xf0,0x55,0x4a,0xc2,0x1b,0xa9,
  0xb1,0x32,0xb6,0xbf,0x11,0x1f,0xda,0xca,0xf6,0x59,0x13,0x8a,0x83,0xdb,0xee,0x51,
  0x7f,0xce,0x39,0x9d,0x4c,0xb0,0xb3,0x81,0xf5,0xc7,0x67,0x2c,0x78,0x78,0xe0,0x2b,
  0xfa,0x92,0x95,0xca,0x19,0x4c,0x83,0x69,0x20,0xb5,0x11,0x56,0x56,0x7a,0x7,0x4,
  0x7a,0xc1,0xf4,0x59,0x24,0x97,0x46,0x2c,0xf5,0x45,0x40,0x42,0xb4,0x31,0x71,0x10,
  0x7c,0xeb,0x93,0x34,0x38,0x11,0x77,0x73,0xbe,0xc,0x93,0xd,0x6e,0x45,0x92,0x19,
  0x5e,0x31,0x2,0xef,0xdd,0x5f,0xfe,0xab,0x27,0xfa,0xda,0xbf,0xf9,0x7a,0xb3,0xb9,
  0xf9,0x98,0xfd,0xb0,0x87,0x7e,0x8f,0xa1,0x73,0x31,0x26,0xb2,0xfa,0x6c,0x6c,0x4,
  0x9f,0xd0,0xab,0xfa,0x8f,0xf0,0x8c,0x6e,0xe2,0xb4,0x62,0x95,0xca,0x94,0x55,0xe3,
  0xba,0x6b,0xc0,0x92,0x6a,0x9f,0xc7,0x74,0xce,0x7f,0x1b,0x7c,0x83,0xe3,0x5c,0xc,
  0xd2,0x8b,0x79,0x98,0x39,0x10,0x7b,0xe9,0x91,0x4d,0x8e,0x2e,0xde,0x1,0xe4,0xe5,
  0xd0,0xdd,0x7b,0x1,0x6c,0x58,0x75,0xba,0x93,0x61,0xd5,0xe9,0x4,0xc3,0xe6,0x36,
  0xad,0x3a,0x2d,0x9a,0x36,0xaf,0x71,0x25,0xb2,0xf4,0xf6,0xa1,0x53,0x56,0x26,0xc2,
  0x7,0x49,0xf9,0x14,0xff,0xe,0xd9,0x55,0xd5,0xe0,0x75,0x8f,0x9e,0xd2,0x47,0xd,
  0xe4,0xc0,0xe,0x3e,0x8,0xce,0xf4,0xee,0x80,0x80,0x4c,0x8f,0x2e,0x88,0x89,0xd2,
  0xa9,0x4a,0x95,0xfd,0xb,0x12,0x90,0x94,0x89,0x76,0x86,0x8f,0x75,0x28,0x7d,0x31,
  0xb6,0xff,0x3,0x1f,0x28,0x27,0x75,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/templates/dark2.qss
  0x0,0x0,0x7,0xfc,
  0x0,
  0x0,0x29,0x93,0x78,0x9c,0xc5,0x5a,0x5b,0x6f,0xd4,0x46,0x14,0x7e,0x8f,0x94,0xff,
  0x30,0x84,0x97,0x76,0x55,0xb3,0x24,0x24,0x12,0x72,0x4,0x52,0x48,0x2b,0xfa,0x0,
  0x2d,0x90,0xa8,0x3c,0x20,0x84,0xbc,0xeb,0x61,0x63,0xc5,0xeb,0xb1,0x6c,0x2f,0xd9,
  0x34,0x8a,0x4,0xa,0x5,0x2,0x29,0x41,0xa8,0x17,0xa0,0x89,0x48,0x50,0x29,0xe9,
  0x3,0x81,0x96,0xa,0x8,0x49,0xca,0x9f,0xd9,0xf5,0x66,0x9f,0xfa,0x17,0x3a,0xf6,
  0xf8,0x32,0x63,0x8f,0x6f,0x49,0x0,0x9e,0xc8,0xcc,0x99,0x73,0xbe,0x73,0x99,0x73,
  0xce,0x1c,0x6f,0xb9,0x4,0x7a,0x7b,0x4c,0x4b,0x32,0x2c,0x60,0x28,0x95,0xa,0xd2,
  0x80,0x9,0xad,0xde,0x1e,0x59,0x32,0x26,0x81,0x69,0x4d,0xab,0x10,0x58,0x13,0xb0,
  0xe,0xc1,0x40,0x6f,0x4f,0xa9,0xdc,0xdb,0xd3,0xdb,0x53,0x2e,0x8d,0x8d,0x9c,0x73,
  0x29,0x47,0x74,0x5d,0x55,0xaa,0x92,0xa5,0x20,0xed,0xbc,0x22,0xd7,0xa0,0xe5,0x10,
  0x24,0x6e,0xce,0xf4,0xf6,0x0,0xfc,0xaf,0x22,0x55,0x27,0x6b,0x6,0x6a,0x68,0xb2,
  0x50,0x45,0x2a,0x32,0x44,0x30,0x33,0x23,0x55,0xab,0x50,0xb3,0x66,0x67,0x87,0x3d,
  0x12,0x64,0xc8,0x10,0x6f,0x98,0x48,0x55,0xe4,0xf8,0x36,0x39,0x36,0x33,0x43,0xe0,
  0xa,0x16,0x6c,0x92,0xbd,0x59,0x16,0xdd,0x9,0xc9,0xa0,0xf1,0xe0,0x3f,0x5d,0x4,
  0x99,0xf2,0x53,0xa5,0x7,0x47,0x1c,0xa9,0x84,0x41,0x28,0x9b,0x91,0xde,0xb0,0x2c,
  0xa4,0x9d,0xc4,0x82,0x74,0xa2,0xfe,0x17,0xfe,0xce,0xd8,0xb4,0x69,0xc1,0x3a,0xd9,
  0xc7,0xa0,0xda,0xf7,0x17,0xce,0x29,0xb5,0x9,0x8b,0x3a,0xd0,0x9d,0xdb,0xb6,0x1f,
  0xbc,0x68,0x6d,0xbf,0xef,0xfc,0xb4,0x66,0x3f,0xff,0xdd,0xbe,0xfb,0xac,0xb5,0xf9,
  0xfa,0xbf,0xad,0x5,0x5d,0xd2,0x34,0xa8,0x76,0x6f,0x2e,0x74,0x97,0x9f,0x74,0x1e,
  0x5d,0xb7,0x17,0xe6,0xbb,0xf7,0xd7,0x3b,0x9b,0xd7,0x5b,0x1b,0x8f,0xe3,0xf4,0x8c,
  0xee,0x51,0x30,0x9,0x96,0xb0,0xc,0x49,0x33,0x75,0xc9,0xc0,0xfa,0x86,0xf6,0x4,
  0x89,0x5c,0x5a,0x6f,0xef,0x10,0xc,0x3b,0xeb,0xff,0x76,0xb6,0xd7,0x53,0x25,0x82,
  0xe3,0xe0,0xec,0x38,0x42,0x2a,0xd9,0x0,0x33,0xac,0xa7,0x35,0xa4,0x41,0xc6,0xbb,
  0x71,0xb,0xf3,0x63,0x87,0x45,0xec,0xd0,0xe8,0x92,0x2c,0x2b,0x5a,0x4d,0x4,0x87,
  0xc1,0x80,0xde,0x24,0x6a,0x64,0x28,0x11,0xe2,0x22,0xea,0x80,0x9,0x74,0x5,0x1a,
  0x87,0x74,0x3,0x9a,0x26,0x94,0xf,0x55,0x27,0x60,0x75,0x12,0xca,0xa0,0x73,0xfb,
  0xb5,0x7d,0xf5,0x1a,0x28,0xa0,0xa6,0xe8,0x32,0x2,0x29,0x91,0x5f,0x45,0x9a,0x85,
  0xb1,0xb,0x9e,0x2c,0xa1,0x52,0xf3,0xc3,0x29,0xaf,0x8,0xef,0x64,0x71,0x19,0x94,
  0xfd,0xfb,0xf5,0x66,0x10,0xee,0x64,0x89,0xd,0xed,0xbc,0x58,0x7c,0x4b,0x7d,0x50,
  0x30,0x45,0x9d,0xb9,0xb3,0xb8,0xd4,0xfe,0xf1,0x67,0xca,0x79,0xe5,0xd2,0x69,0xa8,
  0x35,0x8,0xcd,0x19,0xa4,0x37,0xf4,0x2,0x1e,0xbd,0xa0,0x3b,0x7,0x4e,0x23,0x19,
  0x1e,0xeb,0xeb,0xef,0xbb,0x38,0xc3,0xc4,0x9c,0x60,0x38,0x17,0x59,0x4,0xfd,0x74,
  0xe0,0x91,0x10,0xf0,0xc4,0xb,0x20,0x22,0x1a,0xd8,0x6b,0xab,0xed,0xad,0x45,0x7c,
  0x9b,0x9,0x4c,0x82,0xb9,0x3b,0xb7,0xd6,0xbe,0x75,0xa3,0x48,0xa4,0x45,0x70,0x89,
  0x62,0x1d,0xcb,0x11,0x2a,0x39,0xa3,0xd0,0x9c,0x36,0x3d,0x5a,0xc1,0xa5,0xa5,0x4c,
  0xed,0xf9,0xe9,0xd3,0x2a,0xe0,0x83,0x48,0x51,0x21,0x21,0x52,0x3a,0xbf,0xfd,0x63,
  0xdf,0x7d,0xda,0xde,0xda,0x68,0xdf,0x7c,0xe7,0x1,0x74,0xf1,0xe2,0x6c,0xda,0xbe,
  0xb6,0xd8,0x79,0xb6,0x79,0x54,0x6f,0xe2,0xff,0xef,0xac,0xff,0xe9,0x24,0xb3,0x3b,
  0xf3,0x5e,0x3e,0x9b,0xdb,0xee,0x2c,0xdd,0x69,0xdf,0x7a,0xb9,0x5b,0xd,0x6,0xfa,
  0x2e,0x2,0x7e,0x6c,0x1c,0xa5,0x42,0xc3,0xfe,0x75,0xc5,0xb,0x4e,0x3f,0x99,0xb7,
  0xde,0x2f,0xdb,0xb,0xd7,0xda,0x2f,0x17,0x77,0xfe,0xdd,0xd8,0x79,0xb3,0xbc,0x5b,
  0xf1,0x87,0xa3,0x91,0xc9,0x64,0x43,0xba,0x4c,0x9d,0x6d,0x28,0xd5,0xc9,0x11,0x5c,
  0xe1,0x4c,0x33,0x52,0x2f,0xd9,0x1d,0x50,0xa4,0x62,0xf8,0x2c,0x46,0x2d,0x43,0x1d,
  0xc5,0xf7,0x5d,0x52,0x34,0xc8,0xf0,0x66,0x36,0xa,0xb1,0xe,0x79,0x8c,0x41,0xbc,
  0x27,0x59,0xc8,0x20,0xc6,0xe8,0xed,0xb1,0x5f,0xad,0xd9,0xd7,0x1d,0xc7,0x26,0x50,
  0x38,0x21,0x3a,0xb7,0x60,0x3f,0x7c,0xd1,0x7d,0xb2,0xb4,0x33,0xff,0x77,0x50,0x31,
  0x5d,0x71,0xa4,0xb9,0x49,0x38,0x49,0x10,0x86,0xb7,0xc5,0xdf,0xe5,0x75,0x1d,0xa3,
  0x92,0x5,0x6b,0xc8,0x98,0x66,0xd4,0xf5,0xd6,0x92,0x34,0x8d,0xf4,0x18,0xe5,0x52,
  0x77,0xf5,0x35,0xbe,0x41,0xf6,0xd2,0x7c,0xeb,0xed,0x55,0x7b,0x79,0xb5,0xf3,0xee,
  0xbd,0xc3,0xce,0xcf,0x90,0x82,0x85,0x74,0x26,0x4b,0xfa,0x39,0x35,0x5e,0x3c,0x7c,
  0xc9,0xe2,0x65,0x54,0x6d,0x98,0x44,0x3e,0x6a,0x58,0x2a,0xb6,0x7b,0x50,0x74,0x63,
  0x96,0xf5,0xf,0xe1,0x1b,0x81,0x6d,0xf6,0x31,0xec,0x1d,0x18,0xe8,0x38,0xc8,0xe3,
  0x82,0xd4,0xa0,0x3b,0x23,0xe1,0x1e,0x89,0x74,0xaa,0xcc,0x52,0xb2,0xf1,0x23,0xe6,
  0xe3,0x74,0x25,0xa9,0x5d,0x1f,0x2b,0xe5,0x78,0x72,0x71,0xf2,0x10,0x78,0xcc,0xb3,
  0xca,0x1c,0xeb,0x13,0x97,0xf9,0xc7,0x73,0x48,0x54,0x97,0x3d,0x5c,0x8,0x97,0xd5,
  0x29,0xa9,0x82,0xf9,0xf9,0x4a,0xd8,0x2b,0x37,0xbb,0x4f,0x1e,0xd8,0x2b,0x8b,0x31,
  0x91,0x84,0x2e,0x47,0xfb,0xe0,0x96,0x29,0xba,0x79,0x8,0x28,0xa6,0x26,0x14,0xb,
  0x26,0x2,0xf9,0x56,0x77,0x1e,0x24,0xc4,0x35,0x31,0xe9,0xf4,0x26,0xe3,0xac,0x20,
  0x10,0xb2,0x1b,0x50,0xff,0x71,0x92,0x19,0x29,0xb4,0x30,0xba,0x42,0xc7,0x44,0x24,
  0x97,0x67,0x5a,0xbb,0x31,0x4b,0x72,0x5a,0xaf,0xf8,0x3b,0x8c,0xd9,0x88,0x44,0xa0,
  0xc5,0x7f,0xe4,0x84,0x69,0x46,0x98,0x52,0x64,0x6b,0x2,0x57,0xf,0xbf,0x76,0x70,
  0xb9,0x92,0xec,0x92,0x96,0x5c,0x38,0x8f,0xc2,0xb8,0xf,0x62,0x9b,0x14,0x5a,0xc1,
  0x90,0x64,0xa5,0x61,0x8a,0xa4,0x88,0x25,0x9a,0x99,0xc1,0xae,0xc2,0xcb,0x96,0x7f,
  0xe,0xc,0x7a,0x7,0xa9,0x7d,0xb7,0x22,0xc7,0x9,0xb2,0x33,0x4,0xe3,0xcc,0x18,
  0x6a,0xe2,0xcc,0xcc,0x6c,0x13,0xef,0xf8,0xd3,0x78,0xd2,0x5d,0xfe,0xfe,0x71,0xcd,
  0xf0,0x5b,0xf2,0x41,0xd2,0x9d,0x29,0x9a,0xec,0x2c,0x23,0x2f,0x72,0xcb,0x25,0xb3,
  0x51,0x71,0x80,0x18,0x48,0x15,0x74,0x64,0x2a,0xce,0x1,0x11,0xb8,0x66,0x1e,0x26,
  0x5,0x8c,0x84,0xd3,0x61,0x5e,0x27,0x32,0x2e,0x55,0x22,0x1d,0x8,0x59,0x49,0x4c,
  0x6,0x9c,0x2a,0xc0,0x9e,0x14,0x45,0x4b,0xaa,0x44,0x52,0x15,0x89,0xf3,0x70,0x66,
  0xc0,0x79,0x75,0xd0,0x7c,0xcb,0xa5,0xf6,0xed,0x95,0xd6,0xdb,0xdb,0xb8,0x1d,0xb3,
  0x57,0x6f,0x38,0x4d,0xe3,0xbd,0x3f,0xda,0xb7,0x1e,0x76,0xaf,0xe2,0xca,0xfc,0xbc,
  0x7d,0xef,0x2e,0xde,0x6d,0xff,0xf0,0x94,0xec,0xe2,0x3f,0x5b,0x5b,0x8f,0x70,0xd1,
  0xc6,0xac,0x70,0xaa,0x25,0xed,0xa5,0x57,0xb7,0x3,0xf0,0xf1,0x84,0x51,0x97,0x8c,
  0x9a,0xa2,0xb9,0x35,0xdd,0xbb,0x66,0xc1,0x9a,0xd7,0x32,0x46,0x56,0x9d,0xb0,0x16,
  0xc1,0x10,0xbb,0x58,0x41,0xd8,0x2f,0x75,0x8a,0x16,0xaf,0x79,0x77,0x77,0xc8,0x5f,
  0xf3,0x3b,0x51,0x96,0x41,0xa4,0x3f,0x1d,0xa,0x7d,0xc3,0xb1,0xa6,0x68,0x42,0x15,
  0x56,0x2d,0xbf,0x15,0x4f,0x7e,0xab,0x17,0xbd,0x45,0xb4,0xc,0xf7,0xfe,0x88,0x7,
  0x58,0x51,0xe5,0x52,0xcc,0x51,0x7,0x8f,0xc,0xe,0xe,0xc,0x1d,0x19,0x2e,0xd2,
  0x1b,0x85,0xf9,0xc4,0x55,0x37,0xf,0xa5,0x6b,0xae,0x3c,0x84,0x9e,0xf,0xf8,0x91,
  0xc4,0x98,0x2b,0x52,0xa3,0x78,0x46,0x38,0x90,0x60,0xe9,0x68,0x0,0x47,0x6f,0x51,
  0xf0,0x1e,0x60,0x5b,0xa0,0x70,0x9d,0x9d,0xbc,0x50,0x83,0x17,0x36,0xd1,0xfa,0xe9,
  0x30,0x4c,0xb5,0x21,0x68,0xee,0x7d,0x8c,0xfb,0x17,0xf0,0x1,0xd0,0xbd,0x68,0xae,
  0x66,0x88,0xf,0x6d,0x96,0xcb,0x3b,0x32,0xd,0xd9,0x9b,0x4a,0xb9,0xe6,0x33,0xf1,
  0xf1,0xc7,0xee,0x84,0xe7,0xb3,0xc4,0xfe,0xa1,0xa4,0xca,0xd4,0x27,0xc1,0xca,0x74,
  0x70,0x7c,0xa4,0xec,0xdc,0xe2,0x3,0xfa,0x92,0xe,0xd4,0x93,0x92,0xaa,0x42,0x63,
  0x9a,0xee,0x40,0x32,0x43,0x9d,0x6f,0x92,0xc4,0x51,0x57,0xca,0x73,0x82,0x8b,0x22,
  0xad,0xa3,0x48,0x11,0x93,0xd3,0x4d,0xb9,0x47,0xda,0xce,0xe4,0x87,0xae,0xcf,0xce,
  0xdf,0x74,0x76,0x2a,0x58,0x6,0x92,0x2,0xc0,0x61,0x2b,0x8a,0xd8,0x99,0x75,0xc2,
  0x3c,0x18,0x61,0xc,0x15,0xea,0xd0,0xe2,0xec,0x22,0xc5,0x2b,0x6f,0xee,0xd9,0xd5,
  0x7d,0xa3,0xa4,0x32,0x51,0x9c,0x68,0x29,0x1e,0x9e,0xf8,0x70,0x7b,0x4f,0x60,0xaa,
  0x41,0xf2,0x67,0x3a,0x89,0x7e,0x5e,0x37,0xe6,0xc5,0x1f,0xed,0x6e,0x6f,0x29,0xfb,
  0x1d,0x1d,0x79,0x9b,0xa5,0x34,0x9,0x1c,0xf,0x90,0xb1,0x9f,0xbb,0xc1,0xab,0x6f,
  0x1e,0x8,0xf7,0x55,0xcd,0x1,0xe7,0xae,0x13,0x84,0xe6,0x4,0x9a,0x12,0x64,0x58,
  0x45,0x86,0xdb,0xb8,0xa,0xbe,0xf3,0xb1,0xb8,0x42,0x4f,0xb9,0x3d,0x0,0xe7,0x81,
  0xa3,0x23,0x3b,0x7f,0x1e,0xa0,0x8e,0xee,0x4b,0xb,0xb6,0xbb,0x5c,0x91,0x8e,0x2b,
  0xed,0xd,0x5b,0x4c,0x34,0xa7,0xb3,0x77,0x62,0x80,0x91,0xfc,0x9d,0x2,0xa7,0x74,
  0x64,0x58,0x6c,0x9b,0x13,0xd9,0x2c,0x18,0xab,0x5e,0xac,0xf9,0x63,0x8a,0xb3,0xa7,
  0xf0,0x8b,0xe8,0x2b,0x59,0x71,0x6,0x27,0x9d,0xf9,0x8d,0xf6,0xc2,0x2f,0xbc,0x11,
  0x49,0x40,0xc5,0x64,0x15,0xda,0x8e,0xec,0xdb,0x7a,0x3f,0x2e,0xe,0x9,0x1,0x27,
  0xaa,0x73,0x8d,0xc2,0xe9,0x3,0x39,0x62,0x2e,0xa6,0x19,0xa7,0x55,0xc8,0x37,0xbf,
  0xa,0x2c,0x39,0xea,0xf4,0x1c,0x27,0x50,0x33,0xdd,0x92,0x3e,0x55,0x8e,0x5c,0x19,
  0x55,0x9b,0x3b,0x12,0xc,0xc4,0x9f,0xc3,0x4d,0x3,0x22,0xb5,0x34,0x1d,0x1,0x45,
  0xb8,0xef,0x20,0x46,0x51,0xbd,0x82,0xb2,0x6d,0xe0,0x51,0x51,0xb7,0x1b,0xe4,0xce,
  0x41,0xb9,0x23,0x2d,0x5e,0x23,0x62,0x0,0xe8,0xa6,0xa3,0x60,0x72,0x88,0xf3,0x82,
  0x38,0x8a,0xa4,0x8a,0xa,0xb3,0xb5,0xca,0x7f,0x3d,0x3e,0xd2,0x25,0x8,0x74,0x10,
  0x65,0x3,0xe9,0x82,0x8c,0xa6,0xbc,0xd9,0x20,0x35,0xec,0x40,0xf8,0x35,0xa9,0x68,
  0xa2,0xdf,0xaa,0xc,0x47,0xb6,0xc3,0x59,0x8,0x7e,0x9d,0x7a,0xf3,0x90,0x70,0x1a,
  0x2,0xfa,0x61,0x9d,0xf1,0x68,0x38,0x82,0xc9,0x3,0x86,0xc9,0xbb,0xd1,0xc1,0x75,
  0xde,0x2f,0x65,0x69,0x82,0xb0,0xc,0x41,0x32,0xc,0x34,0x45,0x64,0x28,0x75,0xa9,
  0x6,0x45,0xd0,0x30,0xd4,0xcf,0xc4,0xb2,0x7f,0xae,0xec,0xae,0x96,0x71,0x6a,0x47,
  0xd,0xa3,0xa,0xcb,0x23,0xe,0xfd,0x97,0xf8,0xe4,0x21,0x5d,0xab,0x7d,0x1e,0x2f,
  0xe3,0x91,0x79,0x32,0x9b,0xc4,0xb9,0xc3,0xe6,0x8c,0xaf,0x26,0x9,0x5f,0x61,0xc6,
  0xaa,0xd8,0xfe,0x6a,0x7c,0xd4,0xc8,0xdb,0xa7,0x4d,0x18,0x1d,0x37,0xee,0xb1,0x95,
  0x2e,0x3e,0xf,0xe1,0x1,0xbc,0xe0,0x7a,0x61,0x7c,0x5a,0x87,0xc7,0xfa,0x8e,0xf8,
  0x1f,0x18,0xe9,0x71,0x86,0x3f,0xf1,0xe9,0x8f,0xcf,0x70,0xb2,0xf8,0xd,0x46,0xf8,
  0xb9,0xb3,0xd3,0x28,0xbb,0x34,0x66,0xbb,0x2c,0xff,0xb3,0x4c,0x54,0xb8,0xbf,0x8c,
  0x49,0x1a,0x61,0x44,0x77,0xf3,0x7d,0x37,0x8c,0x4e,0x3a,0x66,0xd3,0x58,0xe6,0xf8,
  0x4c,0x6,0xec,0x37,0x7f,0x39,0x1f,0x4f,0xfc,0xaf,0xde,0x9d,0x57,0x9b,0x9d,0xcd,
  0xc7,0xf4,0xcf,0x60,0xfc,0xdf,0x18,0x28,0x4c,0xc4,0x45,0x45,0x1d,0x1c,0x1b,0xc1,
  0x14,0x4a,0x5d,0xf9,0x1e,0x9e,0x57,0x34,0x7c,0xc9,0x68,0xa5,0x72,0xdd,0xb1,0x71,
  0xc5,0x52,0x61,0x45,0x32,0x2e,0x61,0x3e,0x97,0xbe,0x76,0x7f,0xb1,0x62,0x5e,0x71,
  0x2f,0x1b,0xf5,0x66,0x8a,0xcd,0x4a,0x72,0xe2,0xc8,0x18,0x62,0x27,0x7d,0x91,0x28,
  0x2a,0x25,0x6b,0xac,0x4d,0xc9,0xf1,0x48,0xc3,0xa8,0x39,0x2d,0x35,0xb3,0xc,0x2c,
  0x35,0x13,0xc,0x5c,0xd8,0xc4,0x52,0x33,0x6a,0xe2,0xa2,0x46,0xe6,0x60,0xd9,0xfd,
  0xf,0x33,0x8a,0xa,0x8a,0xfc,0x30,0xa7,0x98,0xf2,0xdf,0x20,0xa3,0x2e,0xa9,0xac,
  0xfe,0x41,0x53,0x3e,0xaa,0x22,0x13,0x66,0xf8,0xc1,0xa5,0xd9,0xbb,0x13,0x5c,0x36,
  0x7b,0x74,0x43,0xc,0x4a,0x66,0xde,0x72,0xe,0x70,0xfa,0xa5,0x5c,0xbc,0x99,0xdf,
  0xad,0xa4,0x71,0x67,0xa3,0xfb,0x7f,0x8e,0x1c,0xe3,0xc0,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/palettes/office2021-blue.json
  0x0,0x0,0x8,0xa8,
  0x7b,
  0xa,0x20,0x20,0x22,0x6e,0x61,0x6d,0x65,0x22,0x3a,0x20,0x22,0x4f,0x66,0x66,0x69,
  0x63,0x65,0x20,0x32,0x30,0x32,0x31,0x20,0x42,0x6c,0x75,0x65,0x22,0x2c,0xa,0x20,
  0x20,0x22,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x22,0x3a,0x20,0x22,0x31,0x2e,0x30,
  0x22,0x2c,0xa,0x20,0x20,0x22,0x69,0x73,0x44,0x61,0x72,0x6b,0x22,0x3a,0x20,0x66,
  0x61,0x6c,0x73,0x65,0x2c,0xa,0xa,0x20,0x20,0x22,0x6b,0x65,0x79,0x43,0x6f,0x6c,
  0x6f,0x72,0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,
  0x65,0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x65,0x35,0x65,0x33,0x65,0x35,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x34,0x32,0x34,0x32,0x34,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,
  0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,
  0x66,0x66,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,
  0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x32,0x34,0x32,0x34,0x32,0x34,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x35,0x65,0x33,0x65,0x35,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,0x2d,0x74,0x65,0x78,
  0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x32,0x62,
  0x32,0x62,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,0x61,0x72,
  0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x62,0x65,0x63,0x30,0x63,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x73,0x75,0x62,0x74,0x69,0x74,0x6c,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x36,0x36,0x36,0x36,0x36,0x36,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x70,0x61,0x6e,0x65,0x6c,0x2d,0x62,0x6f,0x72,0x64,0x65,
  0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x36,0x30,0x36,0x30,
  0x36,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6f,0x72,0x64,0x65,0x72,
  0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x63,0x35,0x63,0x35,0x63,0x35,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,
  0x6e,0x70,0x75,0x74,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x63,0x30,0x63,0x32,0x63,0x34,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x66,0x6f,0x63,0x75,0x73,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x36,0x39,0x62,0x66,
  0x34,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x6c,0x65,0x63,0x74,0x69,
  0x6f,0x6e,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,
  0x39,0x62,0x62,0x62,0x66,0x37,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x6d,0x65,
  0x6e,0x75,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x22,0x23,0x66,0x63,0x66,0x63,0x66,0x63,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x38,0x31,0x31,0x32,0x33,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,
  0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x66,
  0x31,0x37,0x30,0x37,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,0x73,
  0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,
  0x20,0x20,0x22,0x23,0x66,0x35,0x66,0x36,0x66,0x36,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x73,0x79,0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x70,0x72,0x65,
  0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x22,0x23,0x63,0x61,0x63,0x61,0x63,0x62,0x22,
  0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x64,0x65,0x72,0x69,0x76,0x65,
  0x64,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,
  0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,
  0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x61,0x63,0x63,0x65,
  0x6e,0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,
  0x6e,0x74,0x22,0x3a,0x20,0x31,0x30,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,
  0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,
  0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x31,0x35,0x20,0x7d,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,
  0x65,0x72,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,
  0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,
  0x73,0x65,0x22,0x3a,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,
  0x22,0x2c,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x31,
  0x37,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,
  0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x20,0x7b,
  0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,0x2c,
  0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,
  0x6e,0x74,0x2d,0x62,0x67,0x22,0x2c,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,
  0x74,0x22,0x3a,0x20,0x32,0x33,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,
  0x61,0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,
  0x3a,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,
  0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,
  0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x35,0x20,0x20,0x7d,0xa,0x20,0x20,
  0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x66,0x69,0x78,0x65,0x64,0x22,0x3a,0x20,0x7b,
  0xa,0x20,0x20,0x20,0x20,0x22,0x77,0x68,0x69,0x74,0x65,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,0x66,0x66,0x66,0x66,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6c,0x61,0x63,0x6b,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x30,0x30,0x30,0x30,0x30,0x30,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x35,0x65,0x33,0x65,0x35,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,0x2d,0x74,0x65,0x78,
  0x74,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x62,0x32,0x62,0x32,0x62,0x32,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,0x61,0x72,0x61,0x74,0x6f,0x72,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x65,0x63,0x30,0x63,0x32,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x62,0x61,0x73,0x65,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,0x66,0x66,0x66,0x66,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x74,0x65,0x78,0x74,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x30,0x30,0x30,0x30,0x30,0x30,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x61,0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,0x74,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x37,0x36,0x30,0x61,0x37,0x22,0xa,
  0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x63,0x6f,0x6d,0x6d,0x65,0x6e,0x74,
  0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,
  0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x52,0x69,
  0x62,0x62,0x6f,0x6e,0x20,0x62,0x61,0x72,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,
  0x75,0x6e,0x64,0x20,0x2b,0x20,0x61,0x70,0x70,0x6c,0x69,0x63,0x61,0x74,0x69,0x6f,
  0x6e,0x20,0x77,0x69,0x64,0x67,0x65,0x74,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,
  0x75,0x6e,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,
  0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x54,0x61,
  0x62,0x20,0x68,0x6f,0x76,0x65,0x72,0x2c,0x20,0x61,0x70,0x70,0x6c,0x69,0x63,0x61,
  0x74,0x69,0x6f,0x6e,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x68,0x6f,0x76,0x65,
  0x72,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,
  0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x22,0x41,0x70,0x70,0x6c,
  0x69,0x63,0x61,0x74,0x69,0x6f,0x6e,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x70,
  0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,
  0x63,0x65,0x6e,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x22,0x54,0x61,0x62,0x20,0x75,0x6e,0x73,0x65,0x6c,0x65,0x63,0x74,0x65,0x64,0x20,
  0x74,0x65,0x78,0x74,0x20,0x28,0x6d,0x61,0x70,0x73,0x20,0x74,0x6f,0x20,0x74,0x65,
  0x78,0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x20,0x69,0x6e,0x20,0x6f,0x66,0x66,0x69,
  0x63,0x65,0x32,0x30,0x32,0x31,0x29,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,
  0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x43,0x61,0x74,0x65,0x67,0x6f,0x72,0x79,0x2f,0x50,0x61,0x6e,0x65,0x6c,
  0x2f,0x54,0x6f,0x6f,0x6c,0x42,0x75,0x74,0x74,0x6f,0x6e,0x2f,0x47,0x61,0x6c,0x6c,
  0x65,0x72,0x79,0x2f,0x4d,0x65,0x6e,0x75,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,
  0x75,0x6e,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,
  0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,0x67,0x22,0x3a,0x22,0x48,0x6f,
  0x76,0x65,0x72,0x20,0x73,0x74,0x61,0x74,0x65,0x20,0x66,0x6f,0x72,0x20,0x62,0x75,
  0x74,0x74,0x6f,0x6e,0x73,0x2c,0x20,0x73,0x63,0x72,0x6f,0x6c,0x6c,0x20,0x62,0x75,
  0x74,0x74,0x6f,0x6e,0x73,0x2c,0x20,0x6d,0x65,0x6e,0x75,0x20,0x69,0x74,0x65,0x6d,
  0x73,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,
  0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x22,0x50,0x72,
  0x65,0x73,0x73,0x65,0x64,0x2f,0x63,0x68,0x65,0x63,0x6b,0x65,0x64,0x20,0x73,0x74,
  0x61,0x74,0x65,0x20,0x66,0x6f,0x72,0x20,0x74,0x6f,0x6f,0x6c,0x20,0x62,0x75,0x74,
  0x74,0x6f,0x6e,0x73,0x2c,0x20,0x6d,0x65,0x6e,0x75,0x20,0x69,0x74,0x65,0x6d,0x73,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,0x2d,0x63,0x6f,0x6c,
  0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x50,0x72,0x69,0x6d,0x61,
  0x72,0x79,0x20,0x74,0x65,0x78,0x74,0x20,0x63,0x6f,0x6c,0x6f,0x72,0x20,0x28,0x74,
  0x6f,0x6f,0x6c,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x73,0x2c,0x20,0x6d,0x65,0x6e,
  0x75,0x73,0x2c,0x20,0x67,0x61,0x6c,0x6c,0x65,0x72,0x79,0x29,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x74,0x61,0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x54,0x61,0x62,0x20,0x73,0x65,0x6c,0x65,0x63,
  0x74,0x65,0x64,0x20,0x74,0x65,0x78,0x74,0x20,0x63,0x6f,0x6c,0x6f,0x72,0x20,0x61,
  0x6e,0x64,0x20,0x75,0x6e,0x64,0x65,0x72,0x6c,0x69,0x6e,0x65,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x74,0x61,0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,
  0x6f,0x76,0x65,0x72,0x22,0x3a,0x22,0x54,0x61,0x62,0x20,0x68,0x6f,0x76,0x65,0x72,
  0x20,0x75,0x6e,0x64,0x65,0x72,0x6c,0x69,0x6e,0x65,0x2c,0x20,0x64,0x65,0x72,0x69,
  0x76,0x65,0x64,0x20,0x66,0x72,0x6f,0x6d,0x20,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,
  0xa,0x20,0x20,0x7d,0xa,0x7d,0xa,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/palettes/dark-default.json
  0x0,0x0,0x6,0x8d,
  0x7b,
  0xa,0x20,0x20,0x22,0x6e,0x61,0x6d,0x65,0x22,0x3a,0x20,0x22,0x44,0x61,0x72,0x6b,
  0x20,0x44,0x65,0x66,0x61,0x75,0x6c,0x74,0x22,0x2c,0xa,0x20,0x20,0x22,0x76,0x65,
  0x72,0x73,0x69,0x6f,0x6e,0x22,0x3a,0x20,0x22,0x31,0x2e,0x30,0x22,0x2c,0xa,0x20,
  0x20,0x22,0x69,0x73,0x44,0x61,0x72,0x6b,0x22,0x3a,0x20,0x74,0x72,0x75,0x65,0x2c,
  0xa,0xa,0x20,0x20,0x22,0x6b,0x65,0x79,0x43,0x6f,0x6c,0x6f,0x72,0x73,0x22,0x3a,
  0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x31,
  0x65,0x31,0x65,0x31,0x65,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,
  0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x34,0x61,0x34,0x61,0x34,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x35,0x35,0x35,0x35,0x35,0x35,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x74,0x65,
  0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x30,
  0x66,0x31,0x66,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,
  0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x32,0x64,0x32,0x64,0x32,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,
  0x67,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x33,0x61,0x33,0x61,0x33,0x61,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x70,0x72,
  0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x20,0x22,0x23,0x35,0x30,0x35,
  0x30,0x35,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,0x2d,
  0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x65,0x30,0x65,0x30,0x65,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x74,0x65,0x78,0x74,0x2d,0x64,0x69,0x73,0x61,0x62,0x6c,0x65,0x64,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x36,0x36,0x36,0x36,0x36,0x36,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x31,0x65,0x31,0x65,
  0x31,0x65,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,
  0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x62,0x32,0x62,0x32,0x62,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,
  0x65,0x70,0x61,0x72,0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x35,0x61,0x35,0x61,0x35,0x61,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x73,0x75,0x62,0x74,0x69,0x74,0x6c,0x65,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x39,0x39,0x39,0x39,0x39,
  0x39,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x70,0x61,0x6e,0x65,0x6c,0x2d,0x62,
  0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,
  0x34,0x61,0x34,0x61,0x34,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6f,
  0x72,0x64,0x65,0x72,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x22,0x23,0x34,0x61,0x34,0x61,0x34,0x61,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x34,0x61,0x34,0x61,0x34,0x61,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x66,0x6f,
  0x63,0x75,0x73,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x35,
  0x61,0x35,0x61,0x35,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x6c,
  0x65,0x63,0x74,0x69,0x6f,0x6e,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x36,0x61,0x39,0x65,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x6d,0x65,0x6e,0x75,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x33,0x61,0x33,0x61,0x33,0x61,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x38,
  0x31,0x31,0x32,0x33,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,
  0x65,0x2d,0x62,0x67,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,
  0x20,0x22,0x23,0x66,0x31,0x37,0x30,0x37,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x73,0x79,0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x68,0x6f,0x76,0x65,
  0x72,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x34,0x61,0x34,0x61,0x34,0x61,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,
  0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x22,0x23,0x35,0x35,0x35,
  0x35,0x35,0x35,0x22,0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x64,0x65,
  0x72,0x69,0x76,0x65,0x64,0x22,0x3a,0x20,0x7b,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,
  0x66,0x69,0x78,0x65,0x64,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x77,
  0x68,0x69,0x74,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x66,0x66,0x66,0x66,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,
  0x6c,0x61,0x63,0x6b,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x30,0x30,0x30,0x30,0x30,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,
  0x6e,0x70,0x75,0x74,0x2d,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,
  0x23,0x33,0x61,0x33,0x61,0x33,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,
  0x6e,0x70,0x75,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,
  0x23,0x65,0x30,0x65,0x30,0x65,0x30,0x22,0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,
  0x20,0x22,0x63,0x6f,0x6d,0x6d,0x65,0x6e,0x74,0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,
  0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x52,0x69,0x62,0x62,0x6f,0x6e,0x20,0x62,0x61,
  0x72,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x20,0x28,0x64,0x61,
  0x72,0x6b,0x65,0x73,0x74,0x29,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,
  0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x22,0x42,0x75,0x74,0x74,0x6f,0x6e,0x20,0x68,0x6f,0x76,0x65,0x72,0x20,0x73,0x74,
  0x61,0x74,0x65,0x73,0x2c,0x20,0x73,0x79,0x73,0x74,0x65,0x6d,0x20,0x62,0x75,0x74,
  0x74,0x6f,0x6e,0x20,0x68,0x6f,0x76,0x65,0x72,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,
  0x3a,0x20,0x20,0x22,0x42,0x75,0x74,0x74,0x6f,0x6e,0x20,0x70,0x72,0x65,0x73,0x73,
  0x65,0x64,0x20,0x73,0x74,0x61,0x74,0x65,0x73,0x2c,0x20,0x73,0x79,0x73,0x74,0x65,
  0x6d,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x70,0x72,0x65,0x73,0x73,0x65,0x64,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x74,
  0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x22,0x54,0x61,0x62,0x20,0x75,
  0x6e,0x73,0x65,0x6c,0x65,0x63,0x74,0x65,0x64,0x20,0x74,0x65,0x78,0x74,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x43,0x61,0x74,0x65,0x67,0x6f,0x72,
  0x79,0x2f,0x50,0x61,0x6e,0x65,0x6c,0x2f,0x4d,0x65,0x6e,0x75,0x2f,0x47,0x61,0x6c,
  0x6c,0x65,0x72,0x79,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x68,
  0x6f,0x76,0x65,0x72,0x2d,0x62,0x67,0x22,0x3a,0x22,0x53,0x6c,0x69,0x67,0x68,0x74,
  0x6c,0x79,0x20,0x6c,0x69,0x67,0x68,0x74,0x65,0x72,0x20,0x66,0x6f,0x72,0x20,0x68,
  0x6f,0x76,0x65,0x72,0x20,0x73,0x74,0x61,0x74,0x65,0x73,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,
  0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x22,0x50,0x72,0x65,0x73,0x73,0x65,0x64,0x2f,
  0x63,0x68,0x65,0x63,0x6b,0x65,0x64,0x20,0x73,0x74,0x61,0x74,0x65,0x20,0x66,0x6f,
  0x72,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x73,0x2c,0x20,0x6d,0x65,0x6e,0x75,0x20,
  0x69,0x74,0x65,0x6d,0x73,0x2c,0x20,0x67,0x61,0x6c,0x6c,0x65,0x72,0x79,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x4c,0x69,0x67,0x68,0x74,0x20,0x74,
  0x65,0x78,0x74,0x20,0x6f,0x6e,0x20,0x64,0x61,0x72,0x6b,0x20,0x62,0x61,0x63,0x6b,
  0x67,0x72,0x6f,0x75,0x6e,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,
  0x78,0x74,0x2d,0x64,0x69,0x73,0x61,0x62,0x6c,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,
  0x22,0x44,0x69,0x73,0x61,0x62,0x6c,0x65,0x64,0x20,0x74,0x65,0x78,0x74,0x20,0x63,
  0x6f,0x6c,0x6f,0x72,0x22,0xa,0x20,0x20,0x7d,0xa,0x7d,0xa,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/palettes/office2016-blue.json
  0x0,0x0,0x7,0x73,
  0x7b,
  0xa,0x20,0x20,0x22,0x6e,0x61,0x6d,0x65,0x22,0x3a,0x20,0x22,0x4f,0x66,0x66,0x69,
  0x63,0x65,0x20,0x32,0x30,0x31,0x36,0x20,0x42,0x6c,0x75,0x65,0x22,0x2c,0xa,0x20,
  0x20,0x22,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x22,0x3a,0x20,0x22,0x31,0x2e,0x30,
  0x22,0x2c,0xa,0x20,0x20,0x22,0x69,0x73,0x44,0x61,0x72,0x6b,0x22,0x3a,0x20,0x66,
  0x61,0x6c,0x73,0x65,0x2c,0xa,0xa,0x20,0x20,0x22,0x6b,0x65,0x79,0x43,0x6f,0x6c,
  0x6f,0x72,0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,
  0x65,0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x32,0x32,0x35,0x34,0x39,0x37,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x64,0x32,0x65,0x34,0x66,0x66,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,
  0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x31,
  0x66,0x31,0x66,0x31,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,
  0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x33,0x33,0x33,0x33,0x33,0x33,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x37,0x30,0x37,0x30,0x37,0x30,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,0x2d,0x74,0x65,0x78,
  0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x32,0x62,
  0x32,0x62,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,0x61,0x72,
  0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x62,0x65,0x63,0x30,0x63,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x73,0x75,0x62,0x74,0x69,0x74,0x6c,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x36,0x36,0x36,0x36,0x36,0x36,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x70,0x61,0x6e,0x65,0x6c,0x2d,0x62,0x6f,0x72,0x64,0x65,
  0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x63,0x36,0x63,0x36,
  0x63,0x36,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,
  0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x63,0x32,0x64,0x30,0x64,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,
  0x6e,0x70,0x75,0x74,0x2d,0x66,0x6f,0x63,0x75,0x73,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x36,0x39,0x62,0x66,0x34,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x73,0x65,0x6c,0x65,0x63,0x74,0x69,0x6f,0x6e,0x2d,0x62,0x67,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x39,0x62,0x62,0x62,0x66,
  0x37,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x6d,0x65,0x6e,0x75,0x2d,0x62,0x6f,
  0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,
  0x66,0x63,0x66,0x63,0x66,0x63,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,
  0x6f,0x73,0x65,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x22,0x23,0x65,0x38,0x31,0x31,0x32,0x33,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x2d,0x70,0x72,0x65,0x73,
  0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x66,0x31,0x37,0x30,0x37,0x61,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,0x73,0x2d,0x62,0x75,0x74,0x74,
  0x6f,0x6e,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x62,
  0x38,0x62,0x38,0x62,0x38,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,0x73,
  0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,
  0x3a,0x20,0x22,0x23,0x63,0x61,0x63,0x61,0x63,0x62,0x22,0xa,0x20,0x20,0x7d,0x2c,
  0xa,0xa,0x20,0x20,0x22,0x64,0x65,0x72,0x69,0x76,0x65,0x64,0x22,0x3a,0x20,0x7b,
  0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,
  0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,
  0x22,0x3a,0x20,0x22,0x6c,0x69,0x67,0x68,0x74,0x65,0x6e,0x22,0x2c,0x20,0x22,0x62,
  0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x2c,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,
  0x31,0x35,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,
  0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,
  0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x61,0x63,0x63,0x65,
  0x6e,0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,
  0x6e,0x74,0x22,0x3a,0x20,0x32,0x30,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,0x67,
  0x22,0x3a,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,
  0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,
  0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,0x2c,0x20,0x20,0x20,
  0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x31,0x37,0x20,0x7d,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,
  0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,
  0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,
  0x73,0x65,0x22,0x3a,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,
  0x22,0x2c,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x32,
  0x33,0x20,0x7d,0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x66,0x69,0x78,
  0x65,0x64,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x77,0x68,0x69,0x74,
  0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,
  0x66,0x66,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6c,0x61,0x63,
  0x6b,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x30,0x30,
  0x30,0x30,0x30,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x61,0x72,0x2d,
  0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x37,0x30,
  0x37,0x30,0x37,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,
  0x6f,0x6e,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x62,0x32,
  0x62,0x32,0x62,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,0x61,
  0x72,0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x65,
  0x63,0x30,0x63,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,
  0x74,0x2d,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,
  0x66,0x66,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,
  0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x30,0x30,
  0x30,0x30,0x30,0x30,0x22,0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x63,
  0x6f,0x6d,0x6d,0x65,0x6e,0x74,0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,
  0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x22,0x52,0x69,0x62,0x62,0x6f,0x6e,0x20,0x62,0x61,0x72,0x20,0x62,
  0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x20,0x2b,0x20,0x61,0x70,0x70,0x6c,
  0x69,0x63,0x61,0x74,0x69,0x6f,0x6e,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,
  0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x41,0x70,0x70,0x6c,0x69,0x63,0x61,
  0x74,0x69,0x6f,0x6e,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x68,0x6f,0x76,0x65,
  0x72,0x2c,0x20,0x74,0x61,0x62,0x20,0x68,0x6f,0x76,0x65,0x72,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,
  0x65,0x64,0x22,0x3a,0x20,0x20,0x22,0x54,0x6f,0x6f,0x6c,0x20,0x62,0x75,0x74,0x74,
  0x6f,0x6e,0x20,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x20,0x73,0x74,0x61,0x74,0x65,
  0x2c,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x67,0x72,0x6f,0x75,0x70,0x20,0x70,
  0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,
  0x63,0x65,0x6e,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x22,0x54,0x61,0x62,0x20,0x75,0x6e,0x73,0x65,0x6c,0x65,0x63,0x74,0x65,0x64,0x20,
  0x74,0x65,0x78,0x74,0x2c,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x67,0x72,0x6f,
  0x75,0x70,0x20,0x74,0x65,0x78,0x74,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,
  0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x43,0x61,0x74,0x65,0x67,0x6f,0x72,0x79,0x2f,0x50,0x61,0x6e,0x65,0x6c,
  0x2f,0x54,0x6f,0x6f,0x6c,0x42,0x75,0x74,0x74,0x6f,0x6e,0x2f,0x47,0x61,0x6c,0x6c,
  0x65,0x72,0x79,0x2f,0x4d,0x65,0x6e,0x75,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,
  0x75,0x6e,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,
  0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,0x67,0x22,0x3a,0x22,0x48,0x6f,
  0x76,0x65,0x72,0x20,0x73,0x74,0x61,0x74,0x65,0x20,0x66,0x6f,0x72,0x20,0x62,0x75,
  0x74,0x74,0x6f,0x6e,0x73,0x2c,0x20,0x73,0x63,0x72,0x6f,0x6c,0x6c,0x20,0x62,0x75,
  0x74,0x74,0x6f,0x6e,0x73,0x2c,0x20,0x6d,0x65,0x6e,0x75,0x20,0x69,0x74,0x65,0x6d,
  0x73,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,
  0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x22,0x50,0x72,
  0x65,0x73,0x73,0x65,0x64,0x20,0x73,0x74,0x61,0x74,0x65,0x20,0x66,0x6f,0x72,0x20,
  0x74,0x6f,0x6f,0x6c,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x73,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x33,0x33,0x33,0x33,0x33,0x33,0x20,0x70,
  0x72,0x69,0x6d,0x61,0x72,0x79,0x20,0x74,0x65,0x78,0x74,0x20,0x28,0x74,0x6f,0x6f,
  0x6c,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x73,0x2c,0x20,0x6d,0x65,0x6e,0x75,0x73,
  0x2c,0x20,0x67,0x61,0x6c,0x6c,0x65,0x72,0x79,0x29,0x22,0xa,0x20,0x20,0x7d,0xa,
  0x7d,0xa,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/palettes/office2021-dark.json
  0x0,0x0,0x9,0x3d,
  0x7b,
  0xa,0x20,0x20,0x22,0x6e,0x61,0x6d,0x65,0x22,0x3a,0x20,0x22,0x4f,0x66,0x66,0x69,
  0x63,0x65,0x20,0x32,0x30,0x32,0x31,0x20,0x44,0x61,0x72,0x6b,0x22,0x2c,0xa,0x20,
  0x20,0x22,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x22,0x3a,0x20,0x22,0x31,0x2e,0x30,
  0x22,0x2c,0xa,0x20,0x20,0x22,0x69,0x73,0x44,0x61,0x72,0x6b,0x22,0x3a,0x20,0x74,
  0x72,0x75,0x65,0x2c,0xa,0xa,0x20,0x20,0x22,0x6b,0x65,0x79,0x43,0x6f,0x6c,0x6f,
  0x72,0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,
  0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x31,0x65,0x31,0x65,0x31,0x65,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x30,0x65,0x30,0x65,0x30,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x64,0x32,
  0x64,0x32,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,0x2d,
  0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x65,0x30,0x65,0x30,0x65,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x31,0x65,0x31,0x65,0x31,0x65,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,0x2d,0x74,0x65,0x78,0x74,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x32,0x62,0x32,
  0x62,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,0x61,0x72,0x61,
  0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x35,0x61,0x35,0x61,0x35,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,
  0x75,0x62,0x74,0x69,0x74,0x6c,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x39,0x39,0x39,0x39,0x39,0x39,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x70,0x61,0x6e,0x65,0x6c,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x34,0x61,0x34,0x61,0x34,
  0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6f,0x72,0x64,0x65,0x72,0x2d,
  0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,
  0x34,0x61,0x34,0x61,0x34,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,
  0x70,0x75,0x74,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x22,0x23,0x34,0x61,0x34,0x61,0x34,0x61,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x66,0x6f,0x63,0x75,0x73,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x35,0x61,0x35,0x61,0x35,0x61,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x6c,0x65,0x63,0x74,0x69,0x6f,
  0x6e,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x36,
  0x61,0x39,0x65,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x6d,0x65,0x6e,
  0x75,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x33,0x61,0x33,0x61,0x33,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x38,0x31,0x31,0x32,0x33,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x2d,
  0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x66,0x31,
  0x37,0x30,0x37,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,0x73,0x2d,
  0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,
  0x20,0x22,0x23,0x34,0x61,0x34,0x61,0x34,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x73,0x79,0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x70,0x72,0x65,0x73,
  0x73,0x65,0x64,0x22,0x3a,0x20,0x22,0x23,0x35,0x35,0x35,0x35,0x35,0x35,0x22,0xa,
  0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x64,0x65,0x72,0x69,0x76,0x65,0x64,
  0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,
  0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x7b,
  0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,0x2c,
  0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,
  0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,
  0x74,0x22,0x3a,0x20,0x31,0x30,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,
  0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,
  0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,
  0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x31,0x35,0x20,0x7d,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,
  0x72,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,
  0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,
  0x65,0x22,0x3a,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,
  0x2c,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x31,0x37,
  0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,
  0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x20,0x7b,0x20,
  0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,
  0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,
  0x74,0x2d,0x62,0x67,0x22,0x2c,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,
  0x22,0x3a,0x20,0x32,0x33,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x61,
  0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,
  0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,
  0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x61,
  0x63,0x63,0x65,0x6e,0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x61,
  0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x35,0x20,0x20,0x7d,0xa,0x20,0x20,0x7d,
  0x2c,0xa,0xa,0x20,0x20,0x22,0x66,0x69,0x78,0x65,0x64,0x22,0x3a,0x20,0x7b,0xa,
  0x20,0x20,0x20,0x20,0x22,0x77,0x68,0x69,0x74,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,0x66,0x66,0x66,0x66,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x62,0x6c,0x61,0x63,0x6b,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x30,0x30,0x30,0x30,0x30,0x30,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x31,0x65,0x31,0x65,0x31,0x65,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,0x2d,0x74,0x65,0x78,0x74,
  0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x62,0x32,0x62,0x32,0x62,0x32,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,0x61,0x72,0x61,0x74,0x6f,0x72,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x35,0x61,0x35,0x61,0x35,0x61,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x62,0x61,0x73,0x65,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x33,0x61,0x33,0x61,0x33,0x61,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x30,0x65,0x30,0x65,0x30,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x74,0x61,0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x36,0x61,0x39,0x65,0x66,0x66,0x22,0xa,0x20,
  0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x63,0x6f,0x6d,0x6d,0x65,0x6e,0x74,0x73,
  0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x52,0x69,0x62,
  0x62,0x6f,0x6e,0x20,0x62,0x61,0x72,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,
  0x6e,0x64,0x20,0x28,0x64,0x61,0x72,0x6b,0x65,0x73,0x74,0x29,0x2c,0x20,0x61,0x70,
  0x70,0x6c,0x69,0x63,0x61,0x74,0x69,0x6f,0x6e,0x20,0x77,0x69,0x64,0x67,0x65,0x74,
  0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x22,0x54,0x61,0x62,0x20,0x68,0x6f,0x76,0x65,0x72,0x2c,
  0x20,0x61,0x70,0x70,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x68,0x6f,0x76,0x65,
  0x72,0x20,0x2d,0x20,0x64,0x65,0x72,0x69,0x76,0x65,0x64,0x3a,0x20,0x64,0x61,0x72,
  0x6b,0x65,0x6e,0x20,0x72,0x65,0x76,0x65,0x72,0x73,0x65,0x73,0x20,0x74,0x6f,0x20,
  0x6c,0x69,0x67,0x68,0x74,0x65,0x72,0x20,0x28,0x69,0x73,0x44,0x61,0x72,0x6b,0x3d,
  0x74,0x72,0x75,0x65,0x29,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,
  0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x22,
  0x41,0x70,0x70,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x70,0x72,0x65,0x73,0x73,
  0x65,0x64,0x20,0x2d,0x20,0x64,0x65,0x72,0x69,0x76,0x65,0x64,0x3a,0x20,0x64,0x61,
  0x72,0x6b,0x65,0x6e,0x20,0x72,0x65,0x76,0x65,0x72,0x73,0x65,0x73,0x20,0x74,0x6f,
  0x20,0x65,0x76,0x65,0x6e,0x20,0x6c,0x69,0x67,0x68,0x74,0x65,0x72,0x20,0x28,0x69,
  0x73,0x44,0x61,0x72,0x6b,0x3d,0x74,0x72,0x75,0x65,0x29,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x22,0x54,0x61,0x62,0x20,0x75,0x6e,0x73,0x65,0x6c,0x65,
  0x63,0x74,0x65,0x64,0x20,0x74,0x65,0x78,0x74,0x20,0x28,0x6c,0x69,0x67,0x68,0x74,
  0x20,0x6f,0x6e,0x20,0x64,0x61,0x72,0x6b,0x20,0x62,0x67,0x29,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x43,0x61,0x74,0x65,0x67,0x6f,0x72,0x79,0x2f,
  0x50,0x61,0x6e,0x65,0x6c,0x2f,0x54,0x6f,0x6f,0x6c,0x42,0x75,0x74,0x74,0x6f,0x6e,
  0x2f,0x47,0x61,0x6c,0x6c,0x65,0x72,0x79,0x2f,0x4d,0x65,0x6e,0x75,0x20,0x62,0x61,
  0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,0x67,
  0x22,0x3a,0x22,0x48,0x6f,0x76,0x65,0x72,0x20,0x73,0x74,0x61,0x74,0x65,0x20,0x2d,
  0x20,0x64,0x65,0x72,0x69,0x76,0x65,0x64,0x3a,0x20,0x64,0x61,0x72,0x6b,0x65,0x6e,
  0x20,0x72,0x65,0x76,0x65,0x72,0x73,0x65,0x73,0x20,0x74,0x6f,0x20,0x6c,0x69,0x67,
  0x68,0x74,0x65,0x72,0x20,0x28,0x69,0x73,0x44,0x61,0x72,0x6b,0x3d,0x74,0x72,0x75,
  0x65,0x29,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,
  0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x22,0x50,
  0x72,0x65,0x73,0x73,0x65,0x64,0x2f,0x63,0x68,0x65,0x63,0x6b,0x65,0x64,0x20,0x73,
  0x74,0x61,0x74,0x65,0x20,0x2d,0x20,0x64,0x65,0x72,0x69,0x76,0x65,0x64,0x3a,0x20,
  0x64,0x61,0x72,0x6b,0x65,0x6e,0x20,0x72,0x65,0x76,0x65,0x72,0x73,0x65,0x73,0x20,
  0x74,0x6f,0x20,0x65,0x76,0x65,0x6e,0x20,0x6c,0x69,0x67,0x68,0x74,0x65,0x72,0x20,
  0x28,0x69,0x73,0x44,0x61,0x72,0x6b,0x3d,0x74,0x72,0x75,0x65,0x29,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x50,0x72,0x69,0x6d,0x61,0x72,0x79,0x20,
  0x74,0x65,0x78,0x74,0x20,0x63,0x6f,0x6c,0x6f,0x72,0x20,0x28,0x6c,0x69,0x67,0x68,
  0x74,0x20,0x6f,0x6e,0x20,0x64,0x61,0x72,0x6b,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,
  0x6f,0x75,0x6e,0x64,0x73,0x29,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x61,
  0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x54,0x61,0x62,0x20,0x73,0x65,0x6c,0x65,0x63,0x74,0x65,0x64,0x20,0x74,0x65,
  0x78,0x74,0x20,0x63,0x6f,0x6c,0x6f,0x72,0x20,0x61,0x6e,0x64,0x20,0x75,0x6e,0x64,
  0x65,0x72,0x6c,0x69,0x6e,0x65,0x20,0x28,0x62,0x6c,0x75,0x65,0x20,0x68,0x69,0x67,
  0x68,0x6c,0x69,0x67,0x68,0x74,0x20,0x6f,0x6e,0x20,0x64,0x61,0x72,0x6b,0x20,0x62,
  0x67,0x29,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x61,0x62,0x2d,0x61,0x63,
  0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x22,0x54,0x61,0x62,
  0x20,0x68,0x6f,0x76,0x65,0x72,0x20,0x75,0x6e,0x64,0x65,0x72,0x6c,0x69,0x6e,0x65,
  0x2c,0x20,0x64,0x65,0x72,0x69,0x76,0x65,0x64,0x20,0x66,0x72,0x6f,0x6d,0x20,0x61,
  0x63,0x63,0x65,0x6e,0x74,0x22,0xa,0x20,0x20,0x7d,0xa,0x7d,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/palettes/office2016-green.json
  0x0,0x0,0x5,0x2e,
  0x7b,
  0xa,0x20,0x20,0x22,0x6e,0x61,0x6d,0x65,0x22,0x3a,0x20,0x22,0x4f,0x66,0x66,0x69,
  0x63,0x65,0x20,0x32,0x30,0x31,0x36,0x20,0x47,0x72,0x65,0x65,0x6e,0x22,0x2c,0xa,
  0x20,0x20,0x22,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x22,0x3a,0x20,0x22,0x31,0x2e,
  0x30,0x22,0x2c,0xa,0x20,0x20,0x22,0x69,0x73,0x44,0x61,0x72,0x6b,0x22,0x3a,0x20,
  0x66,0x61,0x6c,0x73,0x65,0x2c,0xa,0xa,0x20,0x20,0x22,0x6b,0x65,0x79,0x43,0x6f,
  0x6c,0x6f,0x72,0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,
  0x63,0x65,0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x22,0x23,0x32,0x64,0x37,0x64,0x34,0x36,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x64,0x32,0x66,0x66,0x64,0x38,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,
  0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,
  0x31,0x66,0x31,0x66,0x31,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,
  0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x33,0x33,0x33,0x33,0x33,0x33,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x37,0x30,0x37,0x30,0x37,0x30,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,0x2d,0x74,0x65,
  0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x32,
  0x62,0x32,0x62,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,0x61,
  0x72,0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x62,0x65,0x63,0x30,0x63,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x73,0x75,0x62,0x74,0x69,0x74,0x6c,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x36,0x36,0x36,0x36,0x36,0x36,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x70,0x61,0x6e,0x65,0x6c,0x2d,0x62,0x6f,0x72,0x64,
  0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x63,0x36,0x63,
  0x36,0x63,0x36,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,
  0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x63,0x32,0x64,0x30,0x64,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x69,0x6e,0x70,0x75,0x74,0x2d,0x66,0x6f,0x63,0x75,0x73,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x36,0x39,0x62,0x66,0x34,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x6c,0x65,0x63,0x74,0x69,0x6f,0x6e,0x2d,0x62,
  0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x39,0x62,0x62,0x62,
  0x66,0x37,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x6d,0x65,0x6e,0x75,0x2d,0x62,
  0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x66,0x63,0x66,0x63,0x66,0x63,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,
  0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x38,0x31,0x31,0x32,0x33,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x2d,0x70,0x72,0x65,
  0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x66,0x31,0x37,0x30,0x37,
  0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,0x73,0x2d,0x62,0x75,0x74,
  0x74,0x6f,0x6e,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,
  0x62,0x38,0x62,0x38,0x62,0x38,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,
  0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,
  0x22,0x3a,0x20,0x22,0x23,0x63,0x61,0x63,0x61,0x63,0x62,0x22,0xa,0x20,0x20,0x7d,
  0x2c,0xa,0xa,0x20,0x20,0x22,0x64,0x65,0x72,0x69,0x76,0x65,0x64,0x22,0x3a,0x20,
  0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,
  0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,
  0x6e,0x22,0x3a,0x20,0x22,0x6c,0x69,0x67,0x68,0x74,0x65,0x6e,0x22,0x2c,0x20,0x22,
  0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x2c,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,
  0x20,0x31,0x35,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,
  0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,
  0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x61,0x63,0x63,
  0x65,0x6e,0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,
  0x75,0x6e,0x74,0x22,0x3a,0x20,0x32,0x30,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,
  0x67,0x22,0x3a,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,
  0x61,0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,
  0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,0x2c,0x20,0x20,
  0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x31,0x37,0x20,0x7d,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x70,0x72,
  0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x20,0x7b,0x20,0x22,0x66,0x6e,
  0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,
  0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,
  0x67,0x22,0x2c,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,
  0x32,0x33,0x20,0x7d,0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x66,0x69,
  0x78,0x65,0x64,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x77,0x68,0x69,
  0x74,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,
  0x66,0x66,0x66,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6c,0x61,
  0x63,0x6b,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x30,
  0x30,0x30,0x30,0x30,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x61,0x72,
  0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x37,
  0x30,0x37,0x30,0x37,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,
  0x62,0x6f,0x6e,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x62,
  0x32,0x62,0x32,0x62,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,
  0x61,0x72,0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,
  0x65,0x63,0x30,0x63,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,
  0x75,0x74,0x2d,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x66,
  0x66,0x66,0x66,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,
  0x75,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x30,
  0x30,0x30,0x30,0x30,0x30,0x22,0xa,0x20,0x20,0x7d,0xa,0x7d,0xa,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/palettes/office2021-green.json
  0x0,0x0,0x8,0x81,
  0x7b,
  0xa,0x20,0x20,0x22,0x6e,0x61,0x6d,0x65,0x22,0x3a,0x20,0x22,0x4f,0x66,0x66,0x69,
  0x63,0x65,0x20,0x32,0x30,0x32,0x31,0x20,0x47,0x72,0x65,0x65,0x6e,0x22,0x2c,0xa,
  0x20,0x20,0x22,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x22,0x3a,0x20,0x22,0x31,0x2e,
  0x30,0x22,0x2c,0xa,0x20,0x20,0x22,0x69,0x73,0x44,0x61,0x72,0x6b,0x22,0x3a,0x20,
  0x66,0x61,0x6c,0x73,0x65,0x2c,0xa,0xa,0x20,0x20,0x22,0x6b,0x65,0x79,0x43,0x6f,
  0x6c,0x6f,0x72,0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,
  0x63,0x65,0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x22,0x23,0x64,0x63,0x65,0x38,0x64,0x36,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x34,0x32,0x34,0x32,0x34,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,
  0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,
  0x66,0x66,0x66,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,
  0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x32,0x34,0x32,0x34,0x32,0x34,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x64,0x63,0x65,0x38,0x64,0x36,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,0x2d,0x74,0x65,
  0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x32,
  0x62,0x32,0x62,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,0x61,
  0x72,0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x62,0x65,0x63,0x30,0x63,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x73,0x75,0x62,0x74,0x69,0x74,0x6c,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x36,0x36,0x36,0x36,0x36,0x36,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x70,0x61,0x6e,0x65,0x6c,0x2d,0x62,0x6f,0x72,0x64,
  0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x36,0x30,0x36,
  0x30,0x36,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6f,0x72,0x64,0x65,
  0x72,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x63,0x35,0x63,0x35,0x63,0x35,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x69,0x6e,0x70,0x75,0x74,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x63,0x30,0x63,0x32,0x63,0x34,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x66,0x6f,0x63,0x75,0x73,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x33,0x64,0x39,0x31,
  0x34,0x31,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x6c,0x65,0x63,0x74,
  0x69,0x6f,0x6e,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x61,0x38,0x64,0x62,0x61,0x38,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x6d,
  0x65,0x6e,0x75,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x63,0x66,0x63,0x66,0x63,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x38,0x31,0x31,0x32,
  0x33,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,
  0x67,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,
  0x66,0x31,0x37,0x30,0x37,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,
  0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,
  0x20,0x20,0x20,0x22,0x23,0x66,0x35,0x66,0x36,0x66,0x36,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x73,0x79,0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x70,0x72,
  0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x22,0x23,0x63,0x61,0x63,0x61,0x63,0x62,
  0x22,0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x64,0x65,0x72,0x69,0x76,
  0x65,0x64,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,
  0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,
  0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x61,0x63,0x63,
  0x65,0x6e,0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,
  0x75,0x6e,0x74,0x22,0x3a,0x20,0x31,0x30,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,
  0x61,0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,
  0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x31,0x35,0x20,0x7d,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x68,0x6f,
  0x76,0x65,0x72,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,
  0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,
  0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,
  0x67,0x22,0x2c,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,
  0x31,0x37,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,
  0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x20,
  0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,0x72,0x6b,0x65,0x6e,0x22,
  0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,0x63,0x6f,0x6e,0x74,
  0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,0x2c,0x20,0x20,0x20,0x22,0x61,0x6d,0x6f,0x75,
  0x6e,0x74,0x22,0x3a,0x20,0x32,0x33,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x74,0x61,0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,
  0x22,0x3a,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x64,0x61,
  0x72,0x6b,0x65,0x6e,0x22,0x2c,0x20,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,
  0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x2c,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,0x22,0x3a,0x20,0x35,0x20,0x20,0x7d,0xa,0x20,
  0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x66,0x69,0x78,0x65,0x64,0x22,0x3a,0x20,
  0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x77,0x68,0x69,0x74,0x65,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,0x66,0x66,0x66,0x66,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6c,0x61,0x63,0x6b,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x30,0x30,0x30,0x30,0x30,0x30,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,
  0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x64,0x63,0x65,0x38,0x64,0x36,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,0x2d,0x74,0x65,
  0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x62,0x32,0x62,0x32,0x62,0x32,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,0x61,0x72,0x61,0x74,0x6f,0x72,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x65,0x63,0x30,0x63,0x32,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x62,0x61,0x73,
  0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,0x66,0x66,0x66,0x66,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x74,0x65,0x78,
  0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x30,0x30,0x30,0x30,0x30,0x30,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x61,0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,
  0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x33,0x64,0x39,0x31,0x34,0x31,0x22,
  0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x63,0x6f,0x6d,0x6d,0x65,0x6e,
  0x74,0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,
  0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x52,
  0x69,0x62,0x62,0x6f,0x6e,0x20,0x62,0x61,0x72,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,
  0x6f,0x75,0x6e,0x64,0x20,0x2b,0x20,0x61,0x70,0x70,0x6c,0x69,0x63,0x61,0x74,0x69,
  0x6f,0x6e,0x20,0x77,0x69,0x64,0x67,0x65,0x74,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,
  0x6f,0x75,0x6e,0x64,0x20,0x28,0x67,0x72,0x65,0x65,0x6e,0x20,0x74,0x69,0x6e,0x74,
  0x29,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,
  0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x54,0x61,0x62,0x20,
  0x68,0x6f,0x76,0x65,0x72,0x2c,0x20,0x61,0x70,0x70,0x6c,0x69,0x63,0x61,0x74,0x69,
  0x6f,0x6e,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x68,0x6f,0x76,0x65,0x72,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,
  0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x22,0x41,0x70,0x70,0x6c,0x69,0x63,
  0x61,0x74,0x69,0x6f,0x6e,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x20,0x70,0x72,0x65,
  0x73,0x73,0x65,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,
  0x6e,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x22,0x54,
  0x61,0x62,0x20,0x75,0x6e,0x73,0x65,0x6c,0x65,0x63,0x74,0x65,0x64,0x20,0x74,0x65,
  0x78,0x74,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,
  0x74,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x43,0x61,0x74,
  0x65,0x67,0x6f,0x72,0x79,0x2f,0x50,0x61,0x6e,0x65,0x6c,0x2f,0x54,0x6f,0x6f,0x6c,
  0x42,0x75,0x74,0x74,0x6f,0x6e,0x2f,0x47,0x61,0x6c,0x6c,0x65,0x72,0x79,0x2f,0x4d,
  0x65,0x6e,0x75,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x68,0x6f,
  0x76,0x65,0x72,0x2d,0x62,0x67,0x22,0x3a,0x22,0x48,0x6f,0x76,0x65,0x72,0x20,0x73,
  0x74,0x61,0x74,0x65,0x20,0x66,0x6f,0x72,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x73,
  0x2c,0x20,0x73,0x63,0x72,0x6f,0x6c,0x6c,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x73,
  0x2c,0x20,0x6d,0x65,0x6e,0x75,0x20,0x69,0x74,0x65,0x6d,0x73,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,
  0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x22,0x50,0x72,0x65,0x73,0x73,0x65,0x64,
  0x2f,0x63,0x68,0x65,0x63,0x6b,0x65,0x64,0x20,0x73,0x74,0x61,0x74,0x65,0x20,0x66,
  0x6f,0x72,0x20,0x74,0x6f,0x6f,0x6c,0x20,0x62,0x75,0x74,0x74,0x6f,0x6e,0x73,0x2c,
  0x20,0x6d,0x65,0x6e,0x75,0x20,0x69,0x74,0x65,0x6d,0x73,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x74,0x65,0x78,0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x50,0x72,0x69,0x6d,0x61,0x72,0x79,0x20,0x74,0x65,
  0x78,0x74,0x20,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x74,0x61,0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x54,0x61,0x62,0x20,0x73,0x65,0x6c,0x65,0x63,0x74,0x65,0x64,0x20,
  0x74,0x65,0x78,0x74,0x20,0x63,0x6f,0x6c,0x6f,0x72,0x20,0x61,0x6e,0x64,0x20,0x75,
  0x6e,0x64,0x65,0x72,0x6c,0x69,0x6e,0x65,0x20,0x28,0x67,0x72,0x65,0x65,0x6e,0x20,
  0x74,0x68,0x65,0x6d,0x65,0x29,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x61,
  0x62,0x2d,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,
  0x22,0x54,0x61,0x62,0x20,0x68,0x6f,0x76,0x65,0x72,0x20,0x75,0x6e,0x64,0x65,0x72,
  0x6c,0x69,0x6e,0x65,0x2c,0x20,0x64,0x65,0x72,0x69,0x76,0x65,0x64,0x20,0x66,0x72,
  0x6f,0x6d,0x20,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0xa,0x20,0x20,0x7d,0xa,0x7d,

	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/palettes/office2013-default.json
  0x0,0x0,0x5,0x23,
  0x7b,
  0xa,0x20,0x20,0x22,0x6e,0x61,0x6d,0x65,0x22,0x3a,0x20,0x22,0x4f,0x66,0x66,0x69,
  0x63,0x65,0x20,0x32,0x30,0x31,0x33,0x22,0x2c,0xa,0x20,0x20,0x22,0x76,0x65,0x72,
  0x73,0x69,0x6f,0x6e,0x22,0x3a,0x20,0x22,0x31,0x2e,0x30,0x22,0x2c,0xa,0x20,0x20,
  0x22,0x69,0x73,0x44,0x61,0x72,0x6b,0x22,0x3a,0x20,0x66,0x61,0x6c,0x73,0x65,0x2c,
  0xa,0xa,0x20,0x20,0x22,0x6b,0x65,0x79,0x43,0x6f,0x6c,0x6f,0x72,0x73,0x22,0x3a,
  0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,
  0x62,0x35,0x37,0x39,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,
  0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x66,0x66,0x66,0x66,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x74,0x65,0x78,0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x33,0x33,0x33,0x33,0x33,0x33,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x75,0x62,0x74,0x69,0x74,0x6c,0x65,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x36,0x36,
  0x36,0x36,0x36,0x36,0x22,0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x64,
  0x65,0x72,0x69,0x76,0x65,0x64,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,
  0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x6c,0x69,
  0x67,0x68,0x74,0x65,0x6e,0x22,0x2c,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,
  0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x2c,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,
  0x74,0x22,0x3a,0x20,0x32,0x30,0x20,0x7d,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,
  0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x7b,0x20,0x22,0x66,0x6e,0x22,0x3a,0x20,0x22,0x6c,0x69,0x67,
  0x68,0x74,0x65,0x6e,0x22,0x2c,0x20,0x22,0x62,0x61,0x73,0x65,0x22,0x3a,0x20,0x22,
  0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x2c,0x20,0x22,0x61,0x6d,0x6f,0x75,0x6e,0x74,
  0x22,0x3a,0x20,0x31,0x32,0x20,0x7d,0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,
  0x22,0x66,0x69,0x78,0x65,0x64,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,
  0x77,0x68,0x69,0x74,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,0x66,0x66,
  0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6c,0x61,0x63,0x6b,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x30,0x30,0x30,0x30,0x30,0x30,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,
  0x37,0x30,0x37,0x30,0x37,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x61,
  0x62,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x35,0x37,0x37,0x39,0x61,0x66,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6f,0x72,0x64,0x65,0x72,0x2d,0x63,
  0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x63,0x35,0x64,0x32,0x65,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x73,0x65,0x70,0x61,0x72,0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x65,
  0x63,0x30,0x63,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,
  0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x63,0x65,0x65,0x37,0x66,0x63,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x70,0x72,
  0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x39,0x65,0x64,0x32,0x66,0x39,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x63,0x68,0x65,0x63,0x6b,0x65,0x64,0x2d,
  0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x63,0x65,0x65,0x38,
  0x66,0x63,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,
  0x74,0x2d,0x63,0x68,0x65,0x63,0x6b,0x65,0x64,0x2d,0x68,0x6f,0x76,0x65,0x72,0x2d,
  0x62,0x67,0x22,0x3a,0x22,0x23,0x62,0x38,0x64,0x36,0x66,0x37,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,
  0x62,0x61,0x64,0x66,0x66,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x66,0x6f,
  0x63,0x75,0x73,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x39,0x64,0x65,0x66,0x61,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x66,0x6f,0x63,0x75,0x73,0x2d,0x68,0x69,
  0x67,0x68,0x6c,0x69,0x67,0x68,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x32,0x36,0x39,0x62,0x66,0x34,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x63,0x30,
  0x63,0x32,0x63,0x34,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x6c,0x65,
  0x63,0x74,0x69,0x6f,0x6e,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x39,0x62,0x62,0x62,0x66,0x37,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x6d,0x65,0x6e,0x75,0x2d,0x62,0x67,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x66,0x63,0x66,0x63,0x66,0x63,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x6d,0x65,0x6e,0x75,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x63,0x32,0x64,0x30,
  0x64,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,0x73,0x2d,0x62,0x75,
  0x74,0x74,0x6f,0x6e,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x35,0x65,0x35,0x65,0x35,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x73,0x79,0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x70,
  0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,
  0x63,0x61,0x63,0x61,0x63,0x62,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,
  0x6f,0x73,0x65,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x38,0x31,0x31,0x32,0x33,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,
  0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x66,0x31,0x37,0x30,0x37,0x61,0x22,0xa,0x20,0x20,0x7d,0xa,
  0x7d,0xa,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/palettes/dark2-default.json
  0x0,0x0,0x4,0xe,
  0x7b,
  0xd,0xa,0x20,0x20,0x22,0x6e,0x61,0x6d,0x65,0x22,0x3a,0x20,0x22,0x44,0x61,0x72,
  0x6b,0x32,0x20,0x44,0x65,0x66,0x61,0x75,0x6c,0x74,0x22,0x2c,0xd,0xa,0x20,0x20,
  0x22,0x69,0x73,0x44,0x61,0x72,0x6b,0x22,0x3a,0x20,0x74,0x72,0x75,0x65,0x2c,0xd,
  0xa,0xd,0xa,0x20,0x20,0x22,0x6b,0x65,0x79,0x43,0x6f,0x6c,0x6f,0x72,0x73,0x22,
  0x3a,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x31,0x39,0x32,0x34,0x33,0x30,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,
  0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x34,0x33,0x35,0x63,0x37,0x36,0x22,0x2c,0xd,
  0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,
  0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x34,0x33,0x35,
  0x63,0x37,0x36,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,
  0x6e,0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x61,0x66,0x61,0x66,0x61,0x36,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,
  0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x33,0x34,0x34,0x32,0x35,0x33,0x22,
  0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,
  0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x34,
  0x64,0x35,0x39,0x37,0x33,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,
  0x6e,0x74,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,
  0x22,0x3a,0x20,0x22,0x23,0x34,0x33,0x35,0x63,0x37,0x36,0x22,0x2c,0xd,0xa,0x20,
  0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x64,0x61,0x64,0x61,0x64,
  0x61,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,0x2d,0x64,
  0x69,0x73,0x61,0x62,0x6c,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x35,0x64,0x36,0x33,0x36,0x61,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,
  0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x31,0x39,0x32,0x34,0x33,0x30,0x22,0x2c,0xd,
  0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,0x2d,0x74,0x65,0x78,
  0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x64,0x61,0x64,
  0x61,0x64,0x61,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x70,0x61,
  0x72,0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x35,0x64,0x36,0x33,0x36,0x61,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,
  0x20,0x22,0x73,0x75,0x62,0x74,0x69,0x74,0x6c,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x64,0x61,0x64,0x61,0x64,0x61,0x22,
  0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x70,0x61,0x6e,0x65,0x6c,0x2d,0x62,0x6f,
  0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,
  0x61,0x38,0x64,0x62,0x35,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6f,
  0x72,0x64,0x65,0x72,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x22,0x23,0x32,0x61,0x38,0x64,0x62,0x35,0x22,0x2c,0xd,0xa,0x20,
  0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,
  0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x31,0x39,0x32,0x34,0x33,
  0x30,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,
  0x66,0x6f,0x63,0x75,0x73,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,
  0x23,0x32,0x61,0x38,0x64,0x62,0x35,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,
  0x73,0x65,0x6c,0x65,0x63,0x74,0x69,0x6f,0x6e,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x61,0x38,0x64,0x62,0x35,0x22,0x2c,0xd,
  0xa,0x20,0x20,0x20,0x20,0x22,0x6d,0x65,0x6e,0x75,0x2d,0x62,0x6f,0x72,0x64,0x65,
  0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x38,0x38,0x38,
  0x38,0x38,0x38,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,
  0x65,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x65,0x38,0x31,0x31,0x32,0x33,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,
  0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x2d,0x70,0x72,0x65,0x73,0x73,
  0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x66,0x31,0x37,0x30,0x37,0x61,0x22,
  0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,0x73,0x2d,0x62,0x75,0x74,0x74,
  0x6f,0x6e,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x33,
  0x37,0x35,0x64,0x37,0x37,0x22,0x2c,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,
  0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,
  0x22,0x3a,0x20,0x22,0x23,0x63,0x61,0x63,0x61,0x63,0x62,0x22,0xd,0xa,0x20,0x20,
  0x7d,0x2c,0xd,0xa,0xd,0xa,0x20,0x20,0x22,0x64,0x65,0x72,0x69,0x76,0x65,0x64,
  0x22,0x3a,0x20,0x7b,0x7d,0x2c,0xd,0xa,0x20,0x20,0x22,0x66,0x69,0x78,0x65,0x64,
  0x22,0x3a,0x20,0x7b,0xd,0xa,0x20,0x20,0x20,0x20,0x22,0x77,0x68,0x69,0x74,0x65,
  0x22,0x3a,0x20,0x22,0x23,0x66,0x66,0x66,0x66,0x66,0x66,0x22,0x2c,0xd,0xa,0x20,
  0x20,0x20,0x20,0x22,0x62,0x6c,0x61,0x63,0x6b,0x22,0x3a,0x20,0x22,0x23,0x30,0x30,
  0x30,0x30,0x30,0x30,0x22,0xd,0xa,0x20,0x20,0x7d,0xd,0xa,0x7d,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/palettes/office2016-dark.json
  0x0,0x0,0x5,0xbd,
  0x7b,
  0xa,0x20,0x20,0x22,0x6e,0x61,0x6d,0x65,0x22,0x3a,0x20,0x22,0x4f,0x66,0x66,0x69,
  0x63,0x65,0x20,0x32,0x30,0x31,0x36,0x20,0x44,0x61,0x72,0x6b,0x22,0x2c,0xa,0x20,
  0x20,0x22,0x76,0x65,0x72,0x73,0x69,0x6f,0x6e,0x22,0x3a,0x20,0x22,0x31,0x2e,0x30,
  0x22,0x2c,0xa,0x20,0x20,0x22,0x69,0x73,0x44,0x61,0x72,0x6b,0x22,0x3a,0x20,0x74,
  0x72,0x75,0x65,0x2c,0xa,0xa,0x20,0x20,0x22,0x6b,0x65,0x79,0x43,0x6f,0x6c,0x6f,
  0x72,0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,
  0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x31,0x65,0x31,0x65,0x31,0x65,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x34,0x61,0x34,0x61,0x34,0x61,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,
  0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x35,0x35,0x35,
  0x35,0x35,0x35,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,
  0x74,0x2d,0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x65,0x30,0x65,0x30,0x65,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x64,0x32,0x64,0x32,0x64,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,
  0x65,0x72,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x33,0x61,0x33,0x61,
  0x33,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,
  0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x2d,0x62,0x67,0x22,0x3a,0x20,0x22,
  0x23,0x35,0x30,0x35,0x30,0x35,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,
  0x65,0x78,0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x30,0x65,0x30,0x65,0x30,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x62,0x61,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x31,0x65,0x31,0x65,0x31,
  0x65,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x72,0x69,0x62,0x62,0x6f,0x6e,0x2d,
  0x74,0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,
  0x62,0x32,0x62,0x32,0x62,0x32,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,
  0x70,0x61,0x72,0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x22,0x23,0x35,0x61,0x35,0x61,0x35,0x61,0x22,0x2c,0xa,0x20,0x20,
  0x20,0x20,0x22,0x73,0x75,0x62,0x74,0x69,0x74,0x6c,0x65,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x39,0x39,0x39,0x39,0x39,0x39,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x70,0x61,0x6e,0x65,0x6c,0x2d,0x62,0x6f,
  0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x33,
  0x61,0x33,0x61,0x33,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,
  0x75,0x74,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x33,0x61,0x33,0x61,0x33,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x66,0x6f,0x63,0x75,0x73,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x36,0x39,0x62,0x66,0x34,0x22,
  0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x6c,0x65,0x63,0x74,0x69,0x6f,0x6e,
  0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x36,
  0x34,0x66,0x37,0x38,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x6d,0x65,0x6e,0x75,
  0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x22,0x23,0x33,0x61,0x33,0x61,0x33,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,
  0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x38,0x31,0x31,0x32,0x33,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x2d,0x70,
  0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x22,0x23,0x66,0x31,0x37,
  0x30,0x37,0x61,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,0x73,0x2d,0x62,
  0x75,0x74,0x74,0x6f,0x6e,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,
  0x22,0x23,0x34,0x30,0x34,0x30,0x34,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x73,0x79,0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x70,0x72,0x65,0x73,0x73,
  0x65,0x64,0x22,0x3a,0x20,0x22,0x23,0x35,0x30,0x35,0x30,0x35,0x30,0x22,0xa,0x20,
  0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x64,0x65,0x72,0x69,0x76,0x65,0x64,0x22,
  0x3a,0x20,0x7b,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x66,0x69,0x78,0x65,0x64,0x22,
  0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x77,0x68,0x69,0x74,0x65,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,0x66,0x66,0x66,
  0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6c,0x61,0x63,0x6b,0x22,0x3a,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x30,0x30,0x30,0x30,0x30,
  0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x62,
  0x61,0x73,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x32,0x64,0x32,0x64,0x32,
  0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x69,0x6e,0x70,0x75,0x74,0x2d,0x74,
  0x65,0x78,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x30,0x65,0x30,0x65,
  0x30,0x22,0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x63,0x6f,0x6d,0x6d,
  0x65,0x6e,0x74,0x73,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,
  0x63,0x65,0x6e,0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x52,0x69,0x62,0x62,0x6f,0x6e,0x20,0x62,0x61,0x72,0x20,0x62,0x61,0x63,0x6b,
  0x67,0x72,0x6f,0x75,0x6e,0x64,0x20,0x28,0x64,0x61,0x72,0x6b,0x65,0x73,0x74,0x29,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x68,
  0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x42,0x75,0x74,0x74,0x6f,
  0x6e,0x20,0x68,0x6f,0x76,0x65,0x72,0x20,0x73,0x74,0x61,0x74,0x65,0x73,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,
  0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x22,0x42,0x75,0x74,0x74,0x6f,0x6e,0x20,
  0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x20,0x73,0x74,0x61,0x74,0x65,0x73,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x2d,0x74,0x65,0x78,
  0x74,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x22,0x54,0x65,0x78,0x74,0x20,0x6f,0x6e,
  0x20,0x64,0x61,0x72,0x6b,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,0x6f,0x75,0x6e,0x64,
  0x73,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,
  0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x43,0x61,0x74,0x65,
  0x67,0x6f,0x72,0x79,0x2f,0x50,0x61,0x6e,0x65,0x6c,0x20,0x62,0x61,0x63,0x6b,0x67,
  0x72,0x6f,0x75,0x6e,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,
  0x74,0x65,0x6e,0x74,0x2d,0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,0x67,0x22,0x3a,0x22,
  0x53,0x6c,0x69,0x67,0x68,0x74,0x6c,0x79,0x20,0x6c,0x69,0x67,0x68,0x74,0x65,0x72,
  0x20,0x66,0x6f,0x72,0x20,0x68,0x6f,0x76,0x65,0x72,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,0x6e,0x74,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,
  0x64,0x2d,0x62,0x67,0x22,0x3a,0x22,0x50,0x72,0x65,0x73,0x73,0x65,0x64,0x20,0x73,
  0x74,0x61,0x74,0x65,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x65,0x78,0x74,
  0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,
  0x65,0x30,0x65,0x30,0x65,0x30,0x20,0x6c,0x69,0x67,0x68,0x74,0x20,0x74,0x65,0x78,
  0x74,0x20,0x6f,0x6e,0x20,0x64,0x61,0x72,0x6b,0x20,0x62,0x61,0x63,0x6b,0x67,0x72,
  0x6f,0x75,0x6e,0x64,0x22,0xa,0x20,0x20,0x7d,0xa,0x7d,0xa,
	// C:/src/Qt/SARibbon/src/SARibbonBar/resource/palettes/win7-default.json
  0x0,0x0,0x6,0x36,
  0x7b,
  0xa,0x20,0x20,0x22,0x6e,0x61,0x6d,0x65,0x22,0x3a,0x20,0x22,0x57,0x69,0x6e,0x64,
  0x6f,0x77,0x73,0x20,0x37,0x22,0x2c,0xa,0x20,0x20,0x22,0x76,0x65,0x72,0x73,0x69,
  0x6f,0x6e,0x22,0x3a,0x20,0x22,0x31,0x2e,0x30,0x22,0x2c,0xa,0x20,0x20,0x22,0x69,
  0x73,0x44,0x61,0x72,0x6b,0x22,0x3a,0x20,0x66,0x61,0x6c,0x73,0x65,0x2c,0xa,0xa,
  0x20,0x20,0x22,0x6b,0x65,0x79,0x43,0x6f,0x6c,0x6f,0x72,0x73,0x22,0x3a,0x20,0x7b,
  0xa,0x20,0x20,0x20,0x20,0x22,0x61,0x63,0x63,0x65,0x6e,0x74,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x33,0x65,
  0x36,0x65,0x38,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6f,0x6e,0x74,0x65,
  0x6e,0x74,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x66,0x66,0x66,0x66,0x66,0x66,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x74,0x65,0x78,0x74,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x34,0x34,0x34,0x34,0x34,0x34,0x22,0x2c,0xa,
  0x20,0x20,0x20,0x20,0x22,0x73,0x75,0x62,0x74,0x69,0x74,0x6c,0x65,0x22,0x3a,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x36,0x36,0x36,0x36,
  0x36,0x36,0x22,0xa,0x20,0x20,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x64,0x65,0x72,
  0x69,0x76,0x65,0x64,0x22,0x3a,0x20,0x7b,0x7d,0x2c,0xa,0xa,0x20,0x20,0x22,0x66,
  0x69,0x78,0x65,0x64,0x22,0x3a,0x20,0x7b,0xa,0x20,0x20,0x20,0x20,0x22,0x77,0x68,
  0x69,0x74,0x65,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,0x66,0x66,0x66,0x66,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x62,0x6c,0x61,0x63,0x6b,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x30,0x30,0x30,0x30,
  0x30,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x68,0x6f,0x76,0x65,0x72,0x2d,
  0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x66,0x64,0x65,0x65,0x62,0x33,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x68,0x6f,0x76,0x65,0x72,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x66,0x64,0x37,0x30,0x30,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x74,0x61,0x62,0x2d,0x68,0x6f,0x76,0x65,0x72,0x2d,
  0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x63,
  0x62,0x63,0x33,0x64,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x62,0x6f,0x72,0x64,
  0x65,0x72,0x2d,0x63,0x6f,0x6c,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x63,0x30,0x63,0x32,0x63,0x34,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x73,0x65,0x70,0x61,0x72,0x61,0x74,0x6f,0x72,0x22,0x3a,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x62,0x65,0x63,0x30,0x63,0x32,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x66,0x6f,0x63,0x75,0x73,0x2d,0x62,0x6f,
  0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,
  0x66,0x63,0x62,0x66,0x32,0x31,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x66,0x6f,
  0x63,0x75,0x73,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x63,0x64,0x33,0x36,0x34,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x74,0x61,0x62,0x2d,0x73,0x65,0x6c,0x65,0x63,0x74,0x65,0x64,
  0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,0x3a,0x20,0x22,0x23,0x62,0x61,0x63,0x39,
  0x64,0x62,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x65,0x6c,0x65,0x63,0x74,
  0x69,0x6f,0x6e,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x22,0x23,0x39,0x62,0x62,0x62,0x66,0x37,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,
  0x6d,0x65,0x6e,0x75,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x66,0x63,0x66,0x63,0x66,0x63,0x22,0x2c,
  0xa,0x20,0x20,0x20,0x20,0x22,0x6d,0x65,0x6e,0x75,0x2d,0x62,0x6f,0x72,0x64,0x65,
  0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x38,0x34,
  0x39,0x32,0x61,0x36,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x6d,0x65,0x6e,0x75,
  0x2d,0x62,0x74,0x6e,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x20,0x20,0x22,0x23,0x66,0x66,0x65,0x35,0x38,0x30,0x22,0x2c,0xa,0x20,0x20,0x20,
  0x20,0x22,0x73,0x63,0x72,0x6f,0x6c,0x6c,0x2d,0x62,0x6f,0x72,0x64,0x65,0x72,0x22,
  0x3a,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x63,0x35,0x64,0x32,0x65,0x30,
  0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,0x73,0x2d,0x62,0x75,0x74,0x74,
  0x6f,0x6e,0x2d,0x68,0x6f,0x76,0x65,0x72,0x22,0x3a,0x20,0x20,0x20,0x20,0x22,0x23,
  0x66,0x35,0x66,0x36,0x66,0x36,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x73,0x79,
  0x73,0x2d,0x62,0x75,0x74,0x74,0x6f,0x6e,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,
  0x22,0x3a,0x20,0x20,0x22,0x23,0x63,0x61,0x63,0x61,0x63,0x62,0x22,0x2c,0xa,0x20,
  0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,0x62,0x67,0x22,0x3a,0x20,0x20,
  0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x20,0x22,0x23,0x65,0x38,0x31,0x31,
  0x32,0x33,0x22,0x2c,0xa,0x20,0x20,0x20,0x20,0x22,0x63,0x6c,0x6f,0x73,0x65,0x2d,
  0x62,0x67,0x2d,0x70,0x72,0x65,0x73,0x73,0x65,0x64,0x22,0x3a,0x20,0x20,0x20,0x20,
  0x22,0x23,0x66,0x31,0x37,0x30,0x37,0x61,0x22,0x2c,0xa,,0xa,,0xa,,0xa,,0xa,0x20,
  0x20,0x7d,0xa,0x7d,0xa,

};

static const unsigned char qt_resource_name[] = {
  // SARibbonTheme
  0x0,0xd,
  0xc,0xeb,0x1c,0x55,
  0x0,0x53,
  0x0,0x41,0x0,0x52,0x0,0x69,0x0,0x62,0x0,0x62,0x0,0x6f,0x0,0x6e,0x0,0x54,0x0,0x68,0x0,0x65,0x0,0x6d,0x0,0x65,
	// SARibbon
  0x0,0x8,
  0x6,0x8f,0x83,0xbe,
  0x0,0x53,
  0x0,0x41,0x0,0x52,0x0,0x69,0x0,0x62,0x0,0x62,0x0,0x6f,0x0,0x6e,
	// image
  0x0,0x5,
  0x0,0x70,0x37,0xd5,
  0x0,0x69,
  0x0,0x6d,0x0,0x61,0x0,0x67,0x0,0x65,
	// resource
  0x0,0x8,
  0xc,0xa6,0xc7,0x95,
  0x0,0x72,
  0x0,0x65,0x0,0x73,0x0,0x6f,0x0,0x75,0x0,0x72,0x0,0x63,0x0,0x65,
	// ArrowUp.png
  0x0,0xb,
  0xc,0x52,0x65,0x87,
  0x0,0x41,
  0x0,0x72,0x0,0x72,0x0,0x6f,0x0,0x77,0x0,0x55,0x0,0x70,0x0,0x2e,0x0,0x70,0x0,0x6e,0x0,0x67,
	// Titlebar_Max.svg
  0x0,0x10,
  0x6,0xec,0x88,0x7,
  0x0,0x54,
  0x0,0x69,0x0,0x74,0x0,0x6c,0x0,0x65,0x0,0x62,0x0,0x61,0x0,0x72,0x0,0x5f,0x0,0x4d,0x0,0x61,0x0,0x78,0x0,0x2e,0x0,0x73,0x0,0x76,0x0,0x67,
	// Titlebar_Min.svg
  0x0,0x10,
  0x6,0xa6,0x88,0x7,
  0x0,0x54,
  0x0,0x69,0x0,0x74,0x0,0x6c,0x0,0x65,0x0,0x62,0x0,0x61,0x0,0x72,0x0,0x5f,0x0,0x4d,0x0,0x69,0x0,0x6e,0x0,0x2e,0x0,0x73,0x0,0x76,0x0,0x67,
	// ArrowMore.png
  0x0,0xd,
  0x7,0xb6,0x8e,0xe7,
  0x0,0x41,
  0x0,0x72,0x0,0x72,0x0,0x6f,0x0,0x77,0x0,0x4d,0x0,0x6f,0x0,0x72,0x0,0x65,0x0,0x2e,0x0,0x70,0x0,0x6e,0x0,0x67,
	// Titlebar_Close_Hover.svg
  0x0,0x18,
  0xe,0x56,0x59,0x47,
  0x0,0x54,
  0x0,0x69,0x0,0x74,0x0,0x6c,0x0,0x65,0x0,0x62,0x0,0x61,0x0,0x72,0x0,0x5f,0x0,0x43,0x0,0x6c,0x0,0x6f,0x0,0x73,0x0,0x65,0x0,0x5f,0x0,0x48,0x0,0x6f,
  0x0,0x76,0x0,0x65,0x0,0x72,0x0,0x2e,0x0,0x73,0x0,0x76,0x0,0x67,
	// Titlebar_Close.svg
  0x0,0x12,
  0x1,0x46,0xe4,0x67,
  0x0,0x54,
  0x0,0x69,0x0,0x74,0x0,0x6c,0x0,0x65,0x0,0x62,0x0,0x61,0x0,0x72,0x0,0x5f,0x0,0x43,0x0,0x6c,0x0,0x6f,0x0,0x73,0x0,0x65,0x0,0x2e,0x0,0x73,0x0,0x76,
  0x0,0x67,
	// Titlebar_Min_Hover.svg
  0x0,0x16,
  0x1,0x8a,0x81,0x87,
  0x0,0x54,
  0x0,0x69,0x0,0x74,0x0,0x6c,0x0,0x65,0x0,0x62,0x0,0x61,0x0,0x72,0x0,0x5f,0x0,0x4d,0x0,0x69,0x0,0x6e,0x0,0x5f,0x0,0x48,0x0,0x6f,0x0,0x76,0x0,0x65,
  0x0,0x72,0x0,0x2e,0x0,0x73,0x0,0x76,0x0,0x67,
	// Titlebar_Normal_Hover.svg
  0x0,0x19,
  0x0,0x19,0x27,0xa7,
  0x0,0x54,
  0x0,0x69,0x0,0x74,0x0,0x6c,0x0,0x65,0x0,0x62,0x0,0x61,0x0,0x72,0x0,0x5f,0x0,0x4e,0x0,0x6f,0x0,0x72,0x0,0x6d,0x0,0x61,0x0,0x6c,0x0,0x5f,0x0,0x48,
  0x0,0x6f,0x0,0x76,0x0,0x65,0x0,0x72,0x0,0x2e,0x0,0x73,0x0,0x76,0x0,0x67,
	// Titlebar_Unshade.png
  0x0,0x14,
  0x5,0xc6,0x2b,0x27,
  0x0,0x54,
  0x0,0x69,0x0,0x74,0x0,0x6c,0x0,0x65,0x0,0x62,0x0,0x61,0x0,0x72,0x0,0x5f,0x0,0x55,0x0,0x6e,0x0,0x73,0x0,0x68,0x0,0x61,0x0,0x64,0x0,0x65,0x0,0x2e,
  0x0,0x70,0x0,0x6e,0x0,0x67,
	// define-color.svg
  0x0,0x10,
  0x3,0xff,0xbe,0xc7,
  0x0,0x64,
  0x0,0x65,0x0,0x66,0x0,0x69,0x0,0x6e,0x0,0x65,0x0,0x2d,0x0,0x63,0x0,0x6f,0x0,0x6c,0x0,0x6f,0x0,0x72,0x0,0x2e,0x0,0x73,0x0,0x76,0x0,0x67,
	// Titlebar_Max_Hover.svg
  0x0,0x16,
  0x1,0x1e,0x81,0x87,
  0x0,0x54,
  0x0,0x69,0x0,0x74,0x0,0x6c,0x0,0x65,0x0,0x62,0x0,0x61,0x0,0x72,0x0,0x5f,0x0,0x4d,0x0,0x61,0x0,0x78,0x0,0x5f,0x0,0x48,0x0,0x6f,0x0,0x76,0x0,0x65,
  0x0,0x72,0x0,0x2e,0x0,0x73,0x0,0x76,0x0,0x67,
	// Titlebar_Shade.png
  0x0,0x12,
  0x0,0x76,0xe,0x47,
  0x0,0x54,
  0x0,0x69,0x0,0x74,0x0,0x6c,0x0,0x65,0x0,0x62,0x0,0x61,0x0,0x72,0x0,0x5f,0x0,0x53,0x0,0x68,0x0,0x61,0x0,0x64,0x0,0x65,0x0,0x2e,0x0,0x70,0x0,0x6e,
  0x0,0x67,
	// Titlebar_Normal.svg
  0x0,0x13,
  0xe,0x53,0x5b,0x7,
  0x0,0x54,
  0x0,0x69,0x0,0x74,0x0,0x6c,0x0,0x65,0x0,0x62,0x0,0x61,0x0,0x72,0x0,0x5f,0x0,0x4e,0x0,0x6f,0x0,0x72,0x0,0x6d,0x0,0x61,0x0,0x6c,0x0,0x2e,0x0,0x73,
  0x0,0x76,0x0,0x67,
	// ArrowDown.png
  0x0,0xd,
  0x8,0x13,0x81,0x7,
  0x0,0x41,
  0x0,0x72,0x0,0x72,0x0,0x6f,0x0,0x77,0x0,0x44,0x0,0x6f,0x0,0x77,0x0,0x6e,0x0,0x2e,0x0,0x70,0x0,0x6e,0x0,0x67,
	// ribbonPanelOptionButton.png
  0x0,0x1b,
  0x5,0x57,0xcd,0x27,
  0x0,0x72,
  0x0,0x69,0x0,0x62,0x0,0x62,0x0,0x6f,0x0,0x6e,0x0,0x50,0x0,0x61,0x0,0x6e,0x0,0x65,0x0,0x6c,0x0,0x4f,0x0,0x70,0x0,0x74,0x0,0x69,0x0,0x6f,0x0,0x6e,
  0x0,0x42,0x0,0x75,0x0,0x74,0x0,0x74,0x0,0x6f,0x0,0x6e,0x0,0x2e,0x0,0x70,0x0,0x6e,0x0,0x67,
	// palettes
  0x0,0x8,
  0x8,0x2c,0xb4,0x3,
  0x0,0x70,
  0x0,0x61,0x0,0x6c,0x0,0x65,0x0,0x74,0x0,0x74,0x0,0x65,0x0,0x73,
	// theme-base.qss
  0x0,0xe,
  0x5,0x13,0x41,0x3,
  0x0,0x74,
  0x0,0x68,0x0,0x65,0x0,0x6d,0x0,0x65,0x0,0x2d,0x0,0x62,0x0,0x61,0x0,0x73,0x0,0x65,0x0,0x2e,0x0,0x71,0x0,0x73,0x0,0x73,
	// templates
  0x0,0x9,
  0x4,0x72,0x7f,0x43,
  0x0,0x74,
  0x0,0x65,0x0,0x6d,0x0,0x70,0x0,0x6c,0x0,0x61,0x0,0x74,0x0,0x65,0x0,0x73,
	// office2013.qss
  0x0,0xe,
  0xa,0xdd,0x75,0x63,
  0x0,0x6f,
  0x0,0x66,0x0,0x66,0x0,0x69,0x0,0x63,0x0,0x65,0x0,0x32,0x0,0x30,0x0,0x31,0x0,0x33,0x0,0x2e,0x0,0x71,0x0,0x73,0x0,0x73,
	// office2021.qss
  0x0,0xe,
  0xa,0xcf,0x75,0x63,
  0x0,0x6f,
  0x0,0x66,0x0,0x66,0x0,0x69,0x0,0x63,0x0,0x65,0x0,0x32,0x0,0x30,0x0,0x32,0x0,0x31,0x0,0x2e,0x0,0x71,0x0,0x73,0x0,0x73,
	// office2016.qss
  0x0,0xe,
  0xa,0xd6,0x75,0x63,
  0x0,0x6f,
  0x0,0x66,0x0,0x66,0x0,0x69,0x0,0x63,0x0,0x65,0x0,0x32,0x0,0x30,0x0,0x31,0x0,0x36,0x0,0x2e,0x0,0x71,0x0,0x73,0x0,0x73,
	// win7.qss
  0x0,0x8,
  0x0,0x1a,0x57,0x63,
  0x0,0x77,
  0x0,0x69,0x0,0x6e,0x0,0x37,0x0,0x2e,0x0,0x71,0x0,0x73,0x0,0x73,
	// dark.qss
  0x0,0x8,
  0x8,0x8e,0x55,0xe3,
  0x0,0x64,
  0x0,0x61,0x0,0x72,0x0,0x6b,0x0,0x2e,0x0,0x71,0x0,0x73,0x0,0x73,
	// dark2.qss
  0x0,0x9,
  0x8,0xe5,0x8d,0xa3,
  0x0,0x64,
  0x0,0x61,0x0,0x72,0x0,0x6b,0x0,0x32,0x0,0x2e,0x0,0x71,0x0,0x73,0x0,0x73,
	// office2021-blue.json
  0x0,0x14,
  0xf,0x1b,0x33,0x9e,
  0x0,0x6f,
  0x0,0x66,0x0,0x66,0x0,0x69,0x0,0x63,0x0,0x65,0x0,0x32,0x0,0x30,0x0,0x32,0x0,0x31,0x0,0x2d,0x0,0x62,0x0,0x6c,0x0,0x75,0x0,0x65,0x0,0x2e,0x0,0x6a,
  0x0,0x73,0x0,0x6f,0x0,0x6e,
	// dark-default.json
  0x0,0x11,
  0x5,0x88,0xad,0x5e,
  0x0,0x64,
  0x0,0x61,0x0,0x72,0x0,0x6b,0x0,0x2d,0x0,0x64,0x0,0x65,0x0,0x66,0x0,0x61,0x0,0x75,0x0,0x6c,0x0,0x74,0x0,0x2e,0x0,0x6a,0x0,0x73,0x0,0x6f,0x0,0x6e,

	// office2016-blue.json
  0x0,0x14,
  0xe,0xe9,0x33,0x9e,
  0x0,0x6f,
  0x0,0x66,0x0,0x66,0x0,0x69,0x0,0x63,0x0,0x65,0x0,0x32,0x0,0x30,0x0,0x31,0x0,0x36,0x0,0x2d,0x0,0x62,0x0,0x6c,0x0,0x75,0x0,0x65,0x0,0x2e,0x0,0x6a,
  0x0,0x73,0x0,0x6f,0x0,0x6e,
	// office2021-dark.json
  0x0,0x14,
  0xd,0x7b,0x38,0xfe,
  0x0,0x6f,
  0x0,0x66,0x0,0x66,0x0,0x69,0x0,0x63,0x0,0x65,0x0,0x32,0x0,0x30,0x0,0x32,0x0,0x31,0x0,0x2d,0x0,0x64,0x0,0x61,0x0,0x72,0x0,0x6b,0x0,0x2e,0x0,0x6a,
  0x0,0x73,0x0,0x6f,0x0,0x6e,
	// office2016-green.json
  0x0,0x15,
  0x2,0xd7,0x75,0x9e,
  0x0,0x6f,
  0x0,0x66,0x0,0x66,0x0,0x69,0x0,0x63,0x0,0x65,0x0,0x32,0x0,0x30,0x0,0x31,0x0,0x36,0x0,0x2d,0x0,0x67,0x0,0x72,0x0,0x65,0x0,0x65,0x0,0x6e,0x0,0x2e,
  0x0,0x6a,0x0,0x73,0x0,0x6f,0x0,0x6e,
	// office2021-green.json
  0x0,0x15,
  0x5,0xf7,0x75,0x9e,
  0x0,0x6f,
  0x0,0x66,0x0,0x66,0x0,0x69,0x0,0x63,0x0,0x65,0x0,0x32,0x0,0x30,0x0,0x32,0x0,0x31,0x0,0x2d,0x0,0x67,0x0,0x72,0x0,0x65,0x0,0x65,0x0,0x6e,0x0,0x2e,
  0x0,0x6a,0x0,0x73,0x0,0x6f,0x0,0x6e,
	// office2013-default.json
  0x0,0x17,
  0x1,0x98,0x40,0x1e,
  0x0,0x6f,
  0x0,0x66,0x0,0x66,0x0,0x69,0x0,0x63,0x0,0x65,0x0,0x32,0x0,0x30,0x0,0x31,0x0,0x33,0x0,0x2d,0x0,0x64,0x0,0x65,0x0,0x66,0x0,0x61,0x0,0x75,0x0,0x6c,
  0x0,0x74,0x0,0x2e,0x0,0x6a,0x0,0x73,0x0,0x6f,0x0,0x6e,
	// dark2-default.json
  0x0,0x12,
  0xe,0x80,0xb2,0x3e,
  0x0,0x64,
  0x0,0x61,0x0,0x72,0x0,0x6b,0x0,0x32,0x0,0x2d,0x0,0x64,0x0,0x65,0x0,0x66,0x0,0x61,0x0,0x75,0x0,0x6c,0x0,0x74,0x0,0x2e,0x0,0x6a,0x0,0x73,0x0,0x6f,
  0x0,0x6e,
	// office2016-dark.json
  0x0,0x14,
  0xd,0x89,0x38,0xfe,
  0x0,0x6f,
  0x0,0x66,0x0,0x66,0x0,0x69,0x0,0x63,0x0,0x65,0x0,0x32,0x0,0x30,0x0,0x31,0x0,0x36,0x0,0x2d,0x0,0x64,0x0,0x61,0x0,0x72,0x0,0x6b,0x0,0x2e,0x0,0x6a,
  0x0,0x73,0x0,0x6f,0x0,0x6e,
	// win7-default.json
  0x0,0x11,
  0x5,0xda,0x82,0x5e,
  0x0,0x77,
  0x0,0x69,0x0,0x6e,0x0,0x37,0x0,0x2d,0x0,0x64,0x0,0x65,0x0,0x66,0x0,0x61,0x0,0x75,0x0,0x6c,0x0,0x74,0x0,0x2e,0x0,0x6a,0x0,0x73,0x0,0x6f,0x0,0x6e,

};

static const unsigned char qt_resource_struct[] = {
  // :
  0x0,0x0,0x0,0x0,0x0,0x2,0x0,0x0,0x0,0x2,0x0,0x0,0x0,0x1,
0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,
  // :/SARibbon
  0x0,0x0,0x0,0x20,0x0,0x2,0x0,0x0,0x0,0x1,0x0,0x0,0x0,0x17,
0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,
  // :/SARibbonTheme
  0x0,0x0,0x0,0x0,0x0,0x2,0x0,0x0,0x0,0x1,0x0,0x0,0x0,0x3,
0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,
  // :/SARibbonTheme/resource
  0x0,0x0,0x0,0x46,0x0,0x2,0x0,0x0,0x0,0x3,0x0,0x0,0x0,0x4,
0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,
  // :/SARibbonTheme/resource/templates
  0x0,0x0,0x3,0x1e,0x0,0x2,0x0,0x0,0x0,0x6,0x0,0x0,0x0,0x11,
0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,
  // :/SARibbonTheme/resource/theme-base.qss
  0x0,0x0,0x2,0xfc,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x21,0x21,
0x0,0x0,0x1,0x9e,0x9c,0x32,0xd3,0x68,
  // :/SARibbonTheme/resource/palettes
  0x0,0x0,0x2,0xe6,0x0,0x2,0x0,0x0,0x0,0xa,0x0,0x0,0x0,0x7,
0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,
  // :/SARibbonTheme/resource/palettes/office2013-default.json
  0x0,0x0,0x4,0xf2,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x83,0xe6,
0x0,0x0,0x1,0x9e,0xa1,0x84,0xa,0xfa,
  // :/SARibbonTheme/resource/palettes/office2016-green.json
  0x0,0x0,0x4,0x92,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x76,0x2f,
0x0,0x0,0x1,0x9e,0x5a,0x9a,0x53,0xa3,
  // :/SARibbonTheme/resource/palettes/dark-default.json
  0x0,0x0,0x4,0xe,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x5e,0xe6,
0x0,0x0,0x1,0x9e,0x5a,0xaa,0xd0,0xd3,
  // :/SARibbonTheme/resource/palettes/win7-default.json
  0x0,0x0,0x5,0x7e,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x92,0xe0,
0x0,0x0,0x1,0x9e,0xa1,0x90,0xc6,0x6f,
  // :/SARibbonTheme/resource/palettes/office2021-green.json
  0x0,0x0,0x4,0xc2,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x7b,0x61,
0x0,0x0,0x1,0x9e,0xa1,0xeb,0x30,0x79,
  // :/SARibbonTheme/resource/palettes/office2021-dark.json
  0x0,0x0,0x4,0x64,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x6c,0xee,
0x0,0x0,0x1,0x9e,0xa1,0xeb,0x34,0xfd,
  // :/SARibbonTheme/resource/palettes/office2016-dark.json
  0x0,0x0,0x5,0x50,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x8d,0x1f,
0x0,0x0,0x1,0x9e,0x5a,0x9a,0x9a,0xc4,
  // :/SARibbonTheme/resource/palettes/dark2-default.json
  0x0,0x0,0x5,0x26,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x89,0xd,
0x0,0x0,0x1,0x9e,0x9c,0x38,0x72,0x69,
  // :/SARibbonTheme/resource/palettes/office2016-blue.json
  0x0,0x0,0x4,0x36,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x65,0x77,
0x0,0x0,0x1,0x9e,0x5a,0x99,0xd7,0xd7,
  // :/SARibbonTheme/resource/palettes/office2021-blue.json
  0x0,0x0,0x3,0xe0,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x56,0x3a,
0x0,0x0,0x1,0x9e,0xa1,0xeb,0x63,0x63,
  // :/SARibbonTheme/resource/templates/win7.qss
  0x0,0x0,0x3,0x9c,0x0,0x1,0x0,0x0,0x0,0x1,0x0,0x0,0x40,0xb4,
0x0,0x0,0x1,0x9e,0xa2,0x6,0x4,0x70,
  // :/SARibbonTheme/resource/templates/dark.qss
  0x0,0x0,0x3,0xb2,0x0,0x1,0x0,0x0,0x0,0x1,0x0,0x0,0x46,0x2e,
0x0,0x0,0x1,0x9e,0x9c,0x32,0xeb,0xe5,
  // :/SARibbonTheme/resource/templates/dark2.qss
  0x0,0x0,0x3,0xc8,0x0,0x1,0x0,0x0,0x0,0x1,0x0,0x0,0x4e,0x3a,
0x0,0x0,0x1,0x9e,0x9c,0x38,0x72,0x68,
  // :/SARibbonTheme/resource/templates/office2021.qss
  0x0,0x0,0x3,0x58,0x0,0x1,0x0,0x0,0x0,0x1,0x0,0x0,0x2f,0xab,
0x0,0x0,0x1,0x9e,0xa1,0xeb,0x18,0x3e,
  // :/SARibbonTheme/resource/templates/office2016.qss
  0x0,0x0,0x3,0x7a,0x0,0x1,0x0,0x0,0x0,0x1,0x0,0x0,0x38,0x36,
0x0,0x0,0x1,0x9e,0x5a,0x96,0x9e,0x3b,
  // :/SARibbonTheme/resource/templates/office2013.qss
  0x0,0x0,0x3,0x36,0x0,0x1,0x0,0x0,0x0,0x1,0x0,0x0,0x2a,0x68,
0x0,0x0,0x1,0x9e,0xa2,0x6,0x39,0x22,
  // :/SARibbon/image
  0x0,0x0,0x0,0x36,0x0,0x2,0x0,0x0,0x0,0x1,0x0,0x0,0x0,0x18,
0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,
  // :/SARibbon/image/resource
  0x0,0x0,0x0,0x46,0x0,0x2,0x0,0x0,0x0,0xf,0x0,0x0,0x0,0x19,
0x0,0x0,0x0,0x0,0x0,0x0,0x0,0x0,
  // :/SARibbon/image/resource/Titlebar_Normal_Hover.svg
  0x0,0x0,0x1,0x76,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0xc,0x36,
0x0,0x0,0x1,0x9e,0x57,0xe7,0x59,0x3f,
  // :/SARibbon/image/resource/Titlebar_Shade.png
  0x0,0x0,0x2,0x34,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x1a,0x8c,
0x0,0x0,0x1,0x99,0x52,0xc0,0x75,0x36,
  // :/SARibbon/image/resource/Titlebar_Max_Hover.svg
  0x0,0x0,0x2,0x2,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x18,0xb1,
0x0,0x0,0x1,0x9e,0x57,0xe7,0x59,0x3e,
  // :/SARibbon/image/resource/Titlebar_Close.svg
  0x0,0x0,0x1,0x1a,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x8,0x12,
0x0,0x0,0x1,0x9e,0x57,0xe7,0x59,0x3d,
  // :/SARibbon/image/resource/Titlebar_Min_Hover.svg
  0x0,0x0,0x1,0x44,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0xa,0x66,
0x0,0x0,0x1,0x9e,0x57,0xe7,0x59,0x3f,
  // :/SARibbon/image/resource/define-color.svg
  0x0,0x0,0x1,0xdc,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x10,0x7a,
0x0,0x0,0x1,0x99,0x52,0xc0,0x75,0x36,
  // :/SARibbon/image/resource/ribbonPanelOptionButton.png
  0x0,0x0,0x2,0xaa,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x1f,0xd2,
0x0,0x0,0x1,0x99,0x52,0xc0,0x75,0x37,
  // :/SARibbon/image/resource/Titlebar_Unshade.png
  0x0,0x0,0x1,0xae,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0xe,0xf1,
0x0,0x0,0x1,0x99,0x52,0xc0,0x75,0x36,
  // :/SARibbon/image/resource/Titlebar_Min.svg
  0x0,0x0,0x0,0x9e,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x2,0xda,
0x0,0x0,0x1,0x9e,0x57,0xe7,0x59,0x3e,
  // :/SARibbon/image/resource/Titlebar_Max.svg
  0x0,0x0,0x0,0x78,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x0,0xff,
0x0,0x0,0x1,0x9e,0x57,0xe7,0x59,0x3e,
  // :/SARibbon/image/resource/ArrowMore.png
  0x0,0x0,0x0,0xc4,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x4,0xaa,
0x0,0x0,0x1,0x99,0x52,0xc0,0x75,0x34,
  // :/SARibbon/image/resource/ArrowDown.png
  0x0,0x0,0x2,0x8a,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x1e,0xd2,
0x0,0x0,0x1,0x99,0x52,0xc0,0x75,0x34,
  // :/SARibbon/image/resource/ArrowUp.png
  0x0,0x0,0x0,0x5c,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x0,0x0,
0x0,0x0,0x1,0x99,0x52,0xc0,0x75,0x35,
  // :/SARibbon/image/resource/Titlebar_Normal.svg
  0x0,0x0,0x2,0x5e,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x1c,0x17,
0x0,0x0,0x1,0x9e,0x57,0xe7,0x59,0x3f,
  // :/SARibbon/image/resource/Titlebar_Close_Hover.svg
  0x0,0x0,0x0,0xe4,0x0,0x0,0x0,0x0,0x0,0x1,0x0,0x0,0x5,0xaf,
0x0,0x0,0x1,0x9e,0x57,0xe7,0x59,0x3e,

};

/*** End of inlined file: qrc_SARibbonResource_Datas.cpp ***/


#if QT_VERSION >= QT_VERSION_CHECK(5,14,0)

/*** Start of inlined file: qrc_SARibbonResource_version3.cpp ***/
#ifdef QT_NAMESPACE
#define QT_RCC_PREPEND_NAMESPACE(name) ::QT_NAMESPACE::name
#define QT_RCC_MANGLE_NAMESPACE0(x) x
#define QT_RCC_MANGLE_NAMESPACE1(a, b) a##_##b
#define QT_RCC_MANGLE_NAMESPACE2(a, b) QT_RCC_MANGLE_NAMESPACE1(a, b)
#define QT_RCC_MANGLE_NAMESPACE(name)                                                                                  \
	QT_RCC_MANGLE_NAMESPACE2(QT_RCC_MANGLE_NAMESPACE0(name), QT_RCC_MANGLE_NAMESPACE0(QT_NAMESPACE))
#else
#define QT_RCC_PREPEND_NAMESPACE(name) name
#define QT_RCC_MANGLE_NAMESPACE(name) name
#endif

#ifdef QT_NAMESPACE
namespace QT_NAMESPACE
{
#endif

bool qRegisterResourceData(int, const unsigned char*, const unsigned char*, const unsigned char*);
bool qUnregisterResourceData(int, const unsigned char*, const unsigned char*, const unsigned char*);

#if defined(__ELF__) || defined(__APPLE__)
static inline unsigned char qResourceFeatureZlib()
{
	extern const unsigned char qt_resourceFeatureZlib;
	return qt_resourceFeatureZlib;
}
#else
unsigned char qResourceFeatureZlib();
#endif

#ifdef QT_NAMESPACE
}
#endif

int QT_RCC_MANGLE_NAMESPACE(qInitResources_SARibbonResource)();
int QT_RCC_MANGLE_NAMESPACE(qInitResources_SARibbonResource)()
{
	int version = 3;
	QT_RCC_PREPEND_NAMESPACE(qRegisterResourceData)
	(version, qt_resource_struct, qt_resource_name, qt_resource_data);
	return 1;
}

int QT_RCC_MANGLE_NAMESPACE(qCleanupResources_SARibbonResource)();
int QT_RCC_MANGLE_NAMESPACE(qCleanupResources_SARibbonResource)()
{
	int version = 3;
	version += QT_RCC_PREPEND_NAMESPACE(qResourceFeatureZlib());
	QT_RCC_PREPEND_NAMESPACE(qUnregisterResourceData)
	(version, qt_resource_struct, qt_resource_name, qt_resource_data);
	return 1;
}

#ifdef __clang__
#pragma clang diagnostic push
#pragma clang diagnostic ignored "-Wexit-time-destructors"
#endif

namespace
{
struct initializer
{
	initializer()
	{
		QT_RCC_MANGLE_NAMESPACE(qInitResources_SARibbonResource)();
	}
	~initializer()
	{
		QT_RCC_MANGLE_NAMESPACE(qCleanupResources_SARibbonResource)();
	}
} dummy;
}

#ifdef __clang__
#pragma clang diagnostic pop
#endif

/*** End of inlined file: qrc_SARibbonResource_version3.cpp ***/


#else

/*** Start of inlined file: qrc_SARibbonResource_version2.cpp ***/
#ifdef QT_NAMESPACE
#  define QT_RCC_PREPEND_NAMESPACE(name) ::QT_NAMESPACE::name
#  define QT_RCC_MANGLE_NAMESPACE0(x) x
#  define QT_RCC_MANGLE_NAMESPACE1(a, b) a##_##b
#  define QT_RCC_MANGLE_NAMESPACE2(a, b) QT_RCC_MANGLE_NAMESPACE1(a,b)
#  define QT_RCC_MANGLE_NAMESPACE(name) QT_RCC_MANGLE_NAMESPACE2( \
		QT_RCC_MANGLE_NAMESPACE0(name), QT_RCC_MANGLE_NAMESPACE0(QT_NAMESPACE))
#else
#   define QT_RCC_PREPEND_NAMESPACE(name) name
#   define QT_RCC_MANGLE_NAMESPACE(name) name
#endif

#ifdef QT_NAMESPACE
namespace QT_NAMESPACE {
#endif

bool qRegisterResourceData(int, const unsigned char *, const unsigned char *, const unsigned char *);

bool qUnregisterResourceData(int, const unsigned char *, const unsigned char *, const unsigned char *);

#ifdef QT_NAMESPACE
}
#endif

int QT_RCC_MANGLE_NAMESPACE(qInitResources_SARibbonResource)();
int QT_RCC_MANGLE_NAMESPACE(qInitResources_SARibbonResource)()
{
	QT_RCC_PREPEND_NAMESPACE(qRegisterResourceData)
		(0x2, qt_resource_struct, qt_resource_name, qt_resource_data);
	return 1;
}

int QT_RCC_MANGLE_NAMESPACE(qCleanupResources_SARibbonResource)();
int QT_RCC_MANGLE_NAMESPACE(qCleanupResources_SARibbonResource)()
{
	QT_RCC_PREPEND_NAMESPACE(qUnregisterResourceData)
	   (0x2, qt_resource_struct, qt_resource_name, qt_resource_data);
	return 1;
}

namespace {
   struct initializer {
	   initializer() { QT_RCC_MANGLE_NAMESPACE(qInitResources_SARibbonResource)(); }
	   ~initializer() { QT_RCC_MANGLE_NAMESPACE(qCleanupResources_SARibbonResource)(); }
   } dummy;
}

/*** End of inlined file: qrc_SARibbonResource_version2.cpp ***/


#endif
// clang-format on


/*** Start of inlined file: SAColorMenu.cpp ***/
#include <QWidgetAction>
#include <QColorDialog>
#include <QDebug>
#include <QLabel>
#include <QGridLayout>
#include <QPainter>

/**
 * \if ENGLISH
 * @brief Private data class for SAColorMenu
 * \endif
 *
 * \if CHINESE
 * @brief SAColorMenu的私有数据类
 * \endif
 */
class SAColorMenu::PrivateData
{
	SA_COLOR_WIDGETS_DECLARE_PUBLIC(SAColorMenu)
public:
	/**
	 * \if ENGLISH
	 * @brief Constructor for PrivateData
	 * @param p Parent SAColorMenu
	 * \endif
	 *
	 * \if CHINESE
	 * @brief PrivateData构造函数
	 * @param p 父SAColorMenu
	 * \endif
	 */
	PrivateData(SAColorMenu* p);

	/**
	 * \if ENGLISH
	 * @brief Get color from color dialog
	 * @return Selected color, or invalid color if dialog is canceled
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 通过对话框获取颜色
	 * @return 选中的颜色，如果对话框被取消则返回无效颜色
	 * \endif
	 */
	QColor getColorByDialog();

	/**
	 * \if ENGLISH
	 * @brief Record custom color
	 * @param c Color to record
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 记录自定义颜色
	 * @param c 要记录的颜色
	 * \endif
	 */
	void recordCustomColor(const QColor& c);

	/**
	 * \if ENGLISH
	 * @brief Add widget to menu
	 * @param w Widget to add
	 * @return Created QWidgetAction
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 向菜单添加部件
	 * @param w 要添加的部件
	 * @return 创建的QWidgetAction
	 * \endif
	 */
	QWidgetAction* addWidget(QWidget* w);

	/**
	 * \if ENGLISH
	 * @brief Create a none color icon
	 * @param baseSize Icon base size
	 * @return Created icon
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 创建一个无颜色的icon
	 * @param baseSize 图标基础大小
	 * @return 创建的图标
	 * \endif
	 */
	QIcon createNoneColorIcon(QSize baseSize = QSize(32, 32));

public:
	QLabel* mTitleLabel { nullptr };  ///< 主题颜色标题
	QWidgetAction* mTitleLabelAction { nullptr };
	SAColorPaletteGridWidget* mThemeColorsWidget { nullptr };  ///< 主题颜色
	QWidgetAction* mThemeColorPaletteAction { nullptr };       ///< ThemeColorsPalette对应的action
	QAction* mCustomColorAction { nullptr };                   ///< 自定义颜色action
	SAColorGridWidget* mCustomColorsWidget { nullptr };        ///< 自定义颜色记录
	QWidgetAction* mCustomColorsWidgetAction { nullptr };      ///< 自定义颜色窗口对应的action
	QAction* mNoneColorAction { nullptr };                     ///< 无颜色action
	QList< QColor > mCustomColors;
	int mMaxCustomColorSize { 10 };                        ///< 记录最多的自定义颜色数量
	QScopedPointer< QColorDialog > mColorDlg { nullptr };  ///<颜色对话框
};

SAColorMenu::PrivateData::PrivateData(SAColorMenu* p) : q_ptr(p)
{
}

QColor SAColorMenu::PrivateData::getColorByDialog()
{
	if (nullptr == mColorDlg) {
		mColorDlg.reset(new QColorDialog());
	}
	if (QDialog::Accepted == mColorDlg->exec()) {
		return mColorDlg->currentColor();
	}
	return QColor();
}

void SAColorMenu::PrivateData::recordCustomColor(const QColor& c)
{
	if (mCustomColors.size() < mMaxCustomColorSize) {
		mCustomColors.push_back(c);
	} else {
		//超过数量，就左移动
		for (int i = 1; i < mCustomColors.size(); ++i) {
			mCustomColors[ i - 1 ] = mCustomColors[ i ];
		}
		mCustomColors.back() = c;
	}
}

QWidgetAction* SAColorMenu::PrivateData::addWidget(QWidget* w)
{
	QWidgetAction* wa = new QWidgetAction(q_ptr);
	wa->setDefaultWidget(w);
	q_ptr->addAction(wa);
	return wa;
}

QIcon SAColorMenu::PrivateData::createNoneColorIcon(QSize baseSize)
{
	QPixmap pixmap(baseSize);
	pixmap.fill(Qt::transparent);
	QPainter p(&pixmap);
	SAColorToolButton::paintNoneColor(&p, QRect(0, 0, pixmap.width(), pixmap.height()).adjusted(1, 1, -1, -1));
	return QIcon(pixmap);
}
//===================================================
// SAColorMenu
//===================================================

/**
 * \if ENGLISH
 * @brief Constructor for SAColorMenu
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SAColorMenu构造函数
 * @param parent 父部件
 * \endif
 */
SAColorMenu::SAColorMenu(QWidget* parent) : QMenu(parent), d_ptr(new SAColorMenu::PrivateData(this))
{
	init(SA::getStandardColorList());
}

/**
 * \if ENGLISH
 * @brief Constructor for SAColorMenu with title
 * @param title Menu title
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SAColorMenu构造函数（带标题）
 * @param title 菜单标题
 * @param parent 父部件
 * \endif
 */
SAColorMenu::SAColorMenu(const QString& title, QWidget* parent)
	: QMenu(title, parent), d_ptr(new SAColorMenu::PrivateData(this))
{
	init(SA::getStandardColorList());
}

/**
 * \if ENGLISH
 * @brief Destructor for SAColorMenu
 * \endif
 *
 * \if CHINESE
 * @brief SAColorMenu析构函数
 * \endif
 */
SAColorMenu::~SAColorMenu()
{
}

/**
 * \if ENGLISH
 * @brief Quick bind to SAColorToolButton
 * @param btn SAColorToolButton to bind
 * \endif
 *
 * \if CHINESE
 * @brief 快速绑定ColorToolButton
 * @param btn 要绑定的SAColorToolButton
 * \endif
 */
void SAColorMenu::bindToColorToolButton(SAColorToolButton* btn)
{
	if (!btn) {
		return;
	}
	if (btn->menu() != this) {
		btn->setMenu(this);
	}
	connect(this, &SAColorMenu::selectedColor, btn, &SAColorToolButton::setColor);
}

/**
 * \if ENGLISH
 * @brief Get theme colors palette action
 * @return Theme colors palette action
 * \endif
 *
 * \if CHINESE
 * @brief ThemeColorsPalette对应的action
 * @return 主题颜色面板对应的action
 * \endif
 */
QWidgetAction* SAColorMenu::themeColorsPaletteAction() const
{
	return d_ptr->mThemeColorPaletteAction;
}

/**
 * \if ENGLISH
 * @brief Get custom colors widget action
 * @return Custom colors widget action
 * \endif
 *
 * \if CHINESE
 * @brief CustomColorsWidget对应的action
 * @return 自定义颜色窗口对应的action
 * \endif
 */
QWidgetAction* SAColorMenu::getCustomColorsWidgetAction() const
{
	return d_ptr->mCustomColorsWidgetAction;
}

/**
 * \if ENGLISH
 * @brief Get custom color action
 * @return Custom color action
 * \endif
 *
 * \if CHINESE
 * @brief 自定义颜色action
 * @return 自定义颜色action
 * \endif
 */
QAction* SAColorMenu::customColorAction() const
{
	return d_ptr->mCustomColorAction;
}

/**
 * \if ENGLISH
 * @brief Get theme colors palette widget
 * @return Theme colors palette widget
 * \endif
 *
 * \if CHINESE
 * @brief 获取ThemeColorsPalette
 * @return 主题颜色面板部件
 * \endif
 */
SAColorPaletteGridWidget* SAColorMenu::colorPaletteGridWidget() const
{
	return d_ptr->mThemeColorsWidget;
}

/**
 * \if ENGLISH
 * @brief Get custom colors widget
 * @return Custom colors widget
 * \endif
 *
 * \if CHINESE
 * @brief 获取自定义颜色grid
 * @return 自定义颜色网格部件
 * \endif
 */
SAColorGridWidget* SAColorMenu::customColorsWidget() const
{
	return d_ptr->mCustomColorsWidget;
}

/**
 * \if ENGLISH
 * @brief Enable none color action
 * @details None color selection will emit selectedColor(QColor())
 * @param on Whether to enable none color action
 * \endif
 *
 * \if CHINESE
 * @brief 建立没有颜色的action，可以选择无颜色
 * @details 无颜色选中会发射selectedColor(QColor())
 * @param on 是否启用无颜色action
 * \endif
 */
void SAColorMenu::enableNoneColorAction(bool on)
{
	//无颜色默认是在自定义颜色的上方
	if (on) {
		if (d_ptr->mNoneColorAction) {
			if (actions().contains(d_ptr->mNoneColorAction)) {
				//已经包含了NoneColorAction,退出
				return;
			} else {
				insertAction(d_ptr->mCustomColorAction, d_ptr->mNoneColorAction);
			}
		} else {
			QIcon ic                = d_ptr->createNoneColorIcon();
			d_ptr->mNoneColorAction = new QAction(ic, tr("None"), this);
			connect(d_ptr->mNoneColorAction, &QAction::triggered, this, &SAColorMenu::onNoneColorActionTriggered);
			insertAction(d_ptr->mCustomColorAction, d_ptr->mNoneColorAction);
		}
	} else {
		removeAction(d_ptr->mNoneColorAction);
		// remove后暂时不删除action
	}
}

/**
 * \if ENGLISH
 * @brief Get none color action
 * @note Note that enableNoneColorAction(true) must be called before this returns non-nullptr
 * @return None color action, or nullptr if not created
 * \endif
 *
 * \if CHINESE
 * @brief 获取None Color Action
 * @note 注意，enableNoneColorAction(true),之后才不是nullptr
 * @return 如果没有建立NoneColorAction，会返回nullptr
 * \endif
 */
QAction* SAColorMenu::noneColorAction() const
{
	return d_ptr->mNoneColorAction;
}

/**
 * \if ENGLISH
 * @brief Helper slot function to emit selectedColor signal and hide menu
 * @details This function allows other custom actions to be associated with the menu
 * @param c Selected color
 * \endif
 *
 * \if CHINESE
 * @brief 这是一个辅助槽函数，为了让用户自定义的其他action也能关联menu，可以调用此槽函数，实现selectedColor信号以及menu的隐藏
 * @param c 选中的颜色
 * \endif
 */
void SAColorMenu::emitSelectedColor(const QColor& c)
{
	Q_EMIT selectedColor(c);
	hide();
}

/**
 * \if ENGLISH
 * @brief Initialize menu
 * @param themeCls Theme color list
 * \endif
 *
 * \if CHINESE
 * @brief 初始化菜单
 * @param themeCls 主题颜色列表
 * \endif
 */
void SAColorMenu::init(const QList< QColor >& themeCls)
{
	d_ptr->mTitleLabel = new QLabel(this);
	d_ptr->mTitleLabel->setText(tr("Theme Colors"));
	d_ptr->mTitleLabelAction = d_ptr->addWidget(d_ptr->mTitleLabel);

	d_ptr->mThemeColorsWidget = new SAColorPaletteGridWidget(themeCls, this);
	d_ptr->mThemeColorsWidget->setColorCheckable(false);
	d_ptr->mThemeColorPaletteAction = d_ptr->addWidget(d_ptr->mThemeColorsWidget);

	d_ptr->mCustomColorAction = new QAction(tr("Custom Color"), this);  // cn:自定义颜色
	addAction(d_ptr->mCustomColorAction);

	QSize clrSize              = d_ptr->mThemeColorsWidget->colorIconSize();
	d_ptr->mCustomColorsWidget = new SAColorGridWidget(this);
	d_ptr->mCustomColorsWidget->setRowMinimumHeight(0, clrSize.height());
	d_ptr->mCustomColorsWidget->setHorizontalSpacerToRight();
	d_ptr->mCustomColorsWidget->setColorIconSize(clrSize);
	d_ptr->mCustomColorsWidget->setColumnCount(d_ptr->mMaxCustomColorSize);
	d_ptr->mCustomColorsWidget->setColorCheckable(false);
	d_ptr->mCustomColorsWidgetAction = d_ptr->addWidget(d_ptr->mCustomColorsWidget);

	connect(d_ptr->mCustomColorAction, &QAction::triggered, this, &SAColorMenu::onCustomColorActionTriggered);
	connect(d_ptr->mThemeColorsWidget, &SAColorPaletteGridWidget::colorClicked, this, &SAColorMenu::emitSelectedColor);
	connect(d_ptr->mCustomColorsWidget, &SAColorGridWidget::colorClicked, this, &SAColorMenu::emitSelectedColor);
}

/**
 * \if ENGLISH
 * @brief Handle custom color action triggered
 * @param on Triggered state
 * \endif
 *
 * \if CHINESE
 * @brief 自定义颜色
 * @param on 触发状态
 * \endif
 */
void SAColorMenu::onCustomColorActionTriggered(bool on)
{
	Q_UNUSED(on);
	QColor c = d_ptr->getColorByDialog();
	if (c.isValid()) {
		d_ptr->recordCustomColor(c);
		d_ptr->mCustomColorsWidget->setColorList(d_ptr->mCustomColors);
		updateGeometry();
		emitSelectedColor(c);
	}
}

/**
 * \if ENGLISH
 * @brief Handle none color action triggered
 * @param on Triggered state
 * \endif
 *
 * \if CHINESE
 * @brief 无颜色
 * @param on 触发状态
 * \endif
 */
void SAColorMenu::onNoneColorActionTriggered(bool on)
{
	Q_UNUSED(on);
	emitSelectedColor(QColor());
}

/*** End of inlined file: SAColorMenu.cpp ***/


/*** Start of inlined file: SAColorGridWidget.cpp ***/
#include <QGridLayout>
#include <cmath>
#include <QButtonGroup>

/**
 * \if ENGLISH
 * @brief Private data class for SAColorGridWidget
 * \endif
 *
 * \if CHINESE
 * @brief SAColorGridWidget的私有数据类
 * \endif
 */
class SAColorGridWidget::PrivateData
{
	SA_COLOR_WIDGETS_DECLARE_PUBLIC(SAColorGridWidget)
public:
	/**
	 * \if ENGLISH
	 * @brief Constructor for PrivateData
	 * @param p Parent SAColorGridWidget
	 * \endif
	 *
	 * \if CHINESE
	 * @brief PrivateData构造函数
	 * @param p 父SAColorGridWidget
	 * \endif
	 */
	PrivateData(SAColorGridWidget* p);

	/**
	 * \if ENGLISH
	 * @brief Get ColorToolButton by index
	 * @param index Index of the button
	 * @return SAColorToolButton at the specified index
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 根据索引获取ColorToolButton
	 * @param index 按钮的索引
	 * @return 指定索引处的SAColorToolButton
	 * \endif
	 */
	SAColorToolButton* getColorToolButtonAt(int index);

	/**
	 * \if ENGLISH
	 * @brief Get ColorToolButton by row and column
	 * @param r Row index
	 * @param c Column index
	 * @return SAColorToolButton at the specified position
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 根据行列获取ColorToolButton
	 * @param r 行索引
	 * @param c 列索引
	 * @return 指定位置的SAColorToolButton
	 * \endif
	 */
	SAColorToolButton* getColorToolButtonAt(int r, int c);

	/**
	 * \if ENGLISH
	 * @brief Get the currently checked button
	 * @return Currently checked SAColorToolButton
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取当前选中的按钮
	 * @return 当前选中的SAColorToolButton
	 * \endif
	 */
	SAColorToolButton* getCheckedButton() const;

	/**
	 * \if ENGLISH
	 * @brief Update grid layout based on mColors
	 * @param isRemoveSpacer Whether to remove spacer items
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 根据mColors更新布局
	 * @param isRemoveSpacer 是否移除间隔项
	 * \endif
	 */
	void updateGridColor(bool isRemoveSpacer = false);

	/**
	 * \if ENGLISH
	 * @brief Update color button sizes
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 更新颜色按钮大小
	 * \endif
	 */
	void updateGridColorSize();

	/**
	 * \if ENGLISH
	 * @brief Update color button checkable state
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 更新颜色按钮的可选中状态
	 * \endif
	 */
	void updateGridColorCheckable();

	/**
	 * \if ENGLISH
	 * @brief Iterate through all color buttons
	 * @param fn Function to apply to each button
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 遍历所有颜色按钮
	 * @param fn 应用于每个按钮的函数
	 * \endif
	 */
	void iterationColorBtns(SAColorGridWidget::FunColorBtn fn);

	/**
	 * \if ENGLISH
	 * @brief Remove widget at specified position
	 * @param r Row index
	 * @param c Column index
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 删除指定位置的部件
	 * @param r 行索引
	 * @param c 列索引
	 * \endif
	 */
	void removeAt(int r, int c);

	/**
	 * \if ENGLISH
	 * @brief Set color at specified position
	 * @param clr Color to set
	 * @param r Row index
	 * @param c Column index
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 在指定位置设置颜色
	 * @param clr 要设置的颜色
	 * @param r 行索引
	 * @param c 列索引
	 * \endif
	 */
	void setColorAt(const QColor& clr, int r, int c);

	/**
	 * \if ENGLISH
	 * @brief Check if specified position is a spacer
	 * @param r Row index
	 * @param c Column index
	 * @return true if the position is a spacer, false otherwise
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 检查指定位置是否是间隔
	 * @param r 行索引
	 * @param c 列索引
	 * @return 如果位置是间隔返回true，否则返回false
	 * \endif
	 */
	bool isSpacer(int r, int c) const;

public:
	QList< QColor > mColors;
	QGridLayout* mGridLayout { nullptr };
	QButtonGroup* mButtonGroup { nullptr };
	QSize mIconSize { 16, 16 };
	int mColumnCount { 8 };  ///< 列数，行数量会根据列数量来匹配,如果设置-1或者0，说明不限定列数量，这样会只有一行
	bool mColorCheckable;    ///< 设置颜色是否是checkable
	bool mHorizontalSpacerToRight { false };  ///< 最右边是否有弹簧
};

SAColorGridWidget::PrivateData::PrivateData(SAColorGridWidget* p) : q_ptr(p)
{
	mGridLayout = new QGridLayout(p);
	p->setLayout(mGridLayout);
	mGridLayout->setSpacing(0);
	mGridLayout->setContentsMargins(1, 1, 1, 1);
	mButtonGroup = new QButtonGroup(p);
	mButtonGroup->setExclusive(true);
	p->setMinimumHeight(mIconSize.height());
	p->setMinimumWidth(mIconSize.width());
}

SAColorToolButton* SAColorGridWidget::PrivateData::getColorToolButtonAt(int index)
{
	QLayoutItem* item = mGridLayout->itemAt(index);
	if (nullptr == item) {
		return nullptr;
	}
	return qobject_cast< SAColorToolButton* >(item->widget());
}

SAColorToolButton* SAColorGridWidget::PrivateData::getColorToolButtonAt(int r, int c)
{
	QLayoutItem* item = mGridLayout->itemAtPosition(r, c);
	if (nullptr == item) {
		return nullptr;
	}
	return qobject_cast< SAColorToolButton* >(item->widget());
}

SAColorToolButton* SAColorGridWidget::PrivateData::getCheckedButton() const
{
	return qobject_cast< SAColorToolButton* >(mButtonGroup->checkedButton());
}

/**
 * @brief 根据mColors更新布局
 */
void SAColorGridWidget::PrivateData::updateGridColor(bool isRemoveSpacer)
{
	int row = 1;
	int col = mColumnCount;
	if (col <= 0) {
		col = mColors.size();
	} else {
		row = std::ceil(mColors.size() / (float)col);
	}
	int index = 0;
	for (int r = 0; r < row; ++r) {
		for (int c = 0; c < col; ++c) {
			if (index < mColors.size()) {
				setColorAt(mColors[ index ], r, c);
				++index;
			} else {
				removeAt(r, c);
			}
		}
	}
	// 清除多余单元格
	int nowGridRow = mGridLayout->rowCount();
	int nowGridCol = mGridLayout->columnCount();
	if (nowGridRow > row) {
		// 多余的清除
		for (int r = row; r < nowGridRow; ++r) {
			for (int c = 0; c < nowGridCol; ++c) {
				removeAt(r, c);
			}
		}
	}
	if (nowGridCol > col) {
		// 多余的列清除
		for (int r = 0; r < row; ++r) {
			for (int c = col; c < nowGridCol; ++c) {
				if (isRemoveSpacer) {
					removeAt(r, c);
				} else {
					if (!isSpacer(r, c)) {
						removeAt(r, c);
					}
				}
			}
		}
	}
}

/**
 * @brief 更新colorsize
 */
void SAColorGridWidget::PrivateData::updateGridColorSize()
{
	QSize s = mIconSize;
	iterationColorBtns([ s ](SAColorToolButton* btn) {
		if (btn) {
			btn->setIconSize(s);
		}
	});
}

void SAColorGridWidget::PrivateData::updateGridColorCheckable()
{
	bool v = mColorCheckable;
	iterationColorBtns([ v ](SAColorToolButton* btn) {
		if (btn) {
			btn->setCheckable(v);
		}
	});
}

/**
 * @brief 遍历所有的button
 * @param fn
 */
void SAColorGridWidget::PrivateData::iterationColorBtns(FunColorBtn fn)
{
	int cnt = mGridLayout->count();
	for (int i = 0; i < cnt; ++i) {
		SAColorToolButton* tl = getColorToolButtonAt(i);
		fn(tl);
	}
}

/**
 * @brief 删除网格窗口
 * @param r
 * @param c
 */
void SAColorGridWidget::PrivateData::removeAt(int r, int c)
{
	QLayoutItem* item = mGridLayout->itemAtPosition(r, c);
	if (item) {
		QWidget* w = item->widget();
		mGridLayout->removeItem(item);
		delete item;
		if (w) {
			w->deleteLater();
		}
	}
}

void SAColorGridWidget::PrivateData::setColorAt(const QColor& clr, int r, int c)
{
	QLayoutItem* item = mGridLayout->itemAtPosition(r, c);
	if (item) {
		SAColorToolButton* tl = qobject_cast< SAColorToolButton* >(item->widget());
		if (tl) {
			tl->setColor(clr);
		}
	} else {
		SAColorToolButton* tl = new SAColorToolButton(SAColorToolButton::NoColorMenu, q_ptr);
		tl->setToolButtonStyle(Qt::ToolButtonIconOnly);
		tl->setIconSize(mIconSize);
		tl->setMargins(QMargins(4, 4, 4, 4));
		tl->setColor(clr);
		tl->setCheckable(mColorCheckable);
		tl->setAutoRaise(true);
		mButtonGroup->addButton(tl, r + c);
		mGridLayout->addWidget(tl, r, c);
	}
}

bool SAColorGridWidget::PrivateData::isSpacer(int r, int c) const
{
	QLayoutItem* item = mGridLayout->itemAtPosition(r, c);
	if (item) {
		if (QSpacerItem* si = dynamic_cast< QSpacerItem* >(item)) {
			return true;
		}
	}
	return false;
}

//==============================================================
// SAColorGridWidget
//==============================================================

/**
 * \if ENGLISH
 * @brief Constructor for SAColorGridWidget
 * @param par Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SAColorGridWidget构造函数
 * @param par 父部件
 * \endif
 */
SAColorGridWidget::SAColorGridWidget(QWidget* par) : QWidget(par), d_ptr(new SAColorGridWidget::PrivateData(this))
{
	connect(d_ptr->mButtonGroup,
			QOverload< QAbstractButton* >::of(&QButtonGroup::buttonClicked),
			this,
			&SAColorGridWidget::onButtonClicked);
	connect(d_ptr->mButtonGroup,
			QOverload< QAbstractButton* >::of(&QButtonGroup::buttonPressed),
			this,
			&SAColorGridWidget::onButtonPressed);
	connect(d_ptr->mButtonGroup,
			QOverload< QAbstractButton* >::of(&QButtonGroup::buttonReleased),
			this,
			&SAColorGridWidget::onButtonReleased);
	connect(d_ptr->mButtonGroup,
			QOverload< QAbstractButton*, bool >::of(&QButtonGroup::buttonToggled),
			this,
			&SAColorGridWidget::onButtonToggled);
}

/**
 * \if ENGLISH
 * @brief Destructor for SAColorGridWidget
 * \endif
 *
 * \if CHINESE
 * @brief SAColorGridWidget析构函数
 * \endif
 */
SAColorGridWidget::~SAColorGridWidget()
{
}

/**
 * \if ENGLISH
 * @brief Set column count, row count will be determined by column count
 * @details If set to -1 or 0, column count is not limited, resulting in a single row
 * @param c Column count
 * \endif
 *
 * \if CHINESE
 * @brief 设置列数，行数量会根据列数量来匹配,如果设置-1或者0，说明不限定列数量，这样会只有一行
 * @param c 列数
 * \endif
 */
void SAColorGridWidget::setColumnCount(int c)
{
	d_ptr->mColumnCount = c;
	d_ptr->updateGridColor(true);
	if (d_ptr->mHorizontalSpacerToRight) {
		setHorizontalSpacerToRight();
	}
	updateGeometry();
}

/**
 * \if ENGLISH
 * @brief Set color list
 * @param cls Color list
 * \endif
 *
 * \if CHINESE
 * @brief 设置颜色列表
 * @param cls 颜色列表
 * \endif
 */
void SAColorGridWidget::setColorList(const QList< QColor >& cls)
{
	d_ptr->mColors = cls;
	d_ptr->updateGridColor();
	updateGeometry();
}

/**
 * \if ENGLISH
 * @brief Get color list
 * @return Color list
 * \endif
 *
 * \if CHINESE
 * @brief 获取颜色列表
 * @return 颜色列表
 * \endif
 */
QList< QColor > SAColorGridWidget::getColorList() const
{
	return d_ptr->mColors;
}

/**
 * \if ENGLISH
 * @brief Get spacing
 * @return Spacing value
 * \endif
 *
 * \if CHINESE
 * @brief 获取间隔
 * @return 间隔值
 * \endif
 */
int SAColorGridWidget::spacing() const
{
	return d_ptr->mGridLayout->spacing();
}

/**
 * \if ENGLISH
 * @brief Set spacing
 * @param v Spacing value
 * \endif
 *
 * \if CHINESE
 * @brief 设置间隔
 * @param v 间隔值
 * \endif
 */
void SAColorGridWidget::setSpacing(int v)
{
	d_ptr->mGridLayout->setSpacing(v);
}

/**
 * \if ENGLISH
 * @brief Get color count
 * @return Number of colors
 * \endif
 *
 * \if CHINESE
 * @brief 获取颜色的数量
 * @return 颜色数量
 * \endif
 */
int SAColorGridWidget::colorCount() const
{
	return d_ptr->mColors.size();
}

/**
 * \if ENGLISH
 * @brief Set color icon size
 * @param s Icon size
 * \endif
 *
 * \if CHINESE
 * @brief 设置图标 size
 * @param s 图标大小
 * \endif
 */
void SAColorGridWidget::setColorIconSize(const QSize& s)
{
	d_ptr->mIconSize = s;
	setMinimumHeight(s.height());
	setMinimumWidth(s.width());
	d_ptr->updateGridColorSize();
}

/**
 * \if ENGLISH
 * @brief Get color icon size
 * @return Icon size
 * \endif
 *
 * \if CHINESE
 * @brief 获取图标 size
 * @return 图标大小
 * \endif
 */
QSize SAColorGridWidget::colorIconSize() const
{
	return d_ptr->mIconSize;
}

/**
 * \if ENGLISH
 * @brief Set whether colors are checkable
 * @details Checkable color buttons can be checked
 * @param on Checkable state
 * \endif
 *
 * \if CHINESE
 * @brief 设置颜色是否是checkable
 * @details checkable的颜色按钮是可checked的
 * @param on 可选中状态
 * \endif
 */
void SAColorGridWidget::setColorCheckable(bool on)
{
	d_ptr->mColorCheckable = on;
	d_ptr->updateGridColorCheckable();
}

/**
 * \if ENGLISH
 * @brief Check if colors are checkable
 * @return Checkable state
 * \endif
 *
 * \if CHINESE
 * @brief 颜色是否是checkable
 * @return 可选中状态
 * \endif
 */
bool SAColorGridWidget::isColorCheckable() const
{
	return d_ptr->mColorCheckable;
}

/**
 * \if ENGLISH
 * @brief Get currently checked color
 * @return Currently checked color
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前选中的颜色
 * @return 当前选中的颜色
 * \endif
 */
QColor SAColorGridWidget::currentCheckedColor() const
{
	QAbstractButton* btn = d_ptr->mButtonGroup->checkedButton();
	if (nullptr == btn) {
		return QColor();
	}
	SAColorToolButton* t = qobject_cast< SAColorToolButton* >(btn);
	if (nullptr == t) {
		return QColor();
	}
	return t->color();
}

/**
 * \if ENGLISH
 * @brief Get color button by index
 * @param index Button index
 * @return SAColorToolButton at the specified index, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 获取index对应的colorbutton
 * @param index 按钮索引
 * @return 如果没有返回nullptr
 * \endif
 */
SAColorToolButton* SAColorGridWidget::colorButton(int index) const
{
	return d_ptr->getColorToolButtonAt(index);
}

/**
 * \if ENGLISH
 * @brief Set vertical spacing (equivalent to GridLayout's VerticalSpacing property)
 * @param v Vertical spacing value
 * \endif
 *
 * \if CHINESE
 * @brief 等同GridLayout的VerticalSpacing属性
 * @param v 垂直间隔值
 * \endif
 */
void SAColorGridWidget::setVerticalSpacing(int v)
{
	d_ptr->mGridLayout->setVerticalSpacing(v);
}

/**
 * \if ENGLISH
 * @brief Get vertical spacing (equivalent to GridLayout's VerticalSpacing property)
 * @return Vertical spacing value
 * \endif
 *
 * \if CHINESE
 * @brief 等同GridLayout的VerticalSpacing属性
 * @return 垂直间隔值
 * \endif
 */
int SAColorGridWidget::verticalSpacing() const
{
	return d_ptr->mGridLayout->verticalSpacing();
}

/**
 * \if ENGLISH
 * @brief Set horizontal spacing (equivalent to GridLayout's HorizontalSpacing property)
 * @param v Horizontal spacing value
 * \endif
 *
 * \if CHINESE
 * @brief 等同GridLayout的HorizontalSpacing属性
 * @param v 水平间隔值
 * \endif
 */
void SAColorGridWidget::setHorizontalSpacing(int v)
{
	d_ptr->mGridLayout->setHorizontalSpacing(v);
}

/**
 * \if ENGLISH
 * @brief Get horizontal spacing (equivalent to GridLayout's HorizontalSpacing property)
 * @return Horizontal spacing value
 * \endif
 *
 * \if CHINESE
 * @brief 等同GridLayout的HorizontalSpacing属性
 * @return 水平间隔值
 * \endif
 */
int SAColorGridWidget::horizontalSpacing() const
{
	return d_ptr->mGridLayout->horizontalSpacing();
}

/**
 * \if ENGLISH
 * @brief Get column count
 * @return Column count
 * \endif
 *
 * \if CHINESE
 * @brief 获取列数
 * @return 列数
 * \endif
 */
int SAColorGridWidget::columnCount() const
{
	return d_ptr->mColumnCount;
}

/**
 * \if ENGLISH
 * @brief Clear checked state, no color will be selected
 * \endif
 *
 * \if CHINESE
 * @brief 清除选中状态，这时没有颜色是选中的
 * \endif
 */
void SAColorGridWidget::clearCheckedState()
{
	if (d_ptr->mButtonGroup->exclusive()) {
		SAColorToolButton* btn = d_ptr->getCheckedButton();
		if (btn) {
			d_ptr->mButtonGroup->setExclusive(false);
			btn->setChecked(false);
			d_ptr->mButtonGroup->setExclusive(true);
		}
	} else {
		d_ptr->iterationColorBtns([](SAColorToolButton* btn) {
			if (btn->isChecked()) {
				btn->setChecked(false);
			}
		});
	}
}

/**
 * \if ENGLISH
 * @brief Iterate through all color buttons
 * @param fn Function to apply to each button
 * \endif
 *
 * \if CHINESE
 * @brief 遍历所有颜色按钮
 * @param fn 应用于每个按钮的函数
 * \endif
 */
void SAColorGridWidget::iterationColorBtns(SAColorGridWidget::FunColorBtn fn)
{
	d_ptr->iterationColorBtns(fn);
}

/**
 * \if ENGLISH
 * @brief Set minimum height for a specific row
 * @param row Row index
 * @param minSize Minimum height
 * \endif
 *
 * \if CHINESE
 * @brief 设置指定行的最小高度
 * @param row 行索引
 * @param minSize 最小高度
 * \endif
 */
void SAColorGridWidget::setRowMinimumHeight(int row, int minSize)
{
	d_ptr->mGridLayout->setRowMinimumHeight(row, minSize);
}

/**
 * \if ENGLISH
 * @brief Set horizontal spacer to the right
 * @param on Whether to enable horizontal spacer
 * \endif
 *
 * \if CHINESE
 * @brief 设置右侧水平间隔
 * @param on 是否启用水平间隔
 * \endif
 */
void SAColorGridWidget::setHorizontalSpacerToRight(bool on)
{
	if (on) {
		QSpacerItem* horizontalSpacer = new QSpacerItem(40, 20, QSizePolicy::Expanding, QSizePolicy::Minimum);
		d_ptr->mGridLayout->addItem(horizontalSpacer, 0, d_ptr->mColumnCount, 1, 1);
	} else {
		d_ptr->removeAt(0, d_ptr->mColumnCount);
	}
	d_ptr->mHorizontalSpacerToRight = on;
}

/**
 * \if ENGLISH
 * @brief Handle button clicked event
 * @param btn Clicked button
 * \endif
 *
 * \if CHINESE
 * @brief 处理按钮点击事件
 * @param btn 被点击的按钮
 * \endif
 */
void SAColorGridWidget::onButtonClicked(QAbstractButton* btn)
{
	SAColorToolButton* t = qobject_cast< SAColorToolButton* >(btn);
	if (t) {
		Q_EMIT colorClicked(t->color());
	}
}

/**
 * \if ENGLISH
 * @brief Handle button pressed event
 * @param btn Pressed button
 * \endif
 *
 * \if CHINESE
 * @brief 处理按钮按下事件
 * @param btn 被按下的按钮
 * \endif
 */
void SAColorGridWidget::onButtonPressed(QAbstractButton* btn)
{
	SAColorToolButton* t = qobject_cast< SAColorToolButton* >(btn);
	if (t) {
		Q_EMIT colorPressed(t->color());
	}
}

/**
 * \if ENGLISH
 * @brief Handle button toggled event
 * @param btn Toggled button
 * @param on Toggle state
 * \endif
 *
 * \if CHINESE
 * @brief 处理按钮切换事件
 * @param btn 被切换的按钮
 * @param on 切换状态
 * \endif
 */
void SAColorGridWidget::onButtonToggled(QAbstractButton* btn, bool on)
{
	SAColorToolButton* t = qobject_cast< SAColorToolButton* >(btn);
	if (t) {
		Q_EMIT colorToggled(t->color(), on);
	}
}

/**
 * \if ENGLISH
 * @brief Get size hint
 * @return Recommended size
 * \endif
 *
 * \if CHINESE
 * @brief 获取尺寸提示
 * @return 推荐尺寸
 * \endif
 */
QSize SAColorGridWidget::sizeHint() const
{
	return d_ptr->mGridLayout->sizeHint();
	//    int w = d_ptr->mIconSize.width() + d_ptr->mGridLayout->verticalSpacing();
	//    int h = d_ptr->mIconSize.height();
	//    if (d_ptr->mColumnCount > 0) {
	//        h *= d_ptr->mColumnCount;
	//        if (!d_ptr->mColors.empty()) {
	//            int r = std::ceil(d_ptr->mColors.size() / (float)(d_ptr->mColumnCount));
	//            if (r > 0) {
	//                w *= r;
	//            }
	//        }
	//    }
	//    return QSize(w, h);
}

/**
 * \if ENGLISH
 * @brief Handle button released event
 * @param btn Released button
 * \endif
 *
 * \if CHINESE
 * @brief 处理按钮释放事件
 * @param btn 被释放的按钮
 * \endif
 */
void SAColorGridWidget::onButtonReleased(QAbstractButton* btn)
{
	SAColorToolButton* t = qobject_cast< SAColorToolButton* >(btn);
	if (t) {
		Q_EMIT colorReleased(t->color());
	}
}

namespace SA
{

/**
 * \if ENGLISH
 * @brief Get standard color list
 * @return Standard color list
 * \endif
 *
 * \if CHINESE
 * @brief 获取标准颜色列表
 * @return 标准颜色列表
 * \endif
 */
QList< QColor > getStandardColorList()
{
	static QList< QColor > s_standardColorList({ QColor(192, 0, 0),
												 QColor(255, 0, 0),
												 QColor(255, 192, 0),
												 QColor(255, 255, 0),
												 QColor(146, 208, 80),
												 QColor(0, 176, 80),
												 QColor(0, 176, 240),
												 QColor(0, 112, 192),
												 QColor(0, 32, 96),
												 QColor(112, 48, 160) });
	return s_standardColorList;
}
}

/*** End of inlined file: SAColorGridWidget.cpp ***/


/*** Start of inlined file: SAColorPaletteGridWidget.cpp ***/
// Qt
#include <QMenu>
#include <QWidgetAction>
#include <QVBoxLayout>
#include <QColorDialog>
// SA

/**
 * \if ENGLISH
 * @brief Private data class for SAColorPaletteGridWidget
 * \endif
 *
 * \if CHINESE
 * @brief SAColorPaletteGridWidget的私有数据类
 * \endif
 */
class SAColorPaletteGridWidget::PrivateData
{
	SA_COLOR_WIDGETS_DECLARE_PUBLIC(SAColorPaletteGridWidget)
public:
	/**
	 * \if ENGLISH
	 * @brief Constructor for PrivateData
	 * @param p Parent SAColorPaletteGridWidget
	 * \endif
	 *
	 * \if CHINESE
	 * @brief PrivateData构造函数
	 * @param p 父SAColorPaletteGridWidget
	 * \endif
	 */
	PrivateData(SAColorPaletteGridWidget* p);

	/**
	 * \if ENGLISH
	 * @brief Generate color palette
	 * @param clrList Color list
	 * @return Generated color palette
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 生成color palette
	 * @param clrList 颜色列表
	 * @return 生成的颜色面板
	 * \endif
	 */
	QList< QColor > makeColorPalette(const QList< QColor >& clrList) const;

public:
	QList< int > mFactor { 180, 160, 140, 75, 50 };  ///< palette的比例因子，将调用QColor的lighter函数执行
	QVBoxLayout* mLayout { nullptr };                ///< 垂直布局
	SAColorGridWidget* mMainColorList { nullptr };   ///< 这个用于显示标准颜色
	SAColorGridWidget* mPaletteColorGrid { nullptr };  ///< 这个用于生成3行亮色，2行暗色的palette
};

SAColorPaletteGridWidget::PrivateData::PrivateData(SAColorPaletteGridWidget* p) : q_ptr(p)
{
	mLayout = new QVBoxLayout(p);
	p->setLayout(mLayout);
	mMainColorList    = new SAColorGridWidget(p);
	mPaletteColorGrid = new SAColorGridWidget(p);
	mLayout->addWidget(mMainColorList);
	mLayout->addWidget(mPaletteColorGrid);
	mLayout->setContentsMargins(1, 1, 1, 1);
	mLayout->setSpacing(8);
	QSizePolicy sizePolicy(QSizePolicy::Maximum, QSizePolicy::Maximum);
	mMainColorList->setSizePolicy(sizePolicy);
	mPaletteColorGrid->setSizePolicy(sizePolicy);
	mMainColorList->setColumnCount(0);
	mPaletteColorGrid->setVerticalSpacing(0);
}

QList< QColor > SAColorPaletteGridWidget::PrivateData::makeColorPalette(const QList< QColor >& clrList) const
{
	QList< QColor > res;
	for (int f : sacolor_as_const(mFactor)) {
		for (const QColor& c : sacolor_as_const(clrList)) {
			res.append(c.lighter(f));
		}
	}
	return res;
}
//==============================================================
// SAColorPaletteGridWidget
//==============================================================

/**
 * \if ENGLISH
 * @brief Constructor for SAColorPaletteGridWidget with default standard colors
 * @param par Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 使用默认的标准颜色生成一个color palette
 * @param par 父部件
 * \endif
 */
SAColorPaletteGridWidget::SAColorPaletteGridWidget(QWidget* par) : QWidget(par), d_ptr(new PrivateData(this))
{
	init();
	setColorList(SA::getStandardColorList());
}

/**
 * \if ENGLISH
 * @brief Constructor for SAColorPaletteGridWidget with specified colors
 * @param cls Color list
 * @param par Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 根据指定的颜色生成一个color palette
 * @param cls 颜色列表
 * @param par 父部件
 * \endif
 */
SAColorPaletteGridWidget::SAColorPaletteGridWidget(const QList< QColor >& cls, QWidget* par)
	: QWidget(par), d_ptr(new PrivateData(this))
{
	init();
	setColorList(cls);
}

/**
 * \if ENGLISH
 * @brief Destructor for SAColorPaletteGridWidget
 * \endif
 *
 * \if CHINESE
 * @brief SAColorPaletteGridWidget析构函数
 * \endif
 */
SAColorPaletteGridWidget::~SAColorPaletteGridWidget()
{
}

/**
 * \if ENGLISH
 * @brief Initialize widget
 * \endif
 *
 * \if CHINESE
 * @brief 初始化部件
 * \endif
 */
void SAColorPaletteGridWidget::init()
{
	connect(d_ptr->mMainColorList, &SAColorGridWidget::colorClicked, this, &SAColorPaletteGridWidget::onMainColorClicked);
	connect(d_ptr->mPaletteColorGrid, &SAColorGridWidget::colorClicked, this, &SAColorPaletteGridWidget::onPaletteColorClicked);
	QSizePolicy sizePolicy(QSizePolicy::Maximum, QSizePolicy::Maximum);
	setSizePolicy(sizePolicy);
	setColorIconSize(QSize(10, 10));
}

/**
 * \if ENGLISH
 * @brief Set color list, the number of colors determines the number of main color cards
 * @details Palette color cards will be generated based on factor
 * @param cls Color list
 * \endif
 *
 * \if CHINESE
 * @brief 设置颜色系列，颜色系列的个数决定了主色卡的数量，palette色卡会根据factor进行生成
 * @param cls 颜色列表
 * \endif
 */
void SAColorPaletteGridWidget::setColorList(const QList< QColor >& cls)
{
	d_ptr->mMainColorList->setColumnCount(0);
	d_ptr->mMainColorList->setColorList(cls);
	d_ptr->mPaletteColorGrid->setColumnCount(cls.size());
	d_ptr->mPaletteColorGrid->setColorList(d_ptr->makeColorPalette(cls));
}

/**
 * \if ENGLISH
 * @brief Get color list
 * @return Color list
 * \endif
 *
 * \if CHINESE
 * @brief 获取颜色系列
 * @return 颜色列表
 * \endif
 */
QList< QColor > SAColorPaletteGridWidget::colorList() const
{
	return d_ptr->mMainColorList->getColorList();
}

/**
 * \if ENGLISH
 * @brief Set color shade factor, factor determines the number of palette rows
 * @details Default factor is { 180, 160, 140, 75, 50 }, which will generate 5 rows
 * @details Each factor will apply QColor::lighter operation to standard colors
 * @param factor Factor list
 * \endif
 *
 * \if CHINESE
 * @brief 设置颜色深浅比例factor，factor决定了palette的行数，factor有5个，就有5行
 * @details 默认为{ 180, 160, 140, 75, 50 },相当于会有5行，每个系数会对标准颜色执行QColor::lighter操作
 * @param factor 因子列表
 * \endif
 */
void SAColorPaletteGridWidget::setFactor(const QList< int >& factor)
{
	d_ptr->mFactor = factor;
	d_ptr->mPaletteColorGrid->setColorList(d_ptr->makeColorPalette(d_ptr->mMainColorList->getColorList()));
}

/**
 * \if ENGLISH
 * @brief Get factor list
 * @return Factor list
 * \endif
 *
 * \if CHINESE
 * @brief 获取色卡
 * @return 因子列表
 * \endif
 */
QList< int > SAColorPaletteGridWidget::factor() const
{
	return d_ptr->mFactor;
}

/**
 * \if ENGLISH
 * @brief Set color icon size, default is 10*10
 * @param s Icon size
 * \endif
 *
 * \if CHINESE
 * @brief 设置颜色块的尺寸，默认为10*10
 * @param s 图标大小
 * \endif
 */
void SAColorPaletteGridWidget::setColorIconSize(const QSize& s)
{
	d_ptr->mMainColorList->setColorIconSize(s);
	d_ptr->mPaletteColorGrid->setColorIconSize(s);
}

/**
 * \if ENGLISH
 * @brief Get color icon size
 * @return Icon size
 * \endif
 *
 * \if CHINESE
 * @brief 获取颜色块尺寸
 * @return 图标大小
 * \endif
 */
QSize SAColorPaletteGridWidget::colorIconSize() const
{
	return d_ptr->mMainColorList->colorIconSize();
}

/**
 * \if ENGLISH
 * @brief Set whether color blocks are checkable
 * @param on Checkable state
 * \endif
 *
 * \if CHINESE
 * @brief 设置颜色块是否能被checked
 * @param on 可选中状态
 * \endif
 */
void SAColorPaletteGridWidget::setColorCheckable(bool on)
{
	d_ptr->mMainColorList->setColorCheckable(on);
	d_ptr->mPaletteColorGrid->setColorCheckable(on);
}

/**
 * \if ENGLISH
 * @brief Check if color blocks are checkable
 * @return Checkable state
 * \endif
 *
 * \if CHINESE
 * @brief 检查颜色块是否可选中
 * @return 可选中状态
 * \endif
 */
bool SAColorPaletteGridWidget::isColorCheckable() const
{
	return d_ptr->mMainColorList->isColorCheckable();
}

/**
 * \if ENGLISH
 * @brief Handle main color clicked
 * @param c Clicked color
 * \endif
 *
 * \if CHINESE
 * @brief 处理主颜色点击
 * @param c 点击的颜色
 * \endif
 */
void SAColorPaletteGridWidget::onMainColorClicked(const QColor& c)
{
	d_ptr->mPaletteColorGrid->clearCheckedState();
	Q_EMIT colorClicked(c);
}

/**
 * \if ENGLISH
 * @brief Handle palette color clicked
 * @param c Clicked color
 * \endif
 *
 * \if CHINESE
 * @brief 处理调色板颜色点击
 * @param c 点击的颜色
 * \endif
 */
void SAColorPaletteGridWidget::onPaletteColorClicked(const QColor& c)
{
	d_ptr->mMainColorList->clearCheckedState();
	Q_EMIT colorClicked(c);
}

/*** End of inlined file: SAColorPaletteGridWidget.cpp ***/


/*** Start of inlined file: SAColorToolButton.cpp ***/
#include <QApplication>
#include <QPaintEvent>
#include <QStylePainter>
#include <QStyleOption>
#include <QStyleOptionToolButton>
#include <QResizeEvent>
#include <QDebug>

/**
 * \if ENGLISH
 * @brief Private data class for SAColorToolButton
 * \endif
 *
 * \if CHINESE
 * @brief SAColorToolButton的私有数据类
 * \endif
 */
class SAColorToolButton::PrivateData
{
	SA_COLOR_WIDGETS_DECLARE_PUBLIC(SAColorToolButton)
public:
	/**
	 * \if ENGLISH
	 * @brief Constructor for PrivateData
	 * @param p Parent SAColorToolButton
	 * \endif
	 *
	 * \if CHINESE
	 * @brief PrivateData构造函数
	 * @param p 父SAColorToolButton
	 * \endif
	 */
	PrivateData(SAColorToolButton* p);

	/**
	 * \if ENGLISH
	 * @brief Calculate sizes for icon-only tool button
	 * @param opt Style option
	 * @param iconRect Icon rectangle
	 * @param textRect Text rectangle
	 * @param colorRect Color rectangle
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 计算仅图标工具按钮的尺寸
	 * @param opt 样式选项
	 * @param iconRect 图标矩形
	 * @param textRect 文本矩形
	 * @param colorRect 颜色矩形
	 * \endif
	 */
	void calcSizeOfToolButtonIconOnly(const QStyleOptionToolButton& opt, QRect& iconRect, QRect& textRect, QRect& colorRect);

	/**
	 * \if ENGLISH
	 * @brief Calculate sizes for text-only tool button
	 * @param opt Style option
	 * @param iconRect Icon rectangle
	 * @param textRect Text rectangle
	 * @param colorRect Color rectangle
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 计算仅文本工具按钮的尺寸
	 * @param opt 样式选项
	 * @param iconRect 图标矩形
	 * @param textRect 文本矩形
	 * @param colorRect 颜色矩形
	 * \endif
	 */
	void calcSizeOfToolButtonTextOnly(const QStyleOptionToolButton& opt, QRect& iconRect, QRect& textRect, QRect& colorRect);

	/**
	 * \if ENGLISH
	 * @brief Calculate sizes for text-beside-icon tool button
	 * @param opt Style option
	 * @param iconRect Icon rectangle
	 * @param textRect Text rectangle
	 * @param colorRect Color rectangle
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 计算文本在图标旁边的工具按钮尺寸
	 * @param opt 样式选项
	 * @param iconRect 图标矩形
	 * @param textRect 文本矩形
	 * @param colorRect 颜色矩形
	 * \endif
	 */
	void calcSizeOfToolButtonTextBesideIcon(const QStyleOptionToolButton& opt, QRect& iconRect, QRect& textRect, QRect& colorRect);

	/**
	 * \if ENGLISH
	 * @brief Calculate sizes for text-under-icon tool button
	 * @param opt Style option
	 * @param iconRect Icon rectangle
	 * @param textRect Text rectangle
	 * @param colorRect Color rectangle
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 计算文本在图标下方的工具按钮尺寸
	 * @param opt 样式选项
	 * @param iconRect 图标矩形
	 * @param textRect 文本矩形
	 * @param colorRect 颜色矩形
	 * \endif
	 */
	void calcSizeOfToolButtonTextUnderIcon(const QStyleOptionToolButton& opt, QRect& iconRect, QRect& textRect, QRect& colorRect);

	/**
	 * \if ENGLISH
	 * @brief Create icon pixmap
	 * @param opt Style option
	 * @param iconRect Icon rectangle
	 * @return Created pixmap
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 创建图标像素图
	 * @param opt 样式选项
	 * @param iconRect 图标矩形
	 * @return 创建的像素图
	 * \endif
	 */
	QPixmap createIconPixmap(const QStyleOptionToolButton& opt, const QRect& iconRect);

	/**
	 * \if ENGLISH
	 * @brief Get button rectangle
	 * @param opt Style option
	 * @return Button rectangle
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取按钮的矩形区域
	 * @param opt 样式选项
	 * @return 按钮矩形
	 * \endif
	 */
	QRect getButtonRect(const QStyleOptionToolButton& opt);

	/**
	 * \if ENGLISH
	 * @brief Get indicator rectangle
	 * @param opt Style option
	 * @return Indicator rectangle
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取ToolButtonMenu的区域
	 * @param opt 样式选项
	 * @return 指示器矩形
	 * \endif
	 */
	QRect getIndicatorRect(const QStyleOptionToolButton& opt);

	/**
	 * \if ENGLISH
	 * @brief Get button style state
	 * @param opt Style option
	 * @return Button style state
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 基于Qt qcommonStyle源码，对QStyleOptionToolButton的style修正
	 * @param opt 样式选项
	 * @return 按钮样式状态
	 * \endif
	 */
	QStyle::State getButtonStyleState(const QStyleOptionToolButton& opt);

	/**
	 * \if ENGLISH
	 * @brief Get button menu style state
	 * @param opt Style option
	 * @return Button menu style state
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 基于Qt qcommonStyle源码，对QStyleOptionToolButton的style修正
	 * @param opt 样式选项
	 * @return 按钮菜单样式状态
	 * \endif
	 */
	QStyle::State getButtonMenuStyleState(const QStyleOptionToolButton& opt);

	/**
	 * \if ENGLISH
	 * @brief Remove menu
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 移除菜单
	 * \endif
	 */
	void removeMenu();

public:
	QColor mColor { Qt::white };
	int mSpacing { 2 };                                        ///< 间隔
	QMargins mMargins { 3, 3, 3, 3 };                          ///< box
	ColorToolButtonStyle mColorButtonStyle { WithColorMenu };  ///< 样式
	static int s_indicatorArrorWidth;                          ///< 菜单宽度
};

int SAColorToolButton::PrivateData::s_indicatorArrorWidth = 8;

SAColorToolButton::PrivateData::PrivateData(SAColorToolButton* p) : q_ptr(p)
{
}

void SAColorToolButton::PrivateData::calcSizeOfToolButtonIconOnly(const QStyleOptionToolButton& opt,
																  QRect& iconRect,
																  QRect& textRect,
																  QRect& colorRect)
{
	// 确定文本区域
	textRect         = QRect();
	QRect buttonRect = getButtonRect(opt);
	if (opt.icon.isNull()) {
		colorRect = buttonRect;
		iconRect  = QRect();
	} else {
		QSize tmpSize = opt.iconSize;
		if (tmpSize.isNull()) {
			tmpSize = QSize(16, 16);
		}
		tmpSize         = buttonRect.size().boundedTo(tmpSize);
		int colorHeight = tmpSize.height() / 4;
		int totalHeight = colorHeight + tmpSize.height() + mSpacing;
		if (totalHeight > buttonRect.height()) {
			// 过高，这时要通过高度反推tmpSize和colorHeight
			colorHeight = (buttonRect.height() - mSpacing) / 5;
			tmpSize.setHeight(colorHeight * 4);
			if (opt.iconSize.height() > 0) {
				tmpSize.setWidth(tmpSize.height() * opt.iconSize.width() / opt.iconSize.height());  // 等比例
			}
			tmpSize = buttonRect.size().boundedTo(tmpSize);
		}
		// 有icon，颜色位于图标下面
		int y = (buttonRect.height() - colorHeight - mSpacing - tmpSize.height()) / 2;  //(ButtonHeight-TotalHeight)/2
		int x = (buttonRect.width() - tmpSize.width()) / 2;
		iconRect  = QRect(buttonRect.left() + x, buttonRect.top() + y, tmpSize.width(), tmpSize.height());
		colorRect = QRect(iconRect.x(), iconRect.bottom() + mSpacing, iconRect.width(), colorHeight);
	}
}

/**
 * @brief 纯文本情况，左边有5像素显示颜色
 * @param opt
 * @param iconRect
 * @param textRect
 * @param colorRect 左边有5像素显示颜色
 */
void SAColorToolButton::PrivateData::calcSizeOfToolButtonTextOnly(const QStyleOptionToolButton& opt,
																  QRect& iconRect,
																  QRect& textRect,
																  QRect& colorRect)
{
	QRect buttonRect = getButtonRect(opt);
	iconRect         = QRect();
	QSize colorSize  = opt.iconSize;
	if (colorSize.isNull()) {
		colorSize = QSize(16, 16);
	}
	colorSize = buttonRect.size().boundedTo(colorSize);
	colorRect = QRect(buttonRect.left(),
					  buttonRect.top() + (buttonRect.height() - colorSize.height()) / 2,
					  colorSize.width(),
					  colorSize.height());
	textRect  = buttonRect.adjusted(colorRect.right() + mSpacing, 0, 0, 0);
}

void SAColorToolButton::PrivateData::calcSizeOfToolButtonTextBesideIcon(const QStyleOptionToolButton& opt,
																		QRect& iconRect,
																		QRect& textRect,
																		QRect& colorRect)
{
	QRect buttonRect = getButtonRect(opt);
	if (opt.icon.isNull()) {
		QSize colorSize = opt.iconSize;
		if (colorSize.isNull()) {
			colorSize = QSize(16, 16);
		}
		// 说明没有icon
		// 这时所有都是color
		// iconRect=Null
		colorSize = buttonRect.size().boundedTo(colorSize);
		iconRect  = QRect();
		colorRect = QRect(buttonRect.left(),
						  buttonRect.top() + (buttonRect.height() - colorSize.height()) / 2,
						  colorSize.width(),
						  colorSize.height());
		textRect  = buttonRect.adjusted(colorRect.width() + mSpacing, 0, 0, 0);
	} else {  // 有图标
		QSize tmpSize = opt.iconSize;
		if (tmpSize.isNull()) {
			tmpSize = QSize(16, 16);
		}
		tmpSize         = buttonRect.size().boundedTo(tmpSize);
		int colorHeight = tmpSize.height() / 4;
		int totalHeight = colorHeight + tmpSize.height() + mSpacing;
		if (totalHeight > buttonRect.height()) {
			// 过高，这时要通过高度反推tmpSize和colorHeight
			colorHeight = (buttonRect.height() - mSpacing) / 5;
			tmpSize.setHeight(colorHeight * 4);
			tmpSize.setWidth(tmpSize.height());  // 对于异形也设置为正方行
		}
		// 有icon，颜色位于图标下面
		int y = (buttonRect.height() - colorHeight - mSpacing - tmpSize.height()) / 2;  //(ButtonHeight-TotalHeight)/2
		iconRect  = QRect(buttonRect.left(), buttonRect.top() + y, tmpSize.width(), tmpSize.height());
		colorRect = QRect(iconRect.x(), iconRect.bottom() + mSpacing, iconRect.width(), colorHeight);
		textRect  = buttonRect.adjusted(iconRect.right() + mSpacing, 0, 0, 0);
	}
}

void SAColorToolButton::PrivateData::calcSizeOfToolButtonTextUnderIcon(const QStyleOptionToolButton& opt,
																	   QRect& iconRect,
																	   QRect& textRect,
																	   QRect& colorRect)
{
	QRect buttonRect = getButtonRect(opt);
	QSize tmpSize    = opt.iconSize;
	if (tmpSize.isNull()) {
		tmpSize = QSize(16, 16);
	}
	// 获取字体高度
	int textHeight = opt.fontMetrics.height();
	tmpSize        = buttonRect.size().boundedTo(tmpSize);
	if (opt.icon.isNull()) {
		int totalHeight = textHeight + opt.iconSize.height() + mSpacing;
		if (totalHeight < buttonRect.height()) {
			// 足够高
			colorRect = QRect(buttonRect.left() + (buttonRect.width() - tmpSize.width()) / 2,
							  buttonRect.top() + (buttonRect.height() - totalHeight) / 2,
							  tmpSize.width(),
							  opt.iconSize.height());
		} else {
			// 空间不足
			colorRect = QRect(buttonRect.left() + (buttonRect.width() - tmpSize.width()) / 2,
							  buttonRect.top() + mSpacing,
							  tmpSize.width(),
							  opt.iconSize.height());
		}
		iconRect = QRect();
		textRect = QRect(buttonRect.left(), colorRect.bottom() + mSpacing, buttonRect.width(), textHeight);

	} else {
		// 有图标
		int colorHeight = tmpSize.height() / 4;
		int totalHeight = textHeight + opt.iconSize.height() + colorHeight + 2 * mSpacing;
		if (totalHeight < buttonRect.height()) {
			// 高度空间足够
			// 先布置icon
			iconRect = QRect(buttonRect.left() + (buttonRect.width() - tmpSize.width()) / 2,
							 buttonRect.top() + (buttonRect.height() - totalHeight) / 2,
							 tmpSize.width(),
							 opt.iconSize.height());

		} else {
			// 空间不足
			iconRect = QRect(buttonRect.left() + (buttonRect.width() - tmpSize.width()) / 2,
							 buttonRect.top() + mSpacing,
							 tmpSize.width(),
							 opt.iconSize.height());
		}
		colorRect = QRect(iconRect.x(), iconRect.bottom() + mSpacing, iconRect.width(), colorHeight);
		textRect  = QRect(buttonRect.left(), colorRect.bottom() + mSpacing, buttonRect.width(), textHeight);
	}
}

QPixmap SAColorToolButton::PrivateData::createIconPixmap(const QStyleOptionToolButton& opt, const QRect& iconRect)
{
	if (opt.icon.isNull()) {
		return (QPixmap());
	}
	// 有图标
	QIcon::State state = opt.state & QStyle::State_On ? QIcon::On : QIcon::Off;
	QIcon::Mode mode;
	if (!(opt.state & QStyle::State_Enabled)) {
		mode = QIcon::Disabled;
	} else if ((opt.state & QStyle::State_MouseOver) && (opt.state & QStyle::State_AutoRaise)) {
		mode = QIcon::Active;
	} else {
		mode = QIcon::Normal;
	}
	// return (opt.icon.pixmap(this->window()->windowHandle(), opt.rect.size().boundedTo(realConSize), mode, state));
	return (opt.icon.pixmap(iconRect.size(), mode, state));
}

/**
 * @brief 获取按钮的矩形区域
 * @param opt
 * @return
 */
QRect SAColorToolButton::PrivateData::getButtonRect(const QStyleOptionToolButton& opt)
{
	QRect btnRect = q_ptr->style()->subControlRect(QStyle::CC_ToolButton, &opt, QStyle::SC_ToolButton, q_ptr);
	return btnRect.marginsRemoved(mMargins);
}

/**
 * @brief 获取ToolButtonMenu的区域
 * @note 注意，如果不是QStyleOptionToolButton::MenuButtonPopup|QStyleOptionToolButton::HasMenu下调用此函数，返回的是QRect()
 * @param opt
 * @return
 */
QRect SAColorToolButton::PrivateData::getIndicatorRect(const QStyleOptionToolButton& opt)
{
	if (opt.features & QStyleOptionToolButton::MenuButtonPopup || opt.features & QStyleOptionToolButton::HasMenu) {
		return q_ptr->style()->subControlRect(QStyle::CC_ToolButton, &opt, QStyle::SC_ToolButtonMenu, q_ptr);
	}
	return QRect();
}

/**
 * @brief 基于Qt qcommonStyle源码，对QStyleOptionToolButton的style修正
 * @param opt
 * @return
 */
QStyle::State SAColorToolButton::PrivateData::getButtonStyleState(const QStyleOptionToolButton& opt)
{
	QStyle::State bflags = opt.state & ~QStyle::State_Sunken;
	if (bflags & QStyle::State_AutoRaise) {
		if (!(bflags & QStyle::State_MouseOver) || !(bflags & QStyle::State_Enabled)) {
			bflags &= ~QStyle::State_Raised;
		}
	}
	if (opt.state & QStyle::State_Sunken) {
		if (opt.activeSubControls & QStyle::SC_ToolButton) {
			bflags |= QStyle::State_Sunken;
		}
	}
	return bflags;
}

/**
 * @brief 基于Qt qcommonStyle源码，对QStyleOptionToolButton的style修正
 * @param opt
 * @return
 */
QStyle::State SAColorToolButton::PrivateData::getButtonMenuStyleState(const QStyleOptionToolButton& opt)
{
	QStyle::State mflags = opt.state & ~QStyle::State_Sunken;
	if (mflags & QStyle::State_AutoRaise) {
		if (!(mflags & QStyle::State_MouseOver) || !(mflags & QStyle::State_Enabled)) {
			mflags &= ~QStyle::State_Raised;
		}
	}
	if (opt.state & QStyle::State_Sunken) {
		mflags |= QStyle::State_Sunken;
	}
	return mflags;
}

void SAColorToolButton::PrivateData::removeMenu()
{
	QMenu* m = q_ptr->menu();
	if (m) {
		q_ptr->setMenu(nullptr);
		m->deleteLater();
	}
}

//==============================================================
// SAColorToolButton
//==============================================================

/**
 * \if ENGLISH
 * @brief Constructor for SAColorToolButton
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SAColorToolButton构造函数
 * @param parent 父部件
 * \endif
 */
SAColorToolButton::SAColorToolButton(QWidget* parent) : QToolButton(parent), d_ptr(new PrivateData(this))
{
	setColorToolButtonStyle(WithColorMenu);
	connect(this, &QToolButton::clicked, this, &SAColorToolButton::onButtonClicked);
}

/**
 * \if ENGLISH
 * @brief Constructor for SAColorToolButton with style
 * @param style Color tool button style
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SAColorToolButton构造函数（带样式）
 * @param style 颜色工具按钮样式
 * @param parent 父部件
 * \endif
 */
SAColorToolButton::SAColorToolButton(ColorToolButtonStyle style, QWidget* parent)
	: QToolButton(parent), d_ptr(new PrivateData(this))
{
	setColorToolButtonStyle(style);
	connect(this, &QToolButton::clicked, this, &SAColorToolButton::onButtonClicked);
}

/**
 * \if ENGLISH
 * @brief Destructor for SAColorToolButton
 * \endif
 *
 * \if CHINESE
 * @brief SAColorToolButton析构函数
 * \endif
 */
SAColorToolButton::~SAColorToolButton()
{
}

/**
 * \if ENGLISH
 * @brief Get color
 * @return Current color
 * \endif
 *
 * \if CHINESE
 * @brief 获取颜色
 * @return 当前颜色
 * \endif
 */
QColor SAColorToolButton::color() const
{
	return d_ptr->mColor;
}

/**
 * \if ENGLISH
 * @brief Set margins
 * @param mg Margins
 * \endif
 *
 * \if CHINESE
 * @brief 设置Margins
 * @param mg 边距
 * \endif
 */
void SAColorToolButton::setMargins(const QMargins& mg)
{
	d_ptr->mMargins = mg;
	repaint();
}

/**
 * \if ENGLISH
 * @brief Get margins
 * @return Margins
 * \endif
 *
 * \if CHINESE
 * @brief 获取边距
 * @return 边距
 * \endif
 */
QMargins SAColorToolButton::margins() const
{
	return d_ptr->mMargins;
}

/**
 * \if ENGLISH
 * @brief Paint none color indication
 * @param p Painter
 * @param colorRect Color rectangle
 * \endif
 *
 * \if CHINESE
 * @brief 绘制无颜色表示
 * @param p  painter
 * @param colorRect 绘制的区域
 * \endif
 */
void SAColorToolButton::paintNoneColor(QPainter* p, const QRect& colorRect)
{
	p->save();
	QPen pen(Qt::red, 1, Qt::SolidLine, Qt::RoundCap);
	p->setPen(pen);
	p->setRenderHint(QPainter::SmoothPixmapTransform, true);
	p->setRenderHint(QPainter::Antialiasing, true);
	int ss = colorRect.width() / 3;
	p->drawLine(QPoint(colorRect.x() + ss, colorRect.bottom()), QPoint(colorRect.right() - ss, colorRect.top()));
	pen.setColor(Qt::black);
	p->setPen(pen);
	p->drawRect(colorRect);
	p->restore();
}

/**
 * \if ENGLISH
 * @brief Set color tool button style
 * @param s Style
 * \endif
 *
 * \if CHINESE
 * @brief 设置颜色按钮的样式
 * @param s 样式
 * \endif
 */
void SAColorToolButton::setColorToolButtonStyle(ColorToolButtonStyle s)
{
	if (NoColorMenu == s) {
		d_ptr->removeMenu();
	} else {
		createColorMenu();
	}
	d_ptr->mColorButtonStyle = s;
}

/**
 * \if ENGLISH
 * @brief Get color tool button style
 * @return Style
 * \endif
 *
 * \if CHINESE
 * @brief 颜色按钮的样式
 * @return 样式
 * \endif
 */
SAColorToolButton::ColorToolButtonStyle SAColorToolButton::colorToolButtonStyle() const
{
	return d_ptr->mColorButtonStyle;
}

/**
 * \if ENGLISH
 * @brief Get color menu
 * @note Note that this function may return nullptr if ColorToolButtonStyle is set to NoColorMenu or if a custom menu is set
 * @return SAColorMenu pointer, or nullptr if not available
 * \endif
 *
 * \if CHINESE
 * @brief 获取colorMenu
 * @note 注意，这个函数很有可能会返回nullptr，如果ColorToolButtonStyle设置为NoColorMenu或者自己设置了菜单，此函数返回nullptr
 * @return SAColorMenu指针，如果没有，返回nullptr
 * \endif
 */
SAColorMenu* SAColorToolButton::colorMenu() const
{
	return qobject_cast< SAColorMenu* >(menu());
}

/**
 * \if ENGLISH
 * @brief Create standard color menu
 * @return Created SAColorMenu
 * \endif
 *
 * \if CHINESE
 * @brief 建立标准的颜色菜单
 * @return 创建的SAColorMenu
 * \endif
 */
SAColorMenu* SAColorToolButton::createColorMenu()
{
	setPopupMode(QToolButton::InstantPopup);
	SAColorMenu* m = new SAColorMenu(this);
	m->enableNoneColorAction(true);
	QAction* customColor = m->customColorAction();
	if (customColor) {
		customColor->setIcon(QIcon(":/SARibbon/image/resource/define-color.svg"));
	}
	connect(m, &SAColorMenu::selectedColor, this, &SAColorToolButton::setColor);
	setMenu(m);
	return m;
}

/**
 * \if ENGLISH
 * @brief Set color
 * @note Will emit @sa colorChanged signal
 * @param c Color
 * \endif
 *
 * \if CHINESE
 * @brief 设置颜色
 * @note 会发射@sa colorChanged 信号
 * @param c 颜色
 * \endif
 */
void SAColorToolButton::setColor(const QColor& c)
{
	if (d_ptr->mColor != c) {
		d_ptr->mColor = c;
		repaint();
		Q_EMIT colorChanged(c);
	}
}

/**
 * \if ENGLISH
 * @brief Calculate positions of basic components
 * @param opt Style option
 * @param iconRect Icon rectangle
 * @param textRect Text rectangle
 * @param colorRect Color rectangle
 * \endif
 *
 * \if CHINESE
 * @brief 计算各个基本组件的位置
 * @param opt 样式选项
 * @param iconRect 图标区域
 * @param textRect 文本区域
 * @param colorRect 颜色区域
 * \endif
 */
void SAColorToolButton::calcRect(const QStyleOptionToolButton& opt, QRect& iconRect, QRect& textRect, QRect& colorRect)
{
	switch (opt.toolButtonStyle) {
	case Qt::ToolButtonTextOnly:
		d_ptr->calcSizeOfToolButtonTextOnly(opt, iconRect, textRect, colorRect);
		break;
	case Qt::ToolButtonTextBesideIcon:
		d_ptr->calcSizeOfToolButtonTextBesideIcon(opt, iconRect, textRect, colorRect);
		break;
	case Qt::ToolButtonTextUnderIcon:
		d_ptr->calcSizeOfToolButtonTextUnderIcon(opt, iconRect, textRect, colorRect);
		break;
	case Qt::ToolButtonFollowStyle:
	case Qt::ToolButtonIconOnly:
		d_ptr->calcSizeOfToolButtonIconOnly(opt, iconRect, textRect, colorRect);
		break;
	default:
		d_ptr->calcSizeOfToolButtonIconOnly(opt, iconRect, textRect, colorRect);
		break;
	}
}

/**
 * \if ENGLISH
 * @brief Handle paint event
 * @param e Paint event
 * \endif
 *
 * \if CHINESE
 * @brief 处理绘制事件
 * @param e 绘制事件
 * \endif
 */
void SAColorToolButton::paintEvent(QPaintEvent* e)
{
	Q_UNUSED(e);
	QStylePainter p(this);

	QStyleOptionToolButton opt;

	initStyleOption(&opt);
	QRect iconRect, colorRect, textRect;
	calcRect(opt, iconRect, textRect, colorRect);
	paintButton(&p, opt);
	// 绘制图标
	paintIcon(&p, iconRect, opt);
	// 绘制文字
	paintText(&p, textRect, opt);
	// 绘制颜色
	paintColor(&p, colorRect, d_ptr->mColor, opt);
}

/**
 * \if ENGLISH
 * @brief Handle resize event
 * @param e Resize event
 * \endif
 *
 * \if CHINESE
 * @brief 处理尺寸改变事件
 * @param e 尺寸改变事件
 * \endif
 */
void SAColorToolButton::resizeEvent(QResizeEvent* e)
{
	// 在resizeevent计算绘图所需的尺寸，避免在绘图过程中实时绘制提高效率
	QToolButton::resizeEvent(e);
}

/**
 * \if ENGLISH
 * @brief Override sizeHint
 * @return Recommended size
 * \endif
 *
 * \if CHINESE
 * @brief sizeHint重载
 * @return 推荐尺寸
 * \endif
 */
QSize SAColorToolButton::sizeHint() const
{
	ensurePolished();
	QStyleOptionToolButton opt;
	initStyleOption(&opt);
	int w = 0, h = 0;
	if (Qt::ToolButtonIconOnly == opt.toolButtonStyle || Qt::ToolButtonFollowStyle == opt.toolButtonStyle) {
		// 和文本无关
		w = opt.iconSize.width() + d_ptr->mMargins.left() + d_ptr->mMargins.right();
		h = opt.iconSize.height() + d_ptr->mMargins.top() + d_ptr->mMargins.bottom();
	} else if (Qt::ToolButtonTextOnly == opt.toolButtonStyle || Qt::ToolButtonTextBesideIcon == opt.toolButtonStyle) {
		QSize textSize = opt.fontMetrics.size(Qt::TextSingleLine | Qt::TextShowMnemonic, opt.text);
		textSize.setHeight(textSize.height() + 4);
		QSize iconSize = opt.iconSize;
		if (!opt.icon.isNull()) {
			// 有图标，要有iconsize高度的1/4给颜色
			iconSize.setHeight(iconSize.height() + iconSize.height() / 4 + d_ptr->mSpacing);
		}
		w = textSize.width() + d_ptr->mSpacing + iconSize.width() + d_ptr->mMargins.left() + d_ptr->mMargins.right();
		h = qMax(textSize.height(), iconSize.height()) + d_ptr->mMargins.top() + d_ptr->mMargins.bottom();
	} else if (Qt::ToolButtonTextUnderIcon == opt.toolButtonStyle) {
		QSize textSize = opt.fontMetrics.size(Qt::TextSingleLine | Qt::TextShowMnemonic, opt.text);
		textSize.setHeight(textSize.height() + 4);
		QSize iconSize = opt.iconSize;
		if (!opt.icon.isNull()) {
			// 有图标，要有iconsize高度的1/4给颜色
			iconSize.setHeight(iconSize.height() + iconSize.height() / 4 + d_ptr->mSpacing);
		}
		w = qMax(textSize.width(), iconSize.width()) + d_ptr->mMargins.left() + d_ptr->mMargins.right();
		h = textSize.height() + iconSize.height() + d_ptr->mSpacing + d_ptr->mMargins.top() + d_ptr->mMargins.bottom();
	}
	opt.rect.setSize(QSize(w, h));  // PM_MenuButtonIndicator depends on the height
	if (opt.features & QStyleOptionToolButton::MenuButtonPopup || opt.features & QStyleOptionToolButton::HasMenu) {
		w += style()->pixelMetric(QStyle::PM_MenuButtonIndicator, &opt, this);
	}
	//! Qt6.4 取消了QApplication::globalStrut
	return style()->sizeFromContents(QStyle::CT_ToolButton, &opt, QSize(w, h), this).expandedTo(QSize(2, 2));
}

/**
 * \if ENGLISH
 * @brief Handle button clicked
 * @param checked Checked state
 * \endif
 *
 * \if CHINESE
 * @brief 处理按钮点击
 * @param checked 选中状态
 * \endif
 */
void SAColorToolButton::onButtonClicked(bool checked)
{
	Q_EMIT colorClicked(color(), checked);
}

/**
 * \if ENGLISH
 * @brief Paint button
 * @param p Painter
 * @param opt Style option
 * \endif
 *
 * \if CHINESE
 * @brief 绘制按钮
 * @param p  painter
 * @param opt 样式选项
 * \endif
 */
void SAColorToolButton::paintButton(QStylePainter* p, const QStyleOptionToolButton& opt)
{
	bool autoRaise = opt.state & QStyle::State_AutoRaise;
	// 绘制按钮
	if (autoRaise) {
		style()->drawPrimitive(QStyle::PE_PanelButtonTool, &opt, p, this);
	} else {
		style()->drawPrimitive(QStyle::PE_PanelButtonBevel, &opt, p, this);
	}
	if (opt.features & QStyleOptionToolButton::MenuButtonPopup) {
		QStyleOption tool = opt;
		tool.state        = d_ptr->getButtonMenuStyleState(opt);
		tool.rect         = d_ptr->getIndicatorRect(opt);
		//        if (tool.state & (QStyle::State_Sunken | QStyle::State_On | QStyle::State_Raised)) {
		//            style()->drawPrimitive(QStyle::PE_IndicatorButtonDropDown, &tool, &p, this);
		//        }
		style()->drawPrimitive(QStyle::PE_IndicatorArrowDown, &tool, p, this);
	}

	// 绘制focus
	if (opt.state & QStyle::State_HasFocus) {
		QStyleOptionFocusRect fr;
		fr.QStyleOption::operator=(opt);
		fr.rect.adjust(3, 3, -3, -3);
		if (opt.features & QStyleOptionToolButton::MenuButtonPopup) {
			fr.rect.adjust(0, 0, style()->pixelMetric(QStyle::PM_MenuButtonIndicator, &opt, this), 0);
		}
		style()->drawPrimitive(QStyle::PE_FrameFocusRect, &fr, p, this);
	}
}

/**
 * \if ENGLISH
 * @brief Paint icon
 * @param p Painter
 * @param iconRect Icon rectangle
 * @param opt Style option
 * \endif
 *
 * \if CHINESE
 * @brief 绘制icon
 * @param p  painter
 * @param iconRect 图标矩形
 * @param opt 样式选项
 * \endif
 */
void SAColorToolButton::paintIcon(QStylePainter* p, const QRect& iconRect, const QStyleOptionToolButton& opt)
{
	if (!iconRect.isNull()) {
		QPixmap pm = d_ptr->createIconPixmap(opt, iconRect);
		style()->drawItemPixmap(p, iconRect, Qt::AlignCenter, pm);
	}
}

/**
 * \if ENGLISH
 * @brief Paint text
 * @param p Painter
 * @param textRect Text rectangle
 * @param opt Style option
 * \endif
 *
 * \if CHINESE
 * @brief 绘制文本
 * @param p  painter
 * @param textRect 文本矩形
 * @param opt 样式选项
 * \endif
 */
void SAColorToolButton::paintText(QStylePainter* p, const QRect& textRect, const QStyleOptionToolButton& opt)
{
	if (opt.text.isEmpty()) {
		return;
	}
	if (Qt::ToolButtonIconOnly == opt.toolButtonStyle) {
		return;
	}
	p->save();
	p->setFont(opt.font);
	int alignment = Qt::TextShowMnemonic;
	// 快捷键的下划线
	if (!style()->styleHint(QStyle::SH_UnderlineShortcut, &opt, this)) {
		alignment |= Qt::TextHideMnemonic;
	}
	alignment |= Qt::AlignHCenter | Qt::AlignVCenter;
	style()->drawItemText(p,
						  QStyle::visualRect(opt.direction, opt.rect, textRect),
						  alignment,
						  opt.palette,
						  opt.state & QStyle::State_Enabled,
						  opt.text,
						  QPalette::WindowText);
	p->restore();
}

/**
 * \if ENGLISH
 * @brief Paint color
 * @param p Painter
 * @param colorRect Color rectangle
 * @param color Color
 * @param opt Style option
 * \endif
 *
 * \if CHINESE
 * @brief 绘制color
 * @param p  painter
 * @param colorRect 颜色矩形
 * @param color 颜色
 * @param opt 样式选项
 * \endif
 */
void SAColorToolButton::paintColor(QStylePainter* p,
								   const QRect& colorRect,
								   const QColor& color,
								   const QStyleOptionToolButton& opt)
{
	Q_UNUSED(opt);
	if (colorRect.isNull()) {
		return;
	}
	// 绘制颜色
	if (color.isValid()) {
		p->fillRect(colorRect, color);
	} else {
		paintNoneColor(p, colorRect);
	}
}

/*** End of inlined file: SAColorToolButton.cpp ***/

//sa ribbon

/*** Start of inlined file: SARibbonUtil.cpp ***/
#include <QFile>
#include <QWidget>
#include <QDebug>
#include <QDir>
#include <QApplication>
#include <QScreen>
#include <QGuiApplication>
#include <QStyleHints>
#include <QSettings>
#include <QProcess>
#include <QRegularExpression>
#if QT_VERSION < QT_VERSION_CHECK(5, 14, 0)
#include <QWindow>
#else
#endif
namespace SA
{

/**
 * @brief makeColorVibrant 让颜色鲜艳
 * @param c 原来的颜色
 * @param saturationDelta 增加饱和度（上限255）
 * @param valueDelta 增加明度（上限255）
 * @return
 */
QColor makeColorVibrant(const QColor& c, int saturationDelta, int valueDelta)
{
	int h, s, v, a;
	c.getHsv(&h, &s, &v, &a);  // 分解HSV分量

	// 增加饱和度（上限255）
	s = qMin(s + saturationDelta, 255);
	// 增加明度（上限255）
	v = qMin(v + valueDelta, 255);

	return QColor::fromHsv(h, s, v, a);  // 重新生成颜色
}

/**
 * @brief 按照指定的新高度，保持宽高比缩放 QSize
 *
 * 此函数根据原始尺寸的宽高比，计算出在指定新高度下的对应宽度，
 * 并返回一个新的 QSize 对象。
 *
 * @param originalSize 原始尺寸。
 * @param newHeight    缩放后的新高度。
 * @return             按比例缩放后的 QSize。
 *
 * @par 示例：
 * @code
 * QSize original(800, 600);
 * QSize scaled = scaleSizeByHeight(original, 300);
 * // scaled 将是 (400, 300)
 * @endcode
 */
QSize scaleSizeByHeight(const QSize& originalSize, int newHeight)
{
	// 检查原始尺寸高度、宽度是否有效，以及目标高度是否有效
	if (originalSize.height() <= 0 || originalSize.width() < 0 || newHeight <= 0) {
		return QSize(0, 0);  // 无效输入返回零尺寸
	}

	// 计算宽高比并缩放
	float aspectRatio = static_cast< float >(originalSize.width()) / static_cast< float >(originalSize.height());
	int newWidth      = static_cast< int >(newHeight * aspectRatio);
	return QSize(newWidth, newHeight);
}

/**
 * @brief 按照指定的新高度，宽高比为1:factor缩放 QSize。
 *
 * 此函数根据原始尺寸的宽高比，计算出在指定新高度下的对应宽度，
 * 并返回一个新的 QSize 对象。
 *
 * @param originalSize 原始尺寸。
 * @param newHeight    缩放后的新高度。
 * @param factor    宽高比 1:factor factor=1时，此函数和scaleSizeByHeight的两参数版本一样，如果factor=0.5，则宽高比为1:0.5，也就是高度扩充2倍，宽度扩充1倍
 * @return             按比例缩放后的 QSize。
 *
 * @par 示例：
 * @code
 * QSize original(800, 600);
 * QSize scaled = scaleSizeByHeight(original, 300, 2);
 * // scaled 将是 (600, 300)
 * @endcode
 */
QSize scaleSizeByHeight(const QSize& originalSize, int newHeight, qreal factor)
{
	// 1. 参数合法性检查
	if (originalSize.height() <= 0 || originalSize.width() < 0 || newHeight <= 0 || qFuzzyIsNull(factor)) {
		return QSize(0, 0);
	}

	// 2. 计算原始宽高比
	const qreal originalAspect = static_cast< qreal >(originalSize.width()) / static_cast< qreal >(originalSize.height());

	// 3. 把用户提供的 1:factor 转换成真正的目标宽高比
	//    目标宽高比 = originalAspect / factor
	const qreal targetAspect = originalAspect / factor;

	// 4. 根据新高度反推宽度
	const int newWidth = qRound(newHeight * targetAspect);

	return QSize(newWidth, newHeight);
}

/**
 * @brief 按照指定的新宽度，保持宽高比缩放 QSize。
 *
 * 此函数根据原始尺寸的宽高比，计算出在指定新宽度下的对应高度，
 * 并返回一个新的 QSize 对象。
 *
 * @param originalSize 原始尺寸。
 * @param newWidth     缩放后的新宽度。
 * @return             按比例缩放后的 QSize。
 *
 * @par 示例：
 * @code
 * QSize original(800, 600);
 * QSize scaled = scaleSizeByWidth(original, 400);
 * // scaled 将是 (400, 300)
 * @endcode
 */
QSize scaleSizeByWidth(const QSize& originalSize, int newWidth)
{
	// 检查原始尺寸宽度、高度是否有效，以及目标宽度是否有效
	if (originalSize.width() <= 0 || originalSize.height() < 0 || newWidth <= 0) {
		return QSize(0, 0);  // 无效输入返回零尺寸
	}

	// 计算高宽比并缩放
	float aspectRatio = static_cast< float >(originalSize.height()) / static_cast< float >(originalSize.width());
	int newHeight     = static_cast< int >(newWidth * aspectRatio);
	return QSize(newWidth, newHeight);
}

/**
 * \if ENGLISH
 * @brief Get the complete QSS stylesheet string for a built-in ribbon theme
 * @details Loads the base QSS, theme template, and the default palette for the given theme,
 * then returns the fully resolved stylesheet with all {{token}} placeholders replaced.
 * Returns an empty string if the theme has no template or palette.
 * \endif
 *
 * \if CHINESE
 * @brief 获取指定内置ribbon主题的完整QSS样式表字符串
 * @details 加载基础QSS、主题模板和指定主题的默认调色板，返回所有{{token}}占位符已替换的完整样式表。
 * 如果主题没有模板或调色板，则返回空字符串。
 * \endif
 */
QString getBuiltInRibbonThemeQss(SARibbonTheme theme)
{
	// Load base QSS (common styles without colors)
	QFile baseFile(":/SARibbonTheme/resource/theme-base.qss");
	QString baseQss;
	if (baseFile.open(QIODevice::ReadOnly | QIODevice::Text)) {
		baseQss = QString::fromUtf8(baseFile.readAll());
	}

	// Resolve template resource path for the theme
	auto themeToTemplatePath = [](SARibbonTheme t) -> QString {
		switch (t) {
		case SARibbonTheme::RibbonThemeOffice2016Blue:
		case SARibbonTheme::RibbonThemeOffice2016Green:
		case SARibbonTheme::RibbonThemeOffice2016Dark:
			return ":/SARibbonTheme/resource/templates/office2016.qss";
		case SARibbonTheme::RibbonThemeOffice2021Blue:
		case SARibbonTheme::RibbonThemeOffice2021Green:
		case SARibbonTheme::RibbonThemeOffice2021Dark:
			return ":/SARibbonTheme/resource/templates/office2021.qss";
		case SARibbonTheme::RibbonThemeDark:
			return ":/SARibbonTheme/resource/templates/dark.qss";
		case SARibbonTheme::RibbonThemeDark2:
			return ":/SARibbonTheme/resource/templates/dark2.qss";
		case SARibbonTheme::RibbonThemeWindows7:
			return ":/SARibbonTheme/resource/templates/win7.qss";
		case SARibbonTheme::RibbonThemeOffice2013:
			return ":/SARibbonTheme/resource/templates/office2013.qss";
		default:
			return QString();
		}
	};

	// Resolve default palette resource path for the theme
	auto themeToPalettePath = [](SARibbonTheme t) -> QString {
		switch (t) {
		case SARibbonTheme::RibbonThemeOffice2016Blue:
			return ":/SARibbonTheme/resource/palettes/office2016-blue.json";
		case SARibbonTheme::RibbonThemeOffice2016Green:
			return ":/SARibbonTheme/resource/palettes/office2016-green.json";
		case SARibbonTheme::RibbonThemeOffice2016Dark:
			return ":/SARibbonTheme/resource/palettes/office2016-dark.json";
		case SARibbonTheme::RibbonThemeOffice2021Blue:
			return ":/SARibbonTheme/resource/palettes/office2021-blue.json";
		case SARibbonTheme::RibbonThemeOffice2021Green:
			return ":/SARibbonTheme/resource/palettes/office2021-green.json";
		case SARibbonTheme::RibbonThemeOffice2021Dark:
			return ":/SARibbonTheme/resource/palettes/office2021-dark.json";
		case SARibbonTheme::RibbonThemeDark:
			return ":/SARibbonTheme/resource/palettes/dark-default.json";
		case SARibbonTheme::RibbonThemeDark2:
			return ":/SARibbonTheme/resource/palettes/dark2-default.json";
		case SARibbonTheme::RibbonThemeWindows7:
			return ":/SARibbonTheme/resource/palettes/win7-default.json";
		case SARibbonTheme::RibbonThemeOffice2013:
			return ":/SARibbonTheme/resource/palettes/office2013-default.json";
		default:
			return QString();
		}
	};

	QString templatePath = themeToTemplatePath(theme);
	QString palettePath  = themeToPalettePath(theme);
	if (templatePath.isEmpty() || palettePath.isEmpty()) {
		return baseQss;
	}

	// Load the default palette
	SARibbonThemePalette palette;
	if (!palette.loadFromFile(palettePath)) {
		qWarning() << "getBuiltInRibbonThemeQss: failed to load palette" << palettePath;
		return baseQss;
	}

	// Load template and replace tokens
	QFile templateFile(templatePath);
	if (!templateFile.open(QIODevice::ReadOnly | QIODevice::Text)) {
		qWarning() << "getBuiltInRibbonThemeQss: failed to load template" << templatePath;
		return baseQss;
	}
	QString templateQss = QString::fromUtf8(templateFile.readAll());
	QString resolvedQss = replaceQssTokens(templateQss, palette);
	return baseQss + "\n" + resolvedQss;
}

/**
 * @brief 为 QIcon 生成指定 devicePixelRatio 的高分辨率 pixmap
 *
 * 之所以提供此函数，是因为 Qt5 的 QIcon::pixmap() 没有 devicePixelRatio 参数，而Qt6做了适配，为此专门提供一个兼容函数
 *
 * @param icon   图标源
 * @param size   期望的逻辑像素大小（控件坐标系）
 * @param devicePixelRatio    目标屏幕的实时 devicePixelRatio（可用 QWindow::devicePixelRatio() 获取）
 * @param mode   图标模式（Normal/Disabled/Active/Selected）
 * @param state  图标状态（On/Off）
 * @return 已设置好 devicePixelRatio 的 QPixmap，可直接 drawPixmap 使用
 */
QPixmap iconToPixmap(const QIcon& icon, const QSize& size, qreal devicePixelRatio, QIcon::Mode mode, QIcon::State state)
{
#if QT_VERSION >= QT_VERSION_CHECK(6, 0, 0)
	return icon.pixmap(size, devicePixelRatio, mode, state);
#else
	Q_UNUSED(devicePixelRatio);
	return icon.pixmap(size, mode, state);
#endif
}

/**
 * @brief 获取窗口当前所在屏幕的dpr
 * @param w
 * @return
 */
qreal widgetDevicePixelRatio(QWidget* w)
{
	if (!w) {
		return 1.0;
	}
	// 获取窗口所在的屏幕（优先当前窗口的屏幕）
	QScreen* sc = nullptr;

#if QT_VERSION < QT_VERSION_CHECK(5, 14, 0)
	if (QWindow* wh = w->windowHandle()) {
		sc = wh->screen();
	}
#else
	// 先获取窗口的顶层窗口（避免子部件直接调用screen()可能返回null的问题）
	QWidget* topWidget = w->window();
	if (topWidget) {
		sc = topWidget->screen();
	}
#endif
	if (!sc) {
		// qApp->primaryScreen() 拿到的是“整个系统里被用户标记成 primary 的那一块屏,是“全局主屏”
		sc = QApplication::primaryScreen();
	}
	if (!sc) {
		return 1.0;
	}
	return sc->devicePixelRatio();
}

/**
 * \if ENGLISH
 * @brief Check if the application layout direction is Right-to-Left (RTL)
 * @return true if layout direction is Qt::RightToLeft, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查应用程序布局方向是否为从右到左（RTL）
 * @return 如果布局方向为 Qt::RightToLeft 返回 true，否则返回 false
 * \endif
 */
bool saIsRTL()
{
	return QApplication::layoutDirection() == Qt::RightToLeft;
}

/**
 * \if ENGLISH
 * @brief Mirror X coordinate for RTL layout support
 * @param x The original X coordinate
 * @param containerWidth The width of the container
 * @param elementWidth The width of the element
 * @return containerWidth - x - elementWidth when RTL, x unchanged when LTR
 * \endif
 *
 * \if CHINESE
 * @brief 为 RTL 布局支持镜像 X 坐标
 * @param x 原始 X 坐标
 * @param containerWidth 容器宽度
 * @param elementWidth 元素宽度
 * @return RTL 时返回 containerWidth - x - elementWidth，LTR 时返回 x 不变
 * \endif
 */
int saMirrorX(int x, int containerWidth, int elementWidth)
{
	if (saIsRTL()) {
		return containerWidth - x - elementWidth;
	}
	return x;
}

/**
 * @brief Check if the operating system uses dark mode
 *
 * Detects dark mode via three tiers:
 * 1. Qt 6.5+: Uses QGuiApplication::styleHints()->colorScheme()
 * 2. Qt < 6.5 Windows: Reads registry AppsUseLightTheme
 * 3. Qt < 6.5 macOS: Runs 'defaults read -g AppleInterfaceStyle'
 * 4. Qt < 6.5 Linux: Runs 'gsettings get org.gnome.desktop.interface color-scheme'
 * 5. All other cases: Returns false (assume light mode)
 *
 * @return true if OS dark mode is active, false otherwise
 */
bool isOperatingSystemInDarkMode()
{
#if QT_VERSION >= QT_VERSION_CHECK(6, 5, 0)
	// Qt 6.5+ has native cross-platform API
	return QGuiApplication::styleHints()->colorScheme() == Qt::ColorScheme::Dark;
#elif defined(Q_OS_WIN32)
	// Windows: Read registry AppsUseLightTheme
	// DWORD value 0 = dark mode, 1 = light mode
	// Registry path: HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Themes\Personalize
	QSettings settings(
		"HKEY_CURRENT_USER\\Software\\Microsoft\\Windows\\CurrentVersion\\Themes\\Personalize",
		QSettings::NativeFormat);
	bool ok = false;
	int value = settings.value("AppsUseLightTheme", 1).toInt(&ok);
	return (ok && value == 0);
#elif defined(Q_OS_MACOS)
	// macOS: Run 'defaults read -g AppleInterfaceStyle'
	// Returns "Dark" if dark mode is active, empty otherwise
	QProcess process;
	process.start("/usr/bin/defaults", QStringList() << "read" << "-g" << "AppleInterfaceStyle");
	process.waitForFinished(3000);
	QString output = QString::fromUtf8(process.readAllStandardOutput()).trimmed();
	return output.contains("Dark", Qt::CaseInsensitive);
#elif defined(Q_OS_LINUX)
	// Linux: Try gsettings for GNOME 42+
	// Returns "dark" or "prefer-dark" in dark mode
	QProcess process;
	process.start("gsettings", QStringList() << "get" << "org.gnome.desktop.interface" << "color-scheme");
	process.waitForFinished(3000);
	QString output = QString::fromUtf8(process.readAllStandardOutput()).trimmed();
	return output.contains("dark", Qt::CaseInsensitive);
#else
	// Unknown platform or headless: assume light mode
	return false;
#endif
}

/**
 * @brief Replace {{token}} and {{token|opacity(value)}} patterns in QSS templates with actual color values
 * @param templateQss The QSS template string containing tokens
 * @param palette The theme palette providing color values
 * @return The QSS string with all tokens replaced
 */
QString replaceQssTokens(const QString& templateQss, const SARibbonThemePalette& palette)
{
	QString result = templateQss;

	QRegularExpression re("\\{\\{([^}|]+)(?:\\|opacity\\(([^)]+)\\))?\\}\\}");

	// First pass: collect all token matches and their replacements
	struct TokenMatch
	{
		qsizetype start;
		qsizetype length;
		QString replacement;
	};
	QVector<TokenMatch> matches;

	QRegularExpressionMatchIterator it = re.globalMatch(result);
	while (it.hasNext()) {
		QRegularExpressionMatch match  = it.next();
		QString           tokenName    = match.captured(1);
		QString           opacityStr   = match.captured(2);
		QString           raw          = palette.rawValue(tokenName);

		QString replacement;
		if (!raw.isEmpty()) {
			QColor color(raw);
			if (color.isValid()) {
				if (!opacityStr.isEmpty()) {
					bool  ok;
					float opacity = opacityStr.toFloat(&ok);
					if (ok) {
						int alpha       = qRound(opacity * 255);
						replacement = QString("#%1%2").arg(alpha, 2, 16, QChar('0')).arg(color.name().mid(1));
					} else {
						replacement = color.name();
					}
				} else {
					replacement = color.name();
				}
			} else {
				// Non-color raw string (e.g. qlineargradient(...)) — use directly
				replacement = raw;
			}
		}

		if (!replacement.isEmpty()) {
			matches.append({ match.capturedStart(), match.capturedLength(), replacement });
		}
	}

	// Apply replacements in reverse order to preserve offsets
	for (qsizetype i = matches.size() - 1; i >= 0; --i) {
		result.replace(matches[i].start, matches[i].length, matches[i].replacement);
	}

	// Scan for any unreplaced tokens and warn about them
	{
		QRegularExpression                unreplacedRe("\\{\\{([^}|]+)(?:\\|opacity\\([^)]+\\))?\\}\\}");
		QRegularExpressionMatchIterator   warnIt = unreplacedRe.globalMatch(result);
		while (warnIt.hasNext()) {
			QRegularExpressionMatch warnMatch = warnIt.next();
			qWarning() << "replaceQssTokens: unreplaced token:" << warnMatch.captured(1).trimmed();
		}
	}

	return result;
}

}

/*** End of inlined file: SARibbonUtil.cpp ***/



/*** Start of inlined file: SARibbonThemeManager.cpp ***/
#include <map>
#include <QMargins>
#include <QColor>
#include <QFile>
#include <QIODevice>
#include <QDir>
#include <QCoreApplication>
#include <QDebug>

namespace SA
{

// ===================================================
// Static theme data maps
// ===================================================

/// Tab margin per theme (affects SARibbonContextCategory drawing)
static const std::map< SARibbonTheme, QMargins > s_themeMargins = {
	{ SARibbonTheme::RibbonThemeWindows7, QMargins(5, 0, 0, 0) },
	{ SARibbonTheme::RibbonThemeOffice2013, QMargins(5, 0, 0, 0) },
	{ SARibbonTheme::RibbonThemeOffice2016Blue, QMargins(5, 0, 0, 0) },
	{ SARibbonTheme::RibbonThemeOffice2016Green, QMargins(5, 0, 0, 0) },
	{ SARibbonTheme::RibbonThemeOffice2016Dark, QMargins(5, 0, 0, 0) },
	{ SARibbonTheme::RibbonThemeDark, QMargins(5, 0, 0, 0) },
	{ SARibbonTheme::RibbonThemeDark2, QMargins(5, 0, 0, 0) },
	{ SARibbonTheme::RibbonThemeOffice2021Blue, QMargins(5, 0, 5, 0) },
	{ SARibbonTheme::RibbonThemeOffice2021Green, QMargins(5, 0, 5, 0) },
	{ SARibbonTheme::RibbonThemeOffice2021Dark, QMargins(5, 0, 5, 0) }
};

/// Highlight function: produce a darker variant of the context category color
static const SARibbonBar::FpContextCategoryHighlight s_csDarkerHighlight = [](const QColor& c) -> QColor {
	return c.darker();
};

/// Highlight function: produce a more vibrant variant of the context category color
static const SARibbonBar::FpContextCategoryHighlight s_csVibrantHighlight = [](const QColor& c) -> QColor {
	return SA::makeColorVibrant(c);
};

/// Context category highlight function per theme
static const std::map< SARibbonTheme, SARibbonBar::FpContextCategoryHighlight > s_themeContextHighlights = {
	{ SARibbonTheme::RibbonThemeWindows7, s_csVibrantHighlight },
	{ SARibbonTheme::RibbonThemeOffice2013, s_csVibrantHighlight },
	{ SARibbonTheme::RibbonThemeDark, s_csVibrantHighlight },
	{ SARibbonTheme::RibbonThemeOffice2016Blue, s_csDarkerHighlight },
	{ SARibbonTheme::RibbonThemeOffice2016Green, s_csDarkerHighlight },
	{ SARibbonTheme::RibbonThemeOffice2016Dark, s_csDarkerHighlight },
	{ SARibbonTheme::RibbonThemeOffice2021Blue, [](const QColor&) -> QColor { return QColor(39, 96, 167); } },
	{ SARibbonTheme::RibbonThemeDark2, s_csVibrantHighlight },
	{ SARibbonTheme::RibbonThemeOffice2021Green, s_csVibrantHighlight },
	{ SARibbonTheme::RibbonThemeOffice2021Dark, s_csVibrantHighlight }
};

/// Context category color list per theme
static const std::map< SARibbonTheme, QList< QColor > > s_themeContextColorLists = {
	{ SARibbonTheme::RibbonThemeWindows7, {} },
	{ SARibbonTheme::RibbonThemeOffice2013, {} },
	{ SARibbonTheme::RibbonThemeDark, {} },
	{ SARibbonTheme::RibbonThemeOffice2016Blue, { QColor(18, 64, 120) } },
	{ SARibbonTheme::RibbonThemeOffice2016Green, { QColor(24, 96, 48) } },
	{ SARibbonTheme::RibbonThemeOffice2016Dark, { QColor(60, 60, 60) } },
	{ SARibbonTheme::RibbonThemeOffice2021Blue, { QColor(209, 207, 209) } },
	{ SARibbonTheme::RibbonThemeDark2, { QColor(42, 141, 181) } },
	{ SARibbonTheme::RibbonThemeOffice2021Green, { QColor(180, 200, 180) } },
	{ SARibbonTheme::RibbonThemeOffice2021Dark, { QColor(80, 80, 80) } }
};

/// Tab bar baseline color per theme (only Office2013 has a visible baseline)
static const std::map< SARibbonTheme, QColor > s_themeBaselineColors = {
	{ SARibbonTheme::RibbonThemeWindows7, QColor() },
	{ SARibbonTheme::RibbonThemeOffice2013, QColor(186, 201, 219) },
	{ SARibbonTheme::RibbonThemeOffice2016Blue, QColor() },
	{ SARibbonTheme::RibbonThemeOffice2016Green, QColor() },
	{ SARibbonTheme::RibbonThemeOffice2016Dark, QColor() },
	{ SARibbonTheme::RibbonThemeOffice2021Blue, QColor() },
	{ SARibbonTheme::RibbonThemeDark, QColor() },
	{ SARibbonTheme::RibbonThemeDark2, QColor() },
	{ SARibbonTheme::RibbonThemeOffice2021Green, QColor() },
	{ SARibbonTheme::RibbonThemeOffice2021Dark, QColor() }
};

// ===================================================
// applyRibbonTheme
// ===================================================

/**
 * \if ENGLISH
 * @brief Apply a built-in ribbon theme to the widget and configure the ribbon bar's theme-dependent properties.
 *
 * This performs the full theme-switch sequence:
 * 1. Loads the QSS stylesheet for the theme onto @p w
 * 2. Adjusts the tab bar margins to match the theme's QSS layout
 * 3. Sets the context category color list for context tabs
 * 4. Registers the context category highlight function (controls tab color when selected)
 * 5. Sets the tab bar baseline color (visible only in Office2013)
 *
 * @param w The widget that receives the QSS stylesheet
 * @param bar The SARibbonBar whose theme-dependent properties are configured (may be null)
 * @param theme The built-in ribbon theme to apply
 * \endif
 *
 * \if CHINESE
 * @brief 对窗口部件应用内置ribbon主题，并配置ribbon栏的主题依赖属性。
 *
 * 此函数执行完整的主题切换序列：
 * 1. 将主题的 QSS 样式表加载到 @p w 上
 * 2. 根据主题调整标签栏边距以匹配 QSS 布局
 * 3. 设置上下文标签的颜色列表
 * 4. 注册上下文标签高亮函数（控制标签选中时的颜色）
 * 5. 设置标签栏基线颜色（仅在 Office2013 主题中可见）
 *
 * @param w 接收 QSS 样式表的窗口部件
 * @param bar 需要配置主题依赖属性的 SARibbonBar（可为 null）
 * @param theme 要应用的内置 ribbon 主题
 * \endif
 */
// Forward declaration — defined later in this file
static QString themeToPalettePath(SARibbonTheme theme);

void applyRibbonTheme(QWidget* w, SARibbonBar* bar, SARibbonTheme theme)
{
	SARibbonThemePalette palette;
	QString palettePath = themeToPalettePath(theme);
	if (!palettePath.isEmpty()) {
		if (palette.loadFromFile(palettePath)) {
			applyRibbonTheme(w, bar, theme, palette);
			return;
		}
		qWarning() << "applyRibbonTheme: failed to load palette" << palettePath
					 << "for theme" << static_cast<int>(theme) << "- falling back to built-in QSS";
	}
	applyRibbonTheme(w, bar, theme, SARibbonThemePalette());
}

/// Map SARibbonTheme enum to the corresponding QSS template resource path.
/// Returns empty string if no template exists for the given theme.
static QString themeToTemplatePath(SARibbonTheme theme)
{
	switch (theme) {
	case SARibbonTheme::RibbonThemeOffice2016Blue:
	case SARibbonTheme::RibbonThemeOffice2016Green:
	case SARibbonTheme::RibbonThemeOffice2016Dark:
		return ":/SARibbonTheme/resource/templates/office2016.qss";
	case SARibbonTheme::RibbonThemeOffice2021Blue:
	case SARibbonTheme::RibbonThemeOffice2021Green:
	case SARibbonTheme::RibbonThemeOffice2021Dark:
		return ":/SARibbonTheme/resource/templates/office2021.qss";
	case SARibbonTheme::RibbonThemeDark:
		return ":/SARibbonTheme/resource/templates/dark.qss";
	case SARibbonTheme::RibbonThemeDark2:
		return ":/SARibbonTheme/resource/templates/dark2.qss";
	case SARibbonTheme::RibbonThemeWindows7:
		return ":/SARibbonTheme/resource/templates/win7.qss";
	case SARibbonTheme::RibbonThemeOffice2013:
		return ":/SARibbonTheme/resource/templates/office2013.qss";
	default:
		return QString();
	}
}

/// Map SARibbonTheme enum to the corresponding default palette JSON resource path.
/// Returns empty string if no palette exists for the given theme.
static QString themeToPalettePath(SARibbonTheme theme)
{
	switch (theme) {
	case SARibbonTheme::RibbonThemeOffice2016Blue:
		return ":/SARibbonTheme/resource/palettes/office2016-blue.json";
	case SARibbonTheme::RibbonThemeOffice2016Green:
		return ":/SARibbonTheme/resource/palettes/office2016-green.json";
	case SARibbonTheme::RibbonThemeOffice2016Dark:
		return ":/SARibbonTheme/resource/palettes/office2016-dark.json";
	case SARibbonTheme::RibbonThemeOffice2021Blue:
		return ":/SARibbonTheme/resource/palettes/office2021-blue.json";
	case SARibbonTheme::RibbonThemeOffice2021Green:
		return ":/SARibbonTheme/resource/palettes/office2021-green.json";
	case SARibbonTheme::RibbonThemeOffice2021Dark:
		return ":/SARibbonTheme/resource/palettes/office2021-dark.json";
	case SARibbonTheme::RibbonThemeDark:
		return ":/SARibbonTheme/resource/palettes/dark-default.json";
	case SARibbonTheme::RibbonThemeDark2:
		return ":/SARibbonTheme/resource/palettes/dark2-default.json";
	case SARibbonTheme::RibbonThemeWindows7:
		return ":/SARibbonTheme/resource/palettes/win7-default.json";
	case SARibbonTheme::RibbonThemeOffice2013:
		return ":/SARibbonTheme/resource/palettes/office2013-default.json";
	default:
		return QString();
	}
}

/**
 * \if ENGLISH
 * @brief Apply a ribbon theme with custom color palette
 *
 * This overload loads the QSS template for the specified theme and replaces color tokens
 * using the provided palette. If the palette is empty or no template is found for the theme,
 * it falls back to loading the base QSS only (without theme-specific colors).
 *
 * After applying the stylesheet, the function also configures theme-dependent ribbon bar
 * properties: tab bar margins, context category colors, context category highlight function,
 * and tab bar baseline color.
 *
 * @param w The widget that receives the QSS stylesheet
 * @param bar The SARibbonBar whose theme-dependent properties are configured (may be null)
 * @param theme The built-in ribbon theme to apply
 * @param palette The color palette for token replacement in the QSS template
 * \endif
 *
 * \if CHINESE
 * @brief 使用自定义调色板应用ribbon主题
 *
 * 此重载加载指定主题的QSS模板，并使用提供的调色板替换颜色标记。
 * 如果调色板为空或主题没有对应的模板，则回退到仅加载基础QSS（不含主题特定颜色）。
 *
 * 应用样式表后，此函数还会配置主题依赖的ribbon栏属性：标签栏边距、上下文标签颜色、
 * 上下文标签高亮函数以及标签栏基线颜色。
 *
 * @param w 接收QSS样式表的窗口部件
 * @param bar 需要配置主题依赖属性的SARibbonBar（可为null）
 * @param theme 要应用的内置ribbon主题
 * @param palette 用于QSS模板中标记替换的调色板
 * \endif
 */
void applyRibbonTheme(QWidget* w, SARibbonBar* bar, SARibbonTheme theme,
					  const SARibbonThemePalette& palette)
{
	// If palette is provided and a template exists, use template-based approach
	QString templatePath = themeToTemplatePath(theme);
	if (palette.variables().size() > 0 && !templatePath.isEmpty() && w) {
		QFile baseFile(":/SARibbonTheme/resource/theme-base.qss");
		QString baseQss;
		if (baseFile.open(QIODevice::ReadOnly | QIODevice::Text)) {
			baseQss = QString::fromUtf8(baseFile.readAll());
		}

		// Load the QSS template
		QFile templateFile(templatePath);
		if (templateFile.open(QIODevice::ReadOnly | QIODevice::Text)) {
			QString templateQss = QString::fromUtf8(templateFile.readAll());
			// Replace {{token}} placeholders with palette colors
			QString processedQss = SA::replaceQssTokens(templateQss, palette);
			w->setStyleSheet(baseQss + "\n" + processedQss);
		} else {
			// Template file not found, fall back to base QSS
			qWarning() << "applyRibbonTheme: template not found:" << templatePath
					   << "- falling back to base QSS";
			QFile baseOnly(":/SARibbonTheme/resource/theme-base.qss");
			if (baseOnly.open(QIODevice::ReadOnly | QIODevice::Text)) {
				w->setStyleSheet(QString::fromUtf8(baseOnly.readAll()));
			}
		}
	} else if (w) {
		// Empty palette — fall back to base QSS only
		QFile baseQss(":/SARibbonTheme/resource/theme-base.qss");
		if (baseQss.open(QIODevice::ReadOnly | QIODevice::Text)) {
			w->setStyleSheet(QString::fromUtf8(baseQss.readAll()));
		}
	}

	if (!bar) {
		return;
	}

	// 2. Adjust tab bar margins to match the theme's QSS layout
	if (SARibbonTabBar* tab = bar->ribbonTabBar()) {
		auto itMargins = s_themeMargins.find(theme);
		if (itMargins != s_themeMargins.end()) {
			tab->setTabMargin(itMargins->second);
		}
	}

	// 3. Set context category color list
	{
		auto itColors = s_themeContextColorLists.find(theme);
		if (itColors != s_themeContextColorLists.end()) {
			bar->setContextCategoryColorList(itColors->second);
		}
	}

	// 4. Register context category highlight function
	{
		auto itHighlight = s_themeContextHighlights.find(theme);
		if (itHighlight != s_themeContextHighlights.end()) {
			bar->setContextCategoryColorHighLight(itHighlight->second);
		}
	}

	// 5. Set tab bar baseline color
	{
		auto itBaseline = s_themeBaselineColors.find(theme);
		if (itBaseline != s_themeBaselineColors.end()) {
			bar->setTabBarBaseLineColor(itBaseline->second);
		}
	}
}

}
/*** End of inlined file: SARibbonThemeManager.cpp ***/


/*** Start of inlined file: SARibbonThemePalette.cpp ***/
#include <QJsonDocument>
#include <QJsonObject>
#include <QFile>

namespace {
// Apply a lighten/darken derive operation to a base color; reverses direction for dark themes
QColor deriveColor(const QColor& base, const QString& fn, int amount, bool isDark)
{
	if (isDark) {
		// Reverse direction for dark themes
		if (fn == "darken") {
			return base.lighter(100 + amount);
		} else if (fn == "lighten") {
			return base.darker(100 + amount);
		}
	} else {
		if (fn == "darken") {
			return base.darker(100 + amount);
		} else if (fn == "lighten") {
			return base.lighter(100 + amount);
		}
	}
	return base;
}
} // anonymous namespace

namespace SA {

/**
 * \if ENGLISH
 * @brief Default constructor
 * @details Creates an empty palette. Call loadFromJson() or loadFromFile() to populate it.
 * \endif
 *
 * \if CHINESE
 * @brief 默认构造函数
 * @details 创建一个空调色板。调用loadFromJson()或loadFromFile()来填充颜色数据。
 * \endif
 */
SARibbonThemePalette::SARibbonThemePalette() = default;

/**
 * \if ENGLISH
 * @brief Load palette from a JSON file
 * @param[in] jsonPath Filesystem path or Qt resource path (e.g. ":/themes/light.json")
 * @return true if the file was read and parsed successfully, false otherwise
 * @details Opens the file in read-only text mode, reads all content, then delegates to loadFromJson().
 * \endif
 *
 * \if CHINESE
 * @brief 从JSON文件加载调色板
 * @param[in] jsonPath 文件系统路径或Qt资源路径（例如":/themes/light.json"）
 * @return 文件读取并解析成功返回true，否则返回false
 * @details 以只读文本模式打开文件，读取全部内容后委托给loadFromJson()处理。
 * \endif
 */
bool SARibbonThemePalette::loadFromFile(const QString& jsonPath)
{
	QFile file(jsonPath);
	if (!file.open(QIODevice::ReadOnly | QIODevice::Text)) {
		return false;
	}
	QByteArray data = file.readAll();
	file.close();
	return loadFromJson(data);
}

/**
 * \if ENGLISH
 * @brief Load palette from a JSON byte array
 * @param[in] json Raw JSON data containing the palette definition
 * @return true if the JSON was parsed successfully, false on parse or format error
 * @details The JSON object must contain the following top-level keys:
 * - "isDark": boolean indicating dark theme mode
 * - "keyColors": object mapping token names to hex color strings
 * - "derived": object mapping token names to derive rules (fn, base, amount)
 * - "fixed": object mapping token names to hex color strings
 * \endif
 *
 * \if CHINESE
 * @brief 从JSON字节数组加载调色板
 * @param[in] json 包含调色板定义的原始JSON数据
 * @return JSON解析成功返回true，解析或格式错误返回false
 * @details JSON对象必须包含以下顶层键：
 * - "isDark"：布尔值，表示是否为深色主题
 * - "keyColors"：将标记名映射到十六进制颜色字符串的对象
 * - "derived"：将标记名映射到派生规则（fn、base、amount）的对象
 * - "fixed"：将标记名映射到十六进制颜色字符串的对象
 * \endif
 */
bool SARibbonThemePalette::loadFromJson(const QByteArray& json)
{
	QJsonParseError error;
	QJsonDocument doc = QJsonDocument::fromJson(json, &error);
	if (error.error != QJsonParseError::NoError) {
		return false;
	}

	QJsonObject obj = doc.object();
	m_isDark = obj.value("isDark").toBool(false);

	QJsonObject keyColors = obj.value("keyColors").toObject();
	for (auto it = keyColors.begin(); it != keyColors.end(); ++it) {
		m_keyColors.insert(it.key(), QColor(it.value().toString()));
	}

	// Parse derived colors
	QJsonObject derived = obj.value("derived").toObject();
	for (auto it = derived.begin(); it != derived.end(); ++it) {
		QJsonObject rule = it.value().toObject();
		QString fn = rule.value("fn").toString();
		QString baseName = rule.value("base").toString();
		int amount = rule.value("amount").toInt(10);

		m_deriveRules.insert(it.key(), { fn, baseName, amount });
		if (m_keyColors.contains(baseName)) {
			QColor base = m_keyColors.value(baseName);
			QColor derivedC = ::deriveColor(base, fn, amount, m_isDark);
			m_derivedColors.insert(it.key(), derivedC);
		}
	}

	// Parse fixed colors — store non-color values (e.g. qlineargradient) as raw strings
	QJsonObject fixed = obj.value("fixed").toObject();
	for (auto it = fixed.begin(); it != fixed.end(); ++it) {
		QString value = it.value().toString();
		QColor c(value);
		if (c.isValid()) {
			m_fixedColors.insert(it.key(), c);
		} else if (!value.isEmpty()) {
			m_rawStrings.insert(it.key(), value);
		}
	}

	return true;
}

/**
 * \if ENGLISH
 * @brief Get a color by token name
 * @param[in] tokenName The color token name to look up
 * @return The resolved QColor, or an invalid QColor if the token is not found
 * @details Lookup order: derived colors first, then key colors, then fixed colors.
 * Derived colors take precedence so that overrides via derive rules always win.
 * \endif
 *
 * \if CHINESE
 * @brief 按标记名获取颜色
 * @param[in] tokenName 要查找的颜色标记名
 * @return 解析后的QColor，如果标记未找到则返回无效的QColor
 * @details 查找顺序：先查派生色，再查键色，最后查固定色。
 * 派生色优先，以确保通过派生规则设置的覆盖始终生效。
 * \endif
 */
QColor SARibbonThemePalette::color(const QString& tokenName) const
{
	if (m_derivedColors.contains(tokenName)) {
		return m_derivedColors.value(tokenName);
	}
	if (m_keyColors.contains(tokenName)) {
		return m_keyColors.value(tokenName);
	}
	if (m_fixedColors.contains(tokenName)) {
		return m_fixedColors.value(tokenName);
	}
	return QColor();
}

/**
 * \if ENGLISH
 * @brief Get a raw string value for a token name, supporting non-color CSS values
 * @param[in] tokenName The token name to look up
 * @return The raw string value (e.g. "qlineargradient(...)"), or the hex color name
 *         if the token is a color, or an empty QString if the token is not found
 * @details Lookup order: raw strings first, then derived colors, then key colors,
 * then fixed colors. Raw strings take highest priority since they represent
 * complex CSS values that cannot be expressed as a single color.
 * \endif
 *
 * \if CHINESE
 * @brief 获取标记名的原始字符串值，支持非颜色CSS值
 * @param[in] tokenName 要查找的标记名
 * @return 原始字符串值（例如"qlineargradient(...)"），如果标记是颜色则返回十六进制颜色名，
 *         如果标记未找到则返回空QString
 * @details 查找顺序：先查原始字符串，再查派生色，再查键色，最后查固定色。
 * 原始字符串优先级最高，因为它们代表无法用单一颜色表达的复杂CSS值。
 * \endif
 */
QString SARibbonThemePalette::rawValue(const QString& tokenName) const
{
	if (m_rawStrings.contains(tokenName)) {
		return m_rawStrings.value(tokenName);
	}
	if (m_derivedColors.contains(tokenName)) {
		return m_derivedColors.value(tokenName).name();
	}
	if (m_keyColors.contains(tokenName)) {
		return m_keyColors.value(tokenName).name();
	}
	if (m_fixedColors.contains(tokenName)) {
		return m_fixedColors.value(tokenName).name();
	}
	return QString();
}

/**
 * \if ENGLISH
 * @brief Get all color variables as name-to-hex-string pairs
 * @return A hash mapping token names to their hex color strings (e.g. "#ff0000")
 * @details Merges all three layers (key, derived, fixed) into a single hash.
 * Insertion order ensures derived colors take priority over key colors, and
 * key colors over fixed colors, matching the color() lookup order.
 * \endif
 *
 * \if CHINESE
 * @brief 获取所有颜色变量（名称到十六进制字符串的映射）
 * @return 将标记名映射到其十六进制颜色字符串（例如"#ff0000"）的哈希表
 * @details 将三个层（键色、派生色、固定色）合并到单个哈希表中。
 * 插入顺序确保派生色优先于键色，键色优先于固定色，与color()查找顺序一致。
 * \endif
 */
QHash<QString, QString> SARibbonThemePalette::variables() const
{
	QHash<QString, QString> vars;
	// Insert order matters for conflicts: last writer wins
	// Priority: rawStrings > derived > key > fixed (matches rawValue() lookup order)
	for (auto it = m_fixedColors.cbegin(); it != m_fixedColors.cend(); ++it) {
		vars.insert(it.key(), it.value().name());
	}
	for (auto it = m_keyColors.cbegin(); it != m_keyColors.cend(); ++it) {
		vars.insert(it.key(), it.value().name());
	}
	for (auto it = m_derivedColors.cbegin(); it != m_derivedColors.cend(); ++it) {
		vars.insert(it.key(), it.value().name());
	}
	for (auto it = m_rawStrings.cbegin(); it != m_rawStrings.cend(); ++it) {
		vars.insert(it.key(), it.value());
	}
	return vars;
}

/**
 * \if ENGLISH
 * @brief Check if this palette represents a dark theme
 * @return true if the palette was loaded with "isDark": true, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查此调色板是否表示深色主题
 * @return 如果加载的调色板中"isDark"为true，则返回true，否则返回false
 * \endif
 */
bool SARibbonThemePalette::isDark() const
{
	return m_isDark;
}

/// Recalculate all derived colors from stored rules and current key colors
void SARibbonThemePalette::recalculateDerived()
{
	m_derivedColors.clear();
	for (auto it = m_deriveRules.cbegin(); it != m_deriveRules.cend(); ++it) {
		const DeriveRule& rule = it.value();
		if (m_keyColors.contains(rule.base)) {
			QColor base = m_keyColors.value(rule.base);
			QColor derivedC = ::deriveColor(base, rule.fn, rule.amount, m_isDark);
			m_derivedColors.insert(it.key(), derivedC);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Set the accent key color
 * @param[in] color The new accent color
 * @details Overwrites the "accent" entry in the key colors layer.
 * \endif
 *
 * \if CHINESE
 * @brief 设置强调色（键色层）
 * @param[in] color 新的强调色
 * @details 覆盖键色层中的"accent"条目。
 * \endif
 */
void SARibbonThemePalette::setAccentColor(const QColor& color)
{
	m_keyColors.insert("accent", color);
	recalculateDerived();
}

/**
 * \if ENGLISH
 * @brief Set the content background key color
 * @param[in] color The new content background color
 * @details Overwrites the "content-bg" entry in the key colors layer.
 * \endif
 *
 * \if CHINESE
 * @brief 设置内容背景色（键色层）
 * @param[in] color 新的内容背景色
 * @details 覆盖键色层中的"content-bg"条目。
 * \endif
 */
void SARibbonThemePalette::setContentBgColor(const QColor& color)
{
	m_keyColors.insert("content-bg", color);
	recalculateDerived();
}

/**
 * \if ENGLISH
 * @brief Set the text key color
 * @param[in] color The new text color
 * @details Overwrites the "text-color" entry in the key colors layer.
 * \endif
 *
 * \if CHINESE
 * @brief 设置文字颜色（键色层）
 * @param[in] color 新的文字颜色
 * @details 覆盖键色层中的"text-color"条目。
 * \endif
 */
void SARibbonThemePalette::setTextColor(const QColor& color)
{
	m_keyColors.insert("text-color", color);
	recalculateDerived();
}

} // namespace SA

/*** End of inlined file: SARibbonThemePalette.cpp ***/


/*** Start of inlined file: SAFramelessHelper.cpp ***/
#include <QRect>
#include <QRubberBand>
#include <QMouseEvent>
#include <QHoverEvent>
#include <QApplication>
#include <QDebug>
#include <QScreen>
#include <QWindow>

class SAPrivateFramelessWidgetData;

/*****
 * FramelessHelperPrivate
 * 存储界面对应的数据集合，以及是否可移动、可缩放属性
 *****/
class SAFramelessHelper::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SAFramelessHelper)
	friend class SAPrivateFramelessWidgetData;

public:
	PrivateData(SAFramelessHelper* p);
	std::unique_ptr< SAPrivateFramelessWidgetData > m_widgetData;
	bool m_bWidgetMovable { true };
	bool m_bWidgetResizable { true };
	bool m_bRubberBandOnResize { true };
	bool m_bRubberBandOnMove { true };
};

SAFramelessHelper::PrivateData::PrivateData(SAFramelessHelper* p) : q_ptr(p)
{
}

/**
 * @brief 计算鼠标是否位于左、上、右、下、左上角、左下角、右上角、右下角。
 *
 * 该类用于计算鼠标相对于窗口边框的位置，以便确定是否可以进行缩放操作。
 */
class SAPrivateFramelessCursorPosCalculator
{
public:
	explicit SAPrivateFramelessCursorPosCalculator();
	// 重置鼠标位置状态
	void reset();
	// 根据全局鼠标位置和窗口矩形重新计算鼠标位置状态。
	void recalculate(const QPoint& globalMousePos, const QRect& frameRect);

public:
	bool mIsOnEdges { true };
	bool mIsOnLeftEdge { true };
	bool mIsOnRightEdge { true };
	bool mIsOnTopEdge { true };
	bool mIsOnBottomEdge { true };
	bool mIsOnTopLeftEdge { true };
	bool mIsOnBottomLeftEdge { true };
	bool mIsOnTopRightEdge { true };
	bool mIsOnBottomRightEdge { true };

	static int s_borderWidth;
	static int s_titleHeight;
};

int SAPrivateFramelessCursorPosCalculator::s_borderWidth = 5;
int SAPrivateFramelessCursorPosCalculator::s_titleHeight = 30;

/***** CursorPosCalculator *****/
SAPrivateFramelessCursorPosCalculator::SAPrivateFramelessCursorPosCalculator()
{
	reset();
}

/**
 * @brief 重置鼠标位置状态。
 *
 * 将所有鼠标位置标志重置为false。
 */
void SAPrivateFramelessCursorPosCalculator::reset()
{
	mIsOnEdges           = false;
	mIsOnLeftEdge        = false;
	mIsOnRightEdge       = false;
	mIsOnTopEdge         = false;
	mIsOnBottomEdge      = false;
	mIsOnTopLeftEdge     = false;
	mIsOnBottomLeftEdge  = false;
	mIsOnTopRightEdge    = false;
	mIsOnBottomRightEdge = false;
}

/**
 * @brief 根据全局鼠标位置和窗口矩形重新计算鼠标位置状态。
 * @param globalMousePos 全局鼠标位置。
 * @param frameRect 窗口的矩形区域。
 */
void SAPrivateFramelessCursorPosCalculator::recalculate(const QPoint& gMousePos, const QRect& frameRect)
{
	qreal dpiScale        = SAFramelessHelper::getScreenDpiScale(QApplication::widgetAt(gMousePos));
	int scaledBorderWidth = s_borderWidth * dpiScale;
	int globalMouseX      = gMousePos.x();
	int globalMouseY      = gMousePos.y();

	int frameX = frameRect.x();
	int frameY = frameRect.y();

	int frameWidth  = frameRect.width();
	int frameHeight = frameRect.height();

	mIsOnLeftEdge = (globalMouseX >= frameX && globalMouseX <= frameX + scaledBorderWidth);

	mIsOnRightEdge = (globalMouseX >= frameX + frameWidth - scaledBorderWidth && globalMouseX <= frameX + frameWidth);

	mIsOnTopEdge = (globalMouseY >= frameY && globalMouseY <= frameY + scaledBorderWidth);

	mIsOnBottomEdge = (globalMouseY >= frameY + frameHeight - scaledBorderWidth && globalMouseY <= frameY + frameHeight);

	mIsOnTopLeftEdge     = mIsOnTopEdge && mIsOnLeftEdge;
	mIsOnBottomLeftEdge  = mIsOnBottomEdge && mIsOnLeftEdge;
	mIsOnTopRightEdge    = mIsOnTopEdge && mIsOnRightEdge;
	mIsOnBottomRightEdge = mIsOnBottomEdge && mIsOnRightEdge;

	mIsOnEdges = mIsOnLeftEdge || mIsOnRightEdge || mIsOnTopEdge || mIsOnBottomEdge;
}

/**
 * @brief 更新鼠标样式、移动窗体、缩放窗体。
 *
 * 该类负责处理窗口的鼠标事件，包括鼠标移动、按下、释放等，以实现窗口的移动和缩放功能。
 */
class SAPrivateFramelessWidgetData
{
public:
	explicit SAPrivateFramelessWidgetData(SAFramelessHelper::PrivateData* pd, QWidget* pTopLevelWidget);
	~SAPrivateFramelessWidgetData();
	QWidget* widget();

	// 处理鼠标事件-划过、按下、释放、移动
	bool handleWidgetEvent(QEvent* event);

	// 更新橡皮筋状态
	void updateRubberBandStatus();

private:
	// 更新鼠标样式
	void updateCursorShape(const QPoint& gMousePos);

	// 重置窗体大小
	void resizeWidget(const QPoint& gMousePos);

	// 移动窗体
	void moveWidget(const QPoint& gMousePos);

	// 处理鼠标按下
	bool handleMousePressEvent(QMouseEvent* event);

	// 处理鼠标释放
	bool handleMouseReleaseEvent(QMouseEvent* event);

	// 处理鼠标移动
	bool handleMouseMoveEvent(QMouseEvent* event);

	// 处理鼠标离开
	bool handleLeaveEvent(QEvent* event);

	// 处理鼠标进入
	bool handleHoverMoveEvent(QHoverEvent* event);

	// 处理鼠标双击事件
	bool handleDoubleClickedMouseEvent(QMouseEvent* event);

private:
	SAFramelessHelper::PrivateData* d;
	QRubberBand* m_pRubberBand;
	QWidget* m_pWidget;
	QPoint m_ptDragPos;
	SAPrivateFramelessCursorPosCalculator m_pressedMousePos;
	SAPrivateFramelessCursorPosCalculator m_moveMousePos;
	bool m_bLeftButtonPressed;
	bool m_bCursorShapeChanged;
	bool m_bLeftButtonTitlePressed;
	Qt::WindowFlags m_windowFlags;
};

/***** WidgetData *****/
SAPrivateFramelessWidgetData::SAPrivateFramelessWidgetData(SAFramelessHelper::PrivateData* pd, QWidget* pTopLevelWidget)
{
	d                         = pd;
	m_pWidget                 = pTopLevelWidget;
	m_bLeftButtonPressed      = false;
	m_bCursorShapeChanged     = false;
	m_bLeftButtonTitlePressed = false;
	m_pRubberBand             = NULL;

	m_windowFlags = m_pWidget->windowFlags();
	m_pWidget->setMouseTracking(true);
	m_pWidget->setAttribute(Qt::WA_Hover, true);

	updateRubberBandStatus();
}

SAPrivateFramelessWidgetData::~SAPrivateFramelessWidgetData()
{
	m_pWidget->setMouseTracking(false);
	m_pWidget->setWindowFlags(m_windowFlags);
	m_pWidget->setAttribute(Qt::WA_Hover, false);

	delete m_pRubberBand;
	m_pRubberBand = NULL;
}

QWidget* SAPrivateFramelessWidgetData::widget()
{
	return (m_pWidget);
}

bool SAPrivateFramelessWidgetData::handleWidgetEvent(QEvent* event)
{
	switch (event->type()) {
	case QEvent::MouseButtonPress:
		return (handleMousePressEvent(static_cast< QMouseEvent* >(event)));

	case QEvent::MouseButtonRelease:
		return (handleMouseReleaseEvent(static_cast< QMouseEvent* >(event)));

	case QEvent::MouseMove:
		return (handleMouseMoveEvent(static_cast< QMouseEvent* >(event)));

	case QEvent::Leave:
		return (handleLeaveEvent(event));

	case QEvent::HoverMove:
		return (handleHoverMoveEvent(static_cast< QHoverEvent* >(event)));

	case QEvent::MouseButtonDblClick:
		return (handleDoubleClickedMouseEvent(static_cast< QMouseEvent* >(event)));

	default:
		break;
	}
	return (false);
}

void SAPrivateFramelessWidgetData::updateRubberBandStatus()
{
	if (d->m_bRubberBandOnMove || d->m_bRubberBandOnResize) {
		if (NULL == m_pRubberBand) {
			m_pRubberBand = new QRubberBand(QRubberBand::Rectangle);
		}
	} else {
		delete m_pRubberBand;
		m_pRubberBand = NULL;
	}
}

void SAPrivateFramelessWidgetData::updateCursorShape(const QPoint& gMousePos)
{
	if (m_pWidget->isFullScreen() || m_pWidget->isMaximized()) {
		if (m_bCursorShapeChanged) {
			m_pWidget->unsetCursor();
		}
		return;
	}

	m_moveMousePos.recalculate(gMousePos, m_pWidget->frameGeometry());

	if (m_moveMousePos.mIsOnTopLeftEdge || m_moveMousePos.mIsOnBottomRightEdge) {
		m_pWidget->setCursor(Qt::SizeFDiagCursor);
		m_bCursorShapeChanged = true;
	} else if (m_moveMousePos.mIsOnTopRightEdge || m_moveMousePos.mIsOnBottomLeftEdge) {
		m_pWidget->setCursor(Qt::SizeBDiagCursor);
		m_bCursorShapeChanged = true;
	} else if (m_moveMousePos.mIsOnLeftEdge || m_moveMousePos.mIsOnRightEdge) {
		m_pWidget->setCursor(Qt::SizeHorCursor);
		m_bCursorShapeChanged = true;
	} else if (m_moveMousePos.mIsOnTopEdge || m_moveMousePos.mIsOnBottomEdge) {
		m_pWidget->setCursor(Qt::SizeVerCursor);
		m_bCursorShapeChanged = true;
	} else {
		if (m_bCursorShapeChanged) {
			m_pWidget->unsetCursor();
			m_bCursorShapeChanged = false;
		}
	}
}

void SAPrivateFramelessWidgetData::resizeWidget(const QPoint& gMousePos)
{
	QRect origRect;
	if (d->m_bRubberBandOnResize) {
		origRect = m_pRubberBand->frameGeometry();
	} else {
		origRect = m_pWidget->frameGeometry();
	}

	int left   = origRect.left();
	int top    = origRect.top();
	int right  = origRect.right();
	int bottom = origRect.bottom();

	// origRect.getCoords(&left, &top, &right, &bottom);

	int minWidth  = m_pWidget->minimumWidth();
	int minHeight = m_pWidget->minimumHeight();

	if (m_pressedMousePos.mIsOnTopLeftEdge) {
		left = gMousePos.x();
		top  = gMousePos.y();
	} else if (m_pressedMousePos.mIsOnBottomLeftEdge) {
		left   = gMousePos.x();
		bottom = gMousePos.y();
	} else if (m_pressedMousePos.mIsOnTopRightEdge) {
		right = gMousePos.x();
		top   = gMousePos.y();
	} else if (m_pressedMousePos.mIsOnBottomRightEdge) {
		right  = gMousePos.x();
		bottom = gMousePos.y();
	} else if (m_pressedMousePos.mIsOnLeftEdge) {
		left = gMousePos.x();
	} else if (m_pressedMousePos.mIsOnRightEdge) {
		right = gMousePos.x();
	} else if (m_pressedMousePos.mIsOnTopEdge) {
		top = gMousePos.y();
	} else if (m_pressedMousePos.mIsOnBottomEdge) {
		bottom = gMousePos.y();
	}

	QRect newRect(QPoint(left, top), QPoint(right, bottom));

	if (newRect.isValid()) {
		if (minWidth > newRect.width()) {
			if (left != origRect.left()) {
				newRect.setLeft(origRect.left());
			} else {
				newRect.setRight(origRect.right());
			}
		}
		if (minHeight > newRect.height()) {
			if (top != origRect.top()) {
				newRect.setTop(origRect.top());
			} else {
				newRect.setBottom(origRect.bottom());
			}
		}

		if (d->m_bRubberBandOnResize) {
			m_pRubberBand->setGeometry(newRect);
		} else {
			m_pWidget->setGeometry(newRect);
		}
	}
}

void SAPrivateFramelessWidgetData::moveWidget(const QPoint& gMousePos)
{
	if (d->m_bRubberBandOnMove) {
		m_pRubberBand->move(gMousePos - m_ptDragPos);
	} else {
		m_pWidget->move(gMousePos - m_ptDragPos);
	}
}

/**
 * @brief 处理鼠标事件-划过、按下、释放、移动。
 * @param event 要处理的事件指针。
 * @return 如果事件被处理，则返回true；否则返回false。
 */
bool SAPrivateFramelessWidgetData::handleMousePressEvent(QMouseEvent* event)
{
	if (event->button() == Qt::LeftButton) {
		m_bLeftButtonPressed = true;

		qreal dpiScale        = SAFramelessHelper::getScreenDpiScale(m_pWidget);
		int scaledTitleHeight = SAPrivateFramelessCursorPosCalculator::s_titleHeight * dpiScale;
		// 这里要用eventPosY获取相对位置
		m_bLeftButtonTitlePressed = SA::compat::eventPosY(event) < scaledTitleHeight;

		QRect frameRect = m_pWidget->frameGeometry();
		auto gp         = SA::compat::eventGlobalPos(event);
		m_pressedMousePos.recalculate(gp, frameRect);

		m_ptDragPos = gp - frameRect.topLeft();
		if (m_pressedMousePos.mIsOnEdges) {
			if (m_pWidget->isMaximized()) {
				// 窗口在最大化状态时，点击边界不做任何处理
				return (false);
			}
			if (d->m_bRubberBandOnResize) {
				m_pRubberBand->setGeometry(frameRect);
				m_pRubberBand->show();
				return (true);
			}
		} else if (d->m_bRubberBandOnMove && m_bLeftButtonTitlePressed) {
			if (m_pWidget->isMaximized()) {
				// 窗口在最大化状态时，点击标题栏不做任何处理
				return (false);
			}
			m_pRubberBand->setGeometry(frameRect);
			m_pRubberBand->show();
			return (true);
		}
	}
	return (false);
}

bool SAPrivateFramelessWidgetData::handleMouseReleaseEvent(QMouseEvent* event)
{
	if (event->button() == Qt::LeftButton) {
		m_bLeftButtonPressed      = false;
		m_bLeftButtonTitlePressed = false;
		m_pressedMousePos.reset();
		if (m_pRubberBand && m_pRubberBand->isVisible()) {
			m_pRubberBand->hide();
			m_pWidget->setGeometry(m_pRubberBand->geometry());
			return (true);
		}
	}
	return (false);
}

bool SAPrivateFramelessWidgetData::handleMouseMoveEvent(QMouseEvent* event)
{
	QPoint globalpos = SA::compat::eventGlobalPos(event);
	if (m_bLeftButtonPressed) {
		if (d->m_bWidgetResizable && m_pressedMousePos.mIsOnEdges) {
			if (m_pWidget->isMaximized()) {
				// 窗口在最大化状态时，点击边界不做任何处理
				return (false);
			}
			resizeWidget(globalpos);
			return (true);
		} else if (d->m_bWidgetMovable && m_bLeftButtonTitlePressed) {
			if (m_pWidget->isMaximized()) {
				// 先求出窗口到鼠标的相对位置
				QRect normalGeometry = m_pWidget->normalGeometry();
				m_pWidget->showNormal();
				// 修改前：
				// p.ry() -= 10;
				// p.rx() -= (normalGeometry.width() / 2);

				// 修改后：使用DPI缩放偏移量
				qreal dpiScale = SAFramelessHelper::getScreenDpiScale(m_pWidget);
				globalpos.ry() -= 10 * dpiScale;  // 缩放偏移量
				globalpos.rx() -= (normalGeometry.width() / 2);

				m_pWidget->move(globalpos);
				// 这时要重置m_ptDragPos
				m_ptDragPos = QPoint(normalGeometry.width() / 2, 10);
				return (true);
			}

			bool isOutScreen          = true;
			QList< QScreen* > screens = QGuiApplication::screens();
			for (int i = 0; i < screens.size(); i++) {
				QScreen* pScreen   = screens[ i ];
				QRect geometryRect = pScreen->availableGeometry();
				if (geometryRect.contains(globalpos)) {
					isOutScreen = false;
					break;
				}
			}
			if (isOutScreen) {
				event->ignore();
				return false;
			}
			moveWidget(globalpos);
			return (true);
		}
		return (false);
	} else if (d->m_bWidgetResizable) {
		updateCursorShape(globalpos);
	}
	return (false);
}

bool SAPrivateFramelessWidgetData::handleLeaveEvent(QEvent* event)
{
	Q_UNUSED(event)
	if (!m_bLeftButtonPressed) {
		m_pWidget->unsetCursor();
		return (true);
	}
	return (false);
}

bool SAPrivateFramelessWidgetData::handleHoverMoveEvent(QHoverEvent* event)
{
	if (d->m_bWidgetResizable) {
#if QT_VERSION < QT_VERSION_CHECK(6, 0, 0)
		updateCursorShape(m_pWidget->mapToGlobal(event->pos()));
#else
		updateCursorShape(SA::compat::eventGlobalPos(event));
#endif
	}
	return (false);
}

bool SAPrivateFramelessWidgetData::handleDoubleClickedMouseEvent(QMouseEvent* event)
{
	if (event->button() == Qt::LeftButton) {
		if (m_pWidget) {
			SARibbonMainWindow* mainwindow = qobject_cast< SARibbonMainWindow* >(m_pWidget);
			if (mainwindow) {
				if (mainwindow->windowFlags() & Qt::WindowMaximizeButtonHint) {
					// 在最大化按钮显示时才进行shownormal处理

					// 修改后：考虑DPI缩放
					qreal dpiScale        = SAFramelessHelper::getScreenDpiScale(m_pWidget);
					int scaledTitleHeight = SAPrivateFramelessCursorPosCalculator::s_titleHeight * dpiScale;
					bool titlePressed     = SA::compat::eventPosY(event) < scaledTitleHeight;

					if (titlePressed) {
						if (m_pWidget->isMaximized()) {
							m_pWidget->showNormal();
						} else {
							m_pWidget->showMaximized();
						}
						return (true);
					}
				}
			}
		}
	}
	return (false);
}

//===================================================
// SAFramelessHelper
//===================================================
SAFramelessHelper::SAFramelessHelper(QObject* parent) : QObject(parent), d_ptr(new SAFramelessHelper::PrivateData(this))
{
	d_ptr->m_bWidgetMovable      = true;
	d_ptr->m_bWidgetResizable    = true;
	d_ptr->m_bRubberBandOnResize = false;
	d_ptr->m_bRubberBandOnMove   = false;
	if (parent) {
		QWidget* w = qobject_cast< QWidget* >(parent);
		if (w) {
			w->setWindowFlags(w->windowFlags() | Qt::FramelessWindowHint);
			setWidgetMovable(true);       // 设置窗体可移动
			setWidgetResizable(true);     // 设置窗体可缩放
			setRubberBandOnMove(false);   // 设置橡皮筋效果-可移动
			setRubberBandOnResize(true);  // 设置橡皮筋效果-可缩放
			activateOn(w);                // 激活当前窗体
		}
	}
}

SAFramelessHelper::~SAFramelessHelper()
{
}

bool SAFramelessHelper::eventFilter(QObject* obj, QEvent* event)
{
	switch (event->type()) {
	case QEvent::MouseMove:
	case QEvent::HoverMove:
	case QEvent::MouseButtonPress:
	case QEvent::MouseButtonRelease:
	case QEvent::MouseButtonDblClick:
	case QEvent::Leave: {
		if (d_ptr->m_widgetData) {
			return (d_ptr->m_widgetData->handleWidgetEvent(event));
		}
		break;
	}

	default:
		break;
	}
	return (QObject::eventFilter(obj, event));
}

/**
 * @brief 激活指定的顶级窗体
 * 使指定的顶级窗体能够使用 SAFramelessHelper 提供的无边框移动和缩放功能。
 * @param topLevelWidget 要激活的顶级窗体指针。
 */
void SAFramelessHelper::activateOn(QWidget* topLevelWidget)
{
	d_ptr->m_widgetData.reset(new SAPrivateFramelessWidgetData(d_ptr.get(), topLevelWidget));
	topLevelWidget->installEventFilter(this);
}

/**
 * @brief 从指定的顶级窗体移除帮助功能
 * 停止对指定顶级窗体的无边框移动和缩放功能支持。
 * @param topLevelWidget 要移除功能的顶级窗体指针。
 */
void SAFramelessHelper::removeFrom(QWidget* topLevelWidget)
{
	d_ptr->m_widgetData.reset(nullptr);
	topLevelWidget->removeEventFilter(this);
}

void SAFramelessHelper::setRubberBandOnMove(bool movable)
{
	d_ptr->m_bRubberBandOnMove = movable;
	if (d_ptr->m_widgetData) {
		d_ptr->m_widgetData->updateRubberBandStatus();
	}
}

void SAFramelessHelper::setWidgetMovable(bool movable)
{
	d_ptr->m_bWidgetMovable = movable;
}

void SAFramelessHelper::setWidgetResizable(bool resizable)
{
	d_ptr->m_bWidgetResizable = resizable;
}

void SAFramelessHelper::setRubberBandOnResize(bool resizable)
{
	d_ptr->m_bRubberBandOnResize = resizable;
	if (d_ptr->m_widgetData) {
		d_ptr->m_widgetData->updateRubberBandStatus();
	}
}

void SAFramelessHelper::setBorderWidth(int width)
{
	if (width > 0) {
		SAPrivateFramelessCursorPosCalculator::s_borderWidth = width;
	}
}

void SAFramelessHelper::setTitleHeight(int height)
{
	if (height > 0) {
		SAPrivateFramelessCursorPosCalculator::s_titleHeight = height;
	}
}

bool SAFramelessHelper::widgetMovable()
{
	return (d_ptr->m_bWidgetMovable);
}

bool SAFramelessHelper::widgetResizable()
{
	return (d_ptr->m_bWidgetResizable);
}

bool SAFramelessHelper::rubberBandOnMove()
{
	return (d_ptr->m_bRubberBandOnMove);
}

bool SAFramelessHelper::rubberBandOnResisze()
{
	return (d_ptr->m_bRubberBandOnResize);
}

uint SAFramelessHelper::borderWidth()
{
	return (SAPrivateFramelessCursorPosCalculator::s_borderWidth);
}

uint SAFramelessHelper::titleHeight()
{
	return (SAPrivateFramelessCursorPosCalculator::s_titleHeight);
}

qreal SAFramelessHelper::getScreenDpiScale(const QWidget* widget)
{
	if (!widget) {
		if (QApplication::primaryScreen()) {
			return QApplication::primaryScreen()->devicePixelRatio();
		}
		return 1.0;
	}

	QWindow* window = widget->windowHandle();
	if (!window) {
		const QWidget* nativeParent = widget->nativeParentWidget();
		if (nativeParent) {
			window = nativeParent->windowHandle();
		}
	}

	if (window && window->screen()) {
		return window->screen()->devicePixelRatio();
	}

	if (QApplication::primaryScreen()) {
		return QApplication::primaryScreen()->devicePixelRatio();
	}

	return 1.0;
}

/*** End of inlined file: SAFramelessHelper.cpp ***/


/*** Start of inlined file: SARibbonApplicationButton.cpp ***/
/**
 * \if ENGLISH
 * @brief Constructs a SARibbonApplicationButton instance
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个 SARibbonApplicationButton 实例
 * @param parent 父窗口部件
 * \endif
 */
SARibbonApplicationButton::SARibbonApplicationButton(QWidget* parent) : QToolButton(parent)
{
	setFocusPolicy(Qt::NoFocus);
	setAutoRaise(true);
	setPopupMode(QToolButton::InstantPopup);
	setToolButtonStyle(Qt::ToolButtonTextBesideIcon);
	setMinimumWidth(40);
}

/**
 * \if ENGLISH
 * @brief Constructs a SARibbonApplicationButton instance with text
 * @param text Button text
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个带有文本的 SARibbonApplicationButton 实例
 * @param text 按钮文本
 * @param parent 父窗口部件
 * \endif
 */
SARibbonApplicationButton::SARibbonApplicationButton(const QString& text, QWidget* parent) : QToolButton(parent)
{
	setFocusPolicy(Qt::NoFocus);
	setAutoRaise(true);
	setPopupMode(QToolButton::InstantPopup);
	setToolButtonStyle(Qt::ToolButtonTextBesideIcon);
	setText(text);
	setMinimumWidth(40);
}

/**
 * \if ENGLISH
 * @brief Constructs a SARibbonApplicationButton instance with icon and text
 * @param icon Button icon
 * @param text Button text
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个带有图标和文本的 SARibbonApplicationButton 实例
 * @param icon 按钮图标
 * @param text 按钮文本
 * @param parent 父窗口部件
 * \endif
 */
SARibbonApplicationButton::SARibbonApplicationButton(const QIcon& icon, const QString& text, QWidget* parent)
	: QToolButton(parent)
{
	setFocusPolicy(Qt::NoFocus);
	setAutoRaise(true);
	setPopupMode(QToolButton::InstantPopup);
	setToolButtonStyle(Qt::ToolButtonTextBesideIcon);
	setIcon(icon);
	setText(text);
	setMinimumWidth(40);
}

/**
 * \if ENGLISH
 * @brief Destructor
 * \endif
 *
 * \if CHINESE
 * @brief 析构函数
 * \endif
 */
SARibbonApplicationButton::~SARibbonApplicationButton()
{
}

/*** End of inlined file: SARibbonApplicationButton.cpp ***/


/*** Start of inlined file: SARibbonSystemButtonBar.cpp ***/
#include <QToolButton>
#include <QResizeEvent>
#include <QStyle>
#include <QDebug>
#include <QScopedPointer>
#include <QWindowStateChangeEvent>

// 为了避免使用此框架的app设置了全局的qpushbutton 的 qss样式影响此按钮，定义了一个类

/**
 * @brief The SARibbonSystemButtonBar class
 */
class SARibbonSystemButtonBar::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonSystemButtonBar)
public:
	SARibbonSystemToolButton* buttonClose { nullptr };
	SARibbonSystemToolButton* buttonMinimize { nullptr };
	SARibbonSystemToolButton* buttonMaximize { nullptr };
	int mCloseStretch { 4 };
	int mMaxStretch { 3 };
	int mMinStretch { 3 };
	int mWindowButtonWidth { 35 };
	int mTitleBarHeight { 28 };
	Qt::WindowFlags mFlags { Qt::WindowMinMaxButtonsHint | Qt::WindowCloseButtonHint };
	SARibbonButtonGroupWidget* mButtonGroup;

public:
	PrivateData(SARibbonSystemButtonBar* p) : q_ptr(p)
	{
		mButtonGroup = new SARibbonButtonGroupWidget(p);
		mButtonGroup->setObjectName("SASystemButtonGroup");
	}

	void setupMinimizeButton(bool on)
	{
		SARibbonSystemButtonBar* par = q_ptr;

		if (on) {
			if (buttonMinimize) {
				buttonMinimize->deleteLater();
				buttonMinimize = nullptr;
			}
			buttonMinimize = new SARibbonSystemToolButton(par);
			buttonMinimize->setObjectName(QStringLiteral("SAMinimizeWindowButton"));
			buttonMinimize->setFocusPolicy(Qt::NoFocus);  // 避免铺抓到
			buttonMinimize->show();
			par->connect(buttonMinimize, &QAbstractButton::clicked, par, &SARibbonSystemButtonBar::minimizeWindow);
		} else {
			if (buttonMinimize) {
				buttonMinimize->deleteLater();
				buttonMinimize = nullptr;
			}
		}
		updateSize();
	}

	void setupMaximizeButton(bool on)
	{
		SARibbonSystemButtonBar* par = q_ptr;

		if (on) {
			if (buttonMaximize) {
				buttonMaximize->deleteLater();
				buttonMaximize = nullptr;
			}
			buttonMaximize = new SARibbonSystemToolButton(par);
			buttonMaximize->setObjectName(QStringLiteral("SAMaximizeWindowButton"));
			buttonMaximize->setCheckable(true);
			buttonMaximize->setFocusPolicy(Qt::NoFocus);  // 避免铺抓到
			//            buttonMaximize->setIconSize(buttonMaximize->size() * mIconscale);
			buttonMaximize->show();
			par->connect(buttonMaximize, &QAbstractButton::clicked, par, &SARibbonSystemButtonBar::maximizeWindow);
		} else {
			if (buttonMaximize) {
				buttonMaximize->deleteLater();
				;
				buttonMaximize = nullptr;
			}
		}
		updateSize();
	}

	void setupCloseButton(bool on)
	{
		SARibbonSystemButtonBar* par = q_ptr;

		if (on) {
			if (buttonClose) {
				buttonClose->deleteLater();
				buttonClose = nullptr;
			}
			buttonClose = new SARibbonSystemToolButton(par);
			buttonClose->setObjectName(QStringLiteral("SACloseWindowButton"));
			buttonClose->setFocusPolicy(Qt::NoFocus);  // 避免铺抓到
			// buttonClose->setFlat(true);
			par->connect(buttonClose, &QAbstractButton::clicked, par, &SARibbonSystemButtonBar::closeWindow);
			//            buttonClose->setIconSize(buttonClose->size() * mIconscale);
			buttonClose->show();
		} else {
			if (buttonClose) {
				buttonClose->deleteLater();
				;
				buttonClose = nullptr;
			}
		}
		updateSize();
	}

	void updateSize()
	{
		resizeElement(q_ptr->size());
	}

	/**
	 * \if ENGLISH
	 * @brief Resizes and positions all elements in the button bar
	 * @param size New size of the button bar
	 * @details Handles both LTR and RTL layouts: in LTR, system buttons are positioned at the right edge;
	 *          in RTL, system buttons are positioned at the left edge while maintaining the same order (min → max → close)
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 调整按钮栏中所有元素的大小和位置
	 * @param size 按钮栏的新尺寸
	 * @details 同时处理LTR和RTL布局：在LTR模式下，系统按钮位于右边缘；
	 *          在RTL模式下，系统按钮位于左边缘，同时保持相同的顺序（最小化→最大化→关闭）
	 * \endif
	 */
	void resizeElement(QSize size)
	{
		if (SA::saIsRTL()) {
			int x = 0;
			if (buttonMinimize) {
				int w = minButtonWidthHint();
				buttonMinimize->setGeometry(x, 0, w, size.height());
				x += w;
			}
			if (buttonMaximize) {
				int w = maxButtonWidthHint();
				buttonMaximize->setGeometry(x, 0, w, size.height());
				x += w;
			}
			if (buttonClose) {
				int w = closeButtonWidthHint();
				buttonClose->setGeometry(x, 0, w, size.height());
				x += w;
			}
			if (mButtonGroup) {
				mButtonGroup->setGeometry(x, 0, size.width() - x, size.height());
			}
		} else {
			int x = size.width();
			if (buttonClose) {
				int w = closeButtonWidthHint();
				x -= w;
				buttonClose->setGeometry(x, 0, w, size.height());
			}
			if (buttonMaximize) {
				int w = maxButtonWidthHint();
				x -= w;
				buttonMaximize->setGeometry(x, 0, w, size.height());
			}
			if (buttonMinimize) {
				int w = minButtonWidthHint();
				x -= w;
				buttonMinimize->setGeometry(x, 0, w, size.height());
			}
			if (mButtonGroup) {
				mButtonGroup->setGeometry(0, 0, x, size.height());
			}
		}
	}

	int closeButtonWidthHint() const
	{
		qreal t = mCloseStretch + mMaxStretch + mMinStretch;
		return (mCloseStretch * (3 * mWindowButtonWidth)) / t;
	}

	int maxButtonWidthHint() const
	{
		qreal t = mCloseStretch + mMaxStretch + mMinStretch;
		return (mMaxStretch * (3 * mWindowButtonWidth)) / t;
	}

	int minButtonWidthHint() const
	{
		qreal t = mCloseStretch + mMaxStretch + mMinStretch;
		return (mMinStretch * (3 * mWindowButtonWidth)) / t;
	}

	QSize sizeHint() const
	{
		int height = mTitleBarHeight;
		if (height < 20) {
			height = 20;
		}
		QSize res(0, 0);
		if (mButtonGroup) {
			res = mButtonGroup->sizeHint();
		}
		res.setHeight(height);
		if (buttonClose) {
			res.setWidth(res.width() + closeButtonWidthHint());
		}
		if (buttonMaximize) {
			res.setWidth(res.width() + maxButtonWidthHint());
		}
		if (buttonMinimize) {
			res.setWidth(res.width() + minButtonWidthHint());
		}
		return res;
	}
};

//===================================================
// SARibbonSystemToolButton
//===================================================
/**
 * \if ENGLISH
 * @brief Constructs a SARibbonSystemToolButton instance
 * @param p Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个 SARibbonSystemToolButton 实例
 * @param p 父窗口部件
 * \endif
 */
SARibbonSystemToolButton::SARibbonSystemToolButton(QWidget* p) : QToolButton(p)
{
	setAutoRaise(true);
}
//===================================================
// SARibbonSystemButtonBar
//===================================================
/**
 * \if ENGLISH
 * @brief Constructs a SARibbonSystemButtonBar instance
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个 SARibbonSystemButtonBar 实例
 * @param parent 父窗口部件
 * \endif
 */
SARibbonSystemButtonBar::SARibbonSystemButtonBar(QWidget* parent)
	: QFrame(parent), d_ptr(new SARibbonSystemButtonBar::PrivateData(this))
{
	updateWindowFlag();
}

/**
 * \if ENGLISH
 * @brief Constructor that forces the use of flags instead of the parent's flags
 * @param parent Parent widget
 * @param flags Window flags
 * \endif
 *
 * \if CHINESE
 * @brief 构造函数，强制使用flags，而不是用parent的flags进行构造
 * @param parent 父窗口部件
 * @param flags 窗口标志
 * \endif
 */
SARibbonSystemButtonBar::SARibbonSystemButtonBar(QWidget* parent, Qt::WindowFlags flags)
	: QFrame(parent), d_ptr(new SARibbonSystemButtonBar::PrivateData(this))
{
	d_ptr->mFlags = flags;
	updateWindowFlag();
}

/**
 * \if ENGLISH
 * @brief Destructor
 * \endif
 *
 * \if CHINESE
 * @brief 析构函数
 * \endif
 */
SARibbonSystemButtonBar::~SARibbonSystemButtonBar()
{
}

/**
 * \if ENGLISH
 * @brief Sets up the minimize button
 * @param on Whether to show the minimize button
 * \endif
 *
 * \if CHINESE
 * @brief 设置最小化按钮
 * @param on 是否显示最小化按钮
 * \endif
 */
void SARibbonSystemButtonBar::setupMinimizeButton(bool on)
{
	d_ptr->setupMinimizeButton(on);
}

/**
 * \if ENGLISH
 * @brief Sets up the maximize button
 * @param on Whether to show the maximize button
 * \endif
 *
 * \if CHINESE
 * @brief 设置最大化按钮
 * @param on 是否显示最大化按钮
 * \endif
 */
void SARibbonSystemButtonBar::setupMaximizeButton(bool on)
{
	d_ptr->setupMaximizeButton(on);
}

/**
 * \if ENGLISH
 * @brief Sets up the close button
 * @param on Whether to show the close button
 * \endif
 *
 * \if CHINESE
 * @brief 设置关闭按钮
 * @param on 是否显示关闭按钮
 * \endif
 */
void SARibbonSystemButtonBar::setupCloseButton(bool on)
{
	d_ptr->setupCloseButton(on);
}

/**
 * \if ENGLISH
 * @brief Updates the window flag based on the top-level window's flags
 * \endif
 *
 * \if CHINESE
 * @brief 根据顶层窗口的标志更新窗口标志
 * \endif
 */
void SARibbonSystemButtonBar::updateWindowFlag()
{
	QWidget* topedWidget = this;
	// 找到最顶层窗口
	while (topedWidget->parentWidget()) {
		topedWidget = topedWidget->parentWidget();
	}
	Qt::WindowFlags flags = topedWidget->windowFlags();
	updateWindowFlag(flags);
}

/**
 * \if ENGLISH
 * @brief This function is only used to control the display of minimize, maximize, and close buttons
 * @param flags Window flags
 * \endif
 *
 * \if CHINESE
 * @brief 此函数仅用于控制最小最大化和关闭按钮的显示
 * @param flags 窗口标志
 * \endif
 */
void SARibbonSystemButtonBar::updateWindowFlag(Qt::WindowFlags flags)
{
	d_ptr->mFlags = flags;
	setupMinimizeButton(flags & Qt::WindowMinimizeButtonHint);
	setupMaximizeButton(flags & Qt::WindowMaximizeButtonHint);
	setupCloseButton(flags & Qt::WindowCloseButtonHint);
}

/**
 * \if ENGLISH
 * @brief Sets the width ratio of the buttons, the final button width will be set according to this ratio
 * @param close Close button ratio
 * @param max Maximize button ratio
 * @param min Minimize button ratio
 * \endif
 *
 * \if CHINESE
 * @brief 设置按钮的宽度比例,最终按钮宽度将按照此比例进行设置
 * @param close 关闭按钮比例
 * @param max 最大化按钮比例
 * @param min 最小化按钮比例
 * \endif
 */
void SARibbonSystemButtonBar::setButtonWidthStretch(int close, int max, int min)
{
	d_ptr->mMaxStretch   = max;
	d_ptr->mMinStretch   = min;
	d_ptr->mCloseStretch = close;
}

/**
 * \if ENGLISH
 * @brief Sets the title bar height
 *
 * The title bar height affects the sizeHint
 * @param h Height
 * \endif
 *
 * \if CHINESE
 * @brief 标题栏高度
 *
 * 标题栏高度会影响sizeHint
 * @param h 高度
 * \endif
 */
void SARibbonSystemButtonBar::setWindowTitleHeight(int h)
{
	d_ptr->mTitleBarHeight = h;
}

/**
 * \if ENGLISH
 * @brief Gets the title bar height
 * @return Title bar height
 * \endif
 *
 * \if CHINESE
 * @brief 标题栏高度
 * @return 标题栏高度
 * \endif
 */
int SARibbonSystemButtonBar::windowTitleHeight() const
{
	return d_ptr->mTitleBarHeight;
}

/**
 * \if ENGLISH
 * @brief Sets the width of the system buttons
 * @param w Width
 * \endif
 *
 * \if CHINESE
 * @brief 系统按钮的宽度
 * @param w 宽度
 * \endif
 */
void SARibbonSystemButtonBar::setWindowButtonWidth(int w)
{
	d_ptr->mWindowButtonWidth = w;
}

/**
 * \if ENGLISH
 * @brief Gets the width of the system buttons
 * @return System button width
 * \endif
 *
 * \if CHINESE
 * @brief 系统按钮的宽度
 * @return 系统按钮宽度
 * \endif
 */
int SARibbonSystemButtonBar::windowButtonWidth() const
{
	return d_ptr->mWindowButtonWidth;
}

/**
 * \if ENGLISH
 * @brief Sets the window state (maximize/minimize button state)
 * @param s Window states
 * \endif
 *
 * \if CHINESE
 * @brief 设置窗口状态（最大最小化按钮状态）
 * @param s 窗口状态
 * \endif
 */
void SARibbonSystemButtonBar::setWindowStates(Qt::WindowStates s)
{
	if (d_ptr->buttonMaximize) {
		bool on = s.testFlag(Qt::WindowMaximized);
		d_ptr->buttonMaximize->setChecked(on);
		d_ptr->buttonMaximize->setToolTip(on ? tr("Restore") : tr("Maximize"));
	}
}

/**
 * \if ENGLISH
 * @brief This function returns flags that only include Qt::WindowCloseButtonHint, Qt::WindowMaximizeButtonHint, Qt::WindowMinimizeButtonHint
 * @return Window flags
 * \endif
 *
 * \if CHINESE
 * @brief 此函数返回的flags仅包括 Qt::WindowCloseButtonHint，Qt::WindowMaximizeButtonHint，Qt::WindowMinimizeButtonHint
 * 三个
 * @return 窗口标志
 * \endif
 */
Qt::WindowFlags SARibbonSystemButtonBar::windowButtonFlags() const
{
	Qt::WindowFlags f = Qt::Widget;  // widget是000

	if (d_ptr->mFlags & Qt::WindowCloseButtonHint) {
		f |= Qt::WindowCloseButtonHint;
	}
	if (d_ptr->mFlags & Qt::WindowMaximizeButtonHint) {
		f |= Qt::WindowMaximizeButtonHint;
	}
	if (d_ptr->mFlags & Qt::WindowMinimizeButtonHint) {
		f |= Qt::WindowMinimizeButtonHint;
	}

	return (f);
}

/**
 * \if ENGLISH
 * @brief Gets the recommended size for the widget
 * @return Recommended size
 * \endif
 *
 * \if CHINESE
 * @brief 获取部件的推荐尺寸
 * @return 推荐尺寸
 * \endif
 */
QSize SARibbonSystemButtonBar::sizeHint() const
{
	return (d_ptr->sizeHint());
}

/**
 * \if ENGLISH
 * @brief Event filter for handling events from SARibbonMainWindow
 * @param obj The object that is sending the event
 * @param event The event being sent
 * @return true if the event was handled, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 用于处理来自SARibbonMainWindow的事件的事件过滤器
 * @param obj 发送事件的对象
 * @param event 正在发送的事件
 * @return 如果事件已处理则返回true，否则返回false
 * \endif
 */
bool SARibbonSystemButtonBar::eventFilter(QObject* obj, QEvent* event)
{
	if (obj && event) {
		SARibbonMainWindow* mainWindow = qobject_cast< SARibbonMainWindow* >(obj);
		if (!mainWindow) {
			// 所有事件都不消费
			return QFrame::eventFilter(obj, event);
		}
		// SARibbonMainWindow的事件
		switch (event->type()) {
		case QEvent::Resize: {
			int th = 25;

			SARibbonBar* ribbonBar = mainWindow->ribbonBar();
			if (ribbonBar) {
				th = ribbonBar->titleBarHeight();
			}
			if (th != height()) {
				setWindowTitleHeight(th);
			}
			QRect fr         = mainWindow->geometry();
			QSize wgSizeHint = sizeHint();
			if (SA::saIsRTL()) {
				setGeometry(0, 0, wgSizeHint.width(), wgSizeHint.height());
			} else {
				setGeometry(fr.width() - wgSizeHint.width(), 0, wgSizeHint.width(), wgSizeHint.height());
			}
			// 把设置好的尺寸给ribbonbar
			if (ribbonBar) {
				ribbonBar->setSystemButtonGroupSize(size());
			}
		} break;
		case QEvent::WindowStateChange: {
			setWindowStates(mainWindow->windowState());
		} break;
		case QEvent::LayoutDirectionChange: {
			/**
			 * \if ENGLISH
			 * @brief Handle layout direction change (LTR/RTL) - recalculate position
			 * @details When the application's layout direction changes, the system button bar
			 * needs to reposition itself (left edge for RTL, right edge for LTR).
			 * This case replicates the Resize logic to ensure proper positioning.
			 * Event is NOT consumed - it continues to propagate.
			 * \endif
			 *
			 * \if CHINESE
			 * @brief 处理布局方向变化 (从左到右/从右到左) - 重新计算位置
			 * @details 当应用程序的布局方向改变时，系统按钮栏需要重新定位
			 * (RTL 时在左侧，LTR 时在右侧)。此 case 复制 Resize 逻辑以确保正确定位。
			 * 事件不会被消费 - 它将继续传播。
			 * \endif
			 */
			int th = 25;

			SARibbonBar* ribbonBar = mainWindow->ribbonBar();
			if (ribbonBar) {
				th = ribbonBar->titleBarHeight();
			}
			if (th != height()) {
				setWindowTitleHeight(th);
			}
			QRect fr         = mainWindow->geometry();
			QSize wgSizeHint = sizeHint();
			if (SA::saIsRTL()) {
				setGeometry(0, 0, wgSizeHint.width(), wgSizeHint.height());
			} else {
				setGeometry(fr.width() - wgSizeHint.width(), 0, wgSizeHint.width(), wgSizeHint.height());
			}
			// 把设置好的尺寸给 ribbonbar
			if (ribbonBar) {
				ribbonBar->setSystemButtonGroupSize(size());
			}
			// 重新定位内部按钮
			d_ptr->resizeElement(size());
		} break;
		default:
			break;
		}
	}
	return QFrame::eventFilter(obj, event);
}

/**
 * \if ENGLISH
 * @brief Gets the minimize button
 * @return Pointer to the minimize button
 * \endif
 *
 * \if CHINESE
 * @brief 获取最小化按钮
 * @return 指向最小化按钮的指针
 * \endif
 */
QAbstractButton* SARibbonSystemButtonBar::minimizeButton() const
{
	return d_ptr->buttonMinimize;
}

/**
 * \if ENGLISH
 * @brief Gets the maximize button
 * @return Pointer to the maximize button
 * \endif
 *
 * \if CHINESE
 * @brief 获取最大化按钮
 * @return 指向最大化按钮的指针
 * \endif
 */
QAbstractButton* SARibbonSystemButtonBar::maximizeButton() const
{
	return d_ptr->buttonMaximize;
}

/**
 * \if ENGLISH
 * @brief Gets the close button
 * @return Pointer to the close button
 * \endif
 *
 * \if CHINESE
 * @brief 获取关闭按钮
 * @return 指向关闭按钮的指针
 * \endif
 */
QAbstractButton* SARibbonSystemButtonBar::closeButton() const
{
	return d_ptr->buttonClose;
}

/**
 * \if ENGLISH
 * @brief Sets the icon size
 * @param ic Icon size
 * \endif
 *
 * \if CHINESE
 * @brief 设置图标大小
 * @param ic 图标大小
 * \endif
 */
void SARibbonSystemButtonBar::setIconSize(const QSize& ic)
{
	d_ptr->mButtonGroup->setIconSize(ic);
}

/**
 * \if ENGLISH
 * @brief Gets the icon size
 * @return Icon size
 * \endif
 *
 * \if CHINESE
 * @brief 获取图标大小
 * @return 图标大小
 * \endif
 */
QSize SARibbonSystemButtonBar::iconSize() const
{
	return d_ptr->mButtonGroup->iconSize();
}

/**
 * \if ENGLISH
 * @brief Adds an action to the button group
 * @param a Action to add
 * \endif
 *
 * \if CHINESE
 * @brief 向按钮组添加一个动作
 * @param a 要添加的动作
 * \endif
 */
void SARibbonSystemButtonBar::addAction(QAction* a)
{
	d_ptr->mButtonGroup->addAction(a);
}

/**
 * \if ENGLISH
 * @brief Adds a menu action to the button group
 * @param menuAction Menu action to add
 * @param popupMode Popup mode
 * \endif
 *
 * \if CHINESE
 * @brief 向按钮组添加一个菜单动作
 * @param menuAction 要添加的菜单动作
 * @param popupMode 弹出模式
 * \endif
 */
void SARibbonSystemButtonBar::addMenuAction(QAction* menuAction, QToolButton::ToolButtonPopupMode popupMode)
{
	d_ptr->mButtonGroup->addMenuAction(menuAction, popupMode);
}

/**
 * \if ENGLISH
 * @brief Adds a menu to the button group
 * @param menu Menu to add
 * @param popupMode Popup mode
 * @return The added action
 * \endif
 *
 * \if CHINESE
 * @brief 向按钮组添加一个菜单
 * @param menu 要添加的菜单
 * @param popupMode 弹出模式
 * @return 添加的动作
 * \endif
 */
QAction* SARibbonSystemButtonBar::addMenuAction(QMenu* menu, QToolButton::ToolButtonPopupMode popupMode)
{
	return d_ptr->mButtonGroup->addMenuAction(menu, popupMode);
}

/**
 * \if ENGLISH
 * @brief Adds a separator to the button group
 * @return The added separator action
 * \endif
 *
 * \if CHINESE
 * @brief 向按钮组添加一个分隔符
 * @return 添加的分隔符动作
 * \endif
 */
QAction* SARibbonSystemButtonBar::addSeparator()
{
	return d_ptr->mButtonGroup->addSeparator();
}

/**
 * \if ENGLISH
 * @brief Adds a widget to the button group
 * @param w Widget to add
 * @return The added action
 * \endif
 *
 * \if CHINESE
 * @brief 向按钮组添加一个窗口部件
 * @param w 要添加的窗口部件
 * @return 添加的动作
 * \endif
 */
QAction* SARibbonSystemButtonBar::addWidget(QWidget* w)
{
	return d_ptr->mButtonGroup->addWidget(w);
}

/**
 * \if ENGLISH
 * @brief Handles resize events
 * @param e Resize event
 * \endif
 *
 * \if CHINESE
 * @brief 处理 resize 事件
 * @param e  resize 事件
 * \endif
 */
void SARibbonSystemButtonBar::resizeEvent(QResizeEvent* e)
{
	Q_UNUSED(e);
	d_ptr->resizeElement(size());
	QFrame::resizeEvent(e);
}

/**
 * \if ENGLISH
 * @brief Handles change events, specifically layout direction changes
 * @param e Change event
 * @details When layout direction changes (LTR to RTL or vice versa), repositions internal buttons
 *          by calling resizeElement() to handle RTL/LTR positioning correctly.
 * \endif
 *
 * \if CHINESE
 * @brief 处理变更事件，特别是布局方向变更
 * @param e 变更事件
 * @details 当布局方向改变时（LTR 到 RTL 或反之），通过调用 resizeElement() 重新定位内部按钮，
 *          以正确处理 RTL/LTR 布局。
 * \endif
 */
void SARibbonSystemButtonBar::changeEvent(QEvent* e)
{
	if (nullptr == e) {
		return;
	}
	switch (e->type()) {
	case QEvent::LayoutDirectionChange: {
		d_ptr->resizeElement(size());
	} break;
	default:
		break;
	}
	QFrame::changeEvent(e);
}

/**
 * \if ENGLISH
 * @brief Closes the window
 * \endif
 *
 * \if CHINESE
 * @brief 关闭窗口
 * \endif
 */
void SARibbonSystemButtonBar::closeWindow()
{
	if (parentWidget()) {
		parentWidget()->close();
	}
}

/**
 * \if ENGLISH
 * @brief Minimizes the window
 * \endif
 *
 * \if CHINESE
 * @brief 最小化窗口
 * \endif
 */
void SARibbonSystemButtonBar::minimizeWindow()
{
	if (parentWidget()) {
		parentWidget()->showMinimized();
	}
}

/**
 * \if ENGLISH
 * @brief Maximizes or restores the window
 * \endif
 *
 * \if CHINESE
 * @brief 最大化或还原窗口
 * \endif
 */
void SARibbonSystemButtonBar::maximizeWindow()
{
	QWidget* par = parentWidget();

	if (par) {
		if (par->isMaximized()) {
			par->showNormal();
		} else {
			par->showMaximized();
		}
	}
}

/*** End of inlined file: SARibbonSystemButtonBar.cpp ***/


/*** Start of inlined file: SARibbonToolButton.cpp ***/

/*** Start of inlined file: SARibbonButtonLayoutStrategy.h ***/
#ifndef SARIBBONBUTTONLAYOUTSTRATEGY_H
#define SARIBBONBUTTONLAYOUTSTRATEGY_H

#include <QStyleOptionToolButton>
#include <QSize>
#include <QRect>
#include <memory>

class QFontMetrics;
class QWidget;
class SARibbonToolButton;  // 前向声明

/**
 * @brief 按钮类型枚举 (独立定义，避免循环依赖)
 */
enum class SARibbonButtonType
{
	LargeButton,
	SmallButton
};

/**
 * @brief 按钮布局矩形集合
 *
 * 封装按钮布局中各个区域的矩形位置
 */
struct SA_RIBBON_EXPORT SARibbonButtonLayoutRects
{
	QRect iconRect;       ///< 图标绘制区域
	QRect textRect;       ///< 文本绘制区域
	QRect indicatorRect;  ///< 指示器(下拉箭头)绘制区域

	void clear()
	{
		iconRect      = QRect();
		textRect      = QRect();
		indicatorRect = QRect();
	}

	bool isValid() const
	{
		return iconRect.isValid() || textRect.isValid() || indicatorRect.isValid();
	}
};

/**
 * @brief 布局上下文
 *
 * 包含布局计算所需的各种参数
 */
struct SA_RIBBON_EXPORT SARibbonButtonLayoutContext
{
	const QWidget* buttonWidget { nullptr };  ///< 目标按钮部件
	int spacing { 1 };                        ///< 元素间距
	int indicatorLength { 8 };                ///< 指示器长度
	QSize largeIconSize { 32, 32 };           ///< 大按钮图标尺寸
	QSize smallIconSize { 16, 16 };           ///< 小按钮图标尺寸
	qreal twoLineHeightFactor { 2.05 };       ///< 两行文本高度系数
	qreal oneLineHeightFactor { 1.2 };        ///< 单行文本高度系数
	qreal buttonMaximumAspectRatio { 1.4 };   ///< 按钮最大宽高比
	bool enableWordWrap { true };             ///< 是否启用自动换行
	mutable bool isTextNeedWrap { false };    ///< 标记文本是否需要换行
	int maximumWidth { QWIDGETSIZE_MAX };     ///< 按钮最大宽度

	SARibbonButtonLayoutContext() = default;
	explicit SARibbonButtonLayoutContext(const SARibbonToolButton* btn);
};

/**
 * @brief Ribbon按钮布局策略抽象基类
 *
 * 定义按钮布局计算的接口，不同的按钮类型(大按钮/小按钮)可以实现不同的布局策略
 */
class SA_RIBBON_EXPORT SARibbonButtonLayoutStrategy
{
public:
	virtual ~SARibbonButtonLayoutStrategy() = default;

	/**
	 * @brief 计算绘制区域矩形
	 * @param opt 样式选项
	 * @param rects 输出的矩形集合
	 * @param ctx 布局上下文
	 */
	virtual void calculateDrawRects(const QStyleOptionToolButton& opt,
									SARibbonButtonLayoutRects& rects,
									const SARibbonButtonLayoutContext& ctx) const = 0;

	/**
	 * @brief 计算按钮的size hint
	 * @param opt 样式选项
	 * @param ctx 布局上下文
	 * @return 推荐的按钮尺寸
	 */
	virtual QSize calculateSizeHint(const QStyleOptionToolButton& opt, const SARibbonButtonLayoutContext& ctx) const = 0;

	/**
	 * @brief 计算文本绘制区域高度
	 * @param opt 样式选项
	 * @param ctx 布局上下文
	 * @return 文本区域高度
	 */
	virtual int calculateTextHeight(const QStyleOptionToolButton& opt, const SARibbonButtonLayoutContext& ctx) const = 0;

	/**
	 * @brief 估算大按钮的最优文本宽度
	 * @param buttonHeight 按钮高度
	 * @param textHeight 文本区域高度
	 * @param text 按钮文本
	 * @param fm 字体度量
	 * @param ctx 布局上下文
	 * @return 估算的文本宽度
	 */
	virtual int estimateTextWidth(int buttonHeight,
								  int textHeight,
								  const QString& text,
								  const QFontMetrics& fm,
								  SARibbonButtonLayoutContext& ctx) const;  // 非const因为可能修改ctx.isTextNeedWrap

protected:
	/**
	 * @brief 判断是否有指示器(下拉菜单)
	 */
	static bool hasIndicator(const QStyleOptionToolButton& opt);

	/**
	 * @brief 调整图标尺寸以适应按钮区域
	 */
	static QSize adjustIconSize(const QRect& buttonRect, const QSize& originIconSize);

	/**
	 * @brief 简化文本(移除换行符)
	 */
	static QString simplifiedText(const QString& str);
};

/**
 * @brief 大按钮布局策略
 */
class SA_RIBBON_EXPORT SARibbonLargeButtonLayoutStrategy : public SARibbonButtonLayoutStrategy
{
public:
	void calculateDrawRects(const QStyleOptionToolButton& opt,
							SARibbonButtonLayoutRects& rects,
							const SARibbonButtonLayoutContext& ctx) const override;

	QSize calculateSizeHint(const QStyleOptionToolButton& opt, const SARibbonButtonLayoutContext& ctx) const override;

	int calculateTextHeight(const QStyleOptionToolButton& opt, const SARibbonButtonLayoutContext& ctx) const override;

private:
	void calculateIconOnlyRects(const QStyleOptionToolButton& opt,
								SARibbonButtonLayoutRects& rects,
								const SARibbonButtonLayoutContext& ctx) const;

	void calculateTextOnlyRects(const QStyleOptionToolButton& opt,
								SARibbonButtonLayoutRects& rects,
								const SARibbonButtonLayoutContext& ctx) const;

	void calculateIconAndTextRects(const QStyleOptionToolButton& opt,
								   SARibbonButtonLayoutRects& rects,
								   const SARibbonButtonLayoutContext& ctx) const;
};

/**
 * @brief 小按钮布局策略
 */
class SA_RIBBON_EXPORT SARibbonSmallButtonLayoutStrategy : public SARibbonButtonLayoutStrategy
{
public:
	void calculateDrawRects(const QStyleOptionToolButton& opt,
							SARibbonButtonLayoutRects& rects,
							const SARibbonButtonLayoutContext& ctx) const override;

	QSize calculateSizeHint(const QStyleOptionToolButton& opt, const SARibbonButtonLayoutContext& ctx) const override;

	int calculateTextHeight(const QStyleOptionToolButton& opt, const SARibbonButtonLayoutContext& ctx) const override;

private:
	void calculateIconOnlyRects(const QStyleOptionToolButton& opt,
								SARibbonButtonLayoutRects& rects,
								const SARibbonButtonLayoutContext& ctx) const;

	void calculateTextOnlyRects(const QStyleOptionToolButton& opt,
								SARibbonButtonLayoutRects& rects,
								const SARibbonButtonLayoutContext& ctx) const;

	void calculateIconAndTextRects(const QStyleOptionToolButton& opt,
								   SARibbonButtonLayoutRects& rects,
								   const SARibbonButtonLayoutContext& ctx) const;
};

/**
 * @brief 布局策略工厂
 */
class SA_RIBBON_EXPORT SARibbonButtonLayoutStrategyFactory
{
public:
	static std::unique_ptr< SARibbonButtonLayoutStrategy > createStrategy(SARibbonButtonType type);
};

#endif  // SARIBBONBUTTONLAYOUTSTRATEGY_H

/*** End of inlined file: SARibbonButtonLayoutStrategy.h ***/

#include <QAction>
#include <QApplication>
#include <QCursor>
#include <QMouseEvent>
#include <QPaintEvent>
#include <QStyleOption>
#include <QStyleOptionFocusRect>
#include <QStyleOptionToolButton>
#include <QStylePainter>
#include <QTextOption>
#include <QApplication>
#include <QScreen>
#include <QProxyStyle>

/**
 * @def 开启此宏会打印一些常见信息
 */
#ifndef SA_RIBBON_TOOLBUTTON_DEBUG_PRINT
#define SA_RIBBON_TOOLBUTTON_DEBUG_PRINT 0
#endif

#ifndef SARIBBONTOOLBUTTON_DEBUG_DRAW
#define SARIBBONTOOLBUTTON_DEBUG_DRAW 0
#endif

// 布局常量定义
namespace SARibbonToolButtonConstants
{
constexpr int DEFAULT_SPACING                   = 1;     ///< 默认按钮与边框的间距
constexpr int DEFAULT_INDICATOR_LEN_SMALL       = 12;    ///< 小按钮模式下默认指示器长度
constexpr int DEFAULT_INDICATOR_LEN_LARGE       = 8;     ///< 大按钮模式下默认指示器长度
constexpr int MIN_BUTTON_WIDTH                  = 16;    ///< 按钮最小宽度
constexpr int GLOBAL_STRUT_WIDTH                = 2;     ///< 全局尺寸约束宽度
constexpr int GLOBAL_STRUT_HEIGHT               = 2;     ///< 全局尺寸约束高度
constexpr qreal LARGE_BUTTON_HEIGHT_FACTOR      = 4.8;   ///< 大按钮高度系数 (相对于行间距)
constexpr qreal LARGE_BUTTON_MIN_WIDTH_RATIO    = 0.75;  ///< 大按钮最小宽度比例 (相对于高度)
constexpr int SMALL_BUTTON_HEIGHT_OFFSET        = 2;     ///< 小按钮文本绘制高度偏移
constexpr qreal TWO_LINE_HEIGHT_FACTOR_DEFAULT  = 2.05;  ///< 两行文本高度系数默认值
constexpr qreal ONE_LINE_HEIGHT_FACTOR_DEFAULT  = 1.2;   ///< 单行文本高度系数默认值
constexpr qreal BUTTON_MAX_ASPECT_RATIO_DEFAULT = 1.4;   ///< 按钮最大宽高比默认值
constexpr int INDICATOR_HEIGHT_FACTOR_NUM       = 12;    ///< 指示器高度计算分子
constexpr int INDICATOR_HEIGHT_FACTOR_DEN       = 10;    ///< 指示器高度计算分母 (即1.2倍)
}

#if SARIBBONTOOLBUTTON_DEBUG_DRAW
#ifndef SARIBBONTOOLBUTTON_DEBUG_DRAW_RECT
#define SARIBBONTOOLBUTTON_DEBUG_DRAW_RECT(p, rect)                                                                    \
	do {                                                                                                               \
		p.save();                                                                                                      \
		p.setPen(Qt::red);                                                                                             \
		p.setBrush(QBrush());                                                                                          \
		p.drawRect(rect);                                                                                              \
		p.restore();                                                                                                   \
	} while (0)
#endif
#else
#ifndef SARIBBONTOOLBUTTON_DEBUG_DRAW_RECT
#define SARIBBONTOOLBUTTON_DEBUG_DRAW_RECT(p, rect)
#endif
#endif
namespace SA
{

QDebug operator<<(QDebug debug, const QStyleOptionToolButton& opt)
{
	debug << "==============" << "\nQStyleOption(" << (QStyleOption)opt << ")"
		  << "\n  QStyleOptionComplex:"
			 "\n     subControls("
		  << opt.subControls
		  << " ) "
			 "\n     activeSubControls("
		  << opt.activeSubControls
		  << "\n  QStyleOptionToolButton"
			 "\n     features("
		  << opt.features
		  << ")"
			 "\n     toolButtonStyle("
		  << opt.toolButtonStyle << ")";

	return (debug);
}
}

//===================================================
// SARibbonToolButtonProxyStyle
//===================================================

class SARibbonToolButtonProxyStyle : public QProxyStyle
{
public:
	void drawPrimitive(PrimitiveElement pe, const QStyleOption* opt, QPainter* p, const QWidget* widget = nullptr) const override
	{
		if (pe == PE_IndicatorArrowUp || pe == PE_IndicatorArrowDown || pe == PE_IndicatorArrowRight
			|| pe == PE_IndicatorArrowLeft) {
			if (opt->rect.width() <= 1 || opt->rect.height() <= 1) {
				return;
			}

			QRect r  = opt->rect;
			int size = qMin(r.height(), r.width());
			QPixmap pixmap;
			qreal pixelRatio = p->device()->devicePixelRatio();
			int border       = qRound(pixelRatio * (size / 4));
			int sqsize       = qRound(pixelRatio * (2 * (size / 2)));
			QImage image(sqsize, sqsize, QImage::Format_ARGB32_Premultiplied);
			image.fill(Qt::transparent);
			QPainter imagePainter(&image);

			QPolygon a;
			switch (pe) {
			case PE_IndicatorArrowUp:
				a.setPoints(3, border, sqsize / 2, sqsize / 2, border, sqsize - border, sqsize / 2);
				break;
			case PE_IndicatorArrowDown:
				a.setPoints(3, border, sqsize / 2, sqsize / 2, sqsize - border, sqsize - border, sqsize / 2);
				break;
			case PE_IndicatorArrowRight:
				a.setPoints(3, sqsize - border, sqsize / 2, sqsize / 2, border, sqsize / 2, sqsize - border);
				break;
			case PE_IndicatorArrowLeft:
				a.setPoints(3, border, sqsize / 2, sqsize / 2, border, sqsize / 2, sqsize - border);
				break;
			default:
				break;
			}

			int bsx = 0;
			int bsy = 0;

			if (opt->state & State_Sunken) {
				bsx = proxy()->pixelMetric(PM_ButtonShiftHorizontal, opt, widget);
				bsy = proxy()->pixelMetric(PM_ButtonShiftVertical, opt, widget);
			}

			QRect bounds = a.boundingRect();
			int sx       = sqsize / 2 - bounds.center().x() - 1;
			int sy       = sqsize / 2 - bounds.center().y() - 1;
			imagePainter.translate(sx + bsx, sy + bsy);
			imagePainter.setPen(QPen(opt->palette.buttonText().color(), 1.4));
			imagePainter.setBrush(Qt::NoBrush);

			if (!(opt->state & State_Enabled)) {
				imagePainter.translate(1, 1);
				imagePainter.setPen(QPen(opt->palette.light().color(), 1.4));
				imagePainter.drawPolyline(a);
				imagePainter.translate(-1, -1);
				imagePainter.setPen(QPen(opt->palette.mid().color(), 1.4));
			}

			imagePainter.drawPolyline(a);
			imagePainter.end();
			pixmap = QPixmap::fromImage(image);
			pixmap.setDevicePixelRatio(pixelRatio);

			int xOffset = r.x() + (r.width() - size) / 2;
			int yOffset = r.y() + (r.height() - size) / 2;
			p->drawPixmap(xOffset, yOffset, pixmap);
		} else {
			QProxyStyle::drawPrimitive(pe, opt, p, widget);
		}
	}
};

//===================================================
// SARibbonToolButton::PrivateData
//===================================================

class SARibbonToolButton::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonToolButton)
public:
	PrivateData(SARibbonToolButton* p);
	// 根据鼠标位置更新按钮的信息
	void updateStatusByMousePosition(const QPoint& pos);
	// 更新绘图相关的尺寸
	void updateDrawRect(const QStyleOptionToolButton& opt);
	// 更新SizeHint
	void updateSizeHint(const QStyleOptionToolButton& opt);
	// 计算涉及到的rect尺寸
	void calcDrawRects(const QStyleOptionToolButton& opt,
					   QRect& iconRect,
					   QRect& textRect,
					   QRect& indicatorArrowRect,
					   int spacing,
					   int indicatorLen) const;
	// 计算小按钮模式下的尺寸
	void calcSmallButtonDrawRects(const QStyleOptionToolButton& opt,
								  QRect& iconRect,
								  QRect& textRect,
								  QRect& indicatorArrowRect,
								  int spacing,
								  int indicatorLen) const;
	// 计算大按钮模式下的尺寸
	void calcLargeButtonDrawRects(const QStyleOptionToolButton& opt,
								  QRect& iconRect,
								  QRect& textRect,
								  QRect& indicatorArrowRect,
								  int spacing,
								  int indicatorLen) const;

	// 根据按钮的尺寸调节iconsize(注意这里的buttonRect是已经减去mSpacing的情况)
	QSize adjustIconSize(const QRect& buttonRect, const QSize& originIconSize) const;
	// 判断是否有Indicator
	bool hasIndicator(const QStyleOptionToolButton& opt) const;
	// 计算sizehint
	QSize calcSizeHint(const QStyleOptionToolButton& opt);
	QSize calcSmallButtonSizeHint(const QStyleOptionToolButton& opt);
	QSize calcLargeButtonSizeHint(const QStyleOptionToolButton& opt);

	// 计算文本绘制矩形的高度
	int calcTextDrawRectHeight(const QStyleOptionToolButton& opt) const;
	// 估算一个最优的文本宽度
	int estimateLargeButtonTextWidth(int buttonHeight,
									 int textDrawRectHeight,
									 const QString& text,
									 const QFontMetrics& fm,
									 int maxTrycount = 3);
	QPixmap createIconPixmap(const QStyleOptionToolButton& opt, const QSize& iconsize) const;
	// 获取文字的对其方式
	int getTextAlignment() const;
	// 确认文字是否确切要换行显示
	bool isTextNeedWrap() const;
	// 获取真实的icon尺寸
	QSize realIconSize() const;
	// 仅仅对\n进行剔除，和QString::simplified不一样
	static QString simplifiedForRibbonButton(const QString& str);
	// 获取有效的按钮类型（当enableIconRightText为true时，强制返回SmallButton）
	SARibbonToolButton::RibbonButtonType effectiveButtonType() const;

public:
	bool mMouseOnSubControl { false };  ///< 这个用于标记MenuButtonPopup模式下，鼠标在文本区域
	bool mMenuButtonPressed { false };  ///< 由于Indicator改变，因此hitButton不能用QToolButton的hitButton
	bool mWordWrap { true };            ///< 标记是否文字换行 @default false
	bool enableIconRightText { false }; ///< 是否启用图标右侧文字模式
	SARibbonToolButton::RibbonButtonType mButtonType { SARibbonToolButton::LargeButton };
	int mSpacing { SARibbonToolButtonConstants::DEFAULT_SPACING };                   ///< 按钮和边框的距离
	int mIndicatorLen { SARibbonToolButtonConstants::DEFAULT_INDICATOR_LEN_LARGE };  ///< Indicator的长度
	QRect mDrawIconRect;                                                             ///< 记录icon的绘制位置
	QRect mDrawTextRect;                                                             ///< 记录text的绘制位置
	QRect mDrawIndicatorArrowRect;                                                   ///< 记录IndicatorArrow的绘制位置
	QSize mSizeHint;                                                                 ///< 保存计算好的sizehint
	QSize mLargeButtonSizeHint { 32, 32 };                                           ///< 大按钮的尺寸
	bool mIsTextNeedWrap { false };                                                  ///< 标记文字是否需要换行显示
	SARibbonToolButton::LayoutFactor layoutFactor;                                   ///< 布局系数
	std::unique_ptr< SARibbonToolButtonProxyStyle > mStyle;                          ///< 按钮样式，主要为了绘制箭头

	// 图标缓存相关
	mutable QPixmap mCachedIconPixmap;                      ///< 缓存的图标pixmap
	mutable QSize mCachedIconSize;                          ///< 缓存的图标尺寸
	mutable QIcon::Mode mCachedIconMode { QIcon::Normal };  ///< 缓存的图标模式
	mutable QIcon::State mCachedIconState { QIcon::Off };   ///< 缓存的图标状态
	mutable bool mIconCacheValid { false };                 ///< 图标缓存是否有效

	void invalidateIconCache()
	{
		mIconCacheValid = false;
	}
};

SARibbonToolButton::PrivateData::PrivateData(SARibbonToolButton* p) : q_ptr(p)
{
	mStyle = std::make_unique< SARibbonToolButtonProxyStyle >();
}

SARibbonToolButton::RibbonButtonType SARibbonToolButton::PrivateData::effectiveButtonType() const
{
	if (enableIconRightText) {
		return SARibbonToolButton::SmallButton;
	}
	return mButtonType;
}

/**
 * @brief 根据鼠标的位置更新状态，主要用于判断鼠标是否位于subcontrol
 *
 * 此函数主要应用在action menu模式下
 * @param pos
 */
void SARibbonToolButton::PrivateData::updateStatusByMousePosition(const QPoint& pos)
{
	bool isMouseOnSubControl(false);
	if (SARibbonToolButton::LargeButton == effectiveButtonType()) {
		isMouseOnSubControl = mDrawTextRect.united(mDrawIndicatorArrowRect).contains(pos);
	} else {
		// 小按钮模式就和普通toolbutton一样
		isMouseOnSubControl = mDrawIndicatorArrowRect.contains(pos);
	}

	if (mMouseOnSubControl != isMouseOnSubControl) {
		mMouseOnSubControl = isMouseOnSubControl;
		// 从icon变到text过程中刷新一次
		q_ptr->update();
	}
}

/**
 * @brief 更新绘图的几个关键尺寸
 *
 * 包括：
 *
 * - DrawIconRect 绘制图标的矩形区域
 *
 * - DrawTextRect 绘制文本的矩形区域
 *
 * - DrawIndicatorArrowRect 绘制菜单下箭头的矩形区域
 *
 * @param opt
 */
void SARibbonToolButton::PrivateData::updateDrawRect(const QStyleOptionToolButton& opt)
{
	if (!mSizeHint.isValid()) {
		updateSizeHint(opt);
	}
	// 先更新IndicatorLen
	mIndicatorLen = q_ptr->style()->pixelMetric(QStyle::PM_MenuButtonIndicator, &opt, q_ptr);
	if (mIndicatorLen < 3) {
		if (SARibbonToolButton::LargeButton == effectiveButtonType()) {
			mIndicatorLen = SARibbonToolButtonConstants::DEFAULT_INDICATOR_LEN_LARGE;
		} else {
			mIndicatorLen = SARibbonToolButtonConstants::DEFAULT_INDICATOR_LEN_SMALL;
		}
	}
	calcDrawRects(opt, mDrawIconRect, mDrawTextRect, mDrawIndicatorArrowRect, mSpacing, mIndicatorLen);
}

/**
 * @brief 更新sizehint
 * @param opt
 */
void SARibbonToolButton::PrivateData::updateSizeHint(const QStyleOptionToolButton& opt)
{
	mSizeHint = calcSizeHint(opt);
}

/**
 * @brief 计算绘图的几个关键区域
 * @param opt
 * @param iconRect  绘制图标的矩形区域
 * @param textRect 绘制文本的矩形区域
 * @param indicatorArrowRect 绘制菜单下箭头的矩形区域
 * @param spacing
 * @param indicatorLen
 */
void SARibbonToolButton::PrivateData::calcDrawRects(const QStyleOptionToolButton& opt,
													QRect& iconRect,
													QRect& textRect,
													QRect& indicatorArrowRect,
													int spacing,
													int indicatorLen) const
{
	if (SARibbonToolButton::LargeButton == effectiveButtonType()) {
		calcLargeButtonDrawRects(opt, iconRect, textRect, indicatorArrowRect, spacing, indicatorLen);

	} else {
		calcSmallButtonDrawRects(opt, iconRect, textRect, indicatorArrowRect, spacing, indicatorLen);
	}
}

/**
 * \if ENGLISH
 * @brief Calculate draw rectangles for small button mode
 * @param[in] opt Style option for the tool button
 * @param[out] iconRect Calculated icon rectangle
 * @param[out] textRect Calculated text rectangle
 * @param[out] indicatorArrowRect Calculated indicator (dropdown arrow) rectangle
 * @param[in] spacing Spacing between elements
 * @param[in] indicatorLen Width reserved for the indicator arrow
 * @details In LTR mode, the indicator is on the right side and the icon starts from the left.
 *          In RTL mode, positions are mirrored: indicator moves to the left side, icon/text start from the right.
 * \endif
 *
 * \if CHINESE
 * @brief 计算小按钮模式下的绘制尺寸
 * @param[in] opt 工具按钮的样式选项
 * @param[out] iconRect 计算出的图标矩形
 * @param[out] textRect 计算出的文字矩形
 * @param[out] indicatorArrowRect 计算出的指示器（下拉箭头）矩形
 * @param[in] spacing 元素之间的间距
 * @param[in] indicatorLen 为指示器箭头预留的宽度
 * @details 在LTR模式下，指示器在右侧，图标从左侧开始。
 *          在RTL模式下，位置镜像：指示器移到左侧，图标/文字从右侧开始。
 * \endif
 */
void SARibbonToolButton::PrivateData::calcSmallButtonDrawRects(const QStyleOptionToolButton& opt,
																QRect& iconRect,
																QRect& textRect,
																QRect& indicatorArrowRect,
																int spacing,
																int indicatorLen) const
{
	switch (opt.toolButtonStyle) {
	case Qt::ToolButtonIconOnly: {
		if (hasIndicator(opt)) {
			// 在仅有图标的小模式显示时，预留一个下拉箭头位置
			iconRect = opt.rect.adjusted(spacing, spacing, -indicatorLen - spacing, -spacing);
			indicatorArrowRect =
				QRect(opt.rect.right() - indicatorLen - spacing, iconRect.y(), indicatorLen, iconRect.height());
		} else {
			iconRect           = opt.rect.adjusted(spacing, spacing, -spacing, -spacing);
			indicatorArrowRect = QRect();
		}
		// 文本区域为空
		textRect = QRect();
	} break;
	case Qt::ToolButtonTextOnly: {
		if (hasIndicator(opt)) {
			// 在仅有图标的小模式显示时，预留一个下拉箭头位置
			textRect = opt.rect.adjusted(spacing, spacing, -indicatorLen - spacing, -spacing);
			indicatorArrowRect = QRect(opt.rect.right() - indicatorLen - spacing, spacing, indicatorLen, textRect.height());
		} else {
			textRect           = opt.rect.adjusted(spacing, spacing, -spacing, -spacing);
			indicatorArrowRect = QRect();
		}
		// 绘图区域为空
		iconRect = QRect();
	} break;
	default: {
		bool hasInd = hasIndicator(opt);
		// icon Beside和under都是一样的
		QRect buttonRect = q_ptr->rect();
		buttonRect.adjust(spacing, spacing, -spacing, -spacing);
		// 先设置IconRect
		if (opt.icon.isNull()) {
			// 没有图标
			iconRect = QRect();
		} else {
			QSize iconSize = adjustIconSize(buttonRect, opt.iconSize);
			iconRect =
				QRect(buttonRect.x(), buttonRect.y(), iconSize.width(), qMax(iconSize.height(), buttonRect.height()));
		}
		// 后设置TextRect
		if (opt.text.isEmpty()) {
			textRect = QRect();
		} else {
			// 分有菜单和没菜单两种情况
			int adjx = iconRect.isValid() ? (iconRect.width() + spacing)
										   : 0;  // 在buttonRect上变换，因此如果没有图标是不用偏移spacing
			if (hasInd) {
				textRect = buttonRect.adjusted(adjx, 0, -indicatorLen, 0);
			} else {
				textRect = buttonRect.adjusted(adjx, 0, 0, 0);  // 在buttonRect上变换，因此如果没有图标是不用偏移spacing
			}
		}
		// 最后设置Indicator
		if (hasInd) {
			if (textRect.isValid()) {
				indicatorArrowRect =
					QRect(buttonRect.right() - indicatorLen + 1, textRect.y(), indicatorLen, textRect.height());
			} else if (iconRect.isValid()) {
				indicatorArrowRect =
					QRect(buttonRect.right() - indicatorLen + 1, iconRect.y(), indicatorLen, iconRect.height());
			} else {
				indicatorArrowRect = buttonRect;
			}
		} else {
			indicatorArrowRect = QRect();
		}
	}
	}

	/**
	 * \if ENGLISH
	 * @brief Mirror all horizontal positions for RTL layout
	 * @details In RTL mode, the indicator (dropdown arrow) moves to the LEFT side instead of the RIGHT side.
	 *          The icon and text positions are mirrored: icon starts from the right edge, text flows RTL.
	 *          All x-coordinates are mirrored using SA::saMirrorX() within the button rect width.
	 *          Coordinates are first converted to relative positions within opt.rect, then mirrored, then converted back.
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 为RTL布局镜像所有水平位置
	 * @details 在RTL模式下，指示器（下拉箭头）移到左侧而非右侧。
	 *          图标和文字位置镜像：图标从右边缘开始，文字从右到左排列。
	 *          所有x坐标先转换为opt.rect内的相对位置，再使用SA::saMirrorX()在按钮矩形宽度内镜像，最后转换回绝对位置。
	 * \endif
	 */
	if (SA::saIsRTL()) {
		int containerWidth = opt.rect.width();
		int rectLeft       = opt.rect.x();
		// Mirror iconRect x position
		if (iconRect.isValid()) {
			int relX       = iconRect.x() - rectLeft;
			int mirroredX  = SA::saMirrorX(relX, containerWidth, iconRect.width());
			iconRect.moveLeft(rectLeft + mirroredX);
		}
		// Mirror textRect x position
		if (textRect.isValid()) {
			int relX       = textRect.x() - rectLeft;
			int mirroredX  = SA::saMirrorX(relX, containerWidth, textRect.width());
			textRect.moveLeft(rectLeft + mirroredX);
		}
		// Mirror indicatorArrowRect x position
		if (indicatorArrowRect.isValid()) {
			int relX       = indicatorArrowRect.x() - rectLeft;
			int mirroredX  = SA::saMirrorX(relX, containerWidth, indicatorArrowRect.width());
			indicatorArrowRect.moveLeft(rectLeft + mirroredX);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Calculate draw rectangles for large button mode
 * @param[in] opt Style option for the tool button
 * @param[out] iconRect Calculated icon rectangle
 * @param[out] textRect Calculated text rectangle
 * @param[out] indicatorArrowRect Calculated indicator (dropdown arrow) rectangle
 * @param[in] spacing Spacing between elements
 * @param[in] indicatorLen Width reserved for the indicator arrow
 * @details In LTR mode, the indicator is at the bottom-right of the button and text is left/center aligned.
 *          In RTL mode, the indicator moves to the bottom-left corner, and text aligns right.
 *          Icon remains centered in large buttons regardless of layout direction.
 *          All x-coordinates are mirrored using SA::saMirrorX() within the button rect width.
 * \endif
 *
 * \if CHINESE
 * @brief 计算大按钮模式下的绘制尺寸
 * @param[in] opt 工具按钮的样式选项
 * @param[out] iconRect 计算出的图标矩形
 * @param[out] textRect 计算出的文字矩形
 * @param[out] indicatorArrowRect 计算出的指示器（下拉箭头）矩形
 * @param[in] spacing 元素之间的间距
 * @param[in] indicatorLen 为指示器箭头预留的宽度
 * @details 在LTR模式下，指示器在按钮右下角，文字左对齐/居中对齐。
 *          在RTL模式下，指示器移到左下角，文字右对齐。
 *          大按钮中的图标保持居中，不受布局方向影响。
 *          所有x坐标使用SA::saMirrorX()在按钮矩形宽度内镜像。
 * \endif
 */
void SARibbonToolButton::PrivateData::calcLargeButtonDrawRects(const QStyleOptionToolButton& opt,
																QRect& iconRect,
																QRect& textRect,
																QRect& indicatorArrowRect,
																int spacing,
																int indicatorLen) const
{
	//! 3行模式的图标比较大，文字换行情况下，indicator会动态调整

	// 初始化
	iconRect           = QRect();
	textRect           = QRect();
	indicatorArrowRect = QRect();

	// 先获取文字矩形的高度
	int textHeight  = calcTextDrawRectHeight(opt);
	bool hIndicator = hasIndicator(opt);
	if (!hIndicator) {
		// 没有菜单，把len设置为0
		indicatorLen = 0;
	}

	// 这里要判断文字是否要换行显示，换行显示的文字的indicatorArrowRect所处的位置不一样
	if (Qt::ToolButtonIconOnly == opt.toolButtonStyle) {
		// 只有图标
		if (hIndicator) {
			// 如果只有icon，且有indicator，那么indicator在图标下面（注意，这个indicator布局和即有图标和文字是不一样的）
			int indicatorHeight = static_cast< int >(indicatorLen * SARibbonToolButtonConstants::INDICATOR_HEIGHT_FACTOR_NUM
													 / SARibbonToolButtonConstants::INDICATOR_HEIGHT_FACTOR_DEN);
			// 周边留下spacing距离
			indicatorArrowRect = QRect(opt.rect.left() + spacing,
									   opt.rect.bottom() - indicatorHeight - spacing,
									   opt.rect.width() - 2 * spacing,
									   indicatorHeight);
			// iconRect布满整个按钮
			iconRect = QRect(opt.rect.left() + spacing,
							 opt.rect.top() + spacing,
							 opt.rect.width() - 2 * spacing,
							 opt.rect.height() - 2 * spacing - indicatorHeight);
		} else {
			// iconRect布满整个按钮
			iconRect = QRect(opt.rect.left() + spacing,
							 opt.rect.top() + spacing,
							 opt.rect.width() - 2 * spacing,
							 opt.rect.height() - 2 * spacing);
		}
	} else if (Qt::ToolButtonTextOnly == opt.toolButtonStyle) {
		// 仅有文字，处理方式和仅有图标一样
		if (hIndicator) {
			// 如果只有text，且有indicator，那么indicator在图标下面（注意，这个indicator布局和即有图标和文字是不一样的）
			int indicatorHeight = static_cast< int >(indicatorLen * SARibbonToolButtonConstants::INDICATOR_HEIGHT_FACTOR_NUM
													 / SARibbonToolButtonConstants::INDICATOR_HEIGHT_FACTOR_DEN);
			// 周边留下spacing距离
			indicatorArrowRect = QRect(opt.rect.left() + spacing,
									   opt.rect.bottom() - indicatorHeight - spacing,
									   opt.rect.width() - 2 * spacing,
									   indicatorHeight);
			// textRect布满整个按钮
			textRect = QRect(opt.rect.left() + spacing,
							 opt.rect.top() + spacing,
							 opt.rect.width() - 2 * spacing,
							 opt.rect.height() - 2 * spacing - indicatorHeight);
		} else {
			// textRect布满整个按钮
			textRect = QRect(opt.rect.left() + spacing,
							 opt.rect.top() + spacing,
							 opt.rect.width() - 2 * spacing,
							 opt.rect.height() - 2 * spacing);
		}
	} else {
		// 先布置textRect
		if (q_ptr->isEnableWordWrap()) {
			// 在换行模式下
			if (isTextNeedWrap()) {
				// 如果文字的确换行，indicator放在最右边
				textRect = QRect(opt.rect.left() + spacing,
								 opt.rect.bottom() - spacing - textHeight,
								 opt.rect.width() - 2 * spacing - indicatorLen,  // 注意，这里会减去indicatorLen的宽度
								 textHeight);
				if (hIndicator) {
					// indicator在文字的右边
					indicatorArrowRect =
						QRect(textRect.right(), textRect.y() + textRect.height() / 2, indicatorLen, textHeight / 2);
				}
			} else {
				// 如果文字不需要换行，由于文字下面会有一行的空白，因此indicator布局在文字下面
				textRect = QRect(opt.rect.left() + spacing,
								 opt.rect.bottom() - spacing - textHeight,
								 opt.rect.width() - 2 * spacing,
								 textHeight);
				if (hIndicator) {
					int dy = textRect.height() / 2;
					dy += (dy - indicatorLen) / 2;
					indicatorArrowRect = QRect(textRect.left(), textRect.top() + dy, textRect.width(), indicatorLen);
				}
			}
		} else {
			// 文字不换行，indicator放在最右边
			int y = opt.rect.bottom() - spacing - textHeight;
			if (hIndicator) {
				// 先布置indicator
				indicatorArrowRect = QRect(opt.rect.right() - indicatorLen - spacing, y, indicatorLen, textHeight);
				textRect           = QRect(spacing, y, indicatorArrowRect.x() - spacing, textHeight);
			} else {
				textRect = QRect(opt.rect.left() + spacing, y, opt.rect.width() - 2 * spacing, textHeight);
			}
		}
		// 剩下就是icon区域
		iconRect = QRect(spacing, spacing, opt.rect.width() - 2 * spacing, textRect.top() - 2 * spacing);
	}

	/**
	 * \if ENGLISH
	 * @brief Mirror all horizontal positions for RTL layout
	 * @details In RTL mode, the indicator (dropdown arrow) moves to the LEFT side instead of the RIGHT side.
	 *          For large buttons: indicator appears at bottom-left instead of bottom-right,
	 *          text alignment is right-aligned instead of left/center-aligned,
	 *          and the icon remains centered (full-width rects are symmetric, mirroring preserves center position).
	 *          All x-coordinates are converted to relative positions within opt.rect, mirrored, then converted back.
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 为RTL布局镜像所有水平位置
	 * @details 在RTL模式下，指示器（下拉箭头）移到左侧而非右侧。
	 *          对于大按钮：指示器出现在左下角而非右下角，
	 *          文字右对齐而非左对齐/居中对齐，
	 *          图标保持居中（全宽矩形是对称的，镜像保持居中位置）。
	 *          所有x坐标先转换为opt.rect内的相对位置，再镜像，最后转换回绝对位置。
	 * \endif
	 */
	if (SA::saIsRTL()) {
		int containerWidth = opt.rect.width();
		int rectLeft       = opt.rect.x();
		// Mirror iconRect x position (full-width centered rects stay centered after mirroring)
		if (iconRect.isValid()) {
			int relX       = iconRect.x() - rectLeft;
			int mirroredX  = SA::saMirrorX(relX, containerWidth, iconRect.width());
			iconRect.moveLeft(rectLeft + mirroredX);
		}
		// Mirror textRect x position
		if (textRect.isValid()) {
			int relX       = textRect.x() - rectLeft;
			int mirroredX  = SA::saMirrorX(relX, containerWidth, textRect.width());
			textRect.moveLeft(rectLeft + mirroredX);
		}
		// Mirror indicatorArrowRect x position
		if (indicatorArrowRect.isValid()) {
			int relX       = indicatorArrowRect.x() - rectLeft;
			int mirroredX  = SA::saMirrorX(relX, containerWidth, indicatorArrowRect.width());
			indicatorArrowRect.moveLeft(rectLeft + mirroredX);
		}
	}
}

/**
 * @brief 适应iconsize
 *
 * 此函数会让originIconSize尽量适配buttonRect的大小
 * @param buttonRect
 * @param originIconSize
 * @return
 */
QSize SARibbonToolButton::PrivateData::adjustIconSize(const QRect& buttonRect, const QSize& originIconSize) const
{
	// 边界检查
	if (buttonRect.isEmpty() || originIconSize.isEmpty()) {
		return QSize(0, 0);
	}

	QSize iconSize = originIconSize;

	// 如果图标已经小于等于按钮区域，则直接返回
	if (iconSize.width() <= buttonRect.width() && iconSize.height() <= buttonRect.height()) {
		return iconSize;
	}

	// 计算宽高比
	qreal aspectRatio = static_cast< qreal >(originIconSize.width()) / originIconSize.height();

	// 先按按钮高度调整
	if (iconSize.height() > buttonRect.height()) {
		iconSize.setHeight(buttonRect.height());
		iconSize.setWidth(qRound(buttonRect.height() * aspectRatio));
	}

	// 再检查宽度是否超限
	if (iconSize.width() > buttonRect.width()) {
		iconSize.setWidth(buttonRect.width());
		iconSize.setHeight(qRound(buttonRect.width() / aspectRatio));
	}

	// 确保不会超过按钮边界
	iconSize.setWidth(qMin(iconSize.width(), buttonRect.width()));
	iconSize.setHeight(qMin(iconSize.height(), buttonRect.height()));

	return iconSize;
}

/**
 * @brief 判断是否有Indicator
 * @param opt
 * @return
 */
bool SARibbonToolButton::PrivateData::hasIndicator(const QStyleOptionToolButton& opt) const
{
	return ((opt.features & QStyleOptionToolButton::MenuButtonPopup) || (opt.features & QStyleOptionToolButton::HasMenu));
}

/**
 * @brief 计算sizehint
 *
 * 此函数非常关键，因为所有尺寸计算都是基于原始的rect来的
 * @param opt
 * @return
 */
QSize SARibbonToolButton::PrivateData::calcSizeHint(const QStyleOptionToolButton& opt)
{
	if (SARibbonToolButton::LargeButton == effectiveButtonType()) {
		return calcLargeButtonSizeHint(opt);
	}
	return calcSmallButtonSizeHint(opt);
}

QSize SARibbonToolButton::PrivateData::calcSmallButtonSizeHint(const QStyleOptionToolButton& opt)
{
	int w = 0, h = 0;

	switch (opt.toolButtonStyle) {
	case Qt::ToolButtonIconOnly: {
		w = opt.iconSize.width() + 2 * mSpacing;
		h = opt.iconSize.height() + 2 * mSpacing;
	} break;
	case Qt::ToolButtonTextOnly: {
		QSize textSize = opt.fontMetrics.size(Qt::TextShowMnemonic, simplifiedForRibbonButton(opt.text));
		textSize.setWidth(textSize.width() + SA::compat::horizontalAdvance(opt.fontMetrics, (QLatin1Char(' '))) * 2);
		textSize.setHeight(calcTextDrawRectHeight(opt));
		w = textSize.width() + 2 * mSpacing;
		h = textSize.height() + 2 * mSpacing;
	} break;
	default: {
		// 先加入icon的尺寸
		w = opt.iconSize.width() + 2 * mSpacing;
		h = opt.iconSize.height() + 2 * mSpacing;
		// 再加入文本的长度
		if (!opt.text.isEmpty()) {
			QSize textSize = opt.fontMetrics.size(Qt::TextShowMnemonic, simplifiedForRibbonButton(opt.text));
			textSize.setWidth(textSize.width() + SA::compat::horizontalAdvance(opt.fontMetrics, (QLatin1Char(' '))) * 2);
			textSize.setHeight(calcTextDrawRectHeight(opt));
			w += mSpacing;
			w += textSize.width();
			h = qMax(h, (textSize.height() + (2 * mSpacing)));
		} else {
			// 没有文本的时候也要设置一下高度
			QSize textSize = opt.fontMetrics.size(Qt::TextShowMnemonic, " ");
			h              = qMax(h, (textSize.height() + (2 * mSpacing)));
		}
	}
	}
	if (hasIndicator(opt)) {
		// 存在indicator的按钮，宽度尺寸要扩展
		w += mIndicatorLen;
	}
	if (w < SARibbonToolButtonConstants::MIN_BUTTON_WIDTH) {
		w = SARibbonToolButtonConstants::MIN_BUTTON_WIDTH;
	}
	//! Qt6.4 取消了QApplication::globalStrut
	return QSize(w, h).expandedTo(
		QSize(SARibbonToolButtonConstants::GLOBAL_STRUT_WIDTH, SARibbonToolButtonConstants::GLOBAL_STRUT_HEIGHT));
}

QSize SARibbonToolButton::PrivateData::calcLargeButtonSizeHint(const QStyleOptionToolButton& opt)
{
	int w    = 0;
	int h    = opt.fontMetrics.lineSpacing() * SARibbonToolButtonConstants::LARGE_BUTTON_HEIGHT_FACTOR;
	int minW = static_cast< int >(
		h * SARibbonToolButtonConstants::LARGE_BUTTON_MIN_WIDTH_RATIO);  // 最小宽度，在panel里面的按钮，最小宽度要和icon适应

	if (SARibbonPanel* panel = qobject_cast< SARibbonPanel* >(q_ptr->parent())) {
		// 对于建立在SARibbonPanel的基础上的大按钮，把高度设置为SARibbonPanel计算的大按钮高度
		h = panel->largeButtonHeight();
	}
	int textHeight = calcTextDrawRectHeight(opt);
	// 估算字体的宽度作为宽度
	w = estimateLargeButtonTextWidth(h, textHeight, opt.text, opt.fontMetrics);
	w += (2 * mSpacing);
	// 判断是否需要加上indicator
	if (q_ptr->isEnableWordWrap() && isTextNeedWrap()) {
		w += mIndicatorLen;
	}

#if SA_RIBBON_TOOLBUTTON_DEBUG_PRINT
	qDebug() << "| | |-SARibbonToolButton::PrivateData::calcLargeButtonSizeHint,text=" << opt.text
			 << "\n| | | |-lineSpacing*4.5=" << opt.fontMetrics.lineSpacing() * 4.5  //
			 << "\n| | | |-textHeight=" << textHeight                                //
			 << "\n| | | |-mDrawIconRect=" << mDrawIconRect                          //
			 << "\n| | | |-minW=" << minW                                            //
			 << "\n| | | |-w=" << w                                                  //
		;
#endif
	//! Qt6.4 取消了QApplication::globalStrut
	return QSize(w, h).expandedTo(QSize(minW, textHeight)
									  .expandedTo(QSize(SARibbonToolButtonConstants::GLOBAL_STRUT_WIDTH,
														SARibbonToolButtonConstants::GLOBAL_STRUT_HEIGHT)));
}

/**
 * @brief 计算文本高度
 * @param opt
 * @return
 */
int SARibbonToolButton::PrivateData::calcTextDrawRectHeight(const QStyleOptionToolButton& opt) const
{
	if (SARibbonToolButton::LargeButton == effectiveButtonType()) {
		if (q_ptr->isEnableWordWrap()) {
			return opt.fontMetrics.lineSpacing() * layoutFactor.twoLineHeightFactor + opt.fontMetrics.leading();
		} else {
			return opt.fontMetrics.lineSpacing() * layoutFactor.oneLineHeightFactor;
		}
	}
	// 小按钮
	return opt.rect.height() - SARibbonToolButtonConstants::SMALL_BUTTON_HEIGHT_OFFSET;
}

/**
 * @brief 估算一个最优的文字尺寸，在可以换行的情况下会进行换行，且只会换一行
 * @param buttonHeight 按钮的高度
 * @param textDrawRectHeight 文本绘制的高度
 * @param fm QFontMetrics
 * @param widthHeightRatio 宽高比，宽度/高度的比值，如果大于这个比值，则会进行尝试换行以获取更低的宽度
 * @param maxTrycount 尝试次数 (保留参数以保持兼容性，但内部使用二分查找)
 * @return
 */
int SARibbonToolButton::PrivateData::estimateLargeButtonTextWidth(int buttonHeight,
																  int textDrawRectHeight,
																  const QString& text,
																  const QFontMetrics& fm,
																  int maxTrycount)
{
	Q_UNUSED(maxTrycount)  // 现在使用二分查找，不再需要尝试次数

	QSize textSize;
	int space        = SA::compat::horizontalAdvance(fm, (QLatin1Char(' '))) * 2;
	int hintMaxWidth = qMin(static_cast< int >(buttonHeight * layoutFactor.buttonMaximumAspectRatio),
							q_ptr->maximumWidth());  ///< 建议的宽度

	if (q_ptr->isEnableWordWrap()) {
		textSize = fm.size(Qt::TextShowMnemonic, text);
		textSize.setWidth(textSize.width() + space);

		if (textSize.height() > fm.lineSpacing() * 1.1) {
			//! 说明文字带有换行符，是用户手动换行，这种情况就直接返回字体尺寸，不进行估算
			mIsTextNeedWrap = true;  // 文字需要换行显示，标记起来
			return textSize.width();
		}

		// 这时候需要估算文本的长度
		if (textSize.width() <= hintMaxWidth) {
			// 范围合理，直接返回
			mIsTextNeedWrap = false;  // 文字不需要换行显示，标记起来
			return textSize.width();
		}

		//! 大于宽高比尝试进行文字换行
		//! 使用二分查找找到最优宽度
		int alignment = Qt::TextShowMnemonic | Qt::TextWordWrap;
		int minWidth  = textSize.width() / 2;  // 最小尝试宽度（一半）
		int maxWidth  = textSize.width();      // 最大宽度（原始宽度）
		int bestWidth = maxWidth;              // 最佳宽度

		// 二分查找，最多10次迭代
		for (int i = 0; i < 10; ++i) {
			int midWidth = (minWidth + maxWidth) / 2;
			QRect textRect(0, 0, midWidth, textDrawRectHeight);
			textRect = fm.boundingRect(textRect, alignment, text);

			if (textRect.height() <= fm.lineSpacing() * 2) {
				// 可以在两行内显示，尝试更小的宽度
				bestWidth = midWidth;
				maxWidth  = midWidth - 1;
			} else {
				// 需要更多行，尝试更大的宽度
				minWidth = midWidth + 1;
			}

			if (minWidth > maxWidth) {
				break;
			}
		}

		mIsTextNeedWrap = true;  // 文字需要换行显示，标记起来
		return bestWidth;
	}

	//! 说明是不换行

	mIsTextNeedWrap = false;  // 文字不需要换行显示，标记起来
							  // 文字不换行情况下，做simplified处理
	textSize = fm.size(Qt::TextShowMnemonic, simplifiedForRibbonButton(text));
	textSize.setWidth(textSize.width() + space);
	if (textSize.width() < hintMaxWidth) {
		// 范围合理，直接返回
		return textSize.width();
	}
	return hintMaxWidth;
}

QPixmap SARibbonToolButton::PrivateData::createIconPixmap(const QStyleOptionToolButton& opt, const QSize& iconsize) const
{
	if (opt.icon.isNull()) {  // 没有图标
		return QPixmap();
	}

	QIcon::State state = (opt.state & QStyle::State_On) ? QIcon::On : QIcon::Off;
	QIcon::Mode mode;
	if (!(opt.state & QStyle::State_Enabled)) {
		mode = QIcon::Disabled;
	} else if ((opt.state & QStyle::State_MouseOver) && (opt.state & QStyle::State_AutoRaise)) {
		mode = QIcon::Active;
	} else {
		mode = QIcon::Normal;
	}

	// 检查缓存是否有效
	if (mIconCacheValid && mCachedIconSize == iconsize && mCachedIconMode == mode && mCachedIconState == state) {
		return mCachedIconPixmap;
	}

	// 生成新的pixmap并缓存
	mCachedIconPixmap = SA::iconToPixmap(opt.icon, iconsize, SA::widgetDevicePixelRatio(q_ptr), mode, state);
	mCachedIconSize   = iconsize;
	mCachedIconMode   = mode;
	mCachedIconState  = state;
	mIconCacheValid   = true;

	return mCachedIconPixmap;
}

int SARibbonToolButton::PrivateData::getTextAlignment() const
{
	if (q_ptr->toolButtonStyle() == Qt::ToolButtonTextOnly) {
		return Qt::TextShowMnemonic | Qt::AlignCenter;
	}

	if (SARibbonToolButton::LargeButton == effectiveButtonType()) {
		return Qt::TextShowMnemonic
			   | (q_ptr->isEnableWordWrap() ? (Qt::TextWordWrap | Qt::AlignTop | Qt::AlignHCenter) : Qt::AlignCenter);
	}

	return Qt::TextShowMnemonic | Qt::AlignCenter;
}

/**
 * @brief 确认文字是否确切要换行显示
 * @return
 */
bool SARibbonToolButton::PrivateData::isTextNeedWrap() const
{
	return mIsTextNeedWrap;
}

/**
 * @brief 获取正真的icon尺寸
 * @return
 */
QSize SARibbonToolButton::PrivateData::realIconSize() const
{
	if (effectiveButtonType() == SARibbonToolButton::LargeButton) {
		return mLargeButtonSizeHint;
	}
	return q_ptr->smallIconSize();
}

/**
 * @brief 仅仅对\n进行剔除
 * @param str
 * @return
 */
QString SARibbonToolButton::PrivateData::simplifiedForRibbonButton(const QString& str)
{
	QString res = str;
	res.remove('\n');
	return res;
}

//===================================================
// SARibbonToolButton
//===================================================

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonToolButton
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonToolButton构造函数
 * @param parent 父窗口部件
 * \endif
 */
SARibbonToolButton::SARibbonToolButton(QWidget* parent)
	: QToolButton(parent), d_ptr(new SARibbonToolButton::PrivateData(this))
{
	// 静态设置也是可以，虽然节省内存，但不清楚未来qt是否会针对绘制有潜在的多线程处理的可能性，因此这里还是使用成员变量
	// static SARibbonToolButtonProxyStyle* ss_style = new SARibbonToolButtonProxyStyle();
	// setStyle(ss_style);

	// setStyle方法不会接管样式的所有权，因此要手动删除，这里使用智能指针
	// 注意：setStyle不会获取所有权，所以使用get()是安全的，样式对象生命周期由unique_ptr管理
	setStyle(d_ptr->mStyle.get());
	setAutoRaise(true);
	setToolButtonStyle(Qt::ToolButtonTextBesideIcon);
	setButtonType(SmallButton);
	setMouseTracking(true);
}

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonToolButton with default action
 * @param defaultAction Default action for the button
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonToolButton构造函数（带默认动作）
 * @param defaultAction 按钮的默认动作
 * @param parent 父窗口部件
 * \endif
 */
SARibbonToolButton::SARibbonToolButton(QAction* defaultAction, QWidget* parent)
	: QToolButton(parent), d_ptr(new SARibbonToolButton::PrivateData(this))
{
	setAutoRaise(true);
	setToolButtonStyle(Qt::ToolButtonTextBesideIcon);
	setDefaultAction(defaultAction);
	setButtonType(SmallButton);
	setMouseTracking(true);
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonToolButton
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonToolButton析构函数
 * \endif
 */
SARibbonToolButton::~SARibbonToolButton()
{
}

/**
 * @brief Sets the layout factor for fine-tuning the button's appearance / 设置布局系数以微调按钮外观
 *
 * This function allows you to customize the button's text height and maximum aspect ratio.
 * After calling this function, the button's geometry will be invalidated to trigger a relayout.
 *
 * 此函数允许您自定义按钮的文本高度和最大宽高比。
 * 调用此函数后，按钮的几何尺寸将被标记为无效，以触发重新布局。
 *
 * Example:
 * @code
 * SARibbonToolButton::LayoutFactor lf;
 * lf.twoLineHeightFactor = 2.2; // Make two-line text taller/让两行文字更高
 * lf.buttonMaximumAspectRatio = 1.6; // Allow a wider button/允许按钮更宽
 * myRibbonButton->setLayoutFactor(lf);
 * @endcode
 *
 * @param fac The new layout factor / 新的布局系数
 * @sa layoutFactor, setButtonMaximumAspectRatio
 */
void SARibbonToolButton::setLayoutFactor(const SARibbonToolButton::LayoutFactor& fac)
{
	d_ptr->layoutFactor = fac;
	// 重新布局
	invalidateSizeHint();
	// 触发重绘以应用新的布局因子
	update();
}

/**
 * @brief Gets a const reference to the current layout factor / 获取当前布局系数的常量引用
 * @return A const reference to the layout factor / 布局系数的常量引用
 * @sa setLayoutFactor, setButtonMaximumAspectRatio
 */
const SARibbonToolButton::LayoutFactor& SARibbonToolButton::layoutFactor() const
{
	return d_ptr->layoutFactor;
}

/**
 * @brief Gets a mutable reference to the current layout factor / 获取当前布局系数的可变引用
 * @return A mutable reference to the layout factor / 布局系数的可变引用
 * @sa setLayoutFactor, setButtonMaximumAspectRatio
 */
SARibbonToolButton::LayoutFactor& SARibbonToolButton::layoutFactor()
{
	return d_ptr->layoutFactor;
}

/**
 * @brief Gets the current button type (LargeButton or SmallButton) / 获取当前按钮的类型（大按钮或小按钮）
 * @return The current button type / 当前按钮类型
 * @sa setButtonType
 */
/**
 * \if ENGLISH
 * @brief Get the current button type
 * @return Current button type (LargeButton or SmallButton)
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前按钮的类型
 * @return 当前按钮类型（大按钮或小按钮）
 * \endif
 */
SARibbonToolButton::RibbonButtonType SARibbonToolButton::buttonType() const
{
	return (d_ptr->mButtonType);
}

/**
 * \if ENGLISH
 * @brief Sets the button type to LargeButton or SmallButton
 *
 * Changing the button type will invalidate the size hint and trigger a relayout.
 * Note: This function may override the tool button style. If you need to set a specific style (e.g.,
 * Qt::ToolButtonIconOnly), do so after calling this function.
 * @param buttonType The new button type
 * @sa isLargeRibbonButton, isSmallRibbonButton
 * \endif
 *
 * \if CHINESE
 * @brief 设置按钮类型为大按钮或小按钮
 *
 * 设置按钮类型会令尺寸提示失效并触发重新布局。
 * 注意：此函数可能会覆盖工具按钮样式。如需设置特定样式（例如 Qt::ToolButtonIconOnly），请在此函数调用之后设置。
 * @param buttonType 新的按钮类型
 * @sa isLargeRibbonButton, isSmallRibbonButton
 * \endif
 */
void SARibbonToolButton::setButtonType(const RibbonButtonType& buttonType)
{
	d_ptr->mButtonType = buttonType;
	// 计算iconrect
	// 根据字体计算文字的高度

	if (LargeButton == buttonType) {
		setSizePolicy(QSizePolicy::Maximum, QSizePolicy::Expanding);
	} else {
		setSizePolicy(QSizePolicy::Preferred, QSizePolicy::Maximum);
	}
	invalidateSizeHint();
}

/**
 * @brief Checks if the button is a small ribbon button / 判断按钮是否为小Ribbon按钮
 * @return `true` if the button type is `SmallButton`; otherwise `false` / 如果按钮类型为 `SmallButton` 则返回 `true`；否则返回 `false`
 * @sa isLargeRibbonButton, buttonType
 */
bool SARibbonToolButton::isSmallRibbonButton() const
{
	return (d_ptr->mButtonType == SmallButton);
}

/**
 * @brief Checks if the button is a large ribbon button / 判断按钮是否为大Ribbon按钮
 * @return `true` if the button type is `LargeButton`; otherwise `false` / 如果按钮类型为 `LargeButton` 则返回 `true`；否则返回 `false`
 * @sa isSmallRibbonButton, buttonType
 */
bool SARibbonToolButton::isLargeRibbonButton() const
{
	return (d_ptr->mButtonType == LargeButton);
}

/**
 * @brief Gets the current spacing value / 获取当前的间距值
 *
 * Spacing is the gap between the icon, text, indicator, and the button's border.
 *
 * 间距是图标、文字、指示器与按钮边框之间的间隙。
 *
 * @return The current spacing in pixels / 当前的间距值（像素）
 * @sa setSpacing
 */
int SARibbonToolButton::spacing() const
{
	return d_ptr->mSpacing;
}

/**
 * @brief Sets the spacing between elements and the border / 设置元素与边框之间的间距
 *
 * This spacing affects the layout of the icon, text, and indicator within the button.
 * After calling this function, the button's geometry will be invalidated to trigger a relayout.
 *
 * 此间距会影响按钮内图标、文字和指示器的布局。
 * 调用此函数后，按钮的几何尺寸将被标记为无效，以触发重新布局。
 *
 * @param v The new spacing value in pixels / 新的间距值（像素）
 * @sa spacing
 */
void SARibbonToolButton::setSpacing(int v)
{
	d_ptr->mSpacing = v;
	invalidateSizeHint();
}

/**
 * @brief Forces an update of the internal layout rectangles / 强制更新内部布局矩形
 *
 * This function recalculates the drawing rectangles for the icon, text, and indicator based on the current button
 * size and style. It also invalidates the cached size hint. This is typically called automatically during a resize
 * event.
 *
 * 此函数会根据当前按钮尺寸和样式，重新计算图标、文字和指示器的绘制矩形。同时会使缓存的尺寸提示失效。
 * 此函数通常在调整大小事件中被自动调用。
 *
 * @note This function invalidates the size hint cache but does not call `updateGeometry()`. If you need to trigger
 * a parent layout update, call `updateGeometry()` manually after this function.
 * / 此函数会清除 sizehint 缓存，但不会调用 updateGeometry()。如果需要触发布局更新，应在调用此函数后手动调用 updateGeometry()。
 */
void SARibbonToolButton::updateRect()
{
	QStyleOptionToolButton opt;
	initStyleOption(&opt);
	d_ptr->updateDrawRect(opt);
	// 这里不调用invalidateSizeHint();因为nvalidateSizeHint();会调用updateGeometry函数，导致父窗口再次布局
	d_ptr->mSizeHint = QSize();
}

/**
 * @brief Enables or disables automatic text wrapping for large buttons / 为大按钮启用或禁用自动文字换行
 *
 * When enabled, the text in a large button will attempt to wrap onto a second line if it is too long to fit on one line.
 * This is particularly useful for long action names in the Ribbon interface.
 * The button's size hint will be recalculated after calling this function.
 *
 * 启用后，如果大按钮中的文字过长无法在一行内显示，将尝试换行到第二行。
 * 这在Ribbon界面中处理较长的操作名称时非常有用。
 * 调用此函数后，按钮的size hint将被重新计算。
 *
 * Example:
 * @code
 * // Enable word wrap for a button with a potentially long label/为一个可能有长标签的按钮启用文字换行
 * myLongLabelButton->setEnableWordWrap(true);
 * @endcode
 *
 * @param on `true` to enable word wrap; `false` to disable it / `true` 启用换行，`false` 禁用换行
 * @sa isEnableWordWrap
 */
void SARibbonToolButton::setEnableWordWrap(bool on)
{
	d_ptr->mWordWrap = on;
	// 通知父布局需要重新布局
	invalidateSizeHint();
}

/**
 * @brief Checks if automatic text wrapping is enabled / 检查是否启用了自动文字换行
 * @return `true` if word wrap is enabled; otherwise `false` / 如果启用了文字换行则返回 `true`；否则返回 `false`
 * @sa setEnableWordWrap
 */
bool SARibbonToolButton::isEnableWordWrap() const
{
	return d_ptr->mWordWrap;
}

/**
 * \if ENGLISH
 * @brief Set whether text is displayed to the right of the icon
 * @param on If true, the button uses horizontal layout (icon-left, text-right),
 *           regardless of the current RibbonButtonType.
 *           When false, the button uses the default layout based on RibbonButtonType.
 * @details When this mode is enabled, LargeButton type buttons are rendered with
 *           the SmallButton horizontal layout strategy. The button's mButtonType
 *           remains unchanged internally.
 * \endif
 *
 * \if CHINESE
 * @brief 设置文字是否显示在图标右侧
 * @param on 如果为true，按钮使用水平布局（图标在左，文字在右），
 *           不受当前RibbonButtonType的影响。
 *           当为false时，按钮根据RibbonButtonType使用默认布局。
 * @details 启用此模式时，LargeButton类型的按钮使用SmallButton的水平布局策略渲染。
 *           按钮的mButtonType在内部保持不变。
 * \endif
 */
void SARibbonToolButton::setEnableIconRightText(bool on)
{
	SA_D(d);
	if (d->enableIconRightText == on) {
		return;
	}
	d->enableIconRightText = on;
	// When enableIconRightText changes, update size policy to match effective type
	if (on) {
		// Force horizontal layout - use SmallButton-style size policy
		setSizePolicy(QSizePolicy::Preferred, QSizePolicy::Maximum);
	} else {
		// Restore size policy based on actual button type
		if (LargeButton == d->mButtonType) {
			setSizePolicy(QSizePolicy::Maximum, QSizePolicy::Expanding);
		} else {
			setSizePolicy(QSizePolicy::Preferred, QSizePolicy::Maximum);
		}
	}
	invalidateSizeHint();
}

bool SARibbonToolButton::isEnableIconRightText() const
{
	SA_DC(d);
	return d->enableIconRightText;
}

/**
 * @brief Sets the button's maximum aspect ratio (width/height) / 设置按钮的最大宽高比
 *
 * This is a convenience function that directly sets the `buttonMaximumAspectRatio` member of the `LayoutFactor`
 * structure. It has the same effect as modifying the structure and calling `setLayoutFactor`.
 *
 * 此函数是直接设置 `LayoutFactor` 结构体中 `buttonMaximumAspectRatio` 成员的便捷方法。
 * 其效果等同于修改结构体后调用 `setLayoutFactor`。
 *
 * @param v The new maximum aspect ratio value / 新的最大宽高比值
 * @sa buttonMaximumAspectRatio, setLayoutFactor
 */
void SARibbonToolButton::setButtonMaximumAspectRatio(qreal v)
{
	d_ptr->layoutFactor.buttonMaximumAspectRatio = v;
	// 重新布局
	invalidateSizeHint();
}

/**
 * @brief Gets the button's maximum aspect ratio (width/height) / 获取按钮的最大宽高比
 * @return The current maximum aspect ratio / 当前的最大宽高比
 * @sa setButtonMaximumAspectRatio, layoutFactor
 */
qreal SARibbonToolButton::buttonMaximumAspectRatio() const
{
	return layoutFactor().buttonMaximumAspectRatio;
}

bool SARibbonToolButton::event(QEvent* e)
{
	switch (e->type()) {
	case QEvent::WindowDeactivate:
		d_ptr->mMouseOnSubControl = false;
		break;
	case QEvent::ActionChanged:
	case QEvent::ActionRemoved:
	case QEvent::ActionAdded: {
		d_ptr->mMouseOnSubControl = false;
		invalidateSizeHint();
	} break;
	default:
		break;
	}

	return (QToolButton::event(e));
}

void SARibbonToolButton::changeEvent(QEvent* e)
{
	if (e) {
		switch (e->type()) {
		case QEvent::FontChange:
		case QEvent::StyleChange:
		case QEvent::LanguageChange: {
			// 说明字体改变，需要重新计算和字体相关的信息
			invalidateSizeHint();
		} break;
		case QEvent::LayoutDirectionChange: {
			// 布局方向改变（如 LTR→RTL），重新计算绘制矩形和尺寸提示
			invalidateSizeHint();
			update();
		} break;
		case QEvent::ScreenChangeInternal:
#if QT_VERSION >= QT_VERSION_CHECK(6, 6, 0)
		case QEvent::DevicePixelRatioChange:
#endif
		{
			invalidateSizeHint();
			break;
		}
		default:
			break;
		}
	}
	QToolButton::changeEvent(e);
}

/**
 * @brief 鼠标移动事件
 *
 * 由于Ribbon的Indicator和正常的Toolbutton不一样，因此无法用QStyleOptionToolButton的activeSubControls的状态
 *
 * 因此需要重新捕获鼠标的位置来更新按钮当前的一些状态
 * @param e
 */
void SARibbonToolButton::mouseMoveEvent(QMouseEvent* e)
{
	bool oldMouseOnSubControl = d_ptr->mMouseOnSubControl;
	d_ptr->updateStatusByMousePosition(SA::compat::eventPos(e));
	// 如果鼠标进入/离开子控件，图标状态可能改变，使缓存失效
	if (oldMouseOnSubControl != d_ptr->mMouseOnSubControl) {
		d_ptr->invalidateIconCache();
	}
	QToolButton::mouseMoveEvent(e);
}

/**
 * @brief SARibbonToolButton::mousePressEvent
 * @param e
 */
void SARibbonToolButton::mousePressEvent(QMouseEvent* e)
{
	if ((e->button() == Qt::LeftButton) && (popupMode() == MenuButtonPopup)) {
		d_ptr->updateStatusByMousePosition(SA::compat::eventPos(e));
		if (d_ptr->mMouseOnSubControl) {
			d_ptr->mMenuButtonPressed = true;
			showMenu();
			// showmenu结束后，在判断当前的鼠标位置是否是在subcontrol
			d_ptr->updateStatusByMousePosition(mapFromGlobal(QCursor::pos()));
			return;
		}
	}
	d_ptr->mMenuButtonPressed = false;
	//! 注意这里要用QAbstractButton的mousePressEvent，而不是QToolButton的mousePressEvent
	//! QToolButton的mousePressEvent主要是为了弹出菜单，这里弹出菜单的方式是不一样的，因此不能执行QToolButton的mousePressEvent
	QToolButton::mousePressEvent(e);
}

void SARibbonToolButton::mouseReleaseEvent(QMouseEvent* e)
{
	d_ptr->mMenuButtonPressed = false;
	QToolButton::mouseReleaseEvent(e);
}

void SARibbonToolButton::focusOutEvent(QFocusEvent* e)
{
	d_ptr->mMouseOnSubControl = false;
	QToolButton::focusOutEvent(e);
}

void SARibbonToolButton::leaveEvent(QEvent* e)
{
	d_ptr->mMouseOnSubControl = false;
	d_ptr->invalidateIconCache();  // 鼠标离开，图标状态改变
	QToolButton::leaveEvent(e);
}

bool SARibbonToolButton::hitButton(const QPoint& pos) const
{
	if (QToolButton::hitButton(pos)) {
		return (!d_ptr->mMenuButtonPressed);
	}
	return (false);
}

/**
 * @brief 在resizeevent计算绘图所需的尺寸，避免在绘图过程中实时绘制提高效率
 * @param e
 */
void SARibbonToolButton::resizeEvent(QResizeEvent* e)
{
	// 在resizeevent计算绘图所需的尺寸，避免在绘图过程中实时绘制提高效率
	QToolButton::resizeEvent(e);
	updateRect();
}

/**
 * @brief Returns the recommended size for the button / 返回按钮的推荐尺寸
 *
 * This size is calculated based on the button's type, text, icon, and current layout factors.
 * The result is cached for performance. The cache is invalidated when relevant properties change.
 *
 * 此尺寸是根据按钮的类型、文字、图标和当前布局系数计算得出的。
 * 为提高性能，计算结果会被缓存。当相关属性改变时，缓存会自动失效。
 *
 * @return The recommended size / 推荐的尺寸
 */
QSize SARibbonToolButton::sizeHint() const
{
#if SA_RIBBON_TOOLBUTTON_DEBUG_PRINT
	qDebug() << "| | |-SARibbonToolButton::sizeHint";
#endif
	if (d_ptr->mSizeHint.isValid()) {
		return d_ptr->mSizeHint;
	}
	QStyleOptionToolButton opt;
	initStyleOption(&opt);
	d_ptr->updateSizeHint(opt);
	return d_ptr->mSizeHint;
}

/**
 * @brief Returns the recommended minimum size for the button / 返回按钮的推荐最小尺寸
 *
 * For `SARibbonToolButton`, the minimum size hint is the same as the size hint.
 *
 * 对于 `SARibbonToolButton`，最小尺寸提示与尺寸提示相同。
 *
 * @return The recommended minimum size / 推荐的最小尺寸
 */
QSize SARibbonToolButton::minimumSizeHint() const
{
	return (sizeHint());
}

void SARibbonToolButton::actionEvent(QActionEvent* e)
{
	QToolButton::actionEvent(e);
	invalidateSizeHint();
	d_ptr->invalidateIconCache();  // action改变，图标可能改变
}

void SARibbonToolButton::paintEvent(QPaintEvent* e)
{
	Q_UNUSED(e);
	QPainter p(this);
	QStyleOptionToolButton opt;
	initStyleOption(&opt);
	if (opt.features & QStyleOptionToolButton::MenuButtonPopup || opt.features & QStyleOptionToolButton::HasMenu) {
		// 在菜单弹出消失后，需要通过此方法取消掉鼠标停留
		if (!rect().contains(mapFromGlobal(QCursor::pos()))) {
			opt.state &= ~QStyle::State_MouseOver;
		}
	}
	paintButton(p, opt);
	paintIcon(p, opt, d_ptr->mDrawIconRect);
	paintText(p, opt, d_ptr->mDrawTextRect);
	paintIndicator(p, opt, d_ptr->mDrawIndicatorArrowRect);
}

/**
 * @brief Paints the button's background and frame / 绘制按钮的背景和边框
 *
 * This function handles the special visual effects for the Ribbon style, particularly for the `MenuButtonPopup`
 * mode where the icon and text areas can have different hover states.
 *
 * 此函数处理Ribbon样式的特殊视觉效果，特别是在 `MenuButtonPopup` 模式下，图标和文字区域可以有不同的悬停状态。
 *
 * @param p The painter to use / 用于绘制的painter
 * @param opt The style option for the tool button / 工具按钮的样式选项
 */
void SARibbonToolButton::paintButton(QPainter& p, const QStyleOptionToolButton& opt)
{
	// QStyle::State_Sunken 代表按钮按下去了
	// QStyle::State_On 代表按钮按checked
	// QStyle::State_MouseOver 代表当前鼠标位于按钮上面
	QStyleOption tool = opt;
	bool autoRaise    = opt.state & QStyle::State_AutoRaise;
	// 绘制按钮
	if (autoRaise) {
		// 这个是为了实现按钮点击下去后(QStyle::State_Sunken),能出现选中的状态
		// 先绘制一个鼠标不在按钮上的状态
		if (opt.state & QStyle::State_Sunken) {
			tool.state &= ~QStyle::State_MouseOver;
		}
		style()->drawPrimitive(QStyle::PE_PanelButtonTool, &tool, &p, this);
	} else {
		style()->drawPrimitive(QStyle::PE_PanelButtonBevel, &tool, &p, this);
	}
	// 针对MenuButtonPopup的ribbon样式的特殊绘制
	if ((opt.subControls & QStyle::SC_ToolButton) && (opt.features & QStyleOptionToolButton::MenuButtonPopup)) {
		if (opt.state & QStyle::State_MouseOver) {                       // 鼠标在按钮上才进行绘制
			if (!(opt.activeSubControls & QStyle::SC_ToolButtonMenu)) {  // 按钮的菜单弹出时不做处理
				if (LargeButton == d_ptr->effectiveButtonType()) {                 // 大按钮模式
					if (d_ptr->mMouseOnSubControl) {                     // 此时鼠标在indecater那
						// 鼠标在文字区，把图标显示为正常（就是鼠标不放上去的状态）
						tool.rect = d_ptr->mDrawIconRect;
						tool.state |= (QStyle::State_Raised);  // 把图标区域显示为正常
						tool.state &= ~QStyle::State_MouseOver;
						if (autoRaise) {
							style()->drawPrimitive(QStyle::PE_PanelButtonTool, &tool, &p, this);
						} else {
							style()->drawPrimitive(QStyle::PE_PanelButtonBevel, &tool, &p, this);
						}
					} else {
						// 鼠标在图标区，把文字显示为正常
						if (!tool.state.testFlag(QStyle::State_Sunken)) {
							// State_Sunken说明此按钮正在按下，这时候，文本区域不需要绘制，只有在非按下状态才需要绘制
							tool.state |= (QStyle::State_Raised);  // 把图标区域显示为正常
							tool.state &= ~QStyle::State_MouseOver;
							// 文字和Indicator都显示正常
							tool.rect = d_ptr->mDrawTextRect.united(d_ptr->mDrawIndicatorArrowRect);
							if (autoRaise) {
								style()->drawPrimitive(QStyle::PE_PanelButtonTool, &tool, &p, this);
							} else {
								style()->drawPrimitive(QStyle::PE_PanelButtonBevel, &tool, &p, this);
							}
						}
					}
				} else {                              // 小按钮模式
					if (d_ptr->mMouseOnSubControl) {  // 此时鼠标在indecater那
						// 鼠标在文字区，把图标和文字显示为正常
						tool.rect  = d_ptr->mDrawIconRect.united(d_ptr->mDrawTextRect);
						tool.state = (QStyle::State_Raised);  // 把图标区域显示为正常
						if (autoRaise) {
							style()->drawPrimitive(QStyle::PE_PanelButtonTool, &tool, &p, this);
						} else {
							style()->drawPrimitive(QStyle::PE_PanelButtonBevel, &tool, &p, this);
						}
					} else {
						// 鼠标在图标区，把文字显示为正常
						tool.state = (QStyle::State_Raised);  // 把图标区域显示为正常
						// 文字和Indicator都显示正常
						tool.rect = d_ptr->mDrawIndicatorArrowRect;
						if (autoRaise) {
							style()->drawPrimitive(QStyle::PE_PanelButtonTool, &tool, &p, this);
						} else {
							style()->drawPrimitive(QStyle::PE_PanelButtonBevel, &tool, &p, this);
						}
					}
				}
			}
		}
	}
	// 绘制Focus
	//     if (opt.state & QStyle::State_HasFocus) {
	//         QStyleOptionFocusRect fr;
	//         fr.QStyleOption::operator=(opt);
	//         fr.rect.adjust(d_ptr->mSpacing, d_ptr->mSpacing, -d_ptr->mSpacing, -d_ptr->mSpacing);
	//         style()->drawPrimitive(QStyle::PE_FrameFocusRect, &fr, &p, this);
	//     }
}

/**
 * @brief Paints the button's icon / 绘制按钮的图标
 *
 * The icon is painted within the specified rectangle, scaled appropriately based on the available space.
 *
 * 图标会在指定的矩形区域内绘制，并根据可用空间进行适当缩放。
 *
 * @param p The painter to use / 用于绘制的painter
 * @param opt The style option for the tool button / 工具按钮的样式选项
 * @param iconDrawRect The rectangle in which to draw the icon / 绘制图标的矩形区域
 */
void SARibbonToolButton::paintIcon(QPainter& p, const QStyleOptionToolButton& opt, const QRect& iconDrawRect)
{
	if (!iconDrawRect.isValid()) {
		return;
	}

	QPixmap pm = createIconPixmap(opt, d_ptr->realIconSize());
	style()->drawItemPixmap(&p, iconDrawRect, Qt::AlignCenter, pm);
	SARIBBONTOOLBUTTON_DEBUG_DRAW_RECT(p, iconDrawRect);
}

/**
 * @brief 创建图标pixmap，子类可以重写此函数以自定义图标绘制
 */
QPixmap SARibbonToolButton::createIconPixmap(const QStyleOptionToolButton& opt, const QSize& iconSize) const
{
	return d_ptr->createIconPixmap(opt, iconSize);
}

/**
 * @brief Paints the button's text / 绘制按钮的文字
 *
 * The text is painted within the specified rectangle, with alignment and elision (truncation with "...") handled
 * according to the button's type and word-wrap setting.
 *
 * 文字会在指定的矩形区域内绘制，其对齐方式和省略（用“...”截断）会根据按钮的类型和文字换行设置进行处理。
 *
 * @param p The painter to use / 用于绘制的painter对象
 * @param opt The style option for the tool button / 工具按钮的样式选项
 * @param textDrawRect The rectangle in which to draw the text / 绘制文字的矩形区域
 */
void SARibbonToolButton::paintText(QPainter& p, const QStyleOptionToolButton& opt, const QRect& textDrawRect)
{
	int alignment = d_ptr->getTextAlignment();

	if (!style()->styleHint(QStyle::SH_UnderlineShortcut, &opt, this)) {
		alignment |= Qt::TextHideMnemonic;
	}
	QString text;
	if (d_ptr->effectiveButtonType() == SARibbonToolButton::SmallButton) {
		text = opt.fontMetrics.elidedText(
			PrivateData::simplifiedForRibbonButton(opt.text), Qt::ElideRight, textDrawRect.width(), alignment);
	} else {
		if (!isEnableWordWrap()) {
			text = opt.fontMetrics.elidedText(
				PrivateData::simplifiedForRibbonButton(opt.text), Qt::ElideRight, textDrawRect.width(), alignment);
		} else {
			text = opt.text;
		}
	}
	//! 以下内容参考QCommonStyle.cpp
	//! void QCommonStyle::drawComplexControl(ComplexControl cc, const QStyleOptionComplex *opt,QPainter *p, const QWidget *widget) const
	//! case CC_ToolButton:
	QStyle::State bflags = opt.state & ~QStyle::State_Sunken;
	if (bflags & QStyle::State_AutoRaise) {
		if (!(bflags & QStyle::State_MouseOver) || !(bflags & QStyle::State_Enabled)) {
			bflags &= ~QStyle::State_Raised;
		}
	}
	if (opt.state & QStyle::State_Sunken) {
		if (opt.activeSubControls & QStyle::SC_ToolButton) {
			bflags |= QStyle::State_Sunken;
		}
	}
	QStyleOptionToolButton label = opt;
	label.state                  = bflags;
	style()->drawItemText(
		&p, textDrawRect, alignment, label.palette, label.state & QStyle::State_Enabled, text, foregroundRole());
	SARIBBONTOOLBUTTON_DEBUG_DRAW_RECT(p, textDrawRect);
}

/**
 * @brief Paints the button's indicator (e.g., dropdown arrow) / 绘制按钮的指示器（例如下拉箭头）
 *
 * The indicator is painted within the specified rectangle if the button has a menu (i.e., features include
 * `MenuButtonPopup` or `HasMenu`).
 *
 * 如果按钮有菜单（即特性包含 `MenuButtonPopup` 或 `HasMenu`），则会在指定的矩形区域内绘制指示器。
 *
 * @param p The painter to use / 用于绘制的painter对象
 * @param opt The style option for the tool button / 工具按钮的样式选项
 * @param indicatorDrawRect The rectangle in which to draw the indicator / 绘制指示器的矩形区域
 */
void SARibbonToolButton::paintIndicator(QPainter& p, const QStyleOptionToolButton& opt, const QRect& indicatorDrawRect)
{
	if (!indicatorDrawRect.isValid() || !d_ptr->hasIndicator(opt)) {
		return;
	}

	QStyleOption tool = opt;
	tool.rect         = indicatorDrawRect;
	style()->drawPrimitive(QStyle::PE_IndicatorArrowDown, &tool, &p, this);
	SARIBBONTOOLBUTTON_DEBUG_DRAW_RECT(p, indicatorDrawRect);
}

/**
 * @brief Invalidates the cached size hint / 使缓存的size hint失效
 *
 * This function clears the internally cached `sizeHint()` value and calls `updateGeometry()`,
 * which notifies the layout system that this widget needs to be relayouted.
 * It is called automatically when properties affecting the size (like text, font, or button type) change.
 *
 * 此函数会清除内部缓存的 `sizeHint()` 值并调用 `updateGeometry()`，
 * 通知布局系统此控件需要重新布局。
 * 当影响尺寸的属性（如文字、字体或按钮类型）发生变化时，会自动调用此函数。
 */
void SARibbonToolButton::invalidateSizeHint()
{
	d_ptr->mSizeHint = QSize();
	updateGeometry();
}

/**
 * @brief 大按钮的尺寸
 * @param largeSize
 */
void SARibbonToolButton::setLargeIconSize(const QSize& largeSize)
{
	d_ptr->mLargeButtonSizeHint = largeSize;
}

/**
 * @brief 大按钮的尺寸
 * @return
 */
QSize SARibbonToolButton::largeIconSize() const
{
	return d_ptr->mLargeButtonSizeHint;
}

/**
 * @brief 小按钮尺寸
 * @param smallSize
 */
void SARibbonToolButton::setSmallIconSize(const QSize& smallSize)
{
	setIconSize(smallSize);
}

/**
 * @brief 小按钮尺寸
 * @return
 */
QSize SARibbonToolButton::smallIconSize() const
{
	return iconSize();
}

void SARibbonToolButton::drawArrow(const QStyle* style,
								   const QStyleOptionToolButton* toolbutton,
								   const QRect& rect,
								   QPainter* painter,
								   const QWidget* widget)
{
	QStyle::PrimitiveElement pe;

	switch (toolbutton->arrowType) {
	case Qt::LeftArrow:
		pe = QStyle::PE_IndicatorArrowLeft;
		break;

	case Qt::RightArrow:
		pe = QStyle::PE_IndicatorArrowRight;
		break;

	case Qt::UpArrow:
		pe = QStyle::PE_IndicatorArrowUp;
		break;

	case Qt::DownArrow:
		pe = QStyle::PE_IndicatorArrowDown;
		break;

	default:
		return;
	}
	QStyleOption arrowOpt = *toolbutton;

	arrowOpt.rect = rect;
	style->drawPrimitive(pe, &arrowOpt, painter, widget);
}

/*** End of inlined file: SARibbonToolButton.cpp ***/


/*** Start of inlined file: SARibbonButtonLayoutStrategy.cpp ***/
#include <QFontMetrics>
#include <QLatin1Char>

// 布局常量定义 (与SARibbonToolButton.cpp中保持一致)
namespace
{
constexpr int MIN_BUTTON_WIDTH               = 16;
constexpr int GLOBAL_STRUT_WIDTH             = 2;
constexpr int GLOBAL_STRUT_HEIGHT            = 2;
constexpr qreal LARGE_BUTTON_HEIGHT_FACTOR   = 4.8;
constexpr qreal LARGE_BUTTON_MIN_WIDTH_RATIO = 0.75;
constexpr int SMALL_BUTTON_HEIGHT_OFFSET     = 2;
constexpr int INDICATOR_HEIGHT_FACTOR_NUM    = 12;
constexpr int INDICATOR_HEIGHT_FACTOR_DEN    = 10;
}

// SARibbonButtonLayoutContext

SARibbonButtonLayoutContext::SARibbonButtonLayoutContext(const SARibbonToolButton* btn) : buttonWidget(btn)
{
	if (btn) {
		spacing                  = btn->spacing();
		largeIconSize            = btn->largeIconSize();
		smallIconSize            = btn->smallIconSize();
		const auto& factor       = btn->layoutFactor();
		twoLineHeightFactor      = factor.twoLineHeightFactor;
		oneLineHeightFactor      = factor.oneLineHeightFactor;
		buttonMaximumAspectRatio = factor.buttonMaximumAspectRatio;
		enableWordWrap           = btn->isEnableWordWrap();
		maximumWidth             = btn->maximumWidth();
	}
}

// SARibbonButtonLayoutStrategy

bool SARibbonButtonLayoutStrategy::hasIndicator(const QStyleOptionToolButton& opt)
{
	return ((opt.features & QStyleOptionToolButton::MenuButtonPopup) || (opt.features & QStyleOptionToolButton::HasMenu));
}

QSize SARibbonButtonLayoutStrategy::adjustIconSize(const QRect& buttonRect, const QSize& originIconSize)
{
	if (buttonRect.isEmpty() || originIconSize.isEmpty()) {
		return QSize(0, 0);
	}

	QSize iconSize = originIconSize;

	if (iconSize.width() <= buttonRect.width() && iconSize.height() <= buttonRect.height()) {
		return iconSize;
	}

	qreal aspectRatio = static_cast< qreal >(originIconSize.width()) / originIconSize.height();

	if (iconSize.height() > buttonRect.height()) {
		iconSize.setHeight(buttonRect.height());
		iconSize.setWidth(qRound(buttonRect.height() * aspectRatio));
	}

	if (iconSize.width() > buttonRect.width()) {
		iconSize.setWidth(buttonRect.width());
		iconSize.setHeight(qRound(buttonRect.width() / aspectRatio));
	}

	iconSize.setWidth(qMin(iconSize.width(), buttonRect.width()));
	iconSize.setHeight(qMin(iconSize.height(), buttonRect.height()));

	return iconSize;
}

QString SARibbonButtonLayoutStrategy::simplifiedText(const QString& str)
{
	QString res = str;
	res.remove('\n');
	return res;
}

int SARibbonButtonLayoutStrategy::estimateTextWidth(int buttonHeight,
													int textHeight,
													const QString& text,
													const QFontMetrics& fm,
													SARibbonButtonLayoutContext& ctx) const
{
	QSize textSize;
	int space        = SA::compat::horizontalAdvance(fm, QLatin1Char(' ')) * 2;
	int hintMaxWidth = qMin(static_cast< int >(buttonHeight * ctx.buttonMaximumAspectRatio), ctx.maximumWidth);

	if (ctx.enableWordWrap) {
		textSize = fm.size(Qt::TextShowMnemonic, text);
		textSize.setWidth(textSize.width() + space);

		if (textSize.height() > fm.lineSpacing() * 1.1) {
			ctx.isTextNeedWrap = true;
			return textSize.width();
		}

		if (textSize.width() <= hintMaxWidth) {
			ctx.isTextNeedWrap = false;
			return textSize.width();
		}

		int alignment = Qt::TextShowMnemonic | Qt::TextWordWrap;
		int minWidth  = textSize.width() / 2;
		int maxWidth  = textSize.width();
		int bestWidth = maxWidth;

		for (int i = 0; i < 10; ++i) {
			int midWidth = (minWidth + maxWidth) / 2;
			QRect textRect(0, 0, midWidth, textHeight);
			textRect = fm.boundingRect(textRect, alignment, text);

			if (textRect.height() <= fm.lineSpacing() * 2) {
				bestWidth = midWidth;
				maxWidth  = midWidth - 1;
			} else {
				minWidth = midWidth + 1;
			}

			if (minWidth > maxWidth) {
				break;
			}
		}

		ctx.isTextNeedWrap = true;
		return bestWidth;
	}

	ctx.isTextNeedWrap = false;
	textSize           = fm.size(Qt::TextShowMnemonic, simplifiedText(text));
	textSize.setWidth(textSize.width() + space);

	if (textSize.width() < hintMaxWidth) {
		return textSize.width();
	}
	return hintMaxWidth;
}

// SARibbonLargeButtonLayoutStrategy

void SARibbonLargeButtonLayoutStrategy::calculateDrawRects(const QStyleOptionToolButton& opt,
														   SARibbonButtonLayoutRects& rects,
														   const SARibbonButtonLayoutContext& ctx) const
{
	rects.clear();

	switch (opt.toolButtonStyle) {
	case Qt::ToolButtonIconOnly:
		calculateIconOnlyRects(opt, rects, ctx);
		break;
	case Qt::ToolButtonTextOnly:
		calculateTextOnlyRects(opt, rects, ctx);
		break;
	default:
		calculateIconAndTextRects(opt, rects, ctx);
		break;
	}
}

/**
 * \if ENGLISH
 * @brief Calculate layout rects for large button with icon only style
 * @param[in] opt Style option for the tool button
 * @param[out] rects Output rectangles collection
 * @param[in] ctx Layout context containing layout parameters
 * @details Supports both LTR and RTL layouts. In RTL mode, indicator is positioned at bottom-left corner.
 * \endif
 *
 * \if CHINESE
 * @brief 计算仅显示图标的大按钮布局矩形
 * @param[in] opt 工具按钮的样式选项
 * @param[out] rects 输出的矩形集合
 * @param[in] ctx 包含布局参数的布局上下文
 * @details 同时支持LTR和RTL布局。在RTL模式下，指示器位于左下角。
 * \endif
 */
void SARibbonLargeButtonLayoutStrategy::calculateIconOnlyRects(const QStyleOptionToolButton& opt,
															   SARibbonButtonLayoutRects& rects,
															   const SARibbonButtonLayoutContext& ctx) const
{
	bool hasInd      = hasIndicator(opt);
	int spacing      = ctx.spacing;
	int indicatorLen = ctx.indicatorLength;

	if (hasInd) {
		int indicatorHeight = static_cast< int >(indicatorLen * INDICATOR_HEIGHT_FACTOR_NUM / INDICATOR_HEIGHT_FACTOR_DEN);
		rects.indicatorRect = QRect(opt.rect.left() + spacing,
									opt.rect.bottom() - indicatorHeight - spacing,
									opt.rect.width() - 2 * spacing,
									indicatorHeight);
		rects.iconRect      = QRect(opt.rect.left() + spacing,
							   opt.rect.top() + spacing,
							   opt.rect.width() - 2 * spacing,
							   opt.rect.height() - 2 * spacing - indicatorHeight);
	} else {
		rects.iconRect = QRect(opt.rect.left() + spacing,
							   opt.rect.top() + spacing,
							   opt.rect.width() - 2 * spacing,
							   opt.rect.height() - 2 * spacing);
	}
}

/**
 * \if ENGLISH
 * @brief Calculate layout rects for large button with text only style
 * @param[in] opt Style option for the tool button
 * @param[out] rects Output rectangles collection
 * @param[in] ctx Layout context containing layout parameters
 * @details Supports both LTR and RTL layouts. In RTL mode, text is right-aligned.
 * \endif
 *
 * \if CHINESE
 * @brief 计算仅显示文本的大按钮布局矩形
 * @param[in] opt 工具按钮的样式选项
 * @param[out] rects 输出的矩形集合
 * @param[in] ctx 包含布局参数的布局上下文
 * @details 同时支持LTR和RTL布局。在RTL模式下，文本右对齐。
 * \endif
 */
void SARibbonLargeButtonLayoutStrategy::calculateTextOnlyRects(const QStyleOptionToolButton& opt,
															   SARibbonButtonLayoutRects& rects,
															   const SARibbonButtonLayoutContext& ctx) const
{
	bool hasInd      = hasIndicator(opt);
	int spacing      = ctx.spacing;
	int indicatorLen = ctx.indicatorLength;

	if (hasInd) {
		int indicatorHeight = static_cast< int >(indicatorLen * INDICATOR_HEIGHT_FACTOR_NUM / INDICATOR_HEIGHT_FACTOR_DEN);
		rects.indicatorRect = QRect(opt.rect.left() + spacing,
									opt.rect.bottom() - indicatorHeight - spacing,
									opt.rect.width() - 2 * spacing,
									indicatorHeight);
		rects.textRect      = QRect(opt.rect.left() + spacing,
							   opt.rect.top() + spacing,
							   opt.rect.width() - 2 * spacing,
							   opt.rect.height() - 2 * spacing - indicatorHeight);
	} else {
		rects.textRect = QRect(opt.rect.left() + spacing,
							   opt.rect.top() + spacing,
							   opt.rect.width() - 2 * spacing,
							   opt.rect.height() - 2 * spacing);
	}
}

/**
 * \if ENGLISH
 * @brief Calculate layout rects for large button with both icon and text
 * @param[in] opt Style option for the tool button
 * @param[out] rects Output rectangles collection
 * @param[in] ctx Layout context containing layout parameters
 * @details Supports both LTR and RTL layouts. In RTL mode:
 * - Indicator is positioned at bottom-left corner
 * - Text is right-aligned
 * - Icon remains centered
 * \endif
 *
 * \if CHINESE
 * @brief 计算同时显示图标和文本的大按钮布局矩形
 * @param[in] opt 工具按钮的样式选项
 * @param[out] rects 输出的矩形集合
 * @param[in] ctx 包含布局参数的布局上下文
 * @details 同时支持LTR和RTL布局。在RTL模式下：
 * - 指示器位于左下角
 * - 文本右对齐
 * - 图标保持居中
 * \endif
 */
void SARibbonLargeButtonLayoutStrategy::calculateIconAndTextRects(const QStyleOptionToolButton& opt,
																  SARibbonButtonLayoutRects& rects,
																  const SARibbonButtonLayoutContext& ctx) const
{
	int spacing      = ctx.spacing;
	int indicatorLen = ctx.indicatorLength;
	bool hasInd      = hasIndicator(opt);

	if (!hasInd) {
		indicatorLen = 0;
	}

	int textHeight = calculateTextHeight(opt, ctx);

	if (ctx.enableWordWrap && ctx.isTextNeedWrap) {
		rects.textRect = QRect(opt.rect.left() + spacing,
							   opt.rect.bottom() - spacing - textHeight,
							   opt.rect.width() - 2 * spacing - indicatorLen,
							   textHeight);
		if (hasInd) {
			int indX = rects.textRect.right();
			if (SA::saIsRTL()) {
				indX = SA::saMirrorX(indX, opt.rect.width(), indicatorLen);
				rects.textRect.moveLeft(opt.rect.right() - spacing - rects.textRect.width());
			}
			rects.indicatorRect = QRect(
				indX, rects.textRect.y() + rects.textRect.height() / 2, indicatorLen, textHeight / 2);
		} else if (SA::saIsRTL()) {
			rects.textRect.moveLeft(opt.rect.right() - spacing - rects.textRect.width());
		}
	} else if (ctx.enableWordWrap) {
		rects.textRect = QRect(
			opt.rect.left() + spacing, opt.rect.bottom() - spacing - textHeight, opt.rect.width() - 2 * spacing, textHeight);
		if (hasInd) {
			int dy = rects.textRect.height() / 2;
			dy += (dy - indicatorLen) / 2;
			rects.indicatorRect =
				QRect(rects.textRect.left(), rects.textRect.top() + dy, rects.textRect.width(), indicatorLen);
		}
		if (SA::saIsRTL()) {
			rects.textRect.moveLeft(opt.rect.right() - spacing - rects.textRect.width());
		}
	} else {
		int y = opt.rect.bottom() - spacing - textHeight;
		if (hasInd) {
			int indX = opt.rect.right() - indicatorLen - spacing;
			int textX = spacing;
			int textWidth = indX - spacing;
			if (SA::saIsRTL()) {
				indX = spacing;
				textX = indX + indicatorLen;
				textWidth = opt.rect.width() - 2 * spacing - indicatorLen;
			}
			rects.indicatorRect = QRect(indX, y, indicatorLen, textHeight);
			rects.textRect      = QRect(textX, y, textWidth, textHeight);
		} else {
			rects.textRect = QRect(opt.rect.left() + spacing, y, opt.rect.width() - 2 * spacing, textHeight);
			if (SA::saIsRTL()) {
				rects.textRect.moveLeft(opt.rect.right() - spacing - rects.textRect.width());
			}
		}
	}

	rects.iconRect = QRect(spacing, spacing, opt.rect.width() - 2 * spacing, rects.textRect.top() - 2 * spacing);
}

QSize SARibbonLargeButtonLayoutStrategy::calculateSizeHint(const QStyleOptionToolButton& opt,
														   const SARibbonButtonLayoutContext& ctx) const
{
	int w    = 0;
	int h    = opt.fontMetrics.lineSpacing() * LARGE_BUTTON_HEIGHT_FACTOR;
	int minW = static_cast< int >(h * LARGE_BUTTON_MIN_WIDTH_RATIO);

	if (ctx.buttonWidget) {
		if (SARibbonPanel* panel = qobject_cast< SARibbonPanel* >(ctx.buttonWidget->parent())) {
			h = panel->largeButtonHeight();
		}
	}

	int textHeight = calculateTextHeight(opt, ctx);

	SARibbonButtonLayoutContext mutableCtx = ctx;
	w                                      = estimateTextWidth(h, textHeight, opt.text, opt.fontMetrics, mutableCtx);
	w += (2 * ctx.spacing);

	if (ctx.enableWordWrap && mutableCtx.isTextNeedWrap) {
		w += ctx.indicatorLength;
	}

	return QSize(w, h).expandedTo(QSize(minW, textHeight)).expandedTo(QSize(GLOBAL_STRUT_WIDTH, GLOBAL_STRUT_HEIGHT));
}

int SARibbonLargeButtonLayoutStrategy::calculateTextHeight(const QStyleOptionToolButton& opt,
														   const SARibbonButtonLayoutContext& ctx) const
{
	if (ctx.enableWordWrap) {
		return opt.fontMetrics.lineSpacing() * ctx.twoLineHeightFactor + opt.fontMetrics.leading();
	} else {
		return opt.fontMetrics.lineSpacing() * ctx.oneLineHeightFactor;
	}
}

// SARibbonSmallButtonLayoutStrategy

void SARibbonSmallButtonLayoutStrategy::calculateDrawRects(const QStyleOptionToolButton& opt,
														   SARibbonButtonLayoutRects& rects,
														   const SARibbonButtonLayoutContext& ctx) const
{
	rects.clear();

	switch (opt.toolButtonStyle) {
	case Qt::ToolButtonIconOnly:
		calculateIconOnlyRects(opt, rects, ctx);
		break;
	case Qt::ToolButtonTextOnly:
		calculateTextOnlyRects(opt, rects, ctx);
		break;
	default:
		calculateIconAndTextRects(opt, rects, ctx);
		break;
	}
}

/**
 * \if ENGLISH
 * @brief Calculate layout rects for small button with icon only style
 * @param[in] opt Style option for the tool button
 * @param[out] rects Output rectangles collection
 * @param[in] ctx Layout context containing layout parameters
 * @details Supports both LTR and RTL layouts. In RTL mode:
 * - Indicator is positioned at left edge
 * - Icon is positioned at right side of the indicator
 * \endif
 *
 * \if CHINESE
 * @brief 计算仅显示图标的小按钮布局矩形
 * @param[in] opt 工具按钮的样式选项
 * @param[out] rects 输出的矩形集合
 * @param[in] ctx 包含布局参数的布局上下文
 * @details 同时支持LTR和RTL布局。在RTL模式下：
 * - 指示器位于左边缘
 * - 图标位于指示器的右侧
 * \endif
 */
void SARibbonSmallButtonLayoutStrategy::calculateIconOnlyRects(const QStyleOptionToolButton& opt,
															   SARibbonButtonLayoutRects& rects,
															   const SARibbonButtonLayoutContext& ctx) const
{
	int spacing      = ctx.spacing;
	int indicatorLen = ctx.indicatorLength;

	if (hasIndicator(opt)) {
		if (SA::saIsRTL()) {
			rects.indicatorRect = QRect(opt.rect.left() + spacing, opt.rect.top() + spacing, indicatorLen, opt.rect.height() - 2 * spacing);
			rects.iconRect = opt.rect.adjusted(indicatorLen + spacing, spacing, -spacing, -spacing);
		} else {
			rects.iconRect = opt.rect.adjusted(spacing, spacing, -indicatorLen - spacing, -spacing);
			rects.indicatorRect =
				QRect(opt.rect.right() - indicatorLen - spacing, rects.iconRect.y(), indicatorLen, rects.iconRect.height());
		}
	} else {
		rects.iconRect = opt.rect.adjusted(spacing, spacing, -spacing, -spacing);
	}
}

/**
 * \if ENGLISH
 * @brief Calculate layout rects for small button with text only style
 * @param[in] opt Style option for the tool button
 * @param[out] rects Output rectangles collection
 * @param[in] ctx Layout context containing layout parameters
 * @details Supports both LTR and RTL layouts. In RTL mode:
 * - Indicator is positioned at left edge
 * - Text is positioned at right side of the indicator
 * \endif
 *
 * \if CHINESE
 * @brief 计算仅显示文本的小按钮布局矩形
 * @param[in] opt 工具按钮的样式选项
 * @param[out] rects 输出的矩形集合
 * @param[in] ctx 包含布局参数的布局上下文
 * @details 同时支持LTR和RTL布局。在RTL模式下：
 * - 指示器位于左边缘
 * - 文本位于指示器的右侧
 * \endif
 */
void SARibbonSmallButtonLayoutStrategy::calculateTextOnlyRects(const QStyleOptionToolButton& opt,
															   SARibbonButtonLayoutRects& rects,
															   const SARibbonButtonLayoutContext& ctx) const
{
	int spacing      = ctx.spacing;
	int indicatorLen = ctx.indicatorLength;

	if (hasIndicator(opt)) {
		if (SA::saIsRTL()) {
			rects.indicatorRect = QRect(opt.rect.left() + spacing, spacing, indicatorLen, opt.rect.height() - 2 * spacing);
			rects.textRect = opt.rect.adjusted(indicatorLen + spacing, spacing, -spacing, -spacing);
		} else {
			rects.textRect = opt.rect.adjusted(spacing, spacing, -indicatorLen - spacing, -spacing);
			rects.indicatorRect =
				QRect(opt.rect.right() - indicatorLen - spacing, spacing, indicatorLen, rects.textRect.height());
		}
	} else {
		rects.textRect = opt.rect.adjusted(spacing, spacing, -spacing, -spacing);
	}
}

/**
 * \if ENGLISH
 * @brief Calculate layout rects for small button with both icon and text
 * @param[in] opt Style option for the tool button
 * @param[out] rects Output rectangles collection
 * @param[in] ctx Layout context containing layout parameters
 * @details Supports both LTR and RTL layouts. In RTL mode:
 * - Indicator is positioned at left edge
 * - Text is positioned at left side, next to indicator
 * - Icon is positioned at right edge
 * \endif
 *
 * \if CHINESE
 * @brief 计算同时显示图标和文本的小按钮布局矩形
 * @param[in] opt 工具按钮的样式选项
 * @param[out] rects 输出的矩形集合
 * @param[in] ctx 包含布局参数的布局上下文
 * @details 同时支持LTR和RTL布局。在RTL模式下：
 * - 指示器位于左边缘
 * - 文本位于左侧，紧邻指示器
 * - 图标位于右边缘
 * \endif
 */
void SARibbonSmallButtonLayoutStrategy::calculateIconAndTextRects(const QStyleOptionToolButton& opt,
																  SARibbonButtonLayoutRects& rects,
																  const SARibbonButtonLayoutContext& ctx) const
{
	bool hasInd      = hasIndicator(opt);
	int spacing      = ctx.spacing;
	int indicatorLen = ctx.indicatorLength;

	QRect buttonRect = opt.rect;
	buttonRect.adjust(spacing, spacing, -spacing, -spacing);

	if (!opt.icon.isNull()) {
		QSize iconSize = adjustIconSize(buttonRect, opt.iconSize);
		int iconX = buttonRect.x();
		if (SA::saIsRTL()) {
			iconX = buttonRect.right() - iconSize.width();
		}
		rects.iconRect =
			QRect(iconX, buttonRect.y(), iconSize.width(), qMax(iconSize.height(), buttonRect.height()));
	}

	if (!opt.text.isEmpty()) {
		int adjx = rects.iconRect.isValid() ? (rects.iconRect.width() + spacing) : 0;
		int textX = rects.iconRect.isValid() ? (SA::saIsRTL() ? buttonRect.x() : rects.iconRect.right() + spacing) : buttonRect.x();
		int textRightMargin = hasInd ? indicatorLen : 0;
		int textWidth = SA::saIsRTL() ? (rects.iconRect.isValid() ? (rects.iconRect.x() - spacing - textX - textRightMargin) : (buttonRect.width() - textRightMargin)) : (buttonRect.width() - adjx - textRightMargin);

		rects.textRect = QRect(textX, buttonRect.y(), textWidth, buttonRect.height());
	}

	if (hasInd) {
		int indX = buttonRect.right() - indicatorLen + 1;
		if (SA::saIsRTL()) {
			indX = buttonRect.left();
		}
		if (rects.textRect.isValid()) {
			rects.indicatorRect =
				QRect(indX, rects.textRect.y(), indicatorLen, rects.textRect.height());
		} else if (rects.iconRect.isValid()) {
			rects.indicatorRect =
				QRect(indX, rects.iconRect.y(), indicatorLen, rects.iconRect.height());
		} else {
			rects.indicatorRect = buttonRect;
		}
	}
}

QSize SARibbonSmallButtonLayoutStrategy::calculateSizeHint(const QStyleOptionToolButton& opt,
														   const SARibbonButtonLayoutContext& ctx) const
{
	int w = 0, h = 0;
	int spacing = ctx.spacing;

	switch (opt.toolButtonStyle) {
	case Qt::ToolButtonIconOnly:
		w = opt.iconSize.width() + 2 * spacing;
		h = opt.iconSize.height() + 2 * spacing;
		break;
	case Qt::ToolButtonTextOnly: {
		QSize textSize = opt.fontMetrics.size(Qt::TextShowMnemonic, simplifiedText(opt.text));
		textSize.setWidth(textSize.width() + SA::compat::horizontalAdvance(opt.fontMetrics, QLatin1Char(' ')) * 2);
		textSize.setHeight(calculateTextHeight(opt, ctx));
		w = textSize.width() + 2 * spacing;
		h = textSize.height() + 2 * spacing;
	} break;
	default:
		w = opt.iconSize.width() + 2 * spacing;
		h = opt.iconSize.height() + 2 * spacing;
		if (!opt.text.isEmpty()) {
			QSize textSize = opt.fontMetrics.size(Qt::TextShowMnemonic, simplifiedText(opt.text));
			textSize.setWidth(textSize.width() + SA::compat::horizontalAdvance(opt.fontMetrics, QLatin1Char(' ')) * 2);
			textSize.setHeight(calculateTextHeight(opt, ctx));
			w += spacing + textSize.width();
			h = qMax(h, textSize.height() + 2 * spacing);
		} else {
			QSize textSize = opt.fontMetrics.size(Qt::TextShowMnemonic, QStringLiteral(" "));
			h              = qMax(h, textSize.height() + 2 * spacing);
		}
	}

	if (hasIndicator(opt)) {
		w += ctx.indicatorLength;
	}

	if (w < MIN_BUTTON_WIDTH) {
		w = MIN_BUTTON_WIDTH;
	}

	return QSize(w, h).expandedTo(QSize(GLOBAL_STRUT_WIDTH, GLOBAL_STRUT_HEIGHT));
}

int SARibbonSmallButtonLayoutStrategy::calculateTextHeight(const QStyleOptionToolButton& opt,
														   const SARibbonButtonLayoutContext& ctx) const
{
	Q_UNUSED(ctx)
	return opt.rect.height() - SMALL_BUTTON_HEIGHT_OFFSET;
}

// SARibbonButtonLayoutStrategyFactory

std::unique_ptr< SARibbonButtonLayoutStrategy > SARibbonButtonLayoutStrategyFactory::createStrategy(SARibbonButtonType type)
{
	switch (type) {
	case SARibbonButtonType::LargeButton:
		return std::make_unique< SARibbonLargeButtonLayoutStrategy >();
	case SARibbonButtonType::SmallButton:
	default:
		return std::make_unique< SARibbonSmallButtonLayoutStrategy >();
	}
}

/*** End of inlined file: SARibbonButtonLayoutStrategy.cpp ***/


/*** Start of inlined file: SARibbonColorToolButton.cpp ***/
#include <QStylePainter>
#include <QStyleOptionToolButton>
#include <QDebug>
#include <QApplication>
#include <QScreen>

//===================================================
// SARibbonColorToolButton::PrivateData
//===================================================
namespace SARibbonColorToolButtonConstants
{
constexpr int COLOR_BLOCK_HEIGHT       = 5;   ///< 颜色块的高度
constexpr int COLOR_BLOCK_MARGIN       = 1;   ///< 颜色块边距
constexpr int COLOR_BLOCK_EXTRA_WIDTH  = 4;   ///< 颜色块额外宽度
constexpr int COLOR_BLOCK_EXTRA_HEIGHT = 4;   ///< 颜色块额外高度
constexpr int ICON_OFFSET_ADJUSTMENT   = 2;   ///< 图标偏移调整值
constexpr int DEFAULT_COLOR_ICON_SIZE  = 32;  ///< 默认颜色图标尺寸
constexpr int INVALID_COLOR_PEN_WIDTH  = 1;   ///< 无效颜色时边框线宽
constexpr int INVALID_COLOR_LINE_RATIO = 3;   ///< 无效颜色对角线比例分母
}

/**
 * \if ENGLISH
 * @brief Private data class for SARibbonColorToolButton
 * @details This class holds private data for SARibbonColorToolButton to implement the PIMPL idiom.
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonColorToolButton 的私有数据类
 * @details 此类持有 SARibbonColorToolButton 的私有数据，实现 PIMPL 设计模式。
 * \endif
 */
class SARibbonColorToolButton::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonColorToolButton)
public:
	PrivateData(SARibbonColorToolButton* p);

	/**
	 * \if ENGLISH
	 * @brief Create an icon pixmap with color under the icon
	 * @param opt Style option for the tool button
	 * @param iconsize Size of the icon
	 * @return Pixmap with color under the icon
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 创建一个带有颜色的图标 pixmap
	 * @param opt 工具按钮的样式选项
	 * @param iconsize 图标的大小
	 * @return 带有颜色的图标 pixmap
	 * \endif
	 */
	QPixmap createIconPixmap(const QStyleOptionToolButton& opt, const QSize& iconsize) const;

	/**
	 * \if ENGLISH
	 * @brief Create a color icon
	 * @param c Color for the icon
	 * @param size Size of the icon
	 * @return Icon with the specified color
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 创建一个颜色图标
	 * @param c 图标的颜色
	 * @param size 图标的大小
	 * @return 带有指定颜色的图标
	 * \endif
	 */
	QIcon createColorIcon(const QColor& c, const QSize& size) const;

public:
	QColor mColor;                                                                                ///< 记录颜色
	SARibbonColorToolButton::ColorStyle mColorStyle { SARibbonColorToolButton::ColorUnderIcon };  ///< 颜色显示样式
	QIcon mOldIcon;                                                                               ///< 记录旧的icon
};

SARibbonColorToolButton::PrivateData::PrivateData(SARibbonColorToolButton* p) : q_ptr(p)
{
}

QPixmap SARibbonColorToolButton::PrivateData::createIconPixmap(const QStyleOptionToolButton& opt, const QSize& iconsize) const
{
	if (opt.icon.isNull()) {  // 没有有图标
		return QPixmap();
	}
	// 有icon，在icon下方加入颜色
	QIcon::State state = (opt.state & QStyle::State_On) ? QIcon::On : QIcon::Off;
	QIcon::Mode mode;
	if (!(opt.state & QStyle::State_Enabled)) {
		mode = QIcon::Disabled;
	} else if ((opt.state & QStyle::State_MouseOver) && (opt.state & QStyle::State_AutoRaise)) {
		mode = QIcon::Active;
	} else {
		mode = QIcon::Normal;
	}
	QSize realIconSize = iconsize
						 - QSize(0,
								 SARibbonColorToolButtonConstants::COLOR_BLOCK_HEIGHT
									 + SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN);
	QPixmap pixmap = SA::iconToPixmap(opt.icon, realIconSize, SA::widgetDevicePixelRatio(q_ptr), mode, state);
	// QPixmap pixmap     = opt.icon.pixmap(q_ptr->window()->windowHandle(), realIconSize, mode, state);
	QPixmap res(pixmap.size()
				+ QSize(SARibbonColorToolButtonConstants::COLOR_BLOCK_EXTRA_WIDTH,
						SARibbonColorToolButtonConstants::COLOR_BLOCK_EXTRA_HEIGHT
							+ SARibbonColorToolButtonConstants::COLOR_BLOCK_HEIGHT));
	res.fill(Qt::transparent);
	QPainter painter(&res);
	int xpixmap = (res.width() - pixmap.width()) / 2;
	int ypixmap = (res.height() - SARibbonColorToolButtonConstants::COLOR_BLOCK_HEIGHT
				   - SARibbonColorToolButtonConstants::ICON_OFFSET_ADJUSTMENT - pixmap.height())
				  / 2;
	int w         = pixmap.width();
	int h         = pixmap.height();
	QRect rpixmap = QRect(xpixmap, ypixmap, w, h);
	painter.drawPixmap(rpixmap, pixmap);
	QRect colorRect = rpixmap.adjusted(0,
									   h + SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN,
									   0,
									   SARibbonColorToolButtonConstants::COLOR_BLOCK_HEIGHT
										   + SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN);
	if (mColor.isValid()) {
		painter.fillRect(colorRect, mColor);
	} else {
		QPen pen(Qt::red, SARibbonColorToolButtonConstants::INVALID_COLOR_PEN_WIDTH, Qt::SolidLine, Qt::RoundCap);
		painter.setPen(pen);
		painter.setRenderHint(QPainter::SmoothPixmapTransform, true);
		painter.setRenderHint(QPainter::Antialiasing, true);
		int ss = colorRect.width() / SARibbonColorToolButtonConstants::INVALID_COLOR_LINE_RATIO;
		painter.drawLine(QPoint(colorRect.x() + ss, colorRect.bottom()), QPoint(colorRect.right() - ss, colorRect.top()));
		pen.setColor(Qt::black);
		painter.setPen(pen);
		painter.drawRect(colorRect);
	}
	return res;
}

QIcon SARibbonColorToolButton::PrivateData::createColorIcon(const QColor& c, const QSize& size) const
{
	// using Constants = SARibbonColorToolButtonConstants;
	QPixmap res(size);
	res.fill(Qt::transparent);
	QPainter painter(&res);
	QRect colorRect(SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN,
					SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN,
					res.height() - 2 * SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN,
					res.width() - 2 * SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN);
	if (c.isValid()) {
		painter.fillRect(colorRect, c);
	} else {
		QPen pen(Qt::black, SARibbonColorToolButtonConstants::INVALID_COLOR_PEN_WIDTH, Qt::SolidLine, Qt::RoundCap);
		painter.setPen(pen);
		painter.drawRect(colorRect);
		pen.setColor(Qt::red);
		painter.setPen(pen);
		painter.setRenderHint(QPainter::SmoothPixmapTransform, true);
		painter.setRenderHint(QPainter::Antialiasing, true);
		painter.drawLine(QPoint(SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN,
								size.height() - SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN),
						 QPoint(size.width() - SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN,
								SARibbonColorToolButtonConstants::COLOR_BLOCK_MARGIN));
	}
	return QIcon(res);
}

//===================================================
// SARibbonColorToolButton
//===================================================

SARibbonColorToolButton::SARibbonColorToolButton(QWidget* parent)
	: SARibbonToolButton(parent), d_ptr(new SARibbonColorToolButton::PrivateData(this))
{
	connect(this, &QAbstractButton::clicked, this, &SARibbonColorToolButton::onButtonClicked);
}

SARibbonColorToolButton::SARibbonColorToolButton(QAction* defaultAction, QWidget* parent)
	: SARibbonToolButton(defaultAction, parent), d_ptr(new SARibbonColorToolButton::PrivateData(this))
{
	connect(this, &QAbstractButton::clicked, this, &SARibbonColorToolButton::onButtonClicked);
}

SARibbonColorToolButton::~SARibbonColorToolButton()
{
}

/**
 * \if ENGLISH
 * @brief Get the color maintained by the button
 * @return Current color
 * \endif
 *
 * \if CHINESE
 * @brief 获取按钮维护的颜色
 * @return 当前颜色
 * \endif
 */
QColor SARibbonColorToolButton::color() const
{
	return d_ptr->mColor;
}

/**
 * \if ENGLISH
 * @brief Set the color display style
 * @param s Color style to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置颜色显示的样式
 * @param s 要设置的颜色样式
 * \endif
 */
void SARibbonColorToolButton::setColorStyle(SARibbonColorToolButton::ColorStyle s)
{
	if (d_ptr->mColorStyle == s) {
		return;
	}
	d_ptr->mColorStyle = s;
	if (ColorUnderIcon == s) {
		setIcon(d_ptr->mOldIcon);
	} else {
		d_ptr->mOldIcon = icon();
		setIcon(d_ptr->createColorIcon(d_ptr->mColor,
									   QSize(SARibbonColorToolButtonConstants::DEFAULT_COLOR_ICON_SIZE,
											 SARibbonColorToolButtonConstants::DEFAULT_COLOR_ICON_SIZE)));
	}
	repaint();
}

/**
 * \if ENGLISH
 * @brief Get the color display style
 * @return Current color style
 * \endif
 *
 * \if CHINESE
 * @brief 颜色显示的样式
 * @return 当前颜色样式
 * \endif
 */
SARibbonColorToolButton::ColorStyle SARibbonColorToolButton::colorStyle() const
{
	return d_ptr->mColorStyle;
}

/**
 * \if ENGLISH
 * @brief Set up a standard color menu
 * @return Created SAColorMenu object
 * \endif
 *
 * \if CHINESE
 * @brief 建立标准的颜色菜单
 * @return 创建的 SAColorMenu 对象
 * \endif
 */
SAColorMenu* SARibbonColorToolButton::setupStandardColorMenu()
{
	setPopupMode(QToolButton::MenuButtonPopup);
	SAColorMenu* m = new SAColorMenu(this);
	m->enableNoneColorAction(true);
	if (QAction* customColor = m->customColorAction()) {
		customColor->setIcon(QIcon(":/SARibbon/image/resource/define-color.svg"));
	}
	connect(m, &SAColorMenu::selectedColor, this, &SARibbonColorToolButton::setColor);
	setMenu(m);

	updateRect();
	return m;
}

/**
 * \if ENGLISH
 * @brief Set the color of the button
 * @param c Color to set
 * @note This will generate a new icon and emit the colorChanged signal
 * \endif
 *
 * \if CHINESE
 * @brief 设置按钮的颜色
 * @param c 要设置的颜色
 * @note 此时会生成一个新的icon，并发射 colorChanged 信号
 * \endif
 */
void SARibbonColorToolButton::setColor(const QColor& c)
{
	if (d_ptr->mColor != c) {
		d_ptr->mColor = c;
		if (ColorFillToIcon == colorStyle()) {
			setIcon(d_ptr->createColorIcon(c,
										   QSize(SARibbonColorToolButtonConstants::DEFAULT_COLOR_ICON_SIZE,
												 SARibbonColorToolButtonConstants::DEFAULT_COLOR_ICON_SIZE)));
		}
		repaint();
		Q_EMIT colorChanged(c);
	}
}

void SARibbonColorToolButton::onButtonClicked(bool checked)
{
	Q_EMIT colorClicked(d_ptr->mColor, checked);
}

/**
 * \if ENGLISH
 * @brief Override createIconPixmap function to add color under the icon
 * @param opt Style option for the tool button
 * @param iconSize Size of the icon
 * @return Pixmap with color under the icon
 * \endif
 *
 * \if CHINESE
 * @brief 重写createIconPixmap函数，把颜色加到icon下面
 * @param opt 工具按钮的样式选项
 * @param iconSize 图标尺寸
 * @return 带有颜色的图标pixmap
 * \endif
 */
QPixmap SARibbonColorToolButton::createIconPixmap(const QStyleOptionToolButton& opt, const QSize& iconSize) const
{
	if (ColorUnderIcon == colorStyle()) {
		// 在图标下方显示颜色
		return d_ptr->createIconPixmap(opt, iconSize);
	} else {
		// 使用父类的实现
		return SARibbonToolButton::createIconPixmap(opt, iconSize);
	}
}

/*** End of inlined file: SARibbonColorToolButton.cpp ***/


/*** Start of inlined file: SARibbonLineWidgetContainer.cpp ***/
#include <QHBoxLayout>

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonLineWidgetContainer
 * @param par Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonLineWidgetContainer 构造函数
 * @param par 父窗口
 * \endif
 */
SARibbonLineWidgetContainer::SARibbonLineWidgetContainer(QWidget *par) : QWidget(par)
	, m_innerWidget(nullptr)
{
	m_labelPrefix = new QLabel(this);
	m_labelSuffix = new QLabel(this);
	QHBoxLayout *lay = new QHBoxLayout();

	lay->setContentsMargins(0, 0, 0, 0);
	lay->setSpacing(0);
	lay->addWidget(m_labelPrefix);
	lay->addWidget(m_labelSuffix);
	setLayout(lay);
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonLineWidgetContainer
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonLineWidgetContainer 析构函数
 * \endif
 */
SARibbonLineWidgetContainer::~SARibbonLineWidgetContainer()
{
}

/**
 * \if ENGLISH
 * @brief Set the inner widget
 * @param innerWidget Inner widget to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置内部部件
 * @param innerWidget 要设置的内部部件
 * \endif
 */
void SARibbonLineWidgetContainer::setWidget(QWidget *innerWidget)
{
	QHBoxLayout *lay = static_cast<QHBoxLayout *>(layout());

	if (m_innerWidget) {
		lay->replaceWidget(m_innerWidget, innerWidget);
	}else{
		lay->insertWidget(1, innerWidget);
	}
	m_innerWidget = innerWidget;
}

/**
 * \if ENGLISH
 * @brief Set prefix text
 * @param str Prefix text
 * \endif
 *
 * \if CHINESE
 * @brief 设置前缀文本
 * @param str 前缀文本
 * \endif
 */
void SARibbonLineWidgetContainer::setPrefix(const QString& str)
{
	m_labelPrefix->setText(str);
}

/**
 * \if ENGLISH
 * @brief Set suffix text
 * @param str Suffix text
 * \endif
 *
 * \if CHINESE
 * @brief 设置后缀文本
 * @param str 后缀文本
 * \endif
 */
void SARibbonLineWidgetContainer::setSuffix(const QString& str)
{
	m_labelSuffix->setText(str);
}

/**
 * \if ENGLISH
 * @brief Get prefix label
 * @return Prefix QLabel pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取前缀标签
 * @return 前缀QLabel指针
 * \endif
 */
QLabel *SARibbonLineWidgetContainer::labelPrefix() const
{
	return (m_labelPrefix);
}

/**
 * \if ENGLISH
 * @brief Get suffix label
 * @return Suffix QLabel pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取后缀标签
 * @return 后缀QLabel指针
 * \endif
 */
QLabel *SARibbonLineWidgetContainer::labelSuffix() const
{
	return (m_labelSuffix);
}

/*** End of inlined file: SARibbonLineWidgetContainer.cpp ***/


/*** Start of inlined file: SARibbonActionsManager.cpp ***/
#include <QMap>
#include <QHash>
#include <QDebug>
#include <QWidgetAction>

class SARibbonActionsManager::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonActionsManager)
public:
	PrivateData(SARibbonActionsManager* p);
	void clear();

	QMap< int, QList< QAction* > > mTagToActions;   ///< tag : QList<QAction*>
	QMap< int, QString > mTagToName;                ///< tag对应的名字
	QHash< QString, QAction* > mKeyToAction;        ///< key对应action
	QMap< QAction*, QString > mActionToKey;         ///< action对应key
	QMap< int, SARibbonCategory* > mTagToCategory;  ///< 仅仅在autoRegisteActions函数会有用
	int mSale;  ///< 盐用于生成固定的id，在用户不主动设置key时，id基于msale生成，只要SARibbonActionsManager的调用registeAction顺序不变，生成的id都不变，因为它是基于自增实现的
};

SARibbonActionsManager::PrivateData::PrivateData(SARibbonActionsManager* p) : q_ptr(p), mSale(0)
{
}

void SARibbonActionsManager::PrivateData::clear()
{
	mTagToActions.clear();
	mTagToName.clear();
	mKeyToAction.clear();
	mActionToKey.clear();
	mTagToCategory.clear();
	mSale = 0;
}

/**
 * \if ENGLISH
 * @brief Constructor
 * @param bar SARibbonBar pointer
 * \endif
 *
 * \if CHINESE
 * @brief 构造函数
 * @param bar SARibbonBar指针
 * \endif
 */
SARibbonActionsManager::SARibbonActionsManager(SARibbonBar* bar)
	: QObject(bar), d_ptr(new SARibbonActionsManager::PrivateData(this))
{
	autoRegisteActions(bar);
}

/**
 * \if ENGLISH
 * @brief Destructor
 * \endif
 *
 * \if CHINESE
 * @brief 析构函数
 * \endif
 */
SARibbonActionsManager::~SARibbonActionsManager()
{
}

/**
 * \if ENGLISH
 * @brief Set the name corresponding to the tag, this allows getting the mapping between tag and text
 * @param tag Tag value
 * @param name Tag name
 * @note In multi-language environments, this needs to be reset when language changes
 * \endif
 *
 * \if CHINESE
 * @brief 设置tag对应的名字，通过这个可以得到tag和文本的映射
 * @param tag 标签值
 * @param name 标签名
 * @note 在支持多语言的环境下，在语言切换时需要重新设置，以更新名字
 * \endif
 */
void SARibbonActionsManager::setTagName(int tag, const QString& name)
{
	d_ptr->mTagToName[ tag ] = name;
}

/**
 * \if ENGLISH
 * @brief Get the name corresponding to the tag
 * @param tag Tag value
 * @return Tag name
 * \endif
 *
 * \if CHINESE
 * @brief 获取tag对应的名字
 * @param tag 标签值
 * @return 标签名
 * \endif
 */
QString SARibbonActionsManager::tagName(int tag) const
{
	return (d_ptr->mTagToName.value(tag, ""));
}

/**
 * \if ENGLISH
 * @brief Remove tag
 * @param tag Tag to remove
 * @note This function is time-consuming
 * \endif
 *
 * \if CHINESE
 * @brief 移除tag
 * @param tag 要移除的标签
 * @note 注意，这个函数非常耗时
 * \endif
 */
void SARibbonActionsManager::removeTag(int tag)
{
	QList< QAction* > oldacts = actions(tag);

	// 开始移除
	d_ptr->mTagToActions.remove(tag);
	d_ptr->mTagToName.remove(tag);
	// 开始查找需要移出总表的action
	QList< QAction* > needRemoveAct;
	QList< QAction* > total;

	for (auto i = d_ptr->mTagToActions.begin(); i != d_ptr->mTagToActions.end(); ++i) {
		total += i.value();
	}
	for (QAction* a : sa_as_const(oldacts)) {
		if (!total.contains(a)) {
			needRemoveAct.append(a);
		}
	}
	// 从总表移除action
	for (QAction* a : sa_as_const(needRemoveAct)) {
		auto i = d_ptr->mActionToKey.find(a);
		if (i != d_ptr->mActionToKey.end()) {
			d_ptr->mKeyToAction.remove(i.value());
			d_ptr->mActionToKey.erase(i);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Register action to the manager
 * @param act Action to register
 * @param tag Tag can be combined by bitwise OR, see @ref ActionTag.
 * For custom tags, it's recommended to use values greater than @ref ActionTag::UserDefineActionTag
 * @param key Key for the action, one key corresponds to one action. Default is QString(), which uses QAction's objectName
 * @param enableEmit Whether to emit @ref actionTagChanged signal
 * @return true if registration succeeded
 * @note Same action registered with different tags can be found by tag, but only the last registered tag can be found by action
 * @note New tag will trigger actionTagChanged signal
 * \endif
 *
 * \if CHINESE
 * @brief 把action注册到管理器中，实现action的管理
 * @param act 要注册的action
 * @param tag tag是可以按照位进行叠加，见 @ref ActionTag 如果
 * 要定义自己的标签，建议定义大于@ref ActionTag::UserDefineActionTag 的值，
 * registeAction的tag是直接记录进去的，如果要多个标签并存，在registe之前先或好tag
 * @param key key是action对应的key，一个key只对应一个action，是查找action的关键
 * ,默认情况为一个QString(),这时key是QAction的objectName
 * @param enableEmit 控制是否发射@ref actionTagChanged 信号
 * @return 注册成功返回true
 * @note 同一个action多次注册不同的tag可以通过tag索引到action，但通过action只能索引到最后一个注册的tag
 * @note tag的新增会触发actionTagChanged信号
 * \endif
 */
bool SARibbonActionsManager::registeAction(QAction* act, int tag, const QString& key, bool enableEmit)
{
	if (nullptr == act) {
		return (false);
	}
	QString k = key;

	if (k.isEmpty()) {
		k = QString("id_%1_%2").arg(d_ptr->mSale++).arg(act->objectName());
	}
	if (d_ptr->mKeyToAction.contains(k)) {
		qWarning() << "key: "
				   << k << " have been exist,you can set key in an unique value when use SARibbonActionsManager::registeAction";
		return (false);
	}
	d_ptr->mKeyToAction[ k ]   = act;
	d_ptr->mActionToKey[ act ] = k;
	// 记录tag 对 action
	bool isneedemit = !(d_ptr->mTagToActions.contains(tag));  // 记录是否需要发射信号
	d_ptr->mTagToActions[ tag ].append(act);
	// 绑定槽
	connect(act, &QObject::destroyed, this, &SARibbonActionsManager::onActionDestroyed);
	if (isneedemit && enableEmit) {
		// 说明新增tag
		Q_EMIT actionTagChanged(tag, false);
	}
	return (true);
}

/**
 * \if ENGLISH
 * @brief Unregister action
 *
 * If the last action corresponding to a tag is removed, the tag will also be deleted
 * @param act Action to unregister
 * @param enableEmit Whether to emit signal
 * @note Tag deletion will trigger actionTagChanged signal
 * @note If action is associated with multiple tags, the action will be removed from all tags, same for key
 * \endif
 *
 * \if CHINESE
 * @brief 取消action的注册
 *
 * 如果tag对应的最后一个action被撤销，tag也将一块删除
 * @param act 要取消注册的action
 * @param enableEmit 是否发射信号
 * @note tag的删除会触发actionTagChanged信号
 * @note 如果action关联了多个tag，这些tag里的action都会被删除，对应的key也同理
 * \endif
 */
void SARibbonActionsManager::unregisteAction(QAction* act, bool enableEmit)
{
	if (nullptr == act) {
		return;
	}
	// 绑定槽
	disconnect(act, &QObject::destroyed, this, &SARibbonActionsManager::onActionDestroyed);
	removeAction(act, enableEmit);
}

/**
 * @brief 移除action
 *
 * 仅移除内存内容
 * @param act
 * @param enableEmit
 */
void SARibbonActionsManager::removeAction(QAction* act, bool enableEmit)
{
	QList< int > deletedTags;                     // 记录删除的tag，用于触发actionTagChanged
	QMap< int, QList< QAction* > > tagToActions;  ///< tag : QList<QAction*>

	for (auto i = d_ptr->mTagToActions.begin(); i != d_ptr->mTagToActions.end(); ++i) {
		// 把不是act的内容转移到tagToActions和tagToActionKeys中，之后再和m_d里的替换
		auto tmpi = tagToActions.insert(i.key(), QList< QAction* >());
		int count = 0;
		for (int j = 0; j < i.value().size(); ++j) {
			if (i.value()[ j ] != act) {
				tmpi.value().append(act);
				++count;
			}
		}
		if (0 == count) {
			// 说明这个tag没有内容
			tagToActions.erase(tmpi);
			deletedTags.append(i.key());
		}
	}
	// 删除mKeyToAction
	QString key = d_ptr->mActionToKey.value(act);

	d_ptr->mActionToKey.remove(act);
	d_ptr->mKeyToAction.remove(key);

	// 置换
	d_ptr->mTagToActions.swap(tagToActions);
	// 发射信号
	if (enableEmit) {
		for (int tagdelete : sa_as_const(deletedTags)) {
			Q_EMIT actionTagChanged(tagdelete, true);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Equivalent to actions function
 * @param tag Action tag
 * @return Reference to action list for the tag
 * \endif
 *
 * \if CHINESE
 * @brief 等同actions函数
 * @param tag 动作标签
 * @return 对应标签的动作列表引用
 * \endif
 */
QList< QAction* >& SARibbonActionsManager::filter(int tag)
{
	return (actions(tag));
}

/**
 * \if ENGLISH
 * @brief Get actions by tag
 * @param tag Action tag
 * @return Reference to action list for the tag
 * \endif
 *
 * \if CHINESE
 * @brief 根据tag得到actions
 * @param tag 动作标签
 * @return 对应标签的动作列表引用
 * \endif
 */
QList< QAction* >& SARibbonActionsManager::actions(int tag)
{
	return (d_ptr->mTagToActions[ tag ]);
}

/**
 * \if ENGLISH
 * @brief Get actions by tag (const version)
 * @param tag Action tag
 * @return Action list for the tag
 * \endif
 *
 * \if CHINESE
 * @brief 根据tag得到actions（常量版本）
 * @param tag 动作标签
 * @return 对应标签的动作列表
 * \endif
 */
const QList< QAction* > SARibbonActionsManager::actions(int tag) const
{
	return (d_ptr->mTagToActions[ tag ]);
}

/**
 * \if ENGLISH
 * @brief Get all tags
 * @return List of all action tags
 * \endif
 *
 * \if CHINESE
 * @brief 获取所有的标签
 * @return 所有动作标签的列表
 * \endif
 */
QList< int > SARibbonActionsManager::actionTags() const
{
	return (d_ptr->mTagToActions.keys());
}

/**
 * \if ENGLISH
 * @brief Get action by key
 * @param key Action key
 * @return Action pointer, returns nullptr if key not found
 * \endif
 *
 * \if CHINESE
 * @brief 通过key获取action
 * @param key 动作键
 * @return 动作指针，如果没有key，返回nullptr
 * \endif
 */
QAction* SARibbonActionsManager::action(const QString& key) const
{
	return (d_ptr->mKeyToAction.value(key, nullptr));
}

/**
 * \if ENGLISH
 * @brief Get key by action
 * @param act Action pointer
 * @return Action key, returns empty QString if action not found
 * \endif
 *
 * \if CHINESE
 * @brief 通过action找到key
 * @param act 动作指针
 * @return 动作键，如果找不到，返回QString()
 * \endif
 */
QString SARibbonActionsManager::key(QAction* act) const
{
	return (d_ptr->mActionToKey.value(act, QString()));
}

/**
 * \if ENGLISH
 * @brief Get count of all managed actions
 * @return Number of managed actions
 * \endif
 *
 * \if CHINESE
 * @brief 返回所有管理的action数
 * @return 管理的动作数量
 * \endif
 */
int SARibbonActionsManager::count() const
{
	return (d_ptr->mKeyToAction.size());
}

/**
 * \if ENGLISH
 * @brief Get all managed actions
 * @return List of all managed actions
 * \endif
 *
 * \if CHINESE
 * @brief 返回所有管理的actions
 * @return 所有管理的动作列表
 * \endif
 */
QList< QAction* > SARibbonActionsManager::allActions() const
{
	return (d_ptr->mKeyToAction.values());
}

/**
 * @brief 自动加载SARibbonBar的action
 * 此函数会遍历@ref SARibbonBar的父窗口(一般是SARibbonMainWindow)下的所有子object，找到action注册，
 * 并会遍历所有@ref SARibbonCategory,把SARibbonCategory下的action按SARibbonCategory的title name进行分类
 *
 * 此函数会把所有category下的action生成tag并注册，返回的QMap<int, SARibbonCategory *>是记录了category对应的tag
 *
 * 此函数还会把SARibbonBar的父窗口(一般是SARibbonMainWindow)下面的action，但不在任何一个category下的作为NotInRibbonCategoryTag标签注册，默认名字会赋予not
 * in ribbon， 可以通过@ref setTagName 改变
 *
 * @param w
 * @return
 * @note 此函数的调用最好在category设置了标题后调用，因为会以category的标题作为标签的命名
 */
QMap< int, SARibbonCategory* > SARibbonActionsManager::autoRegisteActions(SARibbonBar* bar)
{
	QMap< int, SARibbonCategory* > res;
	// 先遍历SARibbonBar的父窗口(一般是SARibbonMainWindow)下的所有子对象，把所有action找到
	QWidget* parWidget = bar->parentWidget();
	QSet< QAction* > mainwindowActions;
	if (parWidget) {
		for (QObject* o : sa_as_const(parWidget->children())) {
			if (QAction* a = qobject_cast< QAction* >(o)) {
				// 说明是action
				if (!a->objectName().isEmpty()) {
					mainwindowActions.insert(a);
				}
			}
		}
	}
	// 开始遍历每个category，加入action

	if (nullptr == bar) {
		// 非ribbon模式，直接退出
		return (res);
	}
	QSet< QAction* > categoryActions;
	QList< SARibbonCategory* > categorys = bar->categoryPages();
	int tag                              = AutoCategoryDistinguishBeginTag;

	for (SARibbonCategory* c : sa_as_const(categorys)) {
		QList< SARibbonPanel* > panels = c->panelList();
		for (SARibbonPanel* p : sa_as_const(panels)) {
			categoryActions += autoRegisteWidgetActions(p, tag, false);
		}
		setTagName(tag, c->categoryName());
		res[ tag ] = c;
		++tag;
	}
	// 找到不在功能区的actions
	QSet< QAction* > notincategory = mainwindowActions - categoryActions;

	for (QAction* a : sa_as_const(notincategory)) {
		if (!a->objectName().isEmpty()) {
			registeAction(a, NotInRibbonCategoryTag, a->objectName(), false);
		}
	}
	if (notincategory.size() > 0) {
		setTagName(NotInRibbonCategoryTag, tr("not in ribbon"));
	}
	for (auto i = res.begin(); i != res.end(); ++i) {
		connect(i.value(), &SARibbonCategory::categoryNameChanged, this, &SARibbonActionsManager::onCategoryTitleChanged);
	}
	d_ptr->mTagToCategory = res;
	return (res);
}

/**
 * @brief 自动加载action
 * @param w
 * @param tag
 * @param enableEmit
 * @return 返回成功加入SARibbonActionsManager管理的action
 */
QSet< QAction* > SARibbonActionsManager::autoRegisteWidgetActions(QWidget* w, int tag, bool enableEmit)
{
	QSet< QAction* > res;
	QList< QAction* > was = w->actions();

	for (QAction* a : sa_as_const(was)) {
		if (res.contains(a) || a->objectName().isEmpty()) {
			// 重复内容不重复加入
			// 没有object name不加入
			continue;
		}
		if (registeAction(a, tag, a->objectName(), enableEmit)) {
			res.insert(a);
		}
	}
	return (res);
}

/**
 * @brief 根据标题查找action
 * @param text
 * @return
 */
QList< QAction* > SARibbonActionsManager::search(const QString& text)
{
	QList< QAction* > res;

	if (text.isEmpty()) {
		return (res);
	}
	QStringList kws = text.split(" ");

	if (kws.isEmpty()) {
		kws.append(text);
	}

	for (const QString& k : sa_as_const(kws)) {
		for (auto i = d_ptr->mActionToKey.begin(); i != d_ptr->mActionToKey.end(); ++i) {
			if (i.key()->text().contains(k, Qt::CaseInsensitive)) {
				res.append(i.key());
			}
		}
	}
	return (res);
}

void SARibbonActionsManager::clear()
{
	d_ptr->clear();
}

/**
 * @brief action 被delete时，将触发此槽把管理的action删除
 * @param o
 * @note 这个函数不会触发actionTagChanged信号
 */
void SARibbonActionsManager::onActionDestroyed(QObject* o)
{
	QAction* act = static_cast< QAction* >(o);

	removeAction(act, false);
}

/**
 * @brief autoRegisteActions函数会关联此槽，在标签内容改变时改变tag 对应 文本
 * @param title
 */
void SARibbonActionsManager::onCategoryTitleChanged(const QString& title)
{
	SARibbonCategory* c = qobject_cast< SARibbonCategory* >(sender());

	if (nullptr == c) {
		return;
	}
	int tag = d_ptr->mTagToCategory.key(c, -1);

	if (tag == -1) {
		return;
	}
	setTagName(tag, title);
}

////////////////////////////////////////////////////////////////////////////////////////////////////////
/// SARibbonActionsModel
////////////////////////////////////////////////////////////////////////////////////////////////////////

class SARibbonActionsManagerModel::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonActionsManagerModel)
public:
	PrivateData(SARibbonActionsManagerModel* p);
	void updateRef();
	int count() const;
	QAction* at(int index);
	bool isNull() const;

public:
	SARibbonActionsManager* mMgr { nullptr };
	int mTag { SARibbonActionsManager::CommonlyUsedActionTag };
	QString mSeatchText;
	QList< QAction* > mActions;
};

SARibbonActionsManagerModel::PrivateData::PrivateData(SARibbonActionsManagerModel* p) : q_ptr(p)
{
}

void SARibbonActionsManagerModel::PrivateData::updateRef()
{
	if (isNull()) {
		return;
	}
	if (!mSeatchText.isEmpty()) {
		mActions = mMgr->search(mSeatchText);
	} else {
		mActions = mMgr->actions(mTag);
	}
}

int SARibbonActionsManagerModel::PrivateData::count() const
{
	if (isNull()) {
		return (0);
	}
	return (mActions.size());
}

QAction* SARibbonActionsManagerModel::PrivateData::at(int index)
{
	if (isNull()) {
		return (nullptr);
	}
	if (index >= count()) {
		return (nullptr);
	}
	return (mActions.at(index));
}

bool SARibbonActionsManagerModel::PrivateData::isNull() const
{
	return (mMgr == nullptr);
}

//===================================================
// SARibbonActionsManagerModel
//===================================================

SARibbonActionsManagerModel::SARibbonActionsManagerModel(QObject* p)
	: QAbstractListModel(p), d_ptr(new SARibbonActionsManagerModel::PrivateData(this))
{
}

SARibbonActionsManagerModel::SARibbonActionsManagerModel(SARibbonActionsManager* m, QObject* p)
	: QAbstractListModel(p), d_ptr(new SARibbonActionsManagerModel::PrivateData(this))
{
	setupActionsManager(m);
}

SARibbonActionsManagerModel::~SARibbonActionsManagerModel()
{
}

int SARibbonActionsManagerModel::rowCount(const QModelIndex& parent) const
{
	if (parent.isValid()) {  // 非顶层
		return (0);
	}
	// 顶层
	return (d_ptr->count());
}

QVariant SARibbonActionsManagerModel::headerData(int section, Qt::Orientation orientation, int role) const
{
	Q_UNUSED(section);
	if (role != Qt::DisplayRole) {
		return (QVariant());
	}
	if (Qt::Horizontal == orientation) {
		return (tr("action name"));
	}
	return (QVariant());
}

Qt::ItemFlags SARibbonActionsManagerModel::flags(const QModelIndex& index) const
{
	if (!index.isValid()) {
		return (Qt::NoItemFlags);
	}
	return (Qt::ItemIsSelectable | Qt::ItemIsEnabled);
}

QVariant SARibbonActionsManagerModel::data(const QModelIndex& index, int role) const
{
	QAction* act = indexToAction(index);

	if (nullptr == act) {
		return (QVariant());
	}

	switch (role) {
	case Qt::DisplayRole: {
		QString str = act->text();
		if (!str.isEmpty()) {
			return str;
		}
		// 如果文本为空，一般是没有设置文本，或者是窗口，如果是窗口，那么用windowtitle作为文本
		if (QWidgetAction* wa = qobject_cast< QWidgetAction* >(act)) {
			if (QWidget* w = wa->defaultWidget()) {
				str = w->windowTitle();
			}
		}
		return str;
	}
	case Qt::DecorationRole:
		return (act->icon());

	default:
		break;
	}
	return (QVariant());
}

void SARibbonActionsManagerModel::setFilter(int tag)
{
	d_ptr->mTag = tag;
	update();
}

void SARibbonActionsManagerModel::update()
{
	beginResetModel();
	d_ptr->updateRef();
	endResetModel();
}

void SARibbonActionsManagerModel::setupActionsManager(SARibbonActionsManager* m)
{
	d_ptr->mMgr     = m;
	d_ptr->mTag     = SARibbonActionsManager::CommonlyUsedActionTag;
	d_ptr->mActions = m->filter(d_ptr->mTag);
	connect(m, &SARibbonActionsManager::actionTagChanged, this, &SARibbonActionsManagerModel::onActionTagChanged);
	update();
}

void SARibbonActionsManagerModel::uninstallActionsManager()
{
	if (!d_ptr->isNull()) {
		disconnect(
			d_ptr->mMgr, &SARibbonActionsManager::actionTagChanged, this, &SARibbonActionsManagerModel::onActionTagChanged);
		d_ptr->mMgr = nullptr;
		d_ptr->mTag = SARibbonActionsManager::CommonlyUsedActionTag;
	}
	update();
}

QAction* SARibbonActionsManagerModel::indexToAction(QModelIndex index) const
{
	if (!index.isValid()) {
		return (nullptr);
	}
	if (index.row() >= d_ptr->count()) {
		return (nullptr);
	}
	return (d_ptr->at(index.row()));
}

/**
 * @brief 查找
 * @param text
 */
void SARibbonActionsManagerModel::search(const QString& text)
{
	d_ptr->mSeatchText = text;
	update();
}

void SARibbonActionsManagerModel::onActionTagChanged(int tag, bool isdelete)
{
	if (isdelete && (tag == d_ptr->mTag)) {
		d_ptr->mTag = SARibbonActionsManager::UnknowActionTag;
		update();
	} else {
		if (tag == d_ptr->mTag) {
			update();
		}
	}
}

/*** End of inlined file: SARibbonActionsManager.cpp ***/


/*** Start of inlined file: SARibbonButtonGroupWidget.cpp ***/
#include <QHBoxLayout>
#include <QDebug>
#include <QMargins>
#include <QChildEvent>
#include <QActionEvent>
#include <QWidgetAction>
#include <QApplication>
#include <QStyle>

//===================================================
// SARibbonButtonGroupWidget
//===================================================

SARibbonButtonGroupWidget::SARibbonButtonGroupWidget(QWidget* parent) : QToolBar(parent)
{
	setAutoFillBackground(false);
	setAttribute(Qt::WA_NoSystemBackground);
	setOrientation(Qt::Horizontal);
	setSizePolicy(QSizePolicy::Fixed, QSizePolicy::Fixed);
	setMovable(false);    // 禁止移动
	setFloatable(false);  // 禁止浮动
	setContentsMargins(0, 0, 0, 0);
	const int smallIconSize = QApplication::style()->pixelMetric(QStyle::PM_SmallIconSize);
	setIconSize(QSize(smallIconSize, smallIconSize));
}

SARibbonButtonGroupWidget::~SARibbonButtonGroupWidget()
{
}

/**
 * \if ENGLISH
 * @brief Add a menu QAction to the button group widget
 * @param menuAction QAction object that has been set with a menu
 * @param popupMode Popup mode of the tool button, default is InstantPopup
 * @details This function is used to add a QAction that has been associated with a menu to the toolbar and set its popup mode.
 * The popup mode of the tool button determines how users interact with the menu.
 * @note If menuAction is not associated with a menu, this function will not perform any special processing
 * @note This function will automatically find the corresponding QToolButton and set the popup mode
 * @par Example:
 * @code
 * QMenu *fileMenu = new QMenu("File");
 * fileMenu->addAction("New");
 * fileMenu->addAction("Open");
 *
 * QAction *fileAction = new QAction("File");
 * fileAction->setMenu(fileMenu);
 *
 * // Add to button group widget
 * buttongroup->addMenuAction(fileAction, QToolButton::InstantPopup);
 * @endcode
 * @see QToolButton::ToolButtonPopupMode
 * \endif
 *
 * \if CHINESE
 * @brief 在按钮栏中添加一个带菜单的 QAction
 * @param menuAction 已经设置了菜单的 QAction 对象
 * @param popupMode 工具按钮的弹出模式，默认为 InstantPopup
 * @details 此函数用于向工具栏添加一个已经关联了菜单的 QAction，并设置其弹出模式。工具按钮的弹出模式决定了用户如何与菜单进行交互。
 * @note 如果 menuAction 没有关联菜单，此函数不会进行任何特殊处理
 * @note 此函数会自动查找对应的 QToolButton 并设置弹出模式
 * @par 示例:
 * @code
 * QMenu *fileMenu = new QMenu("File");
 * fileMenu->addAction("New");
 * fileMenu->addAction("Open");
 *
 * QAction *fileAction = new QAction("File");
 * fileAction->setMenu(fileMenu);
 *
 * // 添加到按钮栏
 * buttongroup->addMenuAction(fileAction, QToolButton::InstantPopup);
 * @endcode
 * @see QToolButton::ToolButtonPopupMode
 * \endif
 */
void SARibbonButtonGroupWidget::addMenuAction(QAction* menuAction, QToolButton::ToolButtonPopupMode popupMode)
{
	if (!menuAction) {
		return;
	}

	// 添加动作到工具栏
	addAction(menuAction);

	// 如果动作有关联菜单，则设置工具按钮的弹出模式
	if (menuAction->menu()) {
		QToolButton* toolButton = qobject_cast< QToolButton* >(widgetForAction(menuAction));
		if (toolButton) {
			toolButton->setPopupMode(popupMode);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Create and add a menu action to the button group widget
 * @param menu Menu object to associate
 * @param popupMode Popup mode of the tool button, default is InstantPopup
 * @return Returns the newly created QAction object, or nullptr if the parameter is invalid
 * @details This function creates a new QAction based on the specified text, associates it with the specified menu, adds it to the toolbar, and sets the popup mode.
 * @note The caller does not need to manually manage the lifecycle of the returned QAction, it will be automatically managed by the toolbar
 * @par Example:
 * @code
 * // Create menu
 * QMenu *helpMenu = new QMenu("Help");
 * helpMenu->addAction("Contents");
 * helpMenu->addAction("About");
 *
 * // Add menu to button group widget
 * QAction *helpAction = buttongroup->addMenuAction(helpMenu, QToolButton::MenuButtonPopup);
 * @endcode
 * @see QToolButton::ToolButtonPopupMode
 * \endif
 *
 * \if CHINESE
 * @brief 在按钮栏中创建并添加一个带菜单的动作
 * @param menu 要关联的菜单对象
 * @param popupMode 工具按钮的弹出模式，默认为 InstantPopup
 * @return 返回新创建的 QAction 对象，如果参数无效则返回 nullptr
 * @details 此函数根据指定的文本创建一个新的 QAction，将其与指定菜单关联，添加到工具栏并设置弹出模式。
 * @note 调用者不需要手动管理返回的 QAction 的生命周期，它会由工具栏自动管理
 * @par 示例:
 * @code
 * // 创建菜单
 * QMenu *helpMenu = new QMenu("Help");
 * helpMenu->addAction("Contents");
 * helpMenu->addAction("About");
 *
 * // 添加菜单到按钮栏
 * QAction *helpAction = buttongroup->addMenuAction(helpMenu, QToolButton::MenuButtonPopup);
 * @endcode
 * @see QToolButton::ToolButtonPopupMode
 * \endif
 */
QAction* SARibbonButtonGroupWidget::addMenuAction(QMenu* menu, QToolButton::ToolButtonPopupMode popupMode)
{
	if (!menu) {
		return nullptr;
	}
	addMenuAction(menu->menuAction(), popupMode);
	return menu->menuAction();
}

/*** End of inlined file: SARibbonButtonGroupWidget.cpp ***/


/*** Start of inlined file: SARibbonStackedWidget.cpp ***/
#include <QEventLoop>
#include <QResizeEvent>
#include <QMouseEvent>
#include <QApplication>
#include <QPropertyAnimation>
#include <QLayout>
#ifndef SARIBBONSTACKEDWIDGET_DEBUG_PRINT
#define SARIBBONSTACKEDWIDGET_DEBUG_PRINT 0
#endif
#if SARIBBONSTACKEDWIDGET_DEBUG_PRINT
#include <QDebug>
#endif
/**
 * \if ENGLISH
 * @brief Private data class for SARibbonStackedWidget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonStackedWidget的私有数据类
 * \endif
 */
class SARibbonStackedWidget::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonStackedWidget)
public:
	QEventLoop* eventLoop { nullptr };
	bool useAnimation { false };                ///< Whether to use animation
	QPropertyAnimation* animation { nullptr };  ///< Animation object
	QRect normalGeometry;                       ///< Geometry position in normal state
	bool isAnimating { false };  ///< Mark whether animation is in progress, this is necessary, it marks whether animation is started earlier than animation->state()
	int animationWidgetHeight { 0 };  ///< Window height during animation
public:
	PrivateData(SARibbonStackedWidget* p) : q_ptr(p)
	{
	}

	/**
	 * \if ENGLISH
	 * @brief Check if animation is running
	 * @return true if animation is running, false otherwise
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 检查动画是否正在运行
	 * @return 如果动画正在运行返回 true，否则返回 false
	 * \endif
	 */
	bool isAnimationRunning() const
	{
		return (isAnimating || animation->state() == QAbstractAnimation::Running);
	}
};

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonStackedWidget
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonStackedWidget 构造函数
 * @param parent 父窗口
 * \endif
 */
SARibbonStackedWidget::SARibbonStackedWidget(QWidget* parent)
	: QStackedWidget(parent), d_ptr(new SARibbonStackedWidget::PrivateData(this))
{
	setNormalMode();
	setupAnimation();
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonStackedWidget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonStackedWidget 析构函数
 * \endif
 */
SARibbonStackedWidget::~SARibbonStackedWidget()
{
}

/**
 * \if ENGLISH
 * @brief Setup animation
 * \endif
 *
 * \if CHINESE
 * @brief 设置动画
 * \endif
 */
void SARibbonStackedWidget::setupAnimation()
{
	d_ptr->animation = new QPropertyAnimation(this, "animationWidgetHeight", this);
	d_ptr->animation->setEasingCurve(QEasingCurve::OutQuad);
	d_ptr->animation->setDuration(300);
	connect(d_ptr->animation, &QPropertyAnimation::finished, this, &SARibbonStackedWidget::onAnimationFinished);
}

/**
 * \if ENGLISH
 * @brief Get animation widget height
 * @return Animation widget height
 * \endif
 *
 * \if CHINESE
 * @brief 获取动画窗口高度
 * @return 动画窗口高度
 * \endif
 */
int SARibbonStackedWidget::animationWidgetHeight() const
{
	return d_ptr->animationWidgetHeight;
}

/**
 * \if ENGLISH
 * @brief Set animation widget height
 * @param h Height value
 * \endif
 *
 * \if CHINESE
 * @brief 设置动画窗口高度
 * @param h 高度值
 * \endif
 */
void SARibbonStackedWidget::setAnimationWidgetHeight(int h)
{
	if (d_ptr->animationWidgetHeight == h) {
		return;
	}

	d_ptr->animationWidgetHeight = h;

	if (d_ptr->isAnimationRunning() && isPopupMode()) {
		// 更新窗口大小和位置
		setFixedSize(d_ptr->normalGeometry.width(), h);
#if SARIBBONSTACKEDWIDGET_DEBUG_PRINT
		qDebug() << "setAnimationWidgetHeight setFixedSize=" << d_ptr->normalGeometry.width() << "," << h;
#endif
	}
}

/**
 * \if ENGLISH
 * @brief Set window normalGeometry, since this window has animation, to prevent size setting from being overwritten by animation during animation process, this window's size setting uses setNormalSize
 * @details This function is equivalent to the following when there is no animation:
 * @details @code
 * @details setFixedSize(normalGeometry.width(),normalGeometry.height());
 * @details move(normalGeometry.x(),normalGeometry.y());
 * @details @endcode
 * @param normalGeometry
 * \endif
 *
 * \if CHINESE
 * @brief 设置窗口normalGeometry，由于此窗口会有动画，防止动画过程中设置尺寸又被动画覆盖，因此此窗口的尺寸设置使用setNormalSize
 * @details 此函数在没有动画的时候，等同于
 * @details @code
 * @details setFixedSize(normalGeometry.width(),normalGeometry.height());
 * @details move(normalGeometry.x(),normalGeometry.y());
 * @details @endcode
 * @param normalGeometry
 * \endif
 */
void SARibbonStackedWidget::setNormalGeometry(const QRect& normalGeometry)
{
	d_ptr->normalGeometry = normalGeometry;
	if (!d_ptr->isAnimationRunning()) {
		setFixedSize(d_ptr->normalGeometry.width(), d_ptr->normalGeometry.height());
		move(d_ptr->normalGeometry.x(), d_ptr->normalGeometry.y());
	}
}

/**
 * \if ENGLISH
 * @brief Get normal geometry
 * @return Normal geometry
 * \endif
 *
 * \if CHINESE
 * @brief 获取正常几何位置
 * @return 正常几何位置
 * \endif
 */
QRect SARibbonStackedWidget::normalGeometry() const
{
	return d_ptr->normalGeometry;
}

/**
 * \if ENGLISH
 * @brief Send layout request to inner widgets
 * @details This method will invalidate and recalculate the layout of child widgets
 * \endif
 *
 * \if CHINESE
 * @brief 对内部窗口发送布局请求
 * @details 这个方法会让子窗口布局失效同时重新计算布局
 * \endif
 */
void SARibbonStackedWidget::layoutRequestInnerWidgets()
{
	// 确保所有子部件都填满整个区域
	for (int i = 0; i < count(); ++i) {
		QWidget* innerWidget = widget(i);
		if (!innerWidget) {
			continue;
		}
		// 方法1 update（不生效）
		//  innerWidget->update();

		// 方法2 postEvent（不生效）
		//  QApplication::postEvent(innerWidget, new QEvent(QEvent::LayoutRequest));

		// 方法3 invalidate+activate（生效）
		if (auto lay = innerWidget->layout()) {
			lay->invalidate();
			lay->activate();
		}
	}
}

/**
 * \if ENGLISH
 * @brief Set popup mode
 * @details In this mode, the window will:
 * @details - Set Qt::Popup flag
 * @details - Frameless window
 * @details - Display as panel style
 * @note Switching modes will automatically handle window hiding and showing
 * \endif
 *
 * \if CHINESE
 * @brief 设置弹出模式
 * @details 在此模式下，窗口将：
 * @details - 设置为 Qt::Popup 标志
 * @details - 无边框窗口
 * @details - 显示为面板样式
 * @note 切换模式时会自动处理窗口隐藏和显示
 * \endif
 */
void SARibbonStackedWidget::setPopupMode()
{
	if (isPopupMode()) {
		return;
	}
	setMouseTracking(true);
	setWindowFlags(Qt::Popup | Qt::FramelessWindowHint);
	setFrameShape(QFrame::Panel);
}

/**
 * \if ENGLISH
 * @brief Check if currently in popup mode
 * @return true if in popup mode, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查当前是否处于弹出模式
 * @return 如果是弹出模式返回 true，否则返回 false
 * \endif
 */
bool SARibbonStackedWidget::isPopupMode() const
{
	return (windowFlags() & Qt::Popup);
}

/**
 * \if ENGLISH
 * @brief Set normal mode, same as normal stackwidget
 * @details In this mode, the window will:
 * @details - Set to regular widget mode (Qt::Widget)
 * @details - Frameless window
 * @details - No frame display
 * @note If there is a modal event loop, it will exit that loop
 * \endif
 *
 * \if CHINESE
 * @brief 设置正常模式 和普通的stackwidget一样
 * @details 在此模式下，窗口将：
 * @details - 设置为常规部件模式 (Qt::Widget)
 * @details - 无边框窗口
 * @details - 不显示框架
 * @note 如果存在模态事件循环，将退出该循环
 * \endif
 */
void SARibbonStackedWidget::setNormalMode()
{
	if (isNormalMode()) {
		return;
	}
	if (d_ptr->eventLoop) {
		d_ptr->eventLoop->exit();
		d_ptr->eventLoop = nullptr;
	}
	// 停止动画并恢复最终位置
	if (d_ptr->isAnimationRunning()) {
		d_ptr->animation->stop();
		d_ptr->isAnimating = false;  // 停止后，一定要加上标记
		// 升级到qt6后，setFixedHeight容易出现异常，主要原因是setFixedHeight的实现如下：
		// void QWidget::setFixedHeight(int h) {
		//     // ...
		//     setMinimumSize(minimumSize().width(), h);  // 先设置最小高度
		//     setMaximumSize(maximumSize().width(), h);  // 再设置最大高度
		//     // ...
		// }
		// 如果调用前 maximumSize().height() 已经小于你要设置的 h，那么先调用 setMinimumSize(h)
		// 时，最小高度会大于当前最大高度，这会触发Qt6的断言检查。 因此在调用 setFixedHeight 前，先重置最大尺寸限制：
		if (maximumHeight() < d_ptr->normalGeometry.height()) {
			setMaximumHeight(QWIDGETSIZE_MAX);
		}
		setFixedHeight(d_ptr->normalGeometry.height());
	}
	setMouseTracking(false);
	setWindowFlags(Qt::Widget | Qt::FramelessWindowHint);
	setFrameShape(QFrame::NoFrame);
}

/**
 * \if ENGLISH
 * @brief Check if currently in normal mode
 * @return true if in normal mode, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查当前是否处于正常模式
 * @return 如果是正常模式返回 true，否则返回 false
 * \endif
 */
bool SARibbonStackedWidget::isNormalMode() const
{
	return (!isPopupMode());
}

/**
 * \if ENGLISH
 * @brief Run event loop modally in popup mode
 * @details This method is only valid in popup mode:
 * @details 1. Show window
 * @details 2. Start local event loop
 * @details 3. Exit event loop when window is hidden
 * @note Only show window in normal mode
 * @warning Do not call this method again in event loop
 * \endif
 *
 * \if CHINESE
 * @brief 在弹出模式下以模态方式运行事件循环
 * @details 此方法仅在弹出模式下有效：
 * @details 1. 显示窗口
 * @details 2. 启动局部事件循环
 * @details 3. 窗口隐藏时退出事件循环
 * @note 在正常模式下调用此方法仅显示窗口
 * @warning 不要在事件循环中再次调用此方法
 * \endif
 */
void SARibbonStackedWidget::exec()
{
	if (!isPopupMode()) {
		show();
		return;
	}
	show();
	QEventLoop event;
	d_ptr->eventLoop = &event;
	event.exec();
	d_ptr->eventLoop = nullptr;  // 确保重置
}

/**
 * \if ENGLISH
 * @brief Similar to tabbar's moveTab function, swap the index of two widgets
 * @param from Source index
 * @param to Target index
 * @note This operation will trigger widgetRemoved(int index) signal
 * \endif
 *
 * \if CHINESE
 * @brief 类似tabbar的moveTab函数，交换两个窗口的index
 * @param from 源索引
 * @param to 目标索引
 * @note 此操作会触发widgetRemoved(int index)信号
 * \endif
 */
void SARibbonStackedWidget::moveWidget(int from, int to)
{
	QWidget* w = widget(from);

	removeWidget(w);
	insertWidget(to, w);
}

/**
 * \if ENGLISH
 * @brief Set whether to enable popup animation
 * @param on Enable or disable
 * \endif
 *
 * \if CHINESE
 * @brief 设置是否启用弹出动画
 * @param on 是否启用
 * \endif
 */
void SARibbonStackedWidget::setUseAnimation(bool on)
{
	d_ptr->useAnimation = on;
}

/**
 * \if ENGLISH
 * @brief Get animation enabled state
 * @return Animation enabled state
 * \endif
 *
 * \if CHINESE
 * @brief 获取动画启用状态
 * @return 动画启用状态
 * \endif
 */
bool SARibbonStackedWidget::isUseAnimation() const
{
	return d_ptr->useAnimation;
}

/**
 * \if ENGLISH
 * @brief Set animation duration (milliseconds)
 * @param duration Duration in milliseconds
 * \endif
 *
 * \if CHINESE
 * @brief 设置动画持续时间（毫秒）
 * @param duration 持续时间（毫秒）
 * \endif
 */
void SARibbonStackedWidget::setAnimationDuration(int duration)
{
	d_ptr->animation->setDuration(duration);
}

/**
 * \if ENGLISH
 * @brief Get animation duration
 * @return Animation duration in milliseconds
 * \endif
 *
 * \if CHINESE
 * @brief 获取动画持续时间
 * @return 动画持续时间（毫秒）
 * \endif
 */
int SARibbonStackedWidget::animationDuration() const
{
	return d_ptr->animation->duration();
}

/**
 * \if ENGLISH
 * @brief Animation finished slot function
 * \endif
 *
 * \if CHINESE
 * @brief 动画完成槽函数
 * \endif
 */
void SARibbonStackedWidget::onAnimationFinished()
{
	d_ptr->isAnimating = false;
	if (isPopupMode()) {
		// 完成显示后，把其它窗口的尺寸移动到位置
		if (height() != d_ptr->normalGeometry.height()) {
			// 恢复窗口到正常位置
			// 升级到qt6后，setFixedHeight容易出现异常，主要原因是setFixedHeight的实现如下：
			// void QWidget::setFixedHeight(int h) {
			//     // ...
			//     setMinimumSize(minimumSize().width(), h);  // 先设置最小高度
			//     setMaximumSize(maximumSize().width(), h);  // 再设置最大高度
			//     // ...
			// }
			// 如果调用前 maximumSize().height() 已经小于你要设置的 h，那么先调用 setMinimumSize(h) 时，最小高度会大于当前最大高度，这会触发Qt6的断言检查。
			// 因此在调用 setFixedHeight 前，先重置最大尺寸限制：
			if (maximumHeight() < d_ptr->normalGeometry.height()) {
				setMaximumHeight(QWIDGETSIZE_MAX);
			}
			setFixedHeight(d_ptr->normalGeometry.height());
		} else {
			updateInnerWidgetGeometry();
		}
	}
}

/**
 * \if ENGLISH
 * @brief Handle show event
 * @param e Show event
 * \endif
 *
 * \if CHINESE
 * @brief 处理显示事件
 * @param e 显示事件
 * \endif
 */
void SARibbonStackedWidget::showEvent(QShowEvent* e)
{
	if (isPopupMode() && d_ptr->useAnimation && !d_ptr->isAnimationRunning()) {
		// 这个必须放在最前面，否则setFixedHeight(0);就会把子窗口的尺寸改变
		d_ptr->isAnimating = true;
		// 标记为显示动画

		// 设置动画参数
		d_ptr->animation->setStartValue(0);
		d_ptr->animation->setEndValue(d_ptr->normalGeometry.height());

		// 启动动画
		d_ptr->animation->start();
		// 设置起始位置
		// 升级到qt6后，setFixedHeight容易出现异常，主要原因是setFixedHeight的实现如下：
		// void QWidget::setFixedHeight(int h) {
		//     // ...
		//     setMinimumSize(minimumSize().width(), h);  // 先设置最小高度
		//     setMaximumSize(maximumSize().width(), h);  // 再设置最大高度
		//     // ...
		// }
		// 如果调用前 maximumSize().height() 已经小于你要设置的 h，那么先调用 setMinimumSize(h)
		// 时，最小高度会大于当前最大高度，这会触发Qt6的断言检查。 因此在调用 setFixedHeight 前，先重置最大尺寸限制：
		if (maximumHeight() < d_ptr->normalGeometry.height()) {
			setMaximumHeight(QWIDGETSIZE_MAX);
		}
		setFixedHeight(0);
#if SARIBBONSTACKEDWIDGET_DEBUG_PRINT
		qDebug() << "Starting show animation. Current state:" << d_ptr->animation->state()
				 << "Start value:" << d_ptr->animation->startValue().toInt()
				 << "End value:" << d_ptr->animation->endValue().toInt();
#endif

		// 确保动画已启动
		QCoreApplication::processEvents();
		QStackedWidget::showEvent(e);
	} else {
		QStackedWidget::showEvent(e);
	}
}

/**
 * \if ENGLISH
 * @brief Handle hide event
 * @param e Hide event
 * \endif
 *
 * \if CHINESE
 * @brief 处理隐藏事件
 * @param e 隐藏事件
 * \endif
 */
void SARibbonStackedWidget::hideEvent(QHideEvent* e)
{
	if (!isPopupMode()) {
		// 非弹出模式，正常的隐藏
		QStackedWidget::hideEvent(e);
		return;
	}

	if (d_ptr->eventLoop) {
		d_ptr->eventLoop->exit();
		d_ptr->eventLoop = nullptr;
	}
	Q_EMIT hidWindow();
	QStackedWidget::hideEvent(e);
}

/**
 * \if ENGLISH
 * @brief Update inner widget geometry
 * \endif
 *
 * \if CHINESE
 * @brief 更新内部窗口几何位置
 * \endif
 */
void SARibbonStackedWidget::updateInnerWidgetGeometry()
{
	// 确保所有子部件都填满整个区域
	const QSize newSize = size();
	for (int i = 0; i < count(); ++i) {
		QWidget* innerWidget = widget(i);
		if (!innerWidget)
			continue;

		if (i == currentIndex()) {
			// 确保当前部件也填满整个区域
			if (innerWidget->size() != newSize) {
				innerWidget->move(0, 0);
				innerWidget->setFixedSize(newSize);
			}
			continue;
		}

		// 设置非活动部件的位置和大小
		innerWidget->move(0, 0);
		innerWidget->setFixedSize(newSize);
		// innerWidget->setGeometry(0, 0, newSize.width(), newSize.height());
		// 通知部件布局可能需要更新
#if SARIBBONSTACKEDWIDGET_DEBUG_PRINT
		qDebug() << "SARibbonStackedWidget::resizeEvent [not Animation]innerWidget move to:" << innerWidget->geometry();
#endif
		QApplication::postEvent(innerWidget, new QEvent(QEvent::LayoutRequest));
	}
}

/**
 * \if ENGLISH
 * @brief Handle resize event
 * @param e Resize event
 * \endif
 *
 * \if CHINESE
 * @brief 处理尺寸改变事件
 * @param e 尺寸改变事件
 * \endif
 */
void SARibbonStackedWidget::resizeEvent(QResizeEvent* e)
{
	if (d_ptr->isAnimationRunning() && d_ptr->useAnimation) {
		// 正在动画过程中，移动当前窗口的位置
		QWidget* innerWidget = currentWidget();
		if (!innerWidget) {
			return;
		}
		int x = innerWidget->x();
		if (innerWidget->size() != d_ptr->normalGeometry.size()) {
			innerWidget->setFixedSize(d_ptr->normalGeometry.size());
		}
#if SARIBBONSTACKEDWIDGET_DEBUG_PRINT
		qDebug() << "SARibbonStackedWidget::resizeEvent innerWidget geo:" << innerWidget->geometry();
#endif
		innerWidget->move(x, e->size().height() - d_ptr->normalGeometry.height());

#if SARIBBONSTACKEDWIDGET_DEBUG_PRINT
		qDebug() << "SARibbonStackedWidget::resizeEvent innerWidget move to:" << innerWidget->geometry();
#endif

	} else {
		// 没有动画，尺寸的改变记录下来
		d_ptr->normalGeometry = geometry();
		updateInnerWidgetGeometry();
	}
	// 调用基类处理
	QStackedWidget::resizeEvent(e);
}

/*** End of inlined file: SARibbonStackedWidget.cpp ***/


/*** Start of inlined file: SARibbonSeparatorWidget.cpp ***/
#include <QApplication>
#include <QScreen>
#include <QStylePainter>
#include <QPainter>
#include <QDebug>

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonSeparatorWidget
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonSeparatorWidget 构造函数
 * @param parent 父窗口
 * \endif
 */
SARibbonSeparatorWidget::SARibbonSeparatorWidget(QWidget* parent) : QFrame(parent)
{
	setFrameShape(QFrame::VLine);
	setFrameShadow(QFrame::Plain);

	if (QScreen* screen = QApplication::primaryScreen()) {
		qreal dpr           = screen->physicalDotsPerInch() / screen->logicalDotsPerInch();
		int scaledLineWidth = qRound(1.0 * dpr);  // 假设基础 lineWidth 是 1
		if (scaledLineWidth < 1) {
			scaledLineWidth = 1;
		}
		setLineWidth(scaledLineWidth);
		//    qDebug() << "SARibbonSeparatorWidget:" << scaledLineWidth;
	} else {
		setLineWidth(1);
	}
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonSeparatorWidget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonSeparatorWidget 析构函数
 * \endif
 */
SARibbonSeparatorWidget::~SARibbonSeparatorWidget()
{
}

/*** End of inlined file: SARibbonSeparatorWidget.cpp ***/


/*** Start of inlined file: SARibbonCtrlContainer.cpp ***/
#include <QHBoxLayout>
#include <QPainter>
#include <QPaintEvent>
#include <QStylePainter>
#include <QDebug>
#include <QLabel>
#include <QApplication>
#include <QScreen>

/**
 * \if ENGLISH
 * @brief Private data class for SARibbonCtrlContainer
 * @details This class holds private data for SARibbonCtrlContainer to implement the PIMPL idiom.
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonCtrlContainerPrivate 类
 * @details 此类持有 SARibbonCtrlContainer 的私有数据，实现 PIMPL 设计模式。
 * \endif
 */
class SARibbonCtrlContainer::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonCtrlContainer)
public:
	QWidget* mContainerWidget { nullptr };
	QHBoxLayout* mHorizontalLayout { nullptr };
	QLabel* mLabelPixmap { nullptr };
	QLabel* mLabelText { nullptr };
	QSize mIconSize { 24, 24 };
	QIcon mIcon;
	PrivateData(SARibbonCtrlContainer* p) : q_ptr(p)
	{
		mHorizontalLayout = new QHBoxLayout(p);
		mHorizontalLayout->setSpacing(0);
		mHorizontalLayout->setObjectName(QString::fromUtf8("horizontalLayout"));
		mHorizontalLayout->setContentsMargins(0, 0, 0, 0);

		mLabelPixmap = new QLabel(p);
		mLabelPixmap->setObjectName(QString::fromUtf8("labelPixmap"));
		mLabelPixmap->setAlignment(Qt::AlignCenter);
		mHorizontalLayout->addWidget(mLabelPixmap);

		mLabelText = new QLabel(p);
		mLabelText->setObjectName(QString::fromUtf8("labelText"));
		mHorizontalLayout->addWidget(mLabelText);
	}

	/**
	 * \if ENGLISH
	 * @brief Set the container widget
	 * @param w Widget to set as container
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 设置容器窗口
	 * @param w 要设置为容器的窗口
	 * \endif
	 */
	void setContainerWidget(QWidget* w)
	{
		if (mContainerWidget) {
			// 原来有widget
			QWidget* oldwidget = mContainerWidget;
			takeContainerWidget(oldwidget);
			oldwidget->deleteLater();
		}
		mContainerWidget = w;
		if (!w) {
			return;
		}
		if (w->parent() != q_ptr) {
			w->setParent(q_ptr);
		}
		// ContainerWidget永远在最右边
		mHorizontalLayout->insertWidget(mHorizontalLayout->count(), w);
		QSizePolicy sizePolicy(QSizePolicy::Expanding, QSizePolicy::Preferred);
		sizePolicy.setHorizontalStretch(0);
		sizePolicy.setVerticalStretch(0);
		w->setSizePolicy(sizePolicy);
	}

	/**
	 * \if ENGLISH
	 * @brief Take the container widget from layout
	 * @param w Widget to remove from layout
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 从布局中移除容器窗口
	 * @param w 要从布局中移除的窗口
	 * \endif
	 */
	void takeContainerWidget(QWidget* w)
	{
		int i = mHorizontalLayout->indexOf(w);
		if (i >= 0) {
			QLayoutItem* item = mHorizontalLayout->takeAt(i);
			delete item;
		}
	}
};

//===================================================
// SARibbonCtrlContainer
//===================================================
SARibbonCtrlContainer::SARibbonCtrlContainer(QWidget* parent)
	: QWidget(parent), d_ptr(new SARibbonCtrlContainer::PrivateData(this))
{
}

SARibbonCtrlContainer::~SARibbonCtrlContainer()
{
}

QWidget* SARibbonCtrlContainer::containerWidget()
{
	return (d_ptr->mContainerWidget);
}

const QWidget* SARibbonCtrlContainer::containerWidget() const
{
	return (d_ptr->mContainerWidget);
}

void SARibbonCtrlContainer::setEnableShowIcon(bool b)
{
	d_ptr->mLabelPixmap->setVisible(b);
}

void SARibbonCtrlContainer::setEnableShowTitle(bool b)
{
	d_ptr->mLabelText->setVisible(b);
}

/**
 * \if ENGLISH
 * @brief Check if container widget exists
 * @return true if container widget exists
 * \endif
 *
 * \if CHINESE
 * @brief 判断是否存在容器窗口
 * @return 如果容器窗口存在返回true
 * \endif
 */
bool SARibbonCtrlContainer::hasContainerWidget() const
{
	return (d_ptr->mContainerWidget != nullptr);
}

/**
 * \if ENGLISH
 * @brief Set the icon
 * @param i Icon to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置图标
 * @param i 要设置的图标
 * \endif
 */
void SARibbonCtrlContainer::setIcon(const QIcon& i)
{
	d_ptr->mIcon = i;
	// QPixmap pixmap = i.pixmap(d_ptr->mIconSize);
	QPixmap pixmap = SA::iconToPixmap(i, d_ptr->mIconSize, SA::widgetDevicePixelRatio(this));
	d_ptr->mLabelPixmap->setPixmap(pixmap);
}

void SARibbonCtrlContainer::setIcon(const QPixmap& pixmap)
{
	d_ptr->mLabelPixmap->setPixmap(pixmap);
}

/**
 * \if ENGLISH
 * @brief Get the icon
 * @return Current icon
 * \endif
 *
 * \if CHINESE
 * @brief 获取图标
 * @return 当前图标
 * \endif
 */
QIcon SARibbonCtrlContainer::icon() const
{
	return d_ptr->mIcon;
}

/**
 * \if ENGLISH
 * @brief Set the text
 * @param t Text to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置文字
 * @param t 要设置的文字
 * \endif
 */
void SARibbonCtrlContainer::setText(const QString& t)
{
	d_ptr->mLabelText->setText(t);
}

/**
 * \if ENGLISH
 * @brief Get the text
 * @return Current text
 * \endif
 *
 * \if CHINESE
 * @brief 获取文字
 * @return 当前文字
 * \endif
 */
QString SARibbonCtrlContainer::text() const
{
	return d_ptr->mLabelText->text();
}

void SARibbonCtrlContainer::setContainerWidget(QWidget* w)
{
	d_ptr->setContainerWidget(w);
}

/**
 * \if ENGLISH
 * @brief Get the widget displaying the icon
 * @return Widget displaying the icon
 * \endif
 *
 * \if CHINESE
 * @brief 获取显示icon的窗口
 * @return 显示图标的窗口
 * \endif
 */
QWidget* SARibbonCtrlContainer::iconWidget() const
{
	return d_ptr->mLabelPixmap;
}

/*** End of inlined file: SARibbonCtrlContainer.cpp ***/


/*** Start of inlined file: SARibbonQuickAccessBar.cpp ***/
#include <QGuiApplication>

//===================================================
// SARibbonQuickAccessBar
//===================================================
/**
 * \if ENGLISH
 * @brief Constructs a SARibbonQuickAccessBar instance
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个 SARibbonQuickAccessBar 实例
 * @param parent 父窗口部件
 * \endif
 */
SARibbonQuickAccessBar::SARibbonQuickAccessBar(QWidget* parent) : SARibbonButtonGroupWidget(parent)
{
}

/**
 * \if ENGLISH
 * @brief Destructor
 * \endif
 *
 * \if CHINESE
 * @brief 析构函数
 * \endif
 */
SARibbonQuickAccessBar::~SARibbonQuickAccessBar()
{
}

/*** End of inlined file: SARibbonQuickAccessBar.cpp ***/


/*** Start of inlined file: SARibbonTabBar.cpp ***/
#include <QStyleOptionTab>
#include <QFontMetrics>

/**
 * \if ENGLISH
 * @brief Constructs a SARibbonTabBar instance
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个 SARibbonTabBar 实例
 * @param parent 父窗口部件
 * \endif
 */
SARibbonTabBar::SARibbonTabBar(QWidget* parent) : QTabBar(parent), m_tabMargin(6, 0, 0, 0)
{
	setExpanding(false);
}

/**
 * \if ENGLISH
 * @brief Destructor
 * \endif
 *
 * \if CHINESE
 * @brief 析构函数
 * \endif
 */
SARibbonTabBar::~SARibbonTabBar()
{
}

/**
 * \if ENGLISH
 * @brief Gets the tab margin
 * @return The tab margin
 * \endif
 *
 * \if CHINESE
 * @brief 获取tab边距
 * @return tab边距
 * \endif
 */
const QMargins& SARibbonTabBar::tabMargin() const
{
	return (m_tabMargin);
}

/**
 * \if ENGLISH
 * @brief Sets the tab margin
 * @param tabMargin The tab margin to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置tab边距
 * @param tabMargin 要设置的tab边距
 * \endif
 */
void SARibbonTabBar::setTabMargin(const QMargins& tabMargin)
{
	m_tabMargin = tabMargin;
}

/**
 * \if ENGLISH
 * @brief Estimates the size of a tab
 *
 * Unlike the system default tabbar, SARibbonTabBar's tab height is the same as the tabbar height, and does not consider vertical distribution
 * @param index The index of the tab
 * @return The estimated size of the tab
 * \endif
 *
 * \if CHINESE
 * @brief tab的尺寸预估
 *
 * 有别于系统默认的tabbar，SARibbonTabBar的tab高度和tabbar高度一致，且不考虑纵向分布情况
 * @param index tab的索引
 * @return tab的预估尺寸
 * \endif
 */
QSize SARibbonTabBar::tabSizeHint(int index) const
{
	if (index < 0) {
		return QSize();
	}
	QStyleOptionTab opt;
	initStyleOption(&opt, index);
	int hframe            = style()->pixelMetric(QStyle::PM_TabBarTabHSpace, &opt, this);
	const QFontMetrics fm = fontMetrics();

	int widgetWidth = 0;
	int padding     = 0;
	if (!opt.leftButtonSize.isEmpty()) {
		padding += 4;
		widgetWidth += opt.leftButtonSize.width();
	}
	if (!opt.rightButtonSize.isEmpty()) {
		padding += 4;
		widgetWidth += opt.rightButtonSize.width();
	}
	if (!opt.icon.isNull()) {
		padding += 4;
	}
	const int textWidth = fm.size(Qt::TextShowMnemonic, opt.text).width();
	QSize csz           = QSize(textWidth + opt.iconSize.width() + hframe + widgetWidth + padding, height());
	return style()->sizeFromContents(QStyle::CT_TabBarTab, &opt, csz, this);
}

/*** End of inlined file: SARibbonTabBar.cpp ***/


/*** Start of inlined file: SARibbonMenu.cpp ***/
#include <QWidgetAction>

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonMenu
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonMenu 构造函数
 * @param parent 父窗口
 * \endif
 */
SARibbonMenu::SARibbonMenu(QWidget *parent) : QMenu(parent)
{
	setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Expanding);
}

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonMenu with title
 * @param title Menu title
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonMenu 构造函数（带标题）
 * @param title 菜单标题
 * @param parent 父窗口
 * \endif
 */
SARibbonMenu::SARibbonMenu(const QString& title, QWidget *parent) : QMenu(title, parent)
{
	setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Expanding);
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonMenu
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonMenu 析构函数
 * \endif
 */
SARibbonMenu::~SARibbonMenu()
{
}

/**
 * \if ENGLISH
 * @brief Add a SARibbonMenu
 * @param menu SARibbonMenu pointer
 * @return QAction pointer
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个SARibbonMenu
 * @param menu SARibbonMenu指针
 * @return QAction指针
 * \endif
 */
QAction *SARibbonMenu::addRibbonMenu(SARibbonMenu *menu)
{
	return (QMenu::addMenu(menu));
}

/**
 * \if ENGLISH
 * @brief Add a SARibbonMenu with title
 * @param title Menu title
 * @return SARibbonMenu pointer
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个带标题的SARibbonMenu
 * @param title 菜单标题
 * @return SARibbonMenu指针
 * \endif
 */
SARibbonMenu *SARibbonMenu::addRibbonMenu(const QString& title)
{
	SARibbonMenu *menu = new SARibbonMenu(title, this);

	return (menu);
}

/**
 * \if ENGLISH
 * @brief Add a SARibbonMenu with icon and title
 * @param icon Menu icon
 * @param title Menu title
 * @return SARibbonMenu pointer
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个带图标和标题的SARibbonMenu
 * @param icon 菜单图标
 * @param title 菜单标题
 * @return SARibbonMenu指针
 * \endif
 */
SARibbonMenu *SARibbonMenu::addRibbonMenu(const QIcon& icon, const QString& title)
{
	SARibbonMenu *menu = new SARibbonMenu(title, this);

	menu->setIcon(icon);
	return (menu);
}

/**
 * \if ENGLISH
 * @brief Add a widget to the menu
 * @param w Widget to add
 * @return QAction pointer
 * \endif
 *
 * \if CHINESE
 * @brief 向菜单添加一个部件
 * @param w 要添加的部件
 * @return QAction指针
 * \endif
 */
QAction *SARibbonMenu::addWidget(QWidget *w)
{
	QWidgetAction *action = new QWidgetAction(this);

	action->setDefaultWidget(w);
	addAction(action);
	return (action);
}

/*** End of inlined file: SARibbonMenu.cpp ***/


/*** Start of inlined file: SARibbonTitleIconWidget.cpp ***/
// SARibbonTitleIconWidget.cpp

#include <QPainter>
#include <QMouseEvent>
#include <QContextMenuEvent>
#include <QApplication>
#include <QStyle>
#include <QScreen>
#include <QWindow>
#include <QAction>
#include <QDebug>
/**
 * @brief SARibbonTitleIconWidget::SARibbonTitleIconWidget
 * 构造函数，初始化标题栏图标控件
 * @param parent 父窗口部件
 */
SARibbonTitleIconWidget::SARibbonTitleIconWidget(QWidget* parent)
	: QWidget(parent), m_widget(nullptr), m_iconSize(16, 16), mPadding(1)
{
	setCursor(Qt::ArrowCursor);
	createContextMenu();
}

/**
 * @brief SARibbonTitleIconWidget::setWidget
 * 设置关联的窗口对象，使用QPointer确保安全性
 * @param window 要关联的窗口
 */
void SARibbonTitleIconWidget::setWidget(QWidget* window)
{
	m_widget = window;
}

/**
 * @brief 关联的窗口对象
 * @return
 */
QWidget* SARibbonTitleIconWidget::widget() const
{
	return m_widget.data();
}

/**
 * @brief SARibbonTitleIconWidget::setIcon
 * 设置显示的图标
 * @param icon 要显示的图标
 */
void SARibbonTitleIconWidget::setIcon(const QIcon& icon)
{
	m_icon = icon;
	update();
}

/**
 * @brief 图标
 * @return
 */
QIcon SARibbonTitleIconWidget::icon() const
{
	return m_icon;
}

/**
 * @brief SARibbonTitleIconWidget::setIconSize
 * 设置图标大小
 * @param size 图标尺寸
 */
void SARibbonTitleIconWidget::setIconSize(const QSize& size)
{
	m_iconSize = size;
	update();
}

/**
 * @brief 图标大小
 * @return
 */
QSize SARibbonTitleIconWidget::iconSize() const
{
	return m_iconSize;
}

/**
 * @brief 内边距
 * @return
 */
int SARibbonTitleIconWidget::padding() const
{
	return mPadding;
}

/**
 * @brief 设置内边距
 * @param v
 */
void SARibbonTitleIconWidget::setPadding(int v)
{
	mPadding = v;
}

QSize SARibbonTitleIconWidget::sizeHint() const
{
	return QSize(m_iconSize.width() + mPadding, m_iconSize.height() + mPadding);
}

void SARibbonTitleIconWidget::paintEvent(QPaintEvent* event)
{
	Q_UNUSED(event)

	QPainter painter(this);
	// 绘制图标
	if (!m_icon.isNull()) {
		QSize iconSize = m_iconSize.scaled(rect().size(), Qt::KeepAspectRatio);
		iconSize.setWidth(iconSize.width() - 2 * padding());
		iconSize.setHeight(iconSize.height() - 2 * padding());
		iconSize       = iconSize.expandedTo(QSize(8, 8));
		QRect iconRect = QRect(
			(width() - iconSize.width()) / 2, (height() - iconSize.height()) / 2, iconSize.width(), iconSize.height());
		m_icon.paint(&painter, iconRect);
	}
}

void SARibbonTitleIconWidget::mousePressEvent(QMouseEvent* event)
{
	if (event->button() == Qt::LeftButton && m_widget) {
		if (m_contextMenu) {
			QPoint menuPos = mapToGlobal(QPoint(0, height()));
			m_contextMenu->popup(menuPos);
			event->accept();
			return;
		}
	}
	QWidget::mousePressEvent(event);
}

void SARibbonTitleIconWidget::contextMenuEvent(QContextMenuEvent* event)
{
	if (m_contextMenu && m_widget) {
		QPoint menuPos = event->globalPos();
		m_contextMenu->popup(menuPos);
		event->accept();
		return;
	}
	QWidget::contextMenuEvent(event);
}

/**
 * @brief 创建上下文菜单，包含标准的窗口控制选项
 */
void SARibbonTitleIconWidget::createContextMenu()
{
	m_contextMenu = new QMenu(this);
	setupMenuActions();
}

/**
 * @brief 设置菜单动作，包括还原、移动、大小、最小化、最大化和关闭操作
 */
void SARibbonTitleIconWidget::setupMenuActions()
{
	// 还原菜单项
	QAction* restoreAction = new QAction(tr("Restore(R)"), this);  // cn:还原
	restoreAction->setIcon(style()->standardIcon(QStyle::SP_TitleBarNormalButton));
	connect(restoreAction, &QAction::triggered, this, &SARibbonTitleIconWidget::onRestore);

	// 移动菜单项
	QAction* moveAction = new QAction(tr("Move(M)"), this);  // cn:移动
	connect(moveAction, &QAction::triggered, this, &SARibbonTitleIconWidget::onMove);

	// 大小菜单项
	QAction* sizeAction = new QAction(tr("Size(S)"), this);  // cn:大小
	connect(sizeAction, &QAction::triggered, this, &SARibbonTitleIconWidget::onSize);

	// 分隔线
	m_contextMenu->addSeparator();

	// 最小化菜单项
	QAction* minimizeAction = new QAction(tr("Minimize(N)"), this);  // cn:最小化
	minimizeAction->setIcon(style()->standardIcon(QStyle::SP_TitleBarMinButton));
	connect(minimizeAction, &QAction::triggered, this, &SARibbonTitleIconWidget::onMinimize);

	// 最大化菜单项
	QAction* maximizeAction = new QAction(tr("Maximize(X)"), this);  // cn:最小化
	maximizeAction->setIcon(style()->standardIcon(QStyle::SP_TitleBarMaxButton));
	connect(maximizeAction, &QAction::triggered, this, &SARibbonTitleIconWidget::onMaximize);

	// 分隔线
	m_contextMenu->addSeparator();

	// 关闭菜单项
	QAction* closeAction = new QAction(tr("Close(C)"), this);  // cn:关闭
	closeAction->setIcon(style()->standardIcon(QStyle::SP_TitleBarCloseButton));
	connect(closeAction, &QAction::triggered, this, &SARibbonTitleIconWidget::onClose);

	// 添加到菜单
	m_contextMenu->addAction(restoreAction);
	m_contextMenu->addAction(moveAction);
	m_contextMenu->addAction(sizeAction);
	m_contextMenu->addAction(minimizeAction);
	m_contextMenu->addAction(maximizeAction);
	m_contextMenu->addAction(closeAction);
}

/**
 * @brief SARibbonTitleIconWidget::onRestore
 * 还原窗口到正常大小
 */
void SARibbonTitleIconWidget::onRestore()
{
	if (m_widget) {
		m_widget->showNormal();
	}
}

/**
 * @brief SARibbonTitleIconWidget::onMove
 * 启动窗口移动模式
 */
void SARibbonTitleIconWidget::onMove()
{
	if (m_widget) {
		// 在实际应用中，这里可以触发窗口移动逻辑
		// 或者通过其他方式实现移动功能
		m_widget->setFocus();
	}
}

/**
 * @brief SARibbonTitleIconWidget::onSize
 * 启动窗口大小调整模式
 */
void SARibbonTitleIconWidget::onSize()
{
	if (m_widget) {
		// 在实际应用中，这里可以触发窗口大小调整逻辑
		// 或者通过其他方式实现大小调整功能
		m_widget->setFocus();
	}
}

/**
 * @brief SARibbonTitleIconWidget::onMinimize
 * 最小化窗口
 */
void SARibbonTitleIconWidget::onMinimize()
{
	if (m_widget) {
		m_widget->showMinimized();
	}
}

/**
 * @brief SARibbonTitleIconWidget::onMaximize
 * 切换窗口最大化状态
 */
void SARibbonTitleIconWidget::onMaximize()
{
	if (m_widget) {
		if (m_widget->isMaximized()) {
			m_widget->showNormal();
		} else {
			m_widget->showMaximized();
		}
	}
}

/**
 * @brief SARibbonTitleIconWidget::onClose
 * 关闭窗口
 */
void SARibbonTitleIconWidget::onClose()
{
	if (m_widget) {
		m_widget->close();
	}
}

/*** End of inlined file: SARibbonTitleIconWidget.cpp ***/


/*** Start of inlined file: SARibbonPanelOptionButton.cpp ***/
#include <QAction>

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonPanelOptionButton
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonPanelOptionButton 构造函数
 * @param parent 父窗口
 * \endif
 */
SARibbonPanelOptionButton::SARibbonPanelOptionButton(QWidget* parent) : QToolButton(parent)
{
	setAutoRaise(true);
	setCheckable(false);
	setToolButtonStyle(Qt::ToolButtonIconOnly);
	setIconSize(QSize(10, 10));
	static QIcon s_default_icon = QIcon(":/SARibbon/image/resource/ribbonPanelOptionButton.png");
	setIcon(s_default_icon);
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonPanelOptionButton
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonPanelOptionButton 析构函数
 * \endif
 */
SARibbonPanelOptionButton::~SARibbonPanelOptionButton()
{
}

/*** End of inlined file: SARibbonPanelOptionButton.cpp ***/


/*** Start of inlined file: SARibbonPanelItem.cpp ***/
/**
 * \if ENGLISH
 * @brief Constructs a SARibbonPanelItem instance
 * @param widget The widget to wrap
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个 SARibbonPanelItem 实例
 * @param widget 要包装的窗口部件
 * \endif
 */
SARibbonPanelItem::SARibbonPanelItem(QWidget* widget)
	: QWidgetItem(widget), rowIndex(-1), columnIndex(-1), action(nullptr), customWidget(false), rowProportion(Large)
{
}

/**
 * \if ENGLISH
 * @brief Destructor
 * \endif
 *
 * \if CHINESE
 * @brief 析构函数
 * \endif
 */
SARibbonPanelItem::~SARibbonPanelItem()
{
}

/**
 * \if ENGLISH
 * @brief Checks if the item is empty
 * @return true if the item is empty, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查项是否为空
 * @return 如果项为空则返回true，否则返回false
 * \endif
 */
bool SARibbonPanelItem::isEmpty() const
{
	return (action == nullptr || !action->isVisible());
}

/*** End of inlined file: SARibbonPanelItem.cpp ***/


/*** Start of inlined file: SARibbonPanelLayout.cpp ***/
#include <QWidgetAction>
#include <QQueue>

#ifndef SARibbonPanelLayout_DEBUG_PRINT
#define SARibbonPanelLayout_DEBUG_PRINT 0
#endif

#if SARibbonPanelLayout_DEBUG_PRINT
#ifndef SARibbonPanelLayout_HELP_DRAW_RECT
#define SARibbonPanelLayout_HELP_DRAW_RECT(p, rect)                                                                    \
	do {                                                                                                               \
		p.save();                                                                                                      \
		QPen _pen(Qt::DashLine);                                                                                       \
		_pen.setColor(Qt::blue);                                                                                       \
		p.setPen(_pen);                                                                                                \
		p.setBrush(QBrush());                                                                                          \
		p.drawRect(rect);                                                                                              \
		p.restore();                                                                                                   \
	} while (0)
#endif  // SARibbonPanelLayout_HELP_DRAW_RECT
#endif  // SARibbonPanelLayout_DEBUG_PRINT
/**
 * \if ENGLISH
 * @brief Constructs a SARibbonPanelLayout instance
 * @param p Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个 SARibbonPanelLayout 实例
 * @param p 父窗口部件
 * \endif
 */
SARibbonPanelLayout::SARibbonPanelLayout(QWidget* p) : QLayout(p), mColumnCount(0), mExpandFlag(false), mDirty(true)
{
	setSpacing(1);
	SARibbonPanel* tb = qobject_cast< SARibbonPanel* >(p);

	if (!tb) {
		return;
	}
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonPanelLayout
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonPanelLayout析构函数
 * \endif
 */
SARibbonPanelLayout::~SARibbonPanelLayout()
{
	// 参考QToolBarLayout
	while (!mItems.isEmpty()) {
		SARibbonPanelItem* item = mItems.takeFirst();
		if (QWidgetAction* widgetAction = qobject_cast< QWidgetAction* >(item->action)) {
			if (item->customWidget) {
				widgetAction->releaseWidget(item->widget());
			}
		}
		delete item;
	}
}

/**
 * \if ENGLISH
 * @brief Finds the index of an action in the layout
 * @param action The action to find
 * @return The index of the action, or -1 if not found
 * \endif
 *
 * \if CHINESE
 * @brief 在布局中查找一个action的索引
 * @param action 要查找的action
 * @return action的索引，如果未找到则返回-1
 * \endif
 */
int SARibbonPanelLayout::indexByAction(QAction* action) const
{
	for (int i = 0; i < mItems.count(); ++i) {
		if (mItems.at(i)->action == action) {
			return (i);
		}
	}
	return (-1);
}

/**
 * \if ENGLISH
 * @brief Gets the SARibbonPanel that owns this layout
 * @return A pointer to the parent SARibbonPanel, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 获取拥有此布局的SARibbonPanel
 * @return 指向父SARibbonPanel的指针，如果未找到则返回nullptr
 * \endif
 */
SARibbonPanel* SARibbonPanelLayout::ribbonPanel() const
{
	return qobject_cast< SARibbonPanel* >(parentWidget());
}

/**
 * \if ENGLISH
 * @brief Adds an item to the layout (not supported)
 *
 * This layout only accepts items created from `QAction` via `insertAction`. Calling this function
 * directly will result in a warning.
 *
 * @param item The layout item to add
 * \endif
 *
 * \if CHINESE
 * @brief 向布局添加一个项目（不支持）
 *
 * 此布局仅接受通过 `insertAction` 从 `QAction` 创建的项目。直接调用此函数将导致警告。
 *
 * @param item 要添加的布局项目
 * \endif
 */
void SARibbonPanelLayout::addItem(QLayoutItem* item)
{
	Q_UNUSED(item);
	qWarning("SARibbonPanelLayout::addItem(): please use addAction() instead");
	return;
}

/**
 * \if ENGLISH
 * @brief Inserts an action at a specific index
 *
 * This is the primary method for adding content to the layout. It creates a `SARibbonPanelItem`
 * from the `QAction` and inserts it at the specified position.
 *
 * @param index The index at which to insert the action
 * @param act The action to insert
 * @param rp The row proportion for the action
 * \endif
 *
 * \if CHINESE
 * @brief 在指定索引处插入一个action
 *
 * 这是向布局添加内容的主要方法。它从 `QAction` 创建一个 `SARibbonPanelItem` 并将其插入到指定位置。
 *
 * @param index 插入action的索引
 * @param act 要插入的action
 * @param rp action的行占比
 * \endif
 */
void SARibbonPanelLayout::insertAction(int index, QAction* act, SARibbonPanelItem::RowProportion rp)
{
	index                   = qMax(0, index);
	index                   = qMin(mItems.count(), index);
	SARibbonPanelItem* item = createItem(act, rp);

	if (item) {
		mItems.insert(index, item);
		// 标记需要重新计算尺寸
		invalidate();
	}
}

/**
 * \if ENGLISH
 * @brief Sets the option action for the panel
 *
 * The option action is displayed as a button in the panel's title area.
 * Pass `nullptr` to remove the current option action.
 *
 * @param action The option action, or nullptr to remove
 * \endif
 *
 * \if CHINESE
 * @brief 为面板设置选项action
 *
 * 选项action显示在面板标题区域的一个按钮中。
 * 传入 `nullptr` 以移除当前的选项action。
 *
 * @param action 选项action，或传入nullptr以移除
 * \endif
 */
void SARibbonPanelLayout::setOptionAction(QAction* action)
{
	SARibbonPanel* p = ribbonPanel();
	if (!p) {
		return;
	}
	if (action) {
		// 创建option action
		if (nullptr == mOptionActionBtn) {
			mOptionActionBtn = RibbonSubElementFactory->createRibbonPanelOptionButton(p);
			QObject::connect(mOptionActionBtn, &SARibbonToolButton::triggered, p, &SARibbonPanel::actionTriggered);
			// 确保m_optionActionBtn在label之上
			if (mTitleLabel) {
				mTitleLabel->stackUnder(mOptionActionBtn);
			}
		}
		mOptionActionBtn->setDefaultAction(action);
		if (action->icon().isNull()) {
			mOptionActionBtn->setIcon(QIcon(":/SARibbon/image/resource/ribbonPanelOptionButton.png"));
		}
		// 标记需要重新计算尺寸
		invalidate();
	} else {
		// 取消option action
		if (mOptionActionBtn) {
			mOptionActionBtn->hide();
			mOptionActionBtn->deleteLater();
			mOptionActionBtn = nullptr;
			// 标记需要重新计算尺寸
			invalidate();
		}
	}
}

/**
 * \if ENGLISH
 * @brief Checks if an option action is set
 * @return true if an option action exists; otherwise false
 * \endif
 *
 * \if CHINESE
 * @brief 检查是否设置了选项action
 * @return 如果存在选项action则返回true；否则返回false
 * \endif
 */
bool SARibbonPanelLayout::isHaveOptionAction() const
{
	return (mOptionActionBtn != nullptr);
}

/**
 * \if ENGLISH
 * @brief Retrieves the item at the specified index
 * @param index The index of the item
 * @return The layout item, or nullptr if index is invalid
 * \endif
 *
 * \if CHINESE
 * @brief 获取指定索引处的项目
 * @param index 项目的索引
 * @return 布局项目，如果索引无效则返回nullptr
 * \endif
 */
QLayoutItem* SARibbonPanelLayout::itemAt(int index) const
{
	if ((index < 0) || (index >= mItems.count())) {
		return (nullptr);
	}
	return (mItems.at(index));
}

/**
 * \if ENGLISH
 * @brief Removes and returns the item at the specified index
 * @param index The index of the item to remove
 * @return The removed layout item, or nullptr if index is invalid
 * \endif
 *
 * \if CHINESE
 * @brief 移除并返回指定索引处的项目
 * @param index 要移除的项目的索引
 * @return 被移除的布局项目，如果索引无效则返回nullptr
 * \endif
 */
QLayoutItem* SARibbonPanelLayout::takeAt(int index)
{
	if ((index < 0) || (index >= mItems.count())) {
		return (nullptr);
	}
	SARibbonPanelItem* item = mItems.takeAt(index);

	QWidgetAction* widgetAction = qobject_cast< QWidgetAction* >(item->action);

	if ((widgetAction != 0) && item->customWidget) {
		widgetAction->releaseWidget(item->widget());
	} else {
		// destroy the QToolButton/QToolBarSeparator
		item->widget()->hide();
		item->widget()->deleteLater();
	}

	invalidate();
	return (item);
}

/**
 * \if ENGLISH
 * @brief Gets the number of items in the layout
 * @return The item count
 * \endif
 *
 * \if CHINESE
 * @brief 获取布局中项目的数量
 * @return 项目数量
 * \endif
 */
int SARibbonPanelLayout::count() const
{
	return (mItems.count());
}

/**
 * \if ENGLISH
 * @brief Checks if the layout is empty
 * @return true if the layout has no items; otherwise false
 * \endif
 *
 * \if CHINESE
 * @brief 检查布局是否为空
 * @return 如果布局没有项目则返回true；否则返回false
 * \endif
 */
bool SARibbonPanelLayout::isEmpty() const
{

	return (mItems.isEmpty());
}

/**
 * \if ENGLISH
 * @brief Invalidates the layout, marking it as dirty
 *
 * This forces a recalculation of the layout geometry on the next update.
 *
 * \endif
 *
 * \if CHINESE
 * @brief 使布局失效，将其标记为“脏”
 *
 * 这会强制在下次更新时重新计算布局几何形状。
 *
 * \endif
 */
void SARibbonPanelLayout::invalidate()
{
	mDirty = true;
	QLayout::invalidate();
}

/**
 * \if ENGLISH
 * @brief Returns the directions in which the layout can expand
 * @return The expanding directions (always Qt::Horizontal for this layout)
 * \endif
 *
 * \if CHINESE
 * @brief 返回布局可以扩展的方向
 * @return 扩展方向（此布局始终为Qt::Horizontal）
 * \endif
 */
Qt::Orientations SARibbonPanelLayout::expandingDirections() const
{
	return (Qt::Horizontal);
}

/**
 * \if ENGLISH
 * @brief Gets the minimum size of the layout
 * @return The minimum size
 * \endif
 *
 * \if CHINESE
 * @brief 获取布局的最小尺寸
 * @return 最小尺寸
 * \endif
 */
QSize SARibbonPanelLayout::minimumSize() const
{
	return (mSizeHint);
}

/**
 * \if ENGLISH
 * @brief Gets the size hint of the layout
 * @return The size hint
 * \endif
 *
 * \if CHINESE
 * @brief 获取布局的尺寸提示
 * @return 尺寸提示
 * \endif
 */
QSize SARibbonPanelLayout::sizeHint() const
{
#if SARibbonPanelLayout_DEBUG_PRINT
	if (SARibbonPanel* panel = ribbonPanel()) {
		qDebug() << "| |-SARibbonPanelLayout sizeHint,sizeHint = " << m_sizeHint;
	}
#endif
	return (mSizeHint);
}

/**
 * \if ENGLISH
 * @brief Retrieves the SARibbonPanelItem associated with an action
 * @param action The action to query
 * @return The associated item, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 获取与action关联的SARibbonPanelItem
 * @param action 要查询的action
 * @return 关联的项目，如果未找到则返回nullptr
 * \endif
 */
SARibbonPanelItem* SARibbonPanelLayout::panelItem(QAction* action) const
{
	int index = indexByAction(action);

	if (index >= 0) {
		return (mItems[ index ]);
	}
	return (nullptr);
}

/**
 * \if ENGLISH
 * @brief Gets the last item added to the layout
 * @return The last item, or nullptr if the layout is empty
 * \endif
 *
 * \if CHINESE
 * @brief 获取最后添加到布局的项目
 * @return 最后一个项目，如果布局为空则返回nullptr
 * \endif
 */
SARibbonPanelItem* SARibbonPanelLayout::lastItem() const
{
	if (mItems.isEmpty()) {
		return (nullptr);
	}
	return (mItems.last());
}

/**
 * \if ENGLISH
 * @brief Gets the widget associated with the last item
 * @return The widget, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 获取与最后一个项目关联的窗口部件
 * @return 窗口部件，如果未找到则返回nullptr
 * \endif
 */
QWidget* SARibbonPanelLayout::lastWidget() const
{
	SARibbonPanelItem* item = lastItem();

	if (item) {
		return (item->widget());
	}
	return (nullptr);
}

/**
 * \if ENGLISH
 * @brief Moves an item from one index to another
 * @param from The current index of the item
 * @param to The new index for the item
 * \endif
 *
 * \if CHINESE
 * @brief 将一个项目从一个索引移动到另一个索引
 * @param from 项目的当前索引
 * @param to 项目的新索引
 * \endif
 */
void SARibbonPanelLayout::move(int from, int to)
{
	if (from == to) {
		return;
	}
	if (to < 0) {
		to = 0;
	}
	if (to >= count()) {
		to = count() - 1;
	}
	mItems.move(from, to);
	invalidate();
}

/**
 * \if ENGLISH
 * @brief Checks if the layout needs to be relayouted
 * @return true if the layout is dirty and needs relayouting, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 判断是否需要重新布局
 * @return 如果布局需要重新布局则返回true，否则返回false
 * \endif
 */
bool SARibbonPanelLayout::isDirty() const
{
	return (mDirty);
}

/**
 * \if ENGLISH
 * @brief Updates the geometry array using the current geometry
 * \endif
 *
 * \if CHINESE
 * @brief 使用当前几何形状更新几何数组
 * \endif
 */
void SARibbonPanelLayout::updateGeomArray()
{
	updateGeomArray(geometry());
}

/**
 * \if ENGLISH
 * @brief Lays out all actions
 * \endif
 *
 * \if CHINESE
 * @brief 布局所有action
 * \endif
 */
void SARibbonPanelLayout::doLayout()
{
#if SARibbonPanelLayout_DEBUG_PRINT
	if (SARibbonPanel* panel = ribbonPanel()) {
		qDebug() << "| |-SARibbonPanelLayout layoutActions,panel name = " << panel->panelName();
	}
#endif
	if (isDirty()) {
		updateGeomArray();
	}
	QWidget* par = parentWidget();
	if (!par) {
		return;
	}
	QList< QWidget* > showWidgets, hideWidgets;
	for (SARibbonPanelItem* item : sa_as_const(mItems)) {
		if (item->isEmpty()) {
			hideWidgets << item->widget();
		} else {
			// 在category发现item->setGeometry有点奇怪的现象，这里统一使用item->widget->setgeo
			// item->setGeometry(item->itemWillSetGeometry);
			if (item->widget()) {
				item->widget()->setGeometry(item->itemWillSetGeometry);
			}
			showWidgets << item->widget();
		}
	}

	// 不在上面那里进行show和hide因为这会触发SARibbonPanelLayout的重绘，导致循环绘制，非常影响效率
	for (QWidget* w : sa_as_const(showWidgets)) {
		if (!w->isVisible())
			w->show();
	}
	for (QWidget* w : sa_as_const(hideWidgets)) {
		if (w->isVisible())
			w->hide();
	}

	// 布局label
	if (mTitleLabel) {
		if (isEnableShowPanelTitle()) {
			mTitleLabel->setGeometry(mTitleLabelGeometry);
			if (!mTitleLabel->isVisibleTo(par)) {
				mTitleLabel->show();
			}
		} else {
			if (mTitleLabel->isVisibleTo(par)) {
				mTitleLabel->hide();
			}
		}
	}
	// 布局m_optionActionBtn
	if (mOptionActionBtn) {
		if (!mOptionActionBtn->isVisibleTo(par)) {
			mOptionActionBtn->show();
		}
		mOptionActionBtn->setGeometry(mOptionActionBtnGeometry);
		mOptionActionBtn->setIconSize(QSize(mOptionActionBtnGeometry.width(), mOptionActionBtnGeometry.height()));
	}
}

/**
 * \if ENGLISH
 * @brief Converts an action to an item
 *
 * This function references QToolBarItem *QToolBarLayout::createItem(QAction *action)
 *
 * For regular QAction, this function creates a SARibbonToolButton. The type of SARibbonToolButton
 * is determined by SARibbonPanelItem::RowProportion.
 *
 * @param action The action to convert
 * @param rp Row proportion
 * @return The converted SARibbonPanelItem
 * @note Each SARibbonPanelItem will eventually carry a widget. If a QWidgetAction is passed,
 * it will directly use the widget carried by QWidgetAction. Otherwise, it will internally generate a SARibbonToolButton.
 * \endif
 *
 * \if CHINESE
 * @brief 把action转换为item
 *
 * 此函数参考QToolBarItem *QToolBarLayout::createItem(QAction *action)
 *
 * 对于普通QAction，此函数会创建SARibbonToolButton，SARibbonToolButton的类型参考SARibbonPanelItem::RowProportion，
 * @param action 要转换的action
 * @param rp 行高占比情况
 * @return 转换的SARibbonPanelItem
 * @note 每个SARibbonPanelItem最终都会携带一个widget，传入的是QWidgetAction的话，会直接使用QWidgetAction带的widget，
 * 否则会内部生成一个SARibbonToolButton
 * \endif
 */
SARibbonPanelItem* SARibbonPanelLayout::createItem(QAction* action, SARibbonPanelItem::RowProportion rp)
{
	bool customWidget    = false;
	QWidget* widget      = nullptr;
	SARibbonPanel* panel = qobject_cast< SARibbonPanel* >(parentWidget());

	if (!panel) {
		// 在没有panel这个函数会返回nullptr
		return (nullptr);
	}
	if (QWidgetAction* widgetAction = qobject_cast< QWidgetAction* >(action)) {
		widget = widgetAction->requestWidget(panel);
		if (widget != nullptr) {
			widget->setAttribute(Qt::WA_LayoutUsesWidgetRect);
			customWidget =
				true;  // 标记为true，在移除的时候是不会对这个窗口进行删除，false默认会进行删除如SARibbonSeparatorWidget和SARibbonToolButton
			// 明确设置父对象，确保 releaseWidget 时能正确工作
			widget->setParent(panel);
		}
	} else if (action->isSeparator()) {
		SARibbonSeparatorWidget* sep = RibbonSubElementFactory->createRibbonSeparatorWidget(panel);
		widget                       = sep;
	}
	// 不是widget，自动生成SARibbonToolbutton
	if (!widget) {
		SARibbonToolButton::RibbonButtonType buttonType =
			((rp == SARibbonPanelItem::Large) ? SARibbonToolButton::LargeButton : SARibbonToolButton::SmallButton);

		SARibbonToolButton* button = RibbonSubElementFactory->createRibbonToolButton(panel);
		button->setFocusPolicy(Qt::NoFocus);
		button->setButtonType(buttonType);
		button->setDefaultAction(action);
		button->setIconSize(mSmallToolButtonIconSize);
		button->setLargeIconSize(mLargeToolButtonIconSize);
		button->setEnableWordWrap(isEnableWordWrap());
		button->setButtonMaximumAspectRatio(mButtonMaximumAspectRatio);
		// 属性设置
		QToolButton::ToolButtonPopupMode popMode = SARibbonPanel::getActionToolButtonPopupModeProperty(action);
		button->setPopupMode(popMode);
		// 根据QAction的属性设置按钮的大小

		QObject::connect(button, &SARibbonToolButton::triggered, panel, &SARibbonPanel::actionTriggered);
		widget = button;
	}
	// 这时总会有widget
	widget->hide();
	SARibbonPanelItem* result = new SARibbonPanelItem(widget);

	result->rowProportion = rp;
	result->customWidget  = customWidget;
	result->action        = action;
	return (result);
}

/**
 * \if ENGLISH
 * @brief Updates the geometry array
 * @param setrect The rectangle to update
 * \endif
 *
 * \if CHINESE
 * @brief 更新尺寸
 * @param setrect 要更新的矩形
 * \endif
 */
void SARibbonPanelLayout::updateGeomArray(const QRect& setrect)
{
	SARibbonPanel* panel = qobject_cast< SARibbonPanel* >(parentWidget());

	if (!panel) {
		return;
	}

	const int height     = setrect.height();
	const QMargins& mag  = contentsMargins();
	const int spacing    = this->spacing();
	const int spacingRow = 1;  // 高度间距，也就是行间距，不同行之间的距离
	int x                = mag.left();
	const int yBegin     = mag.top();
	int titleH           = (mTitleHeight >= 0) ? mTitleHeight : 0;  // 防止负数影响
	int titleSpace       = (mTitleHeight >= 0) ? mTitleSpace : 0;   // 对于没有标题的情况，spacing就不生效
	if (!isEnableShowPanelTitle()) {
		titleH     = 0;
		titleSpace = 0;
	}
	// 获取panel的布局模式 3行或者2行
	//  rowcount 是ribbon的行，有2行和3行两种
	const short rowCount = (panel->panelLayoutMode() == SARibbonPanel::ThreeRowMode) ? 3
					  : (panel->panelLayoutMode() == SARibbonPanel::TwoRowMode) ? 2
					  : 1;
	// largeHeight是对应large占比的高度
	const int largeHeight = qMax(height - mag.bottom() - mag.top() - titleH - titleSpace, 2);  // 大按钮高度不小于2

	mLargeHeight = largeHeight;
	// 计算smallHeight的高度
	const int smallHeight = qMax((largeHeight - (rowCount - 1) * spacingRow) / rowCount, 1);
	// Medium行的y位置
	const int yMediumRow0 = (2 == rowCount) ? yBegin : (yBegin + ((largeHeight - 2 * smallHeight) / 3));
	const int yMediumRow1 = (2 == rowCount) ? (yBegin + smallHeight + spacingRow)
											: (yBegin + ((largeHeight - 2 * smallHeight) / 3) * 2 + smallHeight);
	// Small行的y位置
	const int ySmallRow0 = yBegin;
	const int ySmallRow1 = yBegin + smallHeight + spacingRow;
	const int ySmallRow2 = yBegin + 2 * (smallHeight + spacingRow);
	// row用于记录下个item应该属于第几行，item->rowIndex用于记录当前处于第几行，
	// item->rowIndex主要用于SARibbonPanelItem::Medium
	short row  = 0;
	int column = 0;
	// 记录每列最大的宽度
	int columMaxWidth = 0;
	// 记录总宽度
	int totalWidth = 0;

	int itemCount = mItems.count();

#if SARibbonPanelLayout_DEBUG_PRINT
	QString debug_print__log__;
#endif
	// 本列第一、二行占比
	SARibbonPanelItem::RowProportion thisColumnRP0 = SARibbonPanelItem::None;
	SARibbonPanelItem* lastGeomItem                = nullptr;  // 记录最后一个设置位置的item
	for (int i = 0; i < itemCount; ++i) {
		SARibbonPanelItem* item = mItems.at(i);
		if (item->isEmpty()) {
			// 如果是hide就直接跳过
			item->rowIndex    = -1;
			item->columnIndex = -1;
			continue;
		}
		// 当开始新的一列时，重置列宽
		if (row == 0) {
			columMaxWidth = 0;
		}
		QSize hint = item->sizeHint();
		// SingleRowMode：所有item在同一行，横向排列
		if (1 == rowCount) {
			item->rowIndex            = 0;
			item->columnIndex         = column;
			item->itemWillSetGeometry = QRect(x, yBegin, hint.width(), smallHeight);
			columMaxWidth             = hint.width();
			x += (columMaxWidth + spacing);
			row           = 0;
			columMaxWidth = 0;
			++column;
			lastGeomItem = item;
			continue;
		}
#if SARibbonPanelLayout_DEBUG_PRINT
		if (SARibbonToolButton* tb = qobject_cast< SARibbonToolButton* >(item->widget())) {
			auto ss__ = tb->sizeHint();
			debug_print__log__ += QString("| | |-[%1]SARibbonToolButton.sizeHint=(%2,%3),ButtonText=%4\n")
									  .arg(i)
									  .arg(ss__.width())
									  .arg(ss__.height())
									  .arg(tb->text());
		}
#endif
		Qt::Orientations exp = item->expandingDirections();
		if (item->widget()) {
			// 有窗口是水平扩展，则标记为扩展
			if ((item->widget()->sizePolicy().horizontalPolicy() & QSizePolicy::ExpandFlag)) {
				mExpandFlag = true;
			}
		}
		SARibbonPanelItem::RowProportion rp = item->rowProportion;
		if (SARibbonPanelItem::None == rp) {
			// 为定义行占比但是垂直扩展，就定义为Large占比，否则就是small占比
			if (exp & Qt::Vertical) {
				rp = SARibbonPanelItem::Large;
			} else {
				rp = SARibbonPanelItem::Small;
			}
		}
		// 开始根据占比和layoutmode来布局
		switch (rp) {
		case SARibbonPanelItem::Large: {
			// ！！在Large，如果不是处于新列的第一行，就需要进行换列处理
			// 把large一直设置在下一列的开始
			if (row != 0) {
				x += (columMaxWidth + spacing);
				++column;
			}
			//
			item->rowIndex            = 0;
			item->columnIndex         = column;
			item->itemWillSetGeometry = QRect(x, yBegin, hint.width(), largeHeight);
			columMaxWidth             = hint.width();
			// 换列，x自动递增到下个坐标，列数增加，行数归零，最大列宽归零
			x += (columMaxWidth + spacing);
			row           = 0;
			columMaxWidth = 0;
			++column;
		} break;

		case SARibbonPanelItem::Medium: {
			// 2行模式下Medium和small等价
			if (2 == rowCount) {
				if (0 == row) {
					item->rowIndex            = 0;
					item->columnIndex         = column;
					item->itemWillSetGeometry = QRect(x, yMediumRow0, hint.width(), smallHeight);
					thisColumnRP0             = SARibbonPanelItem::Medium;
					columMaxWidth             = hint.width();
					// 下个row为1
					row = 1;
					// x不变
				} else {
					item->rowIndex            = 1;
					item->columnIndex         = column;
					item->itemWillSetGeometry = QRect(x, yMediumRow1, hint.width(), smallHeight);
					// 和上个进行比较得到最长宽度
					columMaxWidth = qMax(columMaxWidth, hint.width());
					// 换列，x自动递增到下个坐标，列数增加，行数归零，最大列宽归零
					x += (columMaxWidth + spacing);
					row           = 0;
					columMaxWidth = 0;
					++column;
				}
			} else {
				// 3行模式
				if (0 == row) {
					item->rowIndex            = 0;
					item->columnIndex         = column;
					item->itemWillSetGeometry = QRect(x, yMediumRow0, hint.width(), smallHeight);
					thisColumnRP0             = SARibbonPanelItem::Medium;
					columMaxWidth             = hint.width();
					row                       = 1;
					// x不变
				} else if (1 == row) {
					item->rowIndex            = 1;
					item->columnIndex         = column;
					item->itemWillSetGeometry = QRect(x, yMediumRow1, hint.width(), smallHeight);
					columMaxWidth             = qMax(columMaxWidth, hint.width());
					// 换列，x自动递增到下个坐标，列数增加，行数归零，最大列宽归零
					x += (columMaxWidth + spacing);
					row           = 0;
					columMaxWidth = 0;
					++column;
				} else {
					// 这种模式一般情况会发生在当前列前两行是Small，添加了一个Medium
					// 这时需要先换列
					// 换列，x自动递增到下个坐标，列数增加，行数归零，最大列宽归零
					x += (columMaxWidth + spacing);
					++column;
					// 换列后此时等价于0 == row
					item->rowIndex            = 0;
					item->columnIndex         = column;
					item->itemWillSetGeometry = QRect(x, yMediumRow0, hint.width(), smallHeight);
					thisColumnRP0             = SARibbonPanelItem::Medium;
					columMaxWidth             = hint.width();
					row                       = 1;
				}
			}
		} break;

		case SARibbonPanelItem::Small: {
			if (0 == row) {
				// 第一行
				item->rowIndex            = 0;
				item->columnIndex         = column;
				item->itemWillSetGeometry = QRect(x, ySmallRow0, hint.width(), smallHeight);
				thisColumnRP0             = SARibbonPanelItem::Small;
				columMaxWidth             = hint.width();
				// 下个row为1
				row = 1;
				// x不变
			} else if (1 == row) {
				// 第二行
				item->rowIndex            = 1;
				item->columnIndex         = column;
				item->itemWillSetGeometry = QRect(x, ySmallRow1, hint.width(), smallHeight);
				if ((3 == rowCount) && (SARibbonPanelItem::Medium == thisColumnRP0)) {
					// 三行模式，并且第一行是Medium
					item->itemWillSetGeometry = QRect(x, yMediumRow1, hint.width(), smallHeight);
				}
				// 和上个进行比较得到最长宽度
				columMaxWidth = qMax(columMaxWidth, hint.width());
				// 这里要看两行还是三行，确定是否要换列
				if (2 == rowCount) {
					// 两行模式，换列
					// 换列，x自动递增到下个坐标，列数增加，行数归零，最大列宽归零
					x += (columMaxWidth + spacing);
					row           = 0;
					columMaxWidth = 0;
					++column;
				} else {
					// 三行模式，继续增加行数
					row = 2;
					// x不变
				}
				if ((3 == rowCount) && (SARibbonPanelItem::Medium == thisColumnRP0)) {
					// 三行模式，并且第一行是Medium，换列
					// 换列，x自动递增到下个坐标，列数增加，行数归零，最大列宽归零
					x += (columMaxWidth + spacing);
					row           = 0;
					columMaxWidth = 0;
					++column;
				}
			} else {
				// 第三行
				item->rowIndex            = 2;
				item->columnIndex         = column;
				item->itemWillSetGeometry = QRect(x, ySmallRow2, hint.width(), smallHeight);
				// 和上个进行比较得到最长宽度
				columMaxWidth = qMax(columMaxWidth, hint.width());
				// 换列，x自动递增到下个坐标，列数增加，行数归零，最大列宽归零
				x += (columMaxWidth + spacing);
				row           = 0;
				columMaxWidth = 0;
				++column;
			}
		} break;

		default:
			// 不可能出现
			break;
		}
		lastGeomItem = item;
	}
	// 最后一个元素，更新列数
	//  2022-06-20 此句本来在循环里面，如果最后一个元素隐藏，会导致无法到达此判断导致异常
	if (lastGeomItem) {  // 最后一个元素，更新totalWidth
		if (lastGeomItem->columnIndex != column) {
			// 说明最后一个元素处于最后位置，触发了换列，此时真实列数需要减1，直接等于column索引
			mColumnCount = column;
			// 由于最后一个元素触发了换列，x值是新一列的位置，直接作为totalWidth要减去已经加入的spacing
			totalWidth = x - spacing + mag.right();
		} else {
			// 说明最后一个元素处于非最后位置，没有触发下一个换列，此时真实列数等于column索引+1
			mColumnCount = column + 1;
			// 由于最后一个元素未触发换列，需要计算totalWidth
			totalWidth = x + columMaxWidth + mag.right();
		}
	}

	// 在设置完所有窗口后，再设置扩展属性的窗口
	if (totalWidth < setrect.width() && (setrect.width() - totalWidth) > 10) {
		// 说明可以设置扩展属性的窗口
		recalcExpandGeomArray(setrect);
	}
	// 布局label
	const int yTitleBegin      = qMax(height - mag.bottom() - titleH, 1);
	bool isTitleWidthThanPanel = false;
	if (isEnableShowPanelTitle() && mTitleLabel) {
		mTitleLabelGeometry.setRect(mag.left(), yTitleBegin, setrect.width() - mag.left() - mag.right(), titleH);
		// 这里要确认标题宽度是否大于totalWidth，如果大于，则要把标题的宽度作为totalwidth
		QFontMetrics fm = mTitleLabel->fontMetrics();
		int textWidth   = SA::compat::horizontalAdvance(fm, panel->panelName());
		textWidth += 4;
		if (totalWidth < textWidth) {
			totalWidth            = textWidth;
			isTitleWidthThanPanel = true;  // 说明标题的长度大于按钮布局的长度
		}
	}
	// 布局optionActionButton

	if (isHaveOptionAction()) {
		QSize optBtnSize = optionActionButtonSize();
		if (isEnableShowPanelTitle()) {
			// 有标题
			mOptionActionBtnGeometry.setRect(
				mTitleLabelGeometry.right() - mTitleLabelGeometry.height(),
				mTitleLabelGeometry.y(),
				mTitleLabelGeometry.height(),
				mTitleLabelGeometry.height()
			);

			// 特殊情况，如果panel的标题长度大于totalWidth，那么说明totalWidth比较短
			// 这时候，optionActionBtn的宽度要加上到标题宽度上
			if (isTitleWidthThanPanel) {
				// 由于文字是居中对齐，因此要扩展2个按钮的宽度
				totalWidth += (2 * titleH);
			}
		} else {
			// 无标题
			mOptionActionBtnGeometry.setRect(
				setrect.right() - optBtnSize.width() - mag.right(),
				setrect.bottom() - optBtnSize.height() - mag.bottom(),
				optBtnSize.width(),
				optBtnSize.height()
			);
			totalWidth += optBtnSize.width();
		}
	}

	/**
	 * \if ENGLISH
	 * @brief Handle RTL mirroring for all layout elements
	 * @details In RTL mode, all x coordinates are mirrored horizontally across the panel width.
	 *          Button columns are reversed, option button moves to the left, and title aligns right.
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 处理所有布局元素的RTL镜像
	 * @details 在RTL模式下，所有x坐标沿面板宽度水平镜像。按钮列反转，选项按钮移动到左侧，标题右对齐。
	 * \endif
	 */
	if (SA::saIsRTL()) {
		// Mirror all panel items' x coordinates
		for (SARibbonPanelItem* item : sa_as_const(mItems)) {
			if (!item->isEmpty()) {
				int mirroredX = SA::saMirrorX(
					item->itemWillSetGeometry.x(),
					setrect.width(),
					item->itemWillSetGeometry.width()
				);
				item->itemWillSetGeometry.moveLeft(mirroredX);
			}
		}

		// Mirror option button position
		if (isHaveOptionAction()) {
			int mirroredOptX = SA::saMirrorX(
				mOptionActionBtnGeometry.x(),
				setrect.width(),
				mOptionActionBtnGeometry.width()
			);
			mOptionActionBtnGeometry.moveLeft(mirroredOptX);
		}

		// Adjust title alignment for RTL
		if (mTitleLabel && isEnableShowPanelTitle()) {
			mTitleLabel->setAlignment(Qt::AlignRight | Qt::AlignVCenter);
		}
	} else {
		// Reset title alignment to center for LTR
		if (mTitleLabel && isEnableShowPanelTitle()) {
			mTitleLabel->setAlignment(Qt::AlignCenter | Qt::AlignVCenter);
		}
	}
	// 刷新sizeHint
	int heightHint  = SARibbonPanel::panelHeightHint(panel->fontMetrics(), panel->panelLayoutMode(), titleH);
	this->mSizeHint = QSize(totalWidth, heightHint);
#if SARibbonPanelLayout_DEBUG_PRINT
	qDebug() << "| |-SARibbonPanelLayout updateGeomArray(" << setrect << "),panel name = " << panel->panelName()
			 << "\n| | |-size hint =" << this->m_sizeHint  //
			 << "\n| | |-totalWidth=" << totalWidth        //
			 << "\n| | |-last x=" << x                     //
			 << "\n| | |-columMaxWidth=" << columMaxWidth  //
			 << "\n| | |-spacing=" << spacing              //
			 << "\n| | |-mag=" << mag                      //
			 << "\n| | |-largeHeight=" << largeHeight      //
			 << "\n| | |-smallHeight=" << smallHeight      //
		;
	qDebug().noquote() << debug_print__log__;
#endif
}

/**
 * \if ENGLISH
 * @brief Recalculates the expand geometry array
 * @param setrect The rectangle to recalculate
 * \endif
 *
 * \if CHINESE
 * @brief 重新计算扩展几何数组
 * @param setrect 要重新计算的矩形
 * \endif
 */
void SARibbonPanelLayout::recalcExpandGeomArray(const QRect& setrect)
{
	// 计算能扩展的尺寸
	int expandwidth = setrect.width() - this->mSizeHint.width();

	if (expandwidth <= 1) {
		// 没有必要设置
		return;
	}
	// 列扩展信息
	struct _columnExpandInfo
	{
		int oldColumnWidth      = 0;   ///< 原来的列宽
		int columnMaximumWidth  = -1;  ///< 列的最大宽度
		int columnExpandedWidth = 0;   ///< 扩展后列的宽度
		QList< SARibbonPanelItem* > expandItems;
	};
	// 此变量用于记录可以水平扩展的列和控件，在布局结束后，如果还有空间，就把水平扩展的控件进行扩展
	QMap< int, _columnExpandInfo > columnExpandInfo;

	for (SARibbonPanelItem* item : sa_as_const(mItems)) {
		if ((!item->isEmpty()) && item->expandingDirections() & Qt::Horizontal) {
			// 只获取可见的
			QMap< int, _columnExpandInfo >::iterator i = columnExpandInfo.find(item->columnIndex);
			if (i == columnExpandInfo.end()) {
				i = columnExpandInfo.insert(item->columnIndex, _columnExpandInfo());
			}
			i.value().expandItems.append(item);
		}
	}
	if (columnExpandInfo.size() <= 0) {
		// 没有需要扩展的就退出
		return;
	}
	// 获取完可扩展的列和控件后，计算对应的列的尺寸
	// 计算能扩展的尺寸
	int oneColCanexpandWidth = expandwidth / columnExpandInfo.size();

	for (QMap< int, _columnExpandInfo >::iterator i = columnExpandInfo.begin(); i != columnExpandInfo.end();) {
		int& oldColumnWidth     = i.value().oldColumnWidth;
		int& columnMaximumWidth = i.value().columnMaximumWidth;
		this->columnWidthInfo(i.key(), oldColumnWidth, columnMaximumWidth);
		if ((oldColumnWidth <= 0) || (oldColumnWidth > columnMaximumWidth)) {
			// 如果小于0说明没有这个列，这种属于异常，删除继续
			//  oldColumnWidth > columnMaximumWidth也是异常
			i = columnExpandInfo.erase(i);
			continue;
		}
		// 开始调整
		int colwidth = oneColCanexpandWidth + oldColumnWidth;  // 先扩展了
		if (colwidth >= columnMaximumWidth) {
			// 过最大宽度要求
			i.value().columnExpandedWidth = columnMaximumWidth;
		} else {
			i.value().columnExpandedWidth = colwidth;
		}
		++i;
	}
	// 从新调整尺寸
	// 由于会涉及其他列的变更，因此需要所有都遍历一下
	for (auto i = columnExpandInfo.begin(); i != columnExpandInfo.end(); ++i) {
		int moveXLen = i.value().columnExpandedWidth - i.value().oldColumnWidth;
		for (SARibbonPanelItem* item : sa_as_const(mItems)) {
			if (item->isEmpty() || (item->columnIndex < i.key())) {
				// 之前的列不用管
				continue;
			}
			if (item->columnIndex == i.key()) {
				// 此列的扩展
				if (i.value().expandItems.contains(item)) {
					// 此列需要扩展的item才扩展尺寸
					item->itemWillSetGeometry.setWidth(i.value().columnExpandedWidth);
				} else {
					// 此列不扩展的模块保持原来的尺寸
					continue;
				}
			} else {
				// 后面的移动
				item->itemWillSetGeometry.moveLeft(item->itemWillSetGeometry.x() + moveXLen);
			}
		}
	}
#if SARibbonPanelLayout_DEBUG_PRINT
	qDebug() << "| |-SARibbonPanelLayout recalcExpandGeomArray(" << setrect
			 << ") panelName=" << ribbonPanel()->panelName()  //
			 << ",expandwidth=" << expandwidth                //
		;
#endif
}

/**
 * \if ENGLISH
 * @brief Calculates the width and maximum width of the window based on the column index
 * @param colindex Column index
 * @param width Returns -1 if the column does not exist
 * @param maximum Returns -1 if the column does not exist
 * \endif
 *
 * \if CHINESE
 * @brief 根据列数，计算窗口的宽度，以及最大宽度
 * @param colindex 列索引
 * @param width 如果传入没有这个列，返回-1
 * @param maximum 如果传入没有这个列，返回-1
 * \endif
 */
void SARibbonPanelLayout::columnWidthInfo(int colindex, int& width, int& maximum) const
{
	width   = -1;
	maximum = -1;
	for (SARibbonPanelItem* item : mItems) {
		if (!item->isEmpty() && (item->columnIndex == colindex)) {
			width   = qMax(width, item->itemWillSetGeometry.width());
			maximum = qMax(maximum, item->widget()->maximumWidth());
		}
	}
}

/**
 * \if ENGLISH
 * @brief Gets the panel's title label
 * @return A pointer to the SARibbonPanelLabel
 * \endif
 *
 * \if CHINESE
 * @brief 获取面板的标题标签
 * @return 指向SARibbonPanelLabel的指针
 * \endif
 */
SARibbonPanelLabel* SARibbonPanelLayout::panelTitleLabel() const
{
	return mTitleLabel;
}

/**
 * \if ENGLISH
 * @brief Sets the default icon size for tool buttons
 * @param smallSize Small icon size
 * @param largeSize Large icon size
 * @sa toolButtonIconSize
 * \endif
 *
 * \if CHINESE
 * @brief 设置工具按钮的默认图标尺寸
 * @param smallSize 小图标尺寸
 * @param largeSize 大图标尺寸
 * @sa toolButtonIconSize
 * \endif
 */
void SARibbonPanelLayout::setToolButtonIconSize(const QSize& smallSize, const QSize& largeSize)
{
	mSmallToolButtonIconSize = smallSize;
	mLargeToolButtonIconSize = largeSize;
}

/**
 * \if ENGLISH
 * @brief Gets the default icon size for tool buttons
 * @return The current icon size
 * @sa setToolButtonIconSize
 * \endif
 *
 * \if CHINESE
 * @brief 获取工具按钮的默认图标尺寸
 * @return 当前的图标尺寸
 * @sa setToolButtonIconSize
 * \endif
 */
QPair< QSize, QSize > SARibbonPanelLayout::toolButtonIconSize() const
{
	return qMakePair(mSmallToolButtonIconSize, mLargeToolButtonIconSize);
}

/**
 * \if ENGLISH
 * @brief Sets the large button icon size
 * @param largeSize Large icon size
 * \endif
 *
 * \if CHINESE
 * @brief 设置大按钮图标尺寸
 * @param largeSize 大图标尺寸
 * \endif
 */
void SARibbonPanelLayout::setLargeIconSize(const QSize& largeSize)
{
	mLargeToolButtonIconSize = largeSize;
}

/**
 * \if ENGLISH
 * @brief Gets the large button icon size
 * @return Large icon size
 * \endif
 *
 * \if CHINESE
 * @brief 大按钮图标尺寸
 * @return 大图标尺寸
 * \endif
 */
QSize SARibbonPanelLayout::largeIconSize() const
{
	return mLargeToolButtonIconSize;
}

/**
 * \if ENGLISH
 * @brief Sets the small button icon size
 * @param largeSize Small icon size
 * \endif
 *
 * \if CHINESE
 * @brief 设置小按钮图标尺寸
 * @param largeSize 小图标尺寸
 * \endif
 */
void SARibbonPanelLayout::setSmallIconSize(const QSize& largeSize)
{
	mSmallToolButtonIconSize = largeSize;
}

/**
 * \if ENGLISH
 * @brief Gets the small button icon size
 * @return Small icon size
 * \endif
 *
 * \if CHINESE
 * @brief 小按钮图标尺寸
 * @return 小图标尺寸
 * \endif
 */
QSize SARibbonPanelLayout::smallIconSize() const
{
	return mSmallToolButtonIconSize;
}

/**
 * \if ENGLISH
 * @brief Gets the size of the option action button
 * @return The button's size
 * \endif
 *
 * \if CHINESE
 * @brief 获取选项action按钮的尺寸
 * @return 按钮的尺寸
 * \endif
 */
QSize SARibbonPanelLayout::optionActionButtonSize() const
{
	return (isEnableShowPanelTitle() ? QSize(12, 12) : QSize(mTitleHeight, mTitleHeight));
}

/**
 * \if ENGLISH
 * @brief Sets the panel's title label
 * @param newTitleLabel The new title label widget
 * \endif
 *
 * \if CHINESE
 * @brief 设置面板的标题标签
 * @param newTitleLabel 新的标题标签窗口部件
 * \endif
 */
void SARibbonPanelLayout::setPanelTitleLabel(SARibbonPanelLabel* newTitleLabel)
{
	mTitleLabel = newTitleLabel;
	// 确保m_optionActionBtn在label之上
	if (mOptionActionBtn) {
		if (mTitleLabel) {
			mTitleLabel->stackUnder(mOptionActionBtn);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Checks if word wrap is enabled
 * @return true if word wrap is enabled, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 是否允许文字换行
 * @return 如果允许文字换行则返回true，否则返回false
 * \endif
 */
bool SARibbonPanelLayout::isEnableWordWrap() const
{
	return mEnableWordWrap;
}

/**
 * \if ENGLISH
 * @brief Sets whether word wrap is enabled
 * @param on If true, word wrap is enabled
 * \endif
 *
 * \if CHINESE
 * @brief 设置文字允许换行
 * @param on 如果为true，则允许文字换行
 * \endif
 */
void SARibbonPanelLayout::setEnableWordWrap(bool on)
{
	mEnableWordWrap = on;
	// 遍历所有SARibbonToolButton
	for (SARibbonPanelItem* item : sa_as_const(mItems)) {
		if (!item) {
			continue;
		}
		if (SARibbonToolButton* toolbtn = qobject_cast< SARibbonToolButton* >(item->widget())) {
			toolbtn->setEnableWordWrap(on);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Sets the maximum button aspect ratio, which determines the maximum width of the button
 *
 * The maximum width of the button is button height * this coefficient. For example, if the button height is h, then the maximum button width is maxw = h * buttonMaximumAspectRatio
 * If the text cannot be fully displayed at this width, the button will not continue to expand horizontally, and ... will be used to replace the text that is not fully displayed
 *
 * @param fac Aspect ratio factor
 * @see buttonMaximumAspectRatio
 *
 * @note Users should not call @ref SARibbonPanelLayout::setButtonMaximumAspectRatio to set it,
 * but instead call @ref SARibbonBar::setButtonMaximumAspectRatio to set the aspect ratio
 * \endif
 *
 * \if CHINESE
 * @brief 设置按钮最大宽高比，这个系数决定按钮的最大宽度
 *
 * 按钮的最大宽度为按钮高度*此系数，例如按钮高度为h，那么按钮最大宽度maxw=h*buttonMaximumAspectRatio
 * 如果在此宽度下文字还无法完全显示，那么按钮将不会继续横向扩展，将使用...替代未完全显示的文字
 *
 * @param fac 宽高比系数
 * @see buttonMaximumAspectRatio
 *
 * @note 用户不应该调用@ref SARibbonPanelLayout::setButtonMaximumAspectRatio 来设置，
 * 而是调用@ref SARibbonBar::setButtonMaximumAspectRatio 设置宽高比
 * \endif
 */
void SARibbonPanelLayout::setButtonMaximumAspectRatio(qreal fac)
{
	mButtonMaximumAspectRatio = fac;
	// 遍历所有SARibbonToolButton
	for (SARibbonPanelItem* item : sa_as_const(mItems)) {
		if (!item) {
			continue;
		}
		if (SARibbonToolButton* toolbtn = qobject_cast< SARibbonToolButton* >(item->widget())) {
			toolbtn->setButtonMaximumAspectRatio(fac);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Gets the maximum button aspect ratio, which determines the maximum width of the button
 * @return The maximum button aspect ratio
 * @see setButtonMaximumAspectRatio
 * \endif
 *
 * \if CHINESE
 * @brief 按钮最大宽高比，这个系数决定按钮的最大宽度
 * @return 按钮最大宽高比
 * @see setButtonMaximumAspectRatio
 * \endif
 */
qreal SARibbonPanelLayout::buttonMaximumAspectRatio() const
{
	return mButtonMaximumAspectRatio;
}

/**
 * \if ENGLISH
 * @brief Gets the spacing between the title and the buttons
 * @return The current spacing value
 * @sa setPanelTitleSpace
 * \endif
 *
 * \if CHINESE
 * @brief 获取标题与按钮之间的间距
 * @return 当前的间距值
 * @sa setPanelTitleSpace
 * \endif
 */
int SARibbonPanelLayout::panelTitleSpace() const
{
	return mTitleSpace;
}

/**
 * \if ENGLISH
 * @brief Sets the spacing between the title and the buttons
 * @param newTitleSpace The new spacing value
 * @sa panelTitleSpace
 * \endif
 *
 * \if CHINESE
 * @brief 设置标题与按钮之间的间距
 * @param newTitleSpace 新的间距值
 * @sa panelTitleSpace
 * \endif
 */
void SARibbonPanelLayout::setPanelTitleSpace(int newTitleSpace)
{
	if (mTitleSpace == newTitleSpace) {
		return;
	}
	mTitleSpace = newTitleSpace;
	invalidate();
}

/**
 * \if ENGLISH
 * @brief Gets the height of the panel's title
 * @return The current title height
 * @sa setPanelTitleHeight
 * \endif
 *
 * \if CHINESE
 * @brief 获取面板标题的高度
 * @return 当前的标题高度
 * @sa setPanelTitleHeight
 * \endif
 */
int SARibbonPanelLayout::panelTitleHeight() const
{
	return mTitleHeight;
}

/**
 * \if ENGLISH
 * @brief Sets the height of the panel's title
 * @param newTitleHeight The new title height
 * @sa panelTitleHeight
 * \endif
 *
 * \if CHINESE
 * @brief 设置面板标题的高度
 * @param newTitleHeight 新的标题高度
 * @sa panelTitleHeight
 * \endif
 */
void SARibbonPanelLayout::setPanelTitleHeight(int newTitleHeight)
{
	if (mTitleHeight == newTitleHeight) {
		return;
	}
	mTitleHeight = newTitleHeight;
	invalidate();
}

/**
 * @brief Checks if the panel's title is enabled for display / 检查面板标题是否启用显示
 * @return true if the title is enabled; otherwise false / 如果标题启用则返回true；否则返回false
 * @sa setEnableShowPanelTitle
 */
bool SARibbonPanelLayout::isEnableShowPanelTitle() const
{
	return mEnableShowTitle;
}

/**
 * @brief Sets whether the panel's title is enabled for display / 设置面板标题是否启用显示
 * @param on If true, the title is enabled / 如果为true，则启用标题
 * @sa isEnableShowPanelTitle
 */
void SARibbonPanelLayout::setEnableShowPanelTitle(bool on)
{
	if (mEnableShowTitle == on) {
		return;
	}
	mEnableShowTitle = on;
	invalidate();
}

/**
 * @brief Gets the height of large buttons / 获取大按钮的高度
 * @return The large button height / 大按钮高度
 */
int SARibbonPanelLayout::largeButtonHeight() const
{
	return mLargeHeight;
}

void SARibbonPanelLayout::setGeometry(const QRect& rect)
{
	QRect old = geometry();
	if (old == rect) {
		return;
	}
	if (rect.width() <= 0 || rect.height() <= 0) {
		return;
	}
#if SARibbonPanelLayout_DEBUG_PRINT
	qDebug() << "| |----->SARibbonPanelLayout.setGeometry(" << rect << "(" << ribbonPanel()->panelName() << ")=======";
#endif
	QLayout::setGeometry(rect);
	mDirty = false;
	updateGeomArray(rect);
	doLayout();
}

/*** End of inlined file: SARibbonPanelLayout.cpp ***/


/*** Start of inlined file: SARibbonPanel.cpp ***/
#include <QAction>
#include <QApplication>
#include <QDebug>
#include <QFontMetrics>
#include <QGridLayout>
#include <QIcon>
#include <QMenu>
#include <QPainter>
#include <QResizeEvent>
#include <QWidgetAction>

#ifndef SARIBBONPANEL_DEBUG_PRINT
#define SARIBBONPANEL_DEBUG_PRINT 0
#endif

#if SARIBBONPANEL_DEBUG_PRINT
#ifndef SARIBBONPANELLABEL_HELP_DRAW_RECT
#define SARIBBONPANELLABEL_HELP_DRAW_RECT(p, rect)                                                                     \
	do {                                                                                                               \
		p.save();                                                                                                      \
		QPen _pen(Qt::DashLine);                                                                                       \
		_pen.setColor(Qt::blue);                                                                                       \
		p.setPen(_pen);                                                                                                \
		p.setBrush(QBrush());                                                                                          \
		p.drawRect(rect);                                                                                              \
		p.restore();                                                                                                   \
	} while (0)
#endif  // SARIBBONPANELLABEL_HELP_DRAW_RECT
#endif
//===============================================================
// SARibbonPanelLabel
//===============================================================

SARibbonPanelLabel::SARibbonPanelLabel(QWidget* parent) : QLabel(parent)
{
}

//===============================================================
// SARibbonPanel::PrivateData
//===============================================================

class SARibbonPanel::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonPanel)
public:
	PrivateData(SARibbonPanel* p);
	void createLayout();
	// 获取layout
	SARibbonPanelLayout* panelLayout() const;
	// 返回最后一个添加的action对应的button，前提是最后一个是toolbutton，否则返回nullptr
	SARibbonToolButton* lastAddActionButton();
	// 重置labe的字体，这个主要是为了让panel的标题字体能适应标题高度
	void resetTitleLabelFont();
	// 标题
	QString panelName() const;
	void setPanelName(const QString& title);

public:
	bool m_isCanCustomize { true };                                                    ///< 记录是否可自定义
	SARibbonPanel::PanelLayoutMode m_panelLayoutMode { SARibbonPanel::ThreeRowMode };  ///< panel 的布局模式，默认为 3 行模式 ThreeRowMode
	SARibbonPanelOptionButton* m_optionActionButton { nullptr };                       ///< 标题栏的 y 距离
	SARibbonPanelLabel* m_label { nullptr };
	bool enableIconRightText { false };                                                ///< 是否启用图标右侧文字模式
};

SARibbonPanel::PrivateData::PrivateData(SARibbonPanel* p) : q_ptr(p)
{
	createLayout();
}

void SARibbonPanel::PrivateData::createLayout()
{
	m_label = RibbonSubElementFactory->createRibbonPanelLabel(q_ptr);
	m_label->setAlignment(Qt::AlignCenter);
	SARibbonPanelLayout* lay = new SARibbonPanelLayout(q_ptr);
	lay->setPanelTitleLabel(m_label);
	lay->setSpacing(2);
	lay->setContentsMargins(2, 2, 2, 2);
}

SARibbonPanelLayout* SARibbonPanel::PrivateData::panelLayout() const
{
	return qobject_cast< SARibbonPanelLayout* >(q_ptr->layout());
}

SARibbonToolButton* SARibbonPanel::PrivateData::lastAddActionButton()
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		return (qobject_cast< SARibbonToolButton* >(lay->lastWidget()));
	}
	return (nullptr);
}

/**
 * @brief 重置label的字体
 *
 * @note 此函数必须在布局设置后调用
 */
void SARibbonPanel::PrivateData::resetTitleLabelFont()
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		int h = lay->panelTitleHeight();
		if (h > 1) {
			QFont f = q_ptr->font();
			f.setPixelSize(h * 0.8);
			if (m_label) {
				m_label->setFont(f);
			}
		}
	}
}

QString SARibbonPanel::PrivateData::panelName() const
{
	if (m_label) {
		return m_label->text();
	}
	return (QString());
}

void SARibbonPanel::PrivateData::setPanelName(const QString& title)
{
	if (m_label) {
		m_label->setText(title);
	}
}

//==================================================
// SARibbonPanel
//==================================================

/**
 * \if ENGLISH
 * @brief Constructs a SARibbonPanel with the given parent
 * @param parent The parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 使用给定的父对象构造SARibbonPanel
 * @param parent 父窗口部件
 * \endif
 */
/**
 * \if ENGLISH
 * @brief Constructor for SARibbonPanel
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonPanel构造函数
 * @param parent 父窗口部件
 * \endif
 */
SARibbonPanel::SARibbonPanel(QWidget* parent) : QFrame(parent), d_ptr(new SARibbonPanel::PrivateData(this))
{
	setPanelLayoutMode(ThreeRowMode);
}

/**
 * \if ENGLISH
 * @brief Constructs a SARibbonPanel with a name and the given parent
 * @param name The panel's display name
 * @param parent The parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 使用名称和给定的父对象构造SARibbonPanel
 * @param name 面板的显示名称
 * @param parent 父窗口部件
 * \endif
 */
SARibbonPanel::SARibbonPanel(const QString& name, QWidget* parent)
	: QFrame(parent), d_ptr(new SARibbonPanel::PrivateData(this))
{
	setPanelLayoutMode(ThreeRowMode);
	setPanelName(name);
}

/**
 * \if ENGLISH
 * @brief Destructor
 * \endif
 *
 * \if CHINESE
 * @brief 析构函数
 * \endif
 */
SARibbonPanel::~SARibbonPanel()
{
}

/**
 * \if ENGLISH
 * @brief Sets the row proportion property for an action
 *
 * This property determines how much vertical space the corresponding button will occupy within the panel.
 * It should be set before adding the action to the panel.
 *
 * @param action The action to modify
 * @param rp The row proportion (Large, Medium, Small)
 * @sa addAction, getActionRowProportionProperty
 * \endif
 *
 * \if CHINESE
 * @brief 为一个action设置行占比属性
 *
 * 此属性决定了对应按钮在面板内占据的垂直空间比例。
 * 应在将action添加到面板之前设置。
 *
 * @param action 要修改的action
 * @param rp 行占比（大、中、小）
 * @sa addAction, getActionRowProportionProperty
 * \endif
 */
void SARibbonPanel::setActionRowProportionProperty(QAction* action, SARibbonPanelItem::RowProportion rp)
{
	Q_CHECK_PTR(action);
	action->setProperty(SA_ActionPropertyName_RowProportion, static_cast< int >(rp));
}

/**
 * \if ENGLISH
 * @brief Gets the row proportion property from an action
 * @param action The action to query
 * @return The row proportion, defaults to Large if not set
 * @sa setActionRowProportionProperty
 * \endif
 *
 * \if CHINESE
 * @brief 从一个action获取行占比属性
 * @param action 要查询的action
 * @return 行占比，如果未设置则默认为Large
 * @sa setActionRowProportionProperty
 * \endif
 */
SARibbonPanelItem::RowProportion SARibbonPanel::getActionRowProportionProperty(QAction* action)
{
	bool isok = false;
	int r     = action->property(SA_ActionPropertyName_RowProportion).toInt(&isok);

	if (isok) {
		return (static_cast< SARibbonPanelItem::RowProportion >(r));
	}
	return (SARibbonPanelItem::Large);
}

/**
 * \if ENGLISH
 * @brief Sets the ToolButtonPopupMode property for an action
 * @param action The action to modify
 * @param popMode The popup mode (e.g., InstantPopup, MenuButtonPopup)
 * @sa getActionToolButtonPopupModeProperty
 * \endif
 *
 * \if CHINESE
 * @brief 为一个action设置ToolButtonPopupMode属性
 * @param action 要修改的action
 * @param popMode 弹出模式（例如，InstantPopup, MenuButtonPopup）
 * @sa getActionToolButtonPopupModeProperty
 * \endif
 */
void SARibbonPanel::setActionToolButtonPopupModeProperty(QAction* action, QToolButton::ToolButtonPopupMode popMode)
{
	Q_CHECK_PTR(action);
	action->setProperty(SA_ActionPropertyName_ToolButtonPopupMode, static_cast< int >(popMode));
}

/**
 * \if ENGLISH
 * @brief Gets the ToolButtonPopupMode property from an action
 * @param action The action to query
 * @return The popup mode, defaults to InstantPopup if not set
 * @sa setActionToolButtonPopupModeProperty
 * \endif
 *
 * \if CHINESE
 * @brief 从一个action获取ToolButtonPopupMode属性
 * @param action 要查询的action
 * @return 弹出模式，如果未设置则默认为InstantPopup
 * @sa setActionToolButtonPopupModeProperty
 * \endif
 */
QToolButton::ToolButtonPopupMode SARibbonPanel::getActionToolButtonPopupModeProperty(QAction* action)
{
	bool isok = false;
	int r     = action->property(SA_ActionPropertyName_ToolButtonPopupMode).toInt(&isok);

	if (isok) {
		return (static_cast< QToolButton::ToolButtonPopupMode >(r));
	}
	// 使用 QToolButton 的默认值 DelayedPopup，保持一致性
	return (QToolButton::DelayedPopup);
}

/**
 * \if ENGLISH
 * @brief Sets the ToolButtonStyle property for an action
 * @param action The action to modify
 * @param buttonStyle The button style (e.g., ToolButtonIconOnly, ToolButtonTextBesideIcon)
 * @sa getActionToolButtonStyleProperty
 * \endif
 *
 * \if CHINESE
 * @brief 为一个action设置ToolButtonStyle属性
 * @param action 要修改的action
 * @param buttonStyle 按钮样式（例如，ToolButtonIconOnly, ToolButtonTextBesideIcon）
 * @sa getActionToolButtonStyleProperty
 * \endif
 */
void SARibbonPanel::setActionToolButtonStyleProperty(QAction* action, Qt::ToolButtonStyle buttonStyle)
{
	Q_CHECK_PTR(action);
	action->setProperty(SA_ActionPropertyName_ToolButtonStyle, static_cast< int >(buttonStyle));
}

/**
 * \if ENGLISH
 * @brief Gets the ToolButtonStyle property from an action
 * @param action The action to query
 * @return The button style, defaults to ToolButtonIconOnly if not set
 * @sa setActionToolButtonStyleProperty
 * \endif
 *
 * \if CHINESE
 * @brief 从一个action获取ToolButtonStyle属性
 * @param action 要查询的action
 * @return 按钮样式，如果未设置则默认为ToolButtonIconOnly
 * @sa setActionToolButtonStyleProperty
 * \endif
 */
Qt::ToolButtonStyle SARibbonPanel::getActionToolButtonStyleProperty(QAction* action)
{
	bool isok = false;
	int r     = action->property(SA_ActionPropertyName_ToolButtonStyle).toInt(&isok);

	if (isok) {
		return (static_cast< Qt::ToolButtonStyle >(r));
	}
	return (Qt::ToolButtonIconOnly);
}

/**
 * \if ENGLISH
 * @brief Adds an action to the panel
 *
 * This is the primary method for populating the panel. The button created for this action will use the
 * row proportion and popup mode previously set via the static property functions.
 *
 * @param action The action to add
 * @param rp The row proportion for this action
 * @sa addLargeAction, addMediumAction, addSmallAction
 * \endif
 *
 * \if CHINESE
 * @brief 向面板添加一个action
 *
 * 这是填充面板的主要方法。为此action创建的按钮将使用之前通过静态属性函数设置的行占比和弹出模式。
 *
 * @param action 要添加的action
 * @param rp 此action的行占比
 * @sa addLargeAction, addMediumAction, addSmallAction
 * \endif
 */
void SARibbonPanel::addAction(QAction* action, SARibbonPanelItem::RowProportion rowProportion)
{
	Q_CHECK_PTR(action);
	setActionRowProportionProperty(action, rowProportion);
	addAction(action);
}

/**
 * \if ENGLISH
 * @brief Adds an action with a specified popup mode
 * @param act The action to add
 * @param popMode The popup mode for the button
 * @param rp The row proportion for this action
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个具有指定弹出模式的action
 * @param act 要添加的action
 * @param popMode 按钮的弹出模式
 * @param rp 此action的行占比
 * \endif
 */
void SARibbonPanel::addAction(QAction* act,
							  QToolButton::ToolButtonPopupMode popMode,
							  SARibbonPanelItem::RowProportion rowProportion)
{
	Q_CHECK_PTR(act);
	setActionRowProportionProperty(act, rowProportion);
	setActionToolButtonPopupModeProperty(act, popMode);
	addAction(act);
}

/**
 * \if ENGLISH
 * @brief Adds a large action (occupies full height)
 * @param action The action to add
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个大action（占据整个高度）
 * @param action 要添加的action
 * \endif
 */
void SARibbonPanel::addLargeAction(QAction* action)
{
	addAction(action, SARibbonPanelItem::Large);
}

/**
 * \if ENGLISH
 * @brief Adds a medium action
 *
 * In ThreeRowMode, a medium action occupies two rows. In TwoRowMode, it is equivalent to a small action.
 *
 * @param action The action to add
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个中等action
 *
 * 在三行模式下，中等action占据两行。在两行模式下，它等同于一个小action。
 *
 * @param action 要添加的action
 * \endif
 */
void SARibbonPanel::addMediumAction(QAction* action)
{
	addAction(action, SARibbonPanelItem::Medium);
}

/**
 * \if ENGLISH
 * @brief Adds a small action
 * @param action The action to add
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个小action
 * @param action 要添加的action
 * \endif
 */
void SARibbonPanel::addSmallAction(QAction* action)
{
	addAction(action, SARibbonPanelItem::Small);
}

/**
 * \if ENGLISH
 * @brief Adds a small action with a specified popup mode
 * @param action The action to add
 * @param popMode The popup mode for the button
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个具有指定弹出模式的小action
 * @param action 要添加的action
 * @param popMode 按钮的弹出模式
 * \endif
 */
void SARibbonPanel::addSmallAction(QAction* action, QToolButton::ToolButtonPopupMode popMode)
{
	addAction(action, popMode, SARibbonPanelItem::Small);
}

/**
 * \if ENGLISH
 * @brief Adds a large action with a specified popup mode
 * @param action The action to add
 * @param popMode The popup mode for the button
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个具有指定弹出模式的大action
 * @param action 要添加的action
 * @param popMode 按钮的弹出模式
 * \endif
 */
void SARibbonPanel::addLargeAction(QAction* action, QToolButton::ToolButtonPopupMode popMode)
{
	addAction(action, popMode, SARibbonPanelItem::Large);
}

/**
 * \if ENGLISH
 * @brief Adds a medium action with a specified popup mode
 * @param action The action to add
 * @param popMode The popup mode for the button
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个具有指定弹出模式的中等action
 * @param action 要添加的action
 * @param popMode 按钮的弹出模式
 * \endif
 */
void SARibbonPanel::addMediumAction(QAction* action, QToolButton::ToolButtonPopupMode popMode)
{
	addAction(action, popMode, SARibbonPanelItem::Medium);
}

/**
 * \if ENGLISH
 * @brief Creates and adds a new action
 *
 * This is a convenience function that creates a new `QAction`, sets its properties, and adds it to the panel.
 * The panel will take ownership of the action.
 *
 * @param text The action's text
 * @param icon The action's icon
 * @param popMode The button's popup mode
 * @param rp The action's row proportion
 * @return The newly created action
 * \endif
 *
 * \if CHINESE
 * @brief 创建并添加一个新的action
 *
 * 这是一个便捷函数，用于创建一个新的 `QAction`，设置其属性，并将其添加到面板。
 * 面板将拥有此action的所有权。
 *
 * @param text action的文字
 * @param icon action的图标
 * @param popMode 按钮的弹出模式
 * @param rp action的行占比
 * @return 新创建的action
 * \endif
 */
QAction* SARibbonPanel::addAction(const QString& text,
								  const QIcon& icon,
								  QToolButton::ToolButtonPopupMode popMode,
								  SARibbonPanelItem::RowProportion rowProportion)
{
	QAction* action = new QAction(icon, text, this);
	addAction(action, popMode, rowProportion);
	return (action);
}

/**
 * \if ENGLISH
 * @brief Adds a menu to the panel
 * @param menu The menu to add
 * @param rp The row proportion for the menu button
 * @param popMode The popup mode, defaults to InstantPopup
 * @note This function will modify some properties of menu->menuAction() (icon/text/objectName). If you need to preserve
 * custom values, please set them in advance.
 * \endif
 *
 * \if CHINESE
 * @brief 向面板添加一个菜单
 * @param menu 要添加的菜单
 * @param rp 菜单按钮的行占比
 * @param popMode 弹出模式，默认为InstantPopup
 * @note 本函数会修改 menu->menuAction() 的部分属性（icon/text/objectName），如需保留自定义值，请提前设置。
 * \endif
 */
void SARibbonPanel::addMenu(QMenu* menu, SARibbonPanelItem::RowProportion rowProportion, QToolButton::ToolButtonPopupMode popMode)
{
	Q_CHECK_PTR(menu);
	QAction* action = menu->menuAction();
	// 仅当未设置时才填充默认值，避免覆盖用户自定义
	if (action->icon().isNull()) {
		action->setIcon(menu->icon());
	}
	if (action->text().isEmpty()) {
		action->setText(menu->title());
	}
	if (action->objectName().isEmpty()) {
		action->setObjectName("action." + menu->objectName());
	}
	addAction(action, popMode, rowProportion);
}

/**
 * \if ENGLISH
 * @brief Adds a menu as a large button
 * @param menu The menu to add
 * @param popMode The popup mode
 * \endif
 *
 * \if CHINESE
 * @brief 以大按钮形式添加一个菜单
 * @param menu 要添加的菜单
 * @param popMode 弹出模式
 * \endif
 */
void SARibbonPanel::addLargeMenu(QMenu* menu, QToolButton::ToolButtonPopupMode popMode)
{
	addMenu(menu, SARibbonPanelItem::Large, popMode);
}

/**
 * \if ENGLISH
 * @brief Adds a menu as a medium button
 * @param menu The menu to add
 * @param popMode The popup mode
 * \endif
 *
 * \if CHINESE
 * @brief 以中按钮形式添加一个菜单
 * @param menu 要添加的菜单
 * @param popMode 弹出模式
 * \endif
 */
void SARibbonPanel::addMediumMenu(QMenu* menu, QToolButton::ToolButtonPopupMode popMode)
{
	addMenu(menu, SARibbonPanelItem::Medium, popMode);
}

/**
 * \if ENGLISH
 * @brief Adds a menu as a small button
 * @param menu The menu to add
 * @param popMode The popup mode
 * \endif
 *
 * \if CHINESE
 * @brief 以小按钮形式添加一个菜单
 * @param menu 要添加的菜单
 * @param popMode 弹出模式
 * \endif
 */
void SARibbonPanel::addSmallMenu(QMenu* menu, QToolButton::ToolButtonPopupMode popMode)
{
	addMenu(menu, SARibbonPanelItem::Small, popMode);
}

/**
 * \if ENGLISH
 * @brief Adds a custom widget to the panel
 *
 * The panel will create a `QWidgetAction` to manage the widget. The panel does not take ownership of the widget;
 * its lifetime is managed by its parent or the caller.
 *
 * @param w The widget to add
 * @param rp The row proportion for the widget
 * @return The created QWidgetAction
 * \endif
 *
 * \if CHINESE
 * @brief 向面板添加一个自定义窗口部件
 *
 * 面板将创建一个 `QWidgetAction` 来管理此窗口部件。面板不拥有此窗口部件的所有权；
 * 其生命周期由其父对象或调用者管理。
 *
 * @param w 要添加的窗口部件
 * @param rp 窗口部件的行占比
 * @return 创建的QWidgetAction
 * \endif
 */
QAction* SARibbonPanel::addWidget(QWidget* w, SARibbonPanelItem::RowProportion rowProportion)
{
	QWidgetAction* action = new QWidgetAction(this);

	action->setDefaultWidget(w);

	action->setEnabled(w->isEnabled());
	// 建立 Action -> Widget 的单向同步
	connect(action, &QWidgetAction::changed, this, [ w, action ]() {
		if (w) {
			w->setEnabled(action->isEnabled());
		}
	});
	action->setIcon(w->windowIcon());
	action->setText(w->windowTitle());
	action->setObjectName("action." + w->objectName());
	w->setAttribute(Qt::WA_Hover);
	setActionRowProportionProperty(action, rowProportion);
	addAction(action);
	return (action);
}

/**
 * \if ENGLISH
 * @brief Adds a widget as a small button
 * @param w The widget to add
 * @return The created QWidgetAction
 * \endif
 *
 * \if CHINESE
 * @brief 以小按钮形式添加一个窗口部件
 * @param w 要添加的窗口部件
 * @return 创建的QWidgetAction
 * \endif
 */
QAction* SARibbonPanel::addSmallWidget(QWidget* w)
{
	return (addWidget(w, SARibbonPanelItem::Small));
}

/**
 * \if ENGLISH
 * @brief Adds a widget as a medium button
 * @param w The widget to add
 * @return The created QWidgetAction
 * \endif
 *
 * \if CHINESE
 * @brief 以中等按钮形式添加一个窗口部件
 * @param w 要添加的窗口部件
 * @return 创建的QWidgetAction
 * \endif
 */
QAction* SARibbonPanel::addMediumWidget(QWidget* w)
{
	return (addWidget(w, SARibbonPanelItem::Medium));
}

/**
 * \if ENGLISH
 * @brief Adds a widget as a large button
 * @param w The widget to add
 * @return The created QWidgetAction
 * \endif
 *
 * \if CHINESE
 * @brief 以大按钮形式添加一个窗口部件
 * @param w 要添加的窗口部件
 * @return 创建的QWidgetAction
 * \endif
 */
QAction* SARibbonPanel::addLargeWidget(QWidget* w)
{
	return (addWidget(w, SARibbonPanelItem::Large));
}

/**
 * \if ENGLISH
 * @brief Adds a gallery widget to the panel
 *
 * A gallery is a specialized widget for displaying a collection of items. If `expanding` is true,
 * the panel's horizontal size policy will be set to `Expanding`.
 *
 * @param expanding If true, sets the panel's size policy to Expanding
 * @return The created SARibbonGallery
 * \endif
 *
 * \if CHINESE
 * @brief 向面板添加一个画廊窗口部件
 *
 * 画廊是一个用于显示项目集合的专用窗口部件。如果 `expanding` 为true，
 * 面板的水平尺寸策略将被设置为 `Expanding`。
 *
 * @param expanding 如果为true，将面板的尺寸策略设置为Expanding
 * @return 创建的SARibbonGallery
 * \endif
 */
SARibbonGallery* SARibbonPanel::addGallery(bool expanding)
{
	SARibbonGallery* gallery = RibbonSubElementFactory->createRibbonGallery(this);

	addWidget(gallery, SARibbonPanelItem::Large);
	if (expanding) {
		setExpanding(expanding);
	}
	return (gallery);
}

/**
 * \if ENGLISH
 * @brief Adds a separator
 * @return The created separator action
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个分隔符
 * @return 创建的分隔符action
 * \endif
 */
QAction* SARibbonPanel::addSeparator()
{
	QAction* action = new QAction(this);

	action->setSeparator(true);
	setActionRowProportionProperty(action, SARibbonPanelItem::Large);
	addAction(action);
	return (action);
}

/**
 * \if ENGLISH
 * @brief Retrieves the SARibbonToolButton associated with an action
 * @param action The action to query
 * @return The associated tool button, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 获取与action关联的SARibbonToolButton
 * @param action 要查询的action
 * @return 关联的工具按钮，如果未找到则返回nullptr
 * \endif
 */
SARibbonToolButton* SARibbonPanel::actionToRibbonToolButton(QAction* action)
{
	for (auto obj : sa_as_const(children())) {
		if (obj->isWidgetType()) {
			if (SARibbonToolButton* btn = qobject_cast< SARibbonToolButton* >(obj)) {
				if (btn->defaultAction() == action) {
					return btn;
				}
			}
		}
	}
	return (nullptr);
}

/**
 * \if ENGLISH
 * @brief Returns a list of all SARibbonToolButton children
 * @return A list of pointers to SARibbonToolButton
 * \endif
 *
 * \if CHINESE
 * @brief 返回所有SARibbonToolButton子对象的列表
 * @return SARibbonToolButton指针的列表
 * \endif
 */
QList< SARibbonToolButton* > SARibbonPanel::ribbonToolButtons() const
{
	const QObjectList& objs = children();
	QList< SARibbonToolButton* > res;

	for (QObject* o : objs) {
		SARibbonToolButton* b = qobject_cast< SARibbonToolButton* >(o);
		if (b) {
			res.append(b);
		}
	}
	return (res);
}

/**
 * \if ENGLISH
 * @brief Sets the panel's layout mode
 * @param mode The new layout mode (ThreeRowMode or TwoRowMode)
 * @sa panelLayoutMode
 * \endif
 *
 * \if CHINESE
 * @brief 设置面板的布局模式
 * @param mode 新的布局模式（三行模式或两行模式）
 * @sa panelLayoutMode
 * \endif
 */
void SARibbonPanel::setPanelLayoutMode(SARibbonPanel::PanelLayoutMode mode)
{
	if (d_ptr->m_panelLayoutMode == mode) {
		return;
	}
	d_ptr->m_panelLayoutMode = mode;
	updateItemGeometry();
}

/**
 * \if ENGLISH
 * @brief Gets the current layout mode
 * @return The current layout mode
 * @sa setPanelLayoutMode
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前的布局模式
 * @return 当前的布局模式
 * @sa setPanelLayoutMode
 * \endif
 */
SARibbonPanel::PanelLayoutMode SARibbonPanel::panelLayoutMode() const
{
	return (d_ptr->m_panelLayoutMode);
}

/**
 * \if ENGLISH
 * @brief Sets the option action for the panel
 *
 * The option action is typically displayed as a small button in the panel's title area.
 * Pass `nullptr` to remove the current option action.
 *
 * @param action The option action, or nullptr to remove
 * @sa isHaveOptionAction
 * \endif
 *
 * \if CHINESE
 * @brief 为面板设置选项action
 *
 * 选项action通常显示在面板标题区域的一个小按钮中。
 * 传入 `nullptr` 以移除当前的选项action。
 *
 * @param action 选项action，或传入nullptr以移除
 * @sa isHaveOptionAction
 * \endif
 */
void SARibbonPanel::setOptionAction(QAction* action)
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		lay->setOptionAction(action);
	}
}

/**
 * \if ENGLISH
 * @brief Checks if an option action is set
 * @return true if an option action exists; otherwise false
 * @sa setOptionAction
 * \endif
 *
 * \if CHINESE
 * @brief 检查是否设置了选项action
 * @return 如果存在选项action则返回true；否则返回false
 * @sa setOptionAction
 * \endif
 */
bool SARibbonPanel::isHaveOptionAction() const
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		return lay->isHaveOptionAction();
	}
	return false;
}

/**
 * \if ENGLISH
 * @brief Returns the recommended size for the widget
 * @return The recommended size for the widget
 * \endif
 *
 * \if CHINESE
 * @brief 返回窗口部件的推荐大小
 * @return 窗口部件的推荐大小
 * \endif
 */
QSize SARibbonPanel::sizeHint() const
{
	int shWidth  = 500;
	int shHeight = 100;
	if (QLayout* lay = layout()) {
		QSize laySize = layout()->sizeHint();
		shWidth       = laySize.width();
		shHeight      = laySize.height();
	}
	return QSize(shWidth, shHeight);
}

/**
 * \if ENGLISH
 * @brief Returns the minimum recommended size for the widget
 * @return The minimum recommended size for the widget
 * \endif
 *
 * \if CHINESE
 * @brief 返回窗口部件的最小推荐大小
 * @return 窗口部件的最小推荐大小
 * \endif
 */
QSize SARibbonPanel::minimumSizeHint() const
{
	return (layout()->minimumSize());
}

/**
 * \if ENGLISH
 * @brief Sets the icon size for all tool buttons in the panel
 *
 * This sets the default icon size for newly created buttons and updates the icon size for existing buttons.
 *
 * @param smallSize The new small icon size
 * @param largeSize The new large icon size
 * @sa toolButtonIconSize
 * \endif
 *
 * \if CHINESE
 * @brief 设置面板内所有工具按钮的图标尺寸
 *
 * 此函数设置新创建按钮的默认图标尺寸，并更新现有按钮的图标尺寸。
 *
 * @param smallSize 新的小图标尺寸
 * @param largeSize 新的大图标尺寸
 * @sa toolButtonIconSize
 * \endif
 */
void SARibbonPanel::setToolButtonIconSize(const QSize& smallSize, const QSize& largeSize)
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		lay->setToolButtonIconSize(smallSize, largeSize);
	}
	// 对已经管理的QToolButton设置为iconsize
	iterateButton([ smallSize, largeSize ](SARibbonToolButton* t) -> bool {
		t->setIconSize(smallSize);
		t->setLargeIconSize(largeSize);
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Gets the current default icon size for tool buttons
 * @return The current icon size (small size, large size)
 * @sa setToolButtonIconSize
 * \endif
 *
 * \if CHINESE
 * @brief 获取工具按钮当前的默认图标尺寸
 * @return 当前的图标尺寸（小图标尺寸，大图标尺寸）
 * @sa setToolButtonIconSize
 * \endif
 */
QPair< QSize, QSize > SARibbonPanel::toolButtonIconSize() const
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		return lay->toolButtonIconSize();
	}
	return QPair< QSize, QSize >();
}

/**
 * \if ENGLISH
 * @brief Sets the large icon size for tool buttons
 * @param largeSize The new large icon size
 * \endif
 *
 * \if CHINESE
 * @brief 设置工具按钮的大图标尺寸
 * @param largeSize 新的大图标尺寸
 * \endif
 */
void SARibbonPanel::setLargeIconSize(const QSize& largeSize)
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		lay->setLargeIconSize(largeSize);
	}
	// 对已经管理的QToolButton设置为iconsize
	iterateButton([ largeSize ](SARibbonToolButton* t) -> bool {
		t->setLargeIconSize(largeSize);
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Gets the large icon size for tool buttons
 * @return The current large icon size
 * \endif
 *
 * \if CHINESE
 * @brief 获取工具按钮的大图标尺寸
 * @return 当前的大图标尺寸
 * \endif
 */
QSize SARibbonPanel::largeIconSize() const
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		return lay->largeIconSize();
	}
	return QSize();
}

/**
 * \if ENGLISH
 * @brief Sets the small icon size for tool buttons
 * @param smallSize The new small icon size
 * \endif
 *
 * \if CHINESE
 * @brief 设置工具按钮的小图标尺寸
 * @param smallSize 新的小图标尺寸
 * \endif
 */
void SARibbonPanel::setSmallIconSize(const QSize& smallSize)
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		lay->setSmallIconSize(smallSize);
	}
	// 对已经管理的QToolButton设置为iconsize
	iterateButton([ smallSize ](SARibbonToolButton* t) -> bool {
		t->setSmallIconSize(smallSize);
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Gets the small icon size for tool buttons
 * @return The current small icon size
 * \endif
 *
 * \if CHINESE
 * @brief 获取工具按钮的小图标尺寸
 * @return 当前的小图标尺寸
 * \endif
 */
QSize SARibbonPanel::smallIconSize() const
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		return lay->smallIconSize();
	}
	return QSize();
}

/**
 * \if ENGLISH
 * @brief Iterates over all SARibbonToolButton children
 *
 * Executes the provided function pointer for each `SARibbonToolButton`. If the function returns `false`,
 * iteration stops immediately.
 *
 * Example:
 * @code
 * myPanel->iterateButton([](SARibbonToolButton* btn) -> bool {
 *     btn->setToolTip("Visited");
 *     return true; // Continue to next button
 * });
 * @endcode
 *
 * @param fp A function pointer that takes a SARibbonToolButton* and returns a bool
 * @return true if all buttons were iterated; false if iteration was stopped early
 * \endif
 *
 * \if CHINESE
 * @brief 遍历所有SARibbonToolButton子对象
 *
 * 对每个 `SARibbonToolButton` 执行提供的函数指针。如果函数返回 `false`，
 * 迭代将立即停止。
 *
 * 示例：
 * @code
 * myPanel->iterateButton([](SARibbonToolButton* btn) -> bool {
 *     btn->setToolTip("Visited");
 *     return true; // 继续下一个按钮
 * });
 * @endcode
 *
 * @param fp 一个接受SARibbonToolButton*并返回bool的函数指针
 * @return 如果遍历了所有按钮则返回true；如果提前停止则返回false
 * \endif
 */
bool SARibbonPanel::iterateButton(SARibbonPanel::FpRibbonToolButtonIterate fp) const
{
	const QObjectList& ols = children();
	for (QObject* obj : ols) {
		if (SARibbonToolButton* t = qobject_cast< SARibbonToolButton* >(obj)) {
			if (!fp(t)) {
				return false;
			}
		}
	}
	return true;
}

/**
 * \if ENGLISH
 * @brief Sets the panel to expanding mode
 *
 * In expanding mode, the panel will try to occupy all available horizontal space in its parent layout.
 *
 * @param isExpanding If true, sets the horizontal size policy to Expanding
 * @sa isExpanding
 * \endif
 *
 * \if CHINESE
 * @brief 将面板设置为扩展模式
 *
 * 在扩展模式下，面板将尝试占据其父布局中所有可用的水平空间。
 *
 * @param isExpanding 如果为true，将水平尺寸策略设置为Expanding
 * @sa isExpanding
 * \endif
 */
void SARibbonPanel::setExpanding(bool isExpanding)
{
	setSizePolicy(isExpanding ? QSizePolicy::Expanding : QSizePolicy::Preferred, QSizePolicy::Fixed);
}

/**
 * \if ENGLISH
 * @brief Checks if the panel is in expanding mode
 * @return true if the horizontal size policy is Expanding; otherwise false
 * @sa setExpanding
 * \endif
 *
 * \if CHINESE
 * @brief 检查面板是否处于扩展模式
 * @return 如果水平尺寸策略为Expanding则返回true；否则返回false
 * @sa setExpanding
 * \endif
 */
bool SARibbonPanel::isExpanding() const
{
	QSizePolicy sp = sizePolicy();

	return (sp.horizontalPolicy() == QSizePolicy::Expanding);
}

/**
 * \if ENGLISH
 * @brief Sets the height of the panel's title
 *
 * This function is typically called by `SARibbonBar` and is protected to prevent direct user manipulation.
 *
 * @param h The new title height
 * @sa SARibbonBar::setPanelTitleHeight
 * \endif
 *
 * \if CHINESE
 * @brief 设置面板标题的高度
 *
 * 此函数通常由 `SARibbonBar` 调用，为防止用户直接操作而设为protected。
 *
 * @param h 新的标题高度
 * @sa SARibbonBar::setPanelTitleHeight
 * \endif
 */
void SARibbonPanel::setTitleHeight(int h)
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		lay->setPanelTitleHeight(h);
	}
	d_ptr->resetTitleLabelFont();
}

/**
 * \if ENGLISH
 * @brief Gets the height of the panel's title
 * @return The current title height
 * \endif
 *
 * \if CHINESE
 * @brief 获取面板标题的高度
 * @return 当前的标题高度
 * \endif
 */
int SARibbonPanel::titleHeight() const
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		return lay->panelTitleHeight();
	}
	return -1;
}

/**
 * \if ENGLISH
 * @brief Checks if the panel's title is visible
 * @return true if the title is visible; otherwise false
 * \endif
 *
 * \if CHINESE
 * @brief 检查面板标题是否可见
 * @return 如果标题可见则返回true；否则返回false
 * \endif
 */
bool SARibbonPanel::isEnableShowTitle() const
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		return lay->isEnableShowPanelTitle();
	}
	return false;
}

/**
 * \if ENGLISH
 * @brief Sets whether the panel's title is visible
 *
 * This function is typically called by `SARibbonBar` and is protected to prevent direct user manipulation.
 *
 * @param on If true, the title is visible
 * @sa SARibbonBar::setEnableShowPanelTitle
 * \endif
 *
 * \if CHINESE
 * @brief 设置面板标题是否可见
 *
 * 此函数通常由 `SARibbonBar` 调用，为防止用户直接操作而设为protected。
 *
 * @param on 如果为true，则标题可见
 * @sa SARibbonBar::setEnableShowPanelTitle
 * \endif
 */
void SARibbonPanel::setEnableShowTitle(bool on)
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		return lay->setEnableShowPanelTitle(on);
	}
}

/**
 * \if ENGLISH
 * @brief Gets the index of an action within the panel
 * @param act The action to find
 * @return The index of the action, or -1 if not found
 * \endif
 *
 * \if CHINESE
 * @brief 获取一个action在面板内的索引
 * @param act 要查找的action
 * @return action的索引，如果未找到则返回-1
 * \endif
 */
int SARibbonPanel::actionIndex(QAction* act) const
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		return (lay->indexByAction(act));
	}
	return (-1);
}

/**
 * \if ENGLISH
 * @brief Moves an action to a new position
 * @param from The current index of the action
 * @param to The new index for the action
 * \endif
 *
 * \if CHINESE
 * @brief 将一个action移动到新位置
 * @param from action的当前索引
 * @param to action的新索引
 * \endif
 */
void SARibbonPanel::moveAction(int from, int to)
{
	if (SARibbonPanelLayout* lay = panelLayout()) {
		lay->move(from, to);
		lay->activate();
	}
}

/**
 * \if ENGLISH
 * @brief Checks if customization is allowed
 * @return true if customization is allowed; otherwise false
 * @sa setCanCustomize
 * \endif
 *
 * \if CHINESE
 * @brief 检查是否允许自定义
 * @return 如果允许自定义则返回true；否则返回false
 * @sa setCanCustomize
 * \endif
 */
bool SARibbonPanel::isCanCustomize() const
{
	return (d_ptr->m_isCanCustomize);
}

/**
 * \if ENGLISH
 * @brief Sets whether customization is allowed
 * @param b If true, customization is allowed
 * @sa isCanCustomize
 * \endif
 *
 * \if CHINESE
 * @brief 设置是否允许自定义
 * @param b 如果为true，则允许自定义
 * @sa isCanCustomize
 * \endif
 */
void SARibbonPanel::setCanCustomize(bool b)
{
	d_ptr->m_isCanCustomize = b;
}

/**
 * \if ENGLISH
 * @brief Gets the panel's name
 * @return The panel's name
 * @sa setPanelName
 * \endif
 *
 * \if CHINESE
 * @brief 获取面板的名称
 * @return 面板的名称
 * @sa setPanelName
 * \endif
 */
QString SARibbonPanel::panelName() const
{
	return (d_ptr->panelName());
}

/**
 * \if ENGLISH
 * @brief Sets the panel's name
 *
 * Setting the name will also emit the `panelNameChanged` signal.
 *
 * @param title The new name for the panel
 * @sa panelName
 * \endif
 *
 * \if CHINESE
 * @brief 设置面板的名称
 *
 * 设置名称还会发出 `panelNameChanged` 信号。
 *
 * @param title 面板的新名称
 * @sa panelName
 * \endif
 */
void SARibbonPanel::setPanelName(const QString& title)
{
	QString oldName = panelName();
	if (oldName != title) {
		d_ptr->setPanelName(title);
		Q_EMIT panelNameChanged(title);
	}
}

/**
 * \if ENGLISH
 * @brief Gets the height of the large buttons in this panel
 * @return The height of large buttons
 * \endif
 *
 * \if CHINESE
 * @brief 获取此面板中大按钮的高度
 * @return 大按钮的高度
 * \endif
 */
int SARibbonPanel::largeButtonHeight() const
{
	const QMargins& mag = contentsMargins();
	return height() - mag.top() - mag.bottom() - titleHeight();
}

/**
 * \if ENGLISH
 * @brief Gets the panel's layout object
 * @return A pointer to the SARibbonPanelLayout
 * \endif
 *
 * \if CHINESE
 * @brief 获取面板的布局对象
 * @return 指向SARibbonPanelLayout的指针
 * \endif
 */
SARibbonPanelLayout* SARibbonPanel::panelLayout() const
{
	return d_ptr->panelLayout();
}

/**
 * \if ENGLISH
 * @brief Updates the geometry of all items in the panel
 *
 * This function forces a recalculation of the layout and size hints for all buttons and items.
 *
 * \endif
 *
 * \if CHINESE
 * @brief 更新面板内所有项目的几何形状
 *
 * 此函数强制重新计算所有按钮和项目的布局及尺寸提示。
 *
 * \endif
 */
void SARibbonPanel::updateItemGeometry()
{
#if SARIBBONPANEL_DEBUG_PRINT
	qDebug() << "SARibbonPanel updateItemGeometry,panelName=" << panelName();
#endif
	// 此函数需要添加，否则SARibbonBar::setEnableWordWrap无法刷新按钮
	// resetToolButtonSize();
	if (SARibbonPanelLayout* lay = panelLayout()) {
		lay->updateGeomArray();
	}
}

/**
 * \if ENGLISH
 * @brief Gets the parent SARibbonCategory
 * @return The parent category, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 获取父SARibbonCategory
 * @return 父category，如果未找到则返回nullptr
 * \endif
 */
SARibbonCategory* SARibbonPanel::category() const
{
	return qobject_cast< SARibbonCategory* >(parent());
}

/**
 * \if ENGLISH
 * @brief Gets the top-level SARibbonBar
 * @return The ribbon bar, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 获取顶层的SARibbonBar
 * @return Ribbon栏，如果未找到则返回nullptr
 * \endif
 */
SARibbonBar* SARibbonPanel::ribbonBar() const
{
	if (SARibbonCategory* c = category()) {
		return c->ribbonBar();
	}
	return nullptr;
}

/**
 * \if ENGLISH
 * @brief Sets the spacing between elements in the panel
 *
 * This function is typically called by `SARibbonBar` and is protected to prevent direct user manipulation.
 *
 * @param n The new spacing value
 * @sa SARibbonBar::setPanelSpacing
 * \endif
 *
 * \if CHINESE
 * @brief 设置面板内元素之间的间距
 *
 * 此函数通常由 `SARibbonBar` 调用，为防止用户直接操作而设为protected。
 *
 * @param n 新的间距值
 * @sa SARibbonBar::setPanelSpacing
 * \endif
 */
void SARibbonPanel::setSpacing(int n)
{
	if (auto lay = layout()) {
		lay->setSpacing(n);
	}
}

/**
 * \if ENGLISH
 * @brief Gets the spacing between elements in the panel
 * @return The current spacing value
 * @sa setSpacing
 * \endif
 *
 * \if CHINESE
 * @brief 获取面板内元素之间的间距
 * @return 当前的间距值
 * @sa setSpacing
 * \endif
 */
int SARibbonPanel::spacing() const
{
	if (auto lay = layout()) {
		return lay->spacing();
	}
	return 0;
}

/**
 * \if ENGLISH
 * @brief Gets the panel's recommended height
 *
 * This is a static helper function used internally for layout calculations.
 *
 * @param fm The font metrics to use for calculation
 * @param layMode The layout mode
 * @param panelTitleHeight The height of the panel's title
 * @return The recommended height
 * \endif
 *
 * \if CHINESE
 * @brief 获取面板的推荐高度
 *
 * 这是一个用于内部布局计算的静态辅助函数。
 *
 * @param fm 用于计算的字体度量
 * @param layMode 布局模式
 * @param panelTitleHeight 面板标题的高度
 * @return 推荐的高度
 * \endif
 */
int SARibbonPanel::panelHeightHint(const QFontMetrics& fm, PanelLayoutMode layMode, int panelTitleHeight)
{
	int textH = fm.lineSpacing();  // 这里用linespace，因为在换行的情况下，行距是不可忽略的，ribbon的大按钮默认是2行
	switch (layMode) {
	case SARibbonPanel::ThreeRowMode: {
		// 5.5=（3*1.6+1） （三行）,1是给paneltitle预留的
		return textH * 4.8 + panelTitleHeight;
	} break;
	case SARibbonPanel::TwoRowMode: {
		// 3=2*1.6
		return textH * 3.2 + panelTitleHeight;
	} break;
	case SARibbonPanel::SingleRowMode: {
		// Single row: 1.6x text height, no panel title (hidden by default)
		return textH * 1.6;
	} break;
	default: {
		qWarning() << "unknow SARibbonPanel::PanelLayoutMode:" << layMode;
	}
	}
	return (textH * 4.8 + panelTitleHeight);
}

/**
 * \if ENGLISH
 * @brief Resets the size of buttons, after layout changes (especially height changes), the button size needs to be manually changed
 * \endif
 *
 * \if CHINESE
 * @brief 重置按钮的尺寸，在布局改变后（尤其高度变更），按钮的尺寸需要手动变更
 * \endif
 */
void SARibbonPanel::resetToolButtonSize()
{
	QList< SARibbonToolButton* > btns = ribbonToolButtons();

	for (SARibbonToolButton* b : sa_as_const(btns)) {
		if ((nullptr == b)) {
			continue;
		}
		b->invalidateSizeHint();
	}
}

/**
 * \if ENGLISH
 * @brief Checks if the panel is in two-row mode
 * @return true if the panel is in two-row mode, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查面板是否处于两行模式
 * @return 如果面板处于两行模式返回true，否则返回false
 * \endif
 */
bool SARibbonPanel::isTwoRow() const
{
	return (TwoRowMode == panelLayoutMode());
}

/**
 * \if ENGLISH
 * @brief Handles action events (add, remove, change)
 *
 * This function is called by Qt when actions are added, removed, or changed. It ensures the panel's
 * layout is updated accordingly.
 *
 * @param e The action event
 * \endif
 *
 * \if CHINESE
 * @brief 处理action事件（添加、移除、更改）
 *
 * 当action被添加、移除或更改时，Qt会调用此函数。它确保面板的布局相应更新。
 *
 * @param e action事件
 * \endif
 */
void SARibbonPanel::actionEvent(QActionEvent* e)
{
	QAction* action             = e->action();
	QWidgetAction* widgetAction = qobject_cast< QWidgetAction* >(action);

	switch (e->type()) {
	case QEvent::ActionAdded: {
		SARibbonPanelLayout* lay = panelLayout();
		if (nullptr != widgetAction) {
			if (widgetAction->parent() != this) {
				widgetAction->setParent(this);
			}
		}
		int index = lay->count();
		if (e->before()) {
			// 说明是插入
			index = lay->indexByAction(e->before());
			if (-1 == index) {
				index = lay->count();  // 找不到的时候就插入到最后
			}
		}
		lay->insertAction(index, action, getActionRowProportionProperty(action));
		// 通知父布局这个控件的尺寸提示(sizeHint())可能已改变
		updateGeometry();
	} break;

	case QEvent::ActionChanged: {
		// 让布局重新绘制
		// 通知父布局这个控件的尺寸提示(sizeHint())可能已改变
		updateGeometry();
		// 只处理 QWidgetAction 的情况
		if (QWidgetAction* widgetAction = qobject_cast< QWidgetAction* >(e->action())) {
			// 安全获取关联控件
			if (QWidget* w = widgetAction->defaultWidget()) {
				w->setEnabled(widgetAction->isEnabled());
			}
		}
	} break;

	case QEvent::ActionRemoved: {
		SARibbonPanelLayout* lay = panelLayout();
		action->disconnect(this);
		int index = lay->indexByAction(action);
		if (index != -1) {
			QLayoutItem* item = lay->takeAt(index);
			delete item;
		}
		updateGeometry();
	} break;

	default:
		break;
	}
}

/**
 * \if ENGLISH
 * @brief Handles change events (e.g., font change)
 *
 * This function is called when properties like the font change. It propagates the change to child
 * widgets and invalidates the layout.
 *
 * @param e The change event
 * \endif
 *
 * \if CHINESE
 * @brief 处理更改事件（例如，字体更改）
 *
 * 当字体等属性更改时会调用此函数。它将更改传播到子窗口部件并使布局失效。
 *
 * @param e 更改事件
 * \endif
 */
void SARibbonPanel::changeEvent(QEvent* e)
{
	if (nullptr == e) {
		return;
	}
	switch (e->type()) {
	case QEvent::FontChange: {
		if (d_ptr->m_label) {
			d_ptr->m_label->setFont(font());
		}
		if (QLayout* lay = layout()) {
			lay->invalidate();
		}
		d_ptr->resetTitleLabelFont();
	} break;
	case QEvent::LayoutDirectionChange: {
		// 布局方向改变（如 LTR→RTL），失效布局并触发重绘
		if (QLayout* lay = layout()) {
			lay->invalidate();
		}
		update();
	} break;
	default:
		break;
	}
	QWidget::changeEvent(e);
}

/**
 * \if ENGLISH
 * @brief Gets the layout items
 * @return Reference to the list of SARibbonPanelItem
 * \endif
 *
 * \if CHINESE
 * @brief 获取布局item
 * @return SARibbonPanelItem的列表引用
 * \endif
 */
const QList< SARibbonPanelItem* >& SARibbonPanel::ribbonPanelItem() const
{
	return (panelLayout()->mItems);
}

/**
 * \if ENGLISH
 * @brief Gets the last added button
 * @return Pointer to the last added button
 * \endif
 *
 * \if CHINESE
 * @brief 获取最近添加的按钮
 * @return 刚才添加的按钮的指针
 * \endif
 */
SARibbonToolButton* SARibbonPanel::lastAddActionButton()
{
	return d_ptr->lastAddActionButton();
}

/**
 * \if ENGLISH
 * @brief Gets the title label widget
 * @return A pointer to the SARibbonPanelLabel
 * \endif
 *
 * \if CHINESE
 * @brief 获取标题标签窗口部件
 * @return 指向SARibbonPanelLabel的指针
 * \endif
 */
SARibbonPanelLabel* SARibbonPanel::titleLabel() const
{
	return d_ptr->m_label;
}

/**
 * \if ENGLISH
 * @brief Sets whether the panel's button text allows line wrapping
 * @param on If true, enables word wrapping for button text
 * \endif
 *
 * \if CHINESE
 * @brief 设置 panel 的按钮文字允许换行
 * @param on 如果为 true，启用按钮文字换行
 * \endif
 */
void SARibbonPanel::setEnableWordWrap(bool on)
{
	if (SARibbonPanelLayout* lay = qobject_cast< SARibbonPanelLayout* >(layout())) {
		// 此函数会自动设置所有按钮的换行状态
		lay->setEnableWordWrap(on);
		updateGeometry();
	}
}

/**
 * \if ENGLISH
 * @brief Checks if the panel's text allows line wrapping
 * @return true if word wrapping is enabled for button text, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 判断 panel 的文字是否允许换行
 * @return 如果按钮文字换行已启用返回 true，否则返回 false
 * \endif
 */
bool SARibbonPanel::isEnableWordWrap() const
{
	if (SARibbonPanelLayout* lay = qobject_cast< SARibbonPanelLayout* >(layout())) {
		return lay->isEnableWordWrap();
	}
	return false;
}

/**
 * \if ENGLISH
 * @brief Set whether button text is displayed to the right of the icon
 * @param on If true, all toolbuttons will show text to the right of icon
 * @details This cascades to all SARibbonToolButton widgets in this panel.
 *           When enabled, Large buttons are rendered with horizontal layout
 *           (icon-left, text-right) instead of the default vertical layout.
 * \endif
 *
 * \if CHINESE
 * @brief 设置按钮文字是否显示在图标右侧
 * @param on 如果为 true，所有 toolbutton 将显示图标在左、文字在右
 * @details 此属性会级联传递到 panel 中所有 SARibbonToolButton。
 *           启用时，Large 按钮使用水平布局（图标左、文字右）代替默认的垂直布局。
 * \endif
 */
void SARibbonPanel::setEnableIconRightText(bool on)
{
	SA_D(d);
	if (d->enableIconRightText == on) {
		return;
	}
	d->enableIconRightText = on;
	iterateButton([on](SARibbonToolButton* btn) {
		btn->setEnableIconRightText(on);
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Check if icon-right-text mode is enabled
 * @return true if icon-right-text mode is enabled
 * \endif
 *
 * \if CHINESE
 * @brief 检查图标右侧文字模式是否启用
 * @return 如果启用图标右侧文字模式返回 true
 * \endif
 */
bool SARibbonPanel::isEnableIconRightText() const
{
	SA_DC(d);
	return d->enableIconRightText;
}

/**
 * \if ENGLISH
 * @brief Sets the button maximum aspect ratio, this coefficient determines the maximum width of the button
 *
 * The maximum width of the button is button height * this coefficient. For example, if the button height is h,
 * then the maximum button width maxw = h * buttonMaximumAspectRatio
 * If the text cannot be fully displayed at this width, the button will not continue to expand horizontally,
 * and ... will be used instead of the incompletely displayed text
 *
 * @see buttonMaximumAspectRatio
 *
 * @note Users should not call @ref SARibbonPanel::setButtonMaximumAspectRatio to set,
 * but call @ref SARibbonBar::setButtonMaximumAspectRatio to set the aspect ratio
 *
 * @param fac New maximum aspect ratio
 * \endif
 *
 * \if CHINESE
 * @brief 设置按钮最大宽高比，这个系数决定按钮的最大宽度
 *
 * 按钮的最大宽度为按钮高度*此系数，例如按钮高度为h，那么按钮最大宽度maxw=h*buttonMaximumAspectRatio
 * 如果在此宽度下文字还无法完全显示，那么按钮将不会继续横向扩展，将使用...替代未完全显示的文字
 *
 * @see buttonMaximumAspectRatio
 *
 * @note 用户不应该调用@ref SARibbonPanel::setButtonMaximumAspectRatio 来设置，
 * 而是调用@ref SARibbonBar::setButtonMaximumAspectRatio 设置宽高比
 *
 * @param fac 新的最大宽高比
 * \endif
 */
void SARibbonPanel::setButtonMaximumAspectRatio(qreal fac)
{
	if (SARibbonPanelLayout* lay = qobject_cast< SARibbonPanelLayout* >(layout())) {
		// 此函数会自动设置所有按钮的换行状态
		lay->setButtonMaximumAspectRatio(fac);
	}
}

/**
 * \if ENGLISH
 * @brief Button maximum aspect ratio, this coefficient determines the maximum width of the button
 * @return Current button maximum aspect ratio
 * @see setButtonMaximumAspectRatio
 * \endif
 *
 * \if CHINESE
 * @brief 按钮最大宽高比，这个系数决定按钮的最大宽度
 * @return 按钮最大宽高比
 * @see setButtonMaximumAspectRatio
 * \endif
 */
qreal SARibbonPanel::buttonMaximumAspectRatio() const
{
	if (SARibbonPanelLayout* lay = qobject_cast< SARibbonPanelLayout* >(layout())) {
		return lay->buttonMaximumAspectRatio();
	}
	return 1.4;
}

/*** End of inlined file: SARibbonPanel.cpp ***/


/*** Start of inlined file: SARibbonCategory.cpp ***/
#include <QList>
#include <QResizeEvent>
#include <QPainter>
#include <QLinearGradient>
#include <QApplication>
#include <QHBoxLayout>
#include <QList>
#include <QMap>
#include <QResizeEvent>

#ifndef SARIBBONCATEGORY_DEBUG_PRINT
#define SARIBBONCATEGORY_DEBUG_PRINT 0
#endif
#if SARIBBONCATEGORY_DEBUG_PRINT
#include <QDebug>
#endif

/**
 * @brief SARibbonCategory::PrivateData
 */
class SARibbonCategory::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonCategory)
public:
	PrivateData(SARibbonCategory* p);

	SARibbonPanel* addPanel(const QString& title);
	SARibbonPanel* insertPanel(const QString& title, int index);
	void addPanel(SARibbonPanel* panel);
	void insertPanel(int index, SARibbonPanel* panel);

	// 把panel从Category中移除，不会销毁，此时panel的所有权归还操作者
	bool takePanel(SARibbonPanel* panel);

	// 移除Panel，Category会直接回收SARibbonPanel内存
	bool removePanel(SARibbonPanel* panel);
	SARibbonCategory* ribbonCategory();
	const SARibbonCategory* ribbonCategory() const;

	// 返回所有的Panel
	QList< SARibbonPanel* > panelList();

	// 更新item的布局,此函数会调用doItemLayout
	void updateItemGeometry();

	void doWheelEvent(QWheelEvent* event);
	// 初始化
	void init(SARibbonCategory* c);

public:
	bool enableShowPanelTitle { true };             ///< 是否运行panel的标题栏显示
	int panelTitleHeight { 15 };                    ///< panel的标题栏默认高度
	bool isContextCategory { false };               ///< 标记是否是上下文标签
	bool isCanCustomize { true };                   ///< 标记是否可以自定义
	int panelSpacing { 0 };                         ///< panel的spacing
	bool isUseAnimating { true };                   ///< 默认使用动画滚动
	bool enableWordWrap { true };                   ///< 是否文字换行
	bool enableIconRightText { false };             ///< 是否启用图标右侧文字模式
	QSize panelSmallToolButtonIconSize { 20, 20 };  ///< 记录 panel 的默认小图标大小
	QSize panelLargeToolButtonIconSize { 32, 32 };  ///< 记录 panel 的默认大图标大小
	int wheelScrollStep { 400 };                    ///< 默认滚轮滚动步长
	qreal buttonMaximumAspectRatio { 1.4 };         ///< 按钮最大宽高比，这个系数决定按钮的最大宽度
	SARibbonPanel::PanelLayoutMode defaultPanelLayoutMode { SARibbonPanel::ThreeRowMode };
};
SARibbonCategory::PrivateData::PrivateData(SARibbonCategory* p) : q_ptr(p)
{
}

SARibbonPanel* SARibbonCategory::PrivateData::addPanel(const QString& title)
{
	if (SARibbonCategoryLayout* lay = q_ptr->categoryLayout()) {
		SARibbonPanel* p = insertPanel(title, lay->panelCount());
		return p;
	}
	return nullptr;
}

SARibbonPanel* SARibbonCategory::PrivateData::insertPanel(const QString& title, int index)
{
	SARibbonPanel* panel = RibbonSubElementFactory->createRibbonPanel(ribbonCategory());

	panel->setPanelName(title);
	panel->setObjectName(title);
	insertPanel(index, panel);
	return (panel);
}

void SARibbonCategory::PrivateData::addPanel(SARibbonPanel* panel)
{
	if (SARibbonCategoryLayout* lay = q_ptr->categoryLayout()) {
		insertPanel(lay->panelCount(), panel);
	}
}

/**
 * @brief 插入panel到layout
 *
 * 所有的添加操作最终会调用此函数
 * @param index
 * @param panel
 */
void SARibbonCategory::PrivateData::insertPanel(int index, SARibbonPanel* panel)
{
	if (nullptr == panel) {
		return;
	}
	SARibbonCategoryLayout* lay = qobject_cast< SARibbonCategoryLayout* >(q_ptr->layout());
	if (nullptr == lay) {
		return;
	}
	if (panel->parentWidget() != q_ptr) {
		panel->setParent(q_ptr);
	}
	// 同步状态
	panel->setEnableShowTitle(this->enableShowPanelTitle);
	panel->setTitleHeight(this->panelTitleHeight);
	panel->setPanelLayoutMode(this->defaultPanelLayoutMode);
	panel->setSpacing(this->panelSpacing);
	panel->setToolButtonIconSize(this->panelSmallToolButtonIconSize, this->panelLargeToolButtonIconSize);
	panel->setEnableWordWrap(this->enableWordWrap);
	panel->setEnableIconRightText(this->enableIconRightText);

	index = qMax(0, index);
	index = qMin(lay->panelCount(), index);
	lay->insertPanel(index, panel);
	panel->setVisible(true);

	QObject::connect(panel, &SARibbonPanel::actionTriggered, ribbonCategory(), &SARibbonCategory::actionTriggered);
	q_ptr->updateGeometry();  // 通知父布局这个控件的尺寸提示(sizeHint())可能已改变
}

bool SARibbonCategory::PrivateData::takePanel(SARibbonPanel* panel)
{
	SARibbonCategoryLayout* lay = qobject_cast< SARibbonCategoryLayout* >(q_ptr->layout());
	if (nullptr == lay) {
		return false;
	}
	bool res = lay->takePanel(panel);
	q_ptr->updateGeometry();  // 通知父布局这个控件的尺寸提示(sizeHint())可能已改变
	return res;
}

bool SARibbonCategory::PrivateData::removePanel(SARibbonPanel* panel)
{
	if (takePanel(panel)) {
		panel->hide();
		panel->deleteLater();
		return (true);
	}
	return (false);
}

QList< SARibbonPanel* > SARibbonCategory::PrivateData::panelList()
{
	if (SARibbonCategoryLayout* lay = q_ptr->categoryLayout()) {
		return lay->panelList();
	}
	return QList< SARibbonPanel* >();
}

SARibbonCategory* SARibbonCategory::PrivateData::ribbonCategory()
{
	return (q_ptr);
}

const SARibbonCategory* SARibbonCategory::PrivateData::ribbonCategory() const
{
	return (q_ptr);
}

void SARibbonCategory::PrivateData::updateItemGeometry()
{
#if SARIBBONCATEGORY_DEBUG_PRINT
	qDebug() << "SARibbonCategory::PrivateData::updateItemGeometry,categoryName=" << q_ptr->categoryName();
#endif
	SARibbonCategoryLayout* lay = qobject_cast< SARibbonCategoryLayout* >(q_ptr->layout());
	if (!lay) {
		return;
	}
	const QList< SARibbonPanel* > panels = lay->panelList();
	for (auto panel : panels) {
		panel->updateItemGeometry();
	}
	lay->updateGeometryArr();
	return;
}

void SARibbonCategory::PrivateData::doWheelEvent(QWheelEvent* event)
{
	SARibbonCategoryLayout* lay = q_ptr->categoryLayout();
	if (nullptr == lay) {
		return;
	}

	// 如果动画正在进行，忽略新的事件
	if (isUseAnimating && lay->isAnimatingScroll()) {
		event->ignore();
		return;
	}

	QSize contentSize = lay->categoryContentSize();
	int totalWidth    = lay->categoryTotalWidth();

	if (totalWidth > contentSize.width()) {
		int scrollStep = wheelScrollStep;

		// 根据滚轮方向确定滚动方向
		QPoint numPixels  = event->pixelDelta();
		QPoint numDegrees = event->angleDelta() / 8;

		if (!numPixels.isNull()) {
			scrollStep = (numPixels.y() < 0) ? -scrollStep : scrollStep;
		} else if (!numDegrees.isNull()) {
			scrollStep = (numDegrees.y() < 0) ? -scrollStep : scrollStep;
		}

		// 动态调整步长 - 滚动越快步长越大
		const int absDelta = qMax(qAbs(numPixels.y()), qAbs(numDegrees.y()));
		if (absDelta > 60) {
			scrollStep *= 2;
		} else if (absDelta < 20) {
			scrollStep /= 2;
		}

		// 根据设置选择滚动方式
		if (isUseAnimating) {
			lay->scrollByAnimate(scrollStep);
		} else {
			lay->scroll(scrollStep);
		}
	} else {
		event->ignore();
		if (lay->isScrolled()) {
			// 根据设置选择复位方式
			if (isUseAnimating) {
				lay->scrollToByAnimate(0);
			} else {
				lay->scroll(0);
			}
		}
	}
}

void SARibbonCategory::PrivateData::init(SARibbonCategory* c)
{
	c->setLayout(new SARibbonCategoryLayout(c));
	c->connect(c, &SARibbonCategory::windowTitleChanged, c, &SARibbonCategory::categoryNameChanged);
}

//----------------------------------------------------
// SARibbonCategory
//----------------------------------------------------

SARibbonCategory::SARibbonCategory(QWidget* p) : QFrame(p), d_ptr(new SARibbonCategory::PrivateData(this))
{
	d_ptr->init(this);
}

/**
 * \if ENGLISH
 * @brief Constructor with name
 * @param name Category name
 * @param p Parent widget pointer
 * \endif
 *
 * \if CHINESE
 * @brief 带名称的构造函数
 * @param name Category名称
 * @param p 父级控件指针
 * \endif
 */
SARibbonCategory::SARibbonCategory(const QString& name, QWidget* p)
	: QFrame(p), d_ptr(new SARibbonCategory::PrivateData(this))
{
	setCategoryName(name);
	d_ptr->init(this);
}

SARibbonCategory::~SARibbonCategory()
{
}

/**
 * \if ENGLISH
 * @brief Get the category name
 * @return Current category name (i.e., windowTitle)
 * \endif
 *
 * \if CHINESE
 * @brief 获取Category名称
 * @return 当前Category的名称（即windowTitle）
 * \endif
 */
QString SARibbonCategory::categoryName() const
{
	return (windowTitle());
}

/**
 * \if ENGLISH
 * @brief Set the category name
 * @param title New name (equivalent to setWindowTitle)
 * \endif
 *
 * \if CHINESE
 * @brief 设置Category名称
 * @param title 新名称（等价于setWindowTitle）
 * \endif
 */
void SARibbonCategory::setCategoryName(const QString& title)
{
	setWindowTitle(title);
}

/**
 * \if ENGLISH
 * @brief Get the panel layout mode
 * @return Current layout mode for all panels
 * \endif
 *
 * \if CHINESE
 * @brief 获取面板布局模式
 * @return 当前所有面板的布局模式
 * \endif
 */
SARibbonPanel::PanelLayoutMode SARibbonCategory::panelLayoutMode() const
{
	return (d_ptr->defaultPanelLayoutMode);
}

/**
 * \if ENGLISH
 * @brief Set the panel layout mode
 *
 * When @ref SARibbonBar calls the @ref SARibbonBar::setRibbonStyle function, this function will be called for all
 * SARibbonCategory objects to set the new SARibbonPanel::PanelLayoutMode
 * @param m Panel layout mode
 * \endif
 *
 * \if CHINESE
 * @brief 设置面板布局模式
 *
 * 在@ref SARibbonBar 调用@ref SARibbonBar::setRibbonStyle 函数时，会对所有的SARibbonCategory调用此函数
 * 把新的SARibbonPanel::PanelLayoutMode设置进去
 * @param m 面板布局模式
 * \endif
 */
void SARibbonCategory::setPanelLayoutMode(SARibbonPanel::PanelLayoutMode m)
{
	d_ptr->defaultPanelLayoutMode = m;
	iteratePanel([ m ](SARibbonPanel* p) -> bool {
		p->setPanelLayoutMode(m);
		return true;
	});
	updateItemGeometry();
}

/**
 * \if ENGLISH
 * @brief Add a panel
 *
 * @note The ownership of the panel is managed by SARibbonCategory, please do not destroy it externally
 * @param title Panel title, which will be displayed below the panel in the three-row mode of office/wps
 * @return Returns a pointer to the generated @ref SARibbonPanel
 * @see For other operations on SARibbonPanel, refer to @ref SARibbonCategory::takePanel
 * \endif
 *
 * \if CHINESE
 * @brief 添加面板(panel)
 *
 * @note 面板(panel)的所有权由SARibbonCategory来管理，请不要在外部对其进行销毁
 * @param title 面板(panel)的标题，在office/wps的三行模式下会显示在面板(panel)的下方
 * @return 返回生成的@ref SARibbonPanel 指针
 * @see 对SARibbonPanel的其他操作，参考 @ref SARibbonCategory::takePanel
 * \endif
 */
SARibbonPanel* SARibbonCategory::addPanel(const QString& title)
{
	return (d_ptr->addPanel(title));
}

/**
 * \if ENGLISH
 * @brief Add panel
 * @param panel Panel ownership is managed by SARibbonCategory
 * \endif
 *
 * \if CHINESE
 * @brief 添加panel
 * @param panel panel的所有权SARibbonCategory来管理
 * \endif
 */
void SARibbonCategory::addPanel(SARibbonPanel* panel)
{
	d_ptr->addPanel(panel);
}

/**
 * \if ENGLISH
 * @brief Qt Designer specific
 * @param panel Widget to add
 * \endif
 *
 * \if CHINESE
 * @brief Qt Designer专用
 * @param panel 要添加的窗口部件
 * \endif
 */
void SARibbonCategory::addPanel(QWidget* panel)
{
	SARibbonPanel* p = qobject_cast< SARibbonPanel* >(panel);
	if (p) {
		addPanel(p);
	}
}

/**
 * \if ENGLISH
 * @brief Create a new panel and insert it at the index position
 * @param title Panel title
 * @param index Insertion position, if index exceeds the number of panels in the category, it will be inserted at the end
 * @return Returns a pointer to the generated @ref SARibbonPanel
 * \endif
 *
 * \if CHINESE
 * @brief 新建一个panel，并插入到index位置
 * @param title panel的title
 * @param index 插入的位置，如果index超出category里panel的个数，将插入到最后
 * @return 返回生成的@ref SARibbonPanel 指针
 * \endif
 */
SARibbonPanel* SARibbonCategory::insertPanel(const QString& title, int index)
{
	return (d_ptr->insertPanel(title, index));
}

/**
 * \if ENGLISH
 * @brief Insert an existing panel at the specified index position
 *
 * This function inserts an existing SARibbonPanel object at the specified index position.
 * The panel's ownership will be transferred to SARibbonCategory.
 *
 * @param panel Pointer to the SARibbonPanel to insert, if nullptr the function does nothing
 * @param index Insertion position index
 *              - If index < 0, the panel will be inserted at the beginning (index 0)
 *              - If index >= panelCount(), the panel will be appended at the end
 *
 * @note The panel's parent widget will be set to this SARibbonCategory
 * @note The panel will inherit the category's display settings (title height, layout mode, etc.)
 *
 * @see addPanel(SARibbonPanel*), takePanel(SARibbonPanel*)
 * \endif
 *
 * \if CHINESE
 * @brief 在指定索引位置插入已存在的面板
 *
 * 此函数将一个已存在的SARibbonPanel对象插入到指定的索引位置。
 * 面板的所有权将转移给SARibbonCategory管理。
 *
 * @param panel 要插入的SARibbonPanel指针，如果为nullptr则函数不执行任何操作
 * @param index 插入位置的索引
 *              - 如果index < 0，面板将被插入到开头（索引0）
 *              - 如果index >= panelCount()，面板将被追加到末尾
 *
 * @note 面板的父窗口将被设置为此SARibbonCategory
 * @note 面板将继承Category的显示设置（标题高度、布局模式等）
 *
 * @see addPanel(SARibbonPanel*), takePanel(SARibbonPanel*)
 * \endif
 */
void SARibbonCategory::insertPanel(SARibbonPanel* panel, int index)
{
	d_ptr->insertPanel(index, panel);
}

/**
 * \if ENGLISH
 * @brief Find panel by name
 * @param title Panel title to search for
 * @return If there are duplicate names, only the first one that meets the condition will be returned
 * \endif
 *
 * \if CHINESE
 * @brief 通过名字查找panel
 * @param title 要查找的panel标题
 * @return 如果有重名，只会返回第一个符合条件的
 * \endif
 */
SARibbonPanel* SARibbonCategory::panelByName(const QString& title) const
{
	if (SARibbonCategoryLayout* lay = categoryLayout()) {
		return lay->panelByName(title);
	}
	return nullptr;
}

/**
 * \if ENGLISH
 * @brief Find panel by ObjectName
 * @param objname Object name to search for
 * @return Panel with the specified object name, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 通过ObjectName查找panel
 * @param objname 要查找的对象名
 * @return 找到的panel指针，如果没找到返回nullptr
 * \endif
 */
SARibbonPanel* SARibbonCategory::panelByObjectName(const QString& objname) const
{
	if (SARibbonCategoryLayout* lay = categoryLayout()) {
		return lay->panelByObjectName(objname);
	}
	return nullptr;
}

/**
 * \if ENGLISH
 * @brief Find panel by index, returns nullptr if the index is out of range
 * @param index Panel index
 * @return Returns nullptr if the index is out of range
 * \endif
 *
 * \if CHINESE
 * @brief 通过索引找到panel，如果超过索引范围，会返回nullptr
 * @param index panel的索引
 * @return 如果超过索引范围，会返回nullptr
 * \endif
 */
SARibbonPanel* SARibbonCategory::panelByIndex(int index) const
{
	if (SARibbonCategoryLayout* lay = categoryLayout()) {
		return lay->panelByIndex(index);
	}
	return nullptr;
}

/**
 * \if ENGLISH
 * @brief Find the index corresponding to panel
 * @param p Panel to find index for
 * @return Returns -1 if not found
 * \endif
 *
 * \if CHINESE
 * @brief 查找panel对应的索引
 * @param p 要查找索引的panel
 * @return 如果找不到，返回-1
 * \endif
 */
int SARibbonCategory::panelIndex(SARibbonPanel* p) const
{
	if (SARibbonCategoryLayout* lay = categoryLayout()) {
		return lay->panelIndex(p);
	}
	return -1;
}

/**
 * \if ENGLISH
 * @brief Move a Panel from from index to to index
 * @param from Index of the panel to move
 * @param to Position to move to
 * \endif
 *
 * \if CHINESE
 * @brief 移动一个Panel从from index到to index
 * @param from 要移动panel的index
 * @param to 要移动到的位置
 * \endif
 */
void SARibbonCategory::movePanel(int from, int to)
{
	if (from == to) {
		return;
	}
	if (to < 0) {
		to = 0;
	}
	if (to >= panelCount()) {
		to = panelCount() - 1;
	}
	if (SARibbonCategoryLayout* lay = categoryLayout()) {
		lay->movePanel(from, to);
	}
}

/**
 * \if ENGLISH
 * @brief Detach panel from SARibbonCategory management
 * @param panel Panel to extract
 * @return Returns true on success, otherwise false
 * \endif
 *
 * \if CHINESE
 * @brief 把panel脱离SARibbonCategory的管理
 * @param panel 需要提取的panel
 * @return 成功返回true，否则返回false
 * \endif
 */
bool SARibbonCategory::takePanel(SARibbonPanel* panel)
{
	return (d_ptr->takePanel(panel));
}

/**
 * \if ENGLISH
 * @brief Remove Panel, Category will directly recycle SARibbonPanel memory
 * @param panel Panel to remove
 * @note After removal, panel becomes a wild pointer. It is generally recommended to set the panel pointer to nullptr after the operation
 *
 * This operation is equivalent to:
 *
 * @code
 * SARibbonPanel* panel;
 * ...
 * category->takePanel(panel);
 * panel->hide();
 * panel->deleteLater();
 * @endcode
 * \endif
 *
 * \if CHINESE
 * @brief 移除Panel，Category会直接回收SARibbonPanel内存
 * @param panel 需要移除的panel
 * @note 移除后panel为野指针，一般操作完建议把panel指针设置为nullptr
 *
 * 此操作等同于：
 *
 * @code
 * SARibbonPanel* panel;
 * ...
 * category->takePanel(panel);
 * panel->hide();
 * panel->deleteLater();
 * @endcode
 * \endif
 */
bool SARibbonCategory::removePanel(SARibbonPanel* panel)
{
	return (d_ptr->removePanel(panel));
}

/**
 * \if ENGLISH
 * @brief Remove panel
 * @param index Index of panel, returns false if it exceeds the limit
 * @return Returns true on success
 * \endif
 *
 * \if CHINESE
 * @brief 移除panel
 * @param index panel的索引，如果超出会返回false
 * @return 成功返回true
 * \endif
 */
bool SARibbonCategory::removePanel(int index)
{
	SARibbonPanel* p = panelByIndex(index);

	if (nullptr == p) {
		return (false);
	}
	return (removePanel(p));
}

/**
 * \if ENGLISH
 * @brief Return all panels under Category
 * @return List of all panels in the category
 * \endif
 *
 * \if CHINESE
 * @brief 返回Category下的所有panel
 * @return 分类中所有panel的列表
 * \endif
 */
QList< SARibbonPanel* > SARibbonCategory::panelList() const
{
	return (d_ptr->panelList());
}

QSize SARibbonCategory::sizeHint() const
{
	if (SARibbonCategoryLayout* lay = categoryLayout()) {
		return lay->sizeHint();
	}
	return QSize(1000, 100);
}

/**
 * \if ENGLISH
 * @brief Returns true if it is a ContextCategory
 * @return True if this is a context category, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 如果是ContextCategory，此函数返回true
 * @return 如果是上下文分类返回true，否则返回false
 * \endif
 */
bool SARibbonCategory::isContextCategory() const
{
	return (d_ptr->isContextCategory);
}

/**
 * \if ENGLISH
 * @brief Return the number of panels
 * @return Number of panels in the category
 * \endif
 *
 * \if CHINESE
 * @brief 返回panel的个数
 * @return 分类中panel的数量
 * \endif
 */
int SARibbonCategory::panelCount() const
{
	if (SARibbonCategoryLayout* lay = categoryLayout()) {
		return lay->panelCount();
	}
	return -1;
}

/**
 * \if ENGLISH
 * @brief Determine if customization is allowed
 * @return True if customization is allowed, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 判断是否可以自定义
 * @return 如果允许自定义返回true，否则返回false
 * \endif
 */
bool SARibbonCategory::isCanCustomize() const
{
	return (d_ptr->isCanCustomize);
}

/**
 * \if ENGLISH
 * @brief Set whether customization is allowed
 * @param b True to allow customization, false to disallow
 * \endif
 *
 * \if CHINESE
 * @brief 设置是否可以自定义
 * @param b 为true允许自定义，为false禁止自定义
 * \endif
 */
void SARibbonCategory::setCanCustomize(bool b)
{
	d_ptr->isCanCustomize = b;
}

/**
 * \if ENGLISH
 * @brief Height of panel title bar
 * @return Current height of panel title bars
 * \endif
 *
 * \if CHINESE
 * @brief panel标题栏的高度
 * @return 当前panel标题栏的高度
 * \endif
 */
int SARibbonCategory::panelTitleHeight() const
{
	return d_ptr->panelTitleHeight;
}

/**
 * \if ENGLISH
 * @brief Set the height of panel
 * @param h New height for panel title bars
 * \endif
 *
 * \if CHINESE
 * @brief 设置panel的高度
 * @param h panel标题栏的新高度
 * \endif
 */
void SARibbonCategory::setPanelTitleHeight(int h)
{
	d_ptr->panelTitleHeight = h;
	iteratePanel([ h ](SARibbonPanel* p) -> bool {
		p->setTitleHeight(h);
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Whether the panel title bar is displayed
 * @return True if panel title bars are displayed, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 是否panel显示标题栏
 * @return 如果显示panel标题栏返回true，否则返回false
 * \endif
 */
bool SARibbonCategory::isEnableShowPanelTitle() const
{
	return d_ptr->enableShowPanelTitle;
}

/**
 * \if ENGLISH
 * @brief Set to display panel title
 * @param on True to display panel title bars, false to hide them
 * \endif
 *
 * \if CHINESE
 * @brief 设置显示panel标题
 * @param on 为true显示panel标题栏，为false隐藏它们
 * \endif
 */
void SARibbonCategory::setEnableShowPanelTitle(bool on)
{
	d_ptr->enableShowPanelTitle = on;
	iteratePanel([ on ](SARibbonPanel* p) -> bool {
		p->setEnableShowTitle(on);
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Get the corresponding ribbonbar, returns null if not managed by ribbonbar
 * @return Returns null if not managed by ribbonbar
 * \endif
 *
 * \if CHINESE
 * @brief 获取对应的ribbonbar，如果没有加入ribbonbar的管理，此值为null
 * @return 如果没有加入ribbonbar的管理，此值为null
 * \endif
 */
SARibbonBar* SARibbonCategory::ribbonBar() const
{
	// 第一个par是stackwidget
	if (QWidget* par = parentWidget()) {
		// 理论此时是ribbonbar
		par = par->parentWidget();
		while (par) {
			if (SARibbonBar* ribbon = qobject_cast< SARibbonBar* >(par)) {
				return ribbon;
			}
			par = par->parentWidget();
		}
	}
	return nullptr;
}

/**
 * \if ENGLISH
 * @brief Refresh the category layout, suitable for calling after changing the ribbon mode
 * \endif
 *
 * \if CHINESE
 * @brief 刷新category的布局，适用于改变ribbon的模式之后调用
 * \endif
 */
void SARibbonCategory::updateItemGeometry()
{
#if SARIBBONCATEGORY_DEBUG_PRINT
	qDebug() << "SARibbonCategory name=" << categoryName() << " updateItemGeometry";
#endif
	d_ptr->updateItemGeometry();
}

/**
 * \if ENGLISH
 * @brief Set whether to use animation when scrolling
 * @param useAnimating True to use animation, false to scroll directly
 * \endif
 *
 * \if CHINESE
 * @brief 设置滚动时是否使用动画
 * @param useAnimating 为true使用动画，为false直接滚动
 * \endif
 */
void SARibbonCategory::setUseAnimatingScroll(bool useAnimating)
{
	d_ptr->isUseAnimating = useAnimating;
}

/**
 * \if ENGLISH
 * @brief Whether to use animation when scrolling
 * @return True if animation is used, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 滚动时是否使用动画
 * @return 如果使用动画返回true，否则返回false
 * \endif
 */
bool SARibbonCategory::isUseAnimatingScroll() const
{
	return d_ptr->isUseAnimating;
}

/**
 * \if ENGLISH
 * @brief Set wheel scroll step (px)
 * @param step Scroll step in pixels
 * \endif
 *
 * \if CHINESE
 * @brief 设置滚轮滚动步长（px）
 * @param step 以像素为单位的滚动步长
 * \endif
 */
void SARibbonCategory::setWheelScrollStep(int step)
{
	d_ptr->wheelScrollStep = qMax(10, step);  // 最小10像素
}

/**
 * \if ENGLISH
 * @brief Wheel scroll step
 * @return Current scroll step in pixels
 * \endif
 *
 * \if CHINESE
 * @brief 滚轮的滚动步长
 * @return 当前以像素为单位的滚动步长
 * \endif
 */
int SARibbonCategory::wheelScrollStep() const
{
	return d_ptr->wheelScrollStep;
}

/**
 * \if ENGLISH
 * @brief Set animation duration
 * @param duration Animation duration in milliseconds
 * \endif
 *
 * \if CHINESE
 * @brief 设置动画持续时间
 * @param duration 以毫秒为单位的动画持续时间
 * \endif
 */
void SARibbonCategory::setAnimationDuration(int duration)
{
	if (SARibbonCategoryLayout* lay = categoryLayout()) {
		lay->setAnimationDuration(duration);
	}
}

/**
 * \if ENGLISH
 * @brief Animation duration
 * @return Current animation duration in milliseconds, or -1 if not available
 * \endif
 *
 * \if CHINESE
 * @brief 动画持续时间
 * @return 当前以毫秒为单位的动画持续时间，如果不可用则返回-1
 * \endif
 */
int SARibbonCategory::animationDuration() const
{
	if (SARibbonCategoryLayout* lay = categoryLayout()) {
		return lay->animationDuration();
	}
	return -1;
}

/**
 * \if ENGLISH
 * @brief Determine whether the text of panel is allowed to wrap
 * @return True if text wrapping is enabled, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 判断panel的文字是否允许换行
 * @return 如果启用文字换行返回true，否则返回false
 * \endif
 */
bool SARibbonCategory::isEnableWordWrap() const
{
	return d_ptr->enableWordWrap;
}

/**
 * \if ENGLISH
 * @brief Set whether the text of panel buttons is allowed to wrap
 * @param on True to enable text wrapping, false to disable
 * \endif
 *
 * \if CHINESE
 * @brief 设置 panel 的按钮文字允许换行
 * @param on 为 true 启用文字换行，为 false 禁用
 * \endif
 */
void SARibbonCategory::setEnableWordWrap(bool on)
{
	d_ptr->enableWordWrap = on;
	iteratePanel([ on ](SARibbonPanel* panel) -> bool {
		if (panel) {
			panel->setEnableWordWrap(on);
		}
		return true;
	});
	updateGeometry();
}

/**
 * \if ENGLISH
 * @brief Set whether button text is displayed to the right of the icon
 * @param on If true, all toolbuttons in panels will show text to the right of icon
 * @details This cascades to all panels in this category
 * \endif
 *
 * \if CHINESE
 * @brief 设置按钮文字是否显示在图标右侧
 * @param on 如果为 true，所有 panel 中的 toolbutton 将显示图标在左、文字在右
 * @details 此属性会级联传递到所有 panel
 * \endif
 */
void SARibbonCategory::setEnableIconRightText(bool on)
{
	SA_D(d);
	if (d->enableIconRightText == on) {
		return;
	}
	d->enableIconRightText = on;
	iteratePanel([on](SARibbonPanel* panel) {
		panel->setEnableIconRightText(on);
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Check if icon-right-text mode is enabled
 * @return true if icon-right-text mode is enabled
 * \endif
 *
 * \if CHINESE
 * @brief 检查图标右侧文字模式是否启用
 * @return 如果启用图标右侧文字模式返回 true
 * \endif
 */
bool SARibbonCategory::isEnableIconRightText() const
{
	SA_DC(d);
return d->enableIconRightText;
}

/**
 * \if ENGLISH
 * @brief Set button maximum aspect ratio, this coefficient determines the maximum width of the button
 *
 * The maximum width of the button is button height * this coefficient. For example, if the button height is h,
 * then the maximum button width maxw = h * buttonMaximumAspectRatio
 * If the text cannot be fully displayed at this width, the button will not continue to expand horizontally,
 * and ... will be used instead of the incompletely displayed text
 *
 * @see buttonMaximumAspectRatio
 *
 * @note Users should not call @ref SARibbonCategory::setButtonMaximumAspectRatio to set,
 * but call @ref SARibbonBar::setButtonMaximumAspectRatio to set the aspect ratio
 *
 * @param fac New maximum aspect ratio
 * \endif
 *
 * \if CHINESE
 * @brief 设置按钮最大宽高比，这个系数决定按钮的最大宽度
 *
 * 按钮的最大宽度为按钮高度*此系数，例如按钮高度为h，那么按钮最大宽度maxw=h*buttonMaximumAspectRatio
 * 如果在此宽度下文字还无法完全显示，那么按钮将不会继续横向扩展，将使用...替代未完全显示的文字
 *
 * @see buttonMaximumAspectRatio
 *
 * @note 用户不应该调用@ref SARibbonCategory::setButtonMaximumAspectRatio 来设置，
 * 而是调用@ref SARibbonBar::setButtonMaximumAspectRatio 设置宽高比
 *
 * @param fac 新的最大宽高比
 * \endif
 */
void SARibbonCategory::setButtonMaximumAspectRatio(qreal fac)
{
	d_ptr->buttonMaximumAspectRatio = fac;
	iteratePanel([ fac ](SARibbonPanel* panel) -> bool {
		if (panel) {
			panel->setButtonMaximumAspectRatio(fac);
		}
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Button maximum aspect ratio, this coefficient determines the maximum width of the button
 * @return Current button maximum aspect ratio
 * @see setButtonMaximumAspectRatio
 * \endif
 *
 * \if CHINESE
 * @brief 按钮最大宽高比，这个系数决定按钮的最大宽度
 * @return 当前按钮最大宽高比
 * @see setButtonMaximumAspectRatio
 * \endif
 */
qreal SARibbonCategory::buttonMaximumAspectRatio() const
{
	return d_ptr->buttonMaximumAspectRatio;
}

/**
 * \if ENGLISH
 * @brief This function will iterate through all panels under Category and execute the function pointer
 * @param fp Function pointer returns false to stop iteration
 * @return Returns false indicating that all categories have not been iterated. The iteration was interrupted by
 * receiving a false return from the callback function
 * \endif
 *
 * \if CHINESE
 * @brief 此函数会遍历Category下的所有panel,执行函数指针
 * @param fp 函数指针返回false则停止迭代
 * @return 返回false代表没有迭代完所有的category，中途接收到回调函数的false返回而中断迭代
 * \endif
 */
bool SARibbonCategory::iteratePanel(FpPanelIterate fp) const
{
	const QList< SARibbonPanel* > ps = panelList();
	for (SARibbonPanel* p : ps) {
		if (!fp(p)) {
			return false;
		}
	}
	return true;
}

/**
 * \if ENGLISH
 * @brief Set the alignment of Category
 * @param al Alignment for the category
 * \endif
 *
 * \if CHINESE
 * @brief 设置Category的对齐方式
 * @param al 分类的对齐方式
 * \endif
 */
void SARibbonCategory::setCategoryAlignment(SARibbonAlignment al)
{
	SARibbonCategoryLayout* lay = qobject_cast< SARibbonCategoryLayout* >(layout());
	if (lay) {
		lay->setCategoryAlignment(al);
	}
}

/**
 * \if ENGLISH
 * @brief Category alignment
 * @return Current alignment of the category
 * \endif
 *
 * \if CHINESE
 * @brief Category的对齐方式
 * @return 分类的当前对齐方式
 * \endif
 */
SARibbonAlignment SARibbonCategory::categoryAlignment() const
{
	SARibbonCategoryLayout* lay = qobject_cast< SARibbonCategoryLayout* >(layout());
	if (lay) {
		return lay->categoryAlignment();
	}
	return SARibbonAlignment::AlignLeft;
}

/**
 * \if ENGLISH
 * @brief Set the spacing of panel
 * @param n Spacing between panels
 * \endif
 *
 * \if CHINESE
 * @brief 设置panel的spacing
 * @param n panel之间的间距
 * \endif
 */
void SARibbonCategory::setPanelSpacing(int n)
{
	d_ptr->panelSpacing = n;
	iteratePanel([ n ](SARibbonPanel* panel) -> bool {
		if (panel) {
			panel->setSpacing(n);
		}
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief panel spacing
 * @return Current spacing between panels
 * \endif
 *
 * \if CHINESE
 * @brief panel的spacing
 * @return 当前panel之间的间距
 * \endif
 */
int SARibbonCategory::panelSpacing() const
{
	return d_ptr->panelSpacing;
}

/**
 * \if ENGLISH
 * @brief Set the large button icon size of panel
 * @param largeSize New large icon size for panel buttons
 * \endif
 *
 * \if CHINESE
 * @brief 设置panel的大按钮图标尺寸
 * @param largeSize 新的panel大按钮图标尺寸
 * \endif
 */
void SARibbonCategory::setPanelLargeIconSize(const QSize& largeSize)
{
	d_ptr->panelLargeToolButtonIconSize = largeSize;
	iteratePanel([ largeSize ](SARibbonPanel* panel) -> bool {
		if (panel) {
			panel->setLargeIconSize(largeSize);
		}
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Large button icon size of panel
 * @return Current large icon size for panel buttons
 * \endif
 *
 * \if CHINESE
 * @brief panel的大按钮图标尺寸
 * @return 当前panel大按钮图标尺寸
 * \endif
 */
QSize SARibbonCategory::panelLargeIconSize() const
{
	return d_ptr->panelLargeToolButtonIconSize;
}

/**
 * \if ENGLISH
 * @brief Set the small button icon size of panel
 * @param smallSize New small icon size for panel buttons
 * \endif
 *
 * \if CHINESE
 * @brief 设置panel的小按钮图标尺寸
 * @param smallSize 新的panel小按钮图标尺寸
 * \endif
 */
void SARibbonCategory::setPanelSmallIconSize(const QSize& smallSize)
{
	d_ptr->panelSmallToolButtonIconSize = smallSize;
	iteratePanel([ smallSize ](SARibbonPanel* panel) -> bool {
		if (panel) {
			panel->setSmallIconSize(smallSize);
		}
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Small button icon size of panel
 * @return Current small icon size for panel buttons
 * \endif
 *
 * \if CHINESE
 * @brief panel的小按钮图标尺寸
 * @return 当前panel小按钮图标尺寸
 * \endif
 */
QSize SARibbonCategory::panelSmallIconSize() const
{
	return d_ptr->panelSmallToolButtonIconSize;
}

/**
 * \if ENGLISH
 * @brief Set the icon size of panel buttons, large action is not affected
 * @param smallSize New small icon size for panel buttons
 * @param largeSize New large icon size for panel buttons
 * \endif
 *
 * \if CHINESE
 * @brief 设置panel按钮的icon尺寸，large action不受此尺寸影响
 * @param smallSize panel按钮的新小图标大小
 * @param largeSize panel按钮的新大图标大小
 * \endif
 */
void SARibbonCategory::setPanelToolButtonIconSize(const QSize& smallSize, const QSize& largeSize)
{
	d_ptr->panelSmallToolButtonIconSize = smallSize;
	d_ptr->panelLargeToolButtonIconSize = largeSize;
	iteratePanel([ smallSize, largeSize ](SARibbonPanel* panel) -> bool {
		if (panel) {
			panel->setToolButtonIconSize(smallSize, largeSize);
		}
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Icon size of panel buttons, large action is not affected
 * @return Current icon size for panel buttons (small size, large size)
 * \endif
 *
 * \if CHINESE
 * @brief panel按钮的icon尺寸，large action不受此尺寸影响
 * @return 当前panel按钮的图标大小（小图标尺寸，大图标尺寸）
 * \endif
 */
QPair< QSize, QSize > SARibbonCategory::panelToolButtonIconSize() const
{
	return qMakePair(d_ptr->panelSmallToolButtonIconSize, d_ptr->panelLargeToolButtonIconSize);
}

/**
 * \if ENGLISH
 * @brief Wheel event
 *
 * When content exceeds the width of the category, the wheel can scroll the panel
 * @param event Wheel event object
 * \endif
 *
 * \if CHINESE
 * @brief 滚动事件
 *
 * 在内容超出category的宽度情况下，滚轮可滚动panel
 * @param event 滚轮事件对象
 * \endif
 */
void SARibbonCategory::wheelEvent(QWheelEvent* event)
{
	d_ptr->doWheelEvent(event);
	QWidget::wheelEvent(event);
}

/**
 * \if ENGLISH
 * @brief Change event handler
 * @param event Change event object
 * \endif
 *
 * \if CHINESE
 * @brief 变更事件处理函数
 * @param event 变更事件对象
 * \endif
 */
void SARibbonCategory::changeEvent(QEvent* event)
{
	switch (event->type()) {
	case QEvent::StyleChange: {
		if (layout()) {
#if SARIBBONCATEGORY_DEBUG_PRINT
			qDebug() << "SARibbonCategory changeEvent(StyleChange),categoryName=" << categoryName();
#endif
			layout()->invalidate();
		}
	} break;
	case QEvent::LayoutDirectionChange: {
		// 布局方向改变（如 LTR→RTL），失效布局并触发重绘
		if (layout()) {
			layout()->invalidate();
		}
		update();
	} break;
	case QEvent::FontChange: {
#if SARIBBONCATEGORY_DEBUG_PRINT
		qDebug() << "SARibbonCategory changeEvent(FontChange),categoryName=" << categoryName();
#endif
		QFont f = font();
		iteratePanel([ f ](SARibbonPanel* p) -> bool {
			p->setFont(f);
			return true;
		});
		if (layout()) {
			layout()->invalidate();
		}
	} break;
	default:
		break;
	};
	return QWidget::changeEvent(event);
}

/**
 * \if ENGLISH
 * @brief Mark this as a context label
 * @param isContextCategory True to mark as context category, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 标记这个是上下文标签
 * @param isContextCategory 为true标记为上下文分类，否则为false
 * \endif
 */
void SARibbonCategory::markIsContextCategory(bool isContextCategory)
{
	d_ptr->isContextCategory = isContextCategory;
}

/**
 * \if ENGLISH
 * @brief Get SARibbonCategoryLayout
 * @return Pointer to the category layout, or nullptr if not available
 * \endif
 *
 * \if CHINESE
 * @brief 获取SARibbonCategoryLayout
 * @return 指向分类布局的指针，如果不可用则返回nullptr
 * \endif
 */
SARibbonCategoryLayout* SARibbonCategory::categoryLayout() const
{
	return qobject_cast< SARibbonCategoryLayout* >(layout());
}

/**
 * \if ENGLISH
 * @brief Event handler
 * @param e Event object
 * @return True if the event was handled, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 事件处理函数
 * @param e 事件对象
 * @return 如果事件被处理返回true，否则返回false
 * \endif
 */
bool SARibbonCategory::event(QEvent* e)
{
#if SARIBBONCATEGORY_DEBUG_PRINT
	if (e->type() != QEvent::Paint) {
		qDebug() << "SARibbonCategory event(" << e->type() << "),name=" << categoryName();
	}
#endif
	return QWidget::event(e);
}

//===================================================
// SARibbonCategoryScrollButton
//===================================================

/**
 * \if ENGLISH
 * @brief Constructor with arrow type and optional parent
 * @param arr Arrow type for the button
 * @param p Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 带箭头类型和可选父级的构造函数
 * @param arr 按钮的箭头类型
 * @param p 父级控件
 * \endif
 */
SARibbonCategoryScrollButton::SARibbonCategoryScrollButton(Qt::ArrowType arr, QWidget* p) : QToolButton(p)
{
	setArrowType(arr);
}

/**
 * \if ENGLISH
 * @brief Destructor
 * \endif
 *
 * \if CHINESE
 * @brief 析构函数
 * \endif
 */
SARibbonCategoryScrollButton::~SARibbonCategoryScrollButton()
{
}

/*** End of inlined file: SARibbonCategory.cpp ***/


/*** Start of inlined file: SARibbonCategoryLayout.cpp ***/
#include <QLayoutItem>

#include <QApplication>
#include <QPropertyAnimation>

#ifndef SARibbonCategoryLayout_DEBUG_PRINT
#define SARibbonCategoryLayout_DEBUG_PRINT 0
#endif
#if SARibbonCategoryLayout_DEBUG_PRINT
#include <QDebug>
#endif
/**
 * \if ENGLISH
 * @brief Private data class for SARibbonCategoryLayout
 * @details This class holds private data for SARibbonCategoryLayout to implement the PIMPL idiom.
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonCategoryLayout 的私有数据类
 * @details 此类持有 SARibbonCategoryLayout 的私有数据，实现 PIMPL 设计模式。
 * \endif
 */
class SARibbonCategoryLayout::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonCategoryLayout)
public:
	PrivateData(SARibbonCategoryLayout* p);

	/**
	 * \if ENGLISH
	 * @brief Calculate total width of all elements' size hints
	 * @return Total width of all elements
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 计算所有元素的sizehint总宽度
	 * @return 所有元素的总宽度
	 * \endif
	 */
	int totalSizeHintWidth() const;

public:
	bool mDirty { true };
	bool mIsRightScrollBtnShow { false };                       ///< 标记右滚动按钮是否需要显示
	bool mIsLeftScrollBtnShow { false };                        ///< 标记左滚动按钮是否需要显示
	SARibbonCategoryScrollButton* mLeftScrollBtn { nullptr };   ///< 在区域无法显示时显示的按钮
	SARibbonCategoryScrollButton* mRightScrollBtn { nullptr };  ///< 在区域无法显示时显示的按钮
	int mTotalWidth { 0 };
	int mXBase { 0 };
	QSize mSizeHint;
	QSize mMinSizeHint;
	QList< SARibbonCategoryLayoutItem* > mItemList;
	SARibbonAlignment mCategoryAlignment { SARibbonAlignment::AlignLeft };  ///< 对齐方式
	// 动画相关
	QPropertyAnimation* mScrollAnimation { nullptr };
	int mTargetScrollPosition { 0 };
};

//=============================================================
// SARibbonCategoryLayoutPrivate
//=============================================================

SARibbonCategoryLayout::PrivateData::PrivateData(SARibbonCategoryLayout* p) : q_ptr(p)
{
}

/**
 * @brief 计算所有元素的SizeHint宽度总和
 * @return
 */
int SARibbonCategoryLayout::PrivateData::totalSizeHintWidth() const
{
	int total    = 0;
	QMargins mag = q_ptr->contentsMargins();
#if SARibbonCategoryLayout_DEBUG_PRINT
	int debug_i__ = 0;
	QString debug_totalSizeHintWidth__;
#endif
	if (!mag.isNull()) {
		total += (mag.left() + mag.right());
	}
	// 先计算总长
	for (SARibbonCategoryLayoutItem* item : sa_as_const(mItemList)) {
		if (item->isEmpty()) {
// 如果是hide就直接跳过
#if SARibbonCategoryLayout_DEBUG_PRINT
			++debug_i__;
			debug_totalSizeHintWidth__ +=
				QString("   [%1](%2)is empty skip\n").arg(debug_i__).arg(item->toPanelWidget()->panelName());
#endif
			continue;
		}
		// 这里要使用widget()->sizeHint()，因为panel的标题会影总体布局，此处需要修改
		//  TODO
		QSize panelSize = item->widget()->sizeHint();
		QSize SeparatorSize(0, 0);
		if (item->separatorWidget) {
			SeparatorSize = item->separatorWidget->sizeHint();
		}
		total += panelSize.width();
		total += SeparatorSize.width();
#if SARibbonCategoryLayout_DEBUG_PRINT
		++debug_i__;
		debug_totalSizeHintWidth__ += QString("|-[%1]panelSize=(%2,%3),SeparatorSize=(%4,%5),name=(%6) \n")
										  .arg(debug_i__)
										  .arg(panelSize.width())
										  .arg(panelSize.height())
										  .arg(SeparatorSize.width())
										  .arg(SeparatorSize.height())
										  .arg(item->toPanelWidget()->panelName());
#endif
	}
#if SARibbonCategoryLayout_DEBUG_PRINT
	qDebug() << "SARibbonCategoryLayout.totalSizeHintWidth=" << total;
	qDebug().noquote() << debug_totalSizeHintWidth__;
#endif
	return (total);
}

//=============================================================
// SARibbonCategoryLayout
//=============================================================

SARibbonCategoryLayout::SARibbonCategoryLayout(SARibbonCategory* parent)
	: QLayout(parent), d_ptr(new SARibbonCategoryLayout::PrivateData(this))
{
	setContentsMargins(1, 1, 1, 1);
	d_ptr->mLeftScrollBtn  = new SARibbonCategoryScrollButton(Qt::LeftArrow, parent);
	d_ptr->mRightScrollBtn = new SARibbonCategoryScrollButton(Qt::RightArrow, parent);
	d_ptr->mLeftScrollBtn->setVisible(false);
	d_ptr->mRightScrollBtn->setVisible(false);
	connect(d_ptr->mLeftScrollBtn, &QToolButton::clicked, this, &SARibbonCategoryLayout::onLeftScrollButtonClicked);
	connect(d_ptr->mRightScrollBtn, &QToolButton::clicked, this, &SARibbonCategoryLayout::onRightScrollButtonClicked);
	setupAnimateScroll();
}

SARibbonCategoryLayout::~SARibbonCategoryLayout()
{
	while (auto item = takePanelItem(0)) {
		delete item;
	}
}

SARibbonCategory* SARibbonCategoryLayout::ribbonCategory() const
{
	return (qobject_cast< SARibbonCategory* >(parentWidget()));
}

void SARibbonCategoryLayout::addItem(QLayoutItem* item)
{
	Q_UNUSED(item);
	qWarning() << tr("in SARibbonCategoryLayout cannot addItem,use addPanel instead");
	invalidate();
}

/**
 * \if ENGLISH
 * @brief Return the layout item for the panel at the specified index
 * @param index Index of the panel
 * @return Layout item for the panel, or nullptr if index is out of range
 * @note Note that panels are paired with separators, but this function only returns the layout for the panel itself
 * \endif
 *
 * \if CHINESE
 * @brief 返回panel的layout
 * @param index 索引
 * @return panel对应的layout，若索引超出范围则返回nullptr
 * @note 注意，panel是和分割线一起的，但这个只返回一个panel对应的layout
 * \endif
 */
QLayoutItem* SARibbonCategoryLayout::itemAt(int index) const
{
	SARibbonCategoryLayoutItem* item = d_ptr->mItemList.value(index, nullptr);

	return (item);
}

/**
 * \if ENGLISH
 * @brief Take the layout item at the specified index
 * @param index Index of the layout item to take
 * @return The taken layout item, or nullptr if index is out of range
 * \endif
 *
 * \if CHINESE
 * @brief 提取layout
 * @param index 要提取的layout索引
 * @return 提取的layout项，若索引超出范围则返回nullptr
 * \endif
 */
QLayoutItem* SARibbonCategoryLayout::takeAt(int index)
{
	QLayoutItem* r = takePanelItem(index);
	invalidate();
	return r;
}

SARibbonCategoryLayoutItem* SARibbonCategoryLayout::takePanelItem(int index)
{
	if ((index >= 0) && (index < d_ptr->mItemList.size())) {
		SARibbonCategoryLayoutItem* item = d_ptr->mItemList.takeAt(index);
		if (item->widget()) {
			item->widget()->hide();
		}
		if (item->separatorWidget) {
			item->separatorWidget->hide();
		}
		return (item);
	}
	return (nullptr);
}

SARibbonCategoryLayoutItem* SARibbonCategoryLayout::takePanelItem(SARibbonPanel* panel)
{
	for (int i = 0; i < d_ptr->mItemList.size(); ++i) {
		SARibbonCategoryLayoutItem* item = d_ptr->mItemList[ i ];
		if (item->widget() == panel) {
			return (takePanelItem(i));
		}
	}
	return (nullptr);
}

/**
 * \if ENGLISH
 * @brief Remove a panel and its corresponding separator
 * @param panel The panel to remove
 * @return True if the panel was successfully removed, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 移除panel，对应的分割线也会删除
 * @param panel 要移除的panel
 * @return 成功移除返回true，否则返回false
 * \endif
 */
bool SARibbonCategoryLayout::takePanel(SARibbonPanel* panel)
{
	SARibbonCategoryLayoutItem* i = takePanelItem(panel);
	if (i) {
		SARibbonSeparatorWidget* sp = i->separatorWidget;
		if (sp) {
			sp->deleteLater();
		}
		delete i;
		invalidate();
		return true;
	}
	return false;
}

int SARibbonCategoryLayout::count() const
{
	return (d_ptr->mItemList.size());
}

void SARibbonCategoryLayout::addPanel(SARibbonPanel* panel)
{
	insertPanel(d_ptr->mItemList.count(), panel);
}

/**
 * \if ENGLISH
 * @brief Insert a panel at the specified index
 * @param index Index where the panel should be inserted
 * @param panel The panel to insert
 * @note In SARibbonCategoryLayout, each panel is paired with a separator
 * \endif
 *
 * \if CHINESE
 * @brief 插入一个panel
 * @param index 索引
 * @param panel 要插入的panel
 * @note 在SARibbonCategoryLayout的布局中，一个panel会携带一个分割线
 * \endif
 */
void SARibbonCategoryLayout::insertPanel(int index, SARibbonPanel* panel)
{
	index                            = qMax(0, index);
	index                            = qMin(d_ptr->mItemList.count(), index);
	SARibbonCategoryLayoutItem* item = new SARibbonCategoryLayoutItem(panel);

	// 分割线
	item->separatorWidget = RibbonSubElementFactory->createRibbonSeparatorWidget(parentWidget());
	// 插入list中
	d_ptr->mItemList.insert(index, item);
	// 标记需要重新计算尺寸
	invalidate();
}

QSize SARibbonCategoryLayout::sizeHint() const
{
	if (mCachedSizeHint.isNull()) {
		const_cast< SARibbonCategoryLayout* >(this)->updateGeometryArr();
	}
	return mCachedSizeHint;
}

QSize SARibbonCategoryLayout::minimumSize() const
{
	if (mCachedMinSizeHint.isNull()) {
		const_cast< SARibbonCategoryLayout* >(this)->updateGeometryArr();
	}
	return mCachedMinSizeHint;
}

/**
 * \if ENGLISH
 * @brief SARibbonCategory fills the entire stacked widget
 * @return Expanding directions (horizontal and vertical)
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonCategory充满整个stacked widget
 * @return 扩展方向（水平和垂直）
 * \endif
 */
Qt::Orientations SARibbonCategoryLayout::expandingDirections() const
{
	return (Qt::Horizontal | Qt::Vertical);
}

void SARibbonCategoryLayout::invalidate()
{
	mCachedSizeHint    = QSize();
	mCachedMinSizeHint = QSize();
	d_ptr->mDirty      = true;
	QLayout::invalidate();
}
/**
 * \if ENGLISH
 * @brief Get the content size of the category (margins subtracted)
 * @return Content size of the category
 * \endif
 *
 * \if CHINESE
 * @brief category的内容尺寸（把margins减去）
 * @return category的内容尺寸
 * \endif
 */
QSize SARibbonCategoryLayout::categoryContentSize() const
{
	SARibbonCategory* category = ribbonCategory();
	QSize s                    = category->size();
	QMargins mag               = contentsMargins();
	if (!mag.isNull()) {
		s.rheight() -= (mag.top() + mag.bottom());
		s.rwidth() -= (mag.right() + mag.left());
	}
	return (s);
}

/**
 * \if ENGLISH
 * @brief Update geometry of the layout
 * @details This function calculates the layout geometry, including panel positions, sizes, and scroll button visibility
 * \endif
 *
 * \if CHINESE
 * @brief 更新尺寸
 * @details 此函数计算布局几何，包括面板位置、大小和滚动按钮可见性
 * \endif
 */
void SARibbonCategoryLayout::updateGeometryArr()
{
	SARibbonCategory* category = ribbonCategory();
	if (nullptr == category) {
		return;
	}
	int categoryWidth = category->width();
	QMargins mag      = contentsMargins();
	int height        = category->height();
	int y             = 0;

	if (!mag.isNull()) {
		y = mag.top();
		height -= (mag.top() + mag.bottom());
		// categoryWidth不能把mag减去，减去后会导致categoryWidth不是实际的categoryWidth
		// categoryWidth -= (mag.right() + mag.left());
	}
	// total 是总宽，不是x坐标系，x才是坐标系
	int total = d_ptr->totalSizeHintWidth();

	// 扩展的宽度
	int expandWidth = 0;

	// 判断是否需要滚动，总长度超过宽度就需要滚动
	bool needsScrolling = (total > categoryWidth);

#if SARibbonCategoryLayout_DEBUG_PRINT
	qDebug() << "SARibbonCategoryLayout::updateGeometryArr" << "\n  |-category name=" << category->categoryName()  //
			 << "\n  |-category height=" << height                                                                 //
			 << "\n  |-totalSizeHintWidth=" << total                                                               //
			 << "\n  |-y=" << y                                                                                    //
			 << "\n  |-expandWidth:" << expandWidth                                                                //
			 << "\n  |-mag=" << mag;
#endif

	if (needsScrolling) {
		if (SA::saIsRTL()) {
			// RTL: mXBase ranges from 0 (start, rightmost) to total-categoryWidth (end, leftmost)
			const int maxBase = total - categoryWidth;
			if (0 == d_ptr->mXBase) {
				// At start (rightmost), can scroll left only
				d_ptr->mIsRightScrollBtnShow = false;
				d_ptr->mIsLeftScrollBtnShow  = true;
			} else if (d_ptr->mXBase >= maxBase) {
				// At end (leftmost), can scroll right only
				d_ptr->mIsRightScrollBtnShow = true;
				d_ptr->mIsLeftScrollBtnShow  = false;
			} else {
				// In between: both buttons
				d_ptr->mIsRightScrollBtnShow = true;
				d_ptr->mIsLeftScrollBtnShow  = true;
			}
		} else {
			// LTR: mXBase ranges from categoryWidth-total (negative, end, rightmost) to 0 (start, leftmost)
			if (0 == d_ptr->mXBase) {
				// Already moved to leftmost, can scroll right
				d_ptr->mIsRightScrollBtnShow = true;
				d_ptr->mIsLeftScrollBtnShow  = false;
			} else if (d_ptr->mXBase <= (categoryWidth - total)) {
				// Already moved to rightmost, can scroll left
				d_ptr->mIsRightScrollBtnShow = false;
				d_ptr->mIsLeftScrollBtnShow  = true;
			} else {
				// In between: both buttons
				d_ptr->mIsRightScrollBtnShow = true;
				d_ptr->mIsLeftScrollBtnShow  = true;
			}
		}
	} else {
		// 说明total 小于 categoryWidth
		// 记录可以扩展的数量
		int canExpandingCount        = 0;
		d_ptr->mIsRightScrollBtnShow = false;
		d_ptr->mIsLeftScrollBtnShow  = false;
		// 这个是避免一开始totalWidth > categorySize.width()，通过滚动按钮调整了m_d->mBaseX
		// 随之调整了窗体尺寸，调整后totalWidth < categorySize.width()导致category在原来位置
		// 无法显示，必须这里把mBaseX设置为0

		d_ptr->mXBase = 0;
		//

		for (SARibbonCategoryLayoutItem* item : sa_as_const(d_ptr->mItemList)) {
			if (SARibbonPanel* p = qobject_cast< SARibbonPanel* >(item->widget())) {
				if (p->isExpanding()) {
					// panel可扩展
					++canExpandingCount;
				}
			}
		}
		// 计算可扩展的宽度
		if (canExpandingCount > 0) {
			expandWidth = (categoryWidth - total) / canExpandingCount;
		} else {
			expandWidth = 0;
		}
	}
	int x = d_ptr->mXBase;
	if (!needsScrolling && (0 == expandWidth)) {
		// Alignment offset when no scrolling needed and no expanding panels
		SARibbonAlignment align = categoryAlignment();
		if (align == SARibbonAlignment::AlignCenter) {
			// Center alignment: panels centered within category width
			x = (categoryWidth - total) / 2;
		} else if (align == SARibbonAlignment::AlignRight) {
			// Right alignment: panels start from right edge
			x = categoryWidth - total;
		}
		// AlignLeft: x = d_ptr->mXBase (default, starts from left edge)
	}
	total = 0;  // total重新计算
	// 先按照sizeHint设置所有的尺寸
	for (SARibbonCategoryLayoutItem* item : sa_as_const(d_ptr->mItemList)) {
		if (item->isEmpty()) {
			// 如果是hide就直接跳过
			if (item->separatorWidget) {
				// panel hide分割线也要hide
				item->separatorWidget->hide();
			}
			item->mWillSetGeometry          = QRect(0, 0, 0, 0);
			item->mWillSetSeparatorGeometry = QRect(0, 0, 0, 0);
			continue;
		}
		SARibbonPanel* p = item->toPanelWidget();
		if (nullptr == p) {
			qDebug() << "unknow widget in SARibbonCategoryLayout";
			continue;
		}
		// p->layout()->update();
		QSize panelSize = p->sizeHint();
		QSize SeparatorSize(0, 0);
		if (item->separatorWidget) {
			SeparatorSize = item->separatorWidget->sizeHint();
		}
		if (p->isExpanding()) {
			// 可扩展，就把panel扩展到最大
			panelSize.setWidth(panelSize.width() + expandWidth);
		}
		int w = panelSize.width();

		item->mWillSetGeometry = QRect(x, y, w, height);
		x += w;
		total += w;
		w                               = SeparatorSize.width();
		item->mWillSetSeparatorGeometry = QRect(x, y, w, height);
		x += w;
		total += w;
	}
	d_ptr->mTotalWidth      = total;
	mCachedSizeHint         = QSize(d_ptr->mTotalWidth, height);
	mCachedMinSizeHint      = QSize(categoryWidth, height);

	// RTL mirroring: mirror panel and separator x-coordinates
	if (SA::saIsRTL()) {
		for (SARibbonCategoryLayoutItem* item : sa_as_const(d_ptr->mItemList)) {
			if (!item->isEmpty()) {
				// Mirror panel geometry
				QRect panelGeo     = item->mWillSetGeometry;
				int mirroredX      = SA::saMirrorX(panelGeo.x(), categoryWidth, panelGeo.width());
				item->mWillSetGeometry = QRect(mirroredX, panelGeo.y(), panelGeo.width(), panelGeo.height());

				// Mirror separator geometry
				QRect sepGeo       = item->mWillSetSeparatorGeometry;
				int mirroredSepX   = SA::saMirrorX(sepGeo.x(), categoryWidth, sepGeo.width());
				item->mWillSetSeparatorGeometry = QRect(mirroredSepX, sepGeo.y(), sepGeo.width(), sepGeo.height());
			}
		}
	}
#if SARibbonCategoryLayout_DEBUG_PRINT
	qDebug() << "  SARibbonCategoryLayout updateGeometryArr,SizeHint=" << mCachedSizeHint
			 << ",Category name=" << category->categoryName();
#endif
}

/**
 * \if ENGLISH
 * @brief Execute layout adjustment
 * @details This function applies the calculated geometry to the widgets, including panels, separators, and scroll buttons
 * \endif
 *
 * \if CHINESE
 * @brief 执行layout调整
 * @details 此函数将计算好的几何应用到 widgets，包括面板、分隔符和滚动按钮
 * \endif
 */
void SARibbonCategoryLayout::doLayout()
{
	if (d_ptr->mDirty) {
		updateGeometryArr();
	}
	if (d_ptr->mItemList.isEmpty()) {
		if (d_ptr->mLeftScrollBtn->isVisible()) {
			d_ptr->mLeftScrollBtn->hide();
		}
		if (d_ptr->mRightScrollBtn->isVisible()) {
			d_ptr->mRightScrollBtn->hide();
		}
		return;
	}
	SARibbonCategory* category = ribbonCategory();
	// Scroll button positions: in RTL, swap left/right button positions
	if (SA::saIsRTL()) {
		d_ptr->mLeftScrollBtn->setGeometry(category->width() - 12, 0, 12, category->height());
		d_ptr->mRightScrollBtn->setGeometry(0, 0, 12, category->height());
	} else {
		d_ptr->mLeftScrollBtn->setGeometry(0, 0, 12, category->height());
		d_ptr->mRightScrollBtn->setGeometry(category->width() - 12, 0, 12, category->height());
	}
	QList< QWidget* > showWidgets, hideWidgets;
#if SARibbonCategoryLayout_DEBUG_PRINT
	int debug_i__(0);
	qDebug() << "SARibbonCategoryLayout::doLayout(),name=" << category->categoryName();
#endif
	const int itemsize = d_ptr->mItemList.size();
	for (int i = 0; i < itemsize; ++i) {
		SARibbonCategoryLayoutItem* item = d_ptr->mItemList[ i ];
		if (item->isEmpty()) {
			hideWidgets << item->widget();
			if (item->separatorWidget) {
				hideWidgets << item->separatorWidget;
			}
#if SARibbonCategoryLayout_DEBUG_PRINT
			qDebug() << "  |-[" << debug_i__ << "]panelName(" << item->toPanelWidget()->panelName() << ",will hide";
			++debug_i__;
#endif
		} else {
			//! 这里不能用item->setGeometry(item->mWillSetGeometry);这样会得到一个很奇怪的显示效果
			//! 就是窗口的最左边不会移出去，而是把最右边压缩，
			item->widget()->setGeometry(item->mWillSetGeometry);
			//            item->widget()->setFixedSize(item->mWillSetGeometry.size());
			//            item->widget()->move(item->mWillSetGeometry.topLeft());
			//            item->setGeometry(item->mWillSetGeometry);
			showWidgets << item->widget();
			if (item->separatorWidget) {
				item->separatorWidget->setGeometry(item->mWillSetSeparatorGeometry);
				if (i == itemsize - 1) {
					// 最后一个panel的分割线隐藏
					hideWidgets << item->separatorWidget;
				} else {
					showWidgets << item->separatorWidget;
				}
			}
#if SARibbonCategoryLayout_DEBUG_PRINT
			qDebug() << "  |-[" << debug_i__ << "]panelName(" << item->toPanelWidget()->panelName()
					 << "),willSetGeometry:" << item->mWillSetGeometry
					 << ",WillSetSeparatorGeometry:" << item->mWillSetSeparatorGeometry;
			++debug_i__;
#endif
		}
	}

	d_ptr->mRightScrollBtn->setVisible(d_ptr->mIsRightScrollBtnShow);
	d_ptr->mLeftScrollBtn->setVisible(d_ptr->mIsLeftScrollBtnShow);
	if (d_ptr->mIsRightScrollBtnShow) {
		d_ptr->mRightScrollBtn->raise();
	}
	if (d_ptr->mIsLeftScrollBtnShow) {
		d_ptr->mLeftScrollBtn->raise();
	}
	// 不在上面那里进行show和hide因为这会触发SARibbonPanelLayout的重绘，导致循环绘制，非常影响效率
	for (QWidget* w : sa_as_const(showWidgets)) {
		if (!w->isVisible()) {
			w->show();
		}
	}
	for (QWidget* w : sa_as_const(hideWidgets)) {
		if (w->isVisible()) {
			w->hide();
		}
	}
	// 最后一个分割线隐藏
}

/**
 * \if ENGLISH
 * @brief Return all panels
 * @return List of all panels in the layout
 * \endif
 *
 * \if CHINESE
 * @brief 返回所有panels
 * @return 布局中所有panel的列表
 * \endif
 */
QList< SARibbonPanel* > SARibbonCategoryLayout::panels() const
{
	QList< SARibbonPanel* > res;

	for (SARibbonCategoryLayoutItem* item : sa_as_const(d_ptr->mItemList)) {
		SARibbonPanel* p = item->toPanelWidget();
		res.append(p);
	}
	return (res);
}

/**
 * \if ENGLISH
 * @brief Find a panel by its object name
 * @param objname Object name of the panel to find
 * @return Panel with the specified object name, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 通过ObjectName查找panel
 * @param objname 要查找的panel的objectName
 * @return 找到的panel，若未找到则返回nullptr
 * \endif
 */
SARibbonPanel* SARibbonCategoryLayout::panelByObjectName(const QString& objname) const
{
	for (SARibbonCategoryLayoutItem* item : d_ptr->mItemList) {
		if (SARibbonPanel* panel = item->toPanelWidget()) {
			if (panel->objectName() == objname) {
				return panel;
			}
		}
	}
	return nullptr;
}

/**
 * \if ENGLISH
 * @brief Find a panel by its name
 * @param panelname Name of the panel to find
 * @return First panel with the specified name, or nullptr if not found
 * @note If there are multiple panels with the same name, only the first one will be returned
 * \endif
 *
 * \if CHINESE
 * @brief 通过名字查找panel
 * @param panelname 要查找的panel的名称
 * @return 找到的panel，若未找到则返回nullptr
 * @note 如果有重名，只会返回第一个符合条件的
 * \endif
 */
SARibbonPanel* SARibbonCategoryLayout::panelByName(const QString& panelname) const
{
	for (SARibbonCategoryLayoutItem* item : sa_as_const(d_ptr->mItemList)) {
		if (SARibbonPanel* panel = item->toPanelWidget()) {
			if (panel->panelName() == panelname) {
				return (panel);
			}
		}
	}
	return (nullptr);
}

/**
 * \if ENGLISH
 * @brief Find a panel by its index
 * @param i Index of the panel to find
 * @return Panel at the specified index, or nullptr if index is out of range
 * \endif
 *
 * \if CHINESE
 * @brief 通过索引找到panel，如果超过索引范围，会返回nullptr
 * @param i 要查找的panel的索引
 * @return 找到的panel，若索引超出范围则返回nullptr
 * \endif
 */
SARibbonPanel* SARibbonCategoryLayout::panelByIndex(int i) const
{
	if (i >= 0 && i < d_ptr->mItemList.size()) {
		return d_ptr->mItemList[ i ]->toPanelWidget();
	}
	return nullptr;
}

/**
 * \if ENGLISH
 * @brief Move a panel from one position to another
 * @param from Current index of the panel
 * @param to New index of the panel
 * \endif
 *
 * \if CHINESE
 * @brief 移动panel
 * @param from panel的当前索引
 * @param to panel的新索引
 * \endif
 */
void SARibbonCategoryLayout::movePanel(int from, int to)
{
	d_ptr->mItemList.move(from, to);
	doLayout();
}

/**
 * \if ENGLISH
 * @brief Return the number of panels
 * @return Number of panels in the layout
 * \endif
 *
 * \if CHINESE
 * @brief 返回panel的个数
 * @return 布局中panel的数量
 * \endif
 */
int SARibbonCategoryLayout::panelCount() const
{
	return d_ptr->mItemList.size();
}

/**
 * \if ENGLISH
 * @brief Find the index of a panel
 * @param p Panel to find
 * @return Index of the panel, or -1 if not found
 * \endif
 *
 * \if CHINESE
 * @brief 查找panel对应的索引
 * @param p 要查找的panel
 * @return panel的索引，若未找到则返回-1
 * \endif
 */
int SARibbonCategoryLayout::panelIndex(SARibbonPanel* p) const
{
	int c = panelCount();

	for (int i = 0; i < c; ++i) {
		if (d_ptr->mItemList[ i ]->toPanelWidget() == p) {
			return (i);
		}
	}
	return (-1);
}

/**
 * \if ENGLISH
 * @brief Get all panels
 * @return List of all panels in the layout
 * \endif
 *
 * \if CHINESE
 * @brief 获取所有的panel
 * @return 布局中所有panel的列表
 * \endif
 */
QList< SARibbonPanel* > SARibbonCategoryLayout::panelList() const
{
	QList< SARibbonPanel* > res;

	for (SARibbonCategoryLayoutItem* i : sa_as_const(d_ptr->mItemList)) {
		if (SARibbonPanel* p = i->toPanelWidget()) {
			res.append(p);
		}
	}
	return (res);
}

/**
 * \if ENGLISH
 * @brief Execute scrolling
 * @param px Scroll distance in pixels
 * @details In LTR mode, positive px scrolls right (decreases mXBase), negative scrolls left. In RTL mode, positive px scrolls right (increases mXBase), negative scrolls left.
 * \endif
 *
 * \if CHINESE
 * @brief 执行滚动
 * @param px 滚动的像素距离
 * @details 在 LTR 模式下，正 px 向右滚动（减小 mXBase），负 px 向左滚动。在 RTL 模式下，正 px 向右滚动（增加 mXBase），负 px 向左滚动。
 * \endif
 */
void SARibbonCategoryLayout::scroll(int px)
{
	// Calculate new position
	int targetX = d_ptr->mXBase + px;
	// Set position directly
	scrollTo(targetX);
}

/**
 * \if ENGLISH
 * @brief Scroll to a specified position
 * @param targetX Target scroll position in pixels
 * @details Delegates to setScrollPosition() which handles RTL boundary bounds.
 * \endif
 *
 * \if CHINESE
 * @brief 滚动到指定位置
 * @param targetX 目标滚动位置（像素）
 * @details 委托给 setScrollPosition()，该函数处理 RTL 边界限制。
 * \endif
 */
void SARibbonCategoryLayout::scrollTo(int targetX)
{
	setScrollPosition(targetX);
}

/**
 * \if ENGLISH
 * @brief Animate scrolling by a specified distance
 * @param px Scroll distance in pixels
 * @details Boundary checking is handled by scrollToByAnimate(), which applies RTL-specific bounds when saIsRTL() is true.
 * \endif
 *
 * \if CHINESE
 * @brief 带动画的滚动
 * @param px 滚动的像素距离
 * @details 边界检查由 scrollToByAnimate() 处理，当 saIsRTL() 为 true 时会应用 RTL 特定边界。
 * \endif
 */
void SARibbonCategoryLayout::scrollByAnimate(int px)
{
	int targetX = d_ptr->mXBase + px;
	scrollToByAnimate(targetX);
}

/**
 * \if ENGLISH
 * @brief Animate scrolling to a specified position
 * @param targetX Target scroll position in pixels
 * @details In LTR mode, targetX is bounded to [availableWidth-totalWidth, 0]. In RTL mode, targetX is bounded to [0, totalWidth-availableWidth].
 * \endif
 *
 * \if CHINESE
 * @brief 滚动到指定位置，带动画
 * @param targetX 目标滚动位置（像素）
 * @details 在 LTR 模式下，targetX 被限制在 [availableWidth-totalWidth, 0]。在 RTL 模式下，targetX 被限制在 [0, totalWidth-availableWidth]。
 * \endif
 */
void SARibbonCategoryLayout::scrollToByAnimate(int targetX)
{
	QPropertyAnimation* animation = d_ptr->mScrollAnimation;
	if (!animation) {
		scrollTo(targetX);
	}
	if (isAnimatingScroll() && targetX == d_ptr->mTargetScrollPosition) {
		return;  // Already at target position
	}
	// Calculate boundaries
	if (SA::saIsRTL()) {
		// RTL: mXBase ranges from 0 (start) to totalWidth-availableWidth (end)
		const int availableWidth     = categoryContentSize().width();
		const int maxBase            = qMax(0, d_ptr->mTotalWidth - availableWidth);
		d_ptr->mTargetScrollPosition = qBound(0, targetX, maxBase);
	} else {
		// LTR: mXBase ranges from availableWidth-totalWidth (min) to 0 (max)
		const int availableWidth     = categoryContentSize().width();
		const int minBase            = qMin(availableWidth - d_ptr->mTotalWidth, 0);
		d_ptr->mTargetScrollPosition = qBound(minBase, targetX, 0);
	}

	// If animation is running, stop current animation
	if (animation->state() == QPropertyAnimation::Running) {
		animation->stop();
	}

	// Set animation parameters
	animation->setStartValue(d_ptr->mXBase);
	animation->setEndValue(d_ptr->mTargetScrollPosition);
	animation->start();
}

/**
 * \if ENGLISH
 * @brief Get the current scroll position
 * @return Current scroll position in pixels
 * \endif
 *
 * \if CHINESE
 * @brief 滚动后的位置
 * @return 当前滚动位置（像素）
 * \endif
 */
int SARibbonCategoryLayout::scrollPosition() const
{
	return d_ptr->mXBase;
}

/**
 * \if ENGLISH
 * @brief Set the scroll position
 * @param pos New scroll position in pixels
 * @details In LTR mode, mXBase ranges from [availableWidth-totalWidth, 0] (negative values). In RTL mode, mXBase ranges from [0, totalWidth-availableWidth] (positive values).
 * \endif
 *
 * \if CHINESE
 * @brief 设置滚动位置
 * @param pos 新的滚动位置（像素）
 * @details 在 LTR 模式下，mXBase 范围为 [availableWidth-totalWidth, 0]（负值）。在 RTL 模式下，mXBase 范围为 [0, totalWidth-availableWidth]（正值）。
 * \endif
 */
void SARibbonCategoryLayout::setScrollPosition(int pos)
{
	// Boundary check
	const int availableWidth = categoryContentSize().width();
	if (SA::saIsRTL()) {
		// RTL: mXBase ranges from 0 (start, rightmost) to totalWidth-availableWidth (end, leftmost)
		const int maxBase  = qMax(0, d_ptr->mTotalWidth - availableWidth);
		const int newXBase = qBound(0, pos, maxBase);

		if (d_ptr->mXBase != newXBase) {
			d_ptr->mXBase = newXBase;
			invalidate();  // Mark layout as dirty for deferred relayout
			// NOT REDUNDANT: invalidate() triggers deferred layout pass, update() schedules repaint
			// Both are required for smooth scrolling animation: removing update() would cause 1-frame lag
			// Using activate() instead would force synchronous layout which is more expensive during animation
			if (parentWidget()) {
				parentWidget()->update();
			}
		}
	} else {
		// LTR: mXBase ranges from availableWidth-totalWidth (negative, end) to 0 (start, leftmost)
		const int minBase  = qMin(availableWidth - d_ptr->mTotalWidth, 0);
		const int newXBase = qBound(minBase, pos, 0);

		if (d_ptr->mXBase != newXBase) {
			d_ptr->mXBase = newXBase;
			invalidate();  // Mark layout as dirty for deferred relayout
			// NOT REDUNDANT: invalidate() triggers deferred layout pass, update() schedules repaint
			// Both are required for smooth scrolling animation: removing update() would cause 1-frame lag
			// Using activate() instead would force synchronous layout which is more expensive during animation
			if (parentWidget()) {
				parentWidget()->update();
			}
		}
	}
}

/**
 * \if ENGLISH
 * @brief Check if scrolling animation is in progress
 * @return True if scrolling animation is running, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 判断是否在滚动动画中
 * @return 滚动动画正在进行返回true，否则返回false
 * \endif
 */
bool SARibbonCategoryLayout::isAnimatingScroll() const
{
	return d_ptr->mScrollAnimation->state() == QPropertyAnimation::Running;
}

/**
 * \if ENGLISH
 * @brief Check if the layout has been scrolled
 * @return True if the layout has been scrolled (mXBase != 0), false otherwise
 * @details In LTR, mXBase is non-zero (negative) when scrolled. In RTL, mXBase is non-zero (positive) when scrolled.
 * \endif
 *
 * \if CHINESE
 * @brief 判断是否滚动过
 * @return 布局已滚动返回true，否则返回false
 * @details 在 LTR 模式下，mXBase 非零（负值）表示已滚动。在 RTL 模式下，mXBase 非零（正值）表示已滚动。
 * \endif
 */
bool SARibbonCategoryLayout::isScrolled() const
{
	return (d_ptr->mXBase != 0);
}

/**
 * \if ENGLISH
 * @brief Get the total width of the content
 * @return Total width of the content, which may be greater or less than the size.width
 * \endif
 *
 * \if CHINESE
 * @brief 这个宽度是实际内容的宽度，有可能大于size.width，也有可能小于
 * @return 实际内容的总宽度
 * \endif
 */
int SARibbonCategoryLayout::categoryTotalWidth() const
{
	return d_ptr->mTotalWidth;
}

/**
 * \if ENGLISH
 * @brief Set the alignment of the category
 * @param al Alignment type
 * @note Center alignment will center the panels
 * \endif
 *
 * \if CHINESE
 * @brief 设置Category的对齐方式
 * @param al 对齐方式
 * @note 居中对齐会让panel以居中进行对齐
 * \endif
 */
void SARibbonCategoryLayout::setCategoryAlignment(SARibbonAlignment al)
{
	if (d_ptr->mCategoryAlignment != al) {
		d_ptr->mCategoryAlignment = al;
	}
}

/**
 * \if ENGLISH
 * @brief Get the alignment of the category
 * @return Current alignment of the category
 * \endif
 *
 * \if CHINESE
 * @brief Category的对齐方式
 * @return 当前Category的对齐方式
 * \endif
 */
SARibbonAlignment SARibbonCategoryLayout::categoryAlignment() const
{
	return d_ptr->mCategoryAlignment;
}

/**
 * \if ENGLISH
 * @brief Set the duration of the animation
 * @param duration Duration in milliseconds
 * @note Minimum duration is 50ms
 * \endif
 *
 * \if CHINESE
 * @brief 设置动画的持续时间
 * @param duration 毫秒
 * @note 最小50ms
 * \endif
 */
void SARibbonCategoryLayout::setAnimationDuration(int duration)
{
	if (d_ptr->mScrollAnimation) {
		d_ptr->mScrollAnimation->setDuration(qMax(50, duration));  // 最小50ms
	}
}

/**
 * \if ENGLISH
 * @brief Get the duration of the animation
 * @return Animation duration in milliseconds, or -1 if animation is not set
 * @note Default duration is 300ms
 * \endif
 *
 * \if CHINESE
 * @brief 动画的持续时间，默认300ms
 * @return 动画持续时间（毫秒），如果没有设置动画，返回-1
 * \endif
 */
int SARibbonCategoryLayout::animationDuration() const
{
	if (d_ptr->mScrollAnimation) {
		return d_ptr->mScrollAnimation->duration();
	}
	return -1;
}

void SARibbonCategoryLayout::setupAnimateScroll()
{
	if (!d_ptr->mScrollAnimation) {
		// 初始化滚动动画
		d_ptr->mScrollAnimation = new QPropertyAnimation(this, "scrollPosition", this);
		d_ptr->mScrollAnimation->setDuration(300);                       // 动画时长300ms
		d_ptr->mScrollAnimation->setEasingCurve(QEasingCurve::OutQuad);  // 缓动曲线
		d_ptr->mTargetScrollPosition = d_ptr->mXBase;
	}
}

/**
 * \if ENGLISH
 * @brief Handle left scroll button click
 * @details In LTR mode, scrolls panels to the right (reveals right-side hidden panels). In RTL mode, reverses direction to scroll left.
 * \endif
 *
 * \if CHINESE
 * @brief 处理左滚动按钮点击
 * @details 在 LTR 模式下，将面板向右滚动（显示右侧隐藏的面板）。在 RTL 模式下，反向滚动（向左）。
 * \endif
 */
void SARibbonCategoryLayout::onLeftScrollButtonClicked()
{
	SARibbonCategory* category = qobject_cast< SARibbonCategory* >(parentWidget());
	int width                  = category->width();
	width /= 2;
	// In RTL mode, reverse scroll direction: left button scrolls left instead of right
	if (SA::saIsRTL()) {
		scrollByAnimate(-width);
	} else {
		scrollByAnimate(width);
	}
}

/**
 * \if ENGLISH
 * @brief Handle right scroll button click
 * @details In LTR mode, scrolls panels to the left (reveals left-side hidden panels). In RTL mode, reverses direction to scroll right.
 * \endif
 *
 * \if CHINESE
 * @brief 处理右滚动按钮点击
 * @details 在 LTR 模式下，将面板向左滚动（显示左侧隐藏的面板）。在 RTL 模式下，反向滚动（向右）。
 * \endif
 */
void SARibbonCategoryLayout::onRightScrollButtonClicked()
{
	SARibbonCategory* category = qobject_cast< SARibbonCategory* >(parentWidget());
	int width                  = category->width();
	width /= 2;
	// In RTL mode, reverse scroll direction: right button scrolls right instead of left
	if (SA::saIsRTL()) {
		scrollByAnimate(width);
	} else {
		scrollByAnimate(-width);
	}
}

void SARibbonCategoryLayout::setGeometry(const QRect& rect)
{
	QRect old = geometry();
	if (old == rect) {
		return;
	}
#if SARibbonCategoryLayout_DEBUG_PRINT
	qDebug() << "===========SARibbonCategoryLayout.setGeometry(" << rect << "(" << ribbonCategory()->categoryName()
			 << ")=======";
#endif
	QLayout::setGeometry(rect);
	d_ptr->mDirty = false;
	updateGeometryArr();
	doLayout();
}
//=============================================================
// SARibbonCategoryLayoutItem
//=============================================================

SARibbonCategoryLayoutItem::SARibbonCategoryLayoutItem(SARibbonPanel* w) : QWidgetItem(w)
{
	separatorWidget = nullptr;
}

SARibbonCategoryLayoutItem::~SARibbonCategoryLayoutItem()
{
}

SARibbonPanel* SARibbonCategoryLayoutItem::toPanelWidget()
{
	return qobject_cast< SARibbonPanel* >(widget());
}

/*** End of inlined file: SARibbonCategoryLayout.cpp ***/


/*** Start of inlined file: SARibbonContextCategory.cpp ***/
#include <QList>
#include <QVariant>

#include <QDebug>

/**
 * \if ENGLISH
 * @brief Data class for storing SARibbonCategory information
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonCategoryData 类
 * \endif
 */
class SAPrivateRibbonCategoryData
{
public:
	SARibbonCategory* categoryPage;
};

/**
 * \if ENGLISH
 * @brief Private data class for SARibbonContextCategory
 * @details This class holds private data for SARibbonContextCategory to implement the PIMPL idiom.
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonContextCategoryPrivate 类
 * @details 此类持有 SARibbonContextCategory 的私有数据，实现 PIMPL 设计模式。
 * \endif
 */
class SARibbonContextCategory::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonContextCategory)
public:
	PrivateData(SARibbonContextCategory* p);

public:
	QList< SAPrivateRibbonCategoryData > mCategoryDataList;
	QVariant mContextID;
	QColor mContextColor;
	QString mContextTitle;
};
SARibbonContextCategory::PrivateData::PrivateData(SARibbonContextCategory* p) : q_ptr(p)
{
}

//===================================================
// SARibbonContextCategory
//===================================================
SARibbonContextCategory::SARibbonContextCategory(QWidget* parent)
	: QObject(parent), d_ptr(new SARibbonContextCategory::PrivateData(this))
{
}

SARibbonContextCategory::~SARibbonContextCategory()
{
}

/**
 * \if ENGLISH
 * @brief Add a category page
 * @param title Category name
 * @return Created SARibbonCategory object
 * \endif
 *
 * \if CHINESE
 * @brief 添加标签
 * @param title 标签名字
 * @return 创建的 SARibbonCategory 对象
 * \endif
 */
SARibbonCategory* SARibbonContextCategory::addCategoryPage(const QString& title)
{
	SARibbonCategory* category = RibbonSubElementFactory->createRibbonCategory(parentWidget());
	category->setCategoryName(title);
	addCategoryPage(category);
	return (category);
}

/**
 * \if ENGLISH
 * @brief Add a category page
 * @param category SARibbonCategory object to add
 * \endif
 *
 * \if CHINESE
 * @brief 添加标签
 * @param category 要添加的 SARibbonCategory 对象
 * \endif
 */
void SARibbonContextCategory::addCategoryPage(SARibbonCategory* category)
{
	if (isHaveCategory(category)) {
		// cn:SARibbonContextCategory已经持有标签：%1，将跳过
		qWarning() << tr("SARibbonContextCategory have category %1,will skip").arg(category->categoryName());
		return;
	}
	category->markIsContextCategory(true);
	connect(category, &SARibbonCategory::categoryNameChanged, this, &SARibbonContextCategory::onCategoryTitleChanged);
	SAPrivateRibbonCategoryData catData;
	catData.categoryPage = category;
	d_ptr->mCategoryDataList.append(catData);
	category->installEventFilter(this);
	Q_EMIT categoryPageAdded(category);
}

int SARibbonContextCategory::categoryCount() const
{
	return (d_ptr->mCategoryDataList.count());
}

void SARibbonContextCategory::setId(const QVariant& id)
{
	d_ptr->mContextID = id;
}

QVariant SARibbonContextCategory::id() const
{
	return (d_ptr->mContextID);
}

void SARibbonContextCategory::setContextColor(const QColor color)
{
	d_ptr->mContextColor = color;
}

QColor SARibbonContextCategory::contextColor() const
{
	return (d_ptr->mContextColor);
}

QWidget* SARibbonContextCategory::parentWidget() const
{
	return (qobject_cast< QWidget* >(parent()));
}

bool SARibbonContextCategory::eventFilter(QObject* watched, QEvent* e)
{
	if (nullptr == watched) {
		return (false);
	}
	switch (e->type()) {
	case QEvent::Close: {
		SARibbonCategory* c = qobject_cast< SARibbonCategory* >(watched);
		if (c) {
#ifdef SA_RIBBON_DEBUG_HELP_DRAW
			qDebug() << " -----------> close event";
#endif
			takeCategory(c);
		}
	} break;

	default:
		break;
	}
	return (false);
}

/**
 * \if ENGLISH
 * @brief Get the context category title
 * @return Context title
 * \endif
 *
 * \if CHINESE
 * @brief 获取上下文标签的标题
 * @return 上下文标题
 * \endif
 */
QString SARibbonContextCategory::contextTitle() const
{
	return (d_ptr->mContextTitle);
}

/**
 * \if ENGLISH
 * @brief Set the context category title
 * @param contextTitle Context title to set
 * @note The title is only displayed in Office mode @ref SARibbonBar::RibbonStyle
 * @note This function will emit the contextTitleChanged signal
 * \endif
 *
 * \if CHINESE
 * @brief 设置上下文标签的标题，标题仅在office模式下显示 @ref SARibbonBar::RibbonStyle
 * @param contextTitle 要设置的上下文标题
 * @note 此函数会触发信号@sa contextTitleChanged
 * \endif
 */
void SARibbonContextCategory::setContextTitle(const QString& contextTitle)
{
	d_ptr->mContextTitle = contextTitle;
	Q_EMIT contextTitleChanged(contextTitle);
}

/**
 * \if ENGLISH
 * @brief Get the corresponding tab page
 * @param index Index of the category page
 * @return SARibbonCategory at the specified index
 * \endif
 *
 * \if CHINESE
 * @brief 获取对应的tab页
 * @param index 标签页的索引
 * @return 指定索引处的 SARibbonCategory
 * \endif
 */
SARibbonCategory* SARibbonContextCategory::categoryPage(int index)
{
	return (d_ptr->mCategoryDataList[ index ].categoryPage);
}

/**
 * \if ENGLISH
 * @brief Get all SARibbonCategory objects
 * @return List of SARibbonCategory pointers
 * \endif
 *
 * \if CHINESE
 * @brief 获取所有的SARibbonCategory*
 * @return SARibbonCategory 指针列表
 * \endif
 */
QList< SARibbonCategory* > SARibbonContextCategory::categoryList() const
{
	QList< SARibbonCategory* > res;

	for (const SAPrivateRibbonCategoryData& c : sa_as_const(d_ptr->mCategoryDataList)) {
		res.append(c.categoryPage);
	}
	return (res);
}

/**
 * \if ENGLISH
 * @brief Remove this category, SARibbonContextCategory will no longer manage this category
 * @param category SARibbonCategory to remove
 * @return true if removal was successful
 * \endif
 *
 * \if CHINESE
 * @brief 移除这个category，这时SARibbonContextCategory不再管理这个category
 * @param category 要移除的 SARibbonCategory
 * @return 成功移除返回true
 * \endif
 */
bool SARibbonContextCategory::takeCategory(SARibbonCategory* category)
{
	for (int i = 0; i < d_ptr->mCategoryDataList.size(); ++i) {
		if (d_ptr->mCategoryDataList[ i ].categoryPage == category) {
			d_ptr->mCategoryDataList.takeAt(i);
			category->removeEventFilter(this);
			Q_EMIT categoryPageRemoved(category);
			return (true);
		}
	}
	return (false);
}

/**
 * \if ENGLISH
 * @brief Check if the context maintains this SARibbonCategory
 * @param category SARibbonCategory to check
 * @return true if the context maintains this category
 * \endif
 *
 * \if CHINESE
 * @brief 判断上下文是否维护了此SARibbonCategory
 * @param category 要检查的 SARibbonCategory
 * @return 如果上下文维护此category则返回true
 * \endif
 */
bool SARibbonContextCategory::isHaveCategory(SARibbonCategory* category) const
{
	for (int i = 0; i < d_ptr->mCategoryDataList.size(); ++i) {
		if (d_ptr->mCategoryDataList[ i ].categoryPage == category) {
			return (true);
		}
	}
	return (false);
}

/**
 * \if ENGLISH
 * @brief Hide the context category
 * @details This method is equivalent to:
 * @code
 * ribbon->hideContextCategory(this);
 * @endcode
 * \endif
 *
 * \if CHINESE
 * @brief hide contextCategory/隐藏上下文标签
 * @details 此方法等同于：
 * @code
 * ribbon->hideContextCategory(this);
 * @endcode
 * \endif
 */
void SARibbonContextCategory::hide()
{
	if (SARibbonBar* ribbon = qobject_cast< SARibbonBar* >(parent())) {
		ribbon->hideContextCategory(this);
	}
}

/**
 * \if ENGLISH
 * @brief Show the context category
 * @details This method is equivalent to:
 * @code
 * ribbon->showContextCategory(this);
 * @endcode
 * \endif
 *
 * \if CHINESE
 * @brief show contextCategory/显示上下文标签
 * @details 此方法等同于：
 * @code
 * ribbon->showContextCategory(this);
 * @endcode
 * \endif
 */
void SARibbonContextCategory::show()
{
	if (SARibbonBar* ribbon = qobject_cast< SARibbonBar* >(parent())) {
		ribbon->showContextCategory(this);
	}
}

void SARibbonContextCategory::onCategoryTitleChanged(const QString& title)
{
	SARibbonCategory* category = qobject_cast< SARibbonCategory* >(sender());
	if (category) {
		Q_EMIT categoryTitleChanged(category, title);
	}
}

/*** End of inlined file: SARibbonContextCategory.cpp ***/


/*** Start of inlined file: SARibbonGalleryItem.cpp ***/
/**
 * \if ENGLISH
 * @brief Default constructor for SARibbonGalleryItem
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGalleryItem 默认构造函数
 * \endif
 */
SARibbonGalleryItem::SARibbonGalleryItem() : mFlags(Qt::ItemIsEnabled | Qt::ItemIsSelectable), mAction(nullptr)
{
}

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonGalleryItem with text and icon
 * @param text Item text
 * @param icon Item icon
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGalleryItem 构造函数（带文本和图标）
 * @param text 项目文本
 * @param icon 项目图标
 * \endif
 */
SARibbonGalleryItem::SARibbonGalleryItem(const QString& text, const QIcon& icon)
	: mFlags(Qt::ItemIsEnabled | Qt::ItemIsSelectable), mAction(nullptr)
{
	setText(text);
	setIcon(icon);
	setTextAlignment(Qt::AlignTop | Qt::AlignHCenter);
}

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonGalleryItem with QAction
 * @param act QAction pointer
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGalleryItem 构造函数（带QAction）
 * @param act QAction指针
 * \endif
 */
SARibbonGalleryItem::SARibbonGalleryItem(QAction* act) : mFlags(Qt::ItemIsEnabled | Qt::ItemIsSelectable)
{
	setTextAlignment(Qt::AlignTop | Qt::AlignHCenter);
	setAction(act);
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonGalleryItem
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGalleryItem 析构函数
 * \endif
 */
SARibbonGalleryItem::~SARibbonGalleryItem()
{
}

/**
 * \if ENGLISH
 * @brief Set data for role
 * @param role Data role
 * @param data Data value
 * \endif
 *
 * \if CHINESE
 * @brief 为角色设置数据
 * @param role 数据角色
 * @param data 数据值
 * \endif
 */
void SARibbonGalleryItem::setData(int role, const QVariant& data)
{
	mDatas[ role ] = data;
}

/**
 * \if ENGLISH
 * @brief Get data for role
 * @param role Data role
 * @return Data value
 * \endif
 *
 * \if CHINESE
 * @brief 获取角色的数据
 * @param role 数据角色
 * @return 数据值
 * \endif
 */
QVariant SARibbonGalleryItem::data(int role) const
{
	if (mAction) {
		switch (role) {
		case Qt::DisplayRole:
			return (mAction->text());

		case Qt::ToolTipRole:
			return (mAction->toolTip());

		case Qt::DecorationRole:
			return (mAction->icon());
		default:
			break;
		}
	}
	return (mDatas.value(role));
}

/**
 * \if ENGLISH
 * @brief Set text
 * @param text Text
 * \endif
 *
 * \if CHINESE
 * @brief 设置文本
 * @param text 文本
 * \endif
 */
void SARibbonGalleryItem::setText(const QString& text)
{
	setData(Qt::DisplayRole, text);
}

/**
 * \if ENGLISH
 * @brief Get text
 * @return Text
 * \endif
 *
 * \if CHINESE
 * @brief 获取文本
 * @return 文本
 * \endif
 */
QString SARibbonGalleryItem::text() const
{
	if (mAction) {
		return (mAction->text());
	}
	return (data(Qt::DisplayRole).toString());
}

/**
 * \if ENGLISH
 * @brief Set tool tip
 * @param text Tool tip text
 * \endif
 *
 * \if CHINESE
 * @brief 设置工具提示
 * @param text 工具提示文本
 * \endif
 */
void SARibbonGalleryItem::setToolTip(const QString& text)
{
	setData(Qt::ToolTipRole, text);
}

/**
 * \if ENGLISH
 * @brief Get tool tip
 * @return Tool tip text
 * \endif
 *
 * \if CHINESE
 * @brief 获取工具提示
 * @return 工具提示文本
 * \endif
 */
QString SARibbonGalleryItem::toolTip() const
{
	if (mAction) {
		return (mAction->toolTip());
	}
	return (data(Qt::ToolTipRole).toString());
}

/**
 * \if ENGLISH
 * @brief Set icon
 * @param ico Icon
 * \endif
 *
 * \if CHINESE
 * @brief 设置图标
 * @param ico 图标
 * \endif
 */
void SARibbonGalleryItem::setIcon(const QIcon& ico)
{
	setData(Qt::DecorationRole, ico);
}

/**
 * \if ENGLISH
 * @brief Get icon
 * @return Icon
 * \endif
 *
 * \if CHINESE
 * @brief 获取图标
 * @return 图标
 * \endif
 */
QIcon SARibbonGalleryItem::icon() const
{
	if (mAction) {
		return (mAction->icon());
	}
	return (qvariant_cast< QIcon >(data(Qt::DecorationRole)));
}

/**
 * \if ENGLISH
 * @brief Check if item is selectable
 * @return True if selectable
 * \endif
 *
 * \if CHINESE
 * @brief 检查项目是否可选择
 * @return 如果可选择返回true
 * \endif
 */
bool SARibbonGalleryItem::isSelectable() const
{
	return (mFlags & Qt::ItemIsSelectable);
}

/**
 * \if ENGLISH
 * @brief Set selectable
 * @param isSelectable Whether selectable
 * \endif
 *
 * \if CHINESE
 * @brief 设置可选择
 * @param isSelectable 是否可选择
 * \endif
 */
void SARibbonGalleryItem::setSelectable(bool isSelectable)
{
	if (isSelectable) {
		mFlags |= Qt::ItemIsSelectable;
	} else {
		mFlags = (mFlags & (~Qt::ItemIsSelectable));
	}
}

/**
 * \if ENGLISH
 * @brief Check if item is enabled
 * @return True if enabled
 * \endif
 *
 * \if CHINESE
 * @brief 检查项目是否启用
 * @return 如果启用返回true
 * \endif
 */
bool SARibbonGalleryItem::isEnable() const
{
	if (mAction) {
		return (mAction->isEnabled());
	}
	return (mFlags & Qt::ItemIsEnabled);
}

/**
 * \if ENGLISH
 * @brief Set enable
 * @param isEnable Whether enable
 * \endif
 *
 * \if CHINESE
 * @brief 设置启用
 * @param isEnable 是否启用
 * \endif
 */
void SARibbonGalleryItem::setEnable(bool isEnable)
{
	if (mAction) {
		mAction->setEnabled(isEnable);
	}

	if (isEnable) {
		mFlags |= Qt::ItemIsEnabled;
	} else {
		mFlags = (mFlags & (~Qt::ItemIsEnabled));
	}
}

/**
 * \if ENGLISH
 * @brief Set flags
 * @param flag Item flags
 * \endif
 *
 * \if CHINESE
 * @brief 设置标志
 * @param flag 项目标志
 * \endif
 */
void SARibbonGalleryItem::setFlags(Qt::ItemFlags flag)
{
	mFlags = flag;
	if (mAction) {
		mAction->setEnabled(flag & Qt::ItemIsEnabled);
	}
}

/**
 * \if ENGLISH
 * @brief Get flags
 * @return Item flags
 * \endif
 *
 * \if CHINESE
 * @brief 获取标志
 * @return 项目标志
 * \endif
 */
Qt::ItemFlags SARibbonGalleryItem::flags() const
{
	return (mFlags);
}

/**
 * \if ENGLISH
 * @brief Set action
 * @param act QAction pointer
 * \endif
 *
 * \if CHINESE
 * @brief 设置动作
 * @param act QAction指针
 * \endif
 */
void SARibbonGalleryItem::setAction(QAction* act)
{
	mAction = act;
	if (nullptr == mAction) {
		return;
	}
	if (act->isEnabled()) {
		mFlags |= Qt::ItemIsEnabled;
	} else {
		mFlags = (mFlags & (~Qt::ItemIsEnabled));
	}
}

/**
 * \if ENGLISH
 * @brief Get action
 * @return QAction pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取动作
 * @return QAction指针
 * \endif
 */
QAction* SARibbonGalleryItem::action()
{
	return (mAction);
}

/**
 * \if ENGLISH
 * @brief Set text alignment
 * @param a Text alignment
 * \endif
 *
 * \if CHINESE
 * @brief 设置文本对齐方式
 * @param a 文本对齐方式
 * \endif
 */
void SARibbonGalleryItem::setTextAlignment(Qt::Alignment a)
{
	setData(Qt::TextAlignmentRole, (int)a);
}

/**
 * \if ENGLISH
 * @brief Get text alignment
 * @return Text alignment
 * \endif
 *
 * \if CHINESE
 * @brief 获取文本对齐方式
 * @return 文本对齐方式
 * \endif
 */
Qt::Alignment SARibbonGalleryItem::textAlignment() const
{
	return qvariant_cast< Qt::Alignment >(data(Qt::TextAlignmentRole));
}

/*** End of inlined file: SARibbonGalleryItem.cpp ***/


/*** Start of inlined file: SARibbonGalleryGroup.cpp ***/
#include <QPainter>
#include <QDebug>
#include <QActionGroup>
#include <QItemSelectionModel>

/**
 * @brief The SARibbonGalleryGroupPrivate class
 */
class SARibbonGalleryGroup::PrivateData
{
public:
	SARibbonGalleryGroup* q_ptr;
	QString mGroupTitle;
	SARibbonGalleryGroup::GalleryGroupStyle mPreStyle { SARibbonGalleryGroup::IconWithText };
	SARibbonGalleryGroup::DisplayRow mDisplayRow { SARibbonGalleryGroup::DisplayOneRow };
	bool mBlockRecalc { false };
	int mGridMinimumWidth { 0 };             ///< grid最小宽度
	int mGridMaximumWidth { 0 };             ///< grid最大宽度
	QActionGroup* mActionGroup { nullptr };  ///< 所有GalleryGroup管理的actions都由这个actiongroup管理
public:
	PrivateData(SARibbonGalleryGroup* p) : q_ptr(p)
	{
		mActionGroup = new QActionGroup(p);
		p->connect(mActionGroup, &QActionGroup::triggered, p, &SARibbonGalleryGroup::triggered);
		p->connect(mActionGroup, &QActionGroup::hovered, p, &SARibbonGalleryGroup::hovered);
	}
};

//===================================================
// SARibbonGalleryGroupItemDelegate
//===================================================

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonGalleryGroupItemDelegate
 * @param group SARibbonGalleryGroup pointer
 * @param parent Parent object
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGalleryGroupItemDelegate 构造函数
 * @param group SARibbonGalleryGroup指针
 * @param parent 父对象
 * \endif
 */
SARibbonGalleryGroupItemDelegate::SARibbonGalleryGroupItemDelegate(SARibbonGalleryGroup* group, QObject* parent)
	: QStyledItemDelegate(parent), mGroup(group)
{
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonGalleryGroupItemDelegate
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGalleryGroupItemDelegate 析构函数
 * \endif
 */
SARibbonGalleryGroupItemDelegate::~SARibbonGalleryGroupItemDelegate()
{
}

/**
 * \if ENGLISH
 * @brief Paint the item
 * @param painter Painter
 * @param option Style option
 * @param index Model index
 * \endif
 *
 * \if CHINESE
 * @brief 绘制项目
 * @param painter 画家
 * @param option 样式选项
 * @param index 模型索引
 * \endif
 */
void SARibbonGalleryGroupItemDelegate::paint(QPainter* painter, const QStyleOptionViewItem& option, const QModelIndex& index) const
{
	if (nullptr == mGroup) {
		return;
	}
	switch (mGroup->galleryGroupStyle()) {
	case SARibbonGalleryGroup::IconWithText:
		paintIconWithText(painter, option, index);
		break;
	case SARibbonGalleryGroup::IconWithWordWrapText:
		paintIconWithTextWordWrap(painter, option, index);
		break;
	case SARibbonGalleryGroup::IconOnly:
		paintIconOnly(painter, option, index);
		break;
	default:
		paintIconWithText(painter, option, index);
		break;
	}
}

/**
 * \if ENGLISH
 * @brief Paint only icon
 * @param painter Painter
 * @param option Style option
 * @param index Model index
 * \endif
 *
 * \if CHINESE
 * @brief 仅绘制图标
 * @param painter 画家
 * @param option 样式选项
 * @param index 模型索引
 * \endif
 */
void SARibbonGalleryGroupItemDelegate::paintIconOnly(QPainter* painter,
													 const QStyleOptionViewItem& option,
													 const QModelIndex& index) const
{
	QStyle* style = mGroup->style();
	int sp        = mGroup->spacing();
	sp += 3;
	painter->save();
	painter->setClipRect(option.rect);
	style->drawPrimitive(QStyle::PE_PanelItemViewItem, &option, painter, mGroup);
	// draw the icon
	QRect iconRect = option.rect;

	iconRect.adjust(sp, sp, -sp, -sp);
	QIcon ico = qvariant_cast< QIcon >(index.data(Qt::DecorationRole));

	ico.paint(painter, iconRect, Qt::AlignCenter, QIcon::Normal, QIcon::On);
	painter->restore();
}

/**
 * \if ENGLISH
 * @brief Paint icon with text
 * @param painter Painter
 * @param option Style option
 * @param index Model index
 * \endif
 *
 * \if CHINESE
 * @brief 绘制带文本的图标
 * @param painter 画家
 * @param option 样式选项
 * @param index 模型索引
 * \endif
 */
void SARibbonGalleryGroupItemDelegate::paintIconWithText(QPainter* painter,
														 const QStyleOptionViewItem& option,
														 const QModelIndex& index) const
{
	QStyledItemDelegate::paint(painter, option, index);
}

/**
 * \if ENGLISH
 * @brief Paint icon with word wrap text
 * @param painter Painter
 * @param option Style option
 * @param index Model index
 * \endif
 *
 * \if CHINESE
 * @brief 绘制带自动换行文本的图标
 * @param painter 画家
 * @param option 样式选项
 * @param index 模型索引
 * \endif
 */
void SARibbonGalleryGroupItemDelegate::paintIconWithTextWordWrap(QPainter* painter,
																 const QStyleOptionViewItem& option,
																 const QModelIndex& index) const
{
	QStyledItemDelegate::paint(painter, option, index);
}

/**
 * \if ENGLISH
 * @brief Get size hint
 * @param option Style option
 * @param index Model index
 * @return Size hint
 * \endif
 *
 * \if CHINESE
 * @brief 获取大小提示
 * @param option 样式选项
 * @param index 模型索引
 * @return 大小提示
 * \endif
 */
QSize SARibbonGalleryGroupItemDelegate::sizeHint(const QStyleOptionViewItem& option, const QModelIndex& index) const
{
	Q_UNUSED(index);
	Q_UNUSED(option);
	return mGroup->gridSize();
}

//===================================================
// SARibbonGalleryGroupModel
//===================================================

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonGalleryGroupModel
 * @param parent Parent object
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGalleryGroupModel 构造函数
 * @param parent 父对象
 * \endif
 */
SARibbonGalleryGroupModel::SARibbonGalleryGroupModel(QObject* parent) : QAbstractListModel(parent)
{
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonGalleryGroupModel
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGalleryGroupModel 析构函数
 * \endif
 */
SARibbonGalleryGroupModel::~SARibbonGalleryGroupModel()
{
	clear();
}

/**
 * \if ENGLISH
 * @brief Get row count
 * @param parent Parent model index
 * @return Row count
 * \endif
 *
 * \if CHINESE
 * @brief 获取行数
 * @param parent 父模型索引
 * @return 行数
 * \endif
 */
int SARibbonGalleryGroupModel::rowCount(const QModelIndex& parent) const
{
	return (parent.isValid() ? 0 : mItems.size());
}

/**
 * \if ENGLISH
 * @brief Get item flags
 * @param index Model index
 * @return Item flags
 * \endif
 *
 * \if CHINESE
 * @brief 获取项目标志
 * @param index 模型索引
 * @return 项目标志
 * \endif
 */
Qt::ItemFlags SARibbonGalleryGroupModel::flags(const QModelIndex& index) const
{
	if (!index.isValid() || (index.row() >= mItems.size())) {
		return (Qt::NoItemFlags);
	}
	return (mItems.at(index.row())->flags());
}

/**
 * \if ENGLISH
 * @brief Get data at index
 * @param index Model index
 * @param role Data role
 * @return Data value
 * \endif
 *
 * \if CHINESE
 * @brief 获取索引处的数据
 * @param index 模型索引
 * @param role 数据角色
 * @return 数据值
 * \endif
 */
QVariant SARibbonGalleryGroupModel::data(const QModelIndex& index, int role) const
{
	if (!index.isValid() || (index.row() >= mItems.count())) {
		return (QVariant());
	}
	return (mItems.at(index.row())->data(role));
}

/**
 * \if ENGLISH
 * @brief Create model index
 * @param row Row
 * @param column Column
 * @param parent Parent model index
 * @return Model index
 * \endif
 *
 * \if CHINESE
 * @brief 创建模型索引
 * @param row 行
 * @param column 列
 * @param parent 父模型索引
 * @return 模型索引
 * \endif
 */
QModelIndex SARibbonGalleryGroupModel::index(int row, int column, const QModelIndex& parent) const
{
	if (hasIndex(row, column, parent)) {
		return (createIndex(row, column, mItems.at(row)));
	}
	return (QModelIndex());
}

/**
 * \if ENGLISH
 * @brief Set data at index
 * @param index Model index
 * @param value Data value
 * @param role Data role
 * @return True if successful
 * \endif
 *
 * \if CHINESE
 * @brief 设置索引处的数据
 * @param index 模型索引
 * @param value 数据值
 * @param role 数据角色
 * @return 成功返回true
 * \endif
 */
bool SARibbonGalleryGroupModel::setData(const QModelIndex& index, const QVariant& value, int role)
{
	if (!index.isValid() || (index.row() >= mItems.count())) {
		return (false);
	}

	mItems.at(index.row())->setData(role, value);
	return (true);
}

/**
 * \if ENGLISH
 * @brief Clear all items
 * \endif
 *
 * \if CHINESE
 * @brief 清空所有项目
 * \endif
 */
void SARibbonGalleryGroupModel::clear()
{
	beginResetModel();
	for (int i = 0; i < mItems.count(); ++i) {
		if (mItems.at(i)) {
			delete mItems.at(i);
		}
	}
	mItems.clear();
	endResetModel();
}

/**
 * \if ENGLISH
 * @brief Get item at row
 * @param row Row index
 * @return SARibbonGalleryItem pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取行处的项目
 * @param row 行索引
 * @return SARibbonGalleryItem指针
 * \endif
 */
SARibbonGalleryItem* SARibbonGalleryGroupModel::at(int row) const
{
	return (mItems.value(row));
}

/**
 * \if ENGLISH
 * @brief Insert item at row
 * @param row Insert position
 * @param item SARibbonGalleryItem pointer
 * \endif
 *
 * \if CHINESE
 * @brief 在指定行插入项目
 * @param row 插入位置
 * @param item SARibbonGalleryItem指针
 * \endif
 */
void SARibbonGalleryGroupModel::insert(int row, SARibbonGalleryItem* item)
{
	beginInsertRows(QModelIndex(), row, row);
	mItems.insert(row, item);
	endInsertRows();
}

/**
 * \if ENGLISH
 * @brief Take item at row
 * @param row Row index
 * @return SARibbonGalleryItem pointer
 * \endif
 *
 * \if CHINESE
 * @brief 取出指定行的项目
 * @param row 行索引
 * @return SARibbonGalleryItem指针
 * \endif
 */
SARibbonGalleryItem* SARibbonGalleryGroupModel::take(int row)
{
	if ((row < 0) || (row >= mItems.count())) {
		return (0);
	}

	beginRemoveRows(QModelIndex(), row, row);
	SARibbonGalleryItem* item = mItems.takeAt(row);

	endRemoveRows();
	return (item);
}

/**
 * \if ENGLISH
 * @brief Append item
 * @param item SARibbonGalleryItem pointer (memory managed by SARibbonGalleryGroupModel)
 * \endif
 *
 * \if CHINESE
 * @brief 插入条目，item的内存由SARibbonGalleryGroupModel管理
 * @param item SARibbonGalleryItem指针
 * \endif
 */
void SARibbonGalleryGroupModel::append(SARibbonGalleryItem* item)
{
	beginInsertRows(QModelIndex(), mItems.count(), mItems.count() + 1);

	mItems.append(item);
	endInsertRows();
}

/**
 * \if ENGLISH
 * @brief Remove item by action object name
 * @param act_object_name Action object name
 * @return Result of removal operation
 * \endif
 *
 * \if CHINESE
 * @brief 通过QAction的对象名删除内部GalleryItem
 * @param act_object_name 动作对象名
 * @return 删除操作的结果
 * \endif
 */
bool SARibbonGalleryGroupModel::remove(const QString& act_object_name)
{
	for (int index = 0; index < mItems.size(); ++index) {
		if (mItems.at(index)->action()->objectName() == act_object_name) {
			beginRemoveRows(QModelIndex(), index, index);
			SARibbonGalleryItem* item = mItems.takeAt(index);
			endRemoveRows();

			delete item;

			return true;
		}
	}

	return false;
}

/**
 * \if ENGLISH
 * @brief Get item count
 * @return Item count
 * \endif
 *
 * \if CHINESE
 * @brief 获取条目数量
 * @return 条目数量
 * \endif
 */
int SARibbonGalleryGroupModel::itemSize() const
{
	return mItems.size();
}

//===================================================
// SARibbonGalleryGroup
//===================================================
/**
 * \if ENGLISH
 * @brief Constructor for SARibbonGalleryGroup
 * @param w Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGalleryGroup 构造函数
 * @param w 父窗口
 * \endif
 */
SARibbonGalleryGroup::SARibbonGalleryGroup(QWidget* w)
	: QListView(w), d_ptr(new SARibbonGalleryGroup::PrivateData(this))
{
	setViewMode(QListView::IconMode);
	setResizeMode(QListView::Adjust);
	setFlow(QListView::LeftToRight);
	setSelectionRectVisible(true);
	setUniformItemSizes(true);
	setSpacing(1);
	setItemDelegate(new SARibbonGalleryGroupItemDelegate(this, this));
	connect(this, &QAbstractItemView::clicked, this, &SARibbonGalleryGroup::onItemClicked);
	SARibbonGalleryGroupModel* m = setupGroupModel();
	setModel(m);
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonGalleryGroup
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGalleryGroup 析构函数
 * \endif
 */
SARibbonGalleryGroup::~SARibbonGalleryGroup()
{
}

/**
 * \if ENGLISH
 * @brief Recalculate grid and icon sizes
 * \endif
 *
 * \if CHINESE
 * @brief 重新计算grid和icon的尺寸
 * \endif
 */
void SARibbonGalleryGroup::recalcGridSize()
{
	recalcGridSize(height());
}

/**
 * \if ENGLISH
 * @brief Recalculate grid and icon sizes
 * @param galleryHeight Gallery height
 * \endif
 *
 * \if CHINESE
 * @brief 重新计算grid和icon的尺寸
 * @param galleryHeight 画廊高度
 * \endif
 */
void SARibbonGalleryGroup::recalcGridSize(int galleryHeight)
{
	// 首先通过DisplayRow计算GridSize
	int dr = static_cast< int >(displayRow());
	if (dr < 1) {
		dr = 1;
	} else if (dr > 3) {
		dr = 3;
	}
	int h = galleryHeight / dr;
	if (h <= 1) {
		h = galleryHeight;
	}
	int w = h;
	if (gridMinimumWidth() > 0) {
		if (w < gridMinimumWidth()) {
			w = gridMinimumWidth();
		}
	}
	if (gridMaximumWidth() > 0) {
		if (w > gridMaximumWidth()) {
			w = gridMaximumWidth();
		}
	}
	setGridSize(QSize(w, h));
	// 在通过GalleryGroupStyle确定icon的尺寸
	const int shiftpix =
		4;  // 这个是移动像素，qt在鼠标移动到图标上时会移动一下，给用户明确的动态，导致如果布局很满会超出显示范围，因此要在此基础上缩放一点
	switch (galleryGroupStyle()) {
	case IconWithText: {
		int textHeight = fontMetrics().lineSpacing();
		int iconHeight = h - textHeight - 2 * spacing() - shiftpix;
		if (iconHeight > 0) {
			setIconSize(QSize(w - 2 * spacing() - shiftpix, iconHeight));
		} else {
			setIconSize(QSize(w - 2 * spacing() - shiftpix, h - 2 * spacing() - shiftpix));
		}
		break;
	}
	case IconWithWordWrapText: {
		int textHeight = fontMetrics().lineSpacing() * 2;
		int iconHeight = h - textHeight;
		if (iconHeight > 0) {
			setIconSize(QSize(w - 2 * spacing() - shiftpix, iconHeight - 2 * spacing() - shiftpix));
		} else {
			setIconSize(QSize(w - 2 * spacing() - shiftpix, h - 2 * spacing() - shiftpix));
		}
		break;
	}
	case IconOnly: {
		setIconSize(QSize(w - 2 * spacing() - shiftpix, h - 2 * spacing() - shiftpix));
		break;
	}
	default: {
		setIconSize(QSize(w - 2 * spacing() - shiftpix, h - 2 * spacing() - shiftpix));
		break;
	}
	}
#if 0
	qDebug() << "SARibbonGalleryGroup::recalcGridSize(" << galleryHeight << "): gridSize=" << gridSize()
			 << " iconSize=" << iconSize();
#endif
}

/**
 * \if ENGLISH
 * @brief Set the default preset style
 * @param style Gallery group style
 * \endif
 *
 * \if CHINESE
 * @brief 设置默认的预设样式
 * @param style 画廊组样式
 * \endif
 */
void SARibbonGalleryGroup::setGalleryGroupStyle(SARibbonGalleryGroup::GalleryGroupStyle style)
{
	d_ptr->mPreStyle = style;
	setWordWrap(style == IconWithWordWrapText);
	recalcGridSize();
}

/**
 * \if ENGLISH
 * @brief Get the gallery group style
 * @return Gallery group style
 * \endif
 *
 * \if CHINESE
 * @brief 获取画廊组样式
 * @return 画廊组样式
 * \endif
 */
SARibbonGalleryGroup::GalleryGroupStyle SARibbonGalleryGroup::galleryGroupStyle() const
{
	return d_ptr->mPreStyle;
}

/**
 * \if ENGLISH
 * @brief Add item with text and icon
 * @param text Item text
 * @param icon Item icon
 * \endif
 *
 * \if CHINESE
 * @brief 添加带文本和图标的项目
 * @param text 项目文本
 * @param icon 项目图标
 * \endif
 */
void SARibbonGalleryGroup::addItem(const QString& text, const QIcon& icon)
{
	if (nullptr == groupModel()) {
		return;
	}
	addItem(new SARibbonGalleryItem(text, icon));
}

/**
 * \if ENGLISH
 * @brief Add item
 * @param item SARibbonGalleryItem pointer (memory managed by SARibbonGalleryGroup)
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个条目
 * @param item 条目的内存所有权归属SARibbonGalleryGroup管理
 * \endif
 */
void SARibbonGalleryGroup::addItem(SARibbonGalleryItem* item)
{
	if (nullptr == groupModel()) {
		return;
	}
	groupModel()->append(item);
}

/**
 * \if ENGLISH
 * @brief Add action item
 * @param act QAction pointer
 * \endif
 *
 * \if CHINESE
 * @brief 添加动作项目
 * @param act QAction指针
 * \endif
 */
void SARibbonGalleryGroup::addActionItem(QAction* act)
{
	if (nullptr == groupModel()) {
		return;
	}
	d_ptr->mActionGroup->addAction(act);
	groupModel()->append(new SARibbonGalleryItem(act));
}

/**
 * \if ENGLISH
 * @brief Add action item list
 * @param acts QAction list
 * \endif
 *
 * \if CHINESE
 * @brief 添加动作项目列表
 * @param acts QAction列表
 * \endif
 */
void SARibbonGalleryGroup::addActionItemList(const QList< QAction* >& acts)
{
	SARibbonGalleryGroupModel* model = groupModel();

	if (nullptr == model) {
		return;
	}
	for (QAction* a : acts) {
		d_ptr->mActionGroup->addAction(a);
	}
	for (int i = 0; i < acts.size(); ++i) {
		model->append(new SARibbonGalleryItem(acts[ i ]));
	}
}

/**
 * \if ENGLISH
 * @brief Remove QAction from current GalleryGroup
 * @param act QAction pointer
 * @return Result of removal operation
 * \endif
 *
 * \if CHINESE
 * @brief 从当前GalleryGroup中移除QAction
 * @param act QAction指针
 * @return 删除操作的结果
 * \endif
 */
bool SARibbonGalleryGroup::removeActionItem(QAction* act)
{
	if (nullptr == groupModel()) {
		return false;
	} else {
		d_ptr->mActionGroup->removeAction(act);
		return groupModel()->remove(act->objectName());
	}
}

/**
 * \if ENGLISH
 * @brief Setup group model
 * @return SARibbonGalleryGroupModel pointer
 * \endif
 *
 * \if CHINESE
 * @brief 构建一个model，这个model的父类是SARibbonGalleryGroup，如果要共享model，需要手动处理model的父类
 * @return SARibbonGalleryGroupModel指针
 * \endif
 */
SARibbonGalleryGroupModel* SARibbonGalleryGroup::setupGroupModel()
{
	SARibbonGalleryGroupModel* m = new SARibbonGalleryGroupModel(this);
	setModel(m);
	return m;
}

/**
 * \if ENGLISH
 * @brief Get group model
 * @return SARibbonGalleryGroupModel pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取组模型
 * @return SARibbonGalleryGroupModel指针
 * \endif
 */
SARibbonGalleryGroupModel* SARibbonGalleryGroup::groupModel() const
{
	return (qobject_cast< SARibbonGalleryGroupModel* >(model()));
}

/**
 * \if ENGLISH
 * @brief Set group title
 * @param title Group title
 * \endif
 *
 * \if CHINESE
 * @brief 设置组标题
 * @param title 组标题
 * \endif
 */
void SARibbonGalleryGroup::setGroupTitle(const QString& title)
{
	d_ptr->mGroupTitle = title;
	Q_EMIT groupTitleChanged(d_ptr->mGroupTitle);
}

/**
 * \if ENGLISH
 * @brief Get group title
 * @return Group title
 * \endif
 *
 * \if CHINESE
 * @brief 获取组标题
 * @return 组标题
 * \endif
 */
QString SARibbonGalleryGroup::groupTitle() const
{
	return (d_ptr->mGroupTitle);
}

/**
 * \if ENGLISH
 * @brief Select item by index
 * @param i Item index
 * \endif
 *
 * \if CHINESE
 * @brief 通过索引选择项目
 * @param i 项目索引
 * \endif
 */
void SARibbonGalleryGroup::selectByIndex(int i)
{
	SARibbonGalleryGroupModel* model = groupModel();

	if (nullptr == model) {
		return;
	}
	QModelIndex ind               = model->index(i, 0, QModelIndex());
	QItemSelectionModel* selmodel = selectionModel();

	if (selmodel) {
		selmodel->select(ind, QItemSelectionModel::SelectCurrent);
	}
}

/**
 * \if ENGLISH
 * @brief Set display row
 * @param r Display row
 * \endif
 *
 * \if CHINESE
 * @brief 设置显示的行数
 * @param r 显示行数
 * \endif
 */
void SARibbonGalleryGroup::setDisplayRow(DisplayRow r)
{
	d_ptr->mDisplayRow = r;
	recalcGridSize();
}

/**
 * \if ENGLISH
 * @brief Get display row
 * @return Display row
 * \endif
 *
 * \if CHINESE
 * @brief Gallery显示的行数
 * @return 显示行数
 * \endif
 */
SARibbonGalleryGroup::DisplayRow SARibbonGalleryGroup::displayRow() const
{
	return d_ptr->mDisplayRow;
}

/**
 * \if ENGLISH
 * @brief Set grid minimum width
 * @param w Minimum width
 * \endif
 *
 * \if CHINESE
 * @brief 设置grid最小的宽度，默认为0（不限制）
 * @param w 最小宽度
 * \endif
 */
void SARibbonGalleryGroup::setGridMinimumWidth(int w)
{
	d_ptr->mGridMinimumWidth = w;
}

/**
 * \if ENGLISH
 * @brief Get grid minimum width
 * @return Minimum width
 * \endif
 *
 * \if CHINESE
 * @brief grid最小的宽度，默认为0（不限制）
 * @return 最小宽度
 * \endif
 */
int SARibbonGalleryGroup::gridMinimumWidth() const
{
	return d_ptr->mGridMinimumWidth;
}

/**
 * \if ENGLISH
 * @brief Set grid maximum width
 * @param w Maximum width
 * \endif
 *
 * \if CHINESE
 * @brief 设置grid最大的宽度，默认为0（不限制）
 * @param w 最大宽度
 * \endif
 */
void SARibbonGalleryGroup::setGridMaximumWidth(int w)
{
	d_ptr->mGridMaximumWidth = w;
}

/**
 * \if ENGLISH
 * @brief Get grid maximum width
 * @return Maximum width
 * \endif
 *
 * \if CHINESE
 * @brief grid最大的的宽度，默认为0（不限制）
 * @return 最大宽度
 * \endif
 */
int SARibbonGalleryGroup::gridMaximumWidth() const
{
	return d_ptr->mGridMaximumWidth;
}

/**
 * \if ENGLISH
 * @brief Get action group
 * @return QActionGroup pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取SARibbonGalleryGroup管理的actiongroup
 * @return QActionGroup指针
 * \endif
 */
QActionGroup* SARibbonGalleryGroup::actionGroup() const
{
	return d_ptr->mActionGroup;
}

/**
 * \if ENGLISH
 * @brief Calculate grid row count
 * @return Row count
 * \endif
 *
 * \if CHINESE
 * @brief 计算行数
 * @return 行数
 * \endif
 */
int SARibbonGalleryGroup::gridRowCount() const
{
	SARibbonGalleryGroupModel* model = groupModel();
	if (nullptr == model) {
		return 0;
	}
	int gcol = gridColumnCount();
	if (gcol == 0) {
		return 0;
	}
	int itemCnt = model->rowCount(QModelIndex());
	return (itemCnt + gcol - 1) / gcol;
}

/**
 * \if ENGLISH
 * @brief Calculate grid column count
 * @return Column count
 * \endif
 *
 * \if CHINESE
 * @brief 计算列数
 * @return 列数
 * \endif
 */
int SARibbonGalleryGroup::gridColumnCount() const
{
	auto vp = viewport();
	if (!vp) {
		return 0;
	}
	int w    = viewport()->width();
	QSize gs = gridSize();
	if (gs.width() <= 0) {
		return 0;
	}
	return w / gs.width();
}

/**
 * \if ENGLISH
 * @brief Calculate preferred height for width
 * @param w Width
 * @return Preferred height
 * \endif
 *
 * \if CHINESE
 * @brief 计算最紧凑的高度
 * @param w 宽度
 * @return 首选高度
 * \endif
 */
int SARibbonGalleryGroup::preferredHeightForWidth(int w) const
{
	SARibbonGalleryGroupModel* model = groupModel();
	if (nullptr == model) {
		return -1;
	}
	int viewWidth = w - 2 * frameWidth();
	QSize gs      = gridSize();
	if (gs.width() <= 0) {
		return -1;
	}
	int gcol      = viewWidth / gs.width();
	if (gcol == 0) {
		return -1;
	}
	int itemCnt = model->rowCount(QModelIndex());
	int grow    = (itemCnt / gcol) + 1;
	return grow * gs.height() + 2 * frameWidth() + 5;  // 这里加上5是留下一定余量，避免刚好触发滚动条
}

/**
 * \if ENGLISH
 * @brief Check if has height for width
 * @return True if has height for width
 * \endif
 *
 * \if CHINESE
 * @brief 检查是否具有宽度对应的高度
 * @return 如果具有宽度对应的高度返回true
 * \endif
 */
bool SARibbonGalleryGroup::hasHeightForWidth() const
{
	return true;
}

/**
 * \if ENGLISH
 * @brief Get height for width
 * @param w Width
 * @return Height
 * \endif
 *
 * \if CHINESE
 * @brief 获取宽度对应的高度
 * @param w 宽度
 * @return 高度
 * \endif
 */
int SARibbonGalleryGroup::heightForWidth(int w) const
{
	return preferredHeightForWidth(w);
}

/**
 * \if ENGLISH
 * @brief Handle item clicked event
 * @param index Model index
 * \endif
 *
 * \if CHINESE
 * @brief 处理项目点击事件
 * @param index 模型索引
 * \endif
 */
void SARibbonGalleryGroup::onItemClicked(const QModelIndex& index)
{
	if (index.isValid()) {
		SARibbonGalleryItem* item = (SARibbonGalleryItem*)index.internalPointer();
		if (item) {
			QAction* act = item->action();
			if (act) {
				act->activate(QAction::Trigger);
			}
		}
	}
}

/**
 * \if ENGLISH
 * @brief Handle item entered event
 * @param index Model index
 * \endif
 *
 * \if CHINESE
 * @brief 处理项目进入事件
 * @param index 模型索引
 * \endif
 */
void SARibbonGalleryGroup::onItemEntered(const QModelIndex& index)
{
	if (index.isValid()) {
		SARibbonGalleryItem* item = (SARibbonGalleryItem*)index.internalPointer();
		if (item) {
			QAction* act = item->action();
			if (act) {
				act->activate(QAction::Hover);
			}
		}
	}
}

/*** End of inlined file: SARibbonGalleryGroup.cpp ***/


/*** Start of inlined file: SARibbonGallery.cpp ***/
#include <QIcon>
#include <QApplication>
#include <QResizeEvent>
#include <QDebug>
#include <QVBoxLayout>
#include <QScrollBar>
#include <QLabel>
#include <QSizeGrip>
#include <QActionGroup>
#include <QScreen>

#define ICON_ARROW_UP QIcon(":/SARibbon/image/resource/ArrowUp.png")
#define ICON_ARROW_DOWN QIcon(":/SARibbon/image/resource/ArrowDown.png")
#define ICON_ARROW_MORE QIcon(":/SARibbon/image/resource/ArrowMore.png")
/**
 * @brief The SARibbonGalleryPrivate class
 */
class SARibbonGallery::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonGallery)
public:
	static int sGalleryButtonMaximumWidth;
	SARibbonGalleryButton* mButtonUp { nullptr };
	SARibbonGalleryButton* mButtonDown { nullptr };
	SARibbonGalleryButton* mButtonMore { nullptr };
#if 0
	SARibbonMenu *popupWidget;
#else
	SARibbonGalleryViewport* mPopupWidget { nullptr };
#endif
	SARibbonGalleryGroup* mCurrentViewportGroup { nullptr };
	QBoxLayout* mButtonLayout { nullptr };
	QBoxLayout* mLayout { nullptr };
	bool mSingleRowMode { false };
	PrivateData(SARibbonGallery* p) : q_ptr(p)
	{
	}

	void init()
	{
		mButtonUp   = new SARibbonGalleryButton(q_ptr);
		mButtonDown = new SARibbonGalleryButton(q_ptr);
		mButtonMore = new SARibbonGalleryButton(q_ptr);
		mButtonUp->setToolButtonStyle(Qt::ToolButtonIconOnly);
		mButtonDown->setToolButtonStyle(Qt::ToolButtonIconOnly);
		mButtonMore->setToolButtonStyle(Qt::ToolButtonIconOnly);
		mButtonUp->setSizePolicy(QSizePolicy::Preferred, QSizePolicy::Expanding);
		mButtonDown->setSizePolicy(QSizePolicy::Preferred, QSizePolicy::Expanding);
		mButtonMore->setSizePolicy(QSizePolicy::Preferred, QSizePolicy::Expanding);
		mButtonUp->setObjectName(QStringLiteral("SARibbonGalleryButtonUp"));
		mButtonDown->setObjectName(QStringLiteral("SARibbonGalleryButtonDown"));
		mButtonMore->setObjectName(QStringLiteral("SARibbonGalleryButtonMore"));
		mButtonUp->setMaximumWidth(sGalleryButtonMaximumWidth);
		mButtonDown->setMaximumWidth(sGalleryButtonMaximumWidth);
		mButtonMore->setMaximumWidth(sGalleryButtonMaximumWidth);
		mButtonUp->setIcon(ICON_ARROW_UP);
		mButtonDown->setIcon(ICON_ARROW_DOWN);
		mButtonMore->setIcon(ICON_ARROW_MORE);
		q_ptr->connect(mButtonUp, &QAbstractButton::clicked, q_ptr, &SARibbonGallery::pageUp);
		q_ptr->connect(mButtonDown, &QAbstractButton::clicked, q_ptr, &SARibbonGallery::pageDown);
		q_ptr->connect(mButtonMore, &QAbstractButton::clicked, q_ptr, &SARibbonGallery::showMoreDetail);
		// 信号转发
		q_ptr->connect(q_ptr, &SARibbonGallery::triggered, q_ptr, &SARibbonGallery::onTriggered);
		mPopupWidget          = nullptr;
		mCurrentViewportGroup = nullptr;
		mButtonLayout         = new QBoxLayout(QBoxLayout::TopToBottom);
		mButtonLayout->setSpacing(0);
		mButtonLayout->setContentsMargins(0, 0, 0, 0);
		mButtonLayout->addWidget(mButtonUp);
		mButtonLayout->addWidget(mButtonDown);
		mButtonLayout->addWidget(mButtonMore);
		QBoxLayout::Direction layoutDir = SA::saIsRTL() ? QBoxLayout::LeftToRight : QBoxLayout::RightToLeft;
		mLayout = new QBoxLayout(layoutDir);
		mLayout->setSpacing(0);
		mLayout->setContentsMargins(0, 0, 0, 0);
		mLayout->addLayout(mButtonLayout);
		mLayout->addStretch();
		q_ptr->setLayout(mLayout);
	}

	bool isValid() const
	{
		return (q_ptr != nullptr);
	}

	void createPopupWidget()
	{
		if (nullptr == mPopupWidget) {
			mPopupWidget = new SARibbonGalleryViewport(q_ptr);
		}
	}

	void setViewPort(const SARibbonGalleryGroup* v)
	{
		if (nullptr == mCurrentViewportGroup) {
			mCurrentViewportGroup = RibbonSubElementFactory->createRibbonGalleryGroup(q_ptr);
			mLayout->addWidget(mCurrentViewportGroup, 1);
		}

		mCurrentViewportGroup->setGalleryGroupStyle(v->galleryGroupStyle());
		mCurrentViewportGroup->setDisplayRow(v->displayRow());
		mCurrentViewportGroup->setSpacing(v->spacing());
		mCurrentViewportGroup->setGridMaximumWidth(v->gridMaximumWidth());
		mCurrentViewportGroup->setGridMinimumWidth(v->gridMinimumWidth());

		mCurrentViewportGroup->recalcGridSize(mCurrentViewportGroup->height());
		mCurrentViewportGroup->setHorizontalScrollBarPolicy(Qt::ScrollBarAlwaysOff);
		mCurrentViewportGroup->setVerticalScrollBarPolicy(Qt::ScrollBarAlwaysOff);
		mCurrentViewportGroup->setModel(v->model());
		mCurrentViewportGroup->show();
	}
};

// 静态变量初始化

/**
 * @brief SARibbonGalleryPrivate::sGalleryButtonMaximumWidth
 */
int SARibbonGallery::PrivateData::sGalleryButtonMaximumWidth = 15;

//===================================================
// SARibbonGalleryButton
//===================================================

SARibbonGalleryButton::SARibbonGalleryButton(QWidget* parent) : QToolButton(parent)
{
}

SARibbonGalleryButton::~SARibbonGalleryButton()
{
}
//===================================================
// SARibbonGalleryViewport
//===================================================

SARibbonGalleryViewport::SARibbonGalleryViewport(QWidget* parent) : QScrollArea(parent)
{
	setWindowFlags(Qt::Popup);
	setAttribute(Qt::WA_ShowWithoutActivating);
	m_contentWidget = new QWidget(this);
	m_layout        = new QVBoxLayout(m_contentWidget);
	m_sizeGrip      = new QSizeGrip(this);
	// 配置内容窗口
	m_contentWidget->setObjectName("contentWidget");
	m_contentWidget->setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Preferred);
	m_layout->setSpacing(0);
	m_layout->setContentsMargins(1, 1, 1, 20);  // 底部预留手柄空间
	setSizePolicy(QSizePolicy::Preferred, QSizePolicy::Preferred);

	// 配置滚动区域
	setWidget(m_contentWidget);
	setWidgetResizable(true);
	setHorizontalScrollBarPolicy(Qt::ScrollBarAlwaysOff);
	setVerticalScrollBarPolicy(Qt::ScrollBarAsNeeded);

	// 背景色
	QPalette pal = palette();
	pal.setBrush(QPalette::Window, pal.brush(QPalette::Base));
	setPalette(pal);
	m_contentWidget->setPalette(pal);  // 确保视口背景一致
	m_contentWidget->setAutoFillBackground(true);

	// 配置尺寸手柄
	m_sizeGrip->setFixedSize(16, 16);
	m_sizeGrip->setCursor(Qt::SizeFDiagCursor);

	// setCornerWidget(m_sizeGrip);会在滚动条隐藏时也隐藏，导致非滚动条状态无法改变尺寸
	m_sizeGrip->setParent(viewport());  // 手柄挂在 viewport 上
	m_sizeGrip->setFixedSize(16, 16);
	m_sizeGrip->setCursor(Qt::SizeFDiagCursor);
	m_sizeGrip->raise();  // 保证在最上层
}

/**
 * @brief 添加窗口不带标题
 * @param w
 */
void SARibbonGalleryViewport::addWidget(QWidget* w)
{
	if (!w) {
		return;
	}
	w->setParent(m_contentWidget);
	w->setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Maximum);
	m_layout->addWidget(w);
	// 确保新添加的部件可见
	QCoreApplication::postEvent(this, new QEvent(QEvent::LayoutRequest));
}

/**
 * @brief 添加窗口，带标题
 * @param w
 * @param title
 */
void SARibbonGalleryViewport::addWidget(QWidget* w, const QString& title)
{
	if (!w) {
		return;
	}

	// 创建标题标签
	QLabel* label = new QLabel(title, m_contentWidget);
	label->setObjectName("titleLabel");
	label->setAlignment(Qt::AlignLeft | Qt::AlignVCenter);
	label->setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Maximum);

	// 添加到布局
	if (w->parentWidget() != m_contentWidget) {
		w->setParent(m_contentWidget);
	}
	w->setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Preferred);
	m_layout->addWidget(label);
	m_layout->addWidget(w);

	// 记录映射关系
	m_widgetToTitleLabel[ w ] = label;

	// 确保新添加的部件可见
	QCoreApplication::postEvent(this, new QEvent(QEvent::LayoutRequest));
}

/**
 * @brief 通过SARibbonGalleryGroup获取对应的标题，用户可以通过此函数设置QLabel的属性
 * @param g
 * @return 如果没有管理group，将返回nullptr
 */
QLabel* SARibbonGalleryViewport::titleLabel(QWidget* w)
{
	return m_widgetToTitleLabel.value(w, nullptr);
}

/**
 * @brief 移除窗口
 * @param w
 */
void SARibbonGalleryViewport::removeWidget(QWidget* w)
{
	if (!w) {
		return;
	}

	// 移除标题标签（如果存在）
	if (QLabel* label = m_widgetToTitleLabel.take(w)) {
		m_layout->removeWidget(label);
		label->deleteLater();
	}

	// 移除主窗口
	m_layout->removeWidget(w);
	w->setParent(nullptr);  // 断开父子关系，由调用者管理

	// 触发布局更新
	QCoreApplication::postEvent(this, new QEvent(QEvent::LayoutRequest));
}

QList< SARibbonGalleryGroup* > SARibbonGalleryViewport::galleryGroupList() const
{
	return m_contentWidget->findChildren< SARibbonGalleryGroup* >(QString(), Qt::FindDirectChildrenOnly);
}

int SARibbonGalleryViewport::galleryHeight() const
{
	if (parent()) {
		if (SARibbonGallery* gallery = qobject_cast< SARibbonGallery* >(parent())) {
			return gallery->height();
		}
	}
	return 40;
}

/**
 * @brief 根据宽度计算高度推荐值
 * @param w
 * @return
 */
int SARibbonGalleryViewport::heightHintForWidth(int w) const
{
	const QList< QWidget* > cws = m_contentWidget->findChildren< QWidget* >(QString(), Qt::FindDirectChildrenOnly);
	int h                       = 0;
	for (QWidget* widget : cws) {
		if (widget->hasHeightForWidth()) {
			h += widget->heightForWidth(w);
		} else {
			h += widget->sizeHint().height();
		}
	}
	QMargins mag = m_layout->contentsMargins();
	// 最后加上margin
	h += mag.top();
	h += mag.bottom();
	h += frameWidth() * 2;
	// 再加2是为了留2px余量避免显示滚动条
	return h + 2;
}

/**
 * @brief widget的标题改变
 * @param g
 * @param title
 */
void SARibbonGalleryViewport::onTitleChanged(QWidget* w, const QString& title)
{
	if (QLabel* label = titleLabel(w)) {
		label->setText(title);
	}
}

bool SARibbonGalleryViewport::eventFilter(QObject* o, QEvent* e)
{
	if (e->type() == QEvent::MouseButtonPress) {
		auto* me = static_cast< QMouseEvent* >(e);
		if (isVisible()) {  // 一定要加是否可见
			if (!rect().contains(mapFromGlobal(SA::compat::eventGlobalPos(me)))) {
				close();
				return true;  // 防止继续传递
			}
		}
	}
	return QScrollArea::eventFilter(o, e);
}

void SARibbonGalleryViewport::showEvent(QShowEvent* e)
{
	QScrollArea::showEvent(e);
	qApp->installEventFilter(this);  // 监听整个应用的所有事件
}

void SARibbonGalleryViewport::hideEvent(QHideEvent* e)
{
	QScrollArea::hideEvent(e);
	qApp->removeEventFilter(this);  // 隐藏后不监听,避免监听太多
}

/**
 * \if ENGLISH
 * @brief Handle resize events for the gallery viewport
 * @param e Resize event
 * @details Updates the position of the size grip, positioning it at bottom-right for LTR and bottom-left for RTL.
 * \endif
 *
 * \if CHINESE
 * @brief 处理图库视口的调整大小事件
 * @param e 调整大小事件
 * @details 更新尺寸手柄的位置，LTR时位于右下角，RTL时位于左下角。
 * \endif
 */
void SARibbonGalleryViewport::resizeEvent(QResizeEvent* e)
{
	QScrollArea::resizeEvent(e);
	const int gripSize = 16;
	int x;
	if (SA::saIsRTL()) {
		x = 0;
	} else {
		x = viewport()->width() - gripSize;
	}
	const int y = viewport()->height() - gripSize;
	m_sizeGrip->move(x, y);
	m_sizeGrip->raise();  // 再保险一次
}

//===================================================
// SARibbonGallery
//===================================================

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonGallery
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGallery构造函数
 * @param parent 父窗口部件
 * \endif
 */
SARibbonGallery::SARibbonGallery(QWidget* parent) : QFrame(parent), d_ptr(new SARibbonGallery::PrivateData(this))
{
	d_ptr->init();
	setFrameShape(QFrame::Box);
	setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Expanding);
	setMinimumWidth(200);
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonGallery
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonGallery析构函数
 * \endif
 */
SARibbonGallery::~SARibbonGallery()
{
}

/**
 * \if ENGLISH
 * @brief Get a blank SARibbonGalleryGroup
 * @return Pointer to the created SARibbonGalleryGroup
 * @details Creates and returns a new empty gallery group. The group is automatically added to the gallery.
 * \endif
 *
 * \if CHINESE
 * @brief 获取一个空白SARibbonGalleryGroup
 * @return 创建的SARibbonGalleryGroup指针
 * @details 创建并返回一个新的空白图库组。该组会自动添加到图库中。
 * \endif
 */
SARibbonGalleryGroup* SARibbonGallery::addGalleryGroup()
{
	SARibbonGalleryGroup* group = RibbonSubElementFactory->createRibbonGalleryGroup(this);
	addGalleryGroup(group);
	return (group);
}

/**
 * @brief 添加一组SARibbonGalleryGroup
 * @param group
 */
/**
 * \if ENGLISH
 * @brief Add an existing SARibbonGalleryGroup to the gallery
 * @param group Pointer to the gallery group to add
 * @details Adds a pre-existing gallery group to the gallery. The group will be displayed in the popup viewport.
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个已存在的SARibbonGalleryGroup到图库
 * @param group 要添加的图库组指针
 * @details 将已存在的图库组添加到图库中。该组将在弹出视口中显示。
 * \endif
 */
void SARibbonGallery::addGalleryGroup(SARibbonGalleryGroup* group)
{
	group->setSizePolicy(QSizePolicy::Preferred, QSizePolicy::Preferred);
	SARibbonGalleryViewport* viewport = ensureGetPopupViewPort();
	viewport->addWidget(group, group->groupTitle());
	connect(group, &QAbstractItemView::clicked, this, &SARibbonGallery::onItemClicked);
	connect(group, &SARibbonGalleryGroup::groupTitleChanged, this, [ group, viewport ](const QString& t) {
		viewport->onTitleChanged(group, t);
	});
	connect(group, &SARibbonGalleryGroup::triggered, this, &SARibbonGallery::triggered);
	connect(group, &SARibbonGalleryGroup::hovered, this, &SARibbonGallery::hovered);
	if (!currentViewGroup()) {
		// 当前没有viewgroup才设置
		setCurrentViewGroup(group);
	}
}

/**
 * @brief 添加一组actions
 * @param title actions组的名字
 * @param actions
 * @return 返回SARibbonGalleryGroup，用户可以通过修改SARibbonGalleryGroup属性控制其显示方法
 */
/**
 * \if ENGLISH
 * @brief Quickly add a set of actions as a gallery group
 * @param title Title for the gallery group
 * @param actions List of actions to add to the group
 * @return Pointer to the created SARibbonGalleryGroup
 * @details Creates a new gallery group with the specified title and adds the provided actions to it.
 * \endif
 *
 * \if CHINESE
 * @brief 快速添加一组动作作为图库组
 * @param title 图库组标题
 * @param actions 要添加到组中的动作列表
 * @return 创建的SARibbonGalleryGroup指针
 * @details 创建一个具有指定标题的新图库组，并将提供的动作添加到其中。
 * \endif
 */
SARibbonGalleryGroup* SARibbonGallery::addCategoryActions(const QString& title, QList< QAction* > actions)
{
	SARibbonGalleryGroup* group = RibbonSubElementFactory->createRibbonGalleryGroup(this);
	if (!title.isEmpty()) {
		group->setGroupTitle(title);
	}
	group->addActionItemList(actions);
	addGalleryGroup(group);
	return (group);
}

/**
 * \if ENGLISH
 * @brief Set the currently displayed gallery group
 * @param group Pointer to the gallery group to display
 * @details Sets which gallery group should be displayed in the main gallery view (not the popup).
 * \endif
 *
 * \if CHINESE
 * @brief 设置当前显示的图库组
 * @param group 要显示的图库组指针
 * @details 设置哪个图库组应该在主图库视图中显示（非弹出窗口）。
 * \endif
 */
void SARibbonGallery::setCurrentViewGroup(SARibbonGalleryGroup* group)
{
	d_ptr->setViewPort(group);
	QApplication::postEvent(this, new QResizeEvent(size(), size()));
}

/**
 * \if ENGLISH
 * @brief Get the currently displayed gallery group
 * @return Pointer to the current gallery group, or nullptr if none is set
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前显示的图库组
 * @return 当前图库组指针，如果未设置则返回nullptr
 * \endif
 */
SARibbonGalleryGroup* SARibbonGallery::currentViewGroup() const
{
	return (d_ptr->mCurrentViewportGroup);
}

/**
 * @brief 获取弹出窗口
 * @return
 */
/**
 * \if ENGLISH
 * @brief Get the popup viewport widget
 * @return Pointer to the popup viewport, or nullptr if not created yet
 * @details The popup viewport displays all gallery groups when the "more" button is clicked.
 * \endif
 *
 * \if CHINESE
 * @brief 获取弹出视口部件
 * @return 弹出视口指针，如果尚未创建则返回nullptr
 * @details 弹出视口在点击"更多"按钮时显示所有图库组。
 * \endif
 */
SARibbonGalleryViewport* SARibbonGallery::getPopupViewPort() const
{
	return d_ptr->mPopupWidget;
}

/**
 * @brief 设置最右边三个控制按钮的最大宽度（默认15）
 * @param w
 */
/**
 * \if ENGLISH
 * @brief Set the maximum width for gallery control buttons
 * @param w Maximum width in pixels
 * @details Sets the maximum width for the up, down, and more buttons on the gallery.
 * \endif
 *
 * \if CHINESE
 * @brief 设置图库控制按钮的最大宽度
 * @param w 最大宽度（像素）
 * @details 设置图库上、下和更多按钮的最大宽度。
 * \endif
 */
void SARibbonGallery::setGalleryButtonMaximumWidth(int w)
{
	SARibbonGallery::PrivateData::sGalleryButtonMaximumWidth = w;
}

/**
 * \if ENGLISH
 * @brief Set single-row display mode for gallery
 * @param on True to enable single-row mode, false to restore normal mode
 * @details In single-row mode, the scroll up/down buttons are hidden and only the dropdown
 *          "more" button remains visible. The button layout direction changes from vertical
 *          (TopToBottom) to horizontal (LeftToRight or RightToLeft depending on layout direction),
 *          so the dropdown button sits beside the gallery content instead of stacked vertically.
 *          When disabled, all three buttons are shown and the layout direction is restored to
 *          TopToBottom.
 * \endif
 *
 * \if CHINESE
 * @brief 设置图库的单行显示模式
 * @param on true启用单行模式，false恢复普通模式
 * @details 在单行模式下，上翻和下翻按钮被隐藏，仅保留下拉"更多"按钮可见。
 *          按钮布局方向从垂直（TopToBottom）变为水平（LeftToRight或RightToLeft，取决于布局方向），
 *          使下拉按钮与图库内容并排显示而非垂直堆叠。
 *          禁用时，恢复显示三个按钮并将布局方向恢复为TopToBottom。
 * \endif
 */
void SARibbonGallery::setSingleRowMode(bool on)
{
	d_ptr->mSingleRowMode = on;
	if (on) {
		d_ptr->mButtonUp->setVisible(false);
		d_ptr->mButtonDown->setVisible(false);
		d_ptr->mButtonMore->setVisible(true);
		QBoxLayout::Direction buttonDir = SA::saIsRTL() ? QBoxLayout::RightToLeft : QBoxLayout::LeftToRight;
		d_ptr->mButtonLayout->setDirection(buttonDir);
	} else {
		d_ptr->mButtonUp->setVisible(true);
		d_ptr->mButtonDown->setVisible(true);
		d_ptr->mButtonMore->setVisible(true);
		d_ptr->mButtonLayout->setDirection(QBoxLayout::TopToBottom);
	}
	d_ptr->mButtonLayout->update();
}

/**
 * \if ENGLISH
 * @brief Check if gallery is in single-row mode
 * @return True if single-row mode is active, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查图库是否处于单行模式
 * @return 如果单行模式激活则返回true，否则返回false
 * \endif
 */
bool SARibbonGallery::isSingleRowMode() const
{
	return d_ptr->mSingleRowMode;
}

/**
 * @brief 上翻页
 */
/**
 * \if ENGLISH
 * @brief Scroll down one page in the current gallery group
 * @details Scrolls the current gallery group view down by one page (if a scrollbar is present).
 * \endif
 *
 * \if CHINESE
 * @brief 在当前图库组中向下滚动一页
 * @details 将当前图库组视图向下滚动一页（如果存在滚动条）。
 * \endif
 */
void SARibbonGallery::pageDown()
{
	if (d_ptr->mCurrentViewportGroup) {
		QScrollBar* vscrollBar = d_ptr->mCurrentViewportGroup->verticalScrollBar();
		if (!vscrollBar) {
			return;
		}
		int v = vscrollBar->value();
		v += vscrollBar->singleStep();
		vscrollBar->setValue(v);
	}
}

/**
 * @brief 下翻页
 */
/**
 * \if ENGLISH
 * @brief Scroll up one page in the current gallery group
 * @details Scrolls the current gallery group view up by one page (if a scrollbar is present).
 * \endif
 *
 * \if CHINESE
 * @brief 在当前图库组中向上滚动一页
 * @details 将当前图库组视图向上滚动一页（如果存在滚动条）。
 * \endif
 */
void SARibbonGallery::pageUp()
{
	if (d_ptr->mCurrentViewportGroup) {
		QScrollBar* vscrollBar = d_ptr->mCurrentViewportGroup->verticalScrollBar();
		if (!vscrollBar) {
			return;
		}
		int v = vscrollBar->value();
		v -= vscrollBar->singleStep();
		vscrollBar->setValue(v);
	}
}

/**
 * @brief 显示更多触发，默认弹出内部管理的SARibbonGalleryViewport，用户可重载此函数实现自定义的弹出
 */
/**
 * \if ENGLISH
 * @brief Show the popup viewport with all gallery groups
 * @details Displays the popup viewport containing all gallery groups when the "more" button is clicked.
 *          Users can override this function to implement custom popup behavior.
 * \endif
 *
 * \if CHINESE
 * @brief 显示包含所有图库组的弹出视口
 * @details 当点击"更多"按钮时，显示包含所有图库组的弹出视口。
 *          用户可以重写此函数以实现自定义的弹出行为。
 * \endif
 */
void SARibbonGallery::showMoreDetail()
{
	if (Q_UNLIKELY(!d_ptr->mPopupWidget))
		return;

	const QPoint start = mapToGlobal(QPoint(0, 0));
	const QScreen* scr = QGuiApplication::screenAt(start);
	if (!scr)  // 罕见情况：跨屏拖动后原屏幕消失
		scr = QGuiApplication::primaryScreen();
	const QRect avail = scr->availableGeometry();  // 去掉任务栏/Dock

	int w = width();
	if (d_ptr->mCurrentViewportGroup)
		w = d_ptr->mCurrentViewportGroup->width();
	w += style()->pixelMetric(QStyle::PM_ScrollBarExtent);

	/* ---- 宽度不超过屏幕右边缘 ---- */
	w = qMin(w, avail.right() - start.x() + 1);
	w = qMax(w, 1);  // 保险：至少 1 px

	/* ---- 计算期望高度 ---- */
	int h = d_ptr->mPopupWidget->heightHintForWidth(w);

	/* ---- 高度不超过屏幕底部 ---- */
	int maxH = avail.bottom() - start.y() + 1;
	if (h > maxH) {
		h = maxH;
	}

	d_ptr->mPopupWidget->setGeometry(start.x(), start.y(), w, h);
	d_ptr->mPopupWidget->show();
}

/**
 * \if ENGLISH
 * @brief Slot called when an item in a gallery group is clicked
 * @param index Model index of the clicked item
 * @details Handles item click events from gallery groups and triggers the corresponding action.
 * \endif
 *
 * \if CHINESE
 * @brief 当图库组中的项目被点击时调用的槽函数
 * @param index 被点击项目的模型索引
 * @details 处理来自图库组的项目点击事件并触发相应的动作。
 * \endif
 */
void SARibbonGallery::onItemClicked(const QModelIndex& index)
{
	QObject* obj                = sender();
	SARibbonGalleryGroup* group = qobject_cast< SARibbonGalleryGroup* >(obj);
	if (group) {
		setCurrentViewGroup(group);
		SARibbonGalleryGroup* curGroup = currentViewGroup();
		curGroup->scrollTo(index);
		curGroup->setCurrentIndex(index);
	}
}

/**
 * \if ENGLISH
 * @brief Slot called when an action is triggered from the gallery
 * @param action Pointer to the triggered action
 * @details Handles action trigger events and closes the popup viewport if it's open.
 * \endif
 *
 * \if CHINESE
 * @brief 当从图库触发动作时调用的槽函数
 * @param action 被触发动作的指针
 * @details 处理动作触发事件，并在弹出视口打开时关闭它。
 * \endif
 */
void SARibbonGallery::onTriggered(QAction* action)
{
	Q_UNUSED(action);
	// 点击后关闭弹出窗口
	if (d_ptr->mPopupWidget) {
		if (d_ptr->mPopupWidget->isVisible()) {
			d_ptr->mPopupWidget->hide();
		}
	}
}

SARibbonGalleryViewport* SARibbonGallery::ensureGetPopupViewPort()
{
	if (nullptr == d_ptr->mPopupWidget) {
		d_ptr->createPopupWidget();
	}
	return (d_ptr->mPopupWidget);
}

/**
 * \if ENGLISH
 * @brief Handle resize events for the gallery
 * @param event Resize event
 * @details Updates the layout of gallery groups when the gallery is resized.
 * \endif
 *
 * \if CHINESE
 * @brief 处理图库的调整大小事件
 * @param event 调整大小事件
 * @details 在图库调整大小时更新图库组的布局。
 * \endif
 */
void SARibbonGallery::resizeEvent(QResizeEvent* event)
{
	QFrame::resizeEvent(event);
	// 对SARibbonGalleryViewport所有SARibbonGalleryGroup重置尺寸
	int h = layout()->contentsRect().height();
	if (d_ptr->mCurrentViewportGroup) {
		h = d_ptr->mCurrentViewportGroup->height();
		d_ptr->mCurrentViewportGroup->recalcGridSize();
	}
	// 尺寸变化后必须重新计算gridsize
	if (d_ptr->mPopupWidget) {
		const QList< SARibbonGalleryGroup* > groups = d_ptr->mPopupWidget->galleryGroupList();
		for (SARibbonGalleryGroup* group : groups) {
			group->recalcGridSize(h);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Handle change events for the gallery
 * @param event Change event
 * @details Updates the layout direction when layout direction changes.
 * \endif
 *
 * \if CHINESE
 * @brief 处理图库的更改事件
 * @param event 更改事件
 * @details 在布局方向更改时更新布局方向。
 * \endif
 */
void SARibbonGallery::changeEvent(QEvent* event)
{
	QFrame::changeEvent(event);
	if (event->type() == QEvent::LayoutDirectionChange) {
		QBoxLayout::Direction layoutDir = SA::saIsRTL() ? QBoxLayout::LeftToRight : QBoxLayout::RightToLeft;
		d_ptr->mLayout->setDirection(layoutDir);
		if (d_ptr->mSingleRowMode) {
			QBoxLayout::Direction buttonDir = SA::saIsRTL() ? QBoxLayout::RightToLeft : QBoxLayout::LeftToRight;
			d_ptr->mButtonLayout->setDirection(buttonDir);
			d_ptr->mButtonLayout->update();
		}
		if (d_ptr->mPopupWidget) {
			d_ptr->mPopupWidget->update();
		}
	}
}

/*** End of inlined file: SARibbonGallery.cpp ***/


/*** Start of inlined file: SARibbonBar.cpp ***/
#include <QPointer>
#include <QAction>
#include <QApplication>
#include <QDebug>
#include <QHoverEvent>
#include <QLinearGradient>
#include <QPainter>
#include <QResizeEvent>
#include <QSet>
#include <QStyleOptionMenuItem>
#include <QTimer>
#include <QVariant>
#include <QDateTime>

#ifndef SARIBBONBAR_DEBUG_PRINT
#define SARIBBONBAR_DEBUG_PRINT 0
#endif
#if SARIBBONBAR_DEBUG_PRINT
#ifndef SARIBBONBAR_HELP_DRAW_RECT
#define SARIBBONBAR_HELP_DRAW_RECT(p, rect)                                                                            \
	do {                                                                                                               \
		p.save();                                                                                                      \
		QPen _pen(Qt::DashDotDotLine);                                                                                 \
		_pen.setColor(QColor(219, 26, 59));                                                                            \
		p.setPen(_pen);                                                                                                \
		p.setBrush(QBrush());                                                                                          \
		p.drawRect(rect);                                                                                              \
		p.restore();                                                                                                   \
	} while (0)
#endif
#endif
class _SAContextCategoryManagerData
{
public:
	SARibbonContextCategory* contextCategory;
	QList< int > tabPageIndex;
	bool operator==(const SARibbonContextCategory* contextPage)
	{
		return (this->contextCategory == contextPage);
	}
};

/**
 * @brief 隐藏category时保存的数据
 */
class _SARibbonHiddenCategoryData
{
public:
	SARibbonCategory* category { nullptr };
	int originalIndex { -1 };  ///< 隐藏前的tab索引，用于恢复显示时的位置参考
};

class SARibbonBar::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonBar)
public:
	QPointer< QAbstractButton > mApplicationButton;
	QPointer< SARibbonTabBar > mRibbonTabBar;
	QPointer< SARibbonStackedWidget > mStackedContainerWidget;
	QPointer< SARibbonButtonGroupWidget > mRightButtonGroup;  ///< 在tab bar右边的按钮群
	QPointer< SARibbonQuickAccessBar > mQuickAccessBar;       ///< 快速响应栏
	QPointer< SARibbonTitleIconWidget > mTitleIconWidget;     ///< 标题栏图标

	QAction* mMinimumCategoryButtonAction { nullptr };  ///< 隐藏面板按钮action
	QList< _SAContextCategoryManagerData > mCurrentShowingContextCategory;
	QList< SARibbonContextCategory* > mContextCategoryList;  ///< 存放所有的上下文标签
	QList< _SARibbonHiddenCategoryData > mHidedCategory;
	qint64 mTabBarLastClickTime { 0 };  ///< 记录tabbar点击的上次点击时间戳，tabbar无法区分双击单击，双击必定触发单击，因此需要此把快速单击去掉
	SARibbonBar::RibbonStyles mRibbonStyle { SARibbonBar::RibbonStyleLooseThreeRow };  ///< ribbon的风格
	SARibbonBar::RibbonMode mCurrentRibbonMode { SARibbonBar::NormalRibbonMode };      ///< 记录当前模式
	QList< QColor > mContextCategoryColorList;                                         ///< contextCategory的色系
	int mContextCategoryColorListIndex { -1 };                                         ///< 记录contextCategory色系索引
	QColor mTitleTextColor;  ///< 标题文字颜色,默认无效，无效的情况下和SARibbonBar的qss:color属性一致
	QColor mTabBarBaseLineColor { QColor(186, 201, 219) };                ///< tabbar 底部会绘制一条线条，定义线条颜色
	QColor mContextCategoryTitleTextColor { Qt::black };                  ///< 记录Context category的标题字体颜色
	Qt::Alignment mTitleAlignment { Qt::AlignCenter };                     ///< 标题对齐方式
	bool mIsTitleVisible { true };                                        ///< 标题是否显示
	QBrush mTitleBackgroundBrush { Qt::NoBrush };                         ///< 标题的背景颜色
	SARibbonAlignment mRibbonAlignment { SARibbonAlignment::AlignLeft };  ///< 对齐方式
	SARibbonPanel::PanelLayoutMode mDefaultPanelLayoutMode { SARibbonPanel::ThreeRowMode };  ///< 默认的PanelLayoutMode
	bool mEnableShowPanelTitle { true };                         ///< 是否允许panel的标题栏显示
	bool mEnableIconRightText { false };                         ///< 鏄惁鍚敤鍥炬爣鍙充晶鏂囧瓧妯″紡
	int mPanelSpacing { 0 };                                     ///< panel的spacing
	QSize mPanelSmallToolButtonSize { 20, 20 };                  ///< 记录panel的默认图标大小
	QSize mPanelLargeToolButtonSize { 32, 32 };                  ///< 记录panel的大图标尺寸
	SARibbonMainWindowStyles mMainWindowStyle;                   ///< 记录MainWindow的样式
	FpContextCategoryHighlight mFpContextHighlight { nullptr };  ///< 上下文标签高亮
	bool mEnableTabDoubleClickToMinimumMode { true };            ///< 是否允许tab双击激活ribbon的最小化模式
	bool mEnableWordWrap { true };                               ///< 是否允许文字换行
	qreal buttonMaximumAspectRatio { 1.4 };                      ///< 按钮的最大宽高比
public:
	PrivateData(SARibbonBar* par) : q_ptr(par)
	{
		mContextCategoryColorList = SARibbonBar::defaultContextCategoryColorList();
		mFpContextHighlight       = [](const QColor& c) -> QColor { return SA::makeColorVibrant(c); };
	}
	void init();
	// 创建QuickAccessBar
	SARibbonQuickAccessBar* createQuickAccessBar();
	// 创建右边工具栏
	SARibbonButtonGroupWidget* createRightButton();
	// 创建标题栏图标
	SARibbonTitleIconWidget* createTitleIconWidget();
	// 创建一个默认的ApplicationButton
	void createDefaultApplicationButton();
	// 设置一个ApplicationButton
	void setApplicationButton(QAbstractButton* btn);

	bool isContainContextCategoryInList(SARibbonContextCategory* contextCategory);

	void setMinimumMode();

	void setNormalMode();

	QColor getContextCategoryColor();

	void updateTabData();
	// 从tabData获取category指针
	SARibbonCategory* categoryFromTab(int tabIndex) const;
	// 设置tab的category数据
	void setCategoryToTab(int tabIndex, SARibbonCategory* category);
	// 根据category指针查找tab索引，O(n)
	int tabIndexForCategory(const SARibbonCategory* category) const;
	// 根据category在stacked widget中的顺序，计算正确的tab插入位置
	int calcCategoryInsertTabIndex(SARibbonCategory* category) const;
	// 是否使用ribbon边框，如果不是，将影响布局方式
	bool isUseRibbonFrame() const;
	// 计算上下文标签的标题矩形，矩形位置相对于SARibbonBar
	QRect calcContextCategoryTitleRect(const _SAContextCategoryManagerData& contextData);

	// 重新布局
	void relayout();

	// 初始化新的父对象
	void initNewParent(QWidget* par);
};

void SARibbonBar::PrivateData::init()
{
	q_ptr->setNativeMenuBar(false);

	createDefaultApplicationButton();
	mRibbonTabBar = RibbonSubElementFactory->createRibbonTabBar(q_ptr);
	mRibbonTabBar->setObjectName(QStringLiteral("objSARibbonTabBar"));
	mRibbonTabBar->setDrawBase(false);
	q_ptr->connect(mRibbonTabBar.data(), &QTabBar::currentChanged, q_ptr, &SARibbonBar::onCurrentRibbonTabChanged);
	q_ptr->connect(mRibbonTabBar.data(), &QTabBar::tabBarClicked, q_ptr, &SARibbonBar::onCurrentRibbonTabClicked);
	q_ptr->connect(mRibbonTabBar.data(), &QTabBar::tabBarDoubleClicked, q_ptr, &SARibbonBar::onCurrentRibbonTabDoubleClicked);
	q_ptr->connect(mRibbonTabBar.data(), &QTabBar::tabMoved, q_ptr, &SARibbonBar::onTabMoved);
	//
	mStackedContainerWidget = RibbonSubElementFactory->createRibbonStackedWidget(q_ptr);
	mStackedContainerWidget->setObjectName(QStringLiteral("objSARibbonStackedContainerWidget"));
	q_ptr->connect(mStackedContainerWidget.data(), &SARibbonStackedWidget::hidWindow, q_ptr, &SARibbonBar::onStackWidgetHided);
	// 捕获事件，在popmode时必须用到
	mStackedContainerWidget->installEventFilter(q_ptr);
	// 快速工具栏
	createQuickAccessBar();
	// 右侧工具栏
	createRightButton();
	// 创建图标窗口
	createTitleIconWidget();
	setNormalMode();

	SARibbonBarLayout* lay = new SARibbonBarLayout(q_ptr);
	q_ptr->setLayout(lay);
	q_ptr->setRibbonStyle(RibbonStyleLooseThreeRow);
	q_ptr->ensurePolished();
}

SARibbonQuickAccessBar* SARibbonBar::PrivateData::createQuickAccessBar()
{
	mQuickAccessBar = RibbonSubElementFactory->createQuickAccessBar(q_ptr);
	mQuickAccessBar->setObjectName(QStringLiteral("objSARibbonQuickAccessBar"));
	mQuickAccessBar->setIconSize(QSize(22, 22));
	return mQuickAccessBar.data();
}

SARibbonButtonGroupWidget* SARibbonBar::PrivateData::createRightButton()
{
	mRightButtonGroup = RibbonSubElementFactory->createButtonGroupWidget(q_ptr);
	mRightButtonGroup->setObjectName(QStringLiteral("objSARibbonRightButtonGroup"));
	mRightButtonGroup->setIconSize(QSize(22, 22));
	return mRightButtonGroup.data();
}

SARibbonTitleIconWidget* SARibbonBar::PrivateData::createTitleIconWidget()
{
	mTitleIconWidget = RibbonSubElementFactory->createRibbonTitleIconWidget(q_ptr);
	return mTitleIconWidget.data();
}

void SARibbonBar::PrivateData::createDefaultApplicationButton()
{
	setApplicationButton(RibbonSubElementFactory->createRibbonApplicationButton(q_ptr));
}

void SARibbonBar::PrivateData::setApplicationButton(QAbstractButton* btn)
{
	if (mApplicationButton) {
		mApplicationButton->hide();
		mApplicationButton->deleteLater();
	}
	if (btn) {
		if (btn->parent() != q_ptr) {
			btn->setParent(q_ptr);
		}
		if (btn->objectName().isEmpty()) {
			btn->setObjectName(QStringLiteral("SARibbonApplicationButton"));
		}
		btn->setVisible(true);
		btn->show();
		q_ptr->connect(btn, &QAbstractButton::clicked, q_ptr, &SARibbonBar::applicationButtonClicked);
	}
	mApplicationButton = btn;
}

bool SARibbonBar::PrivateData::isContainContextCategoryInList(SARibbonContextCategory* contextCategory)
{
	for (int i = 0; i < mCurrentShowingContextCategory.size(); ++i) {
		if (mCurrentShowingContextCategory[ i ] == contextCategory) {
			return (true);
		}
	}
	return (false);
}

/**
 * @brief 设置最小模式
 */
void SARibbonBar::PrivateData::setMinimumMode()
{
	mCurrentRibbonMode = SARibbonBar::MinimumRibbonMode;
	mStackedContainerWidget->setPopupMode();
	mStackedContainerWidget->setFocusPolicy(Qt::NoFocus);
	mStackedContainerWidget->clearFocus();
	mRibbonTabBar->setFocus();
	mStackedContainerWidget->hide();
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(q_ptr->layout())) {
		lay->resetSize();
		lay->layoutStackedContainerWidget();
	}
}

/**
 * @brief 设置正常模式
 */
void SARibbonBar::PrivateData::setNormalMode()
{
	mCurrentRibbonMode = SARibbonBar::NormalRibbonMode;
	mStackedContainerWidget->setNormalMode();
	mStackedContainerWidget->setFocus();
	mStackedContainerWidget->show();
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(q_ptr->layout())) {
		lay->resetSize();
		lay->layoutStackedContainerWidget();
	}
}

/**
 * @brief 获取ContextCategory的color
 * @return
 */
QColor SARibbonBar::PrivateData::getContextCategoryColor()
{
	if (mContextCategoryColorList.isEmpty()) {
		mContextCategoryColorListIndex = -1;
		return (QColor());
	}
	++mContextCategoryColorListIndex;
	if ((mContextCategoryColorListIndex >= mContextCategoryColorList.size()) || (mContextCategoryColorListIndex < 0)) {
		mContextCategoryColorListIndex = 0;
	}
	return (mContextCategoryColorList.at(mContextCategoryColorListIndex));
}

/**
 * @brief 更新上下文标签的tab索引信息
 *
 * 当tab发生增删移动时，上下文标签管理的tab索引会失效，需要重新刷新
 */
void SARibbonBar::PrivateData::updateTabData()
{
	int tabcount = mRibbonTabBar->count();
	// 刷新ContextCategory的tab索引信息
	for (_SAContextCategoryManagerData& cd : mCurrentShowingContextCategory) {
		cd.tabPageIndex.clear();
		for (int i = 0; i < cd.contextCategory->categoryCount(); ++i) {
			SARibbonCategory* category = cd.contextCategory->categoryPage(i);
			int foundIndex             = tabIndexForCategory(category);
			cd.tabPageIndex.append(foundIndex);
		}
	}
}

/**
 * @brief 从tabData中获取SARibbonCategory指针
 * @param tabIndex tab索引
 * @return category指针，如果无效返回nullptr
 */
SARibbonCategory* SARibbonBar::PrivateData::categoryFromTab(int tabIndex) const
{
	QVariant var = mRibbonTabBar->tabData(tabIndex);
	if (var.isValid()) {
		return reinterpret_cast< SARibbonCategory* >(var.value< quintptr >());
	}
	return nullptr;
}

/**
 * @brief 将category指针存入tab的tabData中
 * @param tabIndex tab索引
 * @param category category指针
 */
void SARibbonBar::PrivateData::setCategoryToTab(int tabIndex, SARibbonCategory* category)
{
	mRibbonTabBar->setTabData(tabIndex, QVariant::fromValue(reinterpret_cast< quintptr >(category)));
}

/**
 * @brief 根据category指针查找对应的tab索引
 * @param category category指针
 * @return tab索引，未找到返回-1
 */
int SARibbonBar::PrivateData::tabIndexForCategory(const SARibbonCategory* category) const
{
	int tabcount = mRibbonTabBar->count();
	for (int i = 0; i < tabcount; ++i) {
		if (categoryFromTab(i) == category) {
			return i;
		}
	}
	return -1;
}

/**
 * @brief 根据category在stacked widget中的顺序，计算正确的tab插入位置
 *
 * 当批量隐藏/显示category时，原先保存的originalIndex可能已不正确（因为其他tab也被移除/插入了）。
 * 此方法通过stacked widget的顺序（即category的逻辑顺序）来计算正确的插入位置。
 * @param category 要插入的category
 * @return 应该插入的tab索引
 */
int SARibbonBar::PrivateData::calcCategoryInsertTabIndex(SARibbonCategory* category) const
{
	int stackIndex = mStackedContainerWidget->indexOf(category);
	if (stackIndex < 0) {
		return mRibbonTabBar->count();
	}
	// 遍历stacked widget中在此category之前的所有widget
	// 找到最后一个当前可见（有tab）的category，插入位置为其tab索引+1
	int insertPos = 0;
	for (int i = 0; i < stackIndex; ++i) {
		SARibbonCategory* c = qobject_cast< SARibbonCategory* >(mStackedContainerWidget->widget(i));
		if (c) {
			int tabIdx = tabIndexForCategory(c);
			if (tabIdx >= 0) {
				insertPos = tabIdx + 1;
			}
		}
	}
	return insertPos;
}

/**
 * @brief 获取当前tabbar的样式
 */
bool SARibbonBar::PrivateData::isUseRibbonFrame() const
{
	return mMainWindowStyle.testFlag(SARibbonMainWindowStyleFlag::UseRibbonFrame);
}

QRect SARibbonBar::PrivateData::calcContextCategoryTitleRect(const _SAContextCategoryManagerData& contextData)
{
	QRect contextTitleRect;
	SARibbonTabBar* ribbonTabBar = mRibbonTabBar.data();
	const QMargins border        = q_ptr->contentsMargins();
	const QList< int >& indexs   = contextData.tabPageIndex;
	if (!indexs.isEmpty()) {
		contextTitleRect = ribbonTabBar->tabRect(indexs.first());
		QRect endRect    = ribbonTabBar->tabRect(indexs.last());
		contextTitleRect.setRight(endRect.right());
		contextTitleRect.translate(ribbonTabBar->x(), ribbonTabBar->y());
		contextTitleRect.setHeight(ribbonTabBar->height() - 1);  // 减1像素，避免tabbar基线覆盖
		contextTitleRect -= ribbonTabBar->tabMargin();
		// 把区域顶部扩展到窗口顶部
		contextTitleRect.setTop(border.top());
	}
	return contextTitleRect;
}

void SARibbonBar::PrivateData::relayout()
{
	if (auto lay = q_ptr->layout()) {
		lay->invalidate();
		lay->activate();
#if SARIBBONBAR_DEBUG_PRINT
		qDebug() << "SARibbonBar relayout";
#endif
	}
}

void SARibbonBar::PrivateData::initNewParent(QWidget* par)
{
	if (!par) {
		return;
	}
	q_ptr->connect(par, &QWidget::windowTitleChanged, q_ptr, &SARibbonBar::onWindowTitleChanged);
	q_ptr->connect(par, &QWidget::windowIconChanged, q_ptr, &SARibbonBar::onWindowIconChanged);
	// 父窗口发生了改变，一般就是加入到了MainWindow中，这时要同步父窗口的信息到图标
	SARibbonTitleIconWidget* titleIcon = q_ptr->titleIconWidget();
	if (!titleIcon) {
		return;
	}

	if (q_ptr->isApplicationButtonVerticalExpansion()) {
		titleIcon->hide();
		return;
	}

	// 非垂直展开模式：尝试设置为 MainWindow 的图标
	if (auto* mainwindow = qobject_cast< SARibbonMainWindow* >(par)) {
		int th = q_ptr->titleBarHeight();
		titleIcon->setWidget(mainwindow);
		titleIcon->setIcon(mainwindow->windowIcon());
		titleIcon->setIconSize(QSize(th - 2, th - 2));
		titleIcon->show();
		titleIcon->raise();
	} else {
		titleIcon->hide();
	}
}

//===================================================
// SARibbonBar
//===================================================

/**
 * \if ENGLISH
 * @brief SARibbonBar constructor
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonBar构造函数
 * @param parent 父窗口
 * \endif
 */
SARibbonBar::SARibbonBar(QWidget* parent) : QMenuBar(parent), d_ptr(new SARibbonBar::PrivateData(this))
{
	d_ptr->init();
	d_ptr->initNewParent(parent);
}

SARibbonBar::~SARibbonBar()
{
}

/**
 * \if ENGLISH
 * @brief Check if the ribbon style is two-row mode
 * @param s Ribbon style
 * @return true if it's two-row style, false if it's three-row style
 * \endif
 *
 * \if CHINESE
 * @brief 判断样式是否为2行
 * @param s  ribbon样式
 * @return 2行返回true，返回false代表当前是3行
 * \endif
 */
bool SARibbonBar::isTwoRowStyle(RibbonStyles s)
{
	return s.testFlag(SARibbonBar::RibbonStyleTwoRow);
}

/**
 * \if ENGLISH
 * @brief Check if the ribbon style is three-row mode
 * @param s Ribbon style
 * @return true if it's three-row style, false if it's two-row style
 * \endif
 *
 * \if CHINESE
 * @brief 判断样式是否为3行
 * @param s  ribbon样式
 * @return 3行返回true，返回false代表当前是2行
 * \endif
 */
bool SARibbonBar::isThreeRowStyle(RibbonStyles s)
{
	return s.testFlag(SARibbonBar::RibbonStyleThreeRow);
}

/**
 * \if ENGLISH
 * @brief Check if the ribbon style is loose (with title bar)
 * @param s Ribbon style
 * @return true if it's loose style, false if it's compact style
 * \endif
 *
 * \if CHINESE
 * @brief 判断是否是宽松样式
 * @param s  ribbon样式
 * @return 宽松样式（带标题栏）返回true，否则就是紧凑样式
 * \endif
 */
bool SARibbonBar::isLooseStyle(SARibbonBar::RibbonStyles s)
{
	return s.testFlag(SARibbonBar::RibbonStyleLoose);
}

/**
 * \if ENGLISH
 * @brief Check if the ribbon style is compact (without title bar)
 * @param s Ribbon style
 * @return true if it's compact style, false if it's loose style
 * \endif
 *
 * \if CHINESE
 * @brief 判断是否是紧凑样式
 * @param s  ribbon样式
 * @return 紧凑样式（不带标题栏）返回true，否则就是宽松样式
 * \endif
 */
bool SARibbonBar::isCompactStyle(RibbonStyles s)
{
	return s.testFlag(SARibbonBar::RibbonStyleCompact);
}

/**
 * \if ENGLISH
 * @brief Check if the ribbon style is single-row mode
 * @param s Ribbon style
 * @return true if it's single-row style
 * \endif
 *
 * \if CHINESE
 * @brief 判断样式是否为单行模式
 * @param s ribbon样式
 * @return 单行返回true
 * \endif
 */
bool SARibbonBar::isSingleRowStyle(RibbonStyles s)
{
	return s.testFlag(SARibbonBar::RibbonStyleSingleRow);
}

/**
 * \if ENGLISH
 * @brief Get version information
 * @return {SA_RIBBON_BAR_VERSION_MAJ}.{SA_RIBBON_BAR_VERSION_MIN}.{SA_RIBBON_BAR_VERSION_PAT}
 * \endif
 *
 * \if CHINESE
 * @brief 获取版本信息
 * @return {SA_RIBBON_BAR_VERSION_MAJ}.{SA_RIBBON_BAR_VERSION_MIN}.{SA_RIBBON_BAR_VERSION_PAT}
 * \endif
 */
QString SARibbonBar::versionString()
{
	return QString("%1.%2.%3").arg(SA_RIBBON_BAR_VERSION_MAJ).arg(SA_RIBBON_BAR_VERSION_MIN).arg(SA_RIBBON_BAR_VERSION_PAT);
}

/**
 * \if ENGLISH
 * @brief Get default context category color list
 * @return Default context category color list
 * \endif
 *
 * \if CHINESE
 * @brief 获取默认的上下文标签颜色列表
 * @return 默认的上下文标签颜色列表
 * \endif
 */
QList< QColor > SARibbonBar::defaultContextCategoryColorList()
{
	QList< QColor > res;
	res                           //
		<< QColor(206, 232, 252)  // 蓝
		<< QColor(253, 238, 179)  // 黄
		<< QColor(212, 255, 174)  // 绿
		<< QColor(255, 196, 214)  // 红
		<< QColor(255, 216, 153)  // 橙
		<< QColor(255, 224, 243)  // 玫红
		;
	return res;
}

/**
 * \if ENGLISH
 * @brief Static function to support high DPI
 *
 * @note This function needs to be called in the main function before QApplication is created
 * @code
 * int main(int argc, char* argv[]){
 *    SARibbonBar::initHighDpi();
 *    QApplication a(argc, argv);
 *    ...
 * }
 * @endcode
 * \endif
 *
 * \if CHINESE
 * @brief 提供高分屏的支持静态函数
 *
 * @note 此函数需要在main函数，QApplication生成之前调用
 * @code
 * int main(int argc, char* argv[]){
 *    SARibbonBar::initHighDpi();
 *    QApplication a(argc, argv);
 *    ...
 * }
 * @endcode
 * \endif
 */
void SARibbonBar::initHighDpi()
{
#if (QT_VERSION >= QT_VERSION_CHECK(5, 6, 0)) && (QT_VERSION < QT_VERSION_CHECK(6, 0, 0))
	QApplication::setAttribute(Qt::AA_EnableHighDpiScaling);
	QApplication::setAttribute(Qt::AA_UseHighDpiPixmaps);
#endif
#if (QT_VERSION >= QT_VERSION_CHECK(5, 14, 0))
	// 在有些时候，无法显示分割线，是由于PassThrough导致的
	QApplication::setHighDpiScaleFactorRoundingPolicy(Qt::HighDpiScaleFactorRoundingPolicy::PassThrough);
#endif
}

/**
 * \if ENGLISH
 * @brief Get application button
 * @return The default application button is generated by @ref SARibbonApplicationButton, can be set to other button via @ref setApplicationButton
 * \endif
 *
 * \if CHINESE
 * @brief 返回applicationButton
 * @return 默认的applicationButton由 @ref SARibbonApplicationButton 生成，通过@ref setApplicationButton 可设置为其他button
 * \endif
 */
QAbstractButton* SARibbonBar::applicationButton()
{
	return (d_ptr->mApplicationButton.data());
}

/**
 * \if ENGLISH
 * @brief Set application button, can pass nullptr to hide
 *
 * By default, there is a SARibbonApplicationButton, can pass nullptr to cancel, or pass custom button
 *
 * @note The ownership of applicationButton belongs to SARibbonBar, do not delete applicationButton externally
 * @param btn Application button pointer, can pass @ref SARibbonApplicationButton,
 * SA has already set the style for SARibbonApplicationButton
 * \endif
 *
 * \if CHINESE
 * @brief 设置applicationButton,如果想隐藏，可以传入nullptr
 *
 * 默认会有一个SARibbonApplicationButton，如果想取消，可传入nullptr，或者自定义的button也可以传入
 *
 * @note applicationButton的所有权归SARibbonBar所有，不要在外部对applicationButton进行delete操作
 * @param btn applicationButton指针，可以传入@ref SARibbonApplicationButton，
 * SA已经对SARibbonApplicationButton进行了样式设置
 * \endif
 */
void SARibbonBar::setApplicationButton(QAbstractButton* btn)
{
	d_ptr->setApplicationButton(btn);
	d_ptr->relayout();
}

/**
 * \if ENGLISH
 * @brief Get tab bar
 * @return SARibbonTabBar pointer
 * @sa SARibbonTabBar
 * \endif
 *
 * \if CHINESE
 * @brief 返回tabbar
 * @return SARibbonTabBar指针
 * @sa SARibbonTabBar
 * \endif
 */
SARibbonTabBar* SARibbonBar::ribbonTabBar()
{
	return (d_ptr->mRibbonTabBar);
}

/**
 * \if ENGLISH
 * @brief Add a category page
 * To delete, simply use delete, SARibbonBar will handle it
 * @param title Category name, by default SARibbonCategory's object name is also set to title
 * @return Returns a window container, can add other controls in Category
 * @sa SARibbonCategory
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个标签
 * 如果需要删除，直接delete即可，SARibbonBar会对其进行处理
 * @param title 标签名字，默认情况下SARibbonCategory的object name也被设置为title
 * @return 返回一个窗口容器，在Category里可以添加其他控件
 * @sa SARibbonCategory
 * \endif
 */
SARibbonCategory* SARibbonBar::addCategoryPage(const QString& title)
{
	SARibbonCategory* category = RibbonSubElementFactory->createRibbonCategory(this);

	category->setObjectName(title);
	category->setCategoryName(title);

	addCategoryPage(category);
	return (category);
}

/**
 * \if ENGLISH
 * @brief Add a category page
 * @param category SARibbonCategory pointer
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个标签
 * @param category SARibbonCategory指针
 * \endif
 */
void SARibbonBar::addCategoryPage(SARibbonCategory* category)
{
	if (nullptr == category) {
		return;
	}
	int index = d_ptr->mRibbonTabBar->count();
	insertCategoryPage(category, index);
}

/**
 * \if ENGLISH
 * @brief For Qt Designer use
 * @param category Widget pointer
 * \endif
 *
 * \if CHINESE
 * @brief qtdesigner专用
 * @param category 窗口指针
 * \endif
 */
void SARibbonBar::addCategoryPage(QWidget* category)
{
	SARibbonCategory* c = qobject_cast< SARibbonCategory* >(category);

	if (c) {
		addCategoryPage(c);
	}
}

/**
 * \if ENGLISH
 * @brief Add a category at specified index, if current category count is less than index, it will be inserted at the end
 * @param title Category title
 * @param index Category position
 * @return SARibbonCategory pointer
 * \endif
 *
 * \if CHINESE
 * @brief 添加一个category，category的位置在index，如果当前category数量少于index，将插入到最后
 * @param title category的标题
 * @param index category的位置
 * @return SARibbonCategory指针
 * \endif
 */
SARibbonCategory* SARibbonBar::insertCategoryPage(const QString& title, int index)
{
	SARibbonCategory* category = RibbonSubElementFactory->createRibbonCategory(this);

	category->setObjectName(title);
	category->setCategoryName(title);
	insertCategoryPage(category, index);
	return (category);
}

/**
 * \if ENGLISH
 * @brief Insert a category
 * @param category SARibbonCategory pointer
 * @param index Insert position, if out of range, will be inserted at the end
 * \endif
 *
 * \if CHINESE
 * @brief 插入一个category
 * @param category SARibbonCategory指针
 * @param index 插入的位置，如果超出范围，将默认插入到最后
 * \endif
 */
void SARibbonBar::insertCategoryPage(SARibbonCategory* category, int index)
{
	if (nullptr == category) {
		return;
	}
	category->setPanelLayoutMode(panelLayoutMode());
	category->setPanelSpacing(panelSpacing());
	category->setPanelLargeIconSize(panelLargeIconSize());
	category->setPanelSmallIconSize(panelSmallIconSize());
	category->setEnableWordWrap(isEnableWordWrap());

	int i = d_ptr->mRibbonTabBar->insertTab(index, category->categoryName());

	d_ptr->setCategoryToTab(i, category);
	d_ptr->mStackedContainerWidget->insertWidget(index, category);
	// 更新上下文标签的索引信息
	d_ptr->updateTabData();
	connect(category, &QWidget::windowTitleChanged, this, &SARibbonBar::onCategoryWindowTitleChanged);
	connect(category, &SARibbonCategory::actionTriggered, this, &SARibbonBar::actionTriggered);
	d_ptr->relayout();
}

/**
 * \if ENGLISH
 * @brief Find category by name
 * @param title Category name, the title of the tab
 * @return Returns nullptr if not found, returns the first found name if there are duplicates, so try to avoid duplicate labels
 * @note Due to translation and other reasons, title may change, so if you want to find category by fixed content, you should use @ref categoryByObjectName
 * @see categoryByObjectName
 * \endif
 *
 * \if CHINESE
 * @brief 通过名字查找Category
 * @param title Category的名字，既标签的标题
 * @return 如果没有找到，将返回nullptr，如果有重名，将返回第一个查询到的名字，因此，尽量避免重名标签
 * @note 由于翻译等原因，可能title会变化，因此如果想通过固定内容查找category，应该使用 @ref categoryByObjectName
 * @see categoryByObjectName
 * \endif
 */
SARibbonCategory* SARibbonBar::categoryByName(const QString& title) const
{
	int c = d_ptr->mStackedContainerWidget->count();

	for (int i = 0; i < c; ++i) {
		SARibbonCategory* w = qobject_cast< SARibbonCategory* >(d_ptr->mStackedContainerWidget->widget(i));
		if (w) {
			if (w->categoryName() == title) {
				return (w);
			}
		}
	}
	return (nullptr);
}

/**
 * \if ENGLISH
 * @brief Find category by object name
 * @param objname Object name
 * @return Returns nullptr if not found, returns the first found name if there are same object names, so try to avoid duplicate object names
 * @see categoryByName
 * \endif
 *
 * \if CHINESE
 * @brief 通过ObjectName查找Category
 * @param objname 对象名
 * @return 如果没有找到，将返回nullptr，如果有同样的ObjectName，将返回第一个查询到的名字，因此，尽量避免ObjectName重名
 * @see categoryByName
 * \endif
 */
SARibbonCategory* SARibbonBar::categoryByObjectName(const QString& objname) const
{
	int c = d_ptr->mStackedContainerWidget->count();

	for (int i = 0; i < c; ++i) {
		SARibbonCategory* w = qobject_cast< SARibbonCategory* >(d_ptr->mStackedContainerWidget->widget(i));
		if (w) {
			if (w->objectName() == objname) {
				return (w);
			}
		}
	}
	return (nullptr);
}

/**
 * \if ENGLISH
 * @brief Find category by index, returns nullptr if out of index range
 * @param index Index
 * @return Returns nullptr if out of index range
 * @note If there are context categories at this time, context labels will also be returned
 * @note The category found by index must be a visible category, hidden labels via @ref hideCategory cannot be found by index
 * @note You can find all added labels via @ref categoryByObjectName,
 * if you want to get all labels managed by ribbonbar, you can get them through function @ref categoryPages
 * @see categoryIndex categoryByObjectName categoryByName
 * \endif
 *
 * \if CHINESE
 * @brief 通过索引找到category，如果超过索引范围，会返回nullptr
 * @param index 索引
 * @return 如果超过索引范围，会返回nullptr
 * @note 如果此时有上下文标签，上下文的标签也会返回
 * @note 通过索引查找的category必须是visible状态的category，如果通过@ref hideCategory 隐藏的标签，通过索引是找不到的
 * @note 通过@ref categoryByObjectName 可以找到所有加入过的标签，
 * 如果想得到ribbonbar管理的所有标签，可以通过函数@ref categoryPages 得到
 * @see categoryIndex categoryByObjectName categoryByName
 * \endif
 */
SARibbonCategory* SARibbonBar::categoryByIndex(int index) const
{
	return d_ptr->categoryFromTab(index);
}

/**
 * \if ENGLISH
 * @brief Hide category, not delete or remove, just hide
 * @param category SARibbonCategory pointer
 * \endif
 *
 * \if CHINESE
 * @brief 隐藏category,并不会删除或者取走，只是隐藏
 * @param category SARibbonCategory指针
 * \endif
 */
void SARibbonBar::hideCategory(SARibbonCategory* category)
{
	int tabIndex = d_ptr->tabIndexForCategory(category);
	if (tabIndex >= 0) {
		_SARibbonHiddenCategoryData hiddenData;
		hiddenData.category      = category;
		hiddenData.originalIndex = tabIndex;
		d_ptr->mHidedCategory.append(hiddenData);
		d_ptr->mRibbonTabBar->removeTab(tabIndex);
		// 注意Category隐藏后，context的位置就会发生变化，需要更新
		d_ptr->updateTabData();
		/**
		 * \if ENGLISH
		 * @brief Fix issue #147: Force layout refresh and repaint after hiding category to avoid display anomalies when batch hiding/showing categories
		 * \endif
		 *
		 * \if CHINESE
		 * @brief 修复 #147 问题：隐藏category后强制刷新布局并重绘，避免批量隐藏/显示category后出现显示异常
		 * \endif
		 */
		d_ptr->relayout();
		update();
	}
}

/**
 * \if ENGLISH
 * @brief Show hidden category
 * @param category SARibbonCategory pointer
 * \endif
 *
 * \if CHINESE
 * @brief 显示被隐藏的category
 * @param category SARibbonCategory指针
 * \endif
 */
void SARibbonBar::showCategory(SARibbonCategory* category)
{
	for (auto i = d_ptr->mHidedCategory.begin(); i != d_ptr->mHidedCategory.end(); ++i) {
		if (i->category == category) {
			// 根据stacked widget的逻辑顺序计算正确的插入位置
			int insertPos = d_ptr->calcCategoryInsertTabIndex(i->category);
			int index     = d_ptr->mRibbonTabBar->insertTab(insertPos, i->category->categoryName());
			d_ptr->setCategoryToTab(index, i->category);
			d_ptr->mHidedCategory.erase(i);  // 移除
			// 更新上下文标签的索引信息
			d_ptr->updateTabData();
			/**
			 * \if ENGLISH
			 * @brief Fix issue #147: Force layout refresh and repaint before raising category to avoid display anomalies when batch hiding/showing categories
			 * \endif
			 *
			 * \if CHINESE
			 * @brief 修复 #147 问题：显示category前强制刷新布局并重绘，避免批量隐藏/显示category后出现显示异常
			 * \endif
			 */
			d_ptr->relayout();
			update();
			raiseCategory(category);
			return;
		}
	}
	raiseCategory(category);
}

/**
 * \if ENGLISH
 * @brief Batch set multiple categories visible or hidden, only triggers one relayout at the end for better performance
 * @param[in] categories List of SARibbonCategory pointers to operate on
 * @param[in] visible True to show categories, false to hide them
 * @details This function is optimized for batch operations, avoiding multiple relayout calls that would occur when
 *          calling hideCategory/showCategory repeatedly for multiple categories. It performs all tab operations first,
 *          then calls updateTabData, relayout and update only once at the end.
 * \endif
 *
 * \if CHINESE
 * @brief 批量设置多个分类的显示或隐藏状态，仅在最后触发一次布局刷新以提升性能
 * @param[in] categories 要操作的SARibbonCategory指针列表
 * @param[in] visible 为true时显示分类，为false时隐藏分类
 * @details 此函数针对批量操作进行了优化，避免了对多个分类重复调用hideCategory/showCategory时会出现的多次布局刷新。
 *          它首先执行所有标签操作，然后在最后仅调用一次updateTabData、relayout和update。
 * \endif
 */
void SARibbonBar::setCategoriesVisible(const QList<SARibbonCategory*>& categories, bool visible)
{
	if (visible) {
		// Show all categories in batch
		for (auto* category : categories) {
			// Find in mHidedCategory list
			for (auto i = d_ptr->mHidedCategory.begin(); i != d_ptr->mHidedCategory.end(); ++i) {
				if (i->category == category) {
					int insertPos = d_ptr->calcCategoryInsertTabIndex(i->category);
					int index = d_ptr->mRibbonTabBar->insertTab(insertPos, i->category->categoryName());
					d_ptr->setCategoryToTab(index, i->category);
					d_ptr->mHidedCategory.erase(i);
					break;
				}
			}
		}
	} else {
		// Hide all categories in batch
		for (auto* category : categories) {
			int tabIndex = d_ptr->tabIndexForCategory(category);
			if (tabIndex >= 0) {
				_SARibbonHiddenCategoryData hiddenData;
				hiddenData.category = category;
				hiddenData.originalIndex = tabIndex;
				d_ptr->mHidedCategory.append(hiddenData);
				d_ptr->mRibbonTabBar->removeTab(tabIndex);
			}
		}
	}
	// Only update tab data and relayout ONCE at the end
	d_ptr->updateTabData();
	d_ptr->relayout();
	update();
}

/**
 * \if ENGLISH
 * @brief Check if this category is visible, i.e., the tabbar has this category
 * @param c SARibbonCategory pointer
 * @return True if category is visible
 * \endif
 *
 * \if CHINESE
 * @brief 判断这个category是否在显示状态，也就是tabbar有这个category
 * @param c SARibbonCategory指针
 * @return 如果category可见返回true
 * \endif
 */
bool SARibbonBar::isCategoryVisible(const SARibbonCategory* c) const
{
	int tabindex = categoryIndex(c);

	return (tabindex >= 0);
}

/**
 * \if ENGLISH
 * @brief Get category index
 * @param c SARibbonCategory pointer
 * @return Returns -1 if not found
 * \endif
 *
 * \if CHINESE
 * @brief 获取category的索引
 * @param c SARibbonCategory指针
 * @return 如果找不到，返回-1
 * \endif
 */
int SARibbonBar::categoryIndex(const SARibbonCategory* c) const
{
	return d_ptr->tabIndexForCategory(c);
}

/**
 * \if ENGLISH
 * @brief Move a category from from index to to index
 * @param from Source index
 * @param to Target index
 * \endif
 *
 * \if CHINESE
 * @brief 移动一个Category从from index到to index
 * @param from 源索引
 * @param to 目标索引
 * \endif
 */
void SARibbonBar::moveCategory(int from, int to)
{
	d_ptr->mRibbonTabBar->moveTab(from, to);
	// 这时要刷新所有tabdata的index信息
	d_ptr->updateTabData();
	// 这里会触发tabMoved信号，在tabMoved信号中调整stacked里窗口的位置
}

/**
 * \if ENGLISH
 * @brief Get all currently displayed SARibbonCategory, including SARibbonCategory of hidden SARibbonContextCategory
 *
 * @param getAll Whether to include all categories including context categories
 * @return List of SARibbonCategory pointers
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前显示的所有的SARibbonCategory，包含未显示的SARibbonContextCategory的SARibbonCategory也一并返回
 *
 * @param getAll 是否包含所有类别，包括上下文类别
 * @return SARibbonCategory指针列表
 * \endif
 */
QList< SARibbonCategory* > SARibbonBar::categoryPages(bool getAll) const
{
	int c = d_ptr->mStackedContainerWidget->count();
	QList< SARibbonCategory* > res;

	for (int i = 0; i < c; ++i) {
		SARibbonCategory* w = qobject_cast< SARibbonCategory* >(d_ptr->mStackedContainerWidget->widget(i));
		if (w) {
			if (!getAll && w->isContextCategory()) {
				// 不是getall且是上下文时跳过
				continue;
			}
			res.append(w);
		}
	}
	return (res);
}

/**
 * \if ENGLISH
 * @brief Remove SARibbonCategory
 *
 * SARibbonBar will not delete SARibbonCategory*, but this SARibbonCategory will be detached from SARibbonBar's management
 * It表现为tabbar will be removed, panel will be removed, after using this function, you can delete SARibbonCategory
 * @param category SARibbonCategory pointer
 * \endif
 *
 * \if CHINESE
 * @brief 移除SARibbonCategory
 *
 * SARibbonBar不会delete SARibbonCategory*，但这个SARibbonCategory会脱离SARibbonBar的管理
 * 表现在tabbar会移除，面板会移除，使用此函数后可以对SARibbonCategory进行delete
 * @param category SARibbonCategory指针
 * \endif
 */
void SARibbonBar::removeCategory(SARibbonCategory* category)
{
	int index     = tabIndex(category);
	bool isupdate = false;
	if (index >= 0) {
		d_ptr->mRibbonTabBar->removeTab(index);
		isupdate = true;
	}
	d_ptr->mStackedContainerWidget->removeWidget(category);
	// 同时验证这个category是否是contexcategory里的

	for (SARibbonContextCategory* c : sa_as_const(d_ptr->mContextCategoryList)) {
		c->takeCategory(category);
	}
	// 这时要刷新所有tabdata的index信息
	if (isupdate) {
		d_ptr->updateTabData();
	}
	// 移除完后需要重新布局
	layout()->activate();
}

/**
 * \if ENGLISH
 * @brief Add context category
 *
 * Context category is a tag triggered in special cases, needs to be triggered manually by the user
 *
 * Call @ref SARibbonContextCategory::addCategoryPage to add SARibbonCategory in context category,
 * SARibbonCategory added in context category will only be displayed when context category is displayed
 * @param title Context category title, will be displayed in Office mode, not displayed in WPS mode. By default, SARibbonContextCategory's object name is also set to title
 * @param color Context category color, if set to empty QColor(), will use SARibbonBar's default color system
 * @param id Context category id for searching
 * @return Returns context category pointer
 * @note SARibbonBar has the management right of SARibbonContextCategory, users avoid directly delete externally, if you want to delete, call @ref destroyContextCategory function
 * \endif
 *
 * \if CHINESE
 * @brief 添加上下文标签
 *
 * 上下文标签是特殊时候触发的标签，需要用户手动触发
 *
 * 调用@ref SARibbonContextCategory::addCategoryPage 可在上下文标签中添加SARibbonCategory，
 * 在上下文标签添加的SARibbonCategory，只有在上下文标签显示的时候才会显示
 * @param title 上下文标签的标题，在Office模式下会显示，在wps模式下不显示。默认情况下SARibbonContextCategory的object name也被设置为title
 * @param color 上下文标签的颜色，如果指定为空QColor(),将会使用SARibbonBar的默认色系
 * @param id 上下文标签的id，以便进行查找
 * @return 返回上下文标签指针
 * @note SARibbonBar拥有SARibbonContextCategory的管理权，用户避免在外部直接delete,如果要删除，调用@ref destroyContextCategory 函数
 * \endif
 */
SARibbonContextCategory* SARibbonBar::addContextCategory(const QString& title, const QColor& color, const QVariant& id)
{
	SARibbonContextCategory* context = RibbonSubElementFactory->createRibbonContextCategory(this);

	context->setObjectName(title);
	context->setContextTitle(title);
	context->setId(id);
	context->setContextColor(color.isValid() ? color : d_ptr->getContextCategoryColor());
	addContextCategory(context);
	return (context);
}

/**
 * \if ENGLISH
 * @brief Add context category
 * @param context SARibbonContextCategory pointer
 * \endif
 *
 * \if CHINESE
 * @brief 添加上下文标签
 * @param context SARibbonContextCategory指针
 * \endif
 */
void SARibbonBar::addContextCategory(SARibbonContextCategory* context)
{
	if (nullptr == context) {
		return;
	}
	connect(context, &SARibbonContextCategory::categoryPageAdded, this, &SARibbonBar::onContextsCategoryPageAdded);
	connect(context, &SARibbonContextCategory::categoryTitleChanged, this, &SARibbonBar::onContextsCategoryCategoryNameChanged);
	// remove并没有绑定，主要是remove后在stacked里也不会显示，remove且delete的话，stacked里也会删除
	// 如果当前SARibbonContextCategory有category，先插入stackwidget中
	for (int i = 0; i < context->categoryCount(); ++i) {
		if (SARibbonCategory* category = context->categoryPage(i)) {
			d_ptr->mStackedContainerWidget->addWidget(category);
		}
	}
	d_ptr->mContextCategoryList.append(context);
}

/**
 * \if ENGLISH
 * @brief Show context category
 * @param context Context category pointer
 * \endif
 *
 * \if CHINESE
 * @brief 显示上下文标签
 * @param context 上下文标签指针
 * \endif
 */
void SARibbonBar::showContextCategory(SARibbonContextCategory* context)
{
	if (isContextCategoryVisible(context)) {
		return;
	}
	_SAContextCategoryManagerData contextCategoryData;

	contextCategoryData.contextCategory = context;
	for (int i = 0; i < context->categoryCount(); ++i) {
		SARibbonCategory* category = context->categoryPage(i);
		// 此句如果模式重复设置不会进行多余操作
		category->setPanelLayoutMode(d_ptr->mDefaultPanelLayoutMode);
		// 切换模式后会改变高度，上下文标签显示时要保证显示出来
		int index = d_ptr->mRibbonTabBar->addTab(category->categoryName());
		contextCategoryData.tabPageIndex.append(index);
		d_ptr->setCategoryToTab(index, category);
	}
	d_ptr->mCurrentShowingContextCategory.append(contextCategoryData);
	// 由于上下文都是在最后追加，不需要调用updateTabData();

	d_ptr->relayout();

	// 重新布局完后需要重绘,因为上下文标签涉及窗口的绘制
	update();
}

/**
 * \if ENGLISH
 * @brief Hide context category
 * @param context Context category pointer
 * \endif
 *
 * \if CHINESE
 * @brief 隐藏上下文标签
 * @param context 上下文标签指针
 * \endif
 */
void SARibbonBar::hideContextCategory(SARibbonContextCategory* context)
{
	bool needResize = false;

	for (int i = 0; i < d_ptr->mCurrentShowingContextCategory.size(); ++i) {
		if (d_ptr->mCurrentShowingContextCategory[ i ].contextCategory == context) {
			const QList< int >& indexs = d_ptr->mCurrentShowingContextCategory[ i ].tabPageIndex;
			for (int j = indexs.size() - 1; j >= 0; --j) {
				d_ptr->mRibbonTabBar->removeTab(indexs[ j ]);
			}
			// 注意，再删除ContextCategory后，tab的序号就会改变，这时，这个tab后面的都要调整它的序号
			needResize = true;
			d_ptr->mCurrentShowingContextCategory.removeAt(i);
			break;
		}
	}
	if (needResize) {
		d_ptr->updateTabData();
		d_ptr->relayout();
		// 重新布局完后需要重绘
		update();
	}
}

/**
 * \if ENGLISH
 * @brief Check if context category is in display state
 * @param context Context category pointer
 * @return Returns true if in display state
 * @sa setContextCategoryVisible
 * \endif
 *
 * \if CHINESE
 * @brief 判断上下文是否在显示状态
 * @param context 上下文标签指针
 * @return 在显示状态返回true
 * @sa setContextCategoryVisible
 * \endif
 */
bool SARibbonBar::isContextCategoryVisible(SARibbonContextCategory* context)
{
	return (d_ptr->isContainContextCategoryInList(context));
}

/**
 * \if ENGLISH
 * @brief Set context category display state
 *
 * The current display state of context category can be judged by @ref isContextCategoryVisible
 * @param context Context category
 * @param visible Display state, true for display
 * \endif
 *
 * \if CHINESE
 * @brief 设置上下文标签的显示状态
 *
 * 上下文标签的当前显示状态可通过 @ref isContextCategoryVisible 进行判断
 * @param context 上下文标签
 * @param visible 显示状态，true为显示
 * \endif
 */
void SARibbonBar::setContextCategoryVisible(SARibbonContextCategory* context, bool visible)
{
	if (nullptr == context) {
		return;
	}
	if (visible) {
		showContextCategory(context);
	} else {
		hideContextCategory(context);
	}
}

/**
 * \if ENGLISH
 * @brief Get all context categories
 * @return Returns context category list
 * \endif
 *
 * \if CHINESE
 * @brief 获取所有的上下文标签
 * @return 返回上下文标签列表
 * \endif
 */
QList< SARibbonContextCategory* > SARibbonBar::contextCategoryList() const
{
	return (d_ptr->mContextCategoryList);
}

/**
 * \if ENGLISH
 * @brief Destroy context category, SARibbonCategory of context category will also be destroyed
 * @param context Context category pointer to be destroyed
 * \endif
 *
 * \if CHINESE
 * @brief 销毁上下文标签，上下文标签的SARibbonCategory也会随之销毁
 * @param context 需要销毁的上下文标签指针
 * \endif
 */
void SARibbonBar::destroyContextCategory(SARibbonContextCategory* context)
{
	if (nullptr == context) {
		return;
	}
	//! 1、如果上下文标签显示中，先隐藏
	if (isContextCategoryVisible(context)) {
		hideContextCategory(context);
	}
	//! 2、删除上下文标签的相关内容
	d_ptr->mContextCategoryList.removeAll(context);
	//!
	QList< SARibbonCategory* > res = context->categoryList();

	for (SARibbonCategory* c : sa_as_const(res)) {
		c->hide();
		c->deleteLater();
	}
	context->deleteLater();
	d_ptr->relayout();
}

/**
 * \if ENGLISH
 * @brief Get current visible context category tab indexes
 * @return The returned indexes are sorted and deduplicated
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前可见的上下文标签的tab索引
 * @return 返回的索引是经过排序且去重的
 * \endif
 */
QList< int > SARibbonBar::currentVisibleContextCategoryTabIndexs() const
{
	QList< int > res;
	for (const _SAContextCategoryManagerData& data : sa_as_const(d_ptr->mCurrentShowingContextCategory)) {
		res += data.tabPageIndex;
	}
	if (res.size() > 1) {
		// 1. 排序
		std::sort(res.begin(), res.end());
		// 2. 去重
		auto last = std::unique(res.begin(), res.end());
		res.erase(last, res.end());
	}
	return res;
}

/**
 * \if ENGLISH
 * @brief Set to minimum/normal mode
 *
 * In hidden mode, only tabbar is displayed, content is not displayed, default state is display mode
 *
 * By default, double-clicking tabbar will switch between hidden and display mode, if you want to disable this function,
 * you can override @ref onCurrentRibbonTabDoubleClicked function, do not do any processing for the function
 *
 * @param isMinimum When parameter is true, switch to Minimum mode
 * @see This function will change @ref RibbonState state, you can check the current state through @ref
 * currentRibbonState function
 * \endif
 *
 * \if CHINESE
 * @brief 设置为最小/正常模式
 *
 * 隐藏模式下，只会显示tabbar，不会显示内容，默认状态是显示模式
 *
 * 默认下双击tabbar会切换隐藏显示模式，如果想禁用此功能，可以重载 @ref onCurrentRibbonTabDoubleClicked
 * 函数，不对函数进行任何处理即可
 *
 * @param isMinimum 参数为true时，切换为Minimum模式
 * @see 此函数会改变@ref RibbonState 状态，通过@ref currentRibbonState 函数可以查看当前状态
 * \endif
 */
void SARibbonBar::setMinimumMode(bool isMinimum)
{
#if SARIBBONBAR_DEBUG_PRINT
	qDebug() << "SARibbonBar::setHideMode " << isMinimum;
#endif
	if (isMinimum) {
		d_ptr->setMinimumMode();
	} else {
		d_ptr->setNormalMode();
	}
	// 发射信号
	Q_EMIT ribbonModeChanged(isMinimum ? MinimumRibbonMode : NormalRibbonMode);
}

/**
 * \if ENGLISH
 * @brief Check if current ribbon is in hidden mode
 * @return True if in minimum mode
 * \endif
 *
 * \if CHINESE
 * @brief 当前ribbon是否是隐藏模式
 * @return 如果是最小模式返回true
 * \endif
 */
bool SARibbonBar::isMinimumMode() const
{
	return (d_ptr->mStackedContainerWidget->isPopupMode());
}

/**
 * \if ENGLISH
 * @brief Show hide ribbon button
 * @param isShow Whether to show the button
 * \endif
 *
 * \if CHINESE
 * @brief 显示隐藏ribbon的按钮
 * @param isShow 是否显示按钮
 * \endif
 */
void SARibbonBar::showMinimumModeButton(bool isShow)
{
	SA_D(d);
	if (isShow && !(d->mMinimumCategoryButtonAction)) {
		activeRightButtonGroup();

		d->mMinimumCategoryButtonAction = new QAction(this);
		d->mMinimumCategoryButtonAction->setIcon(style()->standardIcon(
			isMinimumMode() ? QStyle::SP_TitleBarUnshadeButton : QStyle::SP_TitleBarShadeButton, nullptr));
		connect(d->mMinimumCategoryButtonAction, &QAction::triggered, this, [ this, d ]() {
			this->setMinimumMode(!isMinimumMode());
			d->mMinimumCategoryButtonAction->setIcon(style()->standardIcon(
				isMinimumMode() ? QStyle::SP_TitleBarUnshadeButton : QStyle::SP_TitleBarShadeButton, nullptr));
		});
		if (d->mRightButtonGroup) {
			d->mRightButtonGroup->addAction(d->mMinimumCategoryButtonAction);
		}
	}

	d->mMinimumCategoryButtonAction->setVisible(isShow);
}

/**
 * \if ENGLISH
 * @brief Check if hide ribbon button is shown
 * @return True if the button is shown
 * \endif
 *
 * \if CHINESE
 * @brief 是否显示隐藏ribbon按钮
 * @return 如果按钮显示返回true
 * \endif
 */
bool SARibbonBar::isMinimumModeButtonVisible() const
{
	return (nullptr != d_ptr->mMinimumCategoryButtonAction);
}

/**
 * \if ENGLISH
 * @brief Get hide ribbon corresponding action
 * @return QAction pointer for minimum mode
 * \endif
 *
 * \if CHINESE
 * @brief 隐藏ribbon对应的action
 * @return 最小模式对应的QAction指针
 * \endif
 */
QAction* SARibbonBar::minimumModeAction() const
{
	return d_ptr->mMinimumCategoryButtonAction;
}

/**
 * \if ENGLISH
 * @brief Check if tab double click is allowed to enter ribbon minimum mode
 * @return True if enabled
 * \endif
 *
 * \if CHINESE
 * @brief 是否允许tab双击后进入ribbon的最小模式
 * @return 如果允许返回true
 * \endif
 */
bool SARibbonBar::isEnableTabDoubleClickToMinimumMode() const
{
	return d_ptr->mEnableTabDoubleClickToMinimumMode;
}

/**
 * \if ENGLISH
 * @brief Set whether to allow tab double click to enter ribbon minimum mode, this property is enabled by default
 * @param on Whether to enable
 * \endif
 *
 * \if CHINESE
 * @brief 设置是否允许tab双击后，ribbon进入最小化模式，此属性默认开启
 * @param on 是否开启
 * \endif
 */
void SARibbonBar::setTabDoubleClickToMinimumMode(bool on) const
{
	d_ptr->mEnableTabDoubleClickToMinimumMode = on;
}

/**
 * \if ENGLISH
 * @brief Current ribbon state (normal|minimized)
 * @return Current ribbon mode
 * \endif
 *
 * \if CHINESE
 * @brief 当前ribbon的状态（正常|最小化）
 * @return 当前ribbon模式
 * \endif
 */
SARibbonBar::RibbonMode SARibbonBar::currentRibbonState() const
{
	return (d_ptr->mCurrentRibbonMode);
}

/**
 * \if ENGLISH
 * @brief tabBar height
 * @return tabBar height
 * \endif
 *
 * \if CHINESE
 * @brief tabBar的高度
 * @return tabBar高度
 * \endif
 */
int SARibbonBar::tabBarHeight() const
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		return lay->tabBarHeight();
	}
	return -1;
}

/**
 * \if ENGLISH
 * @brief Set tabbar height
 *
 * After user calls setTabBarHeight, the user-set height will be used instead of the automatically calculated height, and
 * the tabbar height will not be recalculated according to font and other information
 *
 * @note Note that in RibbonStyleCompact** mode, the tabbar height must be less than or equal to the titlebar height, otherwise it will display abnormally
 * @note This function will not automatically refresh, if you need to refresh, you need to call @ref updateRibbonGeometry after calling this function
 * @param h Tabbar height
 * @param resizeByNow Whether to resize immediately
 * \endif
 *
 * \if CHINESE
 * @brief 设置tabbar的高度
 *
 * 用户调用setTabBarHeight后，将使用用户设定的高度，而不使用自动计算的高度，这时tabbar高度不会跟随字体等信息重新计算
 *
 * @note 注意，在RibbonStyleCompact**模式下，tabbar高度要保证小于等于titlebar高度，否则会显示异常
 * @note 此函数不会自动刷新，如果需要刷新调用此函数后需要调用@ref updateRibbonGeometry
 * @param h tabbar高度
 * @param resizeByNow 是否立即调整大小
 * \endif
 */
void SARibbonBar::setTabBarHeight(int h, bool resizeByNow)
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		int oldHeight = lay->tabBarHeight();
		if (oldHeight == h) {
			return;
		}
		lay->setTabBarHeight(h);
		if (resizeByNow) {
			lay->resetSize();
		}
	}
}

/**
 * \if ENGLISH
 * @brief Get title bar height
 * @sa setTitleBarHeight
 * @return Title bar height
 * \endif
 *
 * \if CHINESE
 * @brief 返回标题栏高度
 * @sa setTitleBarHeight
 * @return 标题栏高度
 * \endif
 */
int SARibbonBar::titleBarHeight() const
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		return lay->titleBarHeight();
	}
	return -1;
}

/**
 * \if ENGLISH
 * @brief Set title bar height
 * @sa titleBarHeight
 * @note This operation will emit @ref titleBarHeightChanged signal
 * @param h Title bar height
 * @param resizeByNow Whether to resize immediately
 * \endif
 *
 * \if CHINESE
 * @brief 设置标题栏的高度
 * @sa titleBarHeight
 * @note 此操作会发射@ref titleBarHeightChanged 信号
 * @param h 标题栏高度
 * @param resizeByNow 是否立即调整大小
 * \endif
 */
void SARibbonBar::setTitleBarHeight(int h, bool resizeByNow)
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		int oldHeight = lay->titleBarHeight();
		if (oldHeight == h) {
			return;
		}
		lay->setTitleBarHeight(h);
		if (resizeByNow) {
			lay->resetSize();
		}
		Q_EMIT titleBarHeightChanged(oldHeight, h);
	}
}

/**
 * \if ENGLISH
 * @brief category height
 * @return category height
 * \endif
 *
 * \if CHINESE
 * @brief category的高度
 * @return category高度
 * \endif
 */
int SARibbonBar::categoryHeight() const
{
	return d_ptr->mStackedContainerWidget->height();
}

/**
 * \if ENGLISH
 * @brief Set category height
 * @param h Category height
 * @param resizeByNow Whether to resize immediately
 * \endif
 *
 * \if CHINESE
 * @brief 设置category的高度
 * @param h category高度
 * @param resizeByNow 是否立即调整大小
 * \endif
 */
void SARibbonBar::setCategoryHeight(int h, bool resizeByNow)
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		int oldHeight = lay->categoryHeight();
		if (oldHeight == h) {
			return;
		}
		lay->setCategoryHeight(h);
		if (resizeByNow) {
			lay->resetSize();
		}
	}
}

/**
 * \if ENGLISH
 * @brief Slot for window title changed
 * @param title New window title
 * \endif
 *
 * \if CHINESE
 * @brief 窗口标题改变的槽函数
 * @param title 新的窗口标题
 * \endif
 */
void SARibbonBar::onWindowTitleChanged(const QString& title)
{
	Q_UNUSED(title);
	update();
}

/**
 * \if ENGLISH
 * @brief Slot for window icon changed
 * @param i New window icon
 * \endif
 *
 * \if CHINESE
 * @brief 窗口图标改变的槽函数
 * @param i 新的窗口图标
 * \endif
 */
void SARibbonBar::onWindowIconChanged(const QIcon& i)
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		lay->setWindowIcon(i);
	}
}

/**
 * \if ENGLISH
 * @brief Slot for category name changed
 * @param title New category title
 * \endif
 *
 * \if CHINESE
 * @brief category的名字发生改变触发
 * @param title 新的category标题
 * \endif
 */
void SARibbonBar::onCategoryWindowTitleChanged(const QString& title)
{
	// 全部更新一遍
	Q_UNUSED(title);
	updateCategoryTitleToTabName();
}

/**
 * \if ENGLISH
 * @brief Slot for ribbon display interface hidden
 * \endif
 *
 * \if CHINESE
 * @brief ribbon的显示界面隐藏
 * \endif
 */
void SARibbonBar::onStackWidgetHided()
{
}

/**
 * \if ENGLISH
 * @brief Slot for tab change triggered
 * @param index New tab index
 * \endif
 *
 * \if CHINESE
 * @brief 标签切换触发槽函数
 * @param index 新的标签索引
 * \endif
 */
void SARibbonBar::onCurrentRibbonTabChanged(int index)
{
	SARibbonCategory* category = d_ptr->categoryFromTab(index);

	if (!category) {
		return;
	}
	if (d_ptr->mStackedContainerWidget->currentWidget() != category) {
		d_ptr->mStackedContainerWidget->setCurrentWidget(category);
	}
	Q_EMIT currentRibbonTabChanged(index);
	if (isMinimumMode()) {
		d_ptr->mRibbonTabBar->clearFocus();
		if (!d_ptr->mStackedContainerWidget->isVisible()) {
			if (d_ptr->mStackedContainerWidget->isPopupMode()) {
				// 在stackedContainerWidget弹出前，先给tabbar一个QHoverEvent,让tabbar知道鼠标已经移开
				QPoint pos = d_ptr->mRibbonTabBar->mapFromGlobal(QCursor::pos());
#if QT_VERSION < QT_VERSION_CHECK(6, 0, 0)
				QHoverEvent ehl(QEvent::HoverLeave, pos, pos);
#else
				QHoverEvent ehl(QEvent::HoverLeave, QPointF(pos), QPointF(pos));
#endif
				QApplication::sendEvent(d_ptr->mRibbonTabBar, &ehl);
				if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
					lay->layoutStackedContainerWidget();
				}
				d_ptr->mStackedContainerWidget->setFocus();
				// exec之前先发射信息号，否则会被exec阻塞
				d_ptr->mStackedContainerWidget->show();
			}
		}
	}
}

/**
 * \if ENGLISH
 * @brief ribbon tab bar click
 *
 * This implementation must pass the QEvent::MouseButtonDblClick event of stackedwidget to tabbar in eventfilter,
 * otherwise double click will become two single clicks
 *
 * Click event is only used to respond when clicking the same tab
 * @param index Tab index
 * \endif
 *
 * \if CHINESE
 * @brief ribbon tab bar单击
 *
 * 此实现必须在eventfilter中传递stackedwidget的QEvent::MouseButtonDblClick事件到tabbar中，否则会导致双击变两次单击
 *
 * 单击事件仅用于响应点击同一个tab时
 * @param index 标签索引
 * \endif
 */
void SARibbonBar::onCurrentRibbonTabClicked(int index)
{
	const qint64 now = QDateTime::currentMSecsSinceEpoch();
	if (d_ptr->mTabBarLastClickTime > 0 && ((now - d_ptr->mTabBarLastClickTime) < QApplication::doubleClickInterval())) {
		return;
	}
	d_ptr->mTabBarLastClickTime = now;
	if (index != d_ptr->mRibbonTabBar->currentIndex()) {
		// 点击的标签不一致通过changed槽去处理
		return;
	}
	if (isMinimumMode()) {
		if (!d_ptr->mStackedContainerWidget->isVisible()) {
			if (d_ptr->mStackedContainerWidget->isPopupMode()) {
				QPoint pos = d_ptr->mRibbonTabBar->mapFromGlobal(QCursor::pos());
// 在stackedContainerWidget弹出前，先给tabbar一个QHoverEvent,让tabbar知道鼠标已经移开
#if QT_VERSION < QT_VERSION_CHECK(6, 0, 0)
				QHoverEvent ehl(QEvent::HoverLeave, pos, pos);
#else
				QHoverEvent ehl(QEvent::HoverLeave, QPointF(pos), QPointF(pos));
#endif
				QApplication::sendEvent(d_ptr->mRibbonTabBar, &ehl);
				// 弹出前都调整一下位置，避免移动后位置异常
				if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
					lay->layoutStackedContainerWidget();
				}
				d_ptr->mStackedContainerWidget->exec();
			}
		}
	}
}

/**
 * \if ENGLISH
 * @brief ribbon tab bar double click
 *
 * By default, double click will switch between minimum and normal mode
 * @param index Tab index
 * \endif
 *
 * \if CHINESE
 * @brief ribbon tab bar双击
 *
 * 默认情况下双击会切换最小和正常模式
 * @param index 标签索引
 * \endif
 */
void SARibbonBar::onCurrentRibbonTabDoubleClicked(int index)
{
	Q_UNUSED(index);
	d_ptr->mTabBarLastClickTime = QDateTime::currentMSecsSinceEpoch();  // 更新时间
	if (isEnableTabDoubleClickToMinimumMode()) {
		setMinimumMode(!isMinimumMode());
	}
}

/**
 * \if ENGLISH
 * @brief Slot for contexts category page added
 * @param category Added SARibbonCategory
 * \endif
 *
 * \if CHINESE
 * @brief 上下文标签页面添加的槽函数
 * @param category 添加的SARibbonCategory
 * \endif
 */
void SARibbonBar::onContextsCategoryPageAdded(SARibbonCategory* category)
{
	Q_ASSERT_X(category != nullptr, "onContextsCategoryPageAdded", "add nullptr page");
	d_ptr->mStackedContainerWidget->addWidget(category);  // 这里stackedWidget用append，其他地方都应该使用insert
}

/**
 * \if ENGLISH
 * @brief Slot for contexts category name changed
 * @param category Category whose name changed
 * @param title New category title
 * \endif
 *
 * \if CHINESE
 * @brief 上下文标签管理的标签的名字发生变换
 * @param category 名字发生变化的category
 * @param title 新的category标题
 * \endif
 */
void SARibbonBar::onContextsCategoryCategoryNameChanged(SARibbonCategory* category, const QString& title)
{
	Q_UNUSED(category);
	Q_UNUSED(title);
	updateCategoryTitleToTabName();
}

/**
 * \if ENGLISH
 * @brief Slot for tab moved signal
 * @param from Original tab index
 * @param to New tab index
 * \endif
 *
 * \if CHINESE
 * @brief 标签移动的信号
 * @param from 原始标签索引
 * @param to 新标签索引
 * \endif
 */
void SARibbonBar::onTabMoved(int from, int to)
{
	const QSignalBlocker blocker(d_ptr->mStackedContainerWidget);
	// 调整stacked widget的顺序，调整顺序是为了调用categoryPages函数返回的QList<SARibbonCategory *>顺序和tabbar一致
	d_ptr->mStackedContainerWidget->moveWidget(from, to);
}

/**
 * \if ENGLISH
 * @brief Find tabbar index by SARibbonCategory* pointer
 *
 * @param obj SARibbonCategory corresponding QObject pointer
 * @return Returns -1 if not found
 * @note This function does not call any methods of SARibbonCategory*, so it can be called in the destroyed slot of SARibbonCategory
 * \endif
 *
 * \if CHINESE
 * @brief 根据SARibbonCategory*指针查找tabbar的index
 *
 * @param obj SARibbonCategory对应的QObject指针
 * @return 如果没有找到，返回-1
 * @note 此函数不会调用SARibbonCategory*的任何方法，因此可以在SARibbonCategory的destroyed槽中调用
 * \endif
 */
int SARibbonBar::tabIndex(SARibbonCategory* obj)
{
	int idx = d_ptr->tabIndexForCategory(obj);
	if (idx >= 0) {
		return idx;
	}
	// 如果找不到就从stackedwidget中找

	return (-1);
}

/**
 * \if ENGLISH
 * @brief Synchronize ribbonbar content into each category
 * @param autoUpdate Whether to automatically update layout
 * \endif
 *
 * \if CHINESE
 * @brief 把ribbonbar的内容，同步进各个category中
 * @param autoUpdate 是否自动更新布局
 * \endif
 */
void SARibbonBar::synchronousCategoryData(bool autoUpdate)
{
	iterateCategory([ this ](SARibbonCategory* c) -> bool {
		c->setEnableShowPanelTitle(this->isEnableShowPanelTitle());
		c->setPanelTitleHeight(this->panelTitleHeight());
		c->setCategoryAlignment(this->ribbonAlignment());
		c->setPanelLayoutMode(this->panelLayoutMode());
		return true;
	});
	if (autoUpdate) {
		d_ptr->relayout();
		if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
			// 对category发送布局请求
			lay->layoutCategory();
		}
	}
}

/**
 * \if ENGLISH
 * @brief Convert window title text to display text, mainly replacing [*] placeholder
 * @param title Original window title
 * @return If [*] placeholder exists, replace it according to the parent window's isWindowModified state: if isWindowModified
 * is true, [*] is replaced with *, if isWindowModified is false, [*] is replaced with empty string
 * \endif
 *
 * \if CHINESE
 * @brief 把窗口标题文字转换为显示的文字，这里主要针对[*]占位符进行替换
 * @param title 原始窗口标题
 * @return 如果存在[*]占位符，则根据父窗口的isWindowModified状态进行文字的替换，isWindowModified为true，[*]替换为*，isWindowModified为false，[*]替换为空
 * \endif
 */
QString SARibbonBar::toDisplayTitleText(const QString& title) const
{
	if (!title.contains("[*]")) {
		return title;
	}
	QWidget* w = parentWidget();
	if (!w) {
		return title;
	}
	QString res = title;
	if (w->isWindowModified()) {
		res.replace("[*]", "*");
	} else {
		res.replace("[*]", "");
	}
	return res;
}

/**
 * \if ENGLISH
 * @brief Get title display area
 * @return Title display area rectangle
 * \endif
 *
 * \if CHINESE
 * @brief 获取标题显示的区域
 * @return 标题显示的区域
 * \endif
 */
QRect SARibbonBar::getWindowTitleRect() const
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		return lay->titleRect();
	}
	return QRect();
}

/**
 * \if ENGLISH
 * @brief Activate the button group on the right side of the tabbar
 * @return Returns the pointer to the right button group
 * \endif
 *
 * \if CHINESE
 * @brief 激活tabbar右边的按钮群
 * @return 返回右边的按钮群指针
 * \endif
 */
SARibbonButtonGroupWidget* SARibbonBar::activeRightButtonGroup()
{
	SA_D(d);
	if (!(d->mRightButtonGroup)) {
		d->createRightButton();
	}
	d->mRightButtonGroup->show();
	return d->mRightButtonGroup.data();
}

/**
 * \if ENGLISH
 * @brief Get the right button group pointer
 * @return Returns nullptr if not created
 * \endif
 *
 * \if CHINESE
 * @brief 返回右边的按钮群指针
 * @return 如果没有创建，返回nullptr
 * \endif
 */
SARibbonButtonGroupWidget* SARibbonBar::rightButtonGroup()
{
	return d_ptr->mRightButtonGroup.data();
}

/**
 * \if ENGLISH
 * @brief Activate the QuickAccessBar
 * @return Returns the activated QuickAccessBar pointer
 * \endif
 *
 * \if CHINESE
 * @brief 激活QuickAccessBar
 * @return 返回激活的QuickAccessBar指针
 * \endif
 */
SARibbonQuickAccessBar* SARibbonBar::activeQuickAccessBar()
{
	SARibbonQuickAccessBar* bar = quickAccessBar();
	if (!bar) {
		bar = d_ptr->createQuickAccessBar();
	}
	bar->show();
	return bar;
}

/**
 * \if ENGLISH
 * @brief Get the QuickAccessBar pointer
 * @return Returns the QuickAccessBar pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取QuickAccessBar指针
 * @return 返回QuickAccessBar指针
 * \endif
 */
SARibbonQuickAccessBar* SARibbonBar::quickAccessBar()
{
	return (d_ptr->mQuickAccessBar.data());
}

/**
 * \if ENGLISH
 * @brief Get the title icon widget
 * @return Returns the title icon widget pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取标题栏窗口
 * @return 返回标题栏窗口指针
 * \endif
 */
SARibbonTitleIconWidget* SARibbonBar::titleIconWidget() const
{
	return (d_ptr->mTitleIconWidget.data());
}

/**
 * \if ENGLISH
 * @brief Set title icon visibility
 * @param on Whether to show the title icon
 * \endif
 *
 * \if CHINESE
 * @brief 设置标题图标可见性
 * @param on 是否可见
 * \endif
 */
void SARibbonBar::setTitleIconVisible(bool on)
{
	SARibbonTitleIconWidget* w = titleIconWidget();
	if (w) {
		w->setVisible(on);
	}
}

/**
 * \if ENGLISH
 * @brief Check if title icon is visible
 * @return True if title icon is visible
 * \endif
 *
 * \if CHINESE
 * @brief 标题图标是否可见
 * @return 标题图标是否可见
 * \endif
 */
bool SARibbonBar::isTitleIconVisible() const
{
	SARibbonTitleIconWidget* w = titleIconWidget();
	if (w) {
		return w->isVisibleTo(this);
	}
	return false;
}

/**
 * \if ENGLISH
 * @brief Set the ribbon bar style
 * This function will reset all elements, including button layout
 * Especially when changing from three-row to two-row mode, there are many reset operations
 * @note This function will automatically trigger ResizeEvent, no need to call manually
 * @note By default, two-row mode does not wrap text. If you want to wrap text in two-row mode,
 * call SARibbonToolButton::setEnableWordWrap(true) after calling SARibbonBar::setRibbonStyle,
 * and then call updateRibbonElementGeometry() to refresh the layout
 * @param v Style, see @ref SARibbonBar::RibbonStyle
 * \endif
 *
 * \if CHINESE
 * @brief 设置ribbonbar的风格，此函数会重新设置所有元素，包括button的布局方式，
 * 尤其是从三行变到两行的过程，重设的内容较多
 * @note 此函数会自动触发ResizeEvent，不需要手动调用
 * @note 默认情况下2行模式不换行,如果想在两行模式换行，在调用SARibbonBar::setRibbonStyle后，再SARibbonToolButton::setEnableWordWrap(true)
 * ,同时再调用updateRibbonElementGeometry()刷新布局
 * @param v 样式，见@ref SARibbonBar::RibbonStyle
 * \endif
 */
void SARibbonBar::setRibbonStyle(SARibbonBar::RibbonStyles v)
{
	// 先幅值给变量
	d_ptr->mRibbonStyle = v;
#if SARIBBONBAR_DEBUG_PRINT
	qDebug() << "setRibbonStyle(" << v << ")"                //
			 << "\n  isThreeRowStyle=" << isThreeRowStyle()  //
			 << "\n  isTwoRowStyle=" << isTwoRowStyle()      //
			 << "\n  isLooseStyle=" << isLooseStyle()        //
			 << "\n  isCompactStyle=" << isCompactStyle()    //
		;
#endif
	// 执行判断——SingleRow需优先判断，因其不含ThreeRow/TwoRow位标记
	if (isSingleRowStyle(v)) {
		setEnableWordWrap(false);
		setTabOnTitle(isCompactStyle());
		setEnableShowPanelTitle(false);
		setPanelLayoutMode(SARibbonPanel::SingleRowMode);
		setEnableIconRightText(true);
	} else if (isThreeRowStyle(v)) {
		setEnableWordWrap(true);
		setTabOnTitle(isCompactStyle());
		setEnableShowPanelTitle(true);
		setPanelLayoutMode(SARibbonPanel::ThreeRowMode);
		setEnableIconRightText(false);
	} else {
		setEnableWordWrap(false);
		setTabOnTitle(isCompactStyle());
		setEnableShowPanelTitle(false);
		setPanelLayoutMode(SARibbonPanel::TwoRowMode);
		setEnableIconRightText(false);
	}

	// 此函数会调用setFixedHeight
	synchronousCategoryData(false);  // 这里不急着刷新，下面会继续刷新
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		lay->resetSize();
	}

	Q_EMIT ribbonStyleChanged(d_ptr->mRibbonStyle);
}

/**
 * \if ENGLISH
 * @brief Get the current ribbon style
 * @return Returns the current ribbon style
 * \endif
 *
 * \if CHINESE
 * @brief 返回当前ribbon的风格
 * @return 返回当前ribbon的风格
 * \endif
 */
SARibbonBar::RibbonStyles SARibbonBar::currentRibbonStyle() const
{
	return (d_ptr->mRibbonStyle);
}

/**
 * \if ENGLISH
 * @brief Switch to the corresponding tab
 * @param index Tab index
 * \endif
 *
 * \if CHINESE
 * @brief 切换到对应标签
 * @param index 标签索引
 * \endif
 */
void SARibbonBar::setCurrentIndex(int index)
{
	d_ptr->mRibbonTabBar->setCurrentIndex(index);
	// onCurrentRibbonTabChanged(index);
}

/**
 * \if ENGLISH
 * @brief Get the current tab index
 * @return Current index
 * \endif
 *
 * \if CHINESE
 * @brief 返回当前的tab索引
 * @return 当前的索引
 * \endif
 */
int SARibbonBar::currentIndex()
{
	return (d_ptr->mRibbonTabBar->currentIndex());
}

/**
 * \if ENGLISH
 * @brief Ensure the category is displayed and switch to the corresponding tab
 * @param category Category pointer
 * \endif
 *
 * \if CHINESE
 * @brief 确保标签显示出来，tab并切换到对应页
 * @param category 标签指针
 * \endif
 */
void SARibbonBar::raiseCategory(SARibbonCategory* category)
{
	int idx = d_ptr->tabIndexForCategory(category);
	if (idx >= 0) {
		d_ptr->mRibbonTabBar->setCurrentIndex(idx);
	}
}

/**
 * \if ENGLISH
 * @brief Check if the current style is two-row
 * @return True if the current style is two-row
 * \endif
 *
 * \if CHINESE
 * @brief 判断当前的样式是否为2行
 * @return 当前样式是否为2行
 * \endif
 */
bool SARibbonBar::isTwoRowStyle() const
{
	return (d_ptr->mDefaultPanelLayoutMode == SARibbonPanel::TwoRowMode);
}

/**
 * \if ENGLISH
 * @brief Check if the current style is three-row
 * @return True if the current style is three-row
 * \endif
 *
 * \if CHINESE
 * @brief 判断当前的样式是否为3行
 * @return 当前样式是否为3行
 * \endif
 */
bool SARibbonBar::isThreeRowStyle() const
{
	return (d_ptr->mDefaultPanelLayoutMode == SARibbonPanel::ThreeRowMode);
}

/**
 * \if ENGLISH
 * @brief Check if the current style is loose
 * @return True if the current style is loose
 * \endif
 *
 * \if CHINESE
 * @brief 判断当前的样式是否为宽松样式
 * @return 当前样式是否为宽松样式
 * \endif
 */
bool SARibbonBar::isLooseStyle() const
{
	return (SARibbonBar::isLooseStyle(currentRibbonStyle()));
}

/**
 * \if ENGLISH
 * @brief Check if the current style is compact
 * @return True if the current style is compact
 * \endif
 *
 * \if CHINESE
 * @brief 判断当前的样式是否为紧凑样式
 * @return 当前样式是否为紧凑样式
 * \endif
 */
bool SARibbonBar::isCompactStyle() const
{
	return (SARibbonBar::isCompactStyle(currentRibbonStyle()));
}

bool SARibbonBar::isSingleRowStyle() const
{
	return isSingleRowStyle(currentRibbonStyle());
}

/**
 * \if ENGLISH
 * @brief Set the title text color
 *
 * The title is the mainwindow's windowTitle. If you want to set the title, directly call SARibbonMainWindow::setWindowTitle
 *
 * If you don't set the title color, it defaults to the SARibbonBar's qss color property
 * @param clr Title text color
 * @note This function does not refresh. Please call repaint manually to refresh
 * \endif
 *
 * \if CHINESE
 * @brief 设置标题的文字颜色
 *
 * 标题是mainwindow的windowTitle，如果要设置标题，直接调用SARibbonMainWindow::setWindowTitle 进行设置
 *
 * 如果不设置标题颜色，默认是SARibbonBar的qss的color属性
 * @param clr 标题文字颜色
 * @note 此函数不会刷新，刷新请自行调用repaint
 * \endif
 */
void SARibbonBar::setWindowTitleTextColor(const QColor& clr)
{
	d_ptr->mTitleTextColor = clr;
}

/**
 * \if ENGLISH
 * @brief Get the title text color
 * @return If an invalid color is returned (!QColor::isValid()), it means no color was manually set,
 * and the color will follow the SARibbonBar's qss text color
 * \endif
 *
 * \if CHINESE
 * @brief 获取标题的文字颜色
 * @return 如果返回的是无效颜色，!QColor::isValid()，说明没有手动设置颜色，颜色将跟随SARibbonBar的qss的文字颜色
 * \endif
 */
QColor SARibbonBar::windowTitleTextColor() const
{
	return d_ptr->mTitleTextColor;
}

/**
 * \if ENGLISH
 * @brief Set the tabbar baseline color
 * A line is drawn at the bottom of the tabbar, this interface defines the line color
 * @param clr Baseline color
 * \endif
 *
 * \if CHINESE
 * @brief tabbar 底部会绘制一条线条，此接口定义线条颜色
 * @param clr 基线颜色
 * \endif
 */
void SARibbonBar::setTabBarBaseLineColor(const QColor& clr)
{
	d_ptr->mTabBarBaseLineColor = clr;
}

/**
 * \if ENGLISH
 * @brief Get the tabbar baseline color
 * @return Tabbar baseline color
 * \endif
 *
 * \if CHINESE
 * @brief 获取tabbar底部基线颜色
 * @return tabbar底部基线颜色
 * \endif
 */
QColor SARibbonBar::tabBarBaseLineColor() const
{
	return d_ptr->mTabBarBaseLineColor;
}

/**
 * \if ENGLISH
 * @brief Update ribbon layout data
 * This function is suitable for some critical size changes, triggering the layout of elements below the ribbon
 *
 * @note This function is slow to call, avoid calling in high-speed scenarios
 * \endif
 *
 * \if CHINESE
 * @brief 更新ribbon的布局数据，此函数适用于一些关键性尺寸变化，换起ribbon下面元素的布局
 *
 * @note 此函数调用较慢，避免在高速要求场合调用
 * \endif
 */
void SARibbonBar::updateRibbonGeometry()
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		lay->resetSize();
	}
	iterateCategory([](SARibbonCategory* c) -> bool {
		c->updateItemGeometry();
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Get SARibbonPanel layout mode
 * @return Current panel layout mode
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonPanel的布局模式
 * @return 当前面板布局模式
 * \endif
 */
SARibbonPanel::PanelLayoutMode SARibbonBar::panelLayoutMode() const
{
	return d_ptr->mDefaultPanelLayoutMode;
}

/**
 * \if ENGLISH
 * @brief Set SARibbonPanel layout mode
 * @param m Panel layout mode
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonPanel的布局模式设置
 * @param m 面板布局模式
 * \endif
 */
void SARibbonBar::setPanelLayoutMode(SARibbonPanel::PanelLayoutMode m)
{
	d_ptr->mDefaultPanelLayoutMode = m;
	// 设置布局时，让布局重新计算高度
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		lay->resetSize();
	}
	iterateCategory([ m ](SARibbonCategory* c) -> bool {
		c->setPanelLayoutMode(m);
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Set tab on title
 * This can omit the title area
 * @param on Whether to set tab on title
 * \endif
 *
 * \if CHINESE
 * @brief 设置tab在title上面，这样可以省略title区域
 * @param on 是否设置tab在title上面
 * \endif
 */
void SARibbonBar::setTabOnTitle(bool on)
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		lay->setTabOnTitle(on);
	}
}

/**
 * \if ENGLISH
 * @brief Check if tab is on title
 * @return True if tab is on title
 * \endif
 *
 * \if CHINESE
 * @brief tab是否在title上面
 * @return tab是否在title上面
 * \endif
 */
bool SARibbonBar::isTabOnTitle() const
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		return lay->isTabOnTitle();
	}
	return false;
}

/**
 * \if ENGLISH
 * @brief Set window title alignment
 * @param al Alignment for window title
 * \endif
 *
 * \if CHINESE
 * @brief 设置标题的对齐方式
 * @param al 标题的对齐方式
 * \endif
 */
void SARibbonBar::setWindowTitleAlignment(Qt::Alignment al)
{
	d_ptr->mTitleAlignment = al;
	update();
}

/**
 * \if ENGLISH
 * @brief Get window title alignment
 * @return Window title alignment
 * \endif
 *
 * \if CHINESE
 * @brief 获取标题的对齐方式
 * @return 标题的对齐方式
 * \endif
 */
Qt::Alignment SARibbonBar::windowTitleAlignment() const
{
	return d_ptr->mTitleAlignment;
}

/**
 * \if ENGLISH
 * @brief Set ribbon bar button text word wrap enabled
 * @param on Whether to enable word wrap
 * \endif
 *
 * \if CHINESE
 * @brief 设置ribbonbar的按钮文字允许换行
 * @param on 是否允许换行
 * \endif
 */
void SARibbonBar::setEnableWordWrap(bool on)
{
	d_ptr->mEnableWordWrap = on;
	iterateCategory([ on ](SARibbonCategory* category) -> bool {
		if (category) {
			category->setEnableWordWrap(on);
		}
		return true;
	});
	updateGeometry();
}

/**
 * \if ENGLISH
 * @brief Check if word wrap is enabled
 * @return True if word wrap is enabled
 * \endif
 *
 * \if CHINESE
 * @brief 判断是否允许换行
 * @return 是否允许换行
 * \endif
 */
bool SARibbonBar::isEnableWordWrap() const
{
	return d_ptr->mEnableWordWrap;
}

/**
 * \if ENGLISH
 * @brief Set button maximum aspect ratio
 * This coefficient determines the maximum width of the button
 *
 * The maximum width of the button is button height * this coefficient, for example, if the button height is h,
 * then the maximum button width maxw = h * buttonMaximumAspectRatio
 * If the text still cannot be fully displayed at this width, the button will not continue to expand horizontally,
 * and will use ... to replace the text that is not fully displayed
 *
 * @see buttonMaximumAspectRatio
 * @param fac Maximum aspect ratio
 * \endif
 *
 * \if CHINESE
 * @brief 设置按钮最大宽高比，这个系数决定按钮的最大宽度
 *
 * 按钮的最大宽度为按钮高度*此系数，例如按钮高度为h，那么按钮最大宽度maxw=h*buttonMaximumAspectRatio
 * 如果在此宽度下文字还无法完全显示，那么按钮将不会继续横向扩展，将使用...替代未完全显示的文字
 *
 * @see buttonMaximumAspectRatio
 * @param fac 最大宽高比
 * \endif
 */
void SARibbonBar::setButtonMaximumAspectRatio(qreal fac)
{
	d_ptr->buttonMaximumAspectRatio = fac;
	iterateCategory([ fac ](SARibbonCategory* category) -> bool {
		if (category) {
			category->setButtonMaximumAspectRatio(fac);
		}
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Get button maximum aspect ratio
 * This coefficient determines the maximum width of the button
 * @return Button maximum aspect ratio
 * @see setButtonMaximumAspectRatio
 * \endif
 *
 * \if CHINESE
 * @brief 按钮最大宽高比，这个系数决定按钮的最大宽度
 * @return 按钮最大宽高比
 * @see setButtonMaximumAspectRatio
 * \endif
 */
qreal SARibbonBar::buttonMaximumAspectRatio() const
{
	return d_ptr->buttonMaximumAspectRatio;
}

/**
 * \if ENGLISH
 * @brief Get panel title bar height
 * @return Panel title bar height
 * \endif
 *
 * \if CHINESE
 * @brief panel标题栏的高度
 * @return panel标题栏的高度
 * \endif
 */
int SARibbonBar::panelTitleHeight() const
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		return lay->panelTitleHeight();
	}
	return -1;
}

/**
 * \if ENGLISH
 * @brief Set panel title bar height
 * @param h Panel title bar height
 * \endif
 *
 * \if CHINESE
 * @brief 设置panel的高度
 * @param h panel标题栏的高度
 * \endif
 */
void SARibbonBar::setPanelTitleHeight(int h)
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		lay->setPanelTitleHeight(h);
	}
	iterateCategory([ h ](SARibbonCategory* c) -> bool {
		c->setPanelTitleHeight(h);
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Check if panel title bar is enabled
 * @return True if panel title bar is enabled
 * \endif
 *
 * \if CHINESE
 * @brief 是否panel显示标题栏
 * @return 是否显示panel标题栏
 * \endif
 */
bool SARibbonBar::isEnableShowPanelTitle() const
{
return d_ptr->mEnableShowPanelTitle;
}

/**
 * \if ENGLISH
 * @brief Set panel title bar enabled
 * @param on Whether to enable panel title bar
 * \endif
 *
 * \if CHINESE
 * @brief 设置显示panel标题
 * @param on 是否显示panel标题
 * \endif
 */
void SARibbonBar::setEnableShowPanelTitle(bool on)
{
	d_ptr->mEnableShowPanelTitle = on;
	iterateCategory([ on ](SARibbonCategory* c) -> bool {
		c->setEnableShowPanelTitle(on);
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Set whether button text is displayed to the right of the icon
 * @param on If true, buttons will show text to the right of the icon (horizontal layout).
 *           If false, buttons use the default vertical layout (icon above, text below).
 * @details This property cascades to all categories, panels, and toolbuttons.
 *           When setRibbonStyle is called with a SingleRow style, this is automatically set to true.
 * \endif
 *
 * \if CHINESE
 * @brief 设置按钮文字是否显示在图标右侧
 * @param on 如果为 true，按钮将显示图标在左、文字在右的水平布局。
 *           如果为 false，按钮使用默认的垂直布局（图标在上，文字在下）。
 * @details 此属性会级联传递到所有 category、panel 和 toolbutton。
 *           当 setRibbonStyle 设置为 SingleRow 样式时，此属性会自动设置为 true。
 * \endif
 */
void SARibbonBar::setEnableIconRightText(bool on)
{
	SA_D(d);
	if (d->mEnableIconRightText == on) {
		return;
	}
	d->mEnableIconRightText = on;
	iterateCategory([on](SARibbonCategory* cat) {
		cat->setEnableIconRightText(on);
		return true;
	});
	updateRibbonGeometry();
}

/**
 * \if ENGLISH
 * @brief Check if icon-right-text mode is enabled
 * @return true if icon-right-text mode is enabled
 * \endif
 *
 * \if CHINESE
 * @brief 检查图标右侧文字模式是否启用
 * @return 如果启用图标右侧文字模式返回 true
 * \endif
 */
bool SARibbonBar::isEnableIconRightText() const
{
	SA_DC(d);
	return d->mEnableIconRightText;
}

/**
 * \if ENGLISH
 * @brief Set panel spacing
 * @param n Panel spacing
 * \endif
 *
 * \if CHINESE
 * @brief 设置panel的spacing
 * @param n panel的间距
 * \endif
 */
void SARibbonBar::setPanelSpacing(int n)
{
	d_ptr->mPanelSpacing = n;
	// 同步当前被SARibbonBar管理的SARibbonCategory的PanelSpacing
	iterateCategory([ n ](SARibbonCategory* category) -> bool {
		if (category) {
			category->setPanelSpacing(n);
		}
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Get panel spacing
 * @return Panel spacing
 * \endif
 *
 * \if CHINESE
 * @brief panel的spacing
 * @return panel的间距
 * \endif
 */
int SARibbonBar::panelSpacing() const
{
	return d_ptr->mPanelSpacing;
}

/**
 * \if ENGLISH
 * @brief Set panel tool button icon size
 * Large action is not affected by this size
 * @param smallSize Small icon size
 * @param largeSize Large icon size
 * \endif
 *
 * \if CHINESE
 * @brief 设置panel按钮的icon尺寸，large action不受此尺寸影响
 * @param smallSize 小图标尺寸
 * @param largeSize 大图标尺寸
 * \endif
 */
void SARibbonBar::setPanelToolButtonIconSize(const QSize& smallSize, const QSize& largeSize)
{
	d_ptr->mPanelSmallToolButtonSize = smallSize;
	d_ptr->mPanelLargeToolButtonSize = largeSize;
	// 同步当前被SARibbonBar管理的SARibbonCategory的PanelSpacing
	iterateCategory([ smallSize, largeSize ](SARibbonCategory* category) -> bool {
		if (category) {
			category->setPanelToolButtonIconSize(smallSize, largeSize);
		}
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Get panel tool button icon size
 * Large action is not affected by this size
 *
 * @note Panel buttons refer to the function buttons at the bottom right of the panel
 * @return Pair of small and large icon sizes
 * \endif
 *
 * \if CHINESE
 * @brief panel按钮的icon尺寸，large action不受此尺寸影响
 *
 * @note panel按钮是指panel右下角的功能按钮
 * @return 小图标和大图标尺寸的配对
 * \endif
 */
QPair< QSize, QSize > SARibbonBar::panelToolButtonIconSize() const
{
	return qMakePair(d_ptr->mPanelSmallToolButtonSize, d_ptr->mPanelLargeToolButtonSize);
}

/**
 * \if ENGLISH
 * @brief Set panel large icon size
 * @param largeSize Large icon size
 * \endif
 *
 * \if CHINESE
 * @brief 设置panel的大按钮图标尺寸
 * @param largeSize 大按钮图标尺寸
 * \endif
 */
void SARibbonBar::setPanelLargeIconSize(const QSize& largeSize)
{
	d_ptr->mPanelLargeToolButtonSize = largeSize;
	// 同步当前被SARibbonBar管理的SARibbonCategory的PanelSpacing
	iterateCategory([ largeSize ](SARibbonCategory* category) -> bool {
		if (category) {
			category->setPanelLargeIconSize(largeSize);
		}
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Get panel large icon size
 * @return Panel large icon size
 * \endif
 *
 * \if CHINESE
 * @brief panel的大按钮图标尺寸
 * @return panel的大按钮图标尺寸
 * \endif
 */
QSize SARibbonBar::panelLargeIconSize() const
{
	return d_ptr->mPanelLargeToolButtonSize;
}

/**
 * \if ENGLISH
 * @brief Set panel small icon size
 * @param smallSize Small icon size
 * \endif
 *
 * \if CHINESE
 * @brief 设置panel的小按钮图标尺寸
 * @param smallSize 小按钮图标尺寸
 * \endif
 */
void SARibbonBar::setPanelSmallIconSize(const QSize& smallSize)
{
	d_ptr->mPanelSmallToolButtonSize = smallSize;
	// 同步当前被SARibbonBar管理的SARibbonCategory的PanelSpacing
	iterateCategory([ smallSize ](SARibbonCategory* category) -> bool {
		if (category) {
			category->setPanelSmallIconSize(smallSize);
		}
		return true;
	});
}

/**
 * \if ENGLISH
 * @brief Get panel small icon size
 * @return Panel small icon size
 * \endif
 *
 * \if CHINESE
 * @brief panel的小按钮图标尺寸
 * @return panel的小按钮图标尺寸
 * \endif
 */
QSize SARibbonBar::panelSmallIconSize() const
{
	return d_ptr->mPanelSmallToolButtonSize;
}

/**
 * \if ENGLISH
 * @brief Get ribbon bar internal StackedWidget
 * All categories are placed in the StackedWidget
 * @return Ribbon stacked widget pointer
 * \endif
 *
 * \if CHINESE
 * @brief ribbonbar内部的StackedWidget
 * 所有的category都放置在StackedWidget中
 * @return  ribbon stacked widget指针
 * \endif
 */
SARibbonStackedWidget* SARibbonBar::ribbonStackedWidget()
{
	return d_ptr->mStackedContainerWidget;
}

/**
 * \if ENGLISH
 * @brief Set title visibility
 * @param on Whether to show the title
 * \endif
 *
 * \if CHINESE
 * @brief 设置是否显示标题
 * @param on 是否显示标题
 * \endif
 */
void SARibbonBar::setTitleVisible(bool on)
{
	d_ptr->mIsTitleVisible = on;
}

/**
 * \if ENGLISH
 * @brief Check if title is visible
 * @return True if title is visible
 * \endif
 *
 * \if CHINESE
 * @brief 判断标题是否显示
 * @return 标题是否显示
 * \endif
 */
bool SARibbonBar::isTitleVisible() const
{
	return d_ptr->mIsTitleVisible;
}

/**
 * \if ENGLISH
 * @brief Set window title background brush
 *
 * If no color is needed, set Qt::NoBrush
 * @param bk Background brush
 * \endif
 *
 * \if CHINESE
 * @brief 设置标题栏的背景颜色
 *
 * 如果不需要颜色，设置Qt::NoBrush
 * @param bk 背景颜色
 * \endif
 */
void SARibbonBar::setWindowTitleBackgroundBrush(const QBrush& bk)
{
	d_ptr->mTitleBackgroundBrush = bk;
}

/**
 * \if ENGLISH
 * @brief Get window title background brush
 *
 * When the color is Qt::NoBrush, it means no background color
 * @default Qt::NoBrush
 * @return Window title background brush
 * \endif
 *
 * \if CHINESE
 * @brief 标题栏的背景颜色
 *
 * 颜色为Qt::NoBrush时，代表没有背景颜色
 * @default Qt::NoBrush
 * @return 标题栏的背景颜色
 * \endif
 */
QBrush SARibbonBar::windowTitleBackgroundBrush() const
{
	return d_ptr->mTitleBackgroundBrush;
}

/**
 * \if ENGLISH
 * @brief Set context category color list
 *
 * When context categories are displayed, colors are taken from this list for label rendering
 * @param cls Context category color list
 * \endif
 *
 * \if CHINESE
 * @brief 设置上下文标签的颜色列表
 *
 * 上下文标签显示的时候，会从颜色列表中取颜色进行标签的渲染
 * @param cls 上下文标签颜色列表
 * \endif
 */
void SARibbonBar::setContextCategoryColorList(const QList< QColor >& cls)
{
	d_ptr->mContextCategoryColorList = cls;
	if (d_ptr->mContextCategoryColorList.isEmpty()) {
		d_ptr->mContextCategoryColorList = defaultContextCategoryColorList();
	}
	d_ptr->mContextCategoryColorListIndex = 0;
	// 这时需要对已经显示的contextCategoryData的颜色进行重新设置
	for (SARibbonContextCategory* c : sa_as_const(d_ptr->mContextCategoryList)) {
		c->setContextColor(d_ptr->getContextCategoryColor());
	}
}

/**
 * \if ENGLISH
 * @brief Get context category color list
 * @return Context category color list
 * \endif
 *
 * \if CHINESE
 * @brief 获取上下文标签的颜色列表
 * @return 上下文标签的颜色列表
 * \endif
 */
QList< QColor > SARibbonBar::contextCategoryColorList() const
{
	return d_ptr->mContextCategoryColorList;
}

/**
 * \if ENGLISH
 * @brief Set context category title text color
 * @param clr Context category title text color
 * \endif
 *
 * \if CHINESE
 * @brief 设置contextCategory 标题的颜色
 * @param clr contextCategory 标题的颜色
 * \endif
 */
void SARibbonBar::setContextCategoryTitleTextColor(const QColor& clr)
{
	d_ptr->mContextCategoryTitleTextColor = clr;
}

/**
 * \if ENGLISH
 * @brief Get context category title text color
 * @return Context category title text color
 * \endif
 *
 * \if CHINESE
 * @brief contextCategory 标题的颜色
 * @return contextCategory 标题的颜色
 * \endif
 */
QColor SARibbonBar::contextCategoryTitleTextColor() const
{
	return d_ptr->mContextCategoryTitleTextColor;
}

/**
 * \if ENGLISH
 * @brief Set context category color highlight function
 * Set a function pointer that takes the context category's set color as input
 * and outputs a highlight color, which is used to draw the highlighted parts of the context category,
 * such as the top horizontal line
 * @param fp Context category color highlight function
 * \endif
 *
 * \if CHINESE
 * @brief 设置一个函数指针，函数指针输入上下文标签设定的颜色，输出一个高亮颜色，高亮颜色用于绘制上下文标签的高亮部位，例如最顶部的横线
 * @param fp 上下文标签颜色高亮函数
 * \endif
 */
void SARibbonBar::setContextCategoryColorHighLight(FpContextCategoryHighlight fp)
{
	d_ptr->mFpContextHighlight = fp;
}

/**
 * \if ENGLISH
 * @brief Set ribbon alignment
 * @param al Ribbon alignment
 * \endif
 *
 * \if CHINESE
 * @brief 设置ribbon的对齐方式
 * @param al 对齐方式
 * \endif
 */
void SARibbonBar::setRibbonAlignment(SARibbonAlignment al)
{
	if (d_ptr->mRibbonAlignment != al) {
		d_ptr->mRibbonAlignment = al;
		synchronousCategoryData();
		updateRibbonGeometry();
	}
}

/**
 * \if ENGLISH
 * @brief Get ribbon alignment
 * @return Current ribbon alignment
 * \endif
 *
 * \if CHINESE
 * @brief ribbon的对齐方式
 * @return 当前的对齐方式
 * \endif
 */
SARibbonAlignment SARibbonBar::ribbonAlignment() const
{
	return d_ptr->mRibbonAlignment;
}

/**
 * \if ENGLISH
 * @brief Iterate through all categories in SARibbonBar and execute the function pointer bool(SARibbonCategory*)
 * @param fp Function pointer, returns false to stop iteration
 * @return Returns false if iteration was interrupted by a callback returning false
 * \endif
 *
 * \if CHINESE
 * @brief 此函数会遍历SARibbonBar下的所有Category，执行函数指针bool(SARibbonCategory*)
 * @param fp 函数指针返回false则停止迭代
 * @return 返回false代表没有迭代完所有的category，中途接收到回调函数的false返回而中断迭代
 * \endif
 */
bool SARibbonBar::iterateCategory(FpCategoryIterate fp) const
{
	const QList< SARibbonCategory* > cs = categoryPages(true);
	for (SARibbonCategory* c : cs) {
		if (!fp(c)) {
			return false;
		}
	}
	return true;
}

/**
 * \if ENGLISH
 * @brief Iterate through all categories and panels in SARibbonBar and execute the function pointer bool(SARibbonPanel*)
 * @param fp Function pointer, returns false to stop iteration
 * @return Returns false if iteration was interrupted by a callback returning false
 * \endif
 *
 * \if CHINESE
 * @brief 此函数会遍历SARibbonBar下的所有Category,并迭代所有的panel，执行函数指针bool(SARibbonPanel*)
 * @param fp 函数指针返回false则停止迭代
 * @return 返回false代表没有迭代完所有的category，中途接收到回调函数的false返回而中断迭代
 * \endif
 */
bool SARibbonBar::iteratePanel(FpPanelIterate fp) const
{
	const QList< SARibbonCategory* > cs = categoryPages(true);
	for (SARibbonCategory* c : cs) {
		if (!c->iteratePanel(fp)) {
			return false;
		}
	}
	return true;
}

/**
 * \if ENGLISH
 * @brief Set corner widget visibility, for MDI windows, there will be TopLeftCorner and TopRightCorner widgets
 * @param on Whether to show the corner widget
 * @param c Corner position
 * \endif
 *
 * \if CHINESE
 * @brief 设置边角widget可见性，对于mdi窗口，会出现TopLeftCorner和TopRightCorner两个corner widget
 * @param on 是否可见
 * @param c 边角位置
 * \endif
 */
void SARibbonBar::setCornerWidgetVisible(bool on, Qt::Corner c)
{
	if (QWidget* w = cornerWidget(c)) {
		w->setVisible(on);
	}
}

/**
 * \if ENGLISH
 * @brief Set ApplicationButton vertical expansion, allowing it to occupy both title bar and tab bar height
 * @param on Whether to enable vertical expansion
 * \endif
 *
 * \if CHINESE
 * @brief 设置ApplicationButton垂直方向扩充，这样ApplicationButton能占用标题栏和tab栏两个栏的高度
 * @param on 是否启用垂直扩充
 * \endif
 */
void SARibbonBar::setApplicationButtonVerticalExpansion(bool on)
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		lay->setApplicationButtonVerticalExpansion(on);
		lay->invalidate();
	}
}

/**
 * \if ENGLISH
 * @brief Check if ApplicationButton is vertically expanded
 *
 * Default is false
 * @return True if ApplicationButton is vertically expanded
 * \endif
 *
 * \if CHINESE
 * @brief applicationButton是否是在垂直方向扩充
 *
 * 默认为false
 * @return 是否在垂直方向扩充
 * \endif
 */
bool SARibbonBar::isApplicationButtonVerticalExpansion() const
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		return lay->isApplicationButtonVerticalExpansion();
	}
	return false;
}

/**
 * \if ENGLISH
 * @brief Iterate through all panels and get their actions
 * @return List of all actions in all panels
 * \endif
 *
 * \if CHINESE
 * @brief 此函数会遍历所有panel，并获取它下面的action
 * @return 所有panel下的action列表
 * \endif
 */
QList< QAction* > SARibbonBar::allActions() const
{
	QList< QAction* > res;
	auto fp = [ &res ](SARibbonPanel* panel) -> bool {
		res += panel->actions();
		return true;
	};
	iteratePanel(fp);
	return res;
}

/**
 * \if ENGLISH
 * @brief Check if currently using borderless frame instead of native border
 * @return True if using ribbon frame
 * \endif
 *
 * \if CHINESE
 * @brief 判断当前是否使用的是无边框，而不是native边框
 * @return 是否使用的是无边框
 * \endif
 */
bool SARibbonBar::isUseRibbonFrame() const
{
	return d_ptr->isUseRibbonFrame();
}

/**
 * \if ENGLISH
 * @brief SARibbonBar::eventFilter
 * @param obj Object being filtered
 * @param e Event being filtered
 * @return True if the event was handled
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonBar::eventFilter
 * @param obj 被过滤的对象
 * @param e 被过滤的事件
 * @return 如果事件被处理返回true
 * \endif
 */
bool SARibbonBar::eventFilter(QObject* obj, QEvent* e)
{
	if (obj) {
		// 调整多文档时在窗口模式下的按钮更新
		if ((obj == cornerWidget(Qt::TopLeftCorner)) || (obj == cornerWidget(Qt::TopRightCorner))) {
			/**
			 * \if ENGLISH
			 * Fix #123: Handle QEvent::WindowStateChange to refresh corner widget when MDI window is maximized or restored
			 * \endif
			 *
			 * \if CHINESE
			 * 修复 #123: 处理 QEvent::WindowStateChange 事件，确保MDI窗口最大化/还原时 corner widget 正确刷新
			 * \endif
			 */
			if ((QEvent::UpdateLater == e->type()) || (QEvent::MouseButtonRelease == e->type())
				|| (QEvent::WindowActivate == e->type())
				|| (QEvent::WindowStateChange == e->type())) {
				// 这个是多文档系统按钮的更新
				d_ptr->relayout();
			}
		} else if (obj == d_ptr->mStackedContainerWidget) {
			// 在stack 是popup模式时，点击的是stackedContainerWidget区域外的时候，如果是在ribbonTabBar上点击
			// 那么忽略掉这次点击，把点击下发到ribbonTabBar,这样可以避免stackedContainerWidget在点击ribbonTabBar后
			// 隐藏又显示，出现闪烁
			if ((QEvent::MouseButtonPress == e->type()) || (QEvent::MouseButtonDblClick == e->type())) {
				if (d_ptr->mStackedContainerWidget->isPopupMode()) {
					QMouseEvent* mouseEvent = static_cast< QMouseEvent* >(e);
					if (!d_ptr->mStackedContainerWidget->rect().contains(mouseEvent->pos())) {
						QPoint golPos          = SA::compat::eventGlobalPos(mouseEvent);
						QWidget* clickedWidget = QApplication::widgetAt(golPos);
						if (clickedWidget == d_ptr->mRibbonTabBar) {
							const QPoint targetPoint = clickedWidget->mapFromGlobal(golPos);
							QMouseEvent* evPress     = new QMouseEvent(mouseEvent->type(),
																   targetPoint,
																   golPos,
																   mouseEvent->button(),
																   mouseEvent->buttons(),
																   mouseEvent->modifiers());
							QApplication::postEvent(clickedWidget, evPress);
							return (true);
						}
					}
				}
			}
		}
	}
	return (QMenuBar::eventFilter(obj, e));
}

/**
 * \if ENGLISH
 * @brief Reset tabbar width according to conditions, mainly for WPS mode
 *
 * Calculate the minimum tabbar width based on the actual tab sizes to avoid tabbar occupying the entire top bar
 * This allows mouse events to reach the mainwindow for operations like dragging
 * @return Minimum tabbar width
 * \endif
 *
 * \if CHINESE
 * @brief 根据情况重置tabbar的宽度，主要针对wps模式
 *
 * 为了把tabbar没有tab的部分不占用，这里的宽度需要根据tab的size来进行设置，让tabbar的长度刚刚好，这样能让出
 * mainwindow的空间，接受鼠标事件，从而实现拖动等操作，否则tabbar占用整个顶栏，鼠标无法点击到mainwindow
 * @return 最小tabbar宽度
 * \endif
 */
int SARibbonBar::calcMinTabBarWidth() const
{
	// 20200831
	// tabBarWidth的宽度原来为endX - x;，现需要根据实际进行调整
	// 为了把tabbar没有tab的部分不占用，这里的宽度需要根据tab的size来进行设置，让tabbar的长度刚刚好，这样能让出
	// mainwindow的空间，接受鼠标事件，从而实现拖动等操作，否则tabbar占用整个顶栏，鼠标无法点击到mainwindow
	// 计算tab所占用的宽度
	const QMargins& mg = d_ptr->mRibbonTabBar->tabMargin();
	return d_ptr->mRibbonTabBar->sizeHint().width() + (mg.left() + mg.right());
}

/**
 * \if ENGLISH
 * @brief Height in normal mode
 *
 * SARibbonBar::height and mainBarHeight may not be equal, this happens in RibbonState::MinimumRibbonMode
 * @return Height
 * \endif
 *
 * \if CHINESE
 * @brief 正常模式下的高度
 *
 * 有可能SARibbonBar::height和mainBarHeight不相等，这种情况发生在RibbonState::MinimumRibbonMode状态下
 * @return 高度
 * \endif
 */
int SARibbonBar::normalModeMainBarHeight() const
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		return lay->normalModeMainBarHeight();
	}
	return -1;
}

/**
 * \if ENGLISH
 * @brief Height in minimum mode
 * @return Height
 * \endif
 *
 * \if CHINESE
 * @brief 最小模式下的高度
 * @return 高度
 * \endif
 */
int SARibbonBar::minimumModeMainBarHeight() const
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		return lay->minimumModeMainBarHeight();
	}
	return -1;
}

/**
 * \if ENGLISH
 * @brief Update all category titles corresponding to tab names
 *
 * This function matches all category names with tabs and resets tab names if they don't match
 * @endif
 *
 * \if CHINESE
 * @brief 更新所有的category title对应的tab名
 *
 * 此函数会对所有的category的名字和tab进行匹配，如果匹配不上会重新设置tab名
 * \endif
 */
void SARibbonBar::updateCategoryTitleToTabName()
{
	SARibbonTabBar* tab = d_ptr->mRibbonTabBar;
	for (int i = 0; i < tab->count(); ++i) {
		// 鉴于tab不会很多，不考虑效率问题
		SARibbonCategory* category = d_ptr->categoryFromTab(i);
		if (category && category->categoryName() != tab->tabText(i)) {
			tab->setTabText(i, category->categoryName());
		}
	}
	repaint();
}

/**
 * \if ENGLISH
 * @brief Set system button group size
 * @param s System button group size
 * \endif
 *
 * \if CHINESE
 * @brief 告知WindowButtonGroup的尺寸
 * @param s 系统按钮组尺寸
 * \endif
 */
void SARibbonBar::setSystemButtonGroupSize(const QSize& s)
{
	if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
		lay->setSystemButtonSize(s);
	}
}

/**
 * @brief 更新标题栏的区域位置
 */
// void SARibbonBar::updateTitleRect()
// {
// 	if (!d_ptr->isUseRibbonFrame()) {
// 		d_ptr->mTitleRect = QRect();
// 	}
// 	SARibbonQuickAccessBar* quickAccessBar = d_ptr->mQuickAccessBar.data();
// 	SARibbonTabBar* ribbonTabBar           = d_ptr->mRibbonTabBar.data();
// 	const QMargins border                  = contentsMargins();
// 	const int validTitleBarHeight          = titleBarHeight();
// 	// 计算标题栏区域
// 	if (isCompactStyle()) {
// 		// 两行紧凑模式
// 		int titleStart = ribbonTabBar->geometry().right();
// 		int titleWidth = quickAccessBar->x() - titleStart;
// 		if (titleWidth > 10) {
// 			d_ptr->mTitleRect = QRect(titleStart, border.top(), titleWidth, validTitleBarHeight);
// 		} else {
// 			d_ptr->mTitleRect = QRect();
// 		}
// 	} else {
// 		// 三行宽松模式
// 		const QList< _SAContextCategoryManagerData >& contextCategoryDataList = d_ptr->mCurrentShowingContextCategory;
// 		// 记录当前上下文标签的最右和最左端
// 		int contextRegionLeft  = width();
// 		int contextRegionRight = -1;
// 		for (const _SAContextCategoryManagerData& contextData : contextCategoryDataList) {
// 			QRect contextTitleRect = d_ptr->calcContextCategoryTitleRect(contextData);
// 			// 更新上下文标签的范围，用于控制标题栏的显示
// 			if (contextTitleRect.left() < contextRegionLeft) {
// 				contextRegionLeft = contextTitleRect.left();
// 			}
// 			if (contextTitleRect.right() > contextRegionRight) {
// 				contextRegionRight = contextTitleRect.right();
// 			}
// 		}
// 		int x1 = border.left();
// 		if (d_ptr->mQuickAccessBar) {
// 			x1 = d_ptr->mQuickAccessBar->geometry().right();
// 			x1 += 1;
// 		}
// 		int x2 = width() - d_ptr->mSystemButtonSize.width() - border.right();
// 		if (contextRegionRight < 0) {
// 			//!说明没有上下文标签，contextRegionRight就是默认值-1

// 			d_ptr->mTitleRect = QRect(QPoint(x1, border.top()), QPoint(x2, validTitleBarHeight + border.top()));
// 		} else {
// 			//! 说明有上下文标签，上下文标签会把标题区域切分为两部分，这时候要找出一个更大的标题区域显示标题
// 			int leftwidth  = contextRegionLeft - x1;   // 计算出上下文标签左边标题栏的宽度
// 			int rightwidth = x2 - contextRegionRight;  // 计算出上下文标签右边标题栏的宽度
// 			if (rightwidth > leftwidth) {
// 				// 说明右边的区域大一点，标题显示在右，显示在右边需要减去windowbutton宽度
// 				d_ptr->mTitleRect =
// 					QRect(QPoint(contextRegionRight, border.top()), QPoint(x2, validTitleBarHeight + border.top()));
// 			} else {
// 				// 说明左边的大一点
// 				d_ptr->mTitleRect =
// 					QRect(QPoint(x1, border.top()), QPoint(contextRegionLeft, validTitleBarHeight + border.top()));
// 			}
// 		}
// 	}
// }

/**
 * \if ENGLISH
 * @brief Set main window styles
 * @param s Main window styles
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonBar::setMainWindowStyles
 * @param s 主窗口样式
 * \endif
 */
void SARibbonBar::setMainWindowStyles(SARibbonMainWindowStyles s)
{
	d_ptr->mMainWindowStyle = s;
}

void SARibbonBar::paintEvent(QPaintEvent* e)
{
	Q_UNUSED(e);
	if (isLooseStyle()) {
		paintInLooseStyle();
	} else {
		paintInCompactStyle();
	}
#if SARIBBONBAR_DEBUG_PRINT
	QPainter p(this);
	if (d_ptr->mQuickAccessBar) {
		SARIBBONBAR_HELP_DRAW_RECT(p, d_ptr->mQuickAccessBar->geometry());
	}
	if (d_ptr->mRibbonTabBar) {
		SARIBBONBAR_HELP_DRAW_RECT(p, d_ptr->mRibbonTabBar->geometry());
	}
	if (d_ptr->mStackedContainerWidget) {
		SARIBBONBAR_HELP_DRAW_RECT(p, d_ptr->mStackedContainerWidget->geometry());
	}
	if (d_ptr->mRightButtonGroup) {
		SARIBBONBAR_HELP_DRAW_RECT(p, d_ptr->mRightButtonGroup->geometry());
	}
#endif
}

void SARibbonBar::paintInLooseStyle()
{
	QPainter p(this);

	//! 1.绘制tabbar下的基线，这个函数仅仅对office2013主题有用，大部分主题都不绘制基线
	paintTabbarBaseLine(p);

	//! 2.显示上下文标签
	const QList< _SAContextCategoryManagerData >& contextCategoryDataList = d_ptr->mCurrentShowingContextCategory;
	for (const _SAContextCategoryManagerData& contextData : contextCategoryDataList) {
		QRect contextTitleRect = d_ptr->calcContextCategoryTitleRect(contextData);
		if (!contextData.tabPageIndex.isEmpty()) {
			// 绘制
			paintContextCategoryTab(p,
									contextData.contextCategory->contextTitle(),
									contextTitleRect,
									contextData.contextCategory->contextColor());
		}
	}

	//! 3.显示标题等
	if (d_ptr->isUseRibbonFrame()) {
		QWidget* parWindow = parentWidget();
		if (parWindow && isTitleVisible()) {
			paintWindowTitle(p, toDisplayTitleText(parWindow->windowTitle()), getWindowTitleRect());
		}
	}
}

void SARibbonBar::paintInCompactStyle()
{
	QPainter p(this);

	//! 1.绘制tabbar下的基线，这个函数仅仅对office2013主题有用，大部分主题都不绘制基线
	paintTabbarBaseLine(p);

	//! 显示上下文标签

	const QList< _SAContextCategoryManagerData >& contextCategoryDataList = d_ptr->mCurrentShowingContextCategory;
	for (const _SAContextCategoryManagerData& contextData : contextCategoryDataList) {
		QRect contextTitleRect = d_ptr->calcContextCategoryTitleRect(contextData);
		if (!contextData.tabPageIndex.isEmpty()) {
			// 绘制
			paintContextCategoryTab(p,
									contextData.contextCategory->contextTitle(),
									contextTitleRect,
									contextData.contextCategory->contextColor());
		}
	}

	//! 显示标题等
	if (d_ptr->isUseRibbonFrame()) {
		QWidget* parWindow = parentWidget();
		if (parWindow && isTitleVisible()) {
			paintWindowTitle(p, toDisplayTitleText(parWindow->windowTitle()), getWindowTitleRect());
		}
	}
}

// void SARibbonBar::resizeStackedContainerWidget()
// {
// 	QMargins border                   = contentsMargins();
// 	const QRect& ribbonTabBarGeometry = d_ptr->mRibbonTabBar->geometry();

// 	int x = border.left();
// 	int y = ribbonTabBarGeometry.bottom() + 1;
// 	int w = width() - border.left() - border.right();
// 	int h = d_ptr->categoryHeight();
// 	if (d_ptr->mStackedContainerWidget->isPopupMode()) {
// 		// 弹出模式时，位置为全局位置
// 		QPoint absPosition = mapToGlobal(QPoint(x, y));
// 		x                  = absPosition.x();
// 		y                  = absPosition.y();
// 	}
// 	d_ptr->mStackedContainerWidget->setFixedSize(QSize(w, h));
// 	d_ptr->mStackedContainerWidget->setGeometry(x, y, w, h);
// }

/**
 * \if ENGLISH
 * @brief Refresh all ContextCategoryManagerData, called when a single Category is deleted
 * \endif
 *
 * \if CHINESE
 * @brief 刷新所有ContextCategoryManagerData，这个在单独一个Category删除时调用
 * \endif
 */
void SARibbonBar::updateContextCategoryManagerData()
{
	for (_SAContextCategoryManagerData& cd : d_ptr->mCurrentShowingContextCategory) {
		cd.tabPageIndex.clear();
		for (int i = 0; i < cd.contextCategory->categoryCount(); ++i) {
			SARibbonCategory* category = cd.contextCategory->categoryPage(i);
			int foundIndex             = d_ptr->tabIndexForCategory(category);
			cd.tabPageIndex.append(foundIndex);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Draw context category tab background
 * @param painter QPainter for drawing
 * @param title Context category title
 * @param contextRect Context category drawing area
 * @param color Context category color
 * \endif
 *
 * \if CHINESE
 * @brief 绘制上下文标签的背景
 * @param painter 绘图QPainter
 * @param title 上下文标签的title
 * @param contextRect 上下文标签的绘制区域
 * @param color 上下文标签赋予的颜色
 * \endif
 */
void SARibbonBar::paintContextCategoryTab(QPainter& painter, const QString& title, const QRect& contextRect, const QColor& color)
{
	// 绘制上下文标签
	// 首先有5像素的实体粗线位于顶部
	painter.save();
	painter.setPen(Qt::NoPen);
	painter.setBrush(color);
	painter.drawRect(contextRect);
	const int contextLineWidth = 5;
	// 绘制很线
	QColor highlightColor = color;
	if (d_ptr->mFpContextHighlight) {
		highlightColor = d_ptr->mFpContextHighlight(color);
	}
	painter.fillRect(QRect(contextRect.x(), contextRect.y(), contextRect.width(), contextLineWidth), highlightColor);

	// 只有在office模式下才需要绘制标题
	if (isLooseStyle()) {
		if (!title.isEmpty()) {
			QRect textRect = QRect(contextRect.x(),
								   contextRect.y() + contextLineWidth,
								   contextRect.width(),
								   contextRect.height() - contextLineWidth - d_ptr->mRibbonTabBar->height());
			painter.setPen(contextCategoryTitleTextColor());
			QFontMetrics fontMetrics(painter.font());
			painter.drawText(textRect, Qt::AlignCenter, fontMetrics.elidedText(title, Qt::ElideRight, textRect.width()));
		}
	}
	painter.restore();
}

/**
 * \if ENGLISH
 * @brief Override moveEvent to adjust stackedContainerWidget position in popup mode when moving
 * @param event Move event
 * \endif
 *
 * \if CHINESE
 * @brief 重写moveevent是为了在移动时调整isPopupMode状态下的stackedContainerWidget位置
 * @param event 移动事件
 * \endif
 */
void SARibbonBar::moveEvent(QMoveEvent* e)
{
	if (d_ptr->mStackedContainerWidget) {
		if (d_ptr->mStackedContainerWidget->isPopupMode()) {
			// 弹出模式时，窗口发生了移动，同步调整StackedContainerWidget的位置
			if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
				lay->layoutStackedContainerWidget();
			}
		}
	}
	QMenuBar::moveEvent(e);
}

/**
 * \if ENGLISH
 * @brief Track font changes and other events
 * @param event Event
 * \endif
 *
 * \if CHINESE
 * @brief 跟踪字体改变
 * @param event 事件
 * \endif
 */
void SARibbonBar::changeEvent(QEvent* e)
{
	if (nullptr == e) {
		return;
	}
	switch (e->type()) {
	case QEvent::FontChange: {
		QFont f                       = font();
		QList< QWidget* > listWidgets = findChildren< QWidget* >();
		for (QWidget* w : listWidgets) {
			w->setFont(f);
		}
		// 字体改变总体布局调整
		if (SARibbonBarLayout* lay = qobject_cast< SARibbonBarLayout* >(layout())) {
			lay->resetSize();
		}
	} break;
	case QEvent::StyleChange: {
		updateRibbonGeometry();
	} break;
	case QEvent::LayoutDirectionChange: {
		// 布局方向改变（如 LTR→RTL），触发完整 Ribbon 几何更新
		updateRibbonGeometry();
	} break;
	case QEvent::ParentChange: {
		//! 这种是针对先 new 一个对象，再设置到 MainWindow 的情况，例如
		//! SARibbonBar* ribbon = new SARibbonBar();
		//! mainwinodw->setRibbonBar(ribbon);
		//!
		//! 这种方式，构造的时候由于没有设置父窗口，因此，如果在构造函数绑定信号槽就有可能绑定不上（parent 为空）
		//! 所以在事件里绑定
		d_ptr->initNewParent(parentWidget());
	} break;
	default:
		break;
	}
	QMenuBar::changeEvent(e);
}

/**
 * \if ENGLISH
 * @brief Draw baseline under tabbar, this method is only needed in office2013 mode
 * @param painter QPainter for drawing
 * \endif
 *
 * \if CHINESE
 * @brief 绘制tabbar下的基准线，这个方法仅仅在office2013模式下需要
 * @param painter 绘图QPainter
 * \endif
 */
void SARibbonBar::paintTabbarBaseLine(QPainter& painter)
{
	if (!d_ptr->mTabBarBaseLineColor.isValid()) {
		return;
	}
	painter.save();
	// 在tabbar下绘制一条线
	const int lineY = d_ptr->mRibbonTabBar->geometry().bottom();
	QPen pen(d_ptr->mTabBarBaseLineColor);
	QMargins border = contentsMargins();

	pen.setWidth(1);
	pen.setStyle(Qt::SolidLine);
	painter.setPen(pen);
	painter.drawLine(QPoint(border.left(), lineY), QPoint(width() - border.right() - 1, lineY));
	painter.restore();
}

/**
 * \if ENGLISH
 * @brief Draw title bar
 * @param painter QPainter for drawing
 * @param title Title text
 * @param titleRegion Title display region
 * \endif
 *
 * \if CHINESE
 * @brief 绘制标题栏
 * @param painter 绘图QPainter
 * @param title 标题
 * @param titleRegion 标题显示区域
 * \endif
 */
void SARibbonBar::paintWindowTitle(QPainter& painter, const QString& title, const QRect& titleRegion)
{
	// 如果标题不显示直接跳出
	if (!isTitleVisible()) {
		return;
	}
	painter.save();
	// 先绘制背景
	if (d_ptr->mTitleBackgroundBrush != Qt::NoBrush) {
		painter.setPen(Qt::NoPen);
		painter.setBrush(d_ptr->mTitleBackgroundBrush);
		painter.drawRect(titleRegion);
	}
	// 再绘制文本
	if (d_ptr->mTitleTextColor.isValid()) {
		painter.setPen(d_ptr->mTitleTextColor);
	} else {
		painter.setPen(palette().color(QPalette::WindowText));
	}

	painter.drawText(titleRegion, d_ptr->mTitleAlignment, title);
	painter.restore();
}
/*** End of inlined file: SARibbonBar.cpp ***/


/*** Start of inlined file: SARibbonBarLayout.cpp ***/
#include <QStyle>
#include <QApplication>
#include <QScreen>

class SARibbonBarLayout::PrivateData
{
public:
	SARibbonBar* ribbonBar;
	QList< QLayoutItem* > items;
	QRect titleRect;
	int titleBarHeight { 30 };    ///< Title bar height
	int tabBarHeight { 28 };      ///< Tab bar height
	int panelTitleHeight { 15 };  ///< Panel title default height
	int categoryHeight { 60 };    ///< Category height

	int maxMinWidth { 1000 };  ///< Maximum minimum width, usually 0.8 of screen width to avoid exceeding screen
	int minWidth { 500 };
	int minHeight { 0 };
	bool isTabOnTitle { false };  ///< Whether tab is on title bar
	std::unique_ptr< int > userDefTitleBarHeight;  ///< User-defined title bar height, normally auto-calculated
	std::unique_ptr< int > userDefTabBarHeight;  ///< User-defined tab bar height, normally auto-calculated
	std::unique_ptr< int > userDefCategoryHeight;  ///< User-defined category height, normally auto-calculated
	QSize systemButtonSize;  ///< System button size (close, maximize, etc.) from SARibbonMainWindow
	bool isApplicationButtonVerticalExpansion {
		false
	};  ///< Whether application button is vertically expanded, which will occupy both title and tab heights

public:
	/**
	 * \if ENGLISH
	 * @brief Constructor for PrivateData
	 * @param bar The SARibbonBar instance
	 * \endif
	 *
	 * \if CHINESE
	 * @brief PrivateData 构造函数
	 * @param bar SARibbonBar 实例
	 * \endif
	 */
	PrivateData(SARibbonBar* bar) : ribbonBar(bar), systemButtonSize(0, 0)
	{
		// Get primary screen size
		QScreen* primaryScreen = QGuiApplication::primaryScreen();
		QRect screenGeometry   = primaryScreen->geometry();
		if (currentRibbonMode() == SARibbonBar::MinimumRibbonMode) {
			minHeight = getActualTitleBarHeight() + (isTabOnTitle ? 0 : getActualTabBarHeight());
		} else {
			minHeight = getActualTitleBarHeight() + getActualCategoryHeight()
						+ (isTabOnTitle ? 0 : getActualTabBarHeight());
		}
		maxMinWidth = screenGeometry.width() * 0.8;  // Screen width
	}

	/**
	 * \if ENGLISH
	 * @brief Get the ribbon tab bar
	 * @return The SARibbonTabBar instance
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取功能区标签栏
	 * @return SARibbonTabBar 实例
	 * \endif
	 */
	SARibbonTabBar* ribbonTabBar() const
	{
		return ribbonBar->ribbonTabBar();
	}

	/**
	 * \if ENGLISH
	 * @brief Get the stacked container widget
	 * @return The SARibbonStackedWidget instance
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取堆叠容器窗口
	 * @return SARibbonStackedWidget 实例
	 * \endif
	 */
	SARibbonStackedWidget* stackedContainerWidget() const
	{
		return ribbonBar->ribbonStackedWidget();
	}

	/**
	 * \if ENGLISH
	 * @brief Get the quick access bar
	 * @return The SARibbonQuickAccessBar instance
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取快速访问栏
	 * @return SARibbonQuickAccessBar 实例
	 * \endif
	 */
	SARibbonQuickAccessBar* quickAccessBar() const
	{
		return ribbonBar->quickAccessBar();
	}

	/**
	 * \if ENGLISH
	 * @brief Get the right button group
	 * @return The SARibbonButtonGroupWidget instance
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取右侧按钮组
	 * @return SARibbonButtonGroupWidget 实例
	 * \endif
	 */
	SARibbonButtonGroupWidget* rightButtonGroup() const
	{
		return ribbonBar->rightButtonGroup();
	}

	/**
	 * \if ENGLISH
	 * @brief Get the title icon widget
	 * @return The SARibbonTitleIconWidget instance
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取标题图标窗口
	 * @return SARibbonTitleIconWidget 实例
	 * \endif
	 */
	SARibbonTitleIconWidget* titleIconWidget() const
	{
		return ribbonBar->titleIconWidget();
	}

	/**
	 * \if ENGLISH
	 * @brief Get the application button
	 * @return The application button
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取应用程序按钮
	 * @return 应用程序按钮
	 * \endif
	 */
	QAbstractButton* applicationButton() const
	{
		return ribbonBar->applicationButton();
	}

	/**
	 * \if ENGLISH
	 * @brief Get the current ribbon mode
	 * @return The current ribbon mode
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取当前功能区模式
	 * @return 当前功能区模式
	 * \endif
	 */
	SARibbonBar::RibbonMode currentRibbonMode() const
	{
		return ribbonBar->currentRibbonState();
	}

	/**
	 * \if ENGLISH
	 * @brief Get the contents margins
	 * @return The contents margins
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取内容边距
	 * @return 内容边距
	 * \endif
	 */
	QMargins contentsMargins() const
	{
		return ribbonBar->contentsMargins();
	}

	/**
	 * \if ENGLISH
	 * @brief Get the ribbon style
	 * @return The current ribbon style
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取功能区样式
	 * @return 当前功能区样式
	 * \endif
	 */
	SARibbonBar::RibbonStyles ribbonStyle() const
	{
		return ribbonBar->currentRibbonStyle();
	}

	/**
	 * \if ENGLISH
	 * @brief Set the title bar height
	 * @param h The height to set
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 设置标题栏高度
	 * @param h 要设置的高度
	 * \endif
	 */
	void setTitleBarHeight(int h)
	{
		if (!userDefTitleBarHeight) {
			userDefTitleBarHeight = std::make_unique< int >(h);
		} else {
			*userDefTitleBarHeight = h;
		}
	}

	/**
	 * \if ENGLISH
	 * @brief Set the tab bar height
	 * @param h The height to set
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 设置标签栏高度
	 * @param h 要设置的高度
	 * \endif
	 */
	void setTabBarHeight(int h)
	{
		if (!userDefTabBarHeight) {
			userDefTabBarHeight = std::make_unique< int >(h);
		} else {
			*userDefTabBarHeight = h;
		}
	}

	/**
	 * \if ENGLISH
	 * @brief Set the category height
	 * @param h The height to set
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 设置类别高度
	 * @param h 要设置的高度
	 * \endif
	 */
	void setCategoryHeight(int h)
	{
		if (!userDefCategoryHeight) {
			userDefCategoryHeight = std::make_unique< int >(h);
		} else {
			*userDefCategoryHeight = h;
		}
	}

	/**
	 * \if ENGLISH
	 * @brief Get the actual title bar height
	 * @return The actual title bar height
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取实际标题栏高度
	 * @return 实际标题栏高度
	 * \endif
	 */
	int getActualTitleBarHeight() const
	{
		if (userDefTitleBarHeight) {
			return *userDefTitleBarHeight;
		} else {
			return titleBarHeight;
		}
	}

	/**
	 * \if ENGLISH
	 * @brief Get the actual tab bar height
	 * @return The actual tab bar height
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取实际标签栏高度
	 * @return 实际标签栏高度
	 * \endif
	 */
	int getActualTabBarHeight() const
	{
		if (userDefTabBarHeight) {
			return *userDefTabBarHeight;
		} else {
			return tabBarHeight;
		}
	}

	/**
	 * \if ENGLISH
	 * @brief Get the actual category height
	 * @return The actual category height
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取实际类别高度
	 * @return 实际类别高度
	 * \endif
	 */
	int getActualCategoryHeight() const
	{
		if (userDefCategoryHeight) {
			return *userDefCategoryHeight;
		} else {
			return categoryHeight;
		}
	}

	/**
	 * \if ENGLISH
	 * @brief Estimate size hints for various components
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 估算各个组件的尺寸提示
	 * \endif
	 */
	void estimateSizeHint()
	{
		titleBarHeight = calcDefaultTitleBarHeight();
		// If tabBarHeight is greater than 0, use user-set value
		tabBarHeight   = calcDefaultTabBarHeight();
		categoryHeight = calcCategoryHeight();
	}

	/**
	 * \if ENGLISH
	 * @brief Get the system tab bar height
	 * @return The system tab bar height
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 获取系统标签栏高度
	 * @return 系统标签栏高度
	 * \endif
	 */
	int systemTabBarHeight() const
	{
		QStyle* style = ribbonBar->style();
		return style->pixelMetric(QStyle::PM_TabBarBaseHeight) + style->pixelMetric(QStyle::PM_TabBarTabHSpace)
			   + style->pixelMetric(QStyle::PM_TabBarTabOverlap);
	}

	/**
	 * \if ENGLISH
	 * @brief Calculate default tab bar height
	 * @return The calculated tab bar height
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 估算标签栏的高度
	 * @return 计算出的标签栏高度
	 * \endif
	 */
	int calcDefaultTabBarHeight()
	{
		int defaultHeight = systemTabBarHeight();
		int fontHeight = ribbonBar->fontMetrics().lineSpacing();  // Use lineSpacing instead of height for better font compatibility
		int defaultHeight2 = fontHeight * 1.6;
		if (defaultHeight2 < fontHeight + 10) {
			defaultHeight2 = fontHeight + 10;  // To accommodate office2021 theme with 4px bottom bar
		}
		int r = qMax(defaultHeight, defaultHeight2);
		if (r < 20) {
			r = 20;
		}
		return r;
	}

	/**
	 * \if ENGLISH
	 * @brief Calculate default title bar height
	 * @return The calculated title bar height
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 估算标题栏的高度
	 * @return 计算出的标题栏高度
	 * \endif
	 */
	int calcDefaultTitleBarHeight()
	{
		int defaultHeight  = ribbonBar->style()->pixelMetric(QStyle::PM_TitleBarHeight);
		int defaultHeight2 = ribbonBar->fontMetrics().height() * 1.8;
		int r              = qMax(defaultHeight, defaultHeight2);
		if (r < 25) {
			r = 25;
		}
		return r;
	}

	/**
	 * \if ENGLISH
	 * @brief Calculate category height based on current ribbon style
	 * @note 1.6 line height is close to Office's height.
	 *       SingleRow mode has no panel title, so panelTitleHeight is not added.
	 * @return The calculated category height
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 根据当前Ribbon风格估算类别的高度
	 * @note 经过对照，1.6行高和office的高度比较接近。
	 *       SingleRow模式下面板标题隐藏，因此不添加panelTitleHeight。
	 * @return 计算出的类别高度
	 * \endif
	 */
	int calcCategoryHeight()
	{
		int textH = ribbonBar->fontMetrics().lineSpacing();
		if (ribbonBar->isThreeRowStyle()) {
			// 4.8 = 3*1.6 for three rows
			return textH * 4.8 + panelTitleHeight;
		} else if (ribbonBar->isSingleRowStyle()) {
			// 1.8 for single row, no panel title in single-row mode
			return textH * 2;
		} else {
			// 3.2 = 2*1.6 for two rows
			return textH * 3.2 + panelTitleHeight;
		}
	}

	/**
	 * \if ENGLISH
	 * @brief Calculate main bar height
	 * @param tabHegith Tab bar height
	 * @param titleHeight Title bar height
	 * @param categoryHeight Category height
	 * @param tabOnTitle Whether tab is on title
	 * @param rMode Ribbon mode
	 * @return The calculated main bar height
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 计算主栏高度
	 * @param tabHegith 标签栏高度
	 * @param titleHeight 标题栏高度
	 * @param categoryHeight 类别高度
	 * @param tabOnTitle 标签是否在标题栏上
	 * @param rMode 功能区模式
	 * @return 计算出的主栏高度
	 * \endif
	 */
	static int
	calcMainBarHeight(int tabHegith, int titleHeight, int categoryHeight, bool tabOnTitle, SARibbonBar::RibbonMode rMode)
	{
		if (rMode == SARibbonBar::MinimumRibbonMode) {
			// Minimum mode, no category height
			if (tabOnTitle) {
				return titleHeight;
			} else {
				return titleHeight + tabHegith;
			}
		} else {
			if (tabOnTitle) {
				return titleHeight + categoryHeight;
			} else {
				return tabHegith + titleHeight + categoryHeight;
			}
		}
		return tabHegith + titleHeight + categoryHeight;
	}

	/**
	 * \if ENGLISH
	 * @brief Reset the size
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 重新计算尺寸
	 * \endif
	 */
	void resetSize()
	{
		estimateSizeHint();
		int mainBarHeight = calcMainBarHeight(getActualTabBarHeight(),
											  getActualTitleBarHeight(),
											  getActualCategoryHeight(),
											  isTabOnTitle,
											  ribbonBar->currentRibbonState());
		// In minimum mode, bar height is tab bar bottom, this adjustment must be after resize event
		// After upgrading to Qt 6, setFixedHeight can cause exceptions because:
		// void QWidget::setFixedHeight(int h) {
		//     // ...
		//     setMinimumSize(minimumSize().width(), h);  // Set minimum height first
		//     setMaximumSize(maximumSize().width(), h);  // Then set maximum height
		//     // ...
		// }
		// If maximumSize().height() is already less than h before calling, setting minimumSize(h) first
		// will make minimum height greater than current maximum height, triggering Qt 6 assertion check.
		// Therefore, reset maximum size limit before calling setFixedHeight:
		int maximumHeight = ribbonBar->maximumHeight();
		if (maximumHeight < mainBarHeight) {
			ribbonBar->setMaximumHeight(QWIDGETSIZE_MAX);
		}
		ribbonBar->setFixedHeight(mainBarHeight);
		minHeight = mainBarHeight;  // minHeight matches mainBarHeight
	}

	/**
	 * \if ENGLISH
	 * @brief Get main bar height in minimum mode
	 * @return Main bar height in minimum mode
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 最小模式下的主栏高度
	 * @return 最小模式下的主栏高度
	 * \endif
	 */
	int minimumModeMainBarHeight()
	{
		return calcMainBarHeight(getActualTabBarHeight(),
								 getActualTitleBarHeight(),
								 getActualCategoryHeight(),
								 isTabOnTitle,
								 SARibbonBar::MinimumRibbonMode);
	}

	/**
	 * \if ENGLISH
	 * @brief Get main bar height in normal mode
	 * @return Main bar height in normal mode
	 * \endif
	 *
	 * \if CHINESE
	 * @brief 正常模式下的主栏高度
	 * @return 正常模式下的主栏高度
	 * \endif
	 */
	int normalModeMainBarHeight()
	{
		return calcMainBarHeight(getActualTabBarHeight(),
								 getActualTitleBarHeight(),
								 getActualCategoryHeight(),
								 isTabOnTitle,
								 SARibbonBar::NormalRibbonMode);
	}
};

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonBarLayout
 * @param parent The parent SARibbonBar
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonBarLayout 构造函数
 * @param parent 父窗口 SARibbonBar
 * \endif
 */
SARibbonBarLayout::SARibbonBarLayout(SARibbonBar* parent) : QLayout(parent), d_ptr(new PrivateData(parent))
{
	init();
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonBarLayout
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonBarLayout 析构函数
 * \endif
 */
SARibbonBarLayout::~SARibbonBarLayout()
{
	while (!d_ptr->items.isEmpty()) {
		QLayoutItem* item = d_ptr->items.takeFirst();
		if (item->widget()) {
			item->widget()->hide();
		}
		delete item;
	}
}

/**
 * \if ENGLISH
 * @brief Initialize the layout
 * \endif
 *
 * \if CHINESE
 * @brief 初始化布局
 * \endif
 */
void SARibbonBarLayout::init()
{
	// 不需要初始化子控件，它们会从ribbonBar获取
}

/**
 * \if ENGLISH
 * @brief Add a layout item
 * @param item The layout item to add
 * \endif
 *
 * \if CHINESE
 * @brief 添加布局项
 * @param item 要添加的布局项
 * \endif
 */
void SARibbonBarLayout::addItem(QLayoutItem* item)
{
	d_ptr->items.append(item);
}

/**
 * \if ENGLISH
 * @brief Get the layout item at the specified index
 * @param index The index of the item
 * @return The layout item at the index, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 获取指定索引处的布局项
 * @param index 项的索引
 * @return 指定索引处的布局项，如果未找到则返回 nullptr
 * \endif
 */
QLayoutItem* SARibbonBarLayout::itemAt(int index) const
{
	if (index >= 0 && index < d_ptr->items.size()) {
		return d_ptr->items.at(index);
	}
	return nullptr;
}

/**
 * \if ENGLISH
 * @brief Take and remove the layout item at the specified index
 * @param index The index of the item
 * @return The layout item at the index, or nullptr if not found
 * \endif
 *
 * \if CHINESE
 * @brief 取出并移除指定索引处的布局项
 * @param index 项的索引
 * @return 指定索引处的布局项，如果未找到则返回 nullptr
 * \endif
 */
QLayoutItem* SARibbonBarLayout::takeAt(int index)
{
	if (index < 0 || index >= d_ptr->items.count()) {
		return nullptr;
	}
	return d_ptr->items.takeAt(index);
}

/**
 * \if ENGLISH
 * @brief Get the number of layout items
 * @return The number of layout items
 * \endif
 *
 * \if CHINESE
 * @brief 获取布局项的数量
 * @return 布局项的数量
 * \endif
 */
int SARibbonBarLayout::count() const
{
	return d_ptr->items.size();
}

/**
 * \if ENGLISH
 * @brief Get the recommended size for the layout
 * @return The recommended size
 * \endif
 *
 * \if CHINESE
 * @brief 获取布局的推荐尺寸
 * @return 推荐尺寸
 * \endif
 */
QSize SARibbonBarLayout::sizeHint() const
{
	int height = d_ptr->minHeight;
	int width  = d_ptr->minWidth;
	if (width > d_ptr->maxMinWidth) {
		width = d_ptr->maxMinWidth;
	}
	return QSize(width, height);
}

/**
 * \if ENGLISH
 * @brief Get the minimum size for the layout
 * @return The minimum size
 * \endif
 *
 * \if CHINESE
 * @brief 获取布局的最小尺寸
 * @return 最小尺寸
 * \endif
 */
QSize SARibbonBarLayout::minimumSize() const
{
	return sizeHint();
}

/**
 * \if ENGLISH
 * @brief Set the geometry of the layout
 * @param rect The rectangle to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置布局的几何区域
 * @param rect 要设置的矩形区域
 * \endif
 */
void SARibbonBarLayout::setGeometry(const QRect& rect)
{
	QLayout::setGeometry(rect);
	doLayout();
}

/**
 * \if ENGLISH
 * @brief Perform the layout
 * \endif
 *
 * \if CHINESE
 * @brief 执行布局
 * \endif
 */
void SARibbonBarLayout::doLayout()
{
	if (isLooseStyle()) {
		resizeInLooseStyle();
	} else {
		resizeInCompactStyle();
	}
}

/**
 * \if ENGLISH
 * @brief Check if the current style is loose style
 * @return True if loose style, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查当前样式是否为宽松样式
 * @return 如果是宽松样式返回 true，否则返回 false
 * \endif
 */
bool SARibbonBarLayout::isLooseStyle() const
{
	return SARibbonBar::isLooseStyle(d_ptr->ribbonStyle());
}

/**
 * \if ENGLISH
 * @brief Check if the current style is compact style
 * @return True if compact style, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查当前样式是否为紧凑样式
 * @return 如果是紧凑样式返回 true，否则返回 false
 * \endif
 */
bool SARibbonBarLayout::isCompactStyle() const
{
	return SARibbonBar::isCompactStyle(d_ptr->ribbonStyle());
}

/**
 * \if ENGLISH
 * @brief Get the title rectangle
 * @return The title rectangle
 * \endif
 *
 * \if CHINESE
 * @brief 获取标题栏矩形区域
 * @return 标题栏矩形区域
 * \endif
 */
const QRect& SARibbonBarLayout::titleRect() const
{
	return d_ptr->titleRect;
}

/**
 * \if ENGLISH
 * @brief Calculate the minimum tab bar width
 * @return The minimum tab bar width
 * @details This function calculates the minimum width for the tab bar when it is centered. The tab bar does not occupy the entire width, but uses the minimum width to fit its content, leaving space for the main window to receive mouse events for dragging and other operations.
 * \endif
 *
 * \if CHINESE
 * @brief 计算tabbar的最小尺寸
 * @return tabbar的最小宽度
 * @details 此函数的作用是在tabbar居中布局时，tabbar不是占满整个宽度，而是按照最小尺寸来占据宽度，让tabbar的长度刚刚好，这样能让出mainwindow的空间，接受鼠标事件，从而实现拖动等操作，否则tabbar占用整个顶栏，鼠标无法点击到mainwindow
 * \endif
 */
int SARibbonBarLayout::calcMinTabBarWidth() const
{
	SARibbonTabBar* tabBar = d_ptr->ribbonTabBar();
	if (!tabBar) {
		return 0;
	}
	const QMargins& mg = tabBar->tabMargin();
	return tabBar->sizeHint().width() + (mg.left() + mg.right());
}

/**
 * \if ENGLISH
 * @brief Set the system button size
 * @param size The size to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置系统按钮尺寸
 * @param size 要设置的尺寸
 * \endif
 */
void SARibbonBarLayout::setSystemButtonSize(const QSize& size)
{
	d_ptr->systemButtonSize = size;
}

/**
 * \if ENGLISH
 * @brief Set whether the tab bar is on the title bar
 * @param on True to place tab on title, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 设置tab是否在标题栏上
 * @param on true 表示 tab 在标题栏上，false 表示不在
 * \endif
 */
void SARibbonBarLayout::setTabOnTitle(bool on)
{
	if (d_ptr->isTabOnTitle != on) {
		d_ptr->isTabOnTitle = on;
		d_ptr->resetSize();
	}
}

/**
 * \if ENGLISH
 * @brief Check if the tab bar is on the title bar
 * @return True if tab is on title, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查tab是否在标题栏上
 * @return 如果 tab 在标题栏上返回 true，否则返回 false
 * \endif
 */
bool SARibbonBarLayout::isTabOnTitle() const
{
	return d_ptr->isTabOnTitle;
}

/**
 * \if ENGLISH
 * @brief Get the main bar height in minimum mode
 * @return The main bar height in minimum mode
 * \endif
 *
 * \if CHINESE
 * @brief 最小模式下的主栏高度
 * @return 最小模式下的主栏高度
 * \endif
 */
int SARibbonBarLayout::minimumModeMainBarHeight() const
{
	return d_ptr->minimumModeMainBarHeight();
}

/**
 * \if ENGLISH
 * @brief Get the main bar height in normal mode
 * @return The main bar height in normal mode
 * @note SARibbonBar::height and mainBarHeight may not be equal in RibbonState::MinimumRibbonMode
 * \endif
 *
 * \if CHINESE
 * @brief 正常模式下的主栏高度
 * @return 正常模式下的主栏高度
 * @note 有可能SARibbonBar::height和mainBarHeight不相等，这种情况发生在RibbonState::MinimumRibbonMode状态下
 * \endif
 */
int SARibbonBarLayout::normalModeMainBarHeight() const
{
	return d_ptr->normalModeMainBarHeight();
}

/**
 * \if ENGLISH
 * @brief Get the tab bar height
 * @return The tab bar height
 * \endif
 *
 * \if CHINESE
 * @brief 获取标签栏高度
 * @return 标签栏高度
 * \endif
 */
int SARibbonBarLayout::tabBarHeight() const
{
	return d_ptr->getActualTabBarHeight();
}

/**
 * \if ENGLISH
 * @brief Set the tab bar height
 * @param h The height to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置标签栏高度
 * @param h 要设置的高度
 * \endif
 */
void SARibbonBarLayout::setTabBarHeight(int h)
{
	d_ptr->setTabBarHeight(h);
}

/**
 * \if ENGLISH
 * @brief Get the title bar height
 * @return The title bar height
 * @sa setTitleBarHeight
 * \endif
 *
 * \if CHINESE
 * @brief 获取标题栏高度
 * @return 标题栏高度
 * @sa setTitleBarHeight
 * \endif
 */
int SARibbonBarLayout::titleBarHeight() const
{
	return d_ptr->getActualTitleBarHeight();
}

/**
 * \if ENGLISH
 * @brief Set the title bar height
 * @param h The height to set
 * @note This operation will emit titleBarHeightChanged signal
 * @sa titleBarHeight
 * \endif
 *
 * \if CHINESE
 * @brief 设置标题栏高度
 * @param h 要设置的高度
 * @note 此操作会发射 titleBarHeightChanged 信号
 * @sa titleBarHeight
 * \endif
 */
void SARibbonBarLayout::setTitleBarHeight(int h)
{
	d_ptr->setTitleBarHeight(h);
}

/**
 * \if ENGLISH
 * @brief Get the category height
 * @return The category height
 * \endif
 *
 * \if CHINESE
 * @brief 获取类别高度
 * @return 类别高度
 * \endif
 */
int SARibbonBarLayout::categoryHeight() const
{
	return d_ptr->getActualCategoryHeight();
}

/**
 * \if ENGLISH
 * @brief Set the category height
 * @param h The height to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置类别高度
 * @param h 要设置的高度
 * \endif
 */
void SARibbonBarLayout::setCategoryHeight(int h)
{
	d_ptr->setCategoryHeight(h);
}

/**
 * \if ENGLISH
 * @brief Get the panel title height
 * @return The panel title height
 * \endif
 *
 * \if CHINESE
 * @brief 获取面板标题高度
 * @return 面板标题高度
 * \endif
 */
int SARibbonBarLayout::panelTitleHeight() const
{
	return d_ptr->panelTitleHeight;
}

/**
 * \if ENGLISH
 * @brief Set the panel title height
 * @param h The height to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置面板标题高度
 * @param h 要设置的高度
 * \endif
 */
void SARibbonBarLayout::setPanelTitleHeight(int h)
{
	d_ptr->panelTitleHeight = h;
}

/**
 * \if ENGLISH
 * @brief Set the window icon
 * @param icon The icon to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置主窗口左上角的图标
 * @param icon 要设置的图标
 * \endif
 */
void SARibbonBarLayout::setWindowIcon(const QIcon& icon)
{
	titleIconWidget()->setIcon(icon);
}

/**
 * \if ENGLISH
 * @brief Get the window icon
 * @return The window icon
 * \endif
 *
 * \if CHINESE
 * @brief 获取窗口图标
 * @return 窗口图标
 * \endif
 */
QIcon SARibbonBarLayout::windowIcon() const
{
	return titleIconWidget()->icon();
}

/**
 * \if ENGLISH
 * @brief Set whether the application button is vertically expanded
 * @param on True to expand vertically, false otherwise
 * @details When vertically expanded, the application button will occupy both the title bar and tab bar heights
 * \endif
 *
 * \if CHINESE
 * @brief 设置ApplicationButton是否垂直方向扩充
 * @param on true 表示垂直扩充，false 表示不扩充
 * @details 垂直扩充时，ApplicationButton能占用标题栏和tab栏两个栏的高度
 * \endif
 */
void SARibbonBarLayout::setApplicationButtonVerticalExpansion(bool on)
{
	d_ptr->isApplicationButtonVerticalExpansion = on;
	if (on) {
		if (SARibbonTitleIconWidget* iconWidget = titleIconWidget()) {
			iconWidget->setVisible(!on);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Check if the application button is vertically expanded
 * @return True if vertically expanded, false otherwise
 * @note Default is false
 * \endif
 *
 * \if CHINESE
 * @brief 检查applicationButton是否在垂直方向扩充
 * @return 如果垂直扩充返回 true，否则返回 false
 * @note 默认为 false
 * \endif
 */
bool SARibbonBarLayout::isApplicationButtonVerticalExpansion() const
{
	return d_ptr->isApplicationButtonVerticalExpansion;
}

/**
 * \if ENGLISH
 * @brief Get the ribbon bar
 * @return The ribbon bar
 * \endif
 *
 * \if CHINESE
 * @brief 获取功能区栏
 * @return 功能区栏
 * \endif
 */
SARibbonBar* SARibbonBarLayout::ribbonBar() const
{
	return d_ptr->ribbonBar;
}

/**
 * \if ENGLISH
 * @brief Get the ribbon tab bar
 * @return The ribbon tab bar
 * \endif
 *
 * \if CHINESE
 * @brief 获取功能区标签栏
 * @return 功能区标签栏
 * \endif
 */
SARibbonTabBar* SARibbonBarLayout::ribbonTabBar() const
{
	return d_ptr->ribbonTabBar();
}

/**
 * \if ENGLISH
 * @brief Get the stacked container widget
 * @return The stacked container widget
 * \endif
 *
 * \if CHINESE
 * @brief 获取堆叠容器窗口
 * @return 堆叠容器窗口
 * \endif
 */
SARibbonStackedWidget* SARibbonBarLayout::stackedContainerWidget() const
{
	return d_ptr->stackedContainerWidget();
}

/**
 * \if ENGLISH
 * @brief Get the quick access bar
 * @return The quick access bar
 * \endif
 *
 * \if CHINESE
 * @brief 获取快速访问栏
 * @return 快速访问栏
 * \endif
 */
SARibbonQuickAccessBar* SARibbonBarLayout::quickAccessBar() const
{
	return d_ptr->quickAccessBar();
}

/**
 * \if ENGLISH
 * @brief Get the right button group
 * @return The right button group
 * \endif
 *
 * \if CHINESE
 * @brief 获取右侧按钮组
 * @return 右侧按钮组
 * \endif
 */
SARibbonButtonGroupWidget* SARibbonBarLayout::rightButtonGroup() const
{
	return d_ptr->rightButtonGroup();
}

/**
 * \if ENGLISH
 * @brief Get the application button
 * @return The application button
 * \endif
 *
 * \if CHINESE
 * @brief 获取应用程序按钮
 * @return 应用程序按钮
 * \endif
 */
QAbstractButton* SARibbonBarLayout::applicationButton() const
{
	return d_ptr->applicationButton();
}

/**
 * \if ENGLISH
 * @brief Get the title icon widget
 * @return The title icon widget
 * \endif
 *
 * \if CHINESE
 * @brief 获取标题图标窗口
 * @return 标题图标窗口
 * \endif
 */
SARibbonTitleIconWidget* SARibbonBarLayout::titleIconWidget() const
{
	return d_ptr->titleIconWidget();
}

/**
 * \if ENGLISH
 * @brief Layout the title rectangle
 * \endif
 *
 * \if CHINESE
 * @brief 布局标题栏矩形区域
 * \endif
 */
void SARibbonBarLayout::layoutTitleRect()
{
	SARibbonBar* ribbon                    = d_ptr->ribbonBar;
	SARibbonQuickAccessBar* quickAccessBar = d_ptr->quickAccessBar();
	SARibbonTabBar* ribbonTabBar           = d_ptr->ribbonTabBar();
	const QMargins border                  = d_ptr->contentsMargins();
	const int validTitleBarHeight          = d_ptr->getActualTitleBarHeight();

	// 计算标题栏区域
	if (SA::saIsRTL()) {
		/// RTL mode: title rect positions are mirrored
		if (isCompactStyle()) {
			// 紧凑模式 RTL: title bar in the remaining space left of tabbar
			// In RTL, titleEnd is the left edge of tabbar (mirrored from right)
			int titleEnd   = ribbonTabBar->geometry().left();
			int titleWidth = quickAccessBar ? (titleEnd - quickAccessBar->geometry().right())
											: (titleEnd - border.left());
			if (titleWidth > 10) {
				d_ptr->titleRect = QRect(titleEnd - titleWidth, border.top(), titleWidth, validTitleBarHeight);
			} else {
				d_ptr->titleRect = QRect();
			}
		} else {
			// 三行宽松模式 RTL
			const int tabX = ribbonTabBar->geometry().x();
			// In RTL, contextRegionLeft becomes the leftmost (visual rightmost) tab boundary
			// and contextRegionRight becomes the rightmost (visual leftmost) tab boundary
			// We swap the variable semantics to reflect the mirrored geometry
			int contextRegionLeft  = -1;   // In RTL: the left boundary of context region (visual rightmost)
			int contextRegionRight = ribbon->width();  // In RTL: the right boundary of context region (visual leftmost)

			QList< int > visibleContextIndex = ribbon->currentVisibleContextCategoryTabIndexs();
			if (!visibleContextIndex.empty()) {
				// In RTL, first context tab rect's left() is visually the rightmost edge
				int edgeVal = ribbonTabBar->tabRect(visibleContextIndex.first()).left() + tabX;
				if (edgeVal > contextRegionLeft) {
					contextRegionLeft = edgeVal;
				}
				// In RTL, last context tab rect's right() is visually the leftmost edge
				edgeVal = d_ptr->ribbonTabBar()->tabRect(visibleContextIndex.last()).right() + tabX;
				if (edgeVal < contextRegionRight) {
					contextRegionRight = edgeVal;
				}
			}

			// In RTL, x1 is the right side of the quickAccessBar (which is now on the right)
			int x1 = border.left();
			if (quickAccessBar) {
				// In RTL, quickAccessBar is at right side, so title area starts from its left edge
				x1 = quickAccessBar->geometry().x();
			}
			// In RTL, x2 is the left side (system buttons are at left)
			int x2 = border.left() + d_ptr->systemButtonSize.width();

			if (contextRegionLeft < 0) {
				// No context category: title between system buttons (left) and quickAccessBar (right)
				d_ptr->titleRect = QRect(QPoint(x2, border.top()), QPoint(x1, validTitleBarHeight + border.top()));
			} else {
				int leftwidth  = contextRegionRight - x2;
				int rightwidth = x1 - contextRegionLeft;
				if (leftwidth > rightwidth) {
					d_ptr->titleRect =
						QRect(QPoint(x2, border.top()), QPoint(contextRegionRight, validTitleBarHeight + border.top()));
				} else {
					d_ptr->titleRect =
						QRect(QPoint(contextRegionLeft, border.top()), QPoint(x1, validTitleBarHeight + border.top()));
				}
			}
		}
	} else {
		/// LTR mode: original title rect logic unchanged
		if (isCompactStyle()) {
			// 紧凑模式,紧凑模式的标题栏在tabbar的剩余空间中
			int titleStart = ribbonTabBar->geometry().right();
			int titleWidth = quickAccessBar ? (quickAccessBar->x() - titleStart)
											: (ribbon->width() - titleStart - d_ptr->systemButtonSize.width());
			if (titleWidth > 10) {
				d_ptr->titleRect = QRect(titleStart, border.top(), titleWidth, validTitleBarHeight);
			} else {
				// 标题栏过小，就不显示
				d_ptr->titleRect = QRect();
			}
		} else {
			const int tabX = ribbonTabBar->geometry().x();
			// 三行宽松模式
			int contextRegionLeft  = ribbon->width();
			int contextRegionRight = -1;

			// 使用上下文标签的视觉数据
			// 上下文标签会占用宽松模式下的标题栏位置，因此，要计算此时标题栏应该在哪里显示
			QList< int > visibleContextIndex = ribbon->currentVisibleContextCategoryTabIndexs();
			if (!visibleContextIndex.empty()) {
				int edgeVal = ribbonTabBar->tabRect(visibleContextIndex.first()).left() + tabX;
				if (edgeVal < contextRegionLeft) {
					contextRegionLeft = edgeVal;
				}
				edgeVal = d_ptr->ribbonTabBar()->tabRect(visibleContextIndex.last()).right() + tabX;
				if (edgeVal > contextRegionRight) {
					contextRegionRight = edgeVal;
				}
			}

			int x1 = border.left();
			if (quickAccessBar) {
				x1 = quickAccessBar->geometry().right() + 1;
			}
			int x2 = ribbon->width() - d_ptr->systemButtonSize.width() - border.right();

			if (contextRegionRight < 0) {
				// 说明没有上下文标签，那么标题直接放在quickAccessBar到systembar之间
				d_ptr->titleRect = QRect(QPoint(x1, border.top()), QPoint(x2, validTitleBarHeight + border.top()));
			} else {
				int leftwidth  = contextRegionLeft - x1;
				int rightwidth = x2 - contextRegionRight;
				if (rightwidth > leftwidth) {
					d_ptr->titleRect =
						QRect(QPoint(contextRegionRight, border.top()), QPoint(x2, validTitleBarHeight + border.top()));
				} else {
					d_ptr->titleRect =
						QRect(QPoint(x1, border.top()), QPoint(contextRegionLeft, validTitleBarHeight + border.top()));
				}
			}
		}
	}
}

/**
 * \if ENGLISH
 * @brief Reset the size
 * \endif
 *
 * \if CHINESE
 * @brief 重置尺寸
 * \endif
 */
void SARibbonBarLayout::resetSize()
{
	d_ptr->resetSize();
}

/**
 * \if ENGLISH
 * @brief Layout the stacked container widget
 * \endif
 *
 * \if CHINESE
 * @brief 布局堆叠容器窗口
 * \endif
 */
void SARibbonBarLayout::layoutStackedContainerWidget()
{
	SARibbonBar* ribbon                  = ribbonBar();
	SARibbonStackedWidget* stackedWidget = stackedContainerWidget();
	SARibbonTabBar* tabBar               = ribbonTabBar();
	if (!ribbon || !stackedWidget || !tabBar) {
		return;
	}
	QMargins border            = d_ptr->contentsMargins();
	QRect ribbonTabBarGeometry = tabBar->geometry();

	int x = border.left();
	int y = ribbonTabBarGeometry.bottom() + 1;
	int w = ribbon->width() - border.left() - border.right();
	int h = d_ptr->getActualCategoryHeight();

	if (stackedWidget->isPopupMode()) {
		QPoint absPosition = ribbon->mapToGlobal(QPoint(x, y));
		x                  = absPosition.x();
		y                  = absPosition.y();
	}
	// 受布局影响，这里不能使用stackedWidget->setGeometry(x, y, w, h);
	// stackedWidget->setGeometry(x, y, w, h);
	//    stackedWidget->move(x, y);
	//    stackedWidget->setFixedSize(QSize(w, h));
	stackedWidget->setNormalGeometry(QRect(x, y, w, h));
}

/**
 * \if ENGLISH
 * @brief Layout the category
 * @details This function is called when adjusting the alignment of the category. Since the size of StackedContainerWidget does not change when the alignment changes, but the category needs to be re-layouted, a request needs to be emitted.
 * \endif
 *
 * \if CHINESE
 * @brief 让category重新布局
 * @details 这个函数在调整category的对其方式的时候调用，由于对其方式改变StackedContainerWidget的尺寸没有改变，但category要重新布局,因此需要发射一个
 * \endif
 */
void SARibbonBarLayout::layoutCategory()
{
	if (SARibbonStackedWidget* stackedWidget = stackedContainerWidget()) {
		stackedWidget->layoutRequestInnerWidgets();
	}
}

/**
 * \if ENGLISH
 * @brief Resize in loose style
 * \endif
 *
 * \if CHINESE
 * @brief 宽松模式下调整尺寸
 * \endif
 */
void SARibbonBarLayout::resizeInLooseStyle()
{

	QMargins border = d_ptr->contentsMargins();
	int x           = border.left();
	int y           = border.top();

	const int validTitleBarHeight = d_ptr->getActualTitleBarHeight();
	const int titleBarControlHeight = validTitleBarHeight - 2;  // 标题栏上的控件高度是标题栏高度-2，上下各减1px
	const int tabH                = d_ptr->getActualTabBarHeight();
	const int tabBarControlHeight = tabH;  // tabbar上面的控件高度是tabbar高度-2，上下各减1px
	const bool isAppBtnVExpan     = d_ptr->isApplicationButtonVerticalExpansion;
	int barMinWidth               = 0;  ///< 记录ribbonBar的最小宽度，这个用于给推荐宽度

	SARibbonBar* ribbon                      = ribbonBar();
	SARibbonTabBar* tabBar                   = ribbonTabBar();
	QAbstractButton* appBtn                  = applicationButton();
	SARibbonQuickAccessBar* qb               = quickAccessBar();
	SARibbonTitleIconWidget* titleIcon       = titleIconWidget();
	SARibbonButtonGroupWidget* rightBtnGroup = rightButtonGroup();

	if (SA::saIsRTL()) {
		/// RTL mode: widgets are mirrored — start from right edge, retreat leftward
		x = ribbon->width() - border.right();

		/// 1. 布局corner widget (TopLeftCorner becomes right side in RTL)
		if (QWidget* connerL = ribbon->cornerWidget(Qt::TopLeftCorner)) {
			if (connerL->isVisibleTo(ribbon)) {
				QSize connerSize = connerL->sizeHint();
				connerSize       = SA::scaleSizeByHeight(connerSize, validTitleBarHeight);
				x -= connerSize.width();
				connerL->setGeometry(x, y, connerSize.width(), connerSize.height());
				x -= 5;  // spacing
			}
		}

		/// 2. 布局图标窗口或app button (mirrored to right edge)
		if (isAppBtnVExpan) {
			//! 如果Application button是纵向扩展模式，那么不显示titleicon
			//! 纵向扩展的Application button将占用title和tab的高度
			if (appBtn && appBtn->isVisibleTo(ribbon)) {
				QSize appBtnSize = appBtn->sizeHint();
				int appHeight    = titleBarControlHeight + tabH;
				appHeight -= 2;
				appBtnSize = SA::scaleSizeByHeight(appBtnSize, appHeight, 1.5);
				x -= appBtnSize.width();
				appBtn->setGeometry(x, y + 1, appBtnSize.width(), appBtnSize.height());
			}
		} else {
			//! Application button不是纵向扩展，显示icon
			if (titleIcon && titleIcon->isVisibleTo(ribbon)) {
				QSize titleiconSizeHint = titleIcon->sizeHint();
				titleiconSizeHint.scale(titleBarControlHeight, titleBarControlHeight, Qt::KeepAspectRatio);
				x -= titleiconSizeHint.width();
				titleIcon->setGeometry(x, y + 1, titleiconSizeHint.width(), titleiconSizeHint.height());
			}
		}

		/// 3. 布局quick access bar (positioned at right side in RTL)
		if (qb && qb->isVisibleTo(ribbon)) {
			QSize quickAccessBarSize = qb->sizeHint();
			x -= quickAccessBarSize.width();
			qb->setGeometry(x, y + 1, quickAccessBarSize.width(), titleBarControlHeight);
		}

		/// titleBar上的元素布局完成，开始布局第二行 — start from right edge
		x = ribbon->width() - border.right();
		y += validTitleBarHeight;

		/// 4. 布局 applicationButton（第二行，mirrored to right edge）
		if (appBtn && appBtn->isVisibleTo(ribbon)) {
			if (isAppBtnVExpan) {
				///! 如果是纵向扩展的Application button则只更新x位置，在第2步已经设定了位置
				x = appBtn->geometry().left() - 1;  // retreat past the appBtn left edge
			} else {
				QSize appBtnSize = appBtn->sizeHint();
				appBtnSize       = SA::scaleSizeByHeight(appBtnSize, tabBarControlHeight, 1.5);
				x -= appBtnSize.width();
				appBtn->setGeometry(x, y + 1, appBtnSize.width(), appBtnSize.height());
				barMinWidth += appBtnSize.width();
			}
		}

		/// 5. 布局corner widget (TopRightCorner becomes left side in RTL)
		/// 由于这个窗口一定要在最左（RTL镜像后），因此先对这个窗口进行布局
		int endX = border.left();

		if (QWidget* connerTR = ribbon->cornerWidget(Qt::TopRightCorner)) {
			if (connerTR->isVisibleTo(ribbon)) {
				QSize connerSize = connerTR->sizeHint();
				connerSize       = SA::scaleSizeByHeight(connerSize, tabBarControlHeight);
				connerTR->setGeometry(endX, y + 1, connerSize.width(), connerSize.height());
				endX += connerSize.width();
				barMinWidth += connerSize.width();
			}
		}

		/// 6. rightButtonGroup positioned at left side in RTL (swapped with QuickAccessBar)
		if (rightBtnGroup && rightBtnGroup->isVisibleTo(ribbon)) {
			QSize rightBtnGroupSize = rightBtnGroup->sizeHint();
			endX += 1;  // 先偏移1px
			d_ptr->rightButtonGroup()->setGeometry(endX, y + 1, rightBtnGroupSize.width(), tabBarControlHeight);
			endX += rightBtnGroupSize.width();
			barMinWidth += rightBtnGroupSize.width();
		}

		/// 7. 布局tabbar — remaining area between endX(left) and x(right)
		int tabBarWidth = x - endX;
		if (tabBarWidth < 10) {
			tabBarWidth = 10;
		}

		int mintabBarWidth = calcMinTabBarWidth();
		barMinWidth += mintabBarWidth;
		if (tabBar) {
			if (ribbon->ribbonAlignment() == SARibbonAlignment::AlignLeft) {
				// AlignLeft in RTL means tabs start from the right edge (visual right)
				tabBar->setGeometry(endX, y, tabBarWidth, tabH);
			} else if (ribbon->ribbonAlignment() == SARibbonAlignment::AlignRight) {
				// AlignRight in RTL means tabs start from the left edge (visual left)
				if (mintabBarWidth < tabBarWidth) {
					tabBarWidth = mintabBarWidth;
				}
				tabBar->setGeometry(x - tabBarWidth, y, tabBarWidth, tabH);
			} else {
				// AlignCenter: center the tab bar in RTL
				if (mintabBarWidth >= tabBarWidth) {
					tabBar->setGeometry(endX, y, tabBarWidth, tabH);
				} else {
					int xoffset = (tabBarWidth - mintabBarWidth) / 2;
					tabBar->setGeometry(endX + xoffset, y, mintabBarWidth, tabH);
				}
			}
		}

		d_ptr->minWidth = barMinWidth;
		layoutTitleRect();
		layoutStackedContainerWidget();
	} else {
		/// LTR mode: original layout logic unchanged

		/// 1. 布局corner widget
		if (QWidget* connerL = ribbon->cornerWidget(Qt::TopLeftCorner)) {
			if (connerL->isVisibleTo(ribbon)) {
				QSize connerSize = connerL->sizeHint();
				connerSize       = SA::scaleSizeByHeight(connerSize, validTitleBarHeight);
				connerL->setGeometry(x, y, connerSize.width(), connerSize.height());
				x = connerL->geometry().right() + 5;
			}
		}

		/// 2. 布局图标窗口或app button
		if (isAppBtnVExpan) {
			//! 如果Application button是纵向扩展模式，那么不显示titleicon
			//! 纵向扩展的Application button将占用title和tab的高度
			if (appBtn && appBtn->isVisibleTo(ribbon)) {
				QSize appBtnSize = appBtn->sizeHint();
				// 纵向扩展模式，appBtn的高度撑满titleBarControlHeight+tabH
				int appHeight = titleBarControlHeight + tabH;
				appHeight -= 2;  // 上下留1px
				appBtnSize = SA::scaleSizeByHeight(appBtnSize, appHeight, 1.5);
				appBtn->setGeometry(x, y + 1, appBtnSize.width(), appBtnSize.height());
				x = appBtn->geometry().right();
			}
		} else {
			//! Application button不是纵向扩展，显示icon
			if (titleIcon && titleIcon->isVisibleTo(ribbon)) {
				QSize titleiconSizeHint = titleIcon->sizeHint();
				titleiconSizeHint.scale(titleBarControlHeight, titleBarControlHeight, Qt::KeepAspectRatio);
				titleIcon->setGeometry(x, y + 1, titleiconSizeHint.width(), titleiconSizeHint.height());
				x += titleiconSizeHint.width();
			}
		}

		/// 3. 布局quick access bar
		if (qb && qb->isVisibleTo(ribbon)) {
			QSize quickAccessBarSize = qb->sizeHint();
			//! 这里不用SA::scaleSizeByHeight缩减quickAccessBar的比例：
			//! quickAccessBarSize = SA::scaleSizeByHeight(quickAccessBarSize, titleBarControlHeight);
			//! 原因是，如果quickAccessBar最后是一个widget，如果长度不足，这个widget是不会显示出来，假如默认QToolBar的高度是32，
			//! 而titlebar的高度一是28，肯定会导致宽度缩减，如果宽度缩减，且最后是一个窗口，那么这个窗口会不显示
			//! 针对工具栏类型的窗口，宽度高度都不进行比例缩减
			// 上下留1px的边线
			qb->setGeometry(x, y + 1, quickAccessBarSize.width(), titleBarControlHeight);
		}

		/// titleBar上的元素布局完成，开始布局第二行
		x = border.left();
		y += validTitleBarHeight;  // 此时，y值在titlebar下面

		/// 4. 布局 applicationButton（第二行）
		if (appBtn && appBtn->isVisibleTo(ribbon)) {
			if (isAppBtnVExpan) {
				///! 如果是纵向扩展的Application button则只更新x位置，在第2步已经设定了位置
				x = appBtn->geometry().right();
			} else {
				QSize appBtnSize = appBtn->sizeHint();
				// appBtnSize的sizehint是根据文字宽度来推荐，如果按高度来扩展，会显得有点大，直接设置高度，又显得有点小
				// 因此宽高不按1:1比例扩展，按1:1.5比例扩展，也就是高度扩展3倍，宽度扩展3/1.5倍
				// 目前看这个比例相对比较协调
				appBtnSize = SA::scaleSizeByHeight(appBtnSize, tabBarControlHeight, 1.5);
				appBtn->setGeometry(x, y + 1, appBtnSize.width(), appBtnSize.height());
				x = appBtn->geometry().right();
				// 累加到最小宽度中
				barMinWidth += appBtnSize.width();
			}
		}

		/// 5. 布局右上角corner widget
		/// cornerWidget(Qt::TopRightCorner)是一定要配置的，对于多文档窗口，子窗口的缩放恢复按钮就是通过这个窗口实现，
		/// 由于这个窗口一定要在最右，因此先对这个窗口进行布局
		int endX = ribbon->width() - border.right();

		if (QWidget* connerTR = ribbon->cornerWidget(Qt::TopRightCorner)) {
			if (connerTR->isVisibleTo(ribbon)) {
				QSize connerSize = connerTR->sizeHint();
				connerSize       = SA::scaleSizeByHeight(connerSize, tabBarControlHeight);
				endX -= connerSize.width();
				connerTR->setGeometry(endX, y + 1, connerSize.width(), connerSize.height());
				// 累加到最小宽度中
				barMinWidth += connerSize.width();
			}
		}

		/// 6. tabBar右边的附加按钮组rightButtonGroup，这里一般会附加一些类似登录等按钮组
		if (rightBtnGroup && rightBtnGroup->isVisibleTo(ribbon)) {
			QSize rightBtnGroupSize = rightBtnGroup->sizeHint();
			//! 这里不用SA::scaleSizeByHeight缩减rightButtonGroup的比例：
			//! rightBtnGroupSize       = SA::scaleSizeByHeight(rightBtnGroupSize, tabBarControlHeight);
			//! 针对工具栏类型的窗口，宽度高度都不进行比例缩减
			endX -= 1;  // 先偏移1px
			endX -= rightBtnGroupSize.width();
			d_ptr->rightButtonGroup()->setGeometry(endX, y + 1, rightBtnGroupSize.width(), tabBarControlHeight);
			// 累加到最小宽度中
			barMinWidth += rightBtnGroupSize.width();
		}

		/// 7. 布局tabbar，此时已经确定了applicationButton的位置，以及最右边rightButtonGroup的位置，剩下的区域都给tabbar
		int tabBarWidth = endX - x;
		// 这里极度压缩有可能导致负数因此不能小于0
		if (tabBarWidth < 10) {
			tabBarWidth = 10;
		}

		int mintabBarWidth = calcMinTabBarWidth();
		// 累加到最小宽度中
		barMinWidth += mintabBarWidth;
		if (tabBar) {
			if (ribbon->ribbonAlignment() == SARibbonAlignment::AlignLeft) {
				// 左对齐的tabbar，直接设置位置
				tabBar->setGeometry(x, y, tabBarWidth, tabH);
			} else if (ribbon->ribbonAlignment() == SARibbonAlignment::AlignRight) {
				// 右对齐的情况下，Tab要靠右显示
				if (mintabBarWidth >= tabBarWidth) {
					// 这时tabbar没有右对齐的必要性，空间位置不够了
					tabBar->setGeometry(x, y, tabBarWidth, tabH);
				} else {
					// 说明tabbar的宽度有右对齐的可能性
					tabBar->setGeometry(endX - mintabBarWidth, y, mintabBarWidth, tabH);
				}
			} else {
				// 居中对齐的情况下，Tab要居中显示
				// 得到tab的推荐尺寸
				if (mintabBarWidth >= tabBarWidth) {
					// 这时tabbar没有居中对齐的必要性，空间位置不够了
					tabBar->setGeometry(x, y, tabBarWidth, tabH);
				} else {
					// 说明tabbar的宽度有居中的可能性
					int xoffset = (tabBarWidth - mintabBarWidth) / 2;
					tabBar->setGeometry(x + xoffset, y, mintabBarWidth, tabH);
				}
			}
		}

		d_ptr->minWidth = barMinWidth;

		// 5. 更新标题区域
		layoutTitleRect();

		// 6. 调整 stackedContainerWidget
		layoutStackedContainerWidget();
	}
}

/**
 * \if ENGLISH
 * @brief Resize in compact style
 * \endif
 *
 * \if CHINESE
 * @brief 紧凑模式下调整尺寸
 * \endif
 */
void SARibbonBarLayout::resizeInCompactStyle()
{
	QMargins border               = d_ptr->contentsMargins();
	int x                         = border.left();
	int y                         = border.top();
	int barMinWidth               = 0;  ///< 记录ribbonBar的最小宽度，这个用于给推荐宽度
	const int validTitleBarHeight = d_ptr->getActualTitleBarHeight();
	const int titleBarControlHeight = validTitleBarHeight - 2;  // 标题栏上的控件高度是标题栏高度-2，上下各减1px

	SARibbonBar* ribbon                      = ribbonBar();
	SARibbonTabBar* tabBar                   = ribbonTabBar();
	QAbstractButton* appBtn                  = applicationButton();
	SARibbonQuickAccessBar* qb               = quickAccessBar();
	SARibbonTitleIconWidget* titleIcon       = titleIconWidget();
	SARibbonButtonGroupWidget* rightBtnGroup = rightButtonGroup();

	if (SA::saIsRTL()) {
		/// RTL mode in compact style: start from right edge, retreat leftward
		x = ribbon->width() - border.right();

		/// 1. 布局corner widget - TopLeftCorner becomes right side in RTL
		if (QWidget* connerL = ribbon->cornerWidget(Qt::TopLeftCorner)) {
			if (connerL->isVisibleTo(ribbon)) {
				QSize connerSize = connerL->sizeHint();
				connerSize       = SA::scaleSizeByHeight(connerSize, validTitleBarHeight);
				x -= connerSize.width();
				connerL->setGeometry(x, y, connerSize.width(), connerSize.height());
				barMinWidth += connerSize.width();
			}
		}

		/// 2. 布局图标窗口 (mirrored to right edge)
		if (titleIcon && titleIcon->isVisibleTo(ribbon)) {
			QSize titleiconSizeHint = titleIcon->sizeHint();
			titleiconSizeHint.scale(titleBarControlHeight, titleBarControlHeight, Qt::KeepAspectRatio);
			x -= titleiconSizeHint.width();
			titleIcon->setGeometry(x, y + 1, titleiconSizeHint.width(), titleiconSizeHint.height());
			barMinWidth += titleiconSizeHint.width();
		}

		/// 3. 布局applicationButton (mirrored to right edge)
		if (appBtn && appBtn->isVisibleTo(ribbon)) {
			QSize appBtnSize = appBtn->sizeHint();
			appBtnSize       = SA::scaleSizeByHeight(appBtnSize, titleBarControlHeight, 1.5);
			x -= appBtnSize.width();
			appBtn->setGeometry(x, y + 1, appBtnSize.width(), appBtnSize.height());
			barMinWidth += appBtnSize.width();
		}

		/// Right-side elements now advance from left edge (system buttons, corners, rightBtnGroup, quickAccessBar)
		int endX = border.left();
		if (ribbon->isUseRibbonFrame()) {
			// System buttons at left edge in RTL
			endX += d_ptr->systemButtonSize.width();
		}

		/// 4. 布局corner widget - TopRightCorner becomes left side in RTL
		if (QWidget* connerTR = ribbon->cornerWidget(Qt::TopRightCorner)) {
			if (connerTR->isVisibleTo(ribbon)) {
				QSize connerSize = connerTR->sizeHint();
				connerSize       = SA::scaleSizeByHeight(connerSize, validTitleBarHeight);
				connerTR->setGeometry(endX, y, connerSize.width(), connerSize.height());
				endX += connerSize.width();
				barMinWidth += connerSize.width();
			}
		}

		/// 5. rightButtonGroup positioned at left side in RTL (swapped with QuickAccessBar)
		if (rightBtnGroup && rightBtnGroup->isVisibleTo(ribbon)) {
			QSize rightBtnGroupSize = rightBtnGroup->sizeHint();
			d_ptr->rightButtonGroup()->setGeometry(endX, y + 1, rightBtnGroupSize.width(), titleBarControlHeight);
			endX += rightBtnGroupSize.width();
			barMinWidth += rightBtnGroupSize.width();
		}

		/// 6. quickAccessBar positioned at left side in RTL (swapped with rightButtonGroup)
		if (qb && qb->isVisibleTo(ribbon)) {
			QSize quickAccessBarSize = qb->sizeHint();
			qb->setGeometry(endX, y + 1, quickAccessBarSize.width(), titleBarControlHeight);
			endX += quickAccessBarSize.width();
			barMinWidth += quickAccessBarSize.width();
		}

		/// 7. 布局 tab bar — remaining area between endX (left) and x (right)
		int tabH = d_ptr->getActualTabBarHeight();
		if (tabH > titleBarControlHeight) {
			tabH = titleBarControlHeight;
		}
		int tabBarWidth = x - endX;
		if (tabBarWidth < 10) {
			tabBarWidth = 10;
		}
		int mintabBarWidth = calcMinTabBarWidth();
		barMinWidth += mintabBarWidth;
		if (tabBar) {
			y = border.top() + (titleBarControlHeight - tabH);
			if (ribbon->ribbonAlignment() == SARibbonAlignment::AlignLeft) {
				// AlignLeft in RTL means tabs start from the right edge (visual right)
				if (mintabBarWidth < tabBarWidth) {
					tabBarWidth = mintabBarWidth;
				}
				tabBar->setGeometry(x - tabBarWidth, y, tabBarWidth, tabH);
			} else if (ribbon->ribbonAlignment() == SARibbonAlignment::AlignRight) {
				// AlignRight in RTL means tabs start from the left edge (visual left)
				if (mintabBarWidth < tabBarWidth) {
					tabBarWidth = mintabBarWidth;
				}
				tabBar->setGeometry(endX, y, tabBarWidth, tabH);
			} else {
				// AlignCenter: center the tab bar in RTL
				if (mintabBarWidth >= tabBarWidth) {
					tabBar->setGeometry(endX, y, tabBarWidth, tabH);
				} else {
					int xoffset = (tabBarWidth - mintabBarWidth) / 2;
					tabBar->setGeometry(endX + xoffset, y, mintabBarWidth, tabH);
				}
			}
		}

		d_ptr->minWidth = barMinWidth;
		layoutTitleRect();
		layoutStackedContainerWidget();
	} else {
		/// LTR mode: original compact style layout logic unchanged

		/// 1.  布局corner widget - TopLeftCorner
		if (QWidget* connerL = ribbon->cornerWidget(Qt::TopRightCorner)) {
			if (connerL->isVisibleTo(ribbon)) {
				QSize connerSize = connerL->sizeHint();
				connerSize       = SA::scaleSizeByHeight(connerSize, validTitleBarHeight);
				connerL->setGeometry(x, y, connerSize.width(), connerSize.height());
				x = connerL->geometry().right();
				// 累加到最小宽度中
				barMinWidth += connerSize.width();
			}
		}

		/// 2. 布局图标窗口
		if (titleIcon && titleIcon->isVisibleTo(ribbon)) {
			QSize titleiconSizeHint = titleIcon->sizeHint();
			titleiconSizeHint.scale(titleBarControlHeight, titleBarControlHeight, Qt::KeepAspectRatio);
			titleIcon->setGeometry(x, y + 1, titleiconSizeHint.width(), titleiconSizeHint.height());
			x = titleIcon->geometry().right();
			barMinWidth += titleiconSizeHint.width();
		}

		/// 3. 布局applicationButton
		if (appBtn && appBtn->isVisibleTo(ribbon)) {
			QSize appBtnSize = appBtn->sizeHint();
			appBtnSize       = SA::scaleSizeByHeight(appBtnSize, titleBarControlHeight, 1.5);
			appBtn->setGeometry(x, y + 1, appBtnSize.width(), appBtnSize.height());
			x = appBtn->geometry().right();
			// 累加到最小宽度中
			barMinWidth += appBtnSize.width();
		}

		int endX = ribbon->width() - border.right();
		if (ribbon->isUseRibbonFrame()) {
			endX -= d_ptr->systemButtonSize.width();
		}

		/// 4. 布局右上角corner widget
		if (QWidget* connerTR = ribbon->cornerWidget(Qt::TopRightCorner)) {
			if (connerTR->isVisibleTo(ribbon)) {
				QSize connerSize = connerTR->sizeHint();
				connerSize       = SA::scaleSizeByHeight(connerSize, validTitleBarHeight);
				endX -= connerSize.width();
				connerTR->setGeometry(endX, y, connerSize.width(), connerSize.height());
				// 累加到最小宽度中
				barMinWidth += connerSize.width();
			}
		}

		/// 5. 布局右边按钮组rightButtonGroup
		if (rightBtnGroup && rightBtnGroup->isVisibleTo(ribbon)) {
			QSize rightBtnGroupSize = rightBtnGroup->sizeHint();
			// rightBtnGroupSize       = SA::scaleSizeByHeight(rightBtnGroupSize, titleBarControlHeight);
			endX -= rightBtnGroupSize.width();
			d_ptr->rightButtonGroup()->setGeometry(endX, y + 1, rightBtnGroupSize.width(), titleBarControlHeight);
			// 累加到最小宽度中
			barMinWidth += rightBtnGroupSize.width();
		}

		/// 6. 布局quick access bar
		if (qb && qb->isVisibleTo(ribbon)) {
			QSize quickAccessBarSize = qb->sizeHint();
			// quickAccessBarSize       = SA::scaleSizeByHeight(quickAccessBarSize, titleBarControlHeight);
			endX -= quickAccessBarSize.width();
			qb->setGeometry(endX, y + 1, quickAccessBarSize.width(), titleBarControlHeight);
			// 累加到最小宽度中
			barMinWidth += quickAccessBarSize.width();
		}

		/// 7. 布局 tab bar
		int tabH = d_ptr->getActualTabBarHeight();
		if (tabH > titleBarControlHeight) {
			// 这种直接把tabH设置为validTitleBarHeight
			tabH = titleBarControlHeight;
		}
		int tabBarWidth = endX - x;
		// 这里极度压缩有可能导致负数因此不能小于0
		if (tabBarWidth < 10) {
			tabBarWidth = 10;
		}
		// 紧凑模式下，tabbar要用最小宽度布局，否则会遮挡标题栏的点击，导致无法移动
		int mintabBarWidth = calcMinTabBarWidth();
		// 累加到最小宽度中
		barMinWidth += mintabBarWidth;
		if (tabBar) {
			y = border.top() + (titleBarControlHeight - tabH);
			if (ribbon->ribbonAlignment() == SARibbonAlignment::AlignLeft) {
				// 左对齐的tabbar，直接设置位置
				if (mintabBarWidth < tabBarWidth) {
					tabBarWidth = mintabBarWidth;
				}
				tabBar->setGeometry(x, y, tabBarWidth, tabH);
			} else {
				// 居中对齐的情况下，Tab要居中显示
				// 得到tab的推荐尺寸
				if (mintabBarWidth >= tabBarWidth) {
					// 这时tabbar没有居中对齐的必要性，空间位置不够了
					tabBar->setGeometry(x, y, tabBarWidth, tabH);
				} else {
					// 说明tabbar的宽度有居中的可能性
					int xoffset = (tabBarWidth - mintabBarWidth) / 2;
					tabBar->setGeometry(x + xoffset, y, mintabBarWidth, tabH);
				}
			}
		}

		d_ptr->minWidth = barMinWidth;

		// 5. 更新标题区域
		layoutTitleRect();
		// 6. 调整 stackedContainerWidget
		layoutStackedContainerWidget();
	}
}

/*** End of inlined file: SARibbonBarLayout.cpp ***/


/*** Start of inlined file: SARibbonElementFactory.cpp ***/
#include <QApplication>
#include <QFontMetrics>

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonElementFactory
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonElementFactory 构造函数
 * \endif
 */
SARibbonElementFactory::SARibbonElementFactory()
{
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonElementFactory
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonElementFactory 析构函数
 * \endif
 */
SARibbonElementFactory::~SARibbonElementFactory()
{
}

/**
 * \if ENGLISH
 * @brief Create SARibbonBar
 * @param parent Parent widget
 * @return SARibbonBar pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonBar
 * @param parent 父窗口
 * @return SARibbonBar指针
 * \endif
 */
SARibbonBar* SARibbonElementFactory::createRibbonBar(QWidget* parent)
{
	return (new SARibbonBar(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonTabBar
 * @param parent Parent widget
 * @return SARibbonTabBar pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonTabBar
 * @param parent 父窗口
 * @return SARibbonTabBar指针
 * \endif
 */
SARibbonTabBar* SARibbonElementFactory::createRibbonTabBar(QWidget* parent)
{
	return (new SARibbonTabBar(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonApplicationButton
 * @param parent Parent widget
 * @return SARibbonApplicationButton pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonApplicationButton
 * @param parent 父窗口
 * @return SARibbonApplicationButton指针
 * \endif
 */
SARibbonApplicationButton* SARibbonElementFactory::createRibbonApplicationButton(QWidget* parent)
{
	return (new SARibbonApplicationButton(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonCategory
 * @param parent Parent widget
 * @return SARibbonCategory pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonCategory
 * @param parent 父窗口
 * @return SARibbonCategory指针
 * \endif
 */
SARibbonCategory* SARibbonElementFactory::createRibbonCategory(QWidget* parent)
{
	return (new SARibbonCategory(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonContextCategory
 * @param parent Parent widget
 * @return SARibbonContextCategory pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonContextCategory
 * @param parent 父窗口
 * @return SARibbonContextCategory指针
 * \endif
 */
SARibbonContextCategory* SARibbonElementFactory::createRibbonContextCategory(QWidget* parent)
{
	return (new SARibbonContextCategory(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonPanel
 * @param parent Parent widget
 * @return SARibbonPanel pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonPanel
 * @param parent 父窗口
 * @return SARibbonPanel指针
 * \endif
 */
SARibbonPanel* SARibbonElementFactory::createRibbonPanel(QWidget* parent)
{
	return (new SARibbonPanel(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonSeparatorWidget
 * @param parent Parent widget
 * @return SARibbonSeparatorWidget pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonSeparatorWidget
 * @param parent 父窗口
 * @return SARibbonSeparatorWidget指针
 * \endif
 */
SARibbonSeparatorWidget* SARibbonElementFactory::createRibbonSeparatorWidget(QWidget* parent)
{
	return (new SARibbonSeparatorWidget(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonGallery
 * @param parent Parent widget
 * @return SARibbonGallery pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonGallery
 * @param parent 父窗口
 * @return SARibbonGallery指针
 * \endif
 */
SARibbonGallery* SARibbonElementFactory::createRibbonGallery(QWidget* parent)
{
	return (new SARibbonGallery(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonGalleryGroup
 * @param parent Parent widget
 * @return SARibbonGalleryGroup pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonGalleryGroup
 * @param parent 父窗口
 * @return SARibbonGalleryGroup指针
 * \endif
 */
SARibbonGalleryGroup* SARibbonElementFactory::createRibbonGalleryGroup(QWidget* parent)
{
	return (new SARibbonGalleryGroup(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonToolButton
 * @param parent Parent widget
 * @return SARibbonToolButton pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonToolButton
 * @param parent 父窗口
 * @return SARibbonToolButton指针
 * \endif
 */
SARibbonToolButton* SARibbonElementFactory::createRibbonToolButton(QWidget* parent)
{
	return (new SARibbonToolButton(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonStackedWidget
 * @param parent SARibbonBar parent
 * @return SARibbonStackedWidget pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonStackedWidget
 * @param parent SARibbonBar父窗口
 * @return SARibbonStackedWidget指针
 * \endif
 */
SARibbonStackedWidget* SARibbonElementFactory::createRibbonStackedWidget(SARibbonBar* parent)
{
	return (new SARibbonStackedWidget(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonButtonGroupWidget
 * @param parent Parent widget
 * @return SARibbonButtonGroupWidget pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonButtonGroupWidget
 * @param parent 父窗口
 * @return SARibbonButtonGroupWidget指针
 * \endif
 */
SARibbonButtonGroupWidget* SARibbonElementFactory::createButtonGroupWidget(QWidget* parent)
{
	return (new SARibbonButtonGroupWidget(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonQuickAccessBar
 * @param parent Parent widget
 * @return SARibbonQuickAccessBar pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonQuickAccessBar
 * @param parent 父窗口
 * @return SARibbonQuickAccessBar指针
 * \endif
 */
SARibbonQuickAccessBar* SARibbonElementFactory::createQuickAccessBar(QWidget* parent)
{
	return (new SARibbonQuickAccessBar(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonSystemButtonBar
 * @param parent Parent widget
 * @return SARibbonSystemButtonBar pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonSystemButtonBar
 * @param parent 父窗口
 * @return SARibbonSystemButtonBar指针
 * \endif
 */
SARibbonSystemButtonBar* SARibbonElementFactory::createWindowButtonGroup(QWidget* parent)
{
	return (new SARibbonSystemButtonBar(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonPanelOptionButton
 * @param panel Associated panel
 * @return SARibbonPanelOptionButton pointer
 * @sa SARibbonPanelOptionButton
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonPanelOptionButton
 * @param panel 附属的panel
 * @return SARibbonPanelOptionButton指针
 * @sa SARibbonPanelOptionButton
 * \endif
 */
SARibbonPanelOptionButton* SARibbonElementFactory::createRibbonPanelOptionButton(SARibbonPanel* panel)
{
	return (new SARibbonPanelOptionButton(panel));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonTitleIconWidget
 * @param parent Parent widget
 * @return SARibbonTitleIconWidget pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonTitleIconWidget
 * @param parent 父窗口
 * @return SARibbonTitleIconWidget指针
 * \endif
 */
SARibbonTitleIconWidget* SARibbonElementFactory::createRibbonTitleIconWidget(QWidget* parent)
{
	return (new SARibbonTitleIconWidget(parent));
}

/**
 * \if ENGLISH
 * @brief Create SARibbonPanelLabel
 * @param parent Parent widget
 * @return SARibbonPanelLabel pointer
 * \endif
 *
 * \if CHINESE
 * @brief 创建SARibbonPanelLabel
 * @param parent 父窗口
 * @return SARibbonPanelLabel指针
 * \endif
 */
SARibbonPanelLabel* SARibbonElementFactory::createRibbonPanelLabel(QWidget* parent)
{
	return (new SARibbonPanelLabel(parent));
}

/*** End of inlined file: SARibbonElementFactory.cpp ***/


/*** Start of inlined file: SARibbonElementManager.cpp ***/
/**
 * \if ENGLISH
 * @brief Constructor for SARibbonElementManager
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonElementManager 构造函数
 * \endif
 */
SARibbonElementManager::SARibbonElementManager()
{
	mFactory.reset(new SARibbonElementFactory());
}

/**
 * \if ENGLISH
 * @brief Destructor for SARibbonElementManager
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonElementManager 析构函数
 * \endif
 */
SARibbonElementManager::~SARibbonElementManager()
{
}

/**
 * \if ENGLISH
 * @brief Get the singleton instance of SARibbonElementManager
 * @return SARibbonElementManager singleton instance
 * \endif
 *
 * \if CHINESE
 * @brief 获取SARibbonElementManager的单例实例
 * @return SARibbonElementManager单例实例
 * \endif
 */
SARibbonElementManager* SARibbonElementManager::instance()
{
	static SARibbonElementManager s_instance;

	return (&(s_instance));
}

/**
 * \if ENGLISH
 * @brief Get the current SARibbonElementFactory
 * @return SARibbonElementFactory pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前的SARibbonElementFactory
 * @return SARibbonElementFactory指针
 * \endif
 */
SARibbonElementFactory* SARibbonElementManager::factory()
{
	return (mFactory.data());
}

/**
 * \if ENGLISH
 * @brief Setup the SARibbonElementFactory
 * @param fac SARibbonElementFactory pointer
 * \endif
 *
 * \if CHINESE
 * @brief 设置SARibbonElementFactory
 * @param fac SARibbonElementFactory指针
 * \endif
 */
void SARibbonElementManager::setupFactory(SARibbonElementFactory* fac)
{
	mFactory.reset(fac);
}

/*** End of inlined file: SARibbonElementManager.cpp ***/


/*** Start of inlined file: SARibbonCustomizeData.cpp ***/
#include <QDebug>
#include <QObject>
////////////////////////////////////////////////////////////////////////////////////////////////////////
/// SARibbonCustomizeData
////////////////////////////////////////////////////////////////////////////////////////////////////////

SARibbonCustomizeData::SARibbonCustomizeData()
	: indexValue(-1)
	, actionRowProportionValue(SARibbonPanelItem::Large)
	, mType(UnknowActionType)
	, mActionsManagerPointer(nullptr)
{
}

SARibbonCustomizeData::SARibbonCustomizeData(ActionType type, SARibbonActionsManager* mgr)
	: indexValue(-1), actionRowProportionValue(SARibbonPanelItem::Large), mType(type), mActionsManagerPointer(mgr)
{
}

/**
 * \if ENGLISH
 * @brief Get the action type of the CustomizeData
 * @return Action type
 * \endif
 *
 * \if CHINESE
 * @brief 获取CustomizeData的action type
 * @return 操作类型
 * \endif
 */
SARibbonCustomizeData::ActionType SARibbonCustomizeData::actionType() const
{
	return (mType);
}

/**
 * \if ENGLISH
 * @brief Set the action type of the CustomizeData
 * @param a Action type to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置CustomizeData的action type
 * @param a 要设置的操作类型
 * \endif
 */
void SARibbonCustomizeData::setActionType(SARibbonCustomizeData::ActionType a)
{
	mType = a;
}

/**
 * \if ENGLISH
 * @brief Check if this is a valid CustomizeData
 * @details Actual logic: actionType() != UnknowActionType
 * @return true if the CustomizeData is valid
 * \endif
 *
 * \if CHINESE
 * @brief 判断是否是一个正常的CustomizeData
 * @details 实际逻辑actionType() != UnknowActionType
 * @return 有用的CustomizeData返回true
 * \endif
 */
bool SARibbonCustomizeData::isValid() const
{
	return (actionType() != UnknowActionType);
}

/**
 * \if ENGLISH
 * @brief Apply SARibbonCustomizeData to SARibbonBar
 * @param m SARibbonBar to apply to
 * @return If application fails, returns false; if actionType==UnknowActionType, directly returns false
 * \endif
 *
 * \if CHINESE
 * @brief 应用SARibbonCustomizeData到SARibbonBar
 * @param m 要应用到的 SARibbonBar
 * @return 如果应用失败，返回false,如果actionType==UnknowActionType直接返回false
 * \endif
 */
bool SARibbonCustomizeData::apply(SARibbonBar* bar) const
{
	if (nullptr == bar) {
		return (false);
	}
	switch (actionType()) {
	case UnknowActionType:
		return (false);

	case AddCategoryActionType: {
		// 添加标签
		SARibbonCategory* c = bar->insertCategoryPage(keyValue, indexValue);
		if (nullptr == c) {
			return (false);
		}
		c->setObjectName(categoryObjNameValue);
		SARibbonCustomizeData::setCanCustomize(c);
		return (true);
	}

	case AddPanelActionType: {
		// 添加panel
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		SARibbonPanel* p = c->insertPanel(keyValue, indexValue);
		p->setObjectName(panelObjNameValue);
		SARibbonCustomizeData::setCanCustomize(p);
		return (true);
	}

	case AddActionActionType: {
		if (nullptr == mActionsManagerPointer) {
			return (false);
		}
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		SARibbonPanel* panel = c->panelByObjectName(panelObjNameValue);
		if (nullptr == panel) {
			return (false);
		}
		QAction* act = mActionsManagerPointer->action(keyValue);
		if (nullptr == act) {
			return (false);
		}
		SARibbonCustomizeData::setCanCustomize(act);
		panel->addAction(act, actionRowProportionValue);
		return (true);
	}

	case RemoveCategoryActionType: {
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		bar->removeCategory(c);
		return (true);
	}

	case RemovePanelActionType: {
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		SARibbonPanel* panel = c->panelByObjectName(panelObjNameValue);
		if (nullptr == panel) {
			return (false);
		}
		c->removePanel(panel);
		return (true);
	}

	case RemoveActionActionType: {
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		SARibbonPanel* panel = c->panelByObjectName(panelObjNameValue);
		if (nullptr == panel) {
			return (false);
		}
		QAction* act = mActionsManagerPointer->action(keyValue);
		if (nullptr == act) {
			return (false);
		}
		panel->removeAction(act);
		return (true);
	}

	case ChangeCategoryOrderActionType: {
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		int currentindex = bar->categoryIndex(c);
		if (-1 == currentindex) {
			return (false);
		}
		int toindex = currentindex + indexValue;
		bar->moveCategory(currentindex, toindex);
		return (true);
	}

	case ChangePanelOrderActionType: {
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		SARibbonPanel* panel = c->panelByObjectName(panelObjNameValue);
		if (nullptr == panel) {
			return (false);
		}
		int panelIndex = c->panelIndex(panel);
		if (-1 == panelIndex) {
			return (false);
		}
		c->movePanel(panelIndex, panelIndex + indexValue);
		return (true);
	}

	case ChangeActionOrderActionType: {
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		SARibbonPanel* panel = c->panelByObjectName(panelObjNameValue);
		if (nullptr == panel) {
			return (false);
		}
		QAction* act = mActionsManagerPointer->action(keyValue);
		if (nullptr == act) {
			return (false);
		}
		int actindex = panel->actionIndex(act);
		if (actindex <= -1) {
			return (false);
		}
		panel->moveAction(actindex, actindex + indexValue);
		return (true);
	}

	case RenameCategoryActionType: {
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		c->setCategoryName(keyValue);
		return (true);
	}

	case RenamePanelActionType: {
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		SARibbonPanel* panel = c->panelByObjectName(panelObjNameValue);
		if (nullptr == panel) {
			return (false);
		}
		panel->setPanelName(keyValue);
		return (true);
	}

	case VisibleCategoryActionType: {
		SARibbonCategory* c = bar->categoryByObjectName(categoryObjNameValue);
		if (nullptr == c) {
			return (false);
		}
		if (1 == indexValue) {
			bar->showCategory(c);
		} else {
			bar->hideCategory(c);
		}
		return (true);
	}

	default:
		break;
	}
	return (false);
}

/**
 * \if ENGLISH
 * @brief Get the action manager pointer
 * @return SARibbonActionsManager pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取actionmanager指针
 * @return SARibbonActionsManager 指针
 * \endif
 */
SARibbonActionsManager* SARibbonCustomizeData::actionManager()
{
	return (mActionsManagerPointer);
}

/**
 * \if ENGLISH
 * @brief Set the ActionsManager
 * @param mgr SARibbonActionsManager pointer to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置ActionsManager
 * @param mgr 要设置的 SARibbonActionsManager 指针
 * \endif
 */
void SARibbonCustomizeData::setActionsManager(SARibbonActionsManager* mgr)
{
	mActionsManagerPointer = mgr;
}

/**
 * \if ENGLISH
 * @brief Create an AddCategoryActionType SARibbonCustomizeData
 * @param title Category title
 * @param index Position to insert the category
 * @param objName Object name of the category
 * @return SARibbonCustomizeData with AddCategoryActionType
 * \endif
 *
 * \if CHINESE
 * @brief 创建一个AddCategoryActionType的SARibbonCustomizeData
 * @param title category 的标题
 * @param index category要插入的位置
 * @param objName category的object name
 * @return 返回AddCategoryActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeAddCategoryCustomizeData(const QString& title, int index, const QString& objName)
{
	SARibbonCustomizeData d(AddCategoryActionType);

	d.indexValue           = index;
	d.keyValue             = title;
	d.categoryObjNameValue = objName;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Create an AddPanelActionType SARibbonCustomizeData
 * @param title Panel title
 * @param index Panel index
 * @param categoryobjName Object name of the panel's category
 * @param objName Object name of the panel
 * @return SARibbonCustomizeData with AddPanelActionType
 * \endif
 *
 * \if CHINESE
 * @brief 创建一个AddPanelActionType的SARibbonCustomizeData
 * @param title panel的标题
 * @param index panel的index
 * @param categoryobjName panel的category的objectname
 * @param objName panel的objname
 * @return 返回AddPanelActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeAddPanelCustomizeData(const QString& title,
																	   int index,
																	   const QString& categoryobjName,
																	   const QString& objName)
{
	SARibbonCustomizeData d(AddPanelActionType);

	d.indexValue           = index;
	d.keyValue             = title;
	d.panelObjNameValue    = objName;
	d.categoryObjNameValue = categoryobjName;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Add action
 * @param key Action index key
 * @param mgr Action manager
 * @param rp Define the action's row proportion
 * @param categoryObjName Object name of the category to add action to
 * @param panelObjName Object name of the panel under the category to add action to
 * @return SARibbonCustomizeData with AddActionActionType
 * \endif
 *
 * \if CHINESE
 * @brief 添加action
 * @param key action的索引
 * @param mgr action管理器
 * @param rp 定义action的占位情况
 * @param categoryObjName action添加到的category的objname
 * @param panelObjName action添加到的category下的panel的objname
 * @return 返回AddActionActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeAddActionCustomizeData(const QString& key,
																		SARibbonActionsManager* mgr,
																		SARibbonPanelItem::RowProportion rp,
																		const QString& categoryObjName,
																		const QString& panelObjName)
{
	SARibbonCustomizeData d(AddActionActionType, mgr);

	d.keyValue                 = key;
	d.categoryObjNameValue     = categoryObjName;
	d.panelObjNameValue        = panelObjName;
	d.actionRowProportionValue = rp;

	return (d);
}

/**
 * \if ENGLISH
 * @brief Create a RenameCategoryActionType SARibbonCustomizeData
 * @param newname New name for the category
 * @param categoryobjName Object name of the category
 * @return SARibbonCustomizeData with RenameCategoryActionType
 * \endif
 *
 * \if CHINESE
 * @brief 创建一个RenameCategoryActionType的SARibbonCustomizeData
 * @param newname 新名字
 * @param categoryobjName category的object name
 * @return 返回RenameCategoryActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeRenameCategoryCustomizeData(const QString& newname,
																			 const QString& categoryobjName)
{
	SARibbonCustomizeData d(RenameCategoryActionType);

	if (categoryobjName.isEmpty()) {
		qDebug() << QObject::tr("SARibbon Warning !!! customize rename category,"
								"but get an empty category object name,"
								"if you want to customize SARibbon,"
								"please make sure every element has been set object name.");
	}
	d.keyValue             = newname;
	d.categoryObjNameValue = categoryobjName;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Create a RenamePanelActionType SARibbonCustomizeData
 * @param newname New name for the panel
 * @param categoryobjName Object name of the category the panel belongs to
 * @param panelObjName Object name of the panel
 * @return SARibbonCustomizeData with RenamePanelActionType
 * \endif
 *
 * \if CHINESE
 * @brief 创建一个RenamePanelActionType的SARibbonCustomizeData
 * @param newname panel的名字
 * @param categoryobjName panel对应的category的object name
 * @param panelObjName panel的object name
 * @return 返回RenamePanelActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeRenamePanelCustomizeData(const QString& newname,
																		  const QString& categoryobjName,
																		  const QString& panelObjName)
{
	SARibbonCustomizeData d(RenamePanelActionType);

	if (panelObjName.isEmpty() || categoryobjName.isEmpty()) {
		qDebug() << QObject::tr("SARibbon Warning !!! customize rename panel,"
								"but get an empty category/panel object name,"
								"if you want to customize SARibbon,"
								"please make sure every element has been set object name.");
	}
	d.keyValue             = newname;
	d.panelObjNameValue    = panelObjName;
	d.categoryObjNameValue = categoryobjName;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Create a ChangeCategoryOrderActionType SARibbonCustomizeData
 * @param categoryobjName Object name of the category to move
 * @param moveindex Move position, -1 means move up (left) one position, 1 means move down (right) one position
 * @return SARibbonCustomizeData with ChangeCategoryOrderActionType
 * \endif
 *
 * \if CHINESE
 * @brief 对应ChangeCategoryOrderActionType
 * @param categoryobjName 需要移动的categoryobjName
 * @param moveindex 移动位置，-1代表向上（向左）移动一个位置，1带表向下（向右）移动一个位置
 * @return 返回ChangeCategoryOrderActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeChangeCategoryOrderCustomizeData(const QString& categoryobjName,
																				  int moveindex)
{
	SARibbonCustomizeData d(ChangeCategoryOrderActionType);

	if (categoryobjName.isEmpty()) {
		qDebug() << QObject::tr("SARibbon Warning !!! customize change category order,"
								"but get an empty category object name,"
								"if you want to customize SARibbon,"
								"please make sure every element has been set object name.");
	}
	d.categoryObjNameValue = categoryobjName;
	d.indexValue           = moveindex;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Create a ChangePanelOrderActionType SARibbonCustomizeData
 * @param categoryobjName Object name of the category the panel belongs to
 * @param panelObjName Object name of the panel to move
 * @param moveindex Move position, -1 means move up (left) one position, 1 means move down (right) one position
 * @return SARibbonCustomizeData with ChangePanelOrderActionType
 * \endif
 *
 * \if CHINESE
 * @brief 对应ChangePanelOrderActionType
 * @param categoryobjName 需要移动的panel对应的categoryobjName
 * @param panelObjName 需要移动的panelObjName
 * @param moveindex 移动位置，-1代表向上（向左）移动一个位置，1带表向下（向右）移动一个位置
 * @return 返回ChangePanelOrderActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeChangePanelOrderCustomizeData(const QString& categoryobjName,
																			   const QString& panelObjName,
																			   int moveindex)
{
	SARibbonCustomizeData d(ChangePanelOrderActionType);

	if (categoryobjName.isEmpty() || panelObjName.isEmpty()) {
		qDebug() << QObject::tr("SARibbon Warning !!! customize change panel order,"
								"but get an empty category/panel object name,"
								"if you want to customize SARibbon,"
								"please make sure every element has been set object name.");
	}
	d.categoryObjNameValue = categoryobjName;
	d.panelObjNameValue    = panelObjName;
	d.indexValue           = moveindex;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Create a ChangeActionOrderActionType SARibbonCustomizeData
 * @param categoryobjName Object name of the category the panel belongs to
 * @param panelObjName Object name of the panel the action belongs to
 * @param key Key name managed by SARibbonActionsManager
 * @param mgr SARibbonActionsManager pointer
 * @param moveindex Move position, -1 means move up (left) one position, 1 means move down (right) one position
 * @return SARibbonCustomizeData with ChangeActionOrderActionType
 * \endif
 *
 * \if CHINESE
 * @brief 对应ChangeActionOrderActionType
 * @param categoryobjName 需要移动的panel对应的categoryobjName
 * @param panelObjName 需要移动的panelObjName
 * @param key SARibbonActionsManager管理的key名
 * @param mgr SARibbonActionsManager指针
 * @param moveindex 移动位置，-1代表向上（向左）移动一个位置，1带表向下（向右）移动一个位置
 * @return 返回ChangeActionOrderActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeChangeActionOrderCustomizeData(const QString& categoryobjName,
																				const QString& panelObjName,
																				const QString& key,
																				SARibbonActionsManager* mgr,
																				int moveindex)
{
	SARibbonCustomizeData d(ChangeActionOrderActionType, mgr);

	if (categoryobjName.isEmpty() || panelObjName.isEmpty() || key.isEmpty()) {
		qDebug() << QObject::tr("SARibbon Warning !!! customize change action order,"
								"but get an empty category/panel/action object name,"
								"if you want to customize SARibbon,"
								"please make sure every element has been set object name.");
	}
	d.categoryObjNameValue = categoryobjName;
	d.panelObjNameValue    = panelObjName;
	d.keyValue             = key;
	d.indexValue           = moveindex;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Create a RemoveCategoryActionType SARibbonCustomizeData
 * @param categoryobjName Object name of the category to remove
 * @return SARibbonCustomizeData with RemoveCategoryActionType
 * \endif
 *
 * \if CHINESE
 * @brief 对应RemoveCategoryActionType
 * @param categoryobjName 需要移除的objname
 * @return 返回RemoveCategoryActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeRemoveCategoryCustomizeData(const QString& categoryobjName)
{
	SARibbonCustomizeData d(RemoveCategoryActionType);

	if (categoryobjName.isEmpty()) {
		qDebug() << QObject::tr("SARibbon Warning !!! customize remove category,"
								"but get an empty category object name,"
								"if you want to customize SARibbon,"
								"please make sure every element has been set object name.");
	}
	d.categoryObjNameValue = categoryobjName;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Create a RemovePanelActionType SARibbonCustomizeData
 * @param categoryobjName Object name of the category the panel belongs to
 * @param panelObjName Object name of the panel to remove
 * @return SARibbonCustomizeData with RemovePanelActionType
 * \endif
 *
 * \if CHINESE
 * @brief 对应RemovePanelActionType
 * @param categoryobjName panel对应的category的obj name
 * @param panelObjName panel对应的 obj name
 * @return 返回RemovePanelActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeRemovePanelCustomizeData(const QString& categoryobjName,
																		  const QString& panelObjName)
{
	SARibbonCustomizeData d(RemovePanelActionType);

	if (categoryobjName.isEmpty() || panelObjName.isEmpty()) {
		qDebug() << QObject::tr("SARibbon Warning !!! customize remove panel,"
								"but get an empty category/panel object name,"
								"if you want to customize SARibbon,"
								"please make sure every element has been set object name.");
	}
	d.categoryObjNameValue = categoryobjName;
	d.panelObjNameValue    = panelObjName;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Create a RemoveActionActionType SARibbonCustomizeData
 * @param categoryobjName Object name of the category the panel belongs to
 * @param panelObjName Object name of the panel the action belongs to
 * @param key Key name managed by SARibbonActionsManager
 * @param mgr SARibbonActionsManager pointer
 * @return SARibbonCustomizeData with RemoveActionActionType
 * \endif
 *
 * \if CHINESE
 * @brief 对应RemoveActionActionType
 * @param categoryobjName panel对应的category的obj name
 * @param panelObjName panel对应的 obj name
 * @param key SARibbonActionsManager管理的key名
 * @param mgr SARibbonActionsManager指针
 * @return 返回RemoveActionActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeRemoveActionCustomizeData(const QString& categoryobjName,
																		   const QString& panelObjName,
																		   const QString& key,
																		   SARibbonActionsManager* mgr)
{
	SARibbonCustomizeData d(RemoveActionActionType, mgr);

	if (categoryobjName.isEmpty() || panelObjName.isEmpty() || key.isEmpty()) {
		qDebug() << QObject::tr("SARibbon Warning !!! customize remove action,"
								"but get an empty category/panel/action object name,"
								"if you want to customize SARibbon,"
								"please make sure every element has been set object name.");
	}
	d.categoryObjNameValue = categoryobjName;
	d.panelObjNameValue    = panelObjName;
	d.keyValue             = key;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Create a VisibleCategoryActionType SARibbonCustomizeData
 * @param categoryobjName Object name of the category
 * @param isShow Whether to show the category
 * @return SARibbonCustomizeData with VisibleCategoryActionType
 * \endif
 *
 * \if CHINESE
 * @brief 创建一个VisibleCategoryActionType的SARibbonCustomizeData
 * @param categoryobjName category的object name
 * @param isShow 是否显示
 * @return 返回VisibleCategoryActionType的SARibbonCustomizeData
 * \endif
 */
SARibbonCustomizeData SARibbonCustomizeData::makeVisibleCategoryCustomizeData(const QString& categoryobjName, bool isShow)
{
	SARibbonCustomizeData d(VisibleCategoryActionType);

	if (categoryobjName.isEmpty()) {
		qDebug() << QObject::tr("SARibbon Warning !!! customize visible category,"
								"but get an empty category object name,"
								"if you want to customize SARibbon,"
								"please make sure every element has been set object name.");
	}
	d.categoryObjNameValue = categoryobjName;
	d.indexValue           = isShow ? 1 : 0;
	return (d);
}

/**
 * \if ENGLISH
 * @brief Check the external property whether customization is allowed
 * @param obj Object to check
 * @return true if customization is allowed
 * \endif
 *
 * \if CHINESE
 * @brief 判断外置属性，是否允许自定义
 * @param obj 要检查的对象
 * @return 如果允许自定义返回true
 * \endif
 */
bool SARibbonCustomizeData::isCanCustomize(QObject* obj)
{
	QVariant v = obj->property(SA_RIBBON_BAR_PROP_CAN_CUSTOMIZE);

	if (v.isValid()) {
		return (v.toBool());
	}
	return (false);
}

/**
 * \if ENGLISH
 * @brief Set the external property to allow customization
 * @param obj Object to set
 * @param canbe Whether to allow customization
 * \endif
 *
 * \if CHINESE
 * @brief 设置外置属性允许自定义
 * @param obj 要设置的对象
 * @param canbe 是否允许自定义
 * \endif
 */
void SARibbonCustomizeData::setCanCustomize(QObject* obj, bool canbe)
{
	obj->setProperty(SA_RIBBON_BAR_PROP_CAN_CUSTOMIZE, canbe);
}

QList< SARibbonCustomizeData > remove_indexs(const QList< SARibbonCustomizeData >& csd, const QList< int >& willremoveIndex);

QList< SARibbonCustomizeData > remove_indexs(const QList< SARibbonCustomizeData >& csd, const QList< int >& willremoveIndex)
{
	QList< SARibbonCustomizeData > res;

	for (int i = 0; i < csd.size(); ++i) {
		if (!willremoveIndex.contains(i)) {
			res << csd[ i ];
		}
	}
	return (res);
}

/**
 * @brief 对QList<SARibbonCustomizeData>进行简化操作
 *
 * 此函数会执行如下操作：
 * 1、针对同一个category/panel连续出现的添加和删除操作进行移除（前一步添加，后一步删除）
 *
 * 2、针对VisibleCategoryActionType，对于连续出现的操作只保留最后一步
 *
 * 3、针对RenameCategoryActionType和RenamePanelActionType操作，只保留最后一个
 *
 * 4、针对连续的ChangeCategoryOrderActionType，ChangePanelOrderActionType，ChangeActionOrderActionType进行合并为一个动作，
 * 如果合并后原地不动，则删除
 *
 * @param csd
 * @return 返回简化的QList<SARibbonCustomizeData>
 */
QList< SARibbonCustomizeData > SARibbonCustomizeData::simplify(const QList< SARibbonCustomizeData >& csd)
{
	int size = csd.size();

	if (size <= 1) {
		return (csd);
	}
	QList< SARibbonCustomizeData > res;
	QList< int > willremoveIndex;  // 记录要删除的index

	//! 首先针对连续出现的添加和删除操作进行优化
	for (int i = 1; i < size; ++i) {
		if ((csd[ i - 1 ].actionType() == AddCategoryActionType) && (csd[ i ].actionType() == RemoveCategoryActionType)) {
			if (csd[ i - 1 ].categoryObjNameValue == csd[ i ].categoryObjNameValue) {
				willremoveIndex << i - 1 << i;
			}
		} else if ((csd[ i - 1 ].actionType() == AddPanelActionType) && (csd[ i ].actionType() == RemovePanelActionType)) {
			if ((csd[ i - 1 ].panelObjNameValue == csd[ i ].panelObjNameValue)
				&& (csd[ i - 1 ].categoryObjNameValue == csd[ i ].categoryObjNameValue)) {
				willremoveIndex << i - 1 << i;
			}
		} else if ((csd[ i - 1 ].actionType() == AddActionActionType) && (csd[ i ].actionType() == RemoveActionActionType)) {
			if ((csd[ i - 1 ].keyValue == csd[ i ].keyValue) && (csd[ i - 1 ].panelObjNameValue == csd[ i ].panelObjNameValue)
				&& (csd[ i - 1 ].categoryObjNameValue == csd[ i ].categoryObjNameValue)) {
				willremoveIndex << i - 1 << i;
			}
		}
	}
	res = remove_indexs(csd, willremoveIndex);
	willremoveIndex.clear();

	//! 筛选VisibleCategoryActionType，对于连续出现的操作只保留最后一步
	size = res.size();
	for (int i = 1; i < size; ++i) {
		if ((res[ i - 1 ].actionType() == VisibleCategoryActionType)
			&& (res[ i ].actionType() == VisibleCategoryActionType)) {
			if (res[ i - 1 ].categoryObjNameValue == res[ i ].categoryObjNameValue) {
				// 要保证操作的是同一个内容
				willremoveIndex << i - 1;  // 删除前一个只保留最后一个
			}
		}
	}
	res = remove_indexs(res, willremoveIndex);
	willremoveIndex.clear();

	//! 针对RenameCategoryActionType和RenamePanelActionType操作，只需保留最后一个
	size = res.size();
	for (int i = 0; i < size; ++i) {
		if (res[ i ].actionType() == RenameCategoryActionType) {
			// 向后查询，如果查询到有同一个Category改名，把这个索引加入删除队列
			for (int j = i + 1; j < size; ++j) {
				if ((res[ j ].actionType() == RenameCategoryActionType)
					&& (res[ i ].categoryObjNameValue == res[ j ].categoryObjNameValue)) {
					willremoveIndex << i;
				}
			}
		} else if (res[ i ].actionType() == RenamePanelActionType) {
			// 向后查询，如果查询到有同一个panel改名，把这个索引加入删除队列
			for (int j = i + 1; j < size; ++j) {
				if ((res[ j ].actionType() == RenamePanelActionType)
					&& (res[ i ].panelObjNameValue == res[ j ].panelObjNameValue)
					&& (res[ i ].categoryObjNameValue == res[ j ].categoryObjNameValue)) {
					willremoveIndex << i;
				}
			}
		}
	}
	res = remove_indexs(res, willremoveIndex);
	willremoveIndex.clear();

	//! 针对连续的ChangeCategoryOrderActionType，ChangePanelOrderActionType，ChangeActionOrderActionType进行合并
	size = res.size();
	for (int i = 1; i < size; ++i) {
		if ((res[ i - 1 ].actionType() == ChangeCategoryOrderActionType)
			&& (res[ i ].actionType() == ChangeCategoryOrderActionType)
			&& (res[ i - 1 ].categoryObjNameValue == res[ i ].categoryObjNameValue)) {
			// 说明连续两个顺序调整，把前一个indexvalue和后一个indexvalue相加，前一个删除
			res[ i ].indexValue += res[ i - 1 ].indexValue;
			willremoveIndex << i - 1;
		} else if ((res[ i - 1 ].actionType() == ChangePanelOrderActionType)
				   && (res[ i ].actionType() == ChangePanelOrderActionType)
				   && (res[ i - 1 ].panelObjNameValue == res[ i ].panelObjNameValue)
				   && (res[ i - 1 ].categoryObjNameValue == res[ i ].categoryObjNameValue)) {
			// 说明连续两个顺序调整，把前一个indexvalue和后一个indexvalue相加，前一个删除
			res[ i ].indexValue += res[ i - 1 ].indexValue;
			willremoveIndex << i - 1;
		} else if ((res[ i - 1 ].actionType() == ChangeActionOrderActionType)
				   && (res[ i ].actionType() == ChangeActionOrderActionType) && (res[ i - 1 ].keyValue == res[ i ].keyValue)
				   && (res[ i - 1 ].panelObjNameValue == res[ i ].panelObjNameValue)
				   && (res[ i - 1 ].categoryObjNameValue == res[ i ].categoryObjNameValue)) {
			// 说明连续两个顺序调整，把前一个indexvalue和后一个indexvalue相加，前一个删除
			res[ i ].indexValue += res[ i - 1 ].indexValue;
			willremoveIndex << i - 1;
		}
	}
	res = remove_indexs(res, willremoveIndex);
	willremoveIndex.clear();

	//! 上一步操作可能会产生indexvalue为0的情况，此操作把indexvalue为0的删除
	size = res.size();
	for (int i = 0; i < size; ++i) {
		if ((res[ i ].actionType() == ChangeCategoryOrderActionType) || (res[ i ].actionType() == ChangePanelOrderActionType)
			|| (res[ i ].actionType() == ChangeActionOrderActionType)) {
			if (0 == res[ i ].indexValue) {
				willremoveIndex << i;
			}
		}
	}
	res = remove_indexs(res, willremoveIndex);
	willremoveIndex.clear();
	return (res);
}

/*** End of inlined file: SARibbonCustomizeData.cpp ***/


/*** Start of inlined file: SARibbonCustomizeWidget.cpp ***/
#include <QDebug>
#include <QtCore/QVariant>
#include <QAction>
#include <QtWidgets/QApplication>
#include <QtWidgets/QButtonGroup>
#include <QtWidgets/QComboBox>
#include <QtWidgets/QHBoxLayout>
#include <QtWidgets/QHeaderView>
#include <QtWidgets/QLabel>
#include <QtWidgets/QLineEdit>
#include <QtWidgets/QPushButton>
#include <QtWidgets/QRadioButton>
#include <QtWidgets/QSpacerItem>
#include <QtWidgets/QTreeView>
#include <QtWidgets/QListView>
#include <QtWidgets/QVBoxLayout>
#include <QtWidgets/QWidget>

#include <QStandardItemModel>
#include <QButtonGroup>
#include <QInputDialog>
#include <QLineEdit>
#include <QDateTime>
#include <QXmlStreamWriter>
#include <QXmlStreamReader>

#include <QFile>
#include <QMessageBox>

////////////////////////////////////////////////////////////////////////////////////////////////////////
/// SARibbonCustomizeWidget
////////////////////////////////////////////////////////////////////////////////////////////////////////

bool sa_customize_datas_to_xml(QXmlStreamWriter* xml, const QList< SARibbonCustomizeData >& cds)
{
	if (cds.size() <= 0) {
		return (false);
	}

	xml->writeStartElement("sa-ribbon-customize");
	for (const SARibbonCustomizeData& d : cds) {
		xml->writeStartElement("customize-data");
		xml->writeAttribute("type", QString::number(d.actionType()));
		xml->writeAttribute("index", QString::number(d.indexValue));
		xml->writeAttribute("key", d.keyValue);
		xml->writeAttribute("category", d.categoryObjNameValue);
		xml->writeAttribute("panel", d.panelObjNameValue);
		xml->writeAttribute("row-prop", QString::number(d.actionRowProportionValue));

		xml->writeEndElement();
	}
	xml->writeEndElement();
	if (xml->hasError()) {
		qWarning() << "write has error";
	}
	return (true);
}

QList< SARibbonCustomizeData > sa_customize_datas_from_xml(QXmlStreamReader* xml, SARibbonActionsManager* mgr)
{
	// 先找到"sa-ribbon-customize"
	while (!xml->atEnd()) {

		if (xml->isStartElement() && (xml->name().toString() == "sa-ribbon-customize")) {
			break;
		}
		xml->readNext();
	}
	QList< SARibbonCustomizeData > res;

	// 开始遍历"customize-data"
	while (!xml->atEnd()) {
		if (xml->isStartElement() && (xml->name().toString() == "customize-data")) {
			// 首先读取属性type
			SARibbonCustomizeData d;
			QXmlStreamAttributes attrs = xml->attributes();
			if (!attrs.hasAttribute("type")) {
				// 说明异常，跳过这个
				xml->readNextStartElement();
				continue;
			}
			bool isOk = false;
			int v     = xml->attributes().value("type").toInt(&isOk);
			if (!isOk) {
				// 说明异常，跳过这个
				xml->readNextStartElement();
				continue;
			}
			d.setActionType(static_cast< SARibbonCustomizeData::ActionType >(v));
			// 开始读取子对象
			if (attrs.hasAttribute("index")) {
				v = xml->attributes().value("index").toInt(&isOk);
				if (isOk) {
					d.indexValue = v;
				}
			}
			if (attrs.hasAttribute("key")) {
				d.keyValue = attrs.value("key").toString();
			}
			if (attrs.hasAttribute("category")) {
				d.categoryObjNameValue = attrs.value("category").toString();
			}
			if (attrs.hasAttribute("panel")) {
				d.panelObjNameValue = attrs.value("panel").toString();
			}
			if (attrs.hasAttribute("row-prop")) {
				v = xml->attributes().value("row-prop").toInt(&isOk);
				if (isOk) {
					d.actionRowProportionValue = static_cast< SARibbonPanelItem::RowProportion >(v);
				}
			}
			d.setActionsManager(mgr);
			res.append(d);
		}
		xml->readNext();
	}
	if (xml->hasError()) {
		qWarning() << xml->errorString();
	}
	return (res);
}

int sa_customize_datas_apply(const QList< SARibbonCustomizeData >& cds, SARibbonBar* bar)
{
	int c = 0;

	for (const SARibbonCustomizeData& d : cds) {
		if (d.apply(bar)) {
			++c;
		}
	}
	return (c);
}

int sa_customize_datas_reverse(const QList< SARibbonCustomizeData >& cds, SARibbonBar* bar)
{
	int c = 0;
	// todo 支持反向操作
	return (c);
}

bool sa_apply_customize_from_xml_file(const QString& filePath, SARibbonBar* bar, SARibbonActionsManager* mgr)
{
	QFile f(filePath);

	if (!f.open(QIODevice::ReadOnly | QIODevice::Text)) {
		return (false);
	}
	f.seek(0);
	QXmlStreamReader xml(&f);

	return (SARibbonCustomizeWidget::fromXml(&xml, bar, mgr));
}

/**
 * \if ENGLISH
 * @brief Build the UI for SARibbonCustomizeWidget
 * \endif
 *
 * \if CHINESE
 * @brief 构建SARibbonCustomizeWidget的Ui
 * \endif
 */
class SARibbonCustomizeWidgetUi
{
public:
	QHBoxLayout* horizontalLayoutMain;
	QVBoxLayout* verticalLayoutSelect;
	QLabel* labelSelectAction;
	QHBoxLayout* horizontalLayoutSearch;
	QComboBox* comboBoxActionIndex;
	QLineEdit* lineEditSearchAction;
	QListView* listViewSelect;
	QVBoxLayout* verticalLayoutMidButtons;
	QSpacerItem* verticalSpacerUp;
	QPushButton* pushButtonAdd;
	QPushButton* pushButtonDelete;
	QPushButton* pushButtonReset;
	QSpacerItem* verticalSpacerDown;
	QLabel* labelProportion;
	QComboBox* comboBoxActionProportion;
	QVBoxLayout* verticalLayoutResult;
	QLabel* labelCustomize;
	QHBoxLayout* horizontalLayoutCategorySelect;
	QRadioButton* radioButtonMainCategory;
	QRadioButton* radioButtonAllCategory;
	QButtonGroup* radioButtonGroup;
	QTreeView* treeViewResult;
	QHBoxLayout* horizontalLayoutActionOptBtns;
	QPushButton* pushButtonNewCategory;
	QPushButton* pushButtonNewPanel;
	QPushButton* pushButtonRename;
	QVBoxLayout* verticalLayoutRightButtons;
	QSpacerItem* verticalSpacerUp2;
	QToolButton* toolButtonUp;
	QToolButton* toolButtonDown;
	QSpacerItem* verticalSpacerDown2;

	void setupUi(QWidget* customizeWidget)
	{
		if (customizeWidget->objectName().isEmpty()) {
			customizeWidget->setObjectName(QStringLiteral("SARibbonCustomizeWidget"));
		}
		customizeWidget->resize(800, 600);
		horizontalLayoutMain = new QHBoxLayout(customizeWidget);
		horizontalLayoutMain->setObjectName(QStringLiteral("horizontalLayoutMain"));
		verticalLayoutSelect = new QVBoxLayout();
		verticalLayoutSelect->setObjectName(QStringLiteral("verticalLayoutSelect"));
		labelSelectAction = new QLabel(customizeWidget);
		labelSelectAction->setObjectName(QStringLiteral("labelSelectAction"));

		verticalLayoutSelect->addWidget(labelSelectAction);

		horizontalLayoutSearch = new QHBoxLayout();
		horizontalLayoutSearch->setObjectName(QStringLiteral("horizontalLayoutSearch"));
		comboBoxActionIndex = new QComboBox(customizeWidget);
		comboBoxActionIndex->setObjectName(QStringLiteral("comboBoxActionIndex"));
		comboBoxActionIndex->setEditable(false);

		horizontalLayoutSearch->addWidget(comboBoxActionIndex);

		lineEditSearchAction = new QLineEdit(customizeWidget);
		lineEditSearchAction->setObjectName(QStringLiteral("lineEditSearchAction"));

		horizontalLayoutSearch->addWidget(lineEditSearchAction);

		verticalLayoutSelect->addLayout(horizontalLayoutSearch);

		listViewSelect = new QListView(customizeWidget);
		listViewSelect->setObjectName(QStringLiteral("listViewSelect"));

		verticalLayoutSelect->addWidget(listViewSelect);

		horizontalLayoutMain->addLayout(verticalLayoutSelect);

		verticalLayoutMidButtons = new QVBoxLayout();
		verticalLayoutMidButtons->setObjectName(QStringLiteral("verticalLayoutMidButtons"));
		verticalSpacerUp = new QSpacerItem(20, 40, QSizePolicy::Minimum, QSizePolicy::Expanding);

		verticalLayoutMidButtons->addItem(verticalSpacerUp);

		pushButtonAdd = new QPushButton(customizeWidget);
		pushButtonAdd->setObjectName(QStringLiteral("pushButtonAdd"));
		pushButtonAdd->setEnabled(false);

		verticalLayoutMidButtons->addWidget(pushButtonAdd);

		pushButtonDelete = new QPushButton(customizeWidget);
		pushButtonDelete->setObjectName(QStringLiteral("pushButtonDelete"));
		pushButtonDelete->setEnabled(false);

		verticalLayoutMidButtons->addWidget(pushButtonDelete);

		verticalSpacerDown = new QSpacerItem(20, 40, QSizePolicy::Minimum, QSizePolicy::Expanding);

		verticalLayoutMidButtons->addItem(verticalSpacerDown);

		labelProportion = new QLabel(customizeWidget);
		labelProportion->setObjectName(QStringLiteral("labelProportion"));
		verticalLayoutMidButtons->addWidget(labelProportion);

		comboBoxActionProportion = new QComboBox(customizeWidget);
		comboBoxActionProportion->setObjectName(QStringLiteral("comboBoxActionProportion"));
		comboBoxActionProportion->setEditable(false);
		verticalLayoutMidButtons->addWidget(comboBoxActionProportion);
		horizontalLayoutMain->addLayout(verticalLayoutMidButtons);

		verticalLayoutResult = new QVBoxLayout();
		verticalLayoutResult->setObjectName(QStringLiteral("verticalLayoutResult"));
		labelCustomize = new QLabel(customizeWidget);
		labelCustomize->setObjectName(QStringLiteral("labelCustomize"));

		verticalLayoutResult->addWidget(labelCustomize);

		horizontalLayoutCategorySelect = new QHBoxLayout();
		horizontalLayoutCategorySelect->setObjectName(QStringLiteral("horizontalLayoutCategorySelect"));
		radioButtonMainCategory = new QRadioButton(customizeWidget);
		radioButtonMainCategory->setObjectName(QStringLiteral("radioButtonMainCategory"));
		radioButtonMainCategory->setChecked(false);

		horizontalLayoutCategorySelect->addWidget(radioButtonMainCategory);

		radioButtonAllCategory = new QRadioButton(customizeWidget);
		radioButtonAllCategory->setObjectName(QStringLiteral("radioButtonAllCategory"));
		radioButtonAllCategory->setChecked(true);

		horizontalLayoutCategorySelect->addWidget(radioButtonAllCategory);

		radioButtonGroup = new QButtonGroup(customizeWidget);
		radioButtonGroup->addButton(radioButtonMainCategory);
		radioButtonGroup->addButton(radioButtonAllCategory);

		verticalLayoutResult->addLayout(horizontalLayoutCategorySelect);

		treeViewResult = new QTreeView(customizeWidget);
		treeViewResult->setObjectName(QStringLiteral("treeViewResult"));
		treeViewResult->setHeaderHidden(true);
		treeViewResult->setSelectionMode(QAbstractItemView::SingleSelection);
		treeViewResult->setAnimated(true);                                   // 支持动画
		treeViewResult->setEditTriggers(QAbstractItemView::NoEditTriggers);  // 不允许直接在item上重命名

		verticalLayoutResult->addWidget(treeViewResult);

		horizontalLayoutActionOptBtns = new QHBoxLayout();
		horizontalLayoutActionOptBtns->setObjectName(QStringLiteral("horizontalLayoutActionOptBtns"));
		pushButtonNewCategory = new QPushButton(customizeWidget);
		pushButtonNewCategory->setObjectName(QStringLiteral("pushButtonNewCategory"));

		horizontalLayoutActionOptBtns->addWidget(pushButtonNewCategory);

		pushButtonNewPanel = new QPushButton(customizeWidget);
		pushButtonNewPanel->setObjectName(QStringLiteral("pushButtonNewPanel"));

		horizontalLayoutActionOptBtns->addWidget(pushButtonNewPanel);

		pushButtonRename = new QPushButton(customizeWidget);
		pushButtonRename->setObjectName(QStringLiteral("pushButtonRename"));

		horizontalLayoutActionOptBtns->addWidget(pushButtonRename);

		pushButtonReset = new QPushButton(customizeWidget);
		pushButtonReset->setObjectName(QStringLiteral("pushButtonReset"));
		horizontalLayoutActionOptBtns->addWidget(pushButtonReset);

		verticalLayoutResult->addLayout(horizontalLayoutActionOptBtns);

		horizontalLayoutMain->addLayout(verticalLayoutResult);

		verticalLayoutRightButtons = new QVBoxLayout();
		verticalLayoutRightButtons->setObjectName(QStringLiteral("verticalLayoutRightButtons"));
		verticalSpacerUp2 = new QSpacerItem(20, 40, QSizePolicy::Minimum, QSizePolicy::Expanding);

		verticalLayoutRightButtons->addItem(verticalSpacerUp2);

		toolButtonUp = new QToolButton(customizeWidget);
		toolButtonUp->setObjectName(QStringLiteral("pushButtonUp"));
		toolButtonUp->setArrowType(Qt::UpArrow);
		toolButtonUp->setAutoRaise(true);

		verticalLayoutRightButtons->addWidget(toolButtonUp);

		toolButtonDown = new QToolButton(customizeWidget);
		toolButtonDown->setObjectName(QStringLiteral("pushButtonDown"));
		toolButtonDown->setArrowType(Qt::DownArrow);
		toolButtonDown->setAutoRaise(true);

		verticalLayoutRightButtons->addWidget(toolButtonDown);

		verticalSpacerDown2 = new QSpacerItem(20, 40, QSizePolicy::Minimum, QSizePolicy::Expanding);

		verticalLayoutRightButtons->addItem(verticalSpacerDown2);

		horizontalLayoutMain->addLayout(verticalLayoutRightButtons);

		retranslateUi(customizeWidget);
	}  // setupUi

	void retranslateUi(QWidget* customizeWidget)
	{
		customizeWidget->setWindowTitle(QApplication::translate("SARibbonCustomizeWidget", "Customize Widget", Q_NULLPTR));
		labelSelectAction->setText(QApplication::translate("SARibbonCustomizeWidget", "Please Select", Q_NULLPTR));  // cn:请选择
		lineEditSearchAction->setInputMask(QString());
		lineEditSearchAction->setText(QString());
		lineEditSearchAction->setPlaceholderText(
			QApplication::translate("SARibbonCustomizeWidget", "Find Command", Q_NULLPTR));               // cn:查找命令
		pushButtonAdd->setText(QApplication::translate("SARibbonCustomizeWidget", "Add >>", Q_NULLPTR));  // cn:添加 >>
		pushButtonDelete->setText(QApplication::translate("SARibbonCustomizeWidget", "<< Remove", Q_NULLPTR));  // cn:<< 移除
		labelCustomize->setText(
			QApplication::translate("SARibbonCustomizeWidget", "Customize the Ribbon", Q_NULLPTR));  // cn:自定义功能区
		radioButtonMainCategory->setText(QApplication::translate("SARibbonCustomizeWidget", "Main Category", Q_NULLPTR));  // cn:主选项卡
		radioButtonAllCategory->setText(QApplication::translate("SARibbonCustomizeWidget", "All Category", Q_NULLPTR));  // cn:所有选项卡
		pushButtonNewCategory->setText(QApplication::translate("SARibbonCustomizeWidget", "New Category", Q_NULLPTR));  // cn:新建选项卡
		pushButtonNewPanel->setText(QApplication::translate("SARibbonCustomizeWidget", "New Group", Q_NULLPTR));  // cn:新建组
		pushButtonRename->setText(QApplication::translate("SARibbonCustomizeWidget", "Rename", Q_NULLPTR));  // cn:重命名
		pushButtonReset->setText(QApplication::translate("SARibbonCustomizeWidget", "reset", Q_NULLPTR));    // cn:重置
		labelProportion->setText(QApplication::translate("SARibbonCustomizeWidget", "proportion:", Q_NULLPTR));  // cn:比例
	}  // retranslateUi
};

/**
 * \if ENGLISH
 * @brief Manage the business logic of SARibbonCustomizeWidget
 * \endif
 *
 * \if CHINESE
 * @brief 管理SARibbonCustomizeWidget的业务逻辑
 * \endif
 */
class SARibbonCustomizeWidget::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonCustomizeWidget)
public:
	SARibbonCustomizeWidget::RibbonTreeShowType mShowType { SARibbonCustomizeWidget::ShowAllCategory };  ///< 显示类型
	SARibbonBar* mRibbonBar { nullptr };                   ///< 保存SARibbonMainWindow的指针
	SARibbonActionsManager* mActionMgr { nullptr };        ///< action管理器
	SARibbonActionsManagerModel* mAcionModel { nullptr };  ///< action管理器对应的model
	QStandardItemModel* mRibbonModel { nullptr };          ///< 用于很成ribbon的树
	int mCustomizeCategoryCount { 0 };                     ///< 记录自定义Category的个数
	int mCustomizePanelCount { 0 };                        ///< 记录自定义Panel的个数
public:
	PrivateData(SARibbonCustomizeWidget* p);
	void updateModel();

	QList< SARibbonCustomizeData > mCustomizeDatasCache;    ///< 缓存记录所有的自定义动作
	QList< SARibbonCustomizeData > mCustomizeDatasApplied;  ///< 应用后的所有的自定义动作
	QList< SARibbonCustomizeData > mOldCustomizeDatas;      ///< 记录旧的自定义动作,本地文件缓存
	// 创建一个随机id，形如：pre_QDateTime::currentMSecsSinceEpoch_suf
	static QString makeRandomObjName(const QString& pre);

	int itemLevel(QStandardItem* item) const;

	//
	bool isCustomizeItem(QStandardItem* item) const;

	// 把item转换为category
	SARibbonCategory* itemToCategory(QStandardItem* item) const;

	// 把item转换为SARibbonPanel
	SARibbonPanel* itemToPanel(QStandardItem* item) const;

	// 获取item对应的object name
	QString itemObjectName(QStandardItem* item) const;

	// 判断是否可以自定义
	bool isItemCanCustomize(QStandardItem* item) const;

	// 从item转为action
	QAction* itemToAction(QStandardItem* item) const;
};

SARibbonCustomizeWidget::PrivateData::PrivateData(SARibbonCustomizeWidget* p)
	: q_ptr(p), mAcionModel(new SARibbonActionsManagerModel(p)), mRibbonModel(new QStandardItemModel(p))
{
}

void SARibbonCustomizeWidget::PrivateData::updateModel()
{
	if (mRibbonBar == nullptr) {
		return;
	}
	mRibbonModel->clear();
	SARibbonBar* ribbonbar               = mRibbonBar;
	QList< SARibbonCategory* > categorys = ribbonbar->categoryPages();

	for (const SARibbonCategory* c : sa_as_const(categorys)) {
		if ((mShowType == SARibbonCustomizeWidget::ShowMainCategory) && c->isContextCategory()) {
			// 如果是只显示主内容，如果是上下文标签就忽略
			continue;
		}
		QStandardItem* ci = new QStandardItem();
		if (c->isContextCategory()) {
			ci->setText(QString("[%1]").arg(c->categoryName()));
		} else {
			ci->setText(c->categoryName());
		}
		if (c->isCanCustomize() && !c->isContextCategory()) {
			// 上下文标签不做显示隐藏处理
			ci->setCheckable(true);
			ci->setCheckState(ribbonbar->isCategoryVisible(c) ? Qt::Checked : Qt::Unchecked);
			ci->setData(true, SARibbonCustomizeWidget::CanCustomizeRole);  // 标记这个是可以自定义的
		}
		ci->setData(0, SARibbonCustomizeWidget::LevelRole);
		ci->setData(QVariant::fromValue< qintptr >(qintptr(c)), SARibbonCustomizeWidget::PointerRole);
		QList< SARibbonPanel* > panels = c->panelList();
		for (const SARibbonPanel* p : sa_as_const(panels)) {
			QStandardItem* pi = new QStandardItem(p->panelName());
			pi->setData(1, SARibbonCustomizeWidget::LevelRole);
			pi->setData(QVariant::fromValue< qintptr >(qintptr(p)), SARibbonCustomizeWidget::PointerRole);
			if (p->isCanCustomize()) {
				pi->setData(true, SARibbonCustomizeWidget::CanCustomizeRole);  // 标记这个是可以自定义的
			}
			ci->appendRow(pi);
			const QList< SARibbonPanelItem* >& items = p->ribbonPanelItem();
			for (SARibbonPanelItem* i : sa_as_const(items)) {
				if (i->action->isSeparator()) {
					continue;
				}
				QStandardItem* ii = new QStandardItem();
				//                if (i->customWidget) {
				//                    //如果是自定义窗口
				//                    if (i->widget()->windowTitle().isEmpty() && i->widget()->windowIcon().isNull()) {
				//                        delete ii;
				//                        continue;  //如果窗口啥也没有，就跳过
				//                    }
				//                    ii->setText(i->widget()->windowTitle());
				//                    ii->setIcon(i->widget()->windowIcon());
				//                    if (SARibbonCustomizeData::isCanCustomize(i->widget())) {
				//                        ii->setData(true, SARibbonCustomizeWidget::CanCustomizeRole); //标记这个是可以自定义的
				//                    }
				//                } else {
				//                    //不是自定义，说明是action
				//                    ii->setText(i->action->text());
				//                    ii->setIcon(i->action->icon());
				//                    if (SARibbonCustomizeData::isCanCustomize(i->action)) {
				//                        ii->setData(true, SARibbonCustomizeWidget::CanCustomizeRole); //标记这个是可以自定义的
				//                    }
				//                }
				ii->setText(i->action->text());
				ii->setIcon(i->action->icon());
				if (SARibbonCustomizeData::isCanCustomize(i->action)) {
					ii->setData(true, SARibbonCustomizeWidget::CanCustomizeRole);  // 标记这个是可以自定义的
				}
				ii->setData(2, SARibbonCustomizeWidget::LevelRole);
				ii->setData(QVariant::fromValue< qintptr >(qintptr(i)), SARibbonCustomizeWidget::PointerRole);
				pi->appendRow(ii);
			}
		}
		mRibbonModel->appendRow(ci);
	}
}

/**
 * \if ENGLISH
 * @brief Create a random ID in the format: pre_QDateTime::currentMSecsSinceEpoch
 * @param pre Prefix
 * @return Random object name
 * \endif
 *
 * \if CHINESE
 * @brief 创建一个随机id，形如：pre_QDateTime::currentMSecsSinceEpoch
 * @param pre 前缀
 * @return 随机对象名称
 * \endif
 */
QString SARibbonCustomizeWidget::PrivateData::makeRandomObjName(const QString& pre)
{
	return (QString("%1_%2").arg(pre).arg(QDateTime::currentMSecsSinceEpoch()));
}

/**
 * \if ENGLISH
 * @brief Get the level of the item
 * @param item QStandardItem to check
 * @return Level of the item
 * \endif
 *
 * \if CHINESE
 * @brief 获取item的level
 * @param item 要检查的项
 * @return 项的层级
 * \endif
 */
int SARibbonCustomizeWidget::PrivateData::itemLevel(QStandardItem* item) const
{
	return (item->data(SARibbonCustomizeWidget::LevelRole).toInt());
}

/**
 * \if ENGLISH
 * @brief Check if item is a customize item
 * @details Customize items have CustomizeRole role set
 * @param item QStandardItem to check
 * @return true if the item is a customize item
 * \endif
 *
 * \if CHINESE
 * @brief 判断itemn为自定义的item，自定义的item都带有CustomizeRole角色
 * @param item 要检查的项
 * @return 如果是自定义item返回true
 * \endif
 */
bool SARibbonCustomizeWidget::PrivateData::isCustomizeItem(QStandardItem* item) const
{
	if (nullptr == item) {
		return (false);
	}
	return (item->data(SARibbonCustomizeWidget::CustomizeRole).isValid());
}

/**
 * \if ENGLISH
 * @brief Convert item to category
 * @param item QStandardItem to convert
 * @return SARibbonCategory pointer, nullptr if conversion fails
 * \endif
 *
 * \if CHINESE
 * @brief 把item转换为category
 * @param item 要转换的项
 * @return 无法转换返回nullptr
 * \endif
 */
SARibbonCategory* SARibbonCustomizeWidget::PrivateData::itemToCategory(QStandardItem* item) const
{
	int level = item->data(SARibbonCustomizeWidget::LevelRole).toInt();

	if (level != 0) {
		return (nullptr);
	}
	qintptr p = item->data(SARibbonCustomizeWidget::PointerRole).value< qintptr >();

	return (reinterpret_cast< SARibbonCategory* >(p));
}

/**
 * \if ENGLISH
 * @brief Convert item to panel
 * @param item QStandardItem to convert
 * @return SARibbonPanel pointer, nullptr if conversion fails
 * \endif
 *
 * \if CHINESE
 * @brief 把item转换为panel
 * @param item 要转换的项
 * @return 无法转换返回nullptr
 * \endif
 */
SARibbonPanel* SARibbonCustomizeWidget::PrivateData::itemToPanel(QStandardItem* item) const
{
	int level = item->data(SARibbonCustomizeWidget::LevelRole).toInt();

	if (level != 1) {
		return (nullptr);
	}
	qintptr p = item->data(SARibbonCustomizeWidget::PointerRole).value< qintptr >();

	return (reinterpret_cast< SARibbonPanel* >(p));
}

/**
 * \if ENGLISH
 * @brief Get the object name corresponding to the item
 * @param item QStandardItem to get object name from
 * @return Object name, empty QString if unable to get
 * \endif
 *
 * \if CHINESE
 * @brief 获取item对应的objectname
 * @param item 要获取的项
 * @return 如果无法获取，返回一个空的QString
 * \endif
 */
QString SARibbonCustomizeWidget::PrivateData::itemObjectName(QStandardItem* item) const
{
	QString objName;

	if (isCustomizeItem(item)) {
		// 说明是自定义的
		objName = item->data(SARibbonCustomizeWidget::CustomizeObjNameRole).toString();
	} else {
		// 说明这个是非自定义的
		int level = itemLevel(item);
		if (0 == level) {
			SARibbonCategory* category = itemToCategory(item);
			if (category) {
				objName = category->objectName();
			}
		} else if (1 == level) {
			SARibbonPanel* panel = itemToPanel(item);
			if (panel) {
				objName = panel->objectName();
			}
		}
	}
	return (objName);
}

/**
 * \if ENGLISH
 * @brief Check if item can be customized
 * @param item QStandardItem to check
 * @return true if the item can be customized
 * \endif
 *
 * \if CHINESE
 * @brief 判断item是否可自定义
 * @param item 要检查的项
 * @return 是否可自定义
 * \endif
 */
bool SARibbonCustomizeWidget::PrivateData::isItemCanCustomize(QStandardItem* item) const
{
	if (nullptr == item) {
		return (false);
	}
	QVariant v = item->data(SARibbonCustomizeWidget::CanCustomizeRole);

	if (v.isValid()) {
		return (v.toBool());
	}
	return (false);
}

/**
 * \if ENGLISH
 * @brief Convert item to action
 * @param item QStandardItem to convert
 * @return QAction pointer, nullptr if conversion fails
 * \endif
 *
 * \if CHINESE
 * @brief 从item 转为action
 * @param item 要转换的项
 * @return QAction指针
 * \endif
 */
QAction* SARibbonCustomizeWidget::PrivateData::itemToAction(QStandardItem* item) const
{
	if (2 != itemLevel(item)) {
		return (nullptr);
	}
	// 这里要非常注意，SARibbonCustomizeWidget::CustomizeRole为true时，说明这个是自定义的内容，
	// 这时PointerRole里存放的是action指针，不是SARibbonPanelItem
	QAction* act = nullptr;

	if (item->data(SARibbonCustomizeWidget::CustomizeRole).toBool()) {
		act = reinterpret_cast< QAction* >(item->data(SARibbonCustomizeWidget::PointerRole).value< qintptr >());
	} else {
		SARibbonPanelItem* pi =
			reinterpret_cast< SARibbonPanelItem* >(item->data(SARibbonCustomizeWidget::PointerRole).value< qintptr >());
		act = (pi->action);
	}
	return (act);
}

//===================================================
// SARibbonCustomizeWidget
//===================================================
/**
 * \if ENGLISH
 * @brief Constructor for SARibbonCustomizeWidget
 * @param ribbonWindow SARibbonMainWindow pointer to manage
 * @param parent Parent widget for UI generation, can be the same as SARibbonMainWindow
 * @param f Window flags, same as QWidget::QWidget second parameter
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonCustomizeWidget构造函数
 * @param ribbonWindow 传入需要管理的SARibbonMainWindow指针
 * @param parent 用于界面生成的parent，可以和SARibbonMainWindow一样
 * @param f 同QWidget::QWidget的第二个参数
 * \endif
 */
SARibbonCustomizeWidget::SARibbonCustomizeWidget(SARibbonMainWindow* ribbonWindow, QWidget* parent, Qt::WindowFlags f)
	: QWidget(parent, f), d_ptr(new SARibbonCustomizeWidget::PrivateData(this)), ui(new SARibbonCustomizeWidgetUi)
{
	init(ribbonWindow->ribbonBar());
}

SARibbonCustomizeWidget::SARibbonCustomizeWidget(SARibbonBar* ribbonbar, QWidget* parent, Qt::WindowFlags f)
	: QWidget(parent, f), d_ptr(new SARibbonCustomizeWidget::PrivateData(this)), ui(new SARibbonCustomizeWidgetUi)
{
	init(ribbonbar);
}

void SARibbonCustomizeWidget::init(SARibbonBar* ribbonbar)
{
	d_ptr->mRibbonBar = ribbonbar;
	ui->setupUi(this);
	ui->listViewSelect->setModel(d_ptr->mAcionModel);
	ui->treeViewResult->setModel(d_ptr->mRibbonModel);
	initConnection();
	updateModel();
}

SARibbonCustomizeWidget::~SARibbonCustomizeWidget()
{
	delete ui;
}

void SARibbonCustomizeWidget::initConnection()
{
	//    这个需要qt5.8以上支持
	//    connect(ui->comboBoxActionIndex, QOverload<int>::of(&QComboBox::currentIndexChanged)
	//        , this, &SARibbonCustomizeWidget::onComboBoxActionIndexCurrentIndexChanged);
	connect(ui->comboBoxActionIndex,
			static_cast< void (QComboBox::*)(int) >(&QComboBox::currentIndexChanged),
			this,
			&SARibbonCustomizeWidget::onComboBoxActionIndexCurrentIndexChanged);
	//    这个需要qt5.8以上支持
	//    connect(ui->radioButtonGroup, QOverload<QAbstractButton *>::of(&QButtonGroup::buttonClicked)
	//        , this, &SARibbonCustomizeWidget::onRadioButtonGroupButtonClicked);
	connect(ui->radioButtonGroup,
			static_cast< void (QButtonGroup::*)(QAbstractButton*) >(&QButtonGroup::buttonClicked),
			this,
			&SARibbonCustomizeWidget::onRadioButtonGroupButtonClicked);
	connect(ui->pushButtonNewCategory, &QPushButton::clicked, this, &SARibbonCustomizeWidget::onPushButtonNewCategoryClicked);
	connect(ui->pushButtonNewPanel, &QPushButton::clicked, this, &SARibbonCustomizeWidget::onPushButtonNewPanelClicked);
	connect(ui->pushButtonRename, &QPushButton::clicked, this, &SARibbonCustomizeWidget::onPushButtonRenameClicked);
	connect(ui->pushButtonAdd, &QPushButton::clicked, this, &SARibbonCustomizeWidget::onPushButtonAddClicked);
	connect(ui->pushButtonDelete, &QPushButton::clicked, this, &SARibbonCustomizeWidget::onPushButtonDeleteClicked);
	connect(ui->listViewSelect, &QAbstractItemView::clicked, this, &SARibbonCustomizeWidget::onListViewSelectClicked);
	connect(ui->treeViewResult, &QAbstractItemView::clicked, this, &SARibbonCustomizeWidget::onTreeViewResultClicked);
	connect(ui->toolButtonUp, &QToolButton::clicked, this, &SARibbonCustomizeWidget::onToolButtonUpClicked);
	connect(ui->toolButtonDown, &QToolButton::clicked, this, &SARibbonCustomizeWidget::onToolButtonDownClicked);
	connect(d_ptr->mRibbonModel, &QStandardItemModel::itemChanged, this, &SARibbonCustomizeWidget::onItemChanged);
	connect(ui->lineEditSearchAction, &QLineEdit::textEdited, this, &SARibbonCustomizeWidget::onLineEditSearchActionTextEdited);
	connect(ui->pushButtonReset, &QPushButton::clicked, this, &SARibbonCustomizeWidget::onPushButtonResetClicked);
}

/**
 * \if ENGLISH
 * @brief Set the action manager
 * @param mgr SARibbonActionsManager pointer
 * \endif
 *
 * \if CHINESE
 * @brief 设置action管理器
 * @param mgr SARibbonActionsManager 指针
 * \endif
 */
void SARibbonCustomizeWidget::setupActionsManager(SARibbonActionsManager* mgr)
{
	if (d_ptr->mActionMgr) {
		d_ptr->mAcionModel->uninstallActionsManager();
	}
	d_ptr->mActionMgr = mgr;
	d_ptr->mAcionModel->setupActionsManager(mgr);
	// 更新左边复选框
	QList< int > tags = mgr->actionTags();

	ui->comboBoxActionIndex->clear();
	for (int tag : sa_as_const(tags)) {
		if (mgr->tagName(tag).isEmpty())
			continue;
		ui->comboBoxActionIndex->addItem(mgr->tagName(tag), tag);
	}
}

/**
 * \if ENGLISH
 * @brief Check if there is content to store, corresponding to save action
 * @return true if there is content to save
 * \endif
 *
 * \if CHINESE
 * @brief 判断用户是否有要存储的内容，对应save动作
 * @return 是否有内容可保存
 * \endif
 */
bool SARibbonCustomizeWidget::isApplied() const
{
	return (d_ptr->mCustomizeDatasApplied.size() > 0);
}

/**
 * \if ENGLISH
 * @brief Check if there are modified contents, corresponding to apply action
 * @return true if there are cached changes
 * \endif
 *
 * \if CHINESE
 * @brief 判断用户是否有改动内容，对应apply动作
 * @return 是否有缓存的改动
 * \endif
 */
bool SARibbonCustomizeWidget::isCached() const
{
	return (d_ptr->mCustomizeDatasCache.size() > 0);
}

/**
 * \if ENGLISH
 * @brief Get the model
 * @return QStandardItemModel pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取model
 * @return QStandardItemModel指针
 * \endif
 */
const QStandardItemModel* SARibbonCustomizeWidget::model() const
{
	return (d_ptr->mRibbonModel);
}

/**
 * \if ENGLISH
 * @brief Update the model based on current radiobutton selection
 * \endif
 *
 * \if CHINESE
 * @brief 根据当前的radiobutton选项来更新model
 * \endif
 */
void SARibbonCustomizeWidget::updateModel()
{
	updateModel(ui->radioButtonAllCategory->isChecked() ? ShowAllCategory : ShowMainCategory);
	if (d_ptr->mRibbonBar) {
		SARibbonBar* bar = d_ptr->mRibbonBar;
		if (bar) {
			ui->comboBoxActionProportion->clear();
			if (bar->isTwoRowStyle()) {
				ui->comboBoxActionProportion->addItem(tr("large"), SARibbonPanelItem::Large);
				ui->comboBoxActionProportion->addItem(tr("small"), SARibbonPanelItem::Small);
			} else {
				ui->comboBoxActionProportion->addItem(tr("large"), SARibbonPanelItem::Large);
				ui->comboBoxActionProportion->addItem(tr("medium"), SARibbonPanelItem::Medium);
				ui->comboBoxActionProportion->addItem(tr("small"), SARibbonPanelItem::Small);
			}
		}
	}
}

/**
 * \if ENGLISH
 * @brief Update the model with specified type
 * @param type RibbonTreeShowType to use
 * \endif
 *
 * \if CHINESE
 * @brief 更新model
 * @param type 显示类型
 * \endif
 */
void SARibbonCustomizeWidget::updateModel(RibbonTreeShowType type)
{
	d_ptr->mShowType = type;
	d_ptr->updateModel();
}

/**
 * \if ENGLISH
 * @brief Apply all settings
 * @return true if any setting applied successfully
 * @note Returns true if any setting is applied successfully
 * \endif
 *
 * \if CHINESE
 * @brief 应用所有设定
 * @return 应用成功返回true
 * @note 所有设定有一个应用成功都会返回true
 * \endif
 */
bool SARibbonCustomizeWidget::applys()
{
	simplify();
	if (sa_customize_datas_apply(d_ptr->mCustomizeDatasCache, d_ptr->mRibbonBar) > 0) {
		// 将临时操作存入已应用操作，并清空临时操作
		makeActionsApplied();
		clearCache();
		return true;
	} else {
		return false;
	}
}

/**
 * \if ENGLISH
 * @brief Convert to XML
 * @details This function only writes elements, not document related content.
 *          If you need to write document, call QXmlStreamWriter::writeStartDocument() before this function,
 *          and call QXmlStreamWriter::writeEndDocument() after this function.
 * @note Before passing QXmlStreamWriter, you need to set the encoding to utf-8: xml->setCodec("utf-8");
 * @note Since QXmlStreamWriter does not support encoding when QString is used as io, and this cannot guarantee
 *       that Chinese characters will not appear in the customization process, QXmlStreamWriter should not be
 *       constructed through QString. If you need to use string, you need to construct through QByteArray.
 * @return false if exception occurs, also returns false if there is no customization data
 * @see sa_customize_datas_to_xml
 * \endif
 *
 * \if CHINESE
 * @brief 转换为xml
 * @details 此函数仅会写element，不会写document相关内容，因此如果需要写document，
 *          需要在此函数前调用QXmlStreamWriter::writeStartDocument(),在此函数后调用QXmlStreamWriter::writeEndDocument()
 * @note 注意，在传入QXmlStreamWriter之前，需要设置编码为utf-8:xml->setCodec("utf-8");
 * @note 由于QXmlStreamWriter在QString作为io时，是不支持编码的，而此又无法保证自定义过程不出现中文字符，
 *       因此，QXmlStreamWriter不应该通过QString进行构造，如果需要用到string，也需要通过QByteArray构造
 * @return 如果出现异常，返回false,如果没有自定义数据也会返回false
 * @see sa_customize_datas_to_xml
 * \endif
 */
bool SARibbonCustomizeWidget::toXml(QXmlStreamWriter* xml) const
{
	QList< SARibbonCustomizeData > res = d_ptr->mOldCustomizeDatas;

	if (isApplied())
		res << d_ptr->mCustomizeDatasApplied;
	if (isCached())
		res << d_ptr->mCustomizeDatasCache;

	res = SARibbonCustomizeData::simplify(res);
	return (sa_customize_datas_to_xml(xml, res));
}

/**
 * \if ENGLISH
 * @brief Write configuration to file
 * @param xmlpath XML file path
 * @return true if successful
 * \endif
 *
 * \if CHINESE
 * @brief 把配置写入文件中
 * @param xmlpath XML文件路径
 * @return 是否成功
 * \endif
 */
bool SARibbonCustomizeWidget::toXml(const QString& xmlpath) const
{
	QFile f(xmlpath);

	if (!f.open(QIODevice::ReadWrite | QIODevice::Truncate | QIODevice::Text)) {
		return (false);
	}
	QXmlStreamWriter xml(&f);

	xml.setAutoFormatting(true);
	xml.setAutoFormattingIndent(2);
#if QT_VERSION < QT_VERSION_CHECK(6, 0, 0)  // QXmlStreamWriter always encodes XML in UTF-8.
	xml.setCodec("utf-8");                  // 在writeStartDocument之前指定编码
#endif
	xml.writeStartDocument();
	bool isOK = toXml(&xml);

	xml.writeEndDocument();
	f.close();
	return (isOK);
}

/**
 * \if ENGLISH
 * @brief Load QList<SARibbonCustomizeData> from XML
 * @details For file-based settings, it is recommended to call this function before the dialog is displayed
 *          to ensure correct recording of stacked settings
 * @param xml XML stream reader
 * @note This function should be called after @ref setupActionsManager
 * \endif
 *
 * \if CHINESE
 * @brief 从xml中加载QList<SARibbonCustomizeData>
 * @details 对于基于配置文件的设置，对话框显示前建议调用此函数，保证叠加设置的正确记录
 * @param xml XML流读取器
 * @note 此函数要在@ref setupActionsManager 函数之后调用
 * \endif
 */
void SARibbonCustomizeWidget::fromXml(QXmlStreamReader* xml)
{
	QList< SARibbonCustomizeData > cds = sa_customize_datas_from_xml(xml, d_ptr->mActionMgr);

	d_ptr->mOldCustomizeDatas = cds;
}

/**
 * \if ENGLISH
 * @brief Load QList<SARibbonCustomizeData> from XML file
 * @details For file-based settings, it is recommended to call this function before the dialog is displayed
 *          to ensure correct recording of stacked settings
 * @param xmlpath XML file path
 * @note This function should be called after @ref setupActionsManager
 * @note If the program loads custom configuration after startup, you need to call this function when calling this window
 *       to load the original configuration. The old actions will be saved when generating new actions,
 *       but this loaded action will not be called when applys is called.
 * \endif
 *
 * \if CHINESE
 * @brief 从xml中加载QList<SARibbonCustomizeData>
 * @details 对于基于配置文件的设置，对话框显示前建议调用此函数，保证叠加设置的正确记录
 * @param xmlpath XML文件路径
 * @note 此函数要在@ref setupActionsManager 函数之后调用
 * @note 如果程序启动后加载了自定义配置，再调用此窗口时需要调用此函数，把原来的配置加载进来，
 *       在生成新动作时会把旧动作保存，但在调用applys时不会调用此加载的动作
 * \endif
 */
void SARibbonCustomizeWidget::fromXml(const QString& xmlpath)
{
	QFile f(xmlpath);

	if (!f.open(QIODevice::ReadOnly | QIODevice::Text)) {
		return;
	}
	f.seek(0);
	QXmlStreamReader xml(&f);

	fromXml(&xml);
}

/**
 * \if ENGLISH
 * @brief Apply XML configuration
 * @note Repeating loading a configuration file will cause exceptions. To avoid this, generally use a variable to ensure it is only loaded once.
 * @param xml XML stream reader
 * @param bar SARibbonBar pointer
 * @param mgr SARibbonActionsManager pointer
 * @return Returns true if any setting is applied successfully
 * @see sa_customize_datas_from_xml sa_customize_datas_apply sa_apply_customize_from_xml_file
 * \endif
 *
 * \if CHINESE
 * @brief 应用xml配置
 * @note 重复加载一个配置文件会发生异常，为了避免此类事件发生，一般通过一个变量保证只加载一次
 * @param xml XML流读取器
 * @param bar SARibbonBar指针
 * @param mgr SARibbonActionsManager指针
 * @return 所有设定有一个应用成功都会返回true
 * @see sa_customize_datas_from_xml sa_customize_datas_apply sa_apply_customize_from_xml_file
 * \endif
 */
bool SARibbonCustomizeWidget::fromXml(QXmlStreamReader* xml, SARibbonBar* bar, SARibbonActionsManager* mgr)
{
	// 先找到sa-ribbon-customize标签
	QList< SARibbonCustomizeData > cds = sa_customize_datas_from_xml(xml, mgr);

	return (sa_customize_datas_apply(cds, bar) > 0);
}

/**
 * \if ENGLISH
 * @brief Make actions applied
 * \endif
 *
 * \if CHINESE
 * @brief 清除已应用的动作
 * \endif
 */
void SARibbonCustomizeWidget::makeActionsApplied()
{
	d_ptr->mCustomizeDatasApplied << d_ptr->mCustomizeDatasCache;
}

/**
 * \if ENGLISH
 * @brief Clear applied actions
 * @note Need to clear applied actions after cancel operation
 * \endif
 *
 * \if CHINESE
 * @brief 清除applied的动作
 * @note cancel操作后需要清空已应用的动作
 * \endif
 */
void SARibbonCustomizeWidget::clearApplied()
{
	d_ptr->mCustomizeDatasApplied.clear();
}

/**
 * \if ENGLISH
 * @brief Clear cached actions
 * @note After executing applys function, if you want to continue calling, you should clear, otherwise it will cause exceptions
 * \endif
 *
 * \if CHINESE
 * @brief 清除缓存动作
 * @note 在执行applys函数后，如果要继续调用，应该clear，否则会导致异常
 * \endif
 */
void SARibbonCustomizeWidget::clearCache()
{
	d_ptr->mCustomizeDatasCache.clear();
}

/**
 * \if ENGLISH
 * @brief Clear all actions, excluding locally read data
 * \endif
 *
 * \if CHINESE
 * @brief 清除所有动作，不包含本地读取的数据
 * \endif
 */
void SARibbonCustomizeWidget::clear()
{
	clearApplied();
	clearCache();
}

/**
 * \if ENGLISH
 * @brief Simplify customize data
 * \endif
 *
 * \if CHINESE
 * @brief 精简
 * \endif
 */
void SARibbonCustomizeWidget::simplify()
{
	d_ptr->mCustomizeDatasCache = SARibbonCustomizeData::simplify(d_ptr->mCustomizeDatasCache);
}

/**
 * \if ENGLISH
 * @brief Get the row proportion selected in the current interface
 * @return SARibbonPanelItem::RowProportion
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前界面选中的行属性
 * @return 行比例
 * \endif
 */
SARibbonPanelItem::RowProportion SARibbonCustomizeWidget::selectedRowProportion() const
{
	return (static_cast< SARibbonPanelItem::RowProportion >(ui->comboBoxActionProportion->currentData().toInt()));
}

/**
 * \if ENGLISH
 * @brief Get the action selected in listview
 * @return Returns nullptr if no action is selected
 * @note To get the action selected in treeview, use @ref itemToAction function
 * \endif
 *
 * \if CHINESE
 * @brief 获取listview中选中的action
 * @return 如果没有选中action，返回nullptr
 * @note 如果要获取treeview选中的action，使用@ref itemToAction 函数
 * \endif
 */
QAction* SARibbonCustomizeWidget::selectedAction() const
{
	QItemSelectionModel* m = ui->listViewSelect->selectionModel();

	if ((nullptr == m) || !m->hasSelection()) {
		return (nullptr);
	}
	QModelIndex i = m->currentIndex();

	return (d_ptr->mAcionModel->indexToAction(i));
}

/**
 * \if ENGLISH
 * @brief Convert item to action
 * @param item QStandardItem to convert
 * @return Returns nullptr if no action can be converted
 * \endif
 *
 * \if CHINESE
 * @brief 把item转换为action
 * @param item 要转换的项
 * @return 如果没有action可转换，返回nullptr
 * \endif
 */
QAction* SARibbonCustomizeWidget::itemToAction(QStandardItem* item) const
{
	return (d_ptr->itemToAction(item));
}

/**
 * \if ENGLISH
 * @brief Get the selected item in ribbon tree
 * @return Selected QStandardItem pointer
 * \endif
 *
 * \if CHINESE
 * @brief 获取ribbon tree选中的item
 * @return 选中的项
 * \endif
 */
QStandardItem* SARibbonCustomizeWidget::selectedItem() const
{
	QItemSelectionModel* m = ui->treeViewResult->selectionModel();

	if ((nullptr == m) || !m->hasSelection()) {
		return (nullptr);
	}
	QModelIndex i = m->currentIndex();

	return (d_ptr->mRibbonModel->itemFromIndex(i));
}

/**
 * \if ENGLISH
 * @brief Get the level of selected ribbon tree item
 * @return -1 for abnormal selection, 0 for category selected, 1 for panel selected, 2 for action selected
 * \endif
 *
 * \if CHINESE
 * @brief 获取选中的ribbon tree 的level
 * @return -1为选中异常，0代表选中了category 1代表选中了panel 2代表选中了action
 * \endif
 */
int SARibbonCustomizeWidget::selectedRibbonLevel() const
{
	QStandardItem* item = selectedItem();

	if (item) {
		return (itemLevel(item));
	}
	return (-1);
}

/**
 * \if ENGLISH
 * @brief Get the level of StandardItem
 * @param item QStandardItem to check
 * @return Level of the item
 * \endif
 *
 * \if CHINESE
 * @brief 获取StandardItem 的level
 * @param item 要检查的项
 * @return 项的层级
 * \endif
 */
int SARibbonCustomizeWidget::itemLevel(QStandardItem* item) const
{
	return (d_ptr->itemLevel(item));
}

/**
 * \if ENGLISH
 * @brief Set an item to be selected
 * @param item QStandardItem to select
 * @param ensureVisible Whether to ensure the item is visible
 * \endif
 *
 * \if CHINESE
 * @brief 设置某个item被选中
 * @param item 要选中的项
 * @param ensureVisible 是否确保项可见
 * \endif
 */
void SARibbonCustomizeWidget::setSelectItem(QStandardItem* item, bool ensureVisible)
{
	QItemSelectionModel* m = ui->treeViewResult->selectionModel();

	if (nullptr == m) {
		return;
	}
	if (m) {
		m->clearSelection();
		m->setCurrentIndex(item->index(), QItemSelectionModel::SelectCurrent);
	}
	if (ensureVisible) {
		ui->treeViewResult->scrollTo(item->index());
	}
}

/**
 * \if ENGLISH
 * @brief Check if item can be customized
 * @param item QStandardItem to check
 * @return true if the item can be customized
 * \endif
 *
 * \if CHINESE
 * @brief 判断itemn能否改动，可以改动返回true
 * @param item 要检查的项
 * @return 是否可自定义
 * \endif
 */
bool SARibbonCustomizeWidget::isItemCanCustomize(QStandardItem* item) const
{
	return (d_ptr->isItemCanCustomize(item));
}

bool SARibbonCustomizeWidget::isSelectedItemCanCustomize() const
{
	return (isItemCanCustomize(selectedItem()));
}

/**
 * \if ENGLISH
 * @brief Check if item is a customize item
 * @param item QStandardItem to check
 * @return true if the item is a customize item
 * \endif
 *
 * \if CHINESE
 * @brief 判断itemn能否改动，可以改动返回true
 * @param item 要检查的项
 * @return 是否是自定义项
 * \endif
 */
bool SARibbonCustomizeWidget::isCustomizeItem(QStandardItem* item) const
{
	return (d_ptr->isCustomizeItem(item));
}

bool SARibbonCustomizeWidget::isSelectedItemIsCustomize() const
{
	return (isCustomizeItem(selectedItem()));
}

void SARibbonCustomizeWidget::removeItem(QStandardItem* item)
{
	if (item->parent()) {
		item->parent()->removeRow(item->row());
	} else {
		d_ptr->mRibbonModel->removeRow(item->row());
	}
}

void SARibbonCustomizeWidget::onComboBoxActionIndexCurrentIndexChanged(int index)
{
	int tag = ui->comboBoxActionIndex->itemData(index).toInt();

	d_ptr->mAcionModel->setFilter(tag);
}

void SARibbonCustomizeWidget::onRadioButtonGroupButtonClicked(QAbstractButton* b)
{
	updateModel(b == ui->radioButtonAllCategory ? ShowAllCategory : ShowMainCategory);
}

void SARibbonCustomizeWidget::onPushButtonNewCategoryClicked()
{
	int row                = d_ptr->mRibbonModel->rowCount();
	QItemSelectionModel* m = ui->treeViewResult->selectionModel();

	if (m && m->hasSelection()) {
		QModelIndex i = m->currentIndex();
		while (i.parent().isValid()) {
			i = i.parent();
		}
		// 获取选中的最顶层item
		row = i.row() + 1;
	}
	QStandardItem* ni = new QStandardItem(tr("new category[customize]%1").arg(++(d_ptr->mCustomizeCategoryCount)));

	ni->setData(0, SARibbonCustomizeWidget::LevelRole);
	d_ptr->mRibbonModel->insertRow(row, ni);
	// 设置新增的为选中
	setSelectItem(ni);
	// 把动作插入动作列表中
	SARibbonCustomizeData d = SARibbonCustomizeData::makeAddCategoryCustomizeData(
		ni->text(), ni->row(), SARibbonCustomizeWidget::PrivateData::makeRandomObjName("category"));

	d_ptr->mCustomizeDatasCache.append(d);
	ni->setData(true, SARibbonCustomizeWidget::CanCustomizeRole);  // 有CustomizeRole，必有CanCustomizeRole
	ni->setData(true, SARibbonCustomizeWidget::CustomizeRole);
	ni->setData(d.categoryObjNameValue, SARibbonCustomizeWidget::CustomizeObjNameRole);
}

void SARibbonCustomizeWidget::onPushButtonNewPanelClicked()
{
	QStandardItem* item = selectedItem();

	if (nullptr == item) {
		return;
	}
	int level = selectedRibbonLevel();

	QStandardItem* ni = new QStandardItem(tr("new panel[customize]%1").arg(++(d_ptr->mCustomizePanelCount)));

	ni->setData(1, SARibbonCustomizeWidget::LevelRole);

	if (0 == level) {
		// 说明是category,插入到最后
		item->appendRow(ni);
	} else if (1 == level) {
		// 说明选择的是panel，插入到此panel之后
		QStandardItem* categoryItem = item->parent();
		if (nullptr == categoryItem) {
			return;
		}
		categoryItem->insertRow(item->row() + 1, ni);
	} else {
		// 不符合就删除退出
		delete ni;
		ni = nullptr;
		return;
	}
	// 查找category的object name
	QStandardItem* categoryItem = ni->parent();
	QString categoryObjName     = "";

	categoryObjName         = d_ptr->itemObjectName(categoryItem);
	SARibbonCustomizeData d = SARibbonCustomizeData::makeAddPanelCustomizeData(
		ni->text(), ni->row(), categoryObjName, SARibbonCustomizeWidget::PrivateData::makeRandomObjName("panel"));

	d_ptr->mCustomizeDatasCache.append(d);
	ni->setData(true, SARibbonCustomizeWidget::CanCustomizeRole);  // 有CustomizeRole，必有CanCustomizeRole
	ni->setData(true, SARibbonCustomizeWidget::CustomizeRole);
	ni->setData(d.panelObjNameValue, SARibbonCustomizeWidget::CustomizeObjNameRole);
	setSelectItem(ni);
}

void SARibbonCustomizeWidget::onPushButtonRenameClicked()
{
	QStandardItem* item = selectedItem();

	if (nullptr == item) {
		return;
	}
	bool ok;
	QString text = "";

	text = QInputDialog::getText(this, tr("rename"), tr("name:"), QLineEdit::Normal, item->text(), &ok);

	if (!ok || text.isEmpty()) {
		return;
	}
	int level = itemLevel(item);

	if (0 == level) {
		// 改Category名
		QString cateObjName     = d_ptr->itemObjectName(item);
		SARibbonCustomizeData d = SARibbonCustomizeData::makeRenameCategoryCustomizeData(text, cateObjName);
		d_ptr->mCustomizeDatasCache.append(d);
	} else if (1 == level) {
		QString cateObjName     = d_ptr->itemObjectName(item->parent());
		QString panelObjName    = d_ptr->itemObjectName(item);
		SARibbonCustomizeData d = SARibbonCustomizeData::makeRenamePanelCustomizeData(text, cateObjName, panelObjName);
		d_ptr->mCustomizeDatasCache.append(d);
	} else {
		// action 不允许改名
		return;
	}
	item->setText(text);
}

void SARibbonCustomizeWidget::onPushButtonAddClicked()
{
	QAction* act        = selectedAction();
	QStandardItem* item = selectedItem();

	if ((nullptr == act) || (nullptr == item)) {
		return;
	}
	int level = itemLevel(item);

	if (0 == level) {
		// 选中category不进行操作
		return;
	} else if (2 == level) {
		// 选中action，添加到这个action之后,把item设置为panel
		item = item->parent();
	}
	QString panelObjName    = d_ptr->itemObjectName(item);
	QString categoryObjName = d_ptr->itemObjectName(item->parent());
	QString key             = d_ptr->mActionMgr->key(act);

	SARibbonCustomizeData d = SARibbonCustomizeData::makeAddActionCustomizeData(
		key, d_ptr->mActionMgr, selectedRowProportion(), categoryObjName, panelObjName);

	d_ptr->mCustomizeDatasCache.append(d);

	QStandardItem* actItem = new QStandardItem(act->icon(), act->text());

	actItem->setData(2, SARibbonCustomizeWidget::LevelRole);
	actItem->setData(true, SARibbonCustomizeWidget::CanCustomizeRole);  // 有CustomizeRole，必有CanCustomizeRole
	actItem->setData(true, SARibbonCustomizeWidget::CustomizeRole);
	actItem->setData(act->objectName(), SARibbonCustomizeWidget::CustomizeObjNameRole);
	actItem->setData(qintptr(act), SARibbonCustomizeWidget::PointerRole);  // 把action指针传入
	item->appendRow(actItem);
}

void SARibbonCustomizeWidget::onPushButtonDeleteClicked()
{
	QStandardItem* item = selectedItem();

	if (nullptr == item) {
		return;
	}
	if (!isItemCanCustomize(item)) {
		return;
	}
	int level = itemLevel(item);

	if (0 == level) {
		// 删除category
		SARibbonCustomizeData d = SARibbonCustomizeData::makeRemoveCategoryCustomizeData(d_ptr->itemObjectName(item));
		d_ptr->mCustomizeDatasCache.append(d);
	} else if (1 == level) {
		// 删除panel
		QString catObjName      = d_ptr->itemObjectName(item->parent());
		QString panelObjName    = d_ptr->itemObjectName(item);
		SARibbonCustomizeData d = SARibbonCustomizeData::makeRemovePanelCustomizeData(catObjName, panelObjName);
		d_ptr->mCustomizeDatasCache.append(d);
	} else if (2 == level) {
		// 删除Action
		QString catObjName   = d_ptr->itemObjectName(item->parent()->parent());
		QString panelObjName = d_ptr->itemObjectName(item->parent());
		QAction* act         = itemToAction(item);
		QString key          = d_ptr->mActionMgr->key(act);
		if (key.isEmpty() || catObjName.isEmpty() || panelObjName.isEmpty()) {
			return;
		}

		SARibbonCustomizeData d =
			SARibbonCustomizeData::makeRemoveActionCustomizeData(catObjName, panelObjName, key, d_ptr->mActionMgr);
		d_ptr->mCustomizeDatasCache.append(d);
	}
	// 执行删除操作
	removeItem(item);
	// 删除后重新识别
	ui->pushButtonAdd->setEnabled(selectedAction() && isSelectedItemIsCustomize() && selectedRibbonLevel() > 0);
	ui->pushButtonDelete->setEnabled(isSelectedItemIsCustomize());
}

void SARibbonCustomizeWidget::onListViewSelectClicked(const QModelIndex& index)
{
	// 每次点击，判断是否可以进行操作，决定pushButtonAdd和pushButtonDelete的显示状态
	// 点击了listview，判断treeview的状态
	Q_UNUSED(index);
	ui->pushButtonAdd->setEnabled(isSelectedItemCanCustomize() && selectedRibbonLevel() > 0);
	ui->pushButtonDelete->setEnabled(isSelectedItemCanCustomize());
}

void SARibbonCustomizeWidget::onTreeViewResultClicked(const QModelIndex& index)
{
	Q_UNUSED(index);
	// 每次点击，判断是否可以进行操作，决定pushButtonAdd和pushButtonDelete的显示状态
	QStandardItem* item = selectedItem();

	if (nullptr == item) {
		return;
	}
	int level = itemLevel(item);

	ui->pushButtonAdd->setEnabled(selectedAction() && (level > 0) && isItemCanCustomize(item));
	ui->pushButtonDelete->setEnabled(isItemCanCustomize(item));  // 有CustomizeRole，必有CanCustomizeRole
	ui->pushButtonRename->setEnabled(
		level != 2 || isItemCanCustomize(item));  // QAction 不能改名 ， 有CustomizeRole，必有CanCustomizeRole
}

void SARibbonCustomizeWidget::onToolButtonUpClicked()
{
	QStandardItem* item = selectedItem();

	if ((nullptr == item) || (0 == item->row())) {
		return;
	}
	int level = itemLevel(item);

	if (0 == level) {
		// 移动category
		SARibbonCustomizeData d =
			SARibbonCustomizeData::makeChangeCategoryOrderCustomizeData(d_ptr->itemObjectName(item), -1);
		d_ptr->mCustomizeDatasCache.append(d);
		int r = item->row();
		item  = d_ptr->mRibbonModel->takeItem(r);
		d_ptr->mRibbonModel->removeRow(r);
		d_ptr->mRibbonModel->insertRow(r - 1, item);
	} else if (1 == level) {
		QStandardItem* paritem  = item->parent();
		SARibbonCustomizeData d = SARibbonCustomizeData::makeChangePanelOrderCustomizeData(
			d_ptr->itemObjectName(paritem), d_ptr->itemObjectName(item), -1);
		d_ptr->mCustomizeDatasCache.append(d);
		int r = item->row();
		item  = paritem->takeChild(r);
		paritem->removeRow(r);
		paritem->insertRow(r - 1, item);
	} else if (2 == level) {
		QStandardItem* panelItem    = item->parent();
		QStandardItem* categoryItem = panelItem->parent();
		QAction* act                = itemToAction(item);
		if (!act) {
			return;
		}
		QString key             = d_ptr->mActionMgr->key(act);
		SARibbonCustomizeData d = SARibbonCustomizeData::makeChangeActionOrderCustomizeData(
			d_ptr->itemObjectName(categoryItem), d_ptr->itemObjectName(panelItem), key, d_ptr->mActionMgr, -1);
		d_ptr->mCustomizeDatasCache.append(d);
		int r = item->row();
		item  = panelItem->takeChild(r);
		panelItem->removeRow(r);
		panelItem->insertRow(r - 1, item);
	}

	// 保持焦点，方便连续操作
	setSelectItem(item);
	onTreeViewResultClicked(item->index());
}

void SARibbonCustomizeWidget::onToolButtonDownClicked()
{
	QStandardItem* item = selectedItem();

	if (item == nullptr) {
		return;
	}
	int count = 0;

	if (item->parent()) {
		count = item->parent()->rowCount();
	} else {
		count = d_ptr->mRibbonModel->rowCount();
	}
	if ((nullptr == item) || ((count - 1) == item->row())) {
		return;
	}
	int level = itemLevel(item);

	if (0 == level) {
		// 移动category
		SARibbonCustomizeData d =
			SARibbonCustomizeData::makeChangeCategoryOrderCustomizeData(d_ptr->itemObjectName(item), 1);
		d_ptr->mCustomizeDatasCache.append(d);
		int r = item->row();
		item  = d_ptr->mRibbonModel->takeItem(item->row());
		d_ptr->mRibbonModel->removeRow(r);
		d_ptr->mRibbonModel->insertRow(r + 1, item);
	} else if (1 == level) {
		QStandardItem* paritem  = item->parent();
		SARibbonCustomizeData d = SARibbonCustomizeData::makeChangePanelOrderCustomizeData(
			d_ptr->itemObjectName(paritem), d_ptr->itemObjectName(item), 1);
		d_ptr->mCustomizeDatasCache.append(d);
		int r = item->row();
		item  = paritem->takeChild(r);
		paritem->removeRow(r);
		paritem->insertRow(r + 1, item);
	} else if (2 == level) {
		QStandardItem* panelItem    = item->parent();
		QStandardItem* categoryItem = panelItem->parent();
		QAction* act                = itemToAction(item);
		if (!act) {
			return;
		}
		QString key             = d_ptr->mActionMgr->key(act);
		SARibbonCustomizeData d = SARibbonCustomizeData::makeChangeActionOrderCustomizeData(
			d_ptr->itemObjectName(categoryItem), d_ptr->itemObjectName(panelItem), key, d_ptr->mActionMgr, -1);
		d_ptr->mCustomizeDatasCache.append(d);
		int r = item->row();
		item  = panelItem->takeChild(r);
		panelItem->removeRow(r);
		panelItem->insertRow(r + 1, item);
	}

	// 保持焦点，方便连续操作
	setSelectItem(item);
	onTreeViewResultClicked(item->index());
}

void SARibbonCustomizeWidget::onItemChanged(QStandardItem* item)
{
	if (item == nullptr) {
		return;
	}
	int level = itemLevel(item);

	if (0 == level) {
		if (item->isCheckable()) {
			QString objname = d_ptr->itemObjectName(item);
			SARibbonCustomizeData d =
				SARibbonCustomizeData::makeVisibleCategoryCustomizeData(objname, item->checkState() == Qt::Checked);
			d_ptr->mCustomizeDatasCache.append(d);
		}
	}
}

void SARibbonCustomizeWidget::onLineEditSearchActionTextEdited(const QString& text)
{
	d_ptr->mAcionModel->search(text);
}

void SARibbonCustomizeWidget::onPushButtonResetClicked()
{
	int btn = QMessageBox::warning(this,
								   tr("Warning"),
								   tr("Are you sure reset all customize setting?"),
								   QMessageBox::Yes | QMessageBox::No,
								   QMessageBox::No);

	if (btn == QMessageBox::Yes) {
		clear();
	}
}

/*** End of inlined file: SARibbonCustomizeWidget.cpp ***/


/*** Start of inlined file: SARibbonCustomizeDialog.cpp ***/
#include <QApplication>
#include <QPushButton>
#include <QVBoxLayout>
#include <QSpacerItem>

/**
 * \if ENGLISH
 * @brief UI class for SARibbonCustomizeDialog
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonCustomizeDialogUi 类
 * \endif
 */
class SARibbonCustomizeDialogUi
{
public:
	SARibbonCustomizeWidget* customWidget;
	QVBoxLayout* verticalLayoutMain;
	QHBoxLayout* horizontalLayoutButtonGroup;
	QPushButton* pushButtonCancel;
	QPushButton* pushButtonOk;
	QSpacerItem* spacerItemleft;
	void setupUi(SARibbonMainWindow* ribbonWindow, QWidget* customizeDialog)
	{
		if (customizeDialog->objectName().isEmpty()) {
			customizeDialog->setObjectName(QStringLiteral("SARibbonCustomizeDialog"));
		}
		customizeDialog->resize(800, 600);
		verticalLayoutMain = new QVBoxLayout(customizeDialog);
		verticalLayoutMain->setObjectName(QStringLiteral("verticalLayoutMain"));

		customWidget = new SARibbonCustomizeWidget(ribbonWindow, customizeDialog);
		customWidget->setObjectName(QStringLiteral("customWidget"));
		verticalLayoutMain->addWidget(customWidget);

		horizontalLayoutButtonGroup = new QHBoxLayout();
		horizontalLayoutButtonGroup->setObjectName(QStringLiteral("horizontalLayoutButtonGroup"));

		spacerItemleft = new QSpacerItem(40, 20, QSizePolicy::Expanding, QSizePolicy::Minimum);
		horizontalLayoutButtonGroup->addItem(spacerItemleft);

		pushButtonCancel = new QPushButton(customizeDialog);
		pushButtonCancel->setObjectName(QStringLiteral("pushButtonCancel"));
		horizontalLayoutButtonGroup->addWidget(pushButtonCancel);

		pushButtonOk = new QPushButton(customizeDialog);
		pushButtonOk->setObjectName(QStringLiteral("pushButtonOk"));
		horizontalLayoutButtonGroup->addWidget(pushButtonOk);
		verticalLayoutMain->addItem(horizontalLayoutButtonGroup);
		retranslateUi(customizeDialog);
	}

	void retranslateUi(QWidget* customizeDialog)
	{
		customizeDialog->setWindowTitle(QApplication::translate("SARibbonCustomizeDialog", "Customize Dialog", Q_NULLPTR));
		pushButtonCancel->setText(QApplication::translate("SARibbonCustomizeDialog", "Cancel", Q_NULLPTR));
		pushButtonOk->setText(QApplication::translate("SARibbonCustomizeDialog", "OK", Q_NULLPTR));
	}
};

////////////////////////////////////////////////////////////////////

SARibbonCustomizeDialog::SARibbonCustomizeDialog(SARibbonMainWindow* ribbonWindow, QWidget* p, Qt::WindowFlags f)
	: QDialog(p, f), ui(new SARibbonCustomizeDialogUi)
{
	ui->setupUi(ribbonWindow, this);
	initConnection();
}

/**
 * \if ENGLISH
 * @brief Set the action manager
 * @details Equivalent to @ref SARibbonCustomizeWidget::setupActionsManager
 * @param mgr SARibbonActionsManager pointer
 * \endif
 *
 * \if CHINESE
 * @brief 设置action管理器
 * @details 等同@ref SARibbonCustomizeWidget::setupActionsManager
 * @param mgr SARibbonActionsManager 指针
 * \endif
 */
SARibbonCustomizeDialog::~SARibbonCustomizeDialog()
{
	delete ui;
}
void SARibbonCustomizeDialog::setupActionsManager(SARibbonActionsManager* mgr)
{
	ui->customWidget->setupActionsManager(mgr);
}

/**
 * \if ENGLISH
 * @brief Check if there is content to store, corresponding to save action
 * @return true if there is content to save
 * \endif
 *
 * \if CHINESE
 * @brief 判断用户是否有要存储的内容，对应save动作
 * @return 是否有内容可保存
 * \endif
 */
bool SARibbonCustomizeDialog::isApplied() const
{
	return ui->customWidget->isApplied();
}

/**
 * \if ENGLISH
 * @brief Check if there are modified contents, corresponding to apply action
 * @return true if there are cached changes
 * \endif
 *
 * \if CHINESE
 * @brief 判断用户是否有改动内容，对应apply动作
 * @return 是否有缓存的改动
 * \endif
 */
bool SARibbonCustomizeDialog::isCached() const
{
	return ui->customWidget->isCached();
}

void SARibbonCustomizeDialog::initConnection()
{
	connect(ui->pushButtonOk, &QPushButton::clicked, this, &QDialog::accept);
	connect(ui->pushButtonCancel, &QPushButton::clicked, this, &QDialog::reject);
}

/**
 * \if ENGLISH
 * @brief Equivalent to SARibbonCustomizeWidget::applys
 * @return true if apply succeeded
 * \endif
 *
 * \if CHINESE
 * @brief 等同SARibbonCustomizeWidget::applys
 * @return 应用是否成功
 * \endif
 */
bool SARibbonCustomizeDialog::applys()
{
	return ui->customWidget->applys();
}

/**
 * \if ENGLISH
 * @brief Clear all actions
 * \endif
 *
 * \if CHINESE
 * @brief 清除所有动作
 * \endif
 */
void SARibbonCustomizeDialog::clear()
{
	ui->customWidget->clear();
}

/**
 * \if ENGLISH
 * @brief Convert to XML
 * @param xml XML stream writer
 * @return true if conversion succeeded
 * \endif
 *
 * \if CHINESE
 * @brief 转换为xml
 * @param xml XML 流写入器
 * @return 转换是否成功
 * \endif
 */
bool SARibbonCustomizeDialog::toXml(QXmlStreamWriter* xml) const
{
	return (ui->customWidget->toXml(xml));
}

/**
 * \if ENGLISH
 * @brief Equivalent to SARibbonCustomizeWidget::toXml
 * @param xmlpath XML file path
 * @return true if conversion succeeded
 * \endif
 *
 * \if CHINESE
 * @brief 等同SARibbonCustomizeWidget::toXml
 * @param xmlpath XML 文件路径
 * @return 转换是否成功
 * \endif
 */
bool SARibbonCustomizeDialog::toXml(const QString& xmlpath) const
{
	return (ui->customWidget->toXml(xmlpath));
}

/**
 * \if ENGLISH
 * @brief Equivalent to SARibbonCustomizeWidget::fromXml
 * @param xml XML stream reader
 * \endif
 *
 * \if CHINESE
 * @brief 等同SARibbonCustomizeWidget::fromXml
 * @param xml XML 流读取器
 * \endif
 */
void SARibbonCustomizeDialog::fromXml(QXmlStreamReader* xml)
{
	ui->customWidget->fromXml(xml);
}

/**
 * \if ENGLISH
 * @brief Equivalent to SARibbonCustomizeWidget::fromXml
 * @param xmlpath XML file path
 * \endif
 *
 * \if CHINESE
 * @brief 等同SARibbonCustomizeWidget::fromXml
 * @param xmlpath XML 文件路径
 * \endif
 */
void SARibbonCustomizeDialog::fromXml(const QString& xmlpath)
{
	ui->customWidget->fromXml(xmlpath);
}

/**
 * \if ENGLISH
 * @brief Return the SARibbonCustomizeWidget pointer
 * @details Through SARibbonCustomizeWidget, you can operate more content
 * @return SARibbonCustomizeWidget pointer
 * \endif
 *
 * \if CHINESE
 * @brief 返回SARibbonCustomizeWidget窗口指针
 * @details 通过SARibbonCustomizeWidget窗口可以操作更多的内容
 * @return SARibbonCustomizeWidget指针
 * \endif
 */
SARibbonCustomizeWidget* SARibbonCustomizeDialog::customizeWidget() const
{
	return (ui->customWidget);
}

/*** End of inlined file: SARibbonCustomizeDialog.cpp ***/


/*** Start of inlined file: SARibbonMainWindow.cpp ***/
#include <QApplication>
#include <QDebug>
#include <QFile>
#include <QHash>
#include <QWindowStateChangeEvent>
#include <QScreen>
#include <QTimer>

#if SARIBBON_USE_3RDPARTY_FRAMELESSHELPER
#include <QWKWidgets/widgetwindowagent.h>

#else

#endif

/**
 * @brief The SARibbonMainWindowPrivate class
 */
class SARibbonMainWindow::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonMainWindow)
public:
	PrivateData(SARibbonMainWindow* p);
	void installFrameless(SARibbonMainWindow* p);
	bool isUseRibbonBar() const;
	bool isUseRibbonFrame() const;
	bool isUseNativeFrame() const;
	void checkMainWindowFlag();

public:
	SARibbonMainWindowStyles mRibbonMainWindowStyle;
	SARibbonTheme mCurrentRibbonTheme { SARibbonTheme::RibbonThemeOffice2021Blue };
	SARibbonSystemButtonBar* mWindowButtonGroup { nullptr };
#if SARIBBON_USE_3RDPARTY_FRAMELESSHELPER
	QWK::WidgetWindowAgent* mFramelessHelper { nullptr };
#else
	SAFramelessHelper* mFramelessHelper { nullptr };
#endif
	SARibbonMainWindowEventFilter* mEventFilter { nullptr };
};

SARibbonMainWindow::PrivateData::PrivateData(SARibbonMainWindow* p) : q_ptr(p)
{
}

void SARibbonMainWindow::PrivateData::installFrameless(SARibbonMainWindow* p)
{
#if SARIBBON_USE_3RDPARTY_FRAMELESSHELPER
	mFramelessHelper = new QWK::WidgetWindowAgent(p);
	mFramelessHelper->setup(p);
#else
	mFramelessHelper = new SAFramelessHelper(p);
#endif
}

bool SARibbonMainWindow::PrivateData::isUseRibbonBar() const
{
	return mRibbonMainWindowStyle.testFlag(SARibbonMainWindowStyleFlag::UseRibbonMenuBar);
}

bool SARibbonMainWindow::PrivateData::isUseRibbonFrame() const
{
	return mRibbonMainWindowStyle.testFlag(SARibbonMainWindowStyleFlag::UseRibbonFrame);
}

bool SARibbonMainWindow::PrivateData::isUseNativeFrame() const
{
	return mRibbonMainWindowStyle.testFlag(SARibbonMainWindowStyleFlag::UseNativeFrame);
}

/**
 * @brief 检查flag的设置合理性
 */
void SARibbonMainWindow::PrivateData::checkMainWindowFlag()
{
	// 如果都没有设置边框样式，默认设置为ribbon边框
	if (!mRibbonMainWindowStyle.testFlag(SARibbonMainWindowStyleFlag::UseRibbonFrame)
		&& !mRibbonMainWindowStyle.testFlag(SARibbonMainWindowStyleFlag::UseNativeFrame)) {
		mRibbonMainWindowStyle.setFlag(SARibbonMainWindowStyleFlag::UseRibbonFrame, true);
	}

	// 如果都没有设置MenuBar，默认设置为ribbonbar
	if (!mRibbonMainWindowStyle.testFlag(SARibbonMainWindowStyleFlag::UseRibbonMenuBar)
		&& !mRibbonMainWindowStyle.testFlag(SARibbonMainWindowStyleFlag::UseNativeMenuBar)) {
		mRibbonMainWindowStyle.setFlag(SARibbonMainWindowStyleFlag::UseRibbonMenuBar, true);
	}
}

//===================================================
// SARibbonMainWindow
//===================================================

/**
 * \if ENGLISH
 * @brief Constructs a SARibbonMainWindow instance.
 *
 * This constructor initializes a main window with Ribbon interface style, supporting custom window styles (such as whether to use Ribbon border, menu bar, etc.),
 * and automatically configures window behavior (such as borderless, RibbonBar installation, etc.) according to the style.
 *
 * @param parent Parent widget
 * @param style Window style flags, controlling window appearance and behavior. The following flag combinations are supported:
 *        - @c SARibbonMainWindowStyleFlag::UseRibbonFrame: Use Ribbon custom border (enables borderless window) (enabled by default).
 *        - @c SARibbonMainWindowStyleFlag::UseNativeFrame: Use system native window border.
 *        - @c SARibbonMainWindowStyleFlag::UseRibbonMenuBar: Use Ribbon style menu bar (enabled by default).
 *        - @c SARibbonMainWindowStyleFlag::UseNativeMenuBar: Use system native menu bar (non-ribbon).
 *        Flags can be combined using bitwise OR (|), for example: @c SARibbonMainWindowStyleFlag::UseRibbonFrame | SARibbonMainWindowStyleFlag::UseRibbonMenuBar
 *
 * @param flags Standard Qt window flags
 *
 * @note If @c UseRibbonFrame is enabled, the window will automatically install borderless support.
 *
 * @sa SARibbonMainWindowStyleFlag, setRibbonBar(), ribbonTheme()
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个 SARibbonMainWindow 实例。
 *
 * 此构造函数初始化一个带有 Ribbon 界面风格的主窗口，支持自定义窗口样式（如是否使用 Ribbon 边框、菜单栏等），
 * 并根据样式自动配置窗口行为（如无边框、RibbonBar 安装等）。
 *
 * @param parent 父窗口部件
 * @param style 窗口样式标志，控制窗口外观和行为。支持以下标志组合：
 *        - @c SARibbonMainWindowStyleFlag::UseRibbonFrame：使用Ribbon自定义边框（会启用无边框窗口）（默认启用）。
 *        - @c SARibbonMainWindowStyleFlag::UseNativeFrame：使用系统原生窗口边框。
 *        - @c SARibbonMainWindowStyleFlag::UseRibbonMenuBar：使用 Ribbon 风格菜单栏（默认启用）。
 *        - @c SARibbonMainWindowStyleFlag::UseNativeMenuBar：使用系统原生菜单栏（非ribbon）。
 *        标志可通过位或（|）组合使用，例如：@c SARibbonMainWindowStyleFlag::UseRibbonFrame | SARibbonMainWindowStyleFlag::UseRibbonMenuBar
 *
 * @param flags 标准 Qt 窗口标志
 *
 * @note 如果启用了 @c UseRibbonFrame，则窗口将自动安装无边框支持。
 *
 * @sa SARibbonMainWindowStyleFlag, setRibbonBar(), ribbonTheme()
 * \endif
 */
SARibbonMainWindow::SARibbonMainWindow(QWidget* parent, SARibbonMainWindowStyles style, const Qt::WindowFlags flags)
	: QMainWindow(parent, flags), d_ptr(new SARibbonMainWindow::PrivateData(this))
{
	SA_D(d);
	d->mRibbonMainWindowStyle = style;
	d->checkMainWindowFlag();
	if (d->isUseRibbonBar()) {
		if (d->isUseRibbonFrame()) {
			d->installFrameless(this);
		}
		setRibbonBar(createRibbonBar());
		if (SA::isOperatingSystemInDarkMode()
			&& d->mCurrentRibbonTheme == SARibbonTheme::RibbonThemeOffice2021Blue) {
			d->mCurrentRibbonTheme = SARibbonTheme::RibbonThemeDark;
		}
		QTimer::singleShot(0, this, [this]() {
			SA_D(d);
			SARibbonTheme t = ribbonTheme();
			if (d->mCurrentRibbonTheme == t) {
				SA::applyRibbonTheme(this, ribbonBar(), t);
			} else {
				setRibbonTheme(t);
			}
		});
		setContentsMargins(2, 0, 2, 0);
		if (d->isUseNativeFrame()) {
			// 在ribbon模式下使用本地边框，将隐藏icon，同时默认设置为紧凑模式
			if (SARibbonBar* bar = ribbonBar()) {
				// 隐藏icon
				bar->setTitleIconVisible(false);
				// 设置为紧凑模式
				bar->setRibbonStyle(SARibbonBar::RibbonStyleCompactThreeRow);
			}
		}
	}
	connect(qApp, &QApplication::primaryScreenChanged, this, &SARibbonMainWindow::onPrimaryScreenChanged);
}

/**
 * \if ENGLISH
 * @brief Destructor
 * \endif
 *
 * \if CHINESE
 * @brief 析构函数
 * \endif
 */
SARibbonMainWindow::~SARibbonMainWindow()
{
}

/**
 * \if ENGLISH
 * @brief Returns the ribbonbar, returns nullptr if not using ribbon mode
 * @return Pointer to the SARibbonBar, or nullptr if not using ribbon mode
 * \endif
 *
 * \if CHINESE
 * @brief 返回ribbonbar，如果不是使用ribbon模式，返回nullptr
 * @return 指向SARibbonBar的指针，如果不是使用ribbon模式，返回nullptr
 * \endif
 */
SARibbonBar* SARibbonMainWindow::ribbonBar() const
{
	return qobject_cast< SARibbonBar* >(menuWidget());
}

/**
 * \if ENGLISH
 * @brief Sets the ribbonbar
 * @param ribbon Pointer to the SARibbonBar to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置ribbonbar
 * @param ribbon 要设置的SARibbonBar指针
 * \endif
 */
void SARibbonMainWindow::setRibbonBar(SARibbonBar* ribbon)
{
	Q_ASSERT(ribbon != nullptr);
	QWidget* old = QMainWindow::menuWidget();
	if (old) {
		// 如果之前已经设置了menubar，要把之前的删除
		old->deleteLater();
	}
	QMainWindow::setMenuWidget(ribbon);
	ribbon->setMainWindowStyles(d_ptr->mRibbonMainWindowStyle);
	const int th = ribbon->titleBarHeight();
	// 先提升ribbon，避免被别的窗口覆盖
	ribbon->raise();
	if (d_ptr->isUseRibbonFrame()) {
		// 设置window按钮
		if (nullptr == d_ptr->mWindowButtonGroup) {
			d_ptr->mWindowButtonGroup = RibbonSubElementFactory->createWindowButtonGroup(this);
			d_ptr->mWindowButtonGroup->setObjectName(QStringLiteral("objSARibbonSystemButtonBar"));
			d_ptr->mWindowButtonGroup->setIconSize(QSize(18, 18));
			// SARibbonSystemButtonBar的eventfilter捕获mainwindow的事件
			// 通过eventerfilter来处理mainwindow的事件，避免用户错误的继承resizeEvent导致systembar的位置异常
			installEventFilter(d_ptr->mWindowButtonGroup);
		} else {
			// 避免重复 installEventFilter（Qt 文档：同一 filter 多次 install 会被调用多次）
			removeEventFilter(d_ptr->mWindowButtonGroup);
			installEventFilter(d_ptr->mWindowButtonGroup);
		}

		SARibbonSystemButtonBar* sysBar = d_ptr->mWindowButtonGroup;
		sysBar->setWindowStates(windowState());
		sysBar->setWindowTitleHeight(th);
		// 确保sysbar在最顶层，避免第二次设置ribbonbar的时候，被ribbonbar覆盖了sysbar
		sysBar->raise();
		sysBar->show();

		// 图标
		ribbon->titleIconWidget()->setIcon(windowIcon());
#if SARIBBON_USE_3RDPARTY_FRAMELESSHELPER
		auto helper = d_ptr->mFramelessHelper;
		helper->setTitleBar(ribbon);
		// 以下这些窗口，需要允许点击
		helper->setHitTestVisible(sysBar);                         // IMPORTANT!
		helper->setHitTestVisible(ribbon->ribbonTabBar());         // IMPORTANT!
		helper->setHitTestVisible(ribbon->rightButtonGroup());     // IMPORTANT!
		helper->setHitTestVisible(ribbon->applicationButton());    // IMPORTANT!
		helper->setHitTestVisible(ribbon->quickAccessBar());       // IMPORTANT!
		helper->setHitTestVisible(ribbon->ribbonStackedWidget());  // IMPORTANT!
		helper->setHitTestVisible(ribbon->titleIconWidget());      // IMPORTANT!
#if SARIBBON_ENABLE_SNAP_LAYOUT
		if (sysBar->closeButton()) {
			helper->setSystemButton(QWK::WindowAgentBase::Close, sysBar->closeButton());
		}
		if (sysBar->minimizeButton()) {
			helper->setSystemButton(QWK::WindowAgentBase::Minimize, sysBar->minimizeButton());
		}
		if (sysBar->maximizeButton()) {
			helper->setSystemButton(QWK::WindowAgentBase::Maximize, sysBar->maximizeButton());
		}
#endif
#else
		// 捕获ribbonbar的事件
		ribbon->installEventFilter(this);
		// 设置窗体的标题栏高度
		d_ptr->mFramelessHelper->setTitleHeight(th);
		d_ptr->mFramelessHelper->setRubberBandOnResize(false);
#endif
		// 最后要提升，否则新加入的会被覆盖
		if (d_ptr->mWindowButtonGroup) {
			d_ptr->mWindowButtonGroup->raise();
		}
	}
	if (!d_ptr->mEventFilter) {
		d_ptr->mEventFilter = new SARibbonMainWindowEventFilter(this);
		installEventFilter(d_ptr->mEventFilter);
	}
}

#if SARIBBON_USE_3RDPARTY_FRAMELESSHELPER

/**
 * \if ENGLISH
 * @brief If there are custom windows in the ribbon added to non-clickable areas such as the title bar, and you want them to be clickable, you need to call this interface to inform that they are clickable
 * @param w The widget to set as clickable
 * @param visible Whether the widget should be clickable
 * \endif
 *
 * \if CHINESE
 * @brief 如果ribbon中有自定义的窗口在标题栏等非点击区域加入后，想能点击，需要调用此接口告知可点击
 * @param w 要设置为可点击的窗口部件
 * @param visible 窗口部件是否应该可点击
 * \endif
 */
void SARibbonMainWindow::setFramelessHitTestVisible(QWidget* w, bool visible)
{
	auto helper = d_ptr->mFramelessHelper;
	helper->setHitTestVisible(const_cast< QWidget* >(w), visible);
}
#else

/**
 * \if ENGLISH
 * @brief Borderless helper object
 * @return Pointer to the SAFramelessHelper
 * \endif
 *
 * \if CHINESE
 * @brief 无边框辅助对象
 * @return 指向SAFramelessHelper的指针
 * \endif
 */
SAFramelessHelper* SARibbonMainWindow::framelessHelper() const
{
	return (d_ptr->mFramelessHelper);
}

/**
 * \if ENGLISH
 * @brief Sets whether to enable "rubber band" indication mode during scaling.
 *
 * When enabled, the window does not immediately redraw its content during drag scaling, but first uses a semi-transparent rectangle (rubber band)
 * to display the target size, and only completes the resize once the user releases the mouse. This mode can significantly reduce
 * CPU/GPU consumption during high-frequency resize operations for heavy-load applications such as CAD, 3D rendering, and large charts.
 *
 * @param on  true  Enable rubber band scaling indication;
 *             false Disable rubber band, use real-time redrawing (default behavior).
 *
 * @see isRubberBandOnResize(), SAFramelessHelper::setRubberBandOnResize()
 * \endif
 *
 * \if CHINESE
 * @brief 设置在缩放时是否启用“橡皮筋”示意模式。
 *
 * 当启用时，窗口在拖拽缩放过程中不会立即重绘内容，而是先用一个半透明矩形框（橡皮筋）
 * 显示目标尺寸，待用户释放鼠标后才一次性完成resize。此模式可显著降低
 * CAD、三维渲染、大型图表等重负载应用在高频resize时的CPU/GPU消耗。
 *
 * @param on  true  启用橡皮筋缩放示意；
 *             false 禁用橡皮筋，采用实时重绘（默认行为）。
 *
 * @see isRubberBandOnResize(), SAFramelessHelper::setRubberBandOnResize()
 * \endif
 */
void SARibbonMainWindow::setRubberBandOnResize(bool on)
{
	if (SAFramelessHelper* fl = framelessHelper()) {
		fl->setRubberBandOnResize(on);
	}
}

/**
 * \if ENGLISH
 * @brief Returns whether the rubber band scaling indication mode is currently enabled.
 *
 * @return true  Rubber band mode is enabled;
 *         false Rubber band mode is not enabled
 *
 * @see setRubberBandOnResize(), SAFramelessHelper::rubberBandOnResize()
 * \endif
 *
 * \if CHINESE
 * @brief 返回当前是否启用了橡皮筋缩放示意模式。
 *
 * @return true  橡皮筋模式已启用；
 *         false 橡皮筋模式未启用
 *
 * @see setRubberBandOnResize(), SAFramelessHelper::rubberBandOnResize()
 * \endif
 */
bool SARibbonMainWindow::isRubberBandOnResize() const
{
	if (SAFramelessHelper* fl = framelessHelper()) {
		return fl->rubberBandOnResisze();
	}
	return false;
}
#endif

/**
 * \if ENGLISH
 * @brief Event filter for handling events from the ribbon bar
 *
 * This event filter is used to pass events from the ribbon bar to the main window and then to the frameless helper.
 * Since the ribbon bar may cover the frameless area, causing the frameless helper to fail to capture these messages,
 * it is necessary to call ribbonBar()->installEventFilter(this).
 *
 * @param obj The object that is sending the event
 * @param e The event being sent
 * @return true if the event was handled, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 用于处理来自ribbon bar的事件的事件过滤器
 *
 * 此事件过滤器用于将ribbon bar上的事件传递到mainwindow，再传递到frameless helper。
 * 由于ribbonbar会遮挡掉frameless的区域，导致frameless无法捕获这些消息，
 * 因此必须调用ribbonBar()->installEventFilter(this)。
 *
 * @param obj 发送事件的对象
 * @param e 正在发送的事件
 * @return 如果事件已处理则返回true，否则返回false
 * \endif
 */
bool SARibbonMainWindow::eventFilter(QObject* obj, QEvent* e)
{
#if SARIBBON_USE_3RDPARTY_FRAMELESSHELPER
#else
	// 这个过滤是为了把ribbonBar上的动作传递到mainwindow，再传递到frameless，
	// 由于ribbonbar会遮挡掉frameless的区域，导致frameless无法捕获这些消息
	// 因此必须ribbonBar()->installEventFilter(this);

	if (obj == ribbonBar()) {
		switch (e->type()) {
		case QEvent::MouseButtonPress:
		case QEvent::MouseButtonRelease:
		case QEvent::MouseMove:
		case QEvent::Leave:
		case QEvent::HoverMove:
		case QEvent::MouseButtonDblClick: {
			QApplication::sendEvent(this, e);
		} break;
		default:
			break;
		}
	}
#endif
	return (QMainWindow::eventFilter(obj, e));
}

/**
 * \if ENGLISH
 * @brief Gets the system button group, you can add other buttons based on this
 * @return Pointer to the SARibbonSystemButtonBar
 * \endif
 *
 * \if CHINESE
 * @brief 获取系统按钮组，可以在此基础上添加其他按钮
 * @return 指向SARibbonSystemButtonBar的指针
 * \endif
 */
SARibbonSystemButtonBar* SARibbonMainWindow::windowButtonBar() const
{
	return d_ptr->mWindowButtonGroup;
}

/**
 * \if ENGLISH
 * @brief Gets the current style of the ribbon main window
 * @return The current SARibbonMainWindowStyles
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前ribbonMainWidow的样式
 * @return 当前的SARibbonMainWindowStyles
 * \endif
 */
SARibbonMainWindowStyles SARibbonMainWindow::ribbonMainwindowStyle() const
{
	return d_ptr->mRibbonMainWindowStyle;
}

/**
 * \if ENGLISH
 * @brief This function is only used to control the display of minimize, maximize, and close buttons
 * @param flags The window flags to update
 * \endif
 *
 * \if CHINESE
 * @brief 此函数仅用于控制最小最大化和关闭按钮的显示
 * @param flags 要更新的窗口标志
 * \endif
 */
void SARibbonMainWindow::updateWindowFlag(Qt::WindowFlags flags)
{
	if (d_ptr->isUseRibbonFrame()) {
		if (SARibbonSystemButtonBar* g = d_ptr->mWindowButtonGroup) {
			g->updateWindowFlag(flags);
		}
	}
}

/**
 * \if ENGLISH
 * @brief Sets the ribbon theme
 *
 * Note that in some versions of Qt, setting the theme in the constructor may not take full effect. You can use QTimer to put it at the end of the queue, like:
 * @code
 * QTimer::singleShot(0, this, [ this ]() { this->setRibbonTheme(SARibbonMainWindow::RibbonThemeDark); });
 * @endcode
 *
 * @param theme The theme to set
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonMainWindow::setRibbonTheme
 *
 * 注意某些版本的qt，在构造函数设置主题会不完全生效，可以使用QTimer投放到队列最后执行，如：
 * @code
 * QTimer::singleShot(0, this, [ this ]() { this->setRibbonTheme(SARibbonMainWindow::RibbonThemeDark); });
 * @endcode
 *
 * @param theme 要设置的主题
 * \endif
 */
void SARibbonMainWindow::setRibbonTheme(SARibbonTheme theme)
{
	if (d_ptr->mCurrentRibbonTheme != theme) {
		d_ptr->mCurrentRibbonTheme = theme;
		SA::applyRibbonTheme(this, ribbonBar(), theme);
		Q_EMIT ribbonThemeChanged(theme);
	}
}

/**
 * \if ENGLISH
 * @brief Gets the current ribbon theme
 * @return The current SARibbonTheme
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前的ribbon主题
 * @return 当前的SARibbonTheme
 * \endif
 */
SARibbonTheme SARibbonMainWindow::ribbonTheme() const
{
	return (d_ptr->mCurrentRibbonTheme);
}

/**
 * \if ENGLISH
 * @brief Checks if ribbon is being used
 * @return true if ribbon is being used, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查是否使用ribbon
 * @return 如果使用ribbon则返回true，否则返回false
 * \endif
 */
bool SARibbonMainWindow::isUseRibbon() const
{
	return (nullptr != ribbonBar());
}

/**
 * \if ENGLISH
 * @brief Factory function for creating ribbon bar
 *
 * If the user has overridden SARibbonBar, they can return their own Ribbon instance by overriding this virtual function
 * @return Pointer to the created SARibbonBar
 * \endif
 *
 * \if CHINESE
 * @brief 创建ribbonbar的工厂函数
 *
 * 用户如果重写了SARibbonBar，可以通过重新此虚函数返回自己的Ribbon实例
 * @return 指向创建的SARibbonBar的指针
 * \endif
 */
SARibbonBar* SARibbonMainWindow::createRibbonBar()
{
	SARibbonBar* bar = RibbonSubElementFactory->createRibbonBar(this);
	return bar;
}

/**
 * \if ENGLISH
 * @brief Signal triggered when the primary screen changes
 * @param screen The new primary screen
 * \endif
 *
 * \if CHINESE
 * @brief 主屏幕切换触发的信号
 * @param screen 新的主屏幕
 * \endif
 */
void SARibbonMainWindow::onPrimaryScreenChanged(QScreen* screen)
{
	Q_UNUSED(screen);
	// 主屏幕切换后，从新计算所有尺寸
	if (SARibbonBar* bar = ribbonBar()) {
		qDebug() << "Primary Screen Changed";
		bar->updateRibbonGeometry();
	}
}

//----------------------------------------------------
// SARibbonMainWindowEventFilter
//----------------------------------------------------
SARibbonMainWindowEventFilter::SARibbonMainWindowEventFilter(QObject* par) : QObject(par)
{
}

SARibbonMainWindowEventFilter::~SARibbonMainWindowEventFilter()
{
}

bool SARibbonMainWindowEventFilter::eventFilter(QObject* obj, QEvent* e)
{
	if (e && obj) {
		if (e->type() == QEvent::Resize) {
			if (SARibbonMainWindow* m = qobject_cast< SARibbonMainWindow* >(obj)) {
				if (SARibbonBar* ribbon = m->ribbonBar()) {
					QMargins mg = m->contentsMargins();
					ribbon->setFixedWidth(m->size().width() - mg.left() - mg.right());
				}
			}
		}
	}
	return QObject::eventFilter(obj, e);
}

/*** End of inlined file: SARibbonMainWindow.cpp ***/


/*** Start of inlined file: SARibbonWidget.cpp ***/
#include <QApplication>
#include <QDebug>
#include <QFile>
#include <QScreen>

#include <QTimer>
/**
 * @brief The SARibbonWidget::PrivateData class
 */
class SARibbonWidget::PrivateData
{
	SA_RIBBON_DECLARE_PUBLIC(SARibbonWidget)
public:
	PrivateData(SARibbonWidget* p);
	void installFrameless(SARibbonWidget* p);

public:
	SARibbonTheme mCurrentRibbonTheme { SARibbonTheme::RibbonThemeOffice2021Blue };
};

SARibbonWidget::PrivateData::PrivateData(SARibbonWidget* p) : q_ptr(p)
{
}

//===================================================
// SARibbonWidget
//===================================================
/**
 * \if ENGLISH
 * @brief Constructs a SARibbonWidget instance
 * @param parent Parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 构造一个 SARibbonWidget 实例
 * @param parent 父窗口部件
 * \endif
 */
SARibbonWidget::SARibbonWidget(QWidget* parent) : QWidget(parent), d_ptr(new SARibbonWidget::PrivateData(this))
{
	// 直接创建SARibbonBar
	QVBoxLayout* verticalLayout = new QVBoxLayout(this);
	verticalLayout->setSpacing(1);
	verticalLayout->setObjectName("verticalLayout");
	verticalLayout->setContentsMargins(0, 0, 0, 0);
	SARibbonBar* ribbon = new SARibbonBar(this);
	setRibbonBar(ribbon);
	connect(qApp, &QApplication::primaryScreenChanged, this, &SARibbonWidget::onPrimaryScreenChanged);
	SA_D(d);
	if (SA::isOperatingSystemInDarkMode()
		&& d->mCurrentRibbonTheme == SARibbonTheme::RibbonThemeOffice2021Blue) {
		d->mCurrentRibbonTheme = SARibbonTheme::RibbonThemeDark;
	}
	QTimer::singleShot(0, this, [this]() {
		SA_D(d);
		SARibbonTheme t = ribbonTheme();
		if (d->mCurrentRibbonTheme == t) {
			SA::applyRibbonTheme(this, ribbonBar(), t);
		} else {
			setRibbonTheme(t);
		}
	});
}

/**
 * \if ENGLISH
 * @brief Destructor
 * \endif
 *
 * \if CHINESE
 * @brief 析构函数
 * \endif
 */
SARibbonWidget::~SARibbonWidget()
{
}

/**
 * \if ENGLISH
 * @brief Returns the ribbonbar, returns nullptr if not using ribbon mode
 * @return Pointer to the SARibbonBar, or nullptr if not using ribbon mode
 * \endif
 *
 * \if CHINESE
 * @brief 返回ribbonbar，如果不是使用ribbon模式，返回nullptr
 * @return 指向SARibbonBar的指针，如果不是使用ribbon模式，返回nullptr
 * \endif
 */
SARibbonBar* SARibbonWidget::ribbonBar() const
{
	QLayout* lay = layout();
	if (lay) {
		return qobject_cast< SARibbonBar* >(lay->menuBar());
	}
	return nullptr;
}

/**
 * \if ENGLISH
 * @brief Sets the ribbonbar
 * @param bar Pointer to the SARibbonBar to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置ribbonbar
 * @param bar 要设置的SARibbonBar指针
 * \endif
 */
void SARibbonWidget::setRibbonBar(SARibbonBar* bar)
{
	QLayout* lay = layout();
	if (lay) {
		lay->setMenuBar(bar);
	}
}

/**
 * \if ENGLISH
 * @brief Sets the ribbon theme
 *
 * Note that setting the theme in the constructor may not take full effect. Use QTimer to put it at the end of the queue:
 * @code
 * QTimer::singleShot(0, this, [ this ]() { this->setRibbonTheme(SARibbonMainWindow::RibbonThemeDark); });
 * @endcode
 * @param theme The theme to set
 * \endif
 *
 * \if CHINESE
 * @brief 设置ribbon主题
 *
 * 注意主题在构造函数设置主题会不完全生效，使用QTimer投放到队列最后执行即可
 * @code
 * QTimer::singleShot(0, this, [ this ]() { this->setRibbonTheme(SARibbonMainWindow::RibbonThemeDark); });
 * @endcode
 * @param theme 要设置的主题
 * \endif
 */
void SARibbonWidget::setRibbonTheme(SARibbonTheme theme)
{
	if (d_ptr->mCurrentRibbonTheme != theme) {
		d_ptr->mCurrentRibbonTheme = theme;
		SA::applyRibbonTheme(this, ribbonBar(), theme);
		Q_EMIT ribbonThemeChanged(theme);
	}
}

/**
 * \if ENGLISH
 * @brief Gets the current ribbon theme
 * @return The current SARibbonTheme
 * \endif
 *
 * \if CHINESE
 * @brief 获取当前的ribbon主题
 * @return 当前的SARibbonTheme
 * \endif
 */
SARibbonTheme SARibbonWidget::ribbonTheme() const
{
	return (d_ptr->mCurrentRibbonTheme);
}

/**
 * \if ENGLISH
 * @brief Checks if ribbon is being used
 * @return true if ribbon is being used, false otherwise
 * \endif
 *
 * \if CHINESE
 * @brief 检查是否使用ribbon
 * @return 如果使用ribbon则返回true，否则返回false
 * \endif
 */
bool SARibbonWidget::isUseRibbon() const
{
	return (nullptr != ribbonBar());
}

/**
 * \if ENGLISH
 * @brief Sets the widget
 * @param w Widget pointer
 * @note The ownership of the widget is managed by SARibbonWidget
 * @sa widget
 * @note The previously set widget will be deleted
 * \endif
 *
 * \if CHINESE
 * @brief 设置窗口
 * @param w 窗口指针
 * @note 窗口的所有权归SARibbonWidget管理
 * @sa widget
 * @note 原来设置的窗口会被delete
 * \endif
 */
void SARibbonWidget::setWidget(QWidget* w)
{
	QWidget* oldwidget = takeWidget();
	if (oldwidget) {
		oldwidget->hide();
		oldwidget->deleteLater();
	}
	if (QLayout* lay = layout()) {
		lay->addWidget(w);
	}
}

/**
 * \if ENGLISH
 * @brief Gets the set widget
 * @return The widget, or nullptr if none is set
 * \endif
 *
 * \if CHINESE
 * @brief 获取设置的窗口
 * @return 如果没有，返回nullptr
 * \endif
 */
QWidget* SARibbonWidget::widget() const
{
	QLayout* lay = layout();
	if (lay) {
		if (lay->count() == 0) {
			return nullptr;
		}
		return lay->itemAt(0)->widget();
	}
	return nullptr;
}

/**
 * \if ENGLISH
 * @brief Takes the widget from the layout
 * @return The widget that was taken, or nullptr if no widget was found
 * \endif
 *
 * \if CHINESE
 * @brief 从布局中取出窗口
 * @return 被取出的窗口，如果没有找到则返回nullptr
 * \endif
 */
QWidget* SARibbonWidget::takeWidget()
{
	QLayout* lay = layout();
	if (nullptr == lay) {
		return nullptr;
	}
	if (lay->count() == 0) {
		return nullptr;
	}
	auto item = lay->itemAt(0);
	if (nullptr == item) {
		return nullptr;
	}
	lay->removeItem(item);
	auto w = item->widget();
	delete item;
	if (!w) {
		return nullptr;
	}
	return w;
}

/**
 * \if ENGLISH
 * @brief Signal triggered when the primary screen changes
 * @param screen The new primary screen
 * \endif
 *
 * \if CHINESE
 * @brief 主屏幕切换触发的信号
 * @param screen 新的主屏幕
 * \endif
 */
void SARibbonWidget::onPrimaryScreenChanged(QScreen* screen)
{
	Q_UNUSED(screen);
	// 主屏幕切换后，从新计算所有尺寸
	if (SARibbonBar* bar = ribbonBar()) {
		qDebug() << "Primary Screen Changed";
		bar->updateRibbonGeometry();
	}
}

/*** End of inlined file: SARibbonWidget.cpp ***/


/*** Start of inlined file: SARibbonApplicationWidget.cpp ***/
#include <QEvent>
#include <QKeyEvent>
#include <QResizeEvent>
#include <QDebug>
#include <QPainter>

/**
 * \if ENGLISH
 * @brief Constructor for SARibbonApplicationWidget
 * @param parent The parent SARibbonMainWindow
 * @details Creates a frameless widget that follows the parent window's size
 * \endif
 *
 * \if CHINESE
 * @brief SARibbonApplicationWidget 构造函数
 * @param parent 父窗口 SARibbonMainWindow
 * @details 创建一个无框窗口，跟随父窗口大小变化
 * \endif
 */
SARibbonApplicationWidget::SARibbonApplicationWidget(SARibbonMainWindow* parent) : QFrame(parent)
{
	setWindowFlags(windowFlags() | Qt::FramelessWindowHint);  // 去除边框
	parent->installEventFilter(this);
	if (parent) {
		setGeometry(0, 0, parent->width(), parent->height());
	} else {
		setGeometry(0, 0, 300, 300);
	}
}

/**
 * \if ENGLISH
 * @brief Resizes the widget to match the parent's size
 * @param parentSize The size of the parent widget
 * \endif
 *
 * \if CHINESE
 * @brief 将窗口大小调整为父窗口大小
 * @param parentSize 父窗口的大小
 * \endif
 */
void SARibbonApplicationWidget::resizeToParent(const QSize& parentSize)
{
	setGeometry(0, 0, parentSize.width(), parentSize.height());
}

/**
 * \if ENGLISH
 * @brief Event filter to handle parent window events
 * @param obj The object that received the event
 * @param ev The event
 * @return True if the event was handled, false otherwise
 * @details Handles resize events to keep the widget in sync with parent size
 * \endif
 *
 * \if CHINESE
 * @brief 事件过滤器，处理父窗口事件
 * @param obj 接收事件的对象
 * @param ev 事件
 * @return 如果事件被处理返回true，否则返回false
 * @details 处理 resize 事件，保持窗口与父窗口大小同步
 * \endif
 */
bool SARibbonApplicationWidget::eventFilter(QObject* obj, QEvent* ev)
{
	if (obj && ev && (obj == parent())) {
		switch (ev->type()) {
		case QEvent::Resize: {
			// 跟随父窗口的尺寸变化
			resizeToParent(static_cast< QResizeEvent* >(ev)->size());
			break;
		}
		default:
			break;
		}
	}
	return QFrame::eventFilter(obj, ev);
}

/**
 * \if ENGLISH
 * @brief Handles the show event
 * @param event The show event
 * @details Resizes the widget to match parent size and requests focus
 * \endif
 *
 * \if CHINESE
 * @brief 处理显示事件
 * @param event 显示事件
 * @details 将窗口大小调整为父窗口大小并请求焦点
 * \endif
 */
void SARibbonApplicationWidget::showEvent(QShowEvent* event)
{
	QWidget* par = parentWidget();
	if (par) {
		resizeToParent(par->size());
	}
	setFocus();  // 显示窗口时请求焦点
	QFrame::showEvent(event);
}

/**
 * \if ENGLISH
 * @brief Handles key press events
 * @param ev The key event
 * @details Hides the widget when Escape key is pressed
 * \endif
 *
 * \if CHINESE
 * @brief 处理按键事件
 * @param ev 按键事件
 * @details 当按下 Escape 键时隐藏窗口
 * \endif
 */
void SARibbonApplicationWidget::keyPressEvent(QKeyEvent* ev)
{
	if (ev) {
		if (ev->key() == Qt::Key_Escape) {
			hide();
			qDebug() << "Key_Escape";
			ev->accept();
		}
	}
	return QFrame::keyPressEvent(ev);
}

/*** End of inlined file: SARibbonApplicationWidget.cpp ***/

#ifdef _MSC_VER
#pragma warning (pop)
#pragma pop_macro ("_CRT_SECURE_NO_WARNINGS")
#endif

